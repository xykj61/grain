# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Season - opened `20260729`

*Rows %1 through %172 are folded, and their pointers are gathered in [`archive/REDS-fold-index-rows-1-172.md`](archive/REDS-fold-index-rows-1-172.md) -- one line per fold, every path resolvable.*

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 181 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 161** -- counted from the ledger and its archives on `20260824.045445` by `tools/gen/season/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. Rows 1-24, 25-57, 58-73, 74-79, 80-87, 88-95, 96-100, 101-107, 108-109, 110-111, 112, 113-114, 115-116, 117-118, 119-120, and 122, 123, 124, and 129-130, 131-132, 133-134, and 135-136, 148-149, and 150-151, 155-156, 157-158, 159, 160, 161, and 162 folded to `archive/` -- the last three on `20260821.232733`, when the living pin reached its 24,576-byte bound and the ledger's own guard said so, the elder closures for 81, 83, 84, 98 folded on `20260822.043742` ([`archive/REDS-elder-closures-rows-81-98.md`](archive/REDS-elder-closures-rows-81-98.md)) when the pin passed its bound again as row 129 was booked, the line-citation and spelled-number rows 142 and 143 folded on `20260822.194931` ([`archive/REDS-line-citation-and-spelled-number-rows-142-143.md`](archive/REDS-line-citation-and-spelled-number-rows-142-143.md)) when row %145 pushed the pin past its bound again, and the closed prose notes for 73, 74, 75, 78, 80 folded beside them on `20260819` ([`archive/REDS-closed-prose-notes-73-80.md`](archive/REDS-closed-prose-notes-73-80.md)). Every number from 1 to 181 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*Row 170 folded to [`archive/REDS-the-prevention-off-the-roster-rows-170.md`](archive/REDS-the-prevention-off-the-roster-rows-170.md) on `20260823.152322` -- REDS %83's own ASCII prevention guard standing off the roster while the card it protects carried four non-ASCII lines, CLOSED, moved here on the lap it was booked. Its lesson is carried forward by row %171, which measures the same coverage gap at a wider scale.*

*Row 171 folded to [`archive/REDS-the-sample-nobody-drew-rows-171.md`](archive/REDS-the-sample-nobody-drew-rows-171.md) on `20260823.171000`, **CLOSED** -- the random draw of 86 witnesses across 29 rooms, and the two rungs it left open, both now closed by row %173. Moved here as %173 carried the living pin past its 24,576-byte bound. **Living rows 172 onward below.***

*Row 172 folded to [`archive/REDS-the-roster-that-made-itself-red-rows-172.md`](archive/REDS-the-roster-that-made-itself-red-rows-172.md) on `20260823.174500`, **CLOSED** -- running the roster manufactured the artifact the next run reported, and the scan asks `git check-ignore` now rather than guessing which rooms are generated. Moved here as row %174 carried the living pin past its bound. **Living rows 173 onward below.***

*Row 173 folded to [`archive/REDS-the-machine-with-no-screen-rows-173.md`](archive/REDS-the-machine-with-no-screen-rows-173.md) on `20260823.174500`, **CLOSED** on the lap it was booked -- a Wayland application on a headless pier reading as a broken tree, and the shared `display_seam_probe.sh` six callers now gate on. Moved here as row %174 carried the living pin past its bound. **Living row 174 below.***

*Row 174 folded to [`archive/REDS-the-roster-ran-on-one-tree-rows-174.md`](archive/REDS-the-roster-ran-on-one-tree-rows-174.md) on `20260823.204456`, **CLOSED** -- the build cache that reached main because the roster ran before `git add -A`, and the rule it gave its final shape: stage first, then measure, then read what you staged. Moved here as row %175 carried the living pin past its bound. **Living rows 175 onward below.***

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Row 175 folded to [`archive/REDS-the-sprig-that-was-optional-rows-175.md`](archive/REDS-the-sprig-that-was-optional-rows-175.md) on `20260823.212601`, **CLOSED** -- the canonical dated test required a sprig the session-logs law makes optional, so 237 logs read as living and sat in the repointer's write set. **Eight read-only scans still carry the narrow pattern**, named there and waiting on their own lap. **Living row 176 below.***

*Row 176 folded to [`archive/REDS-the-mirror-that-matched-perfectly-rows-176.md`](archive/REDS-the-mirror-that-matched-perfectly-rows-176.md) -- CLOSED.*

**REDS %177 (`20260824.025704`) -- the tree printed a command that bash cannot run.** *What went wrong:* `tools/l/launch-claude-season.rish` prints the unattended-loop recipe -- long single-quoted shell prompts a hand copies into an outer terminal. Inside a single-quoted shell string a literal apostrophe has to be written `''`, and **one of them was written as a lone `'`**, in the words *the linker's own search path*, added by the lap that seated the ask-the-system discipline. A lone apostrophe closes the string, so every character after it falls out of quoting, and the first later parenthesis -- `(round-set, quest, journey, equinox, season)` -- reached bash as syntax. Both printed launch lines died with ``syntax error near unexpected token `('``. **The season loop could not start at all**, and the paste looked at fault when the tree was. *What caught it:* Keaton pasted the line and read the error, then asked whether it was fixed -- and the root was found by reproducing the parse rather than reasoning about the text: writing the pasted command to a file, `bash -n` on it, then collapsing one `''` to `'` and watching the identical error appear. *What it taught:* **a string a tool prints for a hand to run is code, and until something parses it, it is untested code.** Every guard in the roster read files the tree executes; nothing read the one thing a person was told to execute. The class is wider than quoting: any generated command line -- install lines on the operator card, a recipe, a copyable one-liner -- carries it. *Repaired:* both `say` lines escaped to `linker''s`, and all six printed runnable lines now parse. Prevention seated as the 36th standing guard: [`tools/l/loop_prompt_parse_witness.rish`](../tools/l/loop_prompt_parse_witness.rish) over [`tools/fixtures/loop_prompt_parse_scan.sh`](../tools/fixtures/loop_prompt_parse_scan.sh), which runs the recipe tool and `bash -n`s every printed line the reader is told to run, gated at zero, with narration left free. Proven both ways by [`tools/fixtures/loop_prompt_parse_control.sh`](../tools/fixtures/loop_prompt_parse_control.sh) -- eight cases in a temporary pen: the lone apostrophe refused and its line named, a correctly doubled one passed, prose apostrophes left alone, the outer loop's own line measured beside the jail elder, and an empty or absent printout told apart from a failing one. CLOSED.

**REDS %178 (`20260824.035828`) -- the open remainder was itself counted from memory.** *What went wrong:* REDS %175 found the tree's dated/living test requiring an underscore after the stamp where the session-logs law makes the sprig optional, repaired the canonical seam and the repointer's write set, and named **eight** read-only scans still carrying the narrow pattern. A grep for the three spellings this tree writes stamps in found **nineteen** sites across **fifteen** files -- the eight named, and eleven nobody had counted: `tools/ad/ad_walk_sample.sh`, its witness, `tools/d/dated_amendment_audit.sh`, `tools/fixtures/dated_guard_scan.sh`, three more sites inside `dated_path_repoint_scan.sh` beside the one already repaired, `oldness_census_scan.sh`, `radiant_lint_scan.sh`, and two in `shipped_binary_claim_scan.sh`. **The row that recorded a fault about working from memory named its own remainder from memory.** Two of the eleven carry a consequence past a miscount: `dated_guard_scan.sh` is the guard that refuses edits to dated artifacts, so the 237 sprigless logs stood **outside the protection it exists to give**; and the three repointer sites mean its map, its candidate filter, and its line matcher could none of them see a sprigless reference, so those logs were safe from being wrongly written and invisible to being rightly repointed. *What caught it:* writing the loom. Building a guard for the class required enumerating the class, and the enumeration disagreed with the row. *What it taught:* **a named remainder is a measurement and belongs to a script, not to a sentence.** Widening the right side then surfaced a looseness on the LEFT that had stood the whole time -- the census reference pattern had no boundary, so a stamp could match inside a longer filename, and the retired countdown-prefix names (`99991_20260619-090912.md`) are exactly that shape. 24 such substrings read as lost references. *Repaired:* all nineteen sites widened to `[_.]`, `(_|\.)`, or `(_<sprig>)?\.`; a PCRE lookbehind seats the boundary at the census's three reference greps; `tools/d/dated_path_resolve.rish` reads the stamp before the extension as well as before the sprig, so the fold rule now fires for a sprigless log, and gains a **by-stamp reading** bounded to sprigless references, so an elder name a collision split reports every file it produced rather than `missing`. Measured after: the census sees **239 genuine references it was blind to**, and `refs_lost` stands at **187**, exactly where it stood -- a wider and tighter meter reporting the same number. *Prevention* seated as the **37th** standing guard: [`../tools/d/dated_spelling_witness.rish`](../tools/d/dated_spelling_witness.rish) over [`../tools/fixtures/dated_spelling_scan.sh`](../tools/fixtures/dated_spelling_scan.sh), gating 4,705 living tracked sources at zero, proven both ways by [`../tools/fixtures/dated_spelling_control.sh`](../tools/fixtures/dated_spelling_control.sh) -- twelve behaviors on real git repositories in a throwaway pen, three narrow spellings bitten and nine honest readings left free, including a **sprigless** dated basename read as testimony, since a guard recognising testimony only when it wore a sprig would carry the very fault it exists to catch. `dated_path_witness` gains a seventh verdict case and an eighth refusal. CLOSED.

**REDS %179 (`20260824.040447`) -- a new guard was written straight over a guard that already had the name.** *What went wrong:* the loom for REDS %178 was written to `tools/fixtures/dated_pattern_scan.sh` with a shell heredoc, and a heredoc overwrites. **That path was already tracked** and held a different guard entirely -- the one proving the shared classifier `tools/fixtures/dated_classify.rish` agrees with `context/specs/living-vs-dated.md` on two planted controls, with a `prove-red` mode. Its witness at `tools/gen/season/dated_pattern_witness.rish` stands off the roster, so nothing would have run it and said so; the new witness written beside it took the same basename in a different room, which is how one name came to mean two guards. *What caught it:* reading `git status` before staging and seeing ` M ` where `??` belonged. **A file I believed I had created was reported as modified**, which is the whole tell. *What it taught:* **a name is taken until the tree says otherwise, and the tree answers in one command.** The elder's own header carries the sentence this should have been checked under -- *when two roofs carry one name, either they agree or the name is doing two jobs* -- and the round wrote the second job under the first name without ever asking. The general form is the discipline already written for deletion, reaching one step further: look at the target before overwriting, and `git ls-files <path>` is the whole of the look. *Repaired:* the elder restored byte-for-byte from `HEAD` and re-run GREEN; the new guard renamed **`dated_spelling`** -- what it reads is how every tool *spells* the shape, where the elder reads whether one classifier *answers* correctly -- across scan, control, witness, every internal reference, and the roster row; and the new scan's header now names the elder and says which job is whose, so the two standing side by side explain themselves. Both GREEN. **And the elder is now on the roster** as `dated_pattern`, pointing at `tools/gen/season/dated_pattern_witness.rish` -- a guard that can red and that nobody runs is REDS %170's lesson exactly, and finding this one by colliding with it is a poor substitute for running it. Roster stands at **38**. CLOSED.

**REDS %180 (`20260824.043930`) -- the fold's dated test still required a sprig, in the one language a regex guard cannot read.** *What went wrong:* `day_from_log_fname` in [`../tools/rye/session_logs_archive.rye`](../tools/rye/session_logs_archive.rye) decides which files a room fold moves and which index links it repoints. It read `if (fname[15] != '_') return null;` -- the same narrow rule REDS %175 and %178 widened at nineteen other sites, written here as **index arithmetic on a byte rather than as a regex**. So a log named `20260619-072600.md`, whose stamp runs straight into its extension, returned null: `should_fold_flat` said no, the fold left it flat, and `rewrite_readme_links` left its index link pointing at a bare filename. The tree holds **237** such logs. *What caught it:* reading the fold core while planning a different repair in the same room, and noticing the constant `17` and the literal `'_'` where the widened sites now read `[_.]`. The new `dated_spelling` guard (REDS %178) **does** scan `.rye` -- it greps for regex spellings of the pattern, and there is no regex here to find, so it passed truthfully over a file carrying the fault. *What it taught:* **a guard that reads spellings sees only the places where the rule is spelled.** A rule expressed as control flow is invisible to it, and the invisibility is silent rather than loud -- the scan reports green over the file, which reads exactly like coverage. Where a rule has more than one form, the meter for its second form is a **behavior test on metal**, not a wider grep. *Repaired:* character 15 now accepts `.` as well as `_`, keeping every other guard in place -- the whole stamp, the `-` at 8, digits either side, and the day-only refusal that protects the resolver. Prevention is a **both-ways proof in the selftest the witness already runs**: `tools/fixtures/session_logs_archive/` gains a sprigless `20260703-120000.bron` that must fold with its link repointed, and a day-only `20260814-fill-epsilon-x.md` that must stay exactly where it stands, so the same preview count of three proves the acceptance and the refusal together. Three Zig unit cases join it -- two sprigless names accepted, and a retired countdown-prefix name refused, since a stamp has to begin the filename. Proven RED by restoring the narrow line and watching the witness fail, then GREEN by the fix. Measured on this tree: **zero** files in any of the six enforced rooms were stuck flat by the narrow rule, so this is prevention rather than repair -- every sprigless log had folded before the strictness landed. CLOSED.

**REDS %181 (`20260824.045445`) -- the round that widened the dated rule cited two files that exist nowhere.** *What went wrong:* the REDS %180 repair added Zig unit cases to `day_from_log_fname` and a ledger row describing them, and both reached for a plausible sprigless name rather than a real one -- a `YYYYMMDD-HHMMSS.bron` and a `YYYYMMDD-HHMMSS.kyri`, written out in full digits, naming no file in this tree. **This row cannot quote them.** Writing the two names here would create the citations a second time, inside the row explaining them, and the census would count them again -- which is exactly what happened on the first draft of this row, and is why the digits above are placeholders. A stamp with an extension after it is a **citation** to every reader and to `tools/fixtures/dated_path_scan.sh`, which reads exactly that shape wherever it appears -- so the ledger row acquired one lost reference and the census rose from 187 to 188. *What caught it:* the roster, run **after** `git add` (REDS %174). `dated_path` alone answered red, `refs_lost=188 lost_ceiling=187 under_ceiling=no`, and the two new lines fell straight out of a diff of the scan's `list` output against a stashed tree. *What it taught:* **the mark law already wrote this warning, one lap earlier, and the warning did not carry.** [`.claude/rules/stamp-and-name.md`](../.claude/rules/stamp-and-name.md) says *illustrate with placeholders, cite only what exists*, and names the three fabrications the census caught on the day the law was written. Naming a fault is not preventing it; the meter is what prevents it, and the meter is why this cost one re-run instead of a month. The transferable form: **an example is either a placeholder (`YYYYMMDD-HHMMSS_sprig.ext`, letters where digits go) or a real path -- never a realistic-looking third thing**, and that holds in prose, in a ledger row, and inside a test string, since none of the three is invisible to a grep. *Repaired:* both unit cases now name real tracked files -- `20260619-072600.md`, a folded sprigless log, and `20260703-120000.bron`, the fixture this round created -- with a comment saying why the names have to be real; the ledger row drops its invented `.bron`. Measured after: `refs_lost=187`, `under_ceiling=yes`, exactly where it stood. No new guard: `dated_path` already gates this class at a ceiling with no slack, and it did its whole job. CLOSED.
