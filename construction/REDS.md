# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Season - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 242 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 222** -- counted from the ledger and its archives on `20260825.235254` by `tools/gen/season/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 242 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*Row 170 folded to [`archive/REDS-the-prevention-off-the-roster-rows-170.md`](archive/REDS-the-prevention-off-the-roster-rows-170.md) on `20260823.152322` -- REDS %83's own ASCII prevention guard standing off the roster while the card it protects carried four non-ASCII lines, CLOSED, moved here on the lap it was booked. Its lesson is carried forward by row %171, which measures the same coverage gap at a wider scale.*

*Row 171 folded to [`archive/REDS-the-sample-nobody-drew-rows-171.md`](archive/REDS-the-sample-nobody-drew-rows-171.md) on `20260823.171000`, **CLOSED** -- the random draw of 86 witnesses across 29 rooms, and the two rungs it left open, both now closed by row %173. Moved here as %173 carried the living pin past its 24,576-byte bound. **Living rows 172 onward below.***

*Row 172 folded to [`archive/REDS-the-roster-that-made-itself-red-rows-172.md`](archive/REDS-the-roster-that-made-itself-red-rows-172.md) on `20260823.174500`, **CLOSED** -- running the roster manufactured the artifact the next run reported, and the scan asks `git check-ignore` now rather than guessing which rooms are generated. Moved here as row %174 carried the living pin past its bound. **Living rows 173 onward below.***

*Row 173 folded to [`archive/REDS-the-machine-with-no-screen-rows-173.md`](archive/REDS-the-machine-with-no-screen-rows-173.md) on `20260823.174500`, **CLOSED** on the lap it was booked -- a Wayland application on a headless pier reading as a broken tree, and the shared `display_seam_probe.sh` six callers now gate on. Moved here as row %174 carried the living pin past its bound. **Living row 174 below.***

*Row 174 folded to [`archive/REDS-the-roster-ran-on-one-tree-rows-174.md`](archive/REDS-the-roster-ran-on-one-tree-rows-174.md) on `20260823.204456`, **CLOSED** -- the build cache that reached main because the roster ran before `git add -A`, and the rule it gave its final shape: stage first, then measure, then read what you staged. Moved here as row %175 carried the living pin past its bound. **Living rows 175 onward below.***

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Row 175 folded to [`archive/REDS-the-sprig-that-was-optional-rows-175.md`](archive/REDS-the-sprig-that-was-optional-rows-175.md) on `20260823.212601`, **CLOSED** -- the canonical dated test required a sprig the session-logs law makes optional, so 237 logs read as living and sat in the repointer's write set. **Eight read-only scans still carry the narrow pattern**, named there and waiting on their own lap. **Living row 176 below.***

*Row 176 folded to [`archive/REDS-the-mirror-that-matched-perfectly-rows-176.md`](archive/REDS-the-mirror-that-matched-perfectly-rows-176.md) -- CLOSED.*

*Rows 177-178 folded to [`archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md`](archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md).*

*Row 179 folded to [`archive/REDS-the-name-that-was-taken-rows-179.md`](archive/REDS-the-name-that-was-taken-rows-179.md) on `20260824.062207`, **CLOSED** -- a new guard written straight over a tracked guard that already carried the name, caught by ` M ` where `??` belonged in `git status`. Moved here as row %184 carried the living pin past its bound. **Living rows 180 onward below.***

*Row 180 folded to [`archive/REDS-the-rule-written-as-arithmetic-rows-180.md`](archive/REDS-the-rule-written-as-arithmetic-rows-180.md) on `20260824.071500`, **CLOSED** -- the room fold's dated test still required a sprig, written as a byte comparison rather than a regex, so the spelling guard passed truthfully over the file carrying the fault.*

*Row 181 folded to [`archive/REDS-the-citation-that-named-nothing-rows-181.md`](archive/REDS-the-citation-that-named-nothing-rows-181.md) on `20260824.072600`, **CLOSED** -- a round that widened the dated rule cited two files that exist nowhere, caught by the roster run after `git add`.*

*Row 182 folded to [`archive/REDS-the-index-that-grew-alone-rows-182.md`](archive/REDS-the-index-that-grew-alone-rows-182.md) on `20260824.075409`, **CLOSED** -- the room that folded while its index grew alone, to 2,895,849 bytes against a declared 24,576, and the index fold that seated the 39th guard. Moved here as row %187 carried the living pin past its bound. **Living rows 183 onward below.***

*Row 183 folded to [`archive/REDS-a-fold-moves-depth-rows-183.md`](archive/REDS-a-fold-moves-depth-rows-183.md) on `20260824.075409`, **CLOSED** -- the shelf writer that moved a row's text without its depth, and the rule that repaired it: fix the tool and re-run from a clean base rather than patching its output. Moved here as row %187 carried the living pin past its bound.*

*Row %221 folded to [`archive/REDS-a-cold-roster-across-its-own-edits-rows-221.md`](archive/REDS-a-cold-roster-across-its-own-edits-rows-221.md) on `20260825.144025`, **CLOSED** -- a cold reading spread across the tree it was measuring, closed by a twelve-character tree digest taken at the open and again at the close. Moved as %223 carried the living pin past its bound.*


*Row %222 folded to [`archive/REDS-a-tier-from-one-sample-rows-222.md`](archive/REDS-a-tier-from-one-sample-rows-222.md) on `20260825.162410` -- the tier chosen on one cold-cache sample multiplied by 239, CLOSED, moved here as row %224 carried the living pin past its 24,576-byte bound.*

*Rows %223-%225 folded to [`archive/REDS-a-refusal-a-ratchet-and-a-name-rows-223-225.md`](archive/REDS-a-refusal-a-ratchet-and-a-name-rows-223-225.md) on `20260825.183336`, all three **CLOSED** -- a cold roster refusing to open over an unclosed lap, the reach ratchet moving onto the property it stands for, and the seed wall learning the fund's short name. Moved here as rows %226 through %228 carried the living pin past its 24,576-byte bound.*

**REDS %230 (`20260825.195437`) -- two clones read the same tree for the next row number, and both took 226.** *What went wrong:* a REDS row number is allocated by reading the ledger's spine off disk and adding one. Two clones did that within five minutes of each other on `20260825`, against trees that agreed at `5b94dd2d69`, and both wrote `%226`: the macOS bench at `20260825.183029` for two guards speaking one host's awk dialect, and this pier at `20260825.183336` for five guards red across a signed send. Both commits were signed, both were pushed -- the macOS row to `xykj61`, this pier's to `origin` -- so the two remotes carried different ledgers with the same row number and no tree anywhere held both. *What caught it:* `git push xykj61 main` refusing the fast-forward, and then reading their `construction/REDS.md` before merging rather than after. *What it taught:* **a number allocated by reading a tree is allocated per tree, and the guard that proves it whole proves it whole per tree too.** [`../tools/gen/season/reds_ledger_monotone_witness.rish`](../tools/gen/season/reds_ledger_monotone_witness.rish) holds the spine at 1..N with no gaps or duplicates, and it was GREEN on both clones at the moment they disagreed, because each read a spine that was internally perfect. A guard cannot see a collision it has no second tree to compare against. This is REDS %226's own lesson one layer down: a discipline carried by one machine's reading is not a discipline the tree holds, and the census number this ledger keeps (`stamp-and-name.md`) turns out to be exactly as single-writer as a database sequence. *Repaired:* the merge keeps the earlier-stamped row at %226 -- the macOS bench's, at `183029` -- and shifts this pier's three to %227, %228, and %229, sweeping every citation of them in the ledger, the card, the session log, and the tool comments that name them. The ledger reads 1..230 with no gaps or duplicates, and both remotes carry the same spine. Two dated commit bodies keep the numbers they wrote, which accrete-never-break protects and this row explains. *Surfaced, not crossed:* how the next number is allocated when two piers run at once is a design question rather than a repair -- fetch-before-book, a per-pier band, or a stamp-keyed row with the number derived at merge -- and it wants Keaton's word alongside the three-real-MOX design already booked on the card. OPEN, GATE.

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). Most recently %231-%232 on `20260825.235254` and %233-%236 on `20260826.001630`, all **CLOSED**. One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **Row %230 stays flat: it is OPEN and gated.***


**REDS %237 (`20260825.235254`) -- fourteen guards read a folded ledger as if it were whole, and the fold-aware repair had fixed the other function.** *What went wrong:* `tools/fixtures/reds_ledger_monotone_scan.sh` defaults to `construction/REDS.md` alone when called with no arguments. Since the first fold that file holds a **suffix** of the spine -- 3 flat rows against 232 -- so every bare caller read `expected row 1, found 230` and answered `verdict=not_monotone` over a ledger that is perfectly whole. **Fourteen** `tools/fixtures/equinox_*_scan.sh` files call it bare. The scan's own header has said *"pass every file that holds rows"* since `20260811.143500`, and its witness passes the archives and the pin explicitly, so only the default was ever wrong. *What caught it:* the leaf sing, through `e113_fascia_health` -- one of the two limbs holding `instrument_suite_scan.sh` at 8 of 10, which in turn held e132, e134, and e135. *What it taught:* **a default that contradicts its own documentation is a trap rather than a convenience.** REDS %231 repaired exactly this shape a lap earlier for `reds_row_present` -- 19 sites, one shared fixture -- and the sibling function beside it kept the fault, because the repair was aimed at the call sites it had found rather than at the class. **A lantern that fires twice becomes a loom:** the fix belongs in the tool, where one edit repairs fourteen callers and every caller yet to be written. *Repaired:* the no-argument default now unions `construction/archive/REDS-*rows-*.md` with the living pin, an unmatched glob leaves a never-folded tree read exactly as before, and an explicit argument list still overrides -- which is what the witness's planted single-file pens depend on. Bare, the scan read `rows=232 gaps_or_dupes=0 verdict=ok` at the moment of repair, before this lap's own rows were booked.

**REDS %238 (`20260825.235254`) -- a meter that infers a choir's reach overstates it by 111.** *What went wrong:* `tools/fixtures/witness_reach_scan.sh` learns what a roster row reaches by reading source: a Rishi choir's `let ... = [ ... ]` literals, and calls in command position. The season leaf choir discovers its members at runtime with `git ls-files`, so the meter saw none of them and reported all 33 as unreached on the lap they began to be sung. The first repair taught the meter to expand the choir's glob -- and that glob selects **144** equinox witnesses where the choir sings the **33** that chain nobody, so the ceiling would have fallen by 242 for work that carried 47. *What caught it:* reading the number before trusting it. `unreached` fell 1,201 -> 959 on a change that rostered one guard, which is too large a fall for one row and was the tell. *What it taught:* **a number that says more than it measured is worse than a number that admits a blind spot** -- and the honest way to learn what a discovering choir sings is to ask it. A choir now declares a `# reach-list:` enumerator on one line; the meter runs it, takes the printed paths, and a choir that changes what it sings changes what it reports in the same edit, because the enumerator and the sing read one list. *Repaired:* `season_leaf_choir_scan.sh --list` prints exactly the set the sing runs and runs none of it; the meter asks rather than infers; `unreached` reads **1,154**, a fall of 47 -- the 33 sung leaves plus the 14 those leaves reach -- and the ceiling moved to match.

**REDS %239 (`20260825.235254`) -- the lap starved the roster it was standing on, and the roster reported the starvation as four tree reds.** *What went wrong:* a cold `standing_equipment_run.sh` was 52 guards into its 64 when this lap copied the whole repository into `${TMPDIR}` to test a repair on a throwaway tree. The copy filled the 3.9 GB tmpfs, and the four guards still to run -- `caravan_suite`, `ales_roster`, `reds_ledger_headline`, `reds_row_present` -- answered red on `ENOSPC`. The run card read `guards_red=4` and `run_verdict=guard_red` with `tree_moved=no`, which is exactly what four genuine tree reds look like. *What caught it:* re-running all four by hand on the same unmoved tree after clearing the pen; all four read GREEN, and the tree digest `048537bc345a` was identical at open and close. *What it taught:* **the card says hold still while the roster runs, and a pen is motion.** The runner already refuses a tree that moved under it; it has no reading for a *machine* that moved under it, and a starved guard is indistinguishable from a broken one in its output. It is a machine fact rather than a tree red -- the class the loop prompt names -- yet it arrived wearing a tree red's clothes, so it is booked here rather than waved off. A pen the size of the repository is never the cheap way to test a string replacement: the replacements in this lap each assert their own match count and refuse rather than misfire, which is the check that was actually wanted. *Repaired:* the pen removed, all four re-proven GREEN individually on metal, and the authoritative reading for this round is the **hot** roster over the tree the commit ships, which is what REDS %174 seated the hot pass for.

**REDS %240 (`20260826.001630`) -- a generated page shipped thirty-eight zeros to the anointed remote, and its own guard agreed with it.** *What went wrong:* `tools/fixtures/geode_libraries_scan.sh` counts a room's witnesses through `xargs -a FILE -d '\n' grep -l`. BSD xargs carries neither `-a` nor `-d`, so on the macOS bench that pipeline yields nothing, `wc -l` answers 0, and all thirty-eight rooms read zero witnesses. `docs-geode/libraries/README.md` was regenerated and pushed to `xykj61` in that state on `20260825`, and `tools/g/geode_libraries_witness.rish` stood GREEN on that bench while it happened, because the guard renders through the very function it checks. *What caught it:* a rebase. This pier pulled the anointed remote at `20260826.000301`, met a content conflict in that page, and read both sides side by side -- one carrying real counts, one carrying a column of zeros. *What it taught:* **a guard that renders through the function it checks proves the two agree, never that either is right.** This is the third time the class has cost this tree a number: REDS %169 was a glob answering zero for the same thirty-eight rooms, and REDS %226 was two guards speaking one host's awk dialect -- the same fault in a different tool. A lantern that fires twice becomes a loom. *Repaired:* the counting function reads `tr '\n' '\0' | xargs -0`, which both dialects carry, and spells the word boundary as an ERE alternation rather than `\b`, which BSD grep does not promise. The page renders byte-identical on this pier and every witness count is unchanged, so the repair is proven to move nothing but portability. `tools/fixtures/shell_dialect_scan.sh` and `tools/s/shell_dialect_witness.rish` seat the class: **12 gated sites across 7 files** held under a ceiling that only falls, four further GNU-only families counted and printed without gating, and the mechanism reproduced on metal -- a two-line `xargs` refusing `-a` and `-d` as BSD does, under which the elder form answers 0 and the portable form answers 1.

**REDS %241 (`20260826.001630`) -- `git rebase --continue` makes a commit, and the wall that keeps generated pages fresh never fires.** *What went wrong:* `tools/hooks/pre-commit` regenerates `README.md`'s metrics block and `docs-geode/libraries/README.md`, and it fires at `git commit`. A commit created by `git rebase --continue` runs no `pre-commit` hook at all. This round rebased onto the anointed remote, whose three commits had added two `.rye` modules, and the commit that came out of the rebase carried `1898` Rye modules where the tree holds `1900` -- a number this pier had itself been right about one commit earlier. *What caught it:* rendering the page and reading the diff rather than trusting the earlier render. The earlier render was the trap: `tools/r/readme_metrics.rish` and `tools/g/geode_libraries.rish` both print to stdout and write only when handed `write`, so a run that prints the right number and touches nothing reads exactly like a run that fixed the page. *What it taught:* **a wall placed at one verb does not cover a second verb that does the same work.** The twice-pulled sync rota was seated `20260825.210819`, one day before this round; every round now opens by rebasing, so the path around this wall went from rare to routine in a day, and no row named it. *Repaired:* both pages written with `write` and folded into the same commit by a single amend, so no commit in this tree's history carries the regressed number. The roster's `readme_metrics` and `geode_libraries` guards read the live pages rather than the hook, and would have refused the next lap; the gap this row names is the window between a rebase and the next roster run, and the rota's own rule -- re-verify a package after it moves -- is what closes it.

**REDS %242 (`20260826.001630`) -- a fourth send from the other clone arrived carrying a living pin already over its bound.** *What went wrong:* `construction/ITINERARY.md` reached `xykj61` at **24,933 bytes** against the **24,576** its own header declares, and this pier's rebase carried it to **24,952**. `tools/gen/season/equinox_e123_living_pin_guard_witness.rish` holds a roster row and refuses exactly this reading, so the guard existed, stood on the roster, and was not run before the push. *What caught it:* running the ledger guards after this round's fold, one of which is e123 -- and only because the fold made a reason to run them. *What it taught:* **a discipline that runs at one pier's open does not run at another pier's send.** REDS %232 booked this class three sends ago and repaired the tree rather than the gesture; counting %226, %227-%229, %231-%232, and this row, the class has now fired four times, which by this tree's own ladder -- rule, then reading, then refusal -- has earned its third rung. *Repaired:* the card condensed to **24,251 bytes** with **325** to spare, its landed round folded to one *Prior laps* line and the spent seed-cadence paragraph cut from 981 bytes to 570 with every live fact kept; walk-back `11b9e87321` recorded in `construction/CHECKPOINTS.md` before the sweep. *Surfaced, not crossed:* the rung this asks for is a **pre-push refusal** -- the pin bound, or the roster, read at `git push` rather than at a lap's open. It changes how both piers send, so it waits for Keaton's word.
