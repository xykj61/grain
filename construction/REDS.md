# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`): **OPEN** is live and stays on the pin; **BOOKED** means the instances stand repaired and the row may fold; **CLOSED** closed on a witness. The last bold marker decides.
**Bound:** under `living_pin_max_bytes[construction/REDS.md]` (**65536**, raised `20260915.174500` on Keaton's word from 40960). The general bound stays 24,576. **The raise's own account -- what was measured, why the elder number modelled eight open rows against sixteen standing, and why a bound is the wrong instrument for a page whose rows do not close -- folded to [`archive/20260917-071327_reds-pin-bound-raise-account.md`](archive/20260917-071327_reds-pin-bound-raise-account.md) on `20260917.071327`, because it is a finished decision rather than a declaration.**
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof.* The check at Voice v9 that opened this ledger found six reds written into the tree and twenty owned out loud.

---

## Why this file, and what belongs in it

**Three fields per row** -- what went wrong, what caught it, what it taught -- because a red with
fewer teaches nothing. A red enters when it is found; rows are never edited or removed, and a later
correction accretes. The full statement rests on
[`archive/20260908-161500_reds-why-this-file.md`](archive/20260908-161500_reds-why-this-file.md).

---

## Voice Chapter - opened `20260729`

**REDS %827 (`20260918.090424`) -- a ratchet that only falls read two guards over its own ceiling.** *What went wrong:* `tools/fixtures/s/standing_equipment_redleg_scan.sh` counts rostered guards demonstrating no refusal of their own (one of six named spellings: a `prove-red` call, a `_control.sh` invocation, a planted case, an `== false` assertion, an asserted refusal value, or an asserted refusal exit) and gates the count against a ceiling that is only ever lowered, never raised -- git history shows `CEILING` moving `53 -> 51` on an earlier lap. This lap's cold endurance run read `guards_no_refusal_marker=53` against `no_refusal_marker_ceiling=51`, naming `tools/am/amphora_device_wire.rish` and `tools/gen/fund/gen_linn_fund_prep.rish` as the two guards over it. Both are accrete shims: `amphora_device_wire.rish` carries `assert r.ok else "..."`, which propagates its target's refusal rather than demonstrating one of its own under a planted case, so the scan correctly does not count it. *What caught it:* this lap's cold endurance run, launched per the baton's ORDER clause and held to `run_verdict=` before any tree edit; `standing_equipment_redleg` was one of 29 reds it named. *What it taught:* a ceiling proven at 51 on the day it was measured is a ceiling against the shims written before it, and a shim seated after that measurement (`amphora_device_wire.rish`, roster row dated `20260911`, and `gen_linn_fund_prep.rish`) can cross it again the same way `%826`'s wall did one room over -- a report gate with no check at the moment a new shim is rostered lets the count drift until a cold pass happens to run. *Repaired:* nothing here -- fixing each shim means adding a `_control.sh` (or another of the six markers) proving its refusal actually fires under a planted case, which is real work belonging to whoever owns Amphora's device-wire lab and Dimeroll's fund-prep generator rather than a one-line law-lane patch. **OPEN** -- whoever owns `tools/am/amphora_device_wire.rish` and `tools/gen/fund/gen_linn_fund_prep.rish` gives each a proven refusal leg, or the roster names why an accrete shim inheriting its target's refusal is itself an acceptable seventh marker.

**REDS %826 (`20260918.075447`) -- the wall `%801` closed reads twenty-nine violations of its own class, because it runs only in the cold pass and never at commit time.** *What went wrong:* `tools/c/commit_parent_claim_witness.rish` walls three claim genres at zero forward of anchor `0cf919699b`, and this lap's own cold endurance run reddened it: `sh tools/fixtures/c/commit_parent_claim_scan.sh --window 4000 --anchor 0cf919699b` reads `false_after_anchor=28 unresolved_after_anchor=1 claims_after_anchor=29`, every one of them a `session-logs: confirm/record send` or `remember-git-nib` follow-up body naming its own parent by a hash that a later rebase moved past -- the exact `%801` class, GREEN when that row closed on `20260917.174536` and never stayed that way. *What caught it:* this lap's cold endurance run, launched per the baton's ORDER clause and held to `run_verdict=` before any tree edit; `commit_parent_claim` was one of 30 reds it named. *What it taught:* **a wall proven GREEN on the metal a lap ships is a wall against everything written before it, and this one has no gate at the door where the violation is actually written.** `tools/hooks/commit-msg` carries the mechanism-sentence, `%`-sigil, `@name`, and path-existence walls, and none of them reads a parent-hash claim -- so the witness is read only when a cold pass happens to run, which on an eight-ship pier sharing one habit means the backlog grows by roughly three violations a day between readings. Every one of the 29 is testimony now and stays exactly as written; accrete-never-break forbids editing a pushed body, and `remember-git-nib.md` rule 5 already names the right convention -- leave the parent hash out of the body, let the card carry it -- it is simply not enforced at the one moment enforcement would cost nothing. *Repaired:* nothing here -- a hook addition is a shared wall change across all eight ships' `.git/hooks` via `core.hooksPath`, and belongs to the hand that owns `tools/hooks/commit-msg` and its witness rather than to a single lap's guess. **OPEN** -- whether `commit-msg` gains a fifth wall reading the same claim genres `commit_parent_claim_witness` already names, refusing a body that asserts its own parent by hash at write time rather than reporting it after, is Keaton's word or the owning lane's next touch.

**REDS (`20260918.013500`) -- 42 session-log index rows across one day's shelf stand over the 192-byte row bound, and the guard that reads them is not the one this rule cites.** *What went wrong:* `session-logs/date/README-index-20260918.md` carries 56 rows; `rishi/bin/rishi run tools/in/index_row_bound_witness.rish` reads 42 of them over the 192-byte ceiling, five of them past 250 bytes, with the longest at 295. `session-logs.md`'s own rule cites `index_fold_witness.rish` as the guard holding this room at zero, and that witness answers a different question -- whether a row points at a folded log, not whether a row fits the byte bound `stamp-and-name.md` sets for it. So a room this tree's own law calls carried across and gated at zero is, by a sibling instrument, 42 rows over a bound nobody was reading. *What caught it:* running the row-bound witness on an unrelated pass, to confirm my own new row (181 bytes) landed inside it. *What it taught:* two witnesses can each answer their own question correctly about one file and leave a reader believing a single "gated at zero" story that only one of them tells. *Repaired:* nothing yet -- the 42 rows belong to many hands across today's shelf, and rewriting another lap's row without its author is not this lap's to do alone. **OPEN** -- whether the fix is per-row trims (accrete-safe, each row still points at its unmoved log) or a rule clarifying that `index_row_bound_witness` and `index_fold_witness` are both load-bearing for this room, is Keaton's word or the owning lane's next touch.

**REDS %820 (`20260917.203038`) -- seven files born after %788's census copied the same broken root-finder.** *What went wrong:* `tools/fixtures/r/root_finder_scan.sh` ratchets %788's defect -- a finder testing for `rishi/bin`, an untracked build output, refuses in a checkout nobody has built -- gating NEW growth at zero against a baseline while the legacy 191 sites ride a ratchet by design. Six shell sources and one `.rish` comment landed after that baseline copied `-d "$VAR/rishi/bin"` verbatim, so `tools/r/root_finder_witness.rish` read `finders_bare_new=7 verdict=new_finder`, RED. *What caught it:* opening the queue for %788's repair and running the standing scan first. *What it taught:* a ratchet gating growth at zero still needs someone to run it -- five lanes each copied the idiom from a neighbor rather than the tracked sentinel the census names. *Repaired:* all seven swapped `rishi/bin` for tracked `rishi/src`, each root-find re-proven, all five touched witnesses re-run GREEN. `root_finder_witness.rish` reads `finders_bare_new=0 verdict=ok`. **CLOSED** -- the legacy 191-file sweep stays %788's own ratchet.

**REDS %819 (`20260917.193838`) -- a law room's own gate read RED, and the cause was arithmetic rather than a fault in any one page.** *What went wrong:* `tools/p/prose_register_witness.rish` held `.claude/rules/*.md` to a ratchet ceiling of six pages allowed above the Field target of 30% negative sentences. Reading the tree found seven -- `the-baton.md` at 47%, `git-signing.md` at 55%, `gratitude-licenses.md` at 52%, `debride.md` at 52%, `ascii-first.md` at 40%, `session-log-provenance.md` at 35%, and `derived-spine.md` at 31% -- so `verdict=register_drift` and the witness refused. Nothing in `construction/REDS.md` or the fleet's live claim board named this drift; it stood unclaimed. *What caught it:* a routine cold check of my own lane's pages widened, on a hunch, to the fleet-wide register scan the same tool shares, which reads past docs-geode into every enforced tier at once. *What it taught:* the ceiling for this tier is a bare count with no page named to it, unlike the teaching tier's single named exception -- so any law page crossing 30% on an ordinary edit (here, `.claude/rules/the-baton.md`'s same-day growth) can tip the whole room red without the editor ever running this particular scan. The cheapest repair is not always the newest page: pricing all seven by `--explain` found `derived-spine.md` needed only two sentences moved under the floor to clear the room, where the newest and largest page would have cost dozens. *Repaired:* two sentences in `.claude/rules/derived-spine.md` restated affirmatively with every claim, figure, and stamp held -- `needs no law` became `settles by measurement alone`, and `Neither yields, so no lap may move either row` became `Both hold, so every lap leaves both rows exactly where they stand` -- bringing that page from 31% to 29% and the room from seven over to six. `tools/p/prose_register_witness.rish` and `tools/fixtures/a/ascii_document_scan.sh` both GREEN on metal after the edit. *What this does not reach:* the six pages still riding the ceiling, `.claude/rules/the-baton.md` chief among them at 47% of 187 sentences -- a page read by every ship at the top of every lap, and too large a rewrite for a low-risk pass to attempt mid-flight. **OPEN** -- whether the fleet's own coordination law should carry a Meter-style refusal exemption, the way a funds page's REFUSE table already does, is Keaton's word rather than a lap's guess.

**REDS (`20260917.185557`) -- the shared card sat 482 bytes over its own bound, blocking every commit on the pier.** *What went wrong:* `construction/ITINERARY.md` read 41,442 bytes against its declared `living_pin_max_bytes` of 40,960, landed by a commit path `pre-commit`'s own message names as invisible to it -- a rebase or a fast-forward merge that carried the overflow in without ever passing through this checkout's hook. Every commit attempted here, on any file, refused at `tools/fixtures/p/pin_bound_touch_scan.sh index` with `pin_over=construction/ITINERARY.md ... detail_over_by=482`, so the block reached the whole fleet through the shared pin rather than through anything this seat wrote. *What caught it:* an ordinary session log commit refusing on a file it never touched. *What it taught:* a living pin's bound is checked at commit time on THIS checkout, so a peer's rebase or a fast-forward that skips this hook can still leave a pin over bound for every OTHER ship to inherit -- the guard is sound and its blind spot is structural, named in its own refusal text. *Repaired:* the CLOSED "COPAL -- a reader called a board readable" account (row `20260917.172111`, already CLOSED) folded to [`archive/20260917-185557_itinerary-copal-board-readable-account.md`](archive/20260917-185557_itinerary-copal-board-readable-account.md) per the established shelf pattern, bringing the card to 38,745 bytes. Checkpoint at `construction/CHECKPOINTS.md` `20260917.185557`, walk-back nib `ffccd62f3c`. **CLOSED** -- `pin_bound_touch_scan.sh` reads `verdict=ok` on the card after the fold.

*Row `%817` (`20260917.164918`) -- a reader capped its own map at sixty of nine thousand seven hundred -- folded to [`archive/REDS-the-map-that-was-the-journal-rows-817.md`](archive/REDS-the-map-that-was-the-journal-rows-817.md) on `20260917.190500` as the pin stood over its bound. It stands CLOSED.*

**REDS %808 (`20260917.031907`) -- a send chained its own proof to its own push with a semicolon, so the proof refused and the push went anyway.** *What went wrong:* the lap's last command read `... && git commit -q --amend --no-edit && rishi run tools/r/remember_git_nib_witness.rish >/dev/null 2>&1 && echo NIB_GREEN; git push xy HEAD:main`. The witness refused -- the card's Git nib named `66b48ae7c3`, a hash typed by hand from a seven-character `git log --oneline` line when the ten-character form ended `c4` -- and the `;` before the push means the push never asked. Commit `948863700` reached the anointed remote carrying a nib that names no object in this repository. *What caught it:* re-running the same witness alone, one command later, because `NIB_GREEN` had not printed. The reading was there the whole time, inside a `>/dev/null` and behind a separator that could not carry it. *What it taught:* **two faults, and only the smaller one is about a hash.** The first is that a ten-character hash was transcribed from a seven-character display, which is the class this tree already meets whenever a hand types an identifier it could have derived -- `git rev-parse --short=10 HEAD~1` was run in the same command and its answer was read by a person rather than by the program that needed it. The second is the shape: **`&&` proves and `;` ships, and a send that joins them has built a wall with a door beside it.** `%799` taught that a printed line is not a read one; this teaches that a line printed to `/dev/null` cannot be read at all, and that a guard whose exit code reaches no `if` has been run rather than heeded. *Repaired:* the nib is derived rather than typed, the witness is GREEN, and the correction rides a follow-up commit under rule 5 of `.claude/rules/remember-git-nib.md`, which asks exactly this of a commit landing on top of a work commit. *What this does not reach:* a guard. The refusal sits in a shell command a lap types at its own prompt, which is in no file -- the same structural wall `%457`, `%512`, `%549` and `%569` each met, and `tools/hooks/pre-push` already refuses the two ledger-spine faults at the one moment a push can be stopped. Whether the nib reading belongs there beside them is a lap and a ruling rather than a line here. **OPEN.**

**REDS %807 (`20260917.010830`) -- a line inserted at the HEAD of a stored document leaves the document, and `mantra status` reports a change on a file nothing has touched.** *What went wrong:* store `a\nb\n`, write `zero\na\nb\n`, run `mantra add`, then `mantra status` on the untouched file. It answers **1 added, 1 removed, the same text `zero` on both lines** -- a diff describing a MOVE, which is `%680`'s exact symptom one position over. `mantra annotate` shows why: the document reads `a`, `b`, terminator, `zero`. `diff` in `mantra/src/diff.rye` computes `after` as the last kept line BEFORE each gap, and gap 0 opens the document, so a head insert's anchor is `null`; `apply` in `mantra/src/weave.rye` then falls back to `self.next_pos` for the order key, which stands above every line the weave holds, and the new line sorts last. *What caught it:* an air rota lap pressing the boundary of the terminator repair with a pen -- append, middle edit, newline removal, empty file, single newline, head insert. The first five read clean and the sixth did not. Proven PRE-EXISTING rather than assumed: the module was stashed, the CLI rebuilt from the unmodified source, and the same two lines came back from the elder binary. *What it taught:* **a limit stated only in the code it limits is a limit no lap is asked to close.** `Weave.apply` names this case in its own comment -- *an insert opening the document has no anchor and keeps the counter's own answer, which is the one case still landing at the end; a document that grows at its head wants its own lap* -- written on `20260916` beside the anchor that fixed every other position. It stood in no ledger row, under no guard, and `tools/fixtures/m/mantra_document_roundtrip_scan.sh` never pressed it, so the tree carried a known defect that only its author knew. A comment records a decision; a row books an allocation, and those are different acts. *The doors, and why none is cheap:* an anchor names the line an insert comes AFTER, and the head of a document has no line before it. Placing one there wants an order key below every key the weave holds, and `ord` is a `u32` counter starting at zero, so the head is not expressible -- door 1, a `before` anchor, has nowhere to put it. Door 2, renumbering every `ord` upward to free zero, rewrites every row of the v3 record and breaks merge the way this field's own first draft did: two branches renumbering independently draw the same keys for different lines, which the merge witness refused inside the hour on `20260916`. Door 3, reserving a floor so `ord` starts at one, buys exactly one head insert per weave. **OPEN** -- the shape is measured and the doors are named; which one the weave takes is a ruling about how places are ASSIGNED, the same question `%680` returned to Keaton and the same answer it will want.

**REDS %804 (`20260917.004532`) -- a deny rule reads only because the walk beside it breaks the same way, and the file it names stays out of the public seed by accident.** *What went wrong:* `tools/fixtures/s/sow_project.sh` reads the manifest's withholds as `grep -E '^sub_exclude ' "$MANIFEST" | awk '{print $2}'`, which takes field 2 alone, so the entry `sub_exclude gratitude/Your customers hate MVPs. Make a SLC instead..html` becomes the rule `gratitude/Your` -- a path no file wears. The candidate walk, `for f in $(git ls-files -- "$p")`, word-splits that same path into six fragments, every one of which fails the `[ -f "$f" ]` test and is skipped. **Two faults cancel:** the article stays out of the seed, the `.sow-excluded.log` never names it, and every gate reads green. *What caught it:* building the candidate list by hand while batching the classification (`%642`); exactly one of 9,096 candidate lines carried a space, and the manifest turned out to name that exact path deliberately -- manifest line 336 withholds it as a saved third-party article held by copyright. *What it taught:* **a refusal proven only by another fault is a refusal nobody has tested.** Repairing either half alone publishes a copyrighted article: read the manifest line whole and the article becomes a candidate the rule then catches, which is right; read candidates with `git ls-files -z` and the article becomes a candidate no rule catches, which publishes it. The two halves move together or neither moves. Measured: **one** sub_exclude entry of 146 carries a space, and **one** candidate path of 9,096 does -- the same file, which is why the cancellation has held. **OPEN** -- the repair changes what ships, so it wants Keaton's eye beside custody gate 1, and the batched classification landing this lap preserves the elder reading exactly rather than widening it.

**REDS %803 (`20260917.023605`) -- the nib writer implements the amend and the law asks for two shapes, so every follow-up commit pins a hash one short.** *What went wrong:* `.claude/rules/remember-git-nib.md` describes two moves. Rule 2 is the amend after the final rebase, where the card names `git rev-parse --short=10 HEAD~1`. Rule 5 is the follow-up commit landing ON TOP of the work commit, where the card names `git rev-parse --short=10 HEAD` read BEFORE that commit is made, since that HEAD becomes its parent. `tools/r/remember_git_nib.rish` derives `HEAD^` and writes it -- the rule 2 shape, and its only shape. Run before a follow-up, as rule 5 asks, it writes the grandparent, and `remember_git_nib_witness.rish` reds on the next lap against a hash that is neither HEAD, nor HEAD's pre-amend sibling, nor HEAD's parent. *What caught it:* this ship ran the tool exactly as rule 5 asks, before a claim-close commit, pushed, and read the guard RED afterwards -- then reset to the pushed state and re-read it RED there, which is what told a stale pin from a local mistake. *What it taught:* **a law with two shapes and a tool with one is a law half-automated, and the automated half is the one everybody reaches for.** The writer exists because a hand copying ten hexadecimal characters falsified the nib four times in ninety minutes; a hand is now correct only on the amend path and silently one short on the other, which is worse than the hand it replaced, because the tool's answer carries no doubt. The repair is not a second tool: `remember_git_nib.rish` takes a mode naming which shape is wanted -- the amend's `HEAD^` or the follow-up's `HEAD` -- and refuses to guess, since nothing in the tree can tell a lap that is about to amend from one that is about to commit afresh. **OPEN** -- named here rather than built, because this lap already carries a closed row and its own send, and a writer repaired inside a send is how a repair becomes its own red. *The workaround until then, for whoever meets it:* pass the hash yourself -- `sh tools/fixtures/r/remember_git_nib_write.sh construction/ITINERARY.md $(git rev-parse --short=10 HEAD)` before the follow-up commit. *The first half is CLOSED on a witness* (`20260917.030500`): `tools/r/remember_git_nib.rish` takes a shape word -- `write amend` derives `HEAD^` for rule 2, `write follow-up` derives `HEAD` for rule 5 -- and `write` naming none REFUSES before the card is opened, since nothing readable tells the two laps apart. A bare render prints BOTH candidates beside their rules rather than one answer a hand will copy. Proven by `tools/fixtures/r/remember_git_nib_shape_control.sh` on real git repositories, **34 legs, 0 failing, four mutations bitten**, its centrepiece running this row's red rather than arguing it: two identical pens at one commit, each writing its shape, each then making the follow-up commit rule 5 describes, and the GUARD's own state predicate copied verbatim asked what it sees -- `parent` for the follow-up shape, `stale` for the amend. Full mechanism on the rule page. **The row stays OPEN for its second half**, the contested send, which no shape repairs.

**REDS %795 (`20260916.220730`) -- the lantern fired twice in forty minutes, from a second hand, while its own row stood OPEN.** *What went wrong:* commit `00493b3be` on `20260916.214715`, whose subject and body describe a perturbation probe resolving a guard's instrument by role, replaced item **7** of the INNER LOOP in `construction/ITINERARY.md` -- **Prove on touch**, the directive telling every lap to run the cold endurance run before work and the hot one after staging -- with a `**Git nib:**` line. That is `%789` exactly: same page, same list, same item, same replacement, forty minutes after `db4ae9f0b` restored it and while `%789` stood OPEN on the pin naming the class in plain words. The card again carried the nib twice and the list again read 6, nib, 8. *What caught it:* a hand setting this lap's nib with `sed -i 's/^\*\*Git nib:\*\* \`[0-9a-f]*\`/.../'`, which reported **two** substitutions where the card's own third mention says the fact is *named once above ... one fact in one place*. `git show ca5f356a8:construction/ITINERARY.md | grep -n '^7\.'` against the same read at HEAD named the missing item, and `git log -S 'Prove on touch'` named the commit. Every standing guard was green through both firings, as `%789` had already predicted: `nib_honesty` reads the nib BY ITS KEY and finds it whichever line carries it, `ascii_document`, `tracked_link` and the pin bound read bytes rather than meaning, and the roster's whole 316-guard pass says nothing about a list. *What it taught:* **a red named and left unbuilt is a red that fires again, and the second firing is the measurement the first one lacked.** `%789` closed by naming the cure -- a guard holding every numbered list in a living pin to `1..N` with no gaps -- and declined to build it on the stated ground that the lantern had fired once. The interval between firings was **forty minutes**, across two seats, on the one page an eight-ship fleet reads at every lap open, and the second hand had every reason to have read the row: it stood OPEN, on that same page, two screens above the list it then ate. So the rule this tree already carries -- *a lantern that fires twice becomes a loom* -- is not a counting rule about lanterns but a rule about REACH: a finding that lives only in prose reaches the reader who happens to read that prose, and the class recurs at whatever rate the fleet's hands turn over. The loom is claimed as `bakery-living-pin-list-spine` and **built on this lap**: [`tools/i/itinerary_list_witness.rish`](../tools/i/itinerary_list_witness.rish) over [`tools/fixtures/i/itinerary_list_scan.sh`](../tools/fixtures/i/itinerary_list_scan.sh) holds every ordered run in the seven pins of `tools/fixtures/l/living_pin_guard_roster.txt` to `1..N` -- no gap, no repeat, no foreign line between two items -- at **zero, a wall**, since the population was measured at zero on the repairing lap. A run is keyed by indent and held on a stack, so a nested list is its own run and its parent RESUMES after it; the first reading closed the parent on every indent change and read an ordinary nested list as broken, which three control legs caught. **28 legs** stand in [`tools/fixtures/i/itinerary_list_control.sh`](../tools/fixtures/i/itinerary_list_control.sh), every refusal planted and then lifted, the control's own leg tally asserted beside its verdict -- and the two strongest are not plants: **both firings replay out of this repository's own history**, `5f96df5e1` and `00493b3be`, each with its own parent commit welcomed beside it. Two mutations bite, and the first one written did NOT: with the foreign-line unwind deleted, `1,2,3,STOLEN,5` still fails the ascending check, so the unwind's real job is the other direction -- it keeps two separate `1..N` lists from reading as one run of `1,2,1,2`, a false refusal on ordinary writing. A plant aimed at the wrong clause proves nothing, and only running it says which.

**REDS %788 (`20260916.211800`) -- 164 tracked scripts find the repository root by a directory nothing tracks, so none of them runs in a checkout of tracked bytes alone.** *What went wrong:* 164 tracked shell scripts, **74 of them `*_scan.sh`**, locate the tree root by walking up from their own path until they find a directory holding both `rishi/bin` and `tools/fixtures`, refusing past eight steps with `no tree root within 8 steps`. **Nothing under `rishi/bin` is tracked** -- `git ls-files rishi/bin` reads **0** -- because it holds the built `rishi` binary. So a detached worktree at HEAD, a fresh clone before its first build, or any checkout of tracked bytes has no such directory, and all 164 refuse before reading a single file. *What caught it:* a perturbation probe built to answer the door `%785` left open. It runs a candidate scan twice in a detached worktree -- once whole, once with the scan's own control absent from the index and the working tree -- and **2 of its 9 candidates returned nothing either time**, `dated_path_repoint_scan.sh` and `rye_harness_roster_scan.sh`, both of which run in 32 and 9 seconds in this built tree. No standing guard could have caught it: every one of them runs in a tree somebody has already built in, which is the one condition that hides this. *What it taught:* **a script that identifies its own root by a BUILD OUTPUT has made a claim about the machine rather than about the repository.** The two properties come apart exactly once -- on a checkout nobody has built in -- and that is the checkout every probe, every fresh clone, and every pristine-tree reading starts from. **The repair is one directory name.** `rishi/src` is tracked (4 files), and measured `20260916` both `rishi/src/` and `tools/fixtures/` occur at the repository root **and nowhere below it**, so it is exactly as discriminating as `rishi/bin` and true in a built tree and a bare one alike. *What is NOT wrong, checked rather than assumed:* the bootstrap is not circular. `tools/fixtures/r/rye_build.sh` carries no root-finder, so a fresh clone still builds `rishi` and every one of the 164 then works -- the cost is **latent rather than live**, and it falls on whoever reasons over a tree nobody has built in. *What holds the finding still:* `tools/c/control_perturbation_witness.rish` GREEN on metal, its probe classifying the refusal as `refused_root_finder_needs_built_tree=2` rather than reporting it blind, since a named cause can be repaired and a blind one reads as the scan's own fault. **29 control legs, 0 failing, three mutations proven to bite.** *The falsifier was run rather than named, and it sharpened the row:* `mkdir -p rishi/bin` alone -- an **empty directory holding no binary** -- took `rye_harness_roster_scan.sh` from its one-line refusal to `scripts=3691`. The root-finder tests `[ -d "$_fd_root/rishi/bin" ]`, so it asks whether a directory EXISTS rather than whether a compiler stands in it. The 164 do not depend on a built tree; they depend on a directory only a build creates, which makes the repair surer -- `rishi/src` is kept in existence by four tracked files. **OPEN** -- the sweep is one word in 164 files spread across every lane of the fleet, so the timing is Keaton's or the fleet's rather than one ship's.
**REDS %785 (`20260916.200848`) -- a guard's ceiling was 27 percent composed of the guard's own proof, and the class it belongs to had never been counted.** *What went wrong:* `tools/fixtures/i/ignored_walk_scan.sh` reads a population that includes `tools/fixtures/i/ignored_walk_control.sh`, and that control plants the very walks the scan counts. Measured `20260916` in a detached worktree at `352c17beb`, running the scan with and without its own control in the index: `sites` 153 against 137, `walks_tree` **48 against 35**, `off_tree` 31 against 29, `unresolved_root` 74 against 73. The ceiling is **27** and the verdict reads `over_ceiling` either way, so nothing is presently wrong -- what is wrong is that **13 of the 48 sites a lane reads as its work queue are the guard's own proof**, and repairing them would break the control. *What caught it:* a census built to answer a question this ship booked twice the same day and could not answer from memory, after `awk_lcg_exact` reddened on five generators existing only to be refused inside its own pen (`%779`). Not a guard: nothing in this tree was holding a scan and its control together and asking whether they agree about one population. *What it taught:* **the class is a third of the measured scans, and its live bite is rare.** `tools/fixtures/c/control_in_population_scan.sh` reads each tracked `*_scan.sh`, extracts that scan's own `git ls-files` invocation, RUNS it, and tests the population for a tracked file of the scan's own family: **375 scans, 120 enumerating, 55 self-reading, 39 holding their own control**, and of those 39, **32 gate at zero**. A perturbation probe over all 39 -- two carry no control under their own stem, so 37 were probed, each costing two to three minutes in a detached worktree -- found **24 unmoved and 13 moved**: nine in a denominator alone, two in a measured reading (`ignored_walk`, `plant_liveness`), and **two flipping a verdict**. `awk_lcg_exact` itself reads **unmoved** today, which is `%779`'s repair proving itself. *The bite has two directions and only one was expected.* **Inflation** is the founding case's shape and the less common: `ignored_walk` reads `walks_tree=48` with its control tracked and **35** without, against a ceiling of 27, so 13 of the 48 sites a lane reads as its work queue are the guard's own proof and can never be repaired without breaking the control. **Dependence** is the direction nobody was looking for: `tracked_link` and `phantom_path` each read `verdict=ok` with the control tracked and go RED without it -- `living_links_outside_tree` 0 to 1 and `phantom_paths` 0 to 1 -- because a living file cites the control by path, `phantom_path`'s own witness in one case and a folded REDS shelf in the other. Neither is a defect today; what it means is that **a guard's green is partly a statement about its own instrument rather than the field**, so renaming a control reds the guard for a reason unrelated to the tree's health. *The denominator had to be split four ways, and finding that out was this lap's own red:* a first draft reported one `enumeration_unread=269` and, inside it, silently dropped every **bare** `git ls-files` -- the whole tracked tree, the broadest population a scan reads -- discarded by a filter written to drop prose mentions. The instrument built to measure one file carrying two roles had written one word carrying four facts. Planting that filter back fails **18 of 19** control legs. *Two seated doors, and the census rules between neither:* **move the plant** (`%775`) where the refusable shape can leave the tracked bytes, and **read past the fixtures** (`tools/e/empty_document_witness.rish`, the lesson `%157` and `%158` each paid for once) where it cannot. `%774` warns the second door is wide -- 18 of 24 sites in that family lived under `tools/fixtures/`. *This family is a member of its own class*: staging the pair moved the live reading from 374/119/54/38 to **375/120/55/39**, and it costs nothing, since the scan reports rather than gating and every plant it writes lives in a pen at run time. *What this does not reach:* a scan that enumerates broadly and filters with a `case` pattern (membership is not proof of reading); a control named for the FAMILY rather than the tool, which a basename-stem pairing cannot see, so 39 is a floor; and **12 scans rooting a `find` at the repository root or at `tools/`**, a second population wholly unread, named rather than folded into a zero. *Two process faults of this lap, recorded because neither sits in any file a guard reads:* a bare `pkill -f perturb.sh` killed the calling shell (exit 144), the fault the baton names by hand; and a manual read inside the pen the probe was using made two writers in one checkout (`%291`), repaired by re-probing in a second worktree -- both runs agreed on all 14 shared verdicts. **OPEN** -- `ignored_walk`'s 13 sites are a judgment about which door that guard takes, and the census reports rather than ruling. Paper `active-designing/date/20260916/20260916-200848_the-guard-that-reads-its-own-control.md` grades **B+/88**; `control_in_population` GREEN with **19 control legs, 0 failing, three mutations proven to bite**.

**REDS %767 (`20260916.065731`) -- four fields of the accepted receipt contract refuse at a ceiling the contract never names, and all four borrowed the identifier's.** *What went wrong:* `active-designing/date/20260912/20260912-201126_the-receipt-you-can-read-contract.md` declares eight ceilings and promises that a refusal names `field`, `value`, `ceiling`, `unit`, and `reason` wherever a ceiling applies. `ReceiptOfferFact` publishes **fifteen** fields; **four of them -- `product_digest`, `value_unit`, `return_kind`, `signature` -- carry a ceiling the table never names**, and `mantra/src/receipt_offer.rye:65-77` refuses all four at `bounds.max_identifier_bytes`, the **96** that `mantra/src/tally_receipt_offer_bounds.rye:8` declares for *each identifier*. `product_digest` is refused inside an array literally named `identifiers`. So the refusal line a reader meets reads `field=value_unit value=97 ceiling=96 unit=ascii-byte reason=too-long`, and 96 is a number nobody chose for that field: it is the identifier's, wearing the value unit's name. *What caught it:* a water-row reading of the contract against the code it governs, on the rota's own instruction to read up close and run the actual thing. Not a guard -- nothing in this tree asks whether every field a contract publishes carries a ceiling that contract declares. *What it taught:* **an unnamed bound does not stay unbounded; it gets borrowed.** An implementation obeying *bound everything* must reach for some number, and the nearest declared ceiling is the one it reaches for -- so the gap closes silently, in the direction that looks compliant from both sides. The contract reads complete, the code reads bounded, and the only place the borrow is visible is a refusal line at runtime. **The looseness is real and measurable**: `product_digest` is a SHA-256 hex digest of exactly **64** bytes, admitted at **96**, so 65 through 96 characters pass as a digest. The fixture's other three measure **18**, **14**, and **20** against the same 96. *Repaired, in the half that needs no word:* the contract's ceiling table names all four rows at the 96 the code already enforces, and a paragraph beside it states that the four are **borrowed** rather than derived, names each one's own honest derivation as owed, and marks `product_digest`'s exact 64 as the one that is a tightening rather than a naming. Zero behavior change: every number written is the number already refused. *What holds these figures still:* the four ceilings are **walled** by `mantra/src/receipt_offer.rye` and its witness; the fixture lengths are **free** -- measure them off the contract's own fixture block. *How it reached this commit:* found at `20260916.065731`, parked unsent in the dead-letter box, and adopted whole at `20260916.083946` after the four call sites were re-read on metal. **OPEN** -- choosing the four real derivations changes what the product admits, and the contract's own closing clause returns a material change to Keaton.

**REDS %765 (`20260916.064510`) -- a record version counted up, and the ruling that blessed it never opened the tree's own versioning spec.** *What went wrong:* `mantra-weave-v3` was written, and asked whether that follows `stamp-and-name`, I ruled it a **census number** and wrote that exemption into the law -- reasoning from REDS `%N` alone. `context/specs/rye-versioning-style.md` had settled the question already, in the opposite direction: *Rye versions are chronological*, `YYYYMMDD.HHMMSS`, refusing semantic versioning by name and citing Hickey's *Spec-ulation*, which is a talk this tree keeps in its gratitude. I checked `stamp-and-name.md` and the Lexicon and never the spec that owns the subject. *What caught it:* Keaton, twice -- first asking whether the naming followed the rule at all, then naming the gratitude reading I had walked past. *What it taught:* **a law can be read correctly and still be the wrong law.** The census exemption is real; it governs a number a tree assigns ITSELF for work already done, and a record version is a compatibility contract that a seated spec already ruled on. Reading one rule is not reading the rules. *Repaired:* the wrong paragraph is replaced in `.claude/rules/stamp-and-name.md` and its Cursor twin, now pointing at the spec rather than arguing against it; the record this session created writes a chronological header and its types are named for what they carry. Both counted headers still open every store written before today, which is accrete-never-break doing its ordinary work. *Audited, and the class is wide:* **111 counted version strings across 42 record families** stand in authored Rye -- `mantra-v1`, `amphora-v1`, `portrait-v2`, `mcp-call-v1` and 38 more. Only `mantra-weave` is moved. **OPEN** -- the remaining 41 families are a molt wanting Keaton's word, since each writes bytes a store already carries and the elder headers keep their names forever.

**REDS %756 (`20260916.004153`) -- a wire ceiling was corrected, and the harness measuring it had been publishing an answer shape the wire could never carry.** *What went wrong:* `comlink/query_wire_retention_cost.rye` declares `measured_hit_count` as the literal 4 and slices `kept_store[0..measured_hit_count]` out of an array sized `qw.max_wire_hits`. `0d6e6829a` made that ceiling derived -- `(max_wire_payload - response_head_bytes) / max_hit_bytes` -- where it had been the literal 8, and it reads **2**, so the harness refuses to compile and `query_wire_retention` reads red on every ship. *What caught it:* the hot endurance run, `query_wire_retention red`, whose own line named the slice bound rather than the seam. *What it taught:* **the derived 2 is the correct number, and the elder 8 was never consistent with the 340-byte payload beside it.** Nothing tied the pair until that commit added `assert(response_head_bytes + max_wire_hits * max_hit_bytes <= max_wire_payload)`; an eight-hit answer at today's field sizes needs 954 bytes. So every retention figure this harness has published names a four-hit answer the wire could not hold, and the compile error is the first instrument that ever asked. *And no clamp repairs it*, which is the sharper half: the comparison measures the unused tail a variable-length answer leaves behind, so the count must sit strictly below the ceiling -- at 2 it reads `kept=128 fixed=120` and the control's own leg refuses in words, *the variable form must hold fewer bytes below the ceiling*. Below that, at 1, `r % measured_hit_count` folds to a constant index and the compiler hoists all three measurement loops: each read retired **6** instructions against the 20,000 its control requires. Both together need `max_wire_hits >= 3`. *What holds this still:* a `@compileError` in the harness naming the floor, the cause, and the three doors -- grow `max_wire_payload`, shrink `max_peer`/`max_bolt`/`max_path`, or retire the measurement. The guard still reds, and it reds legibly now rather than on a slice bound. **OPEN** -- the repair is the producer's and it is a protocol decision, so it waits for Keaton's word rather than for a consumer to clamp a green figure about a question the wire can no longer ask.

*Row `%745` (`20260915.220053`) -- a witness leaked a 24MB pen into `/tmp` on every run for nine days -- folded to [`archive/REDS-the-pen-the-runtime-took-over-rows-745.md`](archive/REDS-the-pen-the-runtime-took-over-rows-745.md) on `20260917.205112` as the pin stood over its bound. It stands **BOOKED**.*

**REDS %735 (`20260915.194421`) -- the counsel front door counted 764 pieces on a day the room already held 927, and every meter in this tree read the page as true.** *What went wrong:* `counsel/README.md` is a living front door, and it told every reader three things about the room behind it: **764** dated pieces, **1,977** references pointing in, and a **last dated file** of `20260805.000001`. Measured on metal this lap: the room holds **935** dated files and **2,623** path mentions elsewhere in the tree, and **ten pieces carry a stamp later than the close** -- one on `20260826`, four on `20260908`, four on `20260912`, one on `20260913`, the newest two days old. The size figure was **never** the room's count: at `770dd50b1`, the very commit that closed the room and wrote the page, `git ls-tree -r` answers **927**. So the page was 163 short on the day it was written and 171 short today. *What caught it:* the QA report card's own named blind spot, read by hand because the page was open for a register sweep. `qa_report_card.sh counsel/README.md` answers `truth=100` with `truth_mode=counted` -- **0 of 22 cited paths unresolved** -- which is the counted half answering a question nobody asked. A stale number is no path at all, so no instrument in this tree could see it; `.claude/rules/quality-assurance.md` had already booked that exact shape twice (`20260826`, `20260910`) and this is its third firing. *What it taught:* **a free figure with no command beside it is a rumour with a decimal point**, which is Gauge's own fifth thing -- what holds it still. Nothing held these three, and nothing could: a count of a room only the room's own listing can answer. The repair writes the reproducing command beside each figure rather than the number alone, so the next reader re-derives instead of trusting. **And the second half is larger than the first:** a room this tree declares CLOSED has gone on growing, ten files across five days, the newest `20260913-143321`. The close is seated law in `.claude/rules/design-rooms.md`; the growth is in the tree. Law and tree part, and which of them moves is Keaton's word rather than a lap's. *Repaired (`20260915.194421`), the door only:* `counsel/README.md` carries the three measured readings, each with its own command and each named **free**, and the close line now names the ten later arrivals in place of a last-dated-file claim the room has passed. The same page swept **50% negative to 3%** and joined the enforced DOOR roster of `tools/fixtures/p/prose_register_scan.sh` in the same commit, so it is held at 20% from here. B+/89, register 97, truth 95. *Standing, and handed rather than taken:* the identical stale pair stands in two more living pages -- `.claude/rules/design-rooms.md` and `context/LEXICON.md` both spell *764 pieces* and *1,977 references*, and both also spell the elder last-dated-file claim. Those are law and lexicon rooms this seat does not write; they belong to incense. **OPEN.**

**REDS %734 (`20260915.194903`) -- four guards answered red under parallel load and green alone, and no run ever caught the reason.** *What went wrong:* running the 239 lotus witnesses six at a time answered **1 red** before a migration and **3 red** after it; every one of the four read **GREEN alone**, three times each. A third pass over the same 239 read **0 red**. *What caught it:* migrating a batch to build pens and baselining it BEFORE the edit, so a red afterward would be attributable -- which is what proved these were not. *What it taught:* **a pen removes the shared name and this tree holds another shared thing beside it.** The pen closes ETXTBSY on a fixed path, proven at `%700` both ways on a sixteen-second build; it did not close this, since the same rate stands on both sides of the migration. *Not identified, and named rather than guessed:* six attempts to capture a failing run's own words came back GREEN, so the reason has never been read. The suspicion is a resource shared between DIFFERENT witnesses -- a build cache, or plain exhaustion at six parallel builds beside eight live fleet loops -- and a suspicion is not a finding. *The rate, measured:* 4 reds across 717 runs at six-way parallelism, which is 0.6 percent and free to move with the pier's load. *CAPTURED (`20260915.195922`), and the cause is the build's INPUT rather than its output.* Running the batch eight at a time answered **13 failures in one round**, where six earlier attempts had answered none -- the yield is in the BATCH, and the earlier attempts were mostly single witnesses. **Ten of the thirteen carry the reason in their own words:** `unable to load 'pan.zig'`, `'clock.zig'`, `'timeline.zig'`, `'envelope.zig'`, `'transport.zig'` -- a SOURCE file missing mid-compile. *The mechanism, from the tool's own source:* `rye/src/main.rye` bridges each `.rye` to *an adjacent `.zig`* in the source directory and, in its own words at line 371, writes *ephemeral `.zig` files the toolchain can read, then clears them all away*. So two concurrent `rye build` runs over one module generate the same shadow names and **one deletes them while the other is still compiling**. Confirmed three ways: every named file is a SHARED import (`timeline.rye` by **211** lotus modules, `pan.rye` by 113, `clock.rye` by 41), the failure counts track that popularity, and `lotus/*.zig` reads **zero at rest**. *Why the pens could never reach it:* a pen holds the build's OUTPUT and the shadows are its INPUT, in a namespace shared by every module in the room. **The three remaining failures say only `Lattice build failed`**, because their assert discards `${build.err}` -- the mute shape `%700`'s own header warns of, and the reason a fortnight of these left no evidence. *Doors, none taken:* a per-module build lock (`lock_acquire` already stands in `tools/fixtures/s/shell_portable.sh`), shadows emitted into a per-build directory, or content-addressed shadow names, which the bridge's own import rewriting at line 703 makes thinkable. Each changes how the compiler bridge works. **OPEN** -- cause captured and confirmed; the repair is a compiler-bridge decision wanting Keaton's word.
**REDS %730 (`20260915.182000`) -- the process-by-name family fired a seventh time, and this time the pattern was in the caller's own command line.** *What went wrong:* a background measurement was running too slowly, so this seat typed `pkill -f "/tmp/probe.sh"` to stop it. The calling shell's own command line held that pattern, so the shell matched itself and died -- exit **144**. *What caught it:* the exit code, and the background task notification reporting the same 144. *What it taught:* **the seventh firing of a class the baton names in full, with the bounded tool rostered and the reasoning written down, was typed by a seat that had read all of it twenty minutes earlier.** Nothing new is learned about the fault; what is learned is about the guard. `process_reach_scan.sh` reads **tracked sources**, and a command typed at a session prompt is in no file -- `%569` wrote that sentence and it held again here. Two habits failed together and both are on the baton: the raw `pkill` (firings one through five, and this one), and a scratch path under the shared `/tmp` rather than this tree's own `.lap/` (`%549`, `%601`, `%618`, `%620`). The bounded form is `sh tools/f/fleet_call.sh --pattern <substring>`, which reads, resolves each candidate through `/proc/<pid>/cwd`, and acts only when `--signal` names a verb. *Blast radius, measured immediately:* 24 `fleet-loop` processes alive after, this tree clean, no peer touched -- the pattern named a file only this seat had written. *What is not repaired:* the class. Seven firings by six hands says the written habit does not reach the moment of typing, and no instrument this tree can own reads a session prompt. **OPEN.**

**REDS %729 (`20260915.180733`) -- a control that never entered its pen wrote the pen's identity into the live checkout.** *What went wrong:* [`tools/fixtures/r/readme_reach_control.sh`](../tools/fixtures/r/readme_reach_control.sh) builds each case with `build() { rm -rf "$pen/t"; mkdir -p "$pen/t"; cd "$pen/t"; }` -- no `set -e`, `pen=$(mktemp -d)` unchecked, and the `cd` unchecked. When the pen cannot be made the function returns with the working directory still at the repository ROOT, and every case body then writes against the live tree: `printf ... > README.md` truncates this tree's tracked front door, and lines 127-130 run `git config user.email pen@example.invalid`, `git config user.name pen`, and `git config commit.gpgsign false` against this checkout's own `.git/config`. **This tree stood in exactly that state.** Those three keys matched the control byte for byte while every other key -- signing key, `gpg.program`, ssh path -- was correct and tree-local, and **12 commits on `main` carry `pen <pen@example.invalid>` with signature `N`**, the first dated `20260914` and the last written by this lap before it read its own config. *What caught it:* an earth lap reading the concrete mark at the door rather than arguing with it. A root listing showed **nine ignored fixture files** -- `a.md` carrying `# a` and `done`, `orphan.md`, `20260101-010101_a.md` and six more, all dated `Sep 14 10:28` -- which are this control's own case fixtures. **The tracked README read clean, and that was the tell:** `fleet_round_open.sh` runs `git reset --hard`, which silently restored the truncated front door, while `git stash push -u` passes over IGNORED files, so the repair erased the event and preserved only the evidence. *What it taught:* **`shared_pen` asks whether a pen is SHARED; nothing in this tree asked whether it was ever ENTERED.** Contention and containment are two questions, and one guard answered only the first. Measured: **36 unchecked `cd "$pen..."` sites across 26 files; 18 controls run `git config` after one**, 7 of those `commit.gpgsign false`, and 3 write `> README.md`. *Standing:* repaired at the edge in all **23 control files, 31 sites**, each refusing by name; proven on metal from both sides -- against an unmakeable pen the control exits 1, prints `refused: pen absent`, and both the front door and the identity hold. This checkout's three keys are restored and agree with a peer tree modulo its own paths. **The 12 unsigned commits are already pushed, so repairing them is a history rewrite -- custody gate 5, STOPPED and surfaced for Keaton's word.** The remaining 5 sites sit in three non-control files read separately. **OPEN.**

**Rows: 827 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 807** -- counted from the ledger and its archives on `20260918.090424` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`. The opening census of `20260729` was 6 tree records plus 14 chat records; the remainder is the measured total minus those 20. REDS %93 and %110 record the drift this derivation repairs. The [fold recital](archive/REDS-fold-recital.md) keeps each shelf's record. Every number from 1 to 827 is used; elder rows wear `#`, living rows `%` (`.claude/rules/git-signing.md`).

**REDS %456 (`20260906.020024`) -- eight ships share one login, so one credential's failure is a fleet-wide outage, and every transcript could only say the session expired.** *What went wrong:* seven of eight ships died three laps apiece inside ten seconds on `Failed to authenticate: OAuth session expired and could not be refreshed`. `tools/ag/agent-jail.sh` seeds every tree from the pier's single credential in its own words -- *one login per pier* -- so the fleet holds **one refresh token in up to eight copies**. Measured after the repair login: access token ~8 hours, **refresh token 27 days**, so calendar expiry is excluded and the failure was a refresh *attempted and rejected*. *Leading explanation, unprovable from this side:* refresh tokens commonly **rotate**, so the first ship to refresh strands the other seven **and the pier's copy**, which then seeds the dead token onward. Evidence is circumstantial and consistent: one shared token, calendar expiry excluded, a rejection rather than an absence, and the outage arriving the night the fleet went three to eight. **Falsifier, cheap:** read the pier's refresh value now and again after a ship refreshes; unchanged means this row is wrong and the cause is server-side revocation. *What caught it:* Keaton, reading three tmux windows and asking why. **Nothing in the tree could answer** -- the state was legible only as seven transcripts saying the same words. *What it taught:* **a resource shared by every ship has no blast radius smaller than the fleet.** The enclosure gives each ship its own tree, keyring and transport keys (`%427`), then hands all eight the same API credential. *And:* `claude_login_live()` tested that an access token was **present and long enough**, never whether it could be renewed, so a dead credential read as live -- `%414`-`%416`'s family again. *Repaired (`20260906.020024`), the legibility half:* `claude_refresh_dead()` reads `refreshTokenExpiresAt` against the clock and the launcher prints a **named** refusal telling the operator to `/login` on the host rather than seeding a credential guaranteed to fail. Proven three ways in a pen: a dead refresh token trips it, an expired **access** token does **not** (it renews by design; tripping there refuses every ordinary credential), and a missing field fails open -- a probe that cannot read must never issue a death sentence. `tools/fixtures/f/fleet_login_scan.sh` answers it in one command: pier access, pier refresh, trees seeded, and **trees sharing one token**. *Corroborated `20260906.022430`, by the ship that did NOT die:* **petrichor holds no credential copy and no jail line in its transcript** -- it runs unjailed, reading the pier's single credential live -- and it is the one ship still sailing through the outage and after it. Seven ships holding seven copies died together; the one reading the original did not. That is the copy model's own prediction, met without being aimed at. *And the manual repair measured, because it did not do what it looks like it did:* six per-folder `claude` + `/login` runs wrote **nothing new** -- the whole pier holds exactly two credential files, `$HOME/.claude/.credentials.json` and this tree's seeded copy, both stamped `01:53` and neither touched in the 90 minutes since. `claude` on the host reads `$HOME` **whatever directory it is run from**, so a per-folder login is the same login six times and the last write wins; **copal reading *already logged in* is that mechanism visible from the outside**, and the only ship that showed it is the one that followed a successful login. A genuinely per-ship session must be made where `$HOME` differs, which is **inside the ship's own jail**. *That recipe was published untested and it wants two corrections (`20260906.023800`).* **First, the mechanism does hold:** the dry-run plan binds `loops/claude` over `~/.claude` **after** the `--tmpfs /home/keeper` that would otherwise swallow it, so a login inside the jail persists to the tree -- read from `agent-jail.sh --dry-run`, which runs without nesting. **Second, `/logout` was the wrong verb and could have reproduced the outage:** a logout may revoke the OAuth session **server-side**, and that session is the one every ship shares, so logging out inside one jail could log out the fleet. The safe form removes only the local copy: `rm ~/.claude/.credentials.json` **inside the jail**, then `/login`. **A recipe that touches shared state must say which half is local.** Copal's `loops/` is untouched since `20260905 18:24`, so the launcher never ran there and nothing was risked. *Measured `20260906.025440`, and PARTLY RETRACTED by `%458`:* copal holds **no credential copy** and a `loops/` untouched since `20260905 18:24`, and `trees_own_session` reads **0** -- those stand, read from peer trees bound in read-only. **What does not stand** is everything this seat said about *the pier's* credential: inside the enclosure `$HOME/.claude` is incense's own `loops/claude`, one file at two paths, so the pier's credential was never examined and the claim that a login *went nowhere a ship reads* was drawn from a tree that was never looked at. The pier half of this row is **unmeasured from here** and wants an unjailed shell. *Not repaired, and it is the larger half:* the fleet still shares one credential. One login per ship is the fix and it is **Keaton's hand at custody gate 3**. The scan **reports** `trees_sharing_token` rather than gating it: a guard that reds on the only shape the fleet can take is one someone turns off. **OPEN.**

---

## What the pattern says

Read down the *what caught it* column and a guard, a stopwatch, or a second look catches almost
everything; read down *what it taught* and the same few rules return. Both readings rest whole on
[`archive/20260908-161500_reds-why-this-file.md`](archive/20260908-161500_reds-why-this-file.md).

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

**REDS (`20260917.194613`) -- a bare `tail -n 200` dropped a guard's own header, and the ceiling read stale for want of it.** *What went wrong:* [`tools/fixtures/s/standing_equipment_run.sh`](../tools/fixtures/s/standing_equipment_run.sh) captured every red guard's evidence with `tail -n 200 "$pen/out.$$" > evidence.txt`, and `rye_compiled_reach`'s own 812-line answer opens with three header lines -- `paths=1994 bodies=1761 ceiling=18` -- then 791 lines of `uncompiled ...`, then `verdict=over_ceiling`. A plain tail keeps the last 200 lines: the tail of the list and the verdict, never the header that gives the list its scale, so the roster's stored evidence for this guard carried no ceiling, no bodies count, and no paths count at all. *What caught it:* a lap reading `construction/standing-equipment-reds/rye_compiled_reach.txt` on disk and finding a bare list with nothing above it, then running the guard directly and counting 812 lines against the 200 kept. *What it taught:* most guards' full answers already fit inside 200 lines, so a bare tail is silently correct on nearly every roster row and silently wrong on the rare long one -- which is why the fault stood unnoticed since the evidence room was built. *Repaired:* `capture_evidence` in [`tools/fixtures/s/shell_portable.sh`](../tools/fixtures/s/shell_portable.sh) keeps a 40-line head, an explicit `... N lines omitted ...` count, and a 159-line tail inside the same ~200-line bound; the runner's one call site now reaches it. Proven on the real guard's own 812-line answer (header and verdict both kept, 200 lines total) and by six planted legs in [`tools/fixtures/s/shell_portable_control.sh`](../tools/fixtures/s/shell_portable_control.sh), including the elder bare tail run against the same fixture to show it drops the header the new helper keeps. **CLOSED** -- both witnesses GREEN on metal.

**REDS (`20260917.221030`) -- a second scan's field broke a first scan's fixed placard order, on one shared desk.** *What went wrong:* `ba8747d2d` gave eight shared desks a new `::  law` placard line for `glow_gate_law_agree_scan.sh`, including `src/shape/shape-tablecloth-catalog-capacity.glow`. `tablecloth_glow_tend_scan.sh`'s `placard_of()` reads a fixed first six placard keywords by position; `law` pushed `nib` out of the window, so it read `placard_wrong` with no plant at all. *What caught it:* running the control cold, ahead of an unrelated plant-adoption lap -- `wrong=40` on the unmutated baseline. *What it taught:* a positional first-N reader cannot tell a new field from a lost one; checked for a wider hit (grepped every scan sharing the `expect_order` literal against all 25 `law`-bearing desks) and found none. *Repaired:* `placard_of()` skips an optional `law` line first. Proven: 44 legs, `wrong=0`, witness GREEN. **CLOSED.**

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, since a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson turned on the note itself. **The pin keeps what is open.***

