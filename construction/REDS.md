# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Season - opened `20260729`

*Rows %1 through %172 are folded, and their pointers are gathered in [`archive/REDS-fold-index-rows-1-172.md`](archive/REDS-fold-index-rows-1-172.md) -- one line per fold, every path resolvable.*

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 189 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 169** -- counted from the ledger and its archives on `20260824.084007` by `tools/gen/season/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. Rows 1-24, 25-57, 58-73, 74-79, 80-87, 88-95, 96-100, 101-107, 108-109, 110-111, 112, 113-114, 115-116, 117-118, 119-120, and 122, 123, 124, and 129-130, 131-132, 133-134, and 135-136, 148-149, and 150-151, 155-156, 157-158, 159, 160, 161, 162, 177-178, and 179 folded to `archive/` -- the last three on `20260821.232733`, when the living pin reached its 24,576-byte bound and the ledger's own guard said so, the elder closures for 81, 83, 84, 98 folded on `20260822.043742` ([`archive/REDS-elder-closures-rows-81-98.md`](archive/REDS-elder-closures-rows-81-98.md)) when the pin passed its bound again as row 129 was booked, the line-citation and spelled-number rows 142 and 143 folded on `20260822.194931` ([`archive/REDS-line-citation-and-spelled-number-rows-142-143.md`](archive/REDS-line-citation-and-spelled-number-rows-142-143.md)) when row %145 pushed the pin past its bound again, and the closed prose notes for 73, 74, 75, 78, 80 folded beside them on `20260819` ([`archive/REDS-closed-prose-notes-73-80.md`](archive/REDS-closed-prose-notes-73-80.md)). Every number from 1 to 189 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*Row 170 folded to [`archive/REDS-the-prevention-off-the-roster-rows-170.md`](archive/REDS-the-prevention-off-the-roster-rows-170.md) on `20260823.152322` -- REDS %83's own ASCII prevention guard standing off the roster while the card it protects carried four non-ASCII lines, CLOSED, moved here on the lap it was booked. Its lesson is carried forward by row %171, which measures the same coverage gap at a wider scale.*

*Row 171 folded to [`archive/REDS-the-sample-nobody-drew-rows-171.md`](archive/REDS-the-sample-nobody-drew-rows-171.md) on `20260823.171000`, **CLOSED** -- the random draw of 86 witnesses across 29 rooms, and the two rungs it left open, both now closed by row %173. Moved here as %173 carried the living pin past its 24,576-byte bound. **Living rows 172 onward below.***

*Row 172 folded to [`archive/REDS-the-roster-that-made-itself-red-rows-172.md`](archive/REDS-the-roster-that-made-itself-red-rows-172.md) on `20260823.174500`, **CLOSED** -- running the roster manufactured the artifact the next run reported, and the scan asks `git check-ignore` now rather than guessing which rooms are generated. Moved here as row %174 carried the living pin past its bound. **Living rows 173 onward below.***

*Row 173 folded to [`archive/REDS-the-machine-with-no-screen-rows-173.md`](archive/REDS-the-machine-with-no-screen-rows-173.md) on `20260823.174500`, **CLOSED** on the lap it was booked -- a Wayland application on a headless pier reading as a broken tree, and the shared `display_seam_probe.sh` six callers now gate on. Moved here as row %174 carried the living pin past its bound. **Living row 174 below.***

*Row 174 folded to [`archive/REDS-the-roster-ran-on-one-tree-rows-174.md`](archive/REDS-the-roster-ran-on-one-tree-rows-174.md) on `20260823.204456`, **CLOSED** -- the build cache that reached main because the roster ran before `git add -A`, and the rule it gave its final shape: stage first, then measure, then read what you staged. Moved here as row %175 carried the living pin past its bound. **Living rows 175 onward below.***

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Row 175 folded to [`archive/REDS-the-sprig-that-was-optional-rows-175.md`](archive/REDS-the-sprig-that-was-optional-rows-175.md) on `20260823.212601`, **CLOSED** -- the canonical dated test required a sprig the session-logs law makes optional, so 237 logs read as living and sat in the repointer's write set. **Eight read-only scans still carry the narrow pattern**, named there and waiting on their own lap. **Living row 176 below.***

*Row 176 folded to [`archive/REDS-the-mirror-that-matched-perfectly-rows-176.md`](archive/REDS-the-mirror-that-matched-perfectly-rows-176.md) -- CLOSED.*

*Rows 177-178 folded to [`archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md`](archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md).*

*Row 179 folded to [`archive/REDS-the-name-that-was-taken-rows-179.md`](archive/REDS-the-name-that-was-taken-rows-179.md) on `20260824.062207`, **CLOSED** -- a new guard written straight over a tracked guard that already carried the name, caught by ` M ` where `??` belonged in `git status`. Moved here as row %184 carried the living pin past its bound. **Living rows 180 onward below.***



*Row 180 folded to [`archive/REDS-the-rule-written-as-arithmetic-rows-180.md`](archive/REDS-the-rule-written-as-arithmetic-rows-180.md) on `20260824.071500`, **CLOSED** -- the room fold's dated test still required a sprig, written as a byte comparison rather than a regex, so the spelling guard passed truthfully over the file carrying the fault.*

*Row 181 folded to [`archive/REDS-the-citation-that-named-nothing-rows-181.md`](archive/REDS-the-citation-that-named-nothing-rows-181.md) on `20260824.072600`, **CLOSED** -- a round that widened the dated rule cited two files that exist nowhere, caught by the roster run after `git add`.*

*Row 182 folded to [`archive/REDS-the-index-that-grew-alone-rows-182.md`](archive/REDS-the-index-that-grew-alone-rows-182.md) on `20260824.075409`, **CLOSED** -- the room that folded while its index grew alone, to 2,895,849 bytes against a declared 24,576, and the index fold that seated the 39th guard. Moved here as row %187 carried the living pin past its bound. **Living rows 183 onward below.***

*Row 183 folded to [`archive/REDS-a-fold-moves-depth-rows-183.md`](archive/REDS-a-fold-moves-depth-rows-183.md) on `20260824.075409`, **CLOSED** -- the shelf writer that moved a row's text without its depth, and the rule that repaired it: fix the tool and re-run from a clean base rather than patching its output. Moved here as row %187 carried the living pin past its bound.*

*Row %184 folded to [`archive/REDS-the-ladder-that-named-73-rows-184.md`](archive/REDS-the-ladder-that-named-73-rows-184.md) on `20260824.082144`, **CLOSED** -- the ladder table that named 73 of 110 rungs, and the 40th standing guard it seated. Moved here as row %188 carried the living pin past its bound.*

*Row %185 folded to [`archive/REDS-the-rule-that-reached-95-rows-185.md`](archive/REDS-the-rule-that-reached-95-rows-185.md) on `20260824.084007`, **CLOSED** -- a rule-shaped roster reaching 95 of 98 modules, and the 41st standing guard that reads the suffix rule from the page itself. Moved here as row %189 carried the living pin past its bound. **Living rows 186 onward below.***

**REDS %186 (`20260824.072600`) -- a REDS row folded to archive carried the pin's relative links one directory down.** *What went wrong:* row %180 was folded to `construction/archive/` so the living pin would hold under its 24,576-byte bound, and its body's markdown link `../tools/rye/session_logs_archive.rye` was written for `construction/REDS.md`. From `construction/archive/` that same text names `construction/tools/`, which does not exist. **REDS %183 taught exactly this three hours earlier**, about index shelves, and the lesson reached no further than the room it was learned in. The repair then over-applied in the same shape %183's patch did: a regex rewriting every `](../` also caught the header's `](../REDS.md)`, which the archive destination had already written correctly. *What caught it:* `readme_reach`, on the roster run **after** `git add`, printing `broken_in_living=1` and naming both the file and the target. The cold run that opened the lap read 40 of 40 green because the file did not exist yet, which is the whole of why REDS %174 seated the second run. *What it taught:* **a fold moves depth, and a blanket rewrite moves what was already right.** The first half is %183's, and what is new is that the ledger's own fold is a fold -- a habit learned in one room does not travel to another by being true, only by being written where the next hand will read it. The repair shape both halves want: rewrite the links the fold moved, and leave the ones the destination already wrote. CLOSED, with every link in the folded file resolving from its own directory.

**REDS %187 (`20260824.075409`) -- the meter's own roster named nine module front doors while thirty-four stood in the tree.** *What went wrong:* [`../tools/fixtures/living_docs_lint_roster.sh`](../tools/fixtures/living_docs_lint_roster.sh) decides which living pages `living_docs_lint` reads, and it was a hand-written list of **32 entries**, nine of them module READMEs named one per `for` loop. The tree holds **34** module front doors -- a tracked `README.md` sitting beside a tracked `.rye` source -- so the meter reached **6** of them and reported green over the other 28 for as long as it has run. The four largest documents anywhere in the tree were among the unread: `image/README.md` at **400,042** bytes over 227 `.rye` sources, `lotus/README.md` at **297,878** over 240, `crypto/README.md` at **88,205** over 87, and `constel/README.md` at **69,979** over 31 -- every one past the **24,576** the pin-and-ledger law declares, and none of them ever measured. Two rostered hammocks named files that had folded to `active-designing/yonder/` on an earlier lap and were never repointed, so the roster also pointed at two absent paths. The arithmetic of the blind spot, read from both sides on metal: against the departing roster the coverage scan reads `over_bound=3`, and against the tree it reads `over_bound=7`. **Four over-bound documents were invisible to the meter that exists to find them**, and the ranked remainder's own table of *four living documents over the bound* was measured from the roster rather than from the tree. *What caught it:* `wc -c` over every tracked `README.md` while answering the question the previous lap's `recommend` line left -- whether `docs/CRYPTO.md` is a door or a reference. `image/README.md` and `lotus/README.md` came back first and second, above `session-logs/README.md`, and neither was on any roster. *What it taught:* **a roster that decides what a meter reads wants the bijection most, because everything downstream inherits its blind spot.** This is one lantern's third firing in five hours -- a ladder table naming 73 of 110 rungs (`%184`), a map's suffix rule reaching 95 of 98 modules (`%185`), and now a meter reaching 6 of 34 -- and [`../tools/fixtures/index_fold_scan.sh`](../tools/fixtures/index_fold_scan.sh) had already written the answer into its own body four hours earlier: *discovery rather than a roster, so a room that folds tomorrow is measured the same way.* The lesson reached no further than the room it was learned in, which is `%186`'s finding arriving again on the lap after it was booked. *Repaired:* the roster **discovers** -- it asks `git ls-files '*.rye'` for every directory holding a tracked source, takes the tracked `README.md` beside it, and excludes only `tools/fixtures/`, whose corpora are planted to be read by scans rather than entered by readers. It grows from 32 entries to **60**, reaches all 34 front doors, names no absent file, and drops nothing: `rye/`, `rishi/` and `aurora/` keep their sources one level down where the rule cannot see them and stay listed by hand, since a page dropping off a meter is a page whose pass nobody witnessed (`%170`). The two hammocks are repointed to their `yonder/` homes. `living_docs_lint` duty 6 now names **seven** pages over bound where it named three, and duty 2 reads **clean over all 28 newly-measured doors** -- not one broken relative link among them. *Prevention* seated as the **42nd** standing guard: [`../tools/l/living_docs_roster_witness.rish`](../tools/l/living_docs_roster_witness.rish) over [`../tools/fixtures/living_docs_roster_scan.sh`](../tools/fixtures/living_docs_roster_scan.sh), which asks the tree the same question **independently** of the roster and gates `unrostered` and `phantom` at zero, so a roster that regresses to a hand list reds on the lap it is written. The over-bound count rides a ratchet under a ceiling that only falls, because splitting a 400,042-byte front door is a round of its own -- Caravan's took a whole lap and found a red beneath it. The honest cost is named in the scan: `glow/README.md` sits at **96%** of bound, so ordinary growth will raise the ratchet, and the repair is the fold the pin law already names. Proven RED on metal against the departing roster (`unrostered=28 phantom=2`, exit 1) and GREEN after, and both ways by [`../tools/fixtures/living_docs_roster_control.sh`](../tools/fixtures/living_docs_roster_control.sh) -- **sixteen behaviors on real git repositories** in a throwaway pen, five refusals bitten and eleven honest readings left free, the ceiling shown from both sides by planting seven over-bound pages and then eight. Three cases carry the load: a front door dropped from the roster, an **untracked** README beside a tracked source passing free (which proves the tree is asked with `git ls-files` rather than globbed, since a glob would manufacture a door out of a scratch file), and a roster shortened back to a hand list. CLOSED as to the coverage fault; the seven over-bound pages are named on the ratchet and each is its own round.

**REDS %188 (`20260824.082144`) -- the lap staged its work and stopped, so three guards that read the tree went red on the next lap's cold open.** *What went wrong:* the round that seated the 42nd standing guard ended with eleven paths staged and nothing committed. Three consequences followed, and each is a guard doing exactly its job one lap later than it could have. `readme_metrics` read the front door's generated block at **1669** witnesses while a fresh render read **1670**, because the round had added one. `geode_libraries` read one witness short in five rooms at once -- constel 33 against 34, crypto 114 against 115, glow 104 against 105, image 246 against 247, lotus 239 against 240 -- for the same reason. And `tracked_link` refused `construction/ITINERARY.md`, which cites `../active-designing/20260824-080208_the-roster-that-decides-what-gets-measured.md`: the essay was written, cited from a living card, and never added, so the promise resolved on this pier and would break in every clone. *What caught it:* the cold roster run that opens a lap, reading **42 guards, 39 green, 3 red**, against the previous lap's own cold reading of 41 of 41 green. *What it taught:* **staging is not shipping, and this tree's repair machinery is keyed to the commit rather than to the edit.** Two of the three reds are precisely what [`../tools/hooks/pre-commit`](../tools/hooks/pre-commit) exists to prevent -- it regenerates both generated pages and stages them into the same commit whenever a witness is added, seated for REDS %151 and %152, which are this same class caught twice before. It is armed here: mode `100755`, with `core.hooksPath` naming `tools/hooks`, and [`../tools/g/generated_page_freshness_witness.rish`](../tools/g/generated_page_freshness_witness.rish) proves it on throwaway repositories. It fires at `git commit`. A lap that ends at `git add` stops one step before the step that makes its own tree true, and the standing directive that would have named the gap anyway -- *roster cold, then hot* (REDS %174) -- was not run hot. *Repaired:* `git add -A` for the essay; both generated pages regenerated with their own writers (`readme_metrics.rish write`, `geode_libraries.rish write`); all three guards GREEN on metal, and the whole staged round shipped as one signed commit whose pre-commit hook found nothing left to refresh. *Prevention:* already whole, which is the honest finding here rather than a gap. The generated-page half has its hook and its witness; the citation half has `tracked_link`, doing its founding job from a new direction -- a file never added rather than a symlink gone stale. What was missing was reaching them, so the operator card's standing block now says a lap ends at the commit rather than at `git add`. **No guard can enforce that**, because such a guard would have to run after the lap ends, and naming the limit is better than pretending a check covers it. Measured beside it, since the reason a directive gets skipped is usually its price: the three census guards together run in **67 seconds**. CLOSED.

**REDS %189 (`20260824.084007`) -- the largest page in the tree named 112 of the 227 modules standing beside it, and two of the 112 lived somewhere else.** *What went wrong:* `image/README.md` stood at **400,042 bytes** over 288 lines -- the largest document anywhere in this tree, and the page a reader enters the Open Image module by. It named **112** distinct `.rye` files while **227** stand in `image/`, so **117 modules had landed with nobody adding a line**: the whole Season G shape-analysis, selection, and scene-structure arc, from `threshold.rye` and `label.rye` through `watershed.rye`, `hu_moments.rye`, the nine `click_*` gestures, the four `lasso_*` gestures, the fourteen `cluster_*` modules and the thirteen `line_*` modules. Two of the 112 were worse than absent: `dexter_line.rye` and `scooter_compose.rye` were cited by bare name beside local ones, and both live in `pond/apps/`, so the page read as if they stood in this directory. The page had also never joined the DOOR roster, so no meter had read a line of it. *What caught it:* `wc -c` over every tracked `README.md` on the previous lap (`%187`), then `comm` between `ls image/*.rye` and the names the page cites -- 112 against 227 in one line. *What it taught:* nothing new, and that is the finding. **This is REDS %184 at three and a half times the scale, in the directory the previous lap's own ratchet pointed at.** A roster a program runs gets checked; a roster a reader uses gets checked by nobody. **248** `*_witness.rish` files across the tool rooms name a path under `image/` -- 247 before this round's own guard joined them -- so not one of the 117 modules was unproven, only unlisted. The new half is a corollary worth writing down: **a page that names some of a directory is read as naming all of it, and the larger the page the more completely it is believed.** At 400,042 bytes nobody was going to count. *Repaired:* the page split into a **Door front page of 6,928 bytes** and four pages beside it in the same directory, so every relative link inside them stays true -- [`../image/PHOTOS.md`](../image/PHOTOS.md) (the gesture arc), [`../image/MARKETPLACE.md`](../image/MARKETPLACE.md) (the catalog and its query algebra), [`../image/PRIMITIVES.md`](../image/PRIMITIVES.md) (the Season G color and type floor), and [`../image/MODULES.md`](../image/MODULES.md), which carries **227 rows in twelve families**, each sentence written from that module's own `//!` head comment. The three `pond/apps/` citations are repointed to `../pond/apps/` so a bare name never again reads as local. *Prevention* seated as the **43rd** standing guard: [`../tools/i/image_module_roster_witness.rish`](../tools/i/image_module_roster_witness.rish) over [`../tools/fixtures/image_module_roster_scan.sh`](../tools/fixtures/image_module_roster_scan.sh), gating `unrostered`, `phantom`, `duplicate_rows` and `mismatched_rows` at zero, matching the **Module column** rather than any `.rye` link, and refusing an empty corpus so a reading over nothing can never report clean. Proven RED on metal against the real page by deleting the `watershed.rye` row (`unrostered=1`, `verdict=roster_incomplete`) and GREEN after, and both ways by [`../tools/fixtures/image_module_roster_control.sh`](../tools/fixtures/image_module_roster_control.sh) -- **fifteen behaviors on real directories** in a throwaway pen, nine refusals bitten and six honest readings left free. Three carry the load here rather than in Caravan: a name carrying digits binds its row (forty-eight `font5x7_*` modules do), a **seam symlink counts as a module** and is refused when unrostered (`parse_int.rye` and `tally_copy.rye` point into `../tally/`), and a page naming half its directory counts every absentee. *And the meter bit the repair on the lap it was written:* the new Door page joined `tools/fixtures/prose_register_scan.sh`'s DOOR roster and read **27% negative sentences against the 20% ceiling**, swept to **13%** with every claim held exactly -- `%185`'s rule working the first time it was given the chance. The over-bound ratchet falls **7 to 6**, and its control now plants six free and seven bitten. CLOSED.
