# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes[construction/REDS.md]` (40960, raised `20260906.022430` on Keaton's word -- 8 ships x 4,096 for the OPEN set, plus 8,192 for header and census; measured that night at 6 open rows, mean 3,506, `%360` alone 8,213. Every CLOSED row had already folded and `reds_fold.sh` refuses an open one by name, so the alternative was a ninth shaving pass. The general bound stays 24,576.)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 496 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 476** -- counted from the ledger and its archives on `20260906.141815` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 496 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %493 (`20260906.133724`) -- a `tier lap` guard whose precondition only its author's checkout satisfies reds every other tree in the fleet, every lap.** *What went wrong:* `tools/s/sow_allow_reach_witness.rish` (`%489`, `20260906.122632`) opens *refused: no projection at `seed/`*, and `seed/` is a gitignored projection no lap makes; here the pass closes `guard_red`, the receipt is withheld, and `--scoped` then refuses -- a full cold pass every lap, 826s. *What caught it:* a re-verify on a tree other than the one that seated it. *What it taught:* **a precondition is part of a claim.** The roster's `capability` field already answers this. Owning lane's. **OPEN.**

**REDS %492 (`20260906.131501`) -- a witness proves what it CALLS, and prints GREEN over the module it claims.** *What went wrong:* Zig analyses lazily, so a witness importing a module and calling two of its functions forces exactly those two bodies through semantic analysis and walks past every other declaration. A public function carrying a flat type error therefore rides through the build untouched while the witness, its claims, and its own control all read GREEN. *What caught it:* planting one. A `pub fn` assigning a `u32` to a `[]const u8` was appended to `mantra/src/weave.rye`, nothing else was touched, and `tools/m/mantra_weave_merge_witness.rish` read **GREEN** -- including the five-break control that exists to prove that witness can red. **The file next door to `%470`'s**, guarded by the sibling of the witness whose own header names this gap: *the tree-wide instrument for this question is a peer's lap*. *What it taught:* **`%470` said five guards are no safer than one when all ask the same KIND of question; this is the same sentence one level up -- 130 witnesses asked the same kind, and one asked a different one.** A green is a claim about what ran, and a witness's name is a claim about a module; only a walker makes the second follow from the first. *The measurement:* of **134** tracked `*_witness.rye`, **130** import a tracked sibling, **121** name a subject, **9** are ambiguous -- and `walked` stood at **1**. *Repaired:* the four-line comptime walker landed in `mantra/src/weave_merge_witness.rye` (**walked 1 -> 2**), proven three ways on metal -- the elder sails past the plant, the new one reds on it, and the new one still greens on honest bytes. `rye_witness_walker_scan.sh` gates `unwalked` at **119, no slack**, so the next unwalked witness reds on the lap it arrives; `rye_witness_walker_witness.rish` rostered `tier lap` at **24s**; control **37 behaviors**, the ceiling proven both ways by planting 120 pairs rather than by an override the scan does not offer. *Three faults the control found in the census itself, each a lesson this tree had already paid for:* the `@typeInfo(m).@"struct".decls` pattern read the tree's only real walker as **absent**, since the quoted field name's own letters defeat a character class trying to skip it; the stem rule read `weave_merge_witness.rye` as claiming **nothing**, so the very file the census was built from fell outside it, fixed by crediting a witness that imports exactly one sibling; and two refusals **exited before printing what they read**, which is `%480` one guard over. *The residue, published rather than dropped:* `ambiguous` names the 9 that do not say which module they prove, and `unreached` carries the walker's own condition -- a walker in a file no build compiles forces nothing -- gated at zero and reading **-1** where the compile-reach resolver cannot answer, since an unknown belongs published as unknown (`%488`). **BOOKED** *(the ratchet is the remainder: 119 witnesses claim a module they only partly analyze, and each falls on touch).*

**REDS %491 (`20260906.120905`) -- a fold's own output wears the one property every link guard uses to decide it is testimony and stop gating, so the fault that breaks a link is the fault that hides it.** *What went wrong:* `construction/ITINERARY.md` sits in `construction/`, so a link it writes as `](archive/X)` or `](../Y)` is correct there and broken the instant that text is folded onto `construction/archive/`. `%474` measured sixteen such links, repaired them, and watched the fault fire twice more in the same lap under the hand that had just written the mechanism down. Measured `20260906.120905` over **276 tracked shelves and 1,975 links: 345 stood depth-lost**, across four files -- 288 in `tasks-season-ledger`, 52 in `roadmap-season-ledger`, 3 on a shelf written at 09:23 THIS MORNING after the `%474` repair at 09:09, 2 in an elder open-threads. The class reaches back to July: both season ledgers are `construction/TASKS.md` and `construction/ROADMAP.md` folded down one directory in `20260724`. *What caught it:* taking `%474`'s booked loom and measuring the class before building for it, rather than building to the sixteen instances the row had named. *Why four guards miss it, and three for ONE reason:* `tracked_link` reads only *resolves here, breaks in a fresh clone* AND reads past any stamped basename; `readme_reach` gates `broken_in_living` and deliberately only REPORTS `broken_in_testimony`, calling a file living when its basename carries no one-clock stamp; `living_docs_lint` reads a roster of living pages with zero rows in that room; `reds_fold` re-anchors perfectly, for the shelves its own loom writes. **A fold's output is stamped, stamped means testimony, and testimony is where the guards agree to stop** -- all 345 stood in stamped files and zero in the unstamped `REDS-*` shelves. *What it taught:* **the property that makes a file a fold shelf is the same property that makes every instrument let it go**, which is why three independent guards agreed and all three were blind together. *Why gating testimony is lawful here and nowhere else:* a STALE reference was correct when written and broke because its target moved -- resolved, never rewritten. A DEPTH-LOST link was never correct in the file it sits in; the fold broke it in the same act that created the file, so repairing it restores what the text already meant. The two are told apart **mechanically**: depth-lost resolves under exactly one card-to-shelf correction, stale under neither. **Ambiguity is impossible by construction** -- `archive/` and `../` are disjoint prefixes -- so no repair ever guesses, which is a stronger claim than the `ambig=0` a measurement gives. *Repaired:* [`../tools/fixtures/f/fold_shelf_link_scan.sh`](../tools/fixtures/f/fold_shelf_link_scan.sh) gates `fold_depth_lost` at zero and REPORTS `links_dead` (**737**, the stale class), over [`../tools/f/fold_shelf_link_witness.rish`](../tools/f/fold_shelf_link_witness.rish), rostered `tier lap` at **5s**. [`../tools/fixtures/f/fold_shelf_link_repoint.sh`](../tools/fixtures/f/fold_shelf_link_repoint.sh) applies the one correction each has -- **a blanket sed cannot**, since one shelf held 288 depth-lost links beside 401 stale ones and would have turned 401 honest breaks into 401 differently-wrong ones. All 345 repaired by that program, then **proven to have moved no prose**: with every `](target)` masked, all four files hash **identically** before and after. **50 behaviors** in [`../tools/fixtures/f/fold_shelf_link_control.sh`](../tools/fixtures/f/fold_shelf_link_control.sh) over 18 pens, every refusal lifted in its own pen, the instrument proven blind-able. *And the guard found a red in itself on its FIRST real use:* `git ls-files` reads the INDEX, so a shelf renamed mid-rebase is listed while nothing stands at that name, and the awk died with `fatal: cannot open file` -- **a tree mid-rebase is exactly when this reading matters most.** Absent listed paths are skipped and COUNTED now (`shelves_absent`), since a reading that quietly got smaller is `%463`'s own fault, and a pen proves it by deleting a committed shelf. *A red of my own, inside the pen:* the first control spelled an absence with the presence helper and printed `no_shelves_never_ok=no` -- **an assertion whose name claims one thing while its value means the opposite**, which a witness gates as failure and a reader gates as nothing; an absence has its own word now. And its `git rm` of the last shelf took the directory with it, so the phase reached `no_room` rather than the `no_shelves` it meant to build -- **a pen that cannot reach a state proves nothing about it.** *Named, not taken, with a number:* the five rooms folding two levels deep carry the same class wanting `../../../`; sampled at most 400 files per room, active-designing **730**, counsel **99**, expanding-prompts **34**, waymarks **27** -- at least **890** instances, its own campaign. Closes `%474`'s booked loom. *Erratum:* booked `%486` from a fetched `--next`; a peer had booked `%486` too, stamped `20260906.120134` against this row's `20260906.120905`, and shelved it first -- the earlier stamp keeps the number, so this row moved to `%491`. The renumber ran line by line against an owner classifier over seven sites, because `REDS-fold-recital.md` carries both their `%486` line and this one and a sweep would have rewritten a peer's published record. **CLOSED.**

**REDS %489 (`20260906.122632`) -- the seed allowlist had a second list quietly subtracting from it, so a room could be allowed, projected, and proven clean while shipping nothing.** *What went wrong:* `tools/fixtures/s/sow_project.sh` filtered the allowlist through `grep -vxE 'gratitude|vendor'` -- a hard exclusion **by name**, running after it and winning silently. On Keaton's word I added `allow gratitude`, a `scrub` verdict, and withholds for a saved article (copyright) and all **20** gitlinks (licence). Projection: `copied=7603`. `sow_witness`: **GREEN**. `ls seed/gratitude`: **0**. *What caught it:* counting the shipped directory rather than trusting the verdict. *What it taught:* **a GREEN that a missing room cannot fail is not evidence about that room** -- IDENT_CLEAN over an empty set is a true statement about the wrong corpus, the hazard this same file names at line 32 for a different cause. An allowlist with a second list subtracting from it is `%409`'s two-copies fault, and here it publishes a lie by omission rather than a leak. *Repaired:* the filter keeps `vendor` alone -- unmodified build source, whose shipping would republish rather than cite -- with the reason beside it. Re-projected `copied=7726`, `seed/gratitude` **77**, three withheld classes absent, **zero** personal markers, IDENT_CLEAN and NO_PERSONAL green. *Gated:* `tools/s/sow_allow_reach_witness.rish` asks the direction no seed guard could -- **did the room we allowed arrive** -- reading the manifest rather than a list of its own, `empty` at zero, proven both ways (elder filter `empty=1` naming `gratitude`, living `empty=0`). Its first run found a **second** case that taught it the right shape: `context/LEXICON.md` is allowed and never ships, an identity string surviving the scrub so defence in depth drops the copy -- **absent loudly**, logged, reported as `withheld_by_design` rather than gated. The seed's readers have no Lexicon and nothing said so; that is its own lap. *Erratum:* booked `%485`, renumbered on a rebase that found three peer rows published in the same hour. **BOOKED.**

**REDS %487 (`20260906.120137`) -- a POSIX trap handler that cleans up without exiting does not stop the script, so it carries on against the scratch its own handler just deleted -- and the proof of it sat unlanded in a stash across two dead laps.** *What went wrong:* `trap 'rm -rf "$pen"; lock_release "$lock"' EXIT INT TERM` at `tools/fixtures/s/standing_equipment_run.sh:226` reads as *clean up and stop* and behaves as *clean up and carry on*: POSIX runs the handler and then RESUMES execution where the signal landed. Measured at `15f99e1fe0`, **143** command-position traps naming INT or TERM stood across tracked shell sources and exactly **one** called `exit` -- 142 to 1 rather than a scatter, because `tools/fixtures/s/shell_portable.sh`'s own header taught the broken form beside `lock_acquire`, so the shape had one source and spread by being read. Of the files carrying one, **48** have no `set -e`: those do not lose a verdict, they run to COMPLETION and publish a total counted from a directory that is gone. *What caught it:* a cold roster pass dying at guard 34 with one line about a missing temporary, read up close rather than as weather -- and then, two laps later, `stash_record` standing as the **only** red on this tree and naming the very stash that held the repair. *What it taught, three things.* **The observed half:** the fabrication is real rather than inferred -- `rye_comment_ascii_control.sh` and `shell_comment_ascii_control.sh` both completed at **exit 0** after a TERM delivered the instant they created their scratch, publishing `files=0 chars=0` where clean reads 4 and 7. **Where the severity lives:** a fabricated zero costs nothing unless something believes it, and belief is a property of the WITNESS rather than of the script the signal hit -- a witness asserting a **count** reds when the count collapses, while one asserting only `under_ceiling=yes` reads MORE true once the evidence is gone. **And this one bit in my own hands:** counting the believers by matching reader filenames against `(witness|suite)` answers **8**; counting by reference -- does any runner outside `tools/fixtures/` name this basename -- answers **4**. The four extra are read by `tools/w/width-check.rish` and `tools/co/compass_rose.rish`, runners wearing neither word. That is `%465`'s own fault reproduced by the lap reading the row that books it, so the census this hands on has a named way to be wrong before anyone writes it. *Repaired:* both sites split into three traps -- EXIT cleans once, INT and TERM call `exit 130` and `exit 143` -- and the scan reads `lock_releasing 0`, `verdict ok`, where it read `lock_released_without_exit` an hour ago. `signal_trap_control.sh` proves **16** behaviors on real processes and real signals, including the scan's two directions and **a negative result kept as a leg** -- the first draft claimed a checkout's lock stands free while its pass runs on, and the pen refused it. `tools/s/signal_trap_witness.rish` rostered `tier lap`. *Not proven:* whether a wrong GREEN ever shipped from this -- answering it means replaying past roster runs against signals nothing recorded. Study: [`../external-research/20260906-102443_the-severity-lives-in-the-witness.md`](../external-research/20260906-102443_the-severity-lives-in-the-witness.md). *Booked `%467`, then `%477`, by two laps that died before their commit; both numbers went to peers, and the work existed in no other tree -- `path_absence_scan.sh` confirmed that against the anointed remote first.* **BOOKED** *(the zeroing census -- signal each vulnerable fixture, diff against clean, gate at zero whose witness would not notice -- is the remainder, bounded at two runs each).*

**REDS %483 (`20260906.113000`) -- the roster's own guard reads a run card that is written once, at the close of the pass, so a repair landed in this pass is invisible to it and the lap that fixes the last red still closes `guard_red`.** *What went wrong:* `tools/fixtures/s/standing_equipment_scan.sh` counts `runs_red` off the run card, and the runner writes that card **wholesale at the end of a pass** -- proven by the card's own stamps, `oldest_run=20260906.111808` equal to `newest_run=20260906.111808` across all 140 rows. So `standing_equipment`, at position 151 of 172 output lines, reads the **previous** pass's card no matter how late in the roster it sits. Measured in one lap: the cold pass at `20260906.105803` read `index_row_bound red` honestly; the shelf was repaired; the hot pass at `20260906.111808` read **`index_row_bound green` at line 60 and `standing_equipment red` at line 151**, and the same scan run by hand two minutes later read `runs_red=0 verdict=ok`. Nothing changed between those two readings but the card being finished. *What caught it:* a lap that repaired the only real red and still closed `guards_red=1`, and asked which guard rather than accepting the number. *What it taught:* **a guard whose evidence is a file its own pass writes at close is always one pass behind, and the direction of that lag is the expensive one** -- it reports a fault that is already fixed, on the lap that fixed it. `%475` closed the half where the guard counted its OWN verdict (`runs_red_self`, reported never counted); this is the neighbour half, where a peer guard's stale verdict is counted, and `runs_red_self` cannot see it. *The cost is already priced:* a red guard sets `roster_receipt_write=withheld_guard_red`, `--scoped` refuses without a full green receipt, so **every lap that closes a red pays the next lap a full cold pass** -- 900s and 687s measured here. *Not taken, and why:* the runner and the roster are another seat's, and two shapes fit -- write the card incrementally, or move that guard's roster row so it runs after the write. Naming which is that seat's ruling. *Booked `%480` from a `--next` read that was fresh, and a peer published `%480` first; renumbered on the rebase, the stamp holding, which is exactly what `.claude/rules/derived-spine.md` is for.* **OPEN.**

**REDS %463 (`20260906.045552`) -- a census that reads two files answers *nothing is wrong* when it has read nothing, and on this field that fault is invisible.** *What went wrong:* the first draft of `tools/fixtures/r/rye_compile_reach_scan.sh` -- the census `%449` booked -- loaded the symlink map with awk's `NR==FNR` idiom before walking the corpus. `NR==FNR` holds while the **first** file is read, and an empty first file is never read, so in a tree carrying no symlinks every corpus path fell into the link table and nothing was printed: `distinct=0`, `asserted=0`, `verdict=ok`, exit 0. **Five places in one scan** used it, the graph walk among them. This field's link map holds **227** entries, so all five read correctly and nothing showed. *What caught it:* the control's pens on their first run -- **19 of 50** planted behaviors failed at once, every one from that root, because a pen small enough to read is a pen built without symlinks. *What it taught:* **the tree a census is easiest to test on is the tree its worst defect hides in**, and a census answering zero of everything is indistinguishable from a healthy tree. `%442`'s family with the reading at **zero** rather than partial. Repaired by naming the file (`FILENAME == linkfile`) rather than counting records. *The measurement it delivered:* of **1,940** tracked `.rye`, **227** symlinks, **1,713** distinct; **1,535** reached by a build root or an import from one; **178** not -- **41** planted specimens under `fixtures/`, **114** tests in `rye/tests/` no runner names, and **one** carrying a runner's claim while nothing compiles it, `mantra/src/diff.rye`. **Every count is a floor:** a runner touching the compiler credits every path it names, since a root arrives by literal, variable, and function parameter. **Resolution before accusation:** without it, 17 symlinks read never-compiled and **16 pointed at a file that IS compiled**; links resolve through `git ls-files -s` and `git cat-file`, so no `readlink` is wanted. *And a claim of my own, retracted before it shipped:* I built `diff.rye` through an importing probe and called it type-checked. **That probe is blind** -- Zig analyses lazily and walks past a planted type error, proven by planting one. A peer measured the same blindness the same night against `%449`'s own broken bytes, where `rye build-lib` exits **zero**; the comptime declaration walker that answers is theirs. **This census answers only the graph question -- which files no build reaches -- and nothing about whether the reached ones are sound.** `rye_compile_reach_control.sh` proves **50 behaviors** across sixteen real repositories, the ceiling both ways and the pen proven innocent; witness rostered `tier lap` at **5s**. *Repaired further (`20260906.054500`), and this one is the sharpest of the three:* **the census disarmed its own gate by being committed.** Its source carries the compiler-detector patterns as literal strings and names `mantra/src/diff.rye` in a comment, so the moment `git ls-files` began listing it, the scan classified **itself** as a compiling runner and credited every path it mentions -- `asserted` fell **1 -> 0** on the commit that added the file, with nothing about the accused module changed. It read green because it was reading its own reflection and reporting the room empty, which is this row's own lesson turned back on the instrument that found it. The scan and its control are excluded **by name**, and a control case plants an orphan inside the scan's own comments and holds it accused -- proven from the failing side, where removing the exclusion answers `control_verdict=broken`. **53 behaviors now.** **OPEN** *(the accused file wants a probe beside it, and the comptime walker is the instrument).*

**REDS %460 (`20260906.040355`) -- Glow's two cross-target witnesses proved an architecture by reading `file`'s prose, never checked that the call had run, and on a pier without `file(1)` reported ten correct binaries as the wrong architecture.** *What went wrong:* `tools/g/glow_mobile_target_witness.rish` and `tools/g/glow_riscv_target_witness.rish` each spelled the check `run ["sh" "-c" "file ${bin}"]` and then asserted on a substring of `.out`. **Neither read `.ok`.** With no `file(1)`, `sh -c` exits **127** with empty stdout, an empty string satisfies no substring, so the assert fires -- *cross-built binary is not aarch64*. The binary was 4,719,664 bytes, built that minute, carrying `e_machine=183`. Ten sites, ten false claims. *What caught it:* running the seven Glow witnesses `witness_reach_scan.sh --list` reports **unheard** -- `%360`'s Glow remainder read at first hand rather than counted. Five GREEN, these two red; **neither had ever been run.** *What it taught:* **a guard that cannot run its instrument must not describe its subject** -- `%446`'s ruling one lane over, with a sharper edge: reading `.out` without `.ok` turns *the instrument is absent* into a **confident** false claim rather than a confused one, because an empty string fails every substring test the same way. *And the fix that was available:* a missing utility has two honest answers -- refuse and name it, or stop needing it. `e_machine` is two bytes at offset 18 in a format frozen since 1999, so the second was open; `od` is POSIX where `file` is borrowed, which moves the reading out of the thin edge into the happy zone. *Repaired (`20260906.040355`):* `tools/fixtures/e/elf_machine_scan.sh` reads magic, `EI_CLASS`, `EI_DATA` and `e_machine`, decoding in the byte order the header declares, refusing by name on five shapes. `tools/fixtures/e/elf_machine_control.sh` proves **29 behaviors** on planted headers, every refusal from the failing side, **the pen proven innocent** -- a scan patched to always answer `AArch64` fails it six ways. All ten sites then proven **on metal** -- nine RISC-V cross-builds and one AArch64, every header as claimed. `tools/e/elf_machine_witness.rish` rostered `tier lap` at **1s**, its census one awk pass over ~2,900 runners rather than two processes per file (**17 of an elder 18**). *Left standing:* three sites under `tools/h/` and `tools/t/`, each behind an Android or APK toolchain this pier cannot run -- **editing a guard I cannot run is the fault this row closes**. Census ceiling 13 -> 3, proven both ways on real state. *Not closed:* both witnesses still red at the **emulator** -- both qemu binaries absent, so the run-it half is unproven and says so by name. Their own head promises an honest limit their code never had, and the roster's `capability` field is the mechanism; changing what GREEN means is a design act, named rather than taken. **OPEN.**

**REDS %457 (`20260906.025440`) -- a whole-tree grep at a stale HEAD is the most convincing way to be wrong about a file's existence.** *What went wrong:* the copal seat was handed `tools/fixtures/f/fleet_login_scan.sh` to run and reported *"does not exist -- no file, no reference to that name anywhere in the tree, tracked or untracked"*, then reasoned onward from that absence to a conclusion about which meters the fleet owns. Every word was true of the bytes it could see. The file had landed **three commits earlier** and that checkout had not fetched; the seat found this itself, rebased, ran the scan GREEN, and rewrote its own log rather than leaving the wrong conclusion standing. *What caught it:* the seat, on its own next move -- which is the half worth keeping. *What it taught:* **absence is the one reading a seat must never take from local bytes alone.** A grep across tracked *and* untracked files is the strongest evidence a lap can gather about a path, and at a stale HEAD it is strong evidence about a tree nobody else is standing in. **On a fleet this is the ordinary case rather than the edge:** eight ships push to one remote, the round-open pulls at lap **start**, and a grep happens mid-lap -- so a checkout is behind within minutes of opening and every lap eventually asks a question whose honest answer requires a fetch. This is `%436`'s family -- *a finding that explains itself well is the easiest kind to publish unverified* -- with the twist that here the evidence was gathered correctly and the **corpus** was wrong. *Repaired (`20260906.025440`):* `tools/fixtures/p/path_absence_scan.sh` answers *absent, or behind?* per path -- it **fetches** the anointed remote first (an upstream reading from a stale remote-tracking ref is the same fault one layer down), prints `commits_behind`, and reports `here=` and `upstream=` for each path. `verdict=absent` requires **both** to say no; a path upstream holds and this tree does not reads `verdict=behind` and says so by name. Proven on all three legs against real state rather than a pen: a path present both ways, a name that never existed, and -- against the live one-commit divergence at the time of writing -- a peer's session log that upstream held and this checkout did not. **OPEN** *(the habit is unenforced: nothing yet makes a lap consult it before concluding a file is gone, and a guard on agent habit costs more than it returns while the habit is one lap old).*

**REDS %456 (`20260906.020024`) -- eight ships share one login, so one credential's failure is a fleet-wide outage, and every transcript could only say the session expired.** *What went wrong:* seven of eight ships died three laps apiece inside ten seconds on `Failed to authenticate: OAuth session expired and could not be refreshed`. `tools/ag/agent-jail.sh` seeds every tree from the pier's single credential in its own words -- *one login per pier* -- so the fleet holds **one refresh token in up to eight copies**. Measured after the repair login: access token ~8 hours, **refresh token 27 days**, so calendar expiry is excluded and the failure was a refresh *attempted and rejected*. *Leading explanation, unprovable from this side:* refresh tokens commonly **rotate**, so the first ship to refresh strands the other seven **and the pier's copy**, which then seeds the dead token onward. Evidence is circumstantial and consistent: one shared token, calendar expiry excluded, a rejection rather than an absence, and the outage arriving the night the fleet went three to eight. **Falsifier, cheap:** read the pier's refresh value now and again after a ship refreshes; unchanged means this row is wrong and the cause is server-side revocation. *What caught it:* Keaton, reading three tmux windows and asking why. **Nothing in the tree could answer** -- the state was legible only as seven transcripts saying the same words. *What it taught:* **a resource shared by every ship has no blast radius smaller than the fleet.** The enclosure gives each ship its own tree, keyring and transport keys (`%427`), then hands all eight the same API credential. *And:* `claude_login_live()` tested that an access token was **present and long enough**, never whether it could be renewed, so a dead credential read as live -- `%414`-`%416`'s family again. *Repaired (`20260906.020024`), the legibility half:* `claude_refresh_dead()` reads `refreshTokenExpiresAt` against the clock and the launcher prints a **named** refusal telling the operator to `/login` on the host rather than seeding a credential guaranteed to fail. Proven three ways in a pen: a dead refresh token trips it, an expired **access** token does **not** (it renews by design; tripping there refuses every ordinary credential), and a missing field fails open -- a probe that cannot read must never issue a death sentence. `tools/fixtures/f/fleet_login_scan.sh` answers it in one command: pier access, pier refresh, trees seeded, and **trees sharing one token**. *Corroborated `20260906.022430`, by the ship that did NOT die:* **petrichor holds no credential copy and no jail line in its transcript** -- it runs unjailed, reading the pier's single credential live -- and it is the one ship still sailing through the outage and after it. Seven ships holding seven copies died together; the one reading the original did not. That is the copy model's own prediction, met without being aimed at. *And the manual repair measured, because it did not do what it looks like it did:* six per-folder `claude` + `/login` runs wrote **nothing new** -- the whole pier holds exactly two credential files, `$HOME/.claude/.credentials.json` and this tree's seeded copy, both stamped `01:53` and neither touched in the 90 minutes since. `claude` on the host reads `$HOME` **whatever directory it is run from**, so a per-folder login is the same login six times and the last write wins; **copal reading *already logged in* is that mechanism visible from the outside**, and the only ship that showed it is the one that followed a successful login. A genuinely per-ship session must be made where `$HOME` differs, which is **inside the ship's own jail**. *That recipe was published untested and it wants two corrections (`20260906.023800`).* **First, the mechanism does hold:** the dry-run plan binds `loops/claude` over `~/.claude` **after** the `--tmpfs /home/keeper` that would otherwise swallow it, so a login inside the jail persists to the tree -- read from `agent-jail.sh --dry-run`, which runs without nesting. **Second, `/logout` was the wrong verb and could have reproduced the outage:** a logout may revoke the OAuth session **server-side**, and that session is the one every ship shares, so logging out inside one jail could log out the fleet. The safe form removes only the local copy: `rm ~/.claude/.credentials.json` **inside the jail**, then `/login`. **A recipe that touches shared state must say which half is local.** Copal's `loops/` is untouched since `20260905 18:24`, so the launcher never ran there and nothing was risked. *Measured `20260906.025440`, and PARTLY RETRACTED by `%458`:* copal holds **no credential copy** and a `loops/` untouched since `20260905 18:24`, and `trees_own_session` reads **0** -- those stand, read from peer trees bound in read-only. **What does not stand** is everything this seat said about *the pier's* credential: inside the enclosure `$HOME/.claude` is incense's own `loops/claude`, one file at two paths, so the pier's credential was never examined and the claim that a login *went nowhere a ship reads* was drawn from a tree that was never looked at. The pier half of this row is **unmeasured from here** and wants an unjailed shell. *Not repaired, and it is the larger half:* the fleet still shares one credential. One login per ship is the fix and it is **Keaton's hand at custody gate 3**. The scan **reports** `trees_sharing_token` rather than gating it: a guard that reds on the only shape the fleet can take is one someone turns off. **OPEN.**


**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
