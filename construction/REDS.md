# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/reds_spine_derive_scan.sh --next`.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 317 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 297** -- counted from the ledger and its archives on `20260828.151517` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 317 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*

**REDS %291 (`20260827.122432`) -- two sessions took one booked lap in one checkout, and neither could see the other.** *What went wrong:* two Claude Code sessions ran against this working tree at once, both seated as SOUND, both reading the same ITINERARY *Now* block, and both taking the same booked lap -- the nineteen-module socket-table fold. One opened at `12:10:13` with a cold `tools/fixtures/standing_equipment_run.sh`, sized the class, and listed the nineteen files. The other, started inside a minute of it and unaware of it, rewrote eighteen of those files at `12:14:06` and `linengrow/setu65_lab_tx.rye` at `12:15:33`, carried `linux_layout_files_ceiling` from 19 to 0 in `tools/fixtures/socket_dialect_scan.sh`, and committed and pushed three commits ending `5a500864c`. Nothing in the tree said a second hand was there: `git status` names paths and never who wrote them. *What caught it:* a re-read. The first session ran `socket_dialect_scan.sh` again to size its own lap and got `linux_layout_files=0` where its listing minutes earlier had read 19, then found nineteen `M` rows it had not typed; `ListAgents` named the peer and the two sessions talked. The standing guard would have caught it too, late -- `tree_at_open=a809173fb8fb` against `ed6d7a5454de` measured after the foreign writes, so that roster was already bound to refuse under `run_verdict=tree_moved` twenty-two minutes later, having spent the run. *What it taught:* **a checkout is a single-writer resource, and this tree publishes no reading of who holds it.** REDS %281 named the same root from the build side -- two `rye build` runs deleting each other's `.zig` shadows -- and its answer, *a lock in `bridge_to_zig`, or one tree per star*, is Keaton's word and still uncrossed. This is the source-file half of that row and it is the quieter half: a shadow race makes a correct module report RED, which is loud, while a source race makes two hands do one lap, which looks exactly like work. `tree_moved` is the right reading at the wrong time -- it fires at the close of a twenty-eight-minute run rather than at the moment a foreign write lands. *What the duplication bought:* a second reader. The first session read the second's nineteen-file diff before it was committed and found three faults -- two `setsockopt` blocks sitting at eight spaces inside four-space bodies (`mantra/recall_batch_delivery.rye:209-211` and `mantra/snapshot_export_delivery.rye:143-145`, proven by running `zig fmt` on a copy rather than by eye), a shelf paragraph in `construction/archive/REDS-the-kernel-that-answered-nine-rows-282.md` stamped `20260827.094500` on a file whose mtime reads `20260827.121845`, two hours thirty-three minutes behind the one clock, and an edit to a shelf whose own header declares it never edited. All three shipped. *Repaired in part (`20260827.122432`):* the six indentation lines are dedented, both modules rebuilt, `tools/m/mantra_recall_batch_wire.rish` and `tools/m/mantra_snapshot_wire.rish` GREEN on metal, `tame_style_check`, `width-check` and `socket_dialect_witness` GREEN. The stamp is a committed dated artifact and takes an erratum rather than a quiet edit, so it is surfaced here rather than corrected. **OPEN, gated** -- one tree per star or a lock is Keaton's word, unchanged from %281. What an agent can do without it, starting now: run `ListAgents` at lap open and name any peer in the log before touching a file.

**REDS %295 (`20260827.144029`) -- two of the eight are macOS-shaped work in a shared tree, and a standing roster is one host's promise.** *What went wrong:* `declared_ceiling` and `witness_reach` both refuse on files the Mind star put into this tree, and neither closes by a hand on this pier. `tools/l/chatgpt-mind.sh:23` writes `MAX_PROMPT_BYTES=24576` -- a prompt bound sitting in a block beside `MAX_LOG_BYTES` and `MAX_LAPS_HARD`, which merely equals `living_pin_max_bytes` because 24 KiB is a round number -- and `declared_ceiling_scan.sh` reads digits, so it calls a live line a law copy. That elder is pinned by SHA-256 `ee508804d2...` in `tools/l/chatgpt-mind-rishi-adaptation.md`, so one edited byte breaks the compatibility receipt the MIND adaptation grant rests on. `tools/s/skate_native_core_witness.rish` is reached by nothing, carrying `unreached` to **1,156 against 1,154**, and its last leg runs `xcrun swift test`, which this Linux pier has no toolchain for; the guard's own message refuses raising the ceiling, and rostering it here would red on an environment fact rather than a tree fault. *What caught it:* the same cold roster. *What it taught:* **the alias-sameness law seated `20260826.171130` has no guard that runs on this pier** -- `tools/fixtures/alias_sameness_scan.sh` has exactly one caller, and that caller cannot run here. A standing roster is a promise a particular host makes, and a three-star constellation writes into one tree from three hosts, so a law can arrive fully witnessed and still stand unguarded on the pier that carries it. This is REDS %281 and %291's question -- one tree per star, or a lock -- arriving from the guard side rather than the race side: not two hands colliding, but one hand leaving work another host cannot hold. **A third face arrived mid-lap.** Upstream moved while this round ran, and the two commits that landed left both generated pages stale -- `README.md`'s metrics block read 1,705 witnesses against a measured 1,706, and `docs-geode/libraries/README.md` read 5 and 1,698 where the tree holds 6 and 1,699. `tools/hooks/pre-commit` regenerates both and fires at `git commit`, yet `core.hooksPath` is armed **per clone** by `tools/i/install_hooks.rish`, so the wall stands only on the clones that ran it. A hook armed per clone is a promise only those clones keep, which is this row's own sentence arriving from a third direction. Both pages are regenerated here and their witnesses GREEN. **OPEN, gated.** Three shapes answer it and each is a seat: a **host tier** on the roster beside `lap` and `cadence`; a **per-star roster** in `construction/`; or a **platform gate inside the witness**, splitting its grep-only shape, boundary, and alias legs -- which run anywhere -- from the Swift leg that does not. The shape is Keaton's word.

**REDS %301 (`20260827.200419`) -- five rooms stood over the 256 bound and every meter read green, because a room is discovered by its dated files.** *What went wrong:* `tools/fixtures/room_bound_scan.sh` finds rooms by walking for **dated basenames**, so a room whose files are named for their subject rather than their day reads zero for its whole life and never appears on the meter at all. `tools/` reached **7.4x the bound** that way and was caught by a hand in August. On `20260827` the same blindness was measured again and it was not one room: **`tools/fixtures` at 649, `tools/gen/chapter` at 434, `glow/gen` at 339, `counsel/date/20260728` at 311, and `active-designing/yonder` at 296** -- five rooms, the largest **2.5x** over, with `enforced_over=0` and `advised_over=0` printed beside them. The `tools` entry on the `ENFORCE_ALL` roster counts that room's own flat entries and reaches none of its subdirectories, which is where two of the five sit. *What caught it:* reading the scan to find out why `glow/gen` was green at 340, rather than trusting that a green meter had looked. *What it taught:* **a meter that discovers rooms by a property only some rooms have will never report the room nobody thought to name.** The mark law already states the principle for the enforced set -- *a room that vanishes from a meter is not a room that passed it* -- and the same sentence reaches one step further than it was written: a room that never **arrives** at a meter has not passed it either. The general fix is to ask git for every directory rather than asking the tree for the rooms it already knows. *Repaired (`20260827.200419`):* an **undated sweep** at the end of the scan reads `git ls-files`, counts flat entries per directory, and reports every one over the bound that the rostered passes do not already cover, skipping vendored and inherited trees. All five are named on every run now. It is **advisory by design**, and the reason is worth stating: **a fold is not always mechanical.** Folding `glow/gen/` by letter would break `/+` library resolution for 31 desks, because `glow/glow_run.rye` builds the import path as the flat format string `"glow/gen/{s}.glow"` at its lines 166 and 241 -- so that room's cure is a compiler change with its own witnesses, not a `git mv`. Reporting is the honest act; each fold belongs to the round that can pay for it. **OPEN as five booked folds**, one per room, each its own lap.

**REDS %304 (`20260827.211629`) -- the TAME ban gate never read seven rooms, and 424 authored files stood outside the law they are governed by.** *What went wrong:* `tools/t/tame_style_scan_bans.rish` and its legacy shell twin both built their file set from a `find` naming twelve rooms **plus two individual Glow files** -- `glow/tokens.rye` and `glow/lower_named_cast.rye`. So `glow/` was governed two files out of 131, and `mycelium/` (100 sources), `constel/` (31), `lattice/`, `ember/`, `lantern/` and `scribble/` were never read at all. `.claude/rules/tame-guidance.md` states the bans as law over authored Rye without qualification, and the gate that enforces it covered 684 files where the tree holds 1,108. *What caught it:* an adversarial sweep reading the scan's own roster comment against the law's own scope. *What it taught:* **a gate's roster is a claim about coverage, and a roster written as a list grows stale by omission rather than by error.** Every room added since the list was written was outside the law by default, silently, and nothing in the tree compared the two -- the same shape as `%303`, where only a guard over two sets can see what one set is missing. *Repaired (`20260827.211629`):* the roster widened to all seven rooms with `! -path '*/.cache/*'` and `! -path '*/bin/*'` added, since `glow/.cache/` alone holds **157 generated `.rye` plants** that no style law governs. **Seven of the eight bans were already clean there** and gained 424 files of coverage for free. The eighth was not: `) == error.` had **8 real violations in `constel/apply.rye`, `read.rye` and `transfer.rye`**, each an `assert(call() == error.X)` whose silent `anyerror` upcast is exactly why the ban exists; all eight now read `if (call()) |_| unreachable else |err| assert(err == error.X)`, and `fora_apply`, `fora_read` and `fora_transfer` are GREEN on metal. **The eighth ban walked through its own door the same lap.** The first count read 104 compound asserts across the seven rooms and **both halves of that number were wrong**, in opposite directions. The counting regex `assert\([^)]* and ` stopped at the first `)`, so any call standing left of the `and` hid the conjunction behind it -- four real hits missed. And the gate's own `grep 'assert(.* and .*)'` cannot tell a conjunction from a **quoted string**, so six Glow lowering witnesses asserting that generated source *contains* text like `"pair.p == 5 and pair.q == 3"` read as violations and were not. `tools/fixtures/tame_style_compound_assert.sh` blanks quoted content before looking, the way the comment helper blanks comment lines, and the gate reads through it now. **Six of the seven rooms are at zero and joined the roster** -- glow, mycelium, lattice, ember, lantern, scribble -- with 25 compound asserts split into halves that name which side failed, every touched module built and its witness GREEN on metal (`glow_rune_shape`, `nock_core`, `mycelium_braid`, `_lapse`, `_pledge`, `_freight_conservation`, `_freight_bron`, `_freight_true`). **`constel` alone is held out at 130**, a truer number than the 83 the narrow regex reported. That one room stood open, and the line to delete was named in the source. **CLOSED (`20260828.133016`)**: the 130 read as **129 real conjunctions and one false positive**. The 129 are split one `assert` per conjunct by a depth- and string-aware pass, each trailing comment lifted to its own line above the group it describes, and `constel/reconfig.rye` names its conjunction as a `joint_holds` const OUTSIDE the assert rather than splitting `assert(self.reached == (A and B))`, whose `and` is the right operand of a comparison and whose split would have changed the claim. The 130th was never a violation: `assert(cand_grants == 5); // every voter grants ... (equal) and the two behind (candidate ahead)` in `constel/vote.rye:155`, where the helper blanked quoted strings and left the TRAILING `//` comment standing as code -- **the same fault it was written to fix, one layer over**, so a guard reddened on honest English prose. `tools/fixtures/tame_style_compound_assert.sh` strips the comment after the quotes now, quotes first so a `//` inside a literal cannot truncate real code, proven both ways on eight planted lines: it still bites a conjunction wearing a comment and a call standing left of the `and`, and frees the literal and the prose. And `find_rye_compound` is **deleted rather than widened** -- with `constel` at zero the second roster held out nothing, and two rosters that must agree are one roster plus a bug waiting for the day they stop agreeing, which this pass proved by having to edit both. All **31** `tools/f/fora_*_witness.rish` GREEN on metal, with `tame_style_scan_bans` and its selftest, `tame_style_ban_err_eq_comment_selftest`, `tame_style_check` and `width-check` GREEN beside them.

**REDS %306 (`20260828.002218`) -- one promise, two proofs, and the stricter refuses every seated name.** *What went wrong:* the Constel naming law stands in three rooms. `constel/name.rye` `check_ship`, called at `handshake.rye:117`, admits only **vowel-free** names; `pond/apps/constel_net.rye:98` and `constel_naming_scan.sh` kept the elder **digit** proof from a day earlier. All five seated names carry vowels and ten `pond/apps` modules seat them, unread by any guard. *What caught it:* the Glow-tree charter's booked defect. *What it taught:* **a promise proven twice is a promise nobody compares** -- both are sound, so safety held and every guard read green. *Repaired:* the scan mirrors `check_constellation`, gates the union, and prints `split_seated_refuses=5`; the planted fixtures caught this repair's own bug: a trailing hyphen Rye refuses `BadSegment`. **OPEN** on the redraw, Pond's lap. *Redraw landed (`20260828.120225`):* each elder name with its **vowels dropped** (`xnqvel34` -> `xnqvl34`), so all five satisfy **both** readings rather than trading one away -- `check_ship`'s alphabet is consonants **and** digits, so a name never had to choose. Ten modules repointed, two Bron fixtures regenerated, 15 witnesses GREEN; `split_seated_refuses=0` and `split=0` pinned. **The other direction stays open, pinned at `split_digit_absent=6`** -- where `generate()` draws every name `name_safe` refuses. Which room moves is a seat.

**REDS %311 (`20260828.114450`) -- the wall whose needle the harness itself supplies, and 152 runs that never read an answer.** *What went wrong:* `tools/g/glow_lower_assert_witness.rish:40` asserted `run_un.out contains "EXIT:"` on a run captured as `${un_bin} >/dev/null 2>&1; echo EXIT:$?`. The `echo` fires unconditionally, so `EXIT:` stands in that output before the binary is ever consulted, and the assertion could never red. The same reading found **152 answer-blind runs** across 35 witness files -- a lowered binary proven to launch and exit with a code, nothing asserted about what it said -- and **7 files** asserting a clean exit and never once, by any spelling, a refusal. `tools/g/glow_lower_shop_gate_witness.rish` runs 34 gate binaries that way, `${argv_bin} 9` among them, which is the argv shape %310 caught exiting clean and silent. *What caught it:* the meter written for it. A hand reading those same 35 files had already counted the never-refused files by grep and missed the vacuous needle entirely; the scan named it on its first run. *What it taught:* **the framing a harness prints is not evidence, and a substring test cannot tell the two apart.** %310 is this sentence one layer down -- there the oracle was derived from the implementation it checked, here the needle is derived from the harness that runs it -- and both pass for one reason: the checker and the checked share a source. *Repaired (`20260828.114450`):* the vacuous line becomes `assert run_un.ok`, matching its two siblings in the same file, and the honest negated refusal beneath it stands untouched. `tools/fixtures/glow_gate_answer_scan.sh` gates `laundered_needles` and `unread_runs` at **zero** and ratchets `answer_blind_runs` at **152** and `never_refused_files` at **7** under ceilings that only fall; `tools/fixtures/glow_gate_answer_control.sh` proves **22** cases on real planted `.rish` files -- the real hit and the real %310 needle bitten verbatim, every honest exit-code wall freed, and both ceilings proven from both sides by planting one past them, so no override exists. `tools/g/glow_gate_answer_witness.rish` GREEN on metal. **OPEN** on the two ratchets: 152 and 7 are lowering-side repairs, and a plant must speak before a witness can read it, which is Sound's language custody.

**REDS %313 (`20260828.125441`) -- the row that closed another row said so only in its own line, and then folded onto a shelf.** *What went wrong:* `%308` closed on `20260828.033212` and wrote that `%307`'s open clause closed with it -- a guard reads instruction files against the gate list now. `%307`'s own row kept the marker it was booked with, reading open with the clause *no guard reads an instruction file against the gate list*, and commit `660b36a22` states in its body that `construction/REDS.md` closes that clause: an edit the tree never received. `%308` then folded to `construction/archive/` the same day, so the record of the closure sat on a shelf a lap never walks while the stale marker stayed on the pin a lap reads first. *What caught it:* reds-first doing its job. This lap read the pin to choose its work, took the one open row no other body owns, and found `tools/cu/custody_gate_instruction_witness.rish` already rostered at tier `lap` and GREEN on metal -- 27 instruction files, 7 gated mentions, 7 refusal-framed, `bare_instructions=0`, 11 of 11 control cases. *What it taught:* **a closure written only in the closing row is invisible to the reader of the closed row, and folding makes that structural rather than accidental** -- every fold moves a closing row onto a shelf and leaves the row it closed on the open shelf, so the ledger will keep producing this shape rather than having produced it once. *Repaired (`20260828.125441`):* the elder row carries its closure and has folded beside `%308`. `tools/fixtures/reds_status_consistency_scan.sh` reads every prose row across the pin and 127 shelves and gates `contradicted_rows` and `phantom_closures` at **zero**; `tools/fixtures/reds_status_consistency_control.sh` proves **32** cases on planted markdown in a throwaway pen, each refusal planted and then removed and each welcome asserted as hard, since a reading that refused ordinary open rows would make reds-first impossible; `tools/r/reds_status_consistency_witness.rish` binds the reading to the spine at its live edge -- its highest row is the monotone scan's `expect_next` less one, present by `reds_row_present.sh`, and the row past it absent with exit 1. RED on metal before the repair, GREEN after, rostered at tier `lap`. *Two second looks, both in public:* the first draft of the reading matched two exact strings and reported **four** open rows where **seven** stood, because the ledger writes eight spellings of its two status words -- a status meter lying quietly, and the looser bare-word test already standing in `tools/fixtures/reds_fold.sh` was the more correct of the two; the marker now reads bold text beginning with either word, last one on the line. And where the two readings still differ the scan reports `fold_blocked_rows`, **5** at first reading, of which three were the very pin rows this lap reached for to make room -- each genuinely open and gated on Keaton's word, so the fold went to the two rows that were done. **CLOSED.**

**REDS %314 (`20260828.133811`) -- lifting a trailing comment onto its own line moved it INTO a meter that had no room left.** *What went wrong:* the `%304` compound-assert split lifted twenty trailing `//` comments onto their own lines above the assert group each describes, which reads better and is this tree's own habit. Three of those comments carried an em-dash. `tools/fixtures/rye_comment_ascii_scan.sh` counts non-ASCII on lines whose first non-blank characters are `//` and **deliberately excludes a trailing comment after code**, because finding one needs to know whether the `//` sits inside a string, which is parsing rather than scanning. So three characters that had stood uncounted for their files' whole lives became counted the moment they moved left, and `RYE_COMMENT_ASCII` read **4341 against a ceiling of 4338** -- on a tree already pushed to both remotes. *What caught it:* reading the ratchet this round had named as somebody's next lap, rather than trusting that a round which added no non-ASCII had raised no non-ASCII count. The check the commit actually ran was `git diff | grep '^+' | grep -c` over a non-ASCII class, and it read **zero for every touched file**, which was true: the characters were not added, they were *relocated*, and a diff of added lines cannot tell those apart. *What it taught:* **a meter with an honest exclusion has a boundary, and moving prose across that boundary changes the reading without changing what the tree says.** The scan declares its own limit in its header -- *this therefore UNDERCOUNTS on purpose* -- and an undercount is a promise about what goes unread, so any edit that carries text from the unread side to the read side is an edit that meter will feel. Reformatting is content-neutral only where no meter's scope is drawn by position. *Repaired (`20260828.133811`):* the three em-dashes became `--`, and five typographic minus signs standing where arithmetic wanted a hyphen (`threshold - 1`, `qe + qr - n`, `now - granted`) became `-` on touch, since a lap that has a file open owes the ratchet its turn. `constel`'s comment-line reading fell 49 to 41 and the living reading 4341 to **4333**, with the ceiling lowered to match and its arc line naming why. `tools/as/ascii_comment_witness.rish` GREEN on metal, twenty readings on planted repositories and the shell ceiling proven from both sides; the six touched modules' witnesses GREEN. **CLOSED**

**REDS %315 (`20260828.120225`) -- the card advertised a hash that exists on one machine and nowhere else.** *What went wrong:* `ITINERARY.md` carried `Git nib: 225d4742e2`, which resolves to nothing here: the seated order pins the nib then amends, so the card names the **pre-amend** commit, alive in the authoring clone's reflog alone. *What caught it:* the cold roster -- `nib_honesty` (`FLOATING_CLAIM`) and `remember_git_nib`. *What it taught:* **a hash proven only in the object store that made it is not proven.** `remember_git_nib`'s own header named this flaw when it was built, as one hand's inconvenience; six bodies now reset to the anointed order each lap, making it a standing double-red for five. *Repaired:* the nib names **HEAD's parent**, a pushed commit the guard already accepts; the reasoning is written into that guard's header. **CLOSED.**

**REDS %316 (`20260828.143915`) -- a ratchet read 1,140 under its ceiling while 222 whole witness families carried no clock at all.** *What went wrong:* `tools/fixtures/witness_reach_scan.sh` gates one number over the whole tree, and its `--families` diagnostic printed twenty absolute counts with no denominator, then dropped the rest through `head -20` without a word. So `86 hunk` read as a large family with a long tail when in fact all 86 of its witnesses are unreached, and the 224 other families holding an unreached witness never printed at all -- `comlink` at 4 of 5 and `mantra` at 6 of 7 among them, so the module a reader came to check was the module the census hid. Measured this lap: **222 of 291 families have every member unreached**, including all 86 of `hunk`, all 49 of `buhr`, and all 31 of `fora`. *What caught it:* reading the diagnostic to find where this lane's own 28 unreached witnesses sat, and finding the lane absent from its own census. *What it taught:* **a total cannot say whether it is a thin tail or whole systems with no clock, and a diagnostic that truncates in silence reports a passing tail it never looked at.** This is REDS %301's sentence one layer up -- there a room never arrived at a meter, here a family never arrived at the meter's own diagnostic, and a lantern that fires twice becomes a loom. *Repaired (`20260828.143915`):* a census line now carries `unreached/total` and its share and the list prints whole; `families` and `wholly_unreached` join the summary line, the second under `WITNESS_REACH_FAMILY_CEILING` seated at **222**, a ceiling that only falls. It catches one case the total is structurally blind to, proven by doing it in the pen rather than argued: delete a family's only REACHED member and `total` and `standing` both fall by one, so `unreached` stands exactly still while the family loses its last clock. `tools/fixtures/witness_reach_control.sh` proves **42** behaviors, eleven of them new, and was itself given twenty-five planted families so the truncation it now refuses can actually bite -- a control whose pen held three families could have had `head -20` put back with every check still green. Both regressions were planted in a full copy and both were refused. `tools/w/witness_reach_witness.rish` GREEN on metal. **CLOSED.**

**REDS %317 (`20260828.151517`) -- the meter read the wrapper and never the command it was wrapping, so 89 witnesses a runner names in plain command position were filed as named by nobody.** *What went wrong:* `tools/fixtures/witness_reach_scan.sh` builds its reachability graph from invocations in command position, and it read the FIRST token of a `run [ ... ]` list and stopped. `tools/p/parity_ch01.rish:510` calls `run ["sh" "tools/p/parity_time_one.sh" "rbwire" "--" "rishi/bin/rishi" "run" "tools/m/mantra_recall_batch_wire.rish"]` -- a timing wrapper whose real command sits after the `--` -- so the meter emitted `parity_time_one.sh` as the callee and never saw the witness. **430 invocations** across the two parity choirs wear that shape. A second shape did the same to the choirs themselves: `tools/p/parity.rish:30` reaches both through `run ["env" "PARITY_COST_CHAPTER=ch01" ... "rishi/bin/rishi" "run" "tools/p/parity_ch01.rish"]`, and a leading `env` with its assignments is not a command either, in **146** more places. So `unheard`, which the header defines as *named by no runner at all*, stood at **905** while **89** of those witnesses were named in plain command position by a real runner. *What caught it:* a second, weaker reading, and the order matters. A diagnostic added this lap to count files that name themselves a witness and lack the `_witness.rish` suffix reported **107 of them dark** -- and one of them, `mantra_recall_batch_wire.rish`, was one a `grep` for its basename had found reached minutes earlier. Two honest measurements disagreeing is what sent a hand to line 510. *What it taught:* **a wrapper is a promise that the real command comes later, and a parser that stops at the first token believes the wrapper instead of the promise.** This is REDS %224's own distinction corrupted from underneath: `unheard` and `unclocked` exist precisely to separate *no choir has been written* from *the choir has no clock*, and for 89 witnesses the meter answered the first when the truth was the second -- so the number a hand reads to decide whether to WRITE a choir was counting choirs already written. It is also the third firing of one shape in two days: %301 discovered rooms by a property only some rooms have, `20260828.144049` printed a family census with no denominator, and this read a call graph through a token position only some calls use. A lantern that fires three times is a loom. *Repaired (`20260828.151517`):* the extractor reads the tail after a `"--"` element and the tail after a leading `env` and its assignments, each unquoted into words and passed through the same `commands()` rule every other shape uses, so a wrapper earns no privilege. Measured across the repair: `sung` 817 -> **906**, `unclocked` 235 -> **324**, `unheard` 905 -> **816**, and the **gated `unreached` stands exactly still at 1140** -- nothing on the roster reaches `tools/p/parity.rish`, so these witnesses move from one unreached band to the other rather than out of the set. That is the honest result and it is why this row moves no ceiling. Beside it, `outside_convention=122` and `outside_dark=7` now print every run, REPORTED and not gated: the established repair is the rename Caravan already performed on eight files on `20260827` (`tools/ca/caravan_suite_witness.rish:31`, where **one of the eight had been RED**), yet a renamed file joins `total` unclocked and raises `unreached` against a ceiling that only falls, so a gate here would demand what the gate beside it forbids. `tools/fixtures/witness_reach_control.sh` proves **52** behaviors, ten of them new, every one from both sides -- the plant heard, then the caller removed and the reading returning. Both repairs were reverted in turn in a full copy and the control refused each, two checks apiece. `tools/w/witness_reach_witness.rish` GREEN on metal. **CLOSED** on the two shapes; the tier that would put `tools/p/parity.rish` on a roster, and the rename of the 122, stay Keaton's word.
