# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/reds_spine_derive_scan.sh --next`.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 309 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 289** -- counted from the ledger and its archives on `20260828.050134` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 309 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*


*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). Most recently %233-%236 on `20260826.001630` and %237-%241 on `20260826.010645`, all **CLOSED**. One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **Row %230 stays flat: it is OPEN and gated.***

*Rows %242, %245-%248, %251 and %254 folded to [`archive/REDS-the-integration-and-its-wake-rows-242-254.md`](archive/REDS-the-integration-and-its-wake-rows-242-254.md) on `20260826.044827`, every one **CLOSED** -- the two-pier send, the debride's wake, the parked package, and a witness pinning an exact claim count. Moved because rows %253 and %254 carried the living pin to 35,380 bytes against the 24,576 it declares. **The pin keeps what is open:** %230, %249, %250, %252, and %253 stay flat.*

*Row %249 folded to [`archive/REDS-the-gate-that-guarded-nothing-rows-249.md`](archive/REDS-the-gate-that-guarded-nothing-rows-249.md) on `20260826.070513`, **CLOSED** -- the exec-bit gate that enforced a zero it could not see, repaired and proven from both sides on metal. Moved because row %255 carried the living pin to 25,885 bytes against the 24,576 it declares. **The pin keeps what is open:** %230, %250, %252, %253 and %255 stay flat.*

*Row %308 folded to [`archive/REDS-the-launcher-that-kept-the-cut-cadence-rows-308.md`](archive/REDS-the-launcher-that-kept-the-cut-cadence-rows-308.md) on `20260828.050134`, **CLOSED** -- the loop launcher that kept printing a cadence the card had cut, and the meter that found the file no hand had looked at. Moved because row %309 carried the living pin past the 24,576 bytes it declares. **The pin keeps what is open:** %230, %291, %295, %301, %304, %306, %307 and %309 stay flat.*

**REDS %291 (`20260827.122432`) -- two sessions took one booked lap in one checkout, and neither could see the other.** *What went wrong:* two Claude Code sessions ran against this working tree at once, both seated as SOUND, both reading the same ITINERARY *Now* block, and both taking the same booked lap -- the nineteen-module socket-table fold. One opened at `12:10:13` with a cold `tools/fixtures/standing_equipment_run.sh`, sized the class, and listed the nineteen files. The other, started inside a minute of it and unaware of it, rewrote eighteen of those files at `12:14:06` and `linengrow/setu65_lab_tx.rye` at `12:15:33`, carried `linux_layout_files_ceiling` from 19 to 0 in `tools/fixtures/socket_dialect_scan.sh`, and committed and pushed three commits ending `5a500864c`. Nothing in the tree said a second hand was there: `git status` names paths and never who wrote them. *What caught it:* a re-read. The first session ran `socket_dialect_scan.sh` again to size its own lap and got `linux_layout_files=0` where its listing minutes earlier had read 19, then found nineteen `M` rows it had not typed; `ListAgents` named the peer and the two sessions talked. The standing guard would have caught it too, late -- `tree_at_open=a809173fb8fb` against `ed6d7a5454de` measured after the foreign writes, so that roster was already bound to refuse under `run_verdict=tree_moved` twenty-two minutes later, having spent the run. *What it taught:* **a checkout is a single-writer resource, and this tree publishes no reading of who holds it.** REDS %281 named the same root from the build side -- two `rye build` runs deleting each other's `.zig` shadows -- and its answer, *a lock in `bridge_to_zig`, or one tree per star*, is Keaton's word and still uncrossed. This is the source-file half of that row and it is the quieter half: a shadow race makes a correct module report RED, which is loud, while a source race makes two hands do one lap, which looks exactly like work. `tree_moved` is the right reading at the wrong time -- it fires at the close of a twenty-eight-minute run rather than at the moment a foreign write lands. *What the duplication bought:* a second reader. The first session read the second's nineteen-file diff before it was committed and found three faults -- two `setsockopt` blocks sitting at eight spaces inside four-space bodies (`mantra/recall_batch_delivery.rye:209-211` and `mantra/snapshot_export_delivery.rye:143-145`, proven by running `zig fmt` on a copy rather than by eye), a shelf paragraph in `construction/archive/REDS-the-kernel-that-answered-nine-rows-282.md` stamped `20260827.094500` on a file whose mtime reads `20260827.121845`, two hours thirty-three minutes behind the one clock, and an edit to a shelf whose own header declares it never edited. All three shipped. *Repaired in part (`20260827.122432`):* the six indentation lines are dedented, both modules rebuilt, `tools/m/mantra_recall_batch_wire.rish` and `tools/m/mantra_snapshot_wire.rish` GREEN on metal, `tame_style_check`, `width-check` and `socket_dialect_witness` GREEN. The stamp is a committed dated artifact and takes an erratum rather than a quiet edit, so it is surfaced here rather than corrected. **OPEN, gated** -- one tree per star or a lock is Keaton's word, unchanged from %281. What an agent can do without it, starting now: run `ListAgents` at lap open and name any peer in the log before touching a file.

**REDS %295 (`20260827.144029`) -- two of the eight are macOS-shaped work in a shared tree, and a standing roster is one host's promise.** *What went wrong:* `declared_ceiling` and `witness_reach` both refuse on files the Mind star put into this tree, and neither closes by a hand on this pier. `tools/l/chatgpt-mind.sh:23` writes `MAX_PROMPT_BYTES=24576` -- a prompt bound sitting in a block beside `MAX_LOG_BYTES` and `MAX_LAPS_HARD`, which merely equals `living_pin_max_bytes` because 24 KiB is a round number -- and `declared_ceiling_scan.sh` reads digits, so it calls a live line a law copy. That elder is pinned by SHA-256 `ee508804d2...` in `tools/l/chatgpt-mind-rishi-adaptation.md`, so one edited byte breaks the compatibility receipt the MIND adaptation grant rests on. `tools/s/skate_native_core_witness.rish` is reached by nothing, carrying `unreached` to **1,156 against 1,154**, and its last leg runs `xcrun swift test`, which this Linux pier has no toolchain for; the guard's own message refuses raising the ceiling, and rostering it here would red on an environment fact rather than a tree fault. *What caught it:* the same cold roster. *What it taught:* **the alias-sameness law seated `20260826.171130` has no guard that runs on this pier** -- `tools/fixtures/alias_sameness_scan.sh` has exactly one caller, and that caller cannot run here. A standing roster is a promise a particular host makes, and a three-star constellation writes into one tree from three hosts, so a law can arrive fully witnessed and still stand unguarded on the pier that carries it. This is REDS %281 and %291's question -- one tree per star, or a lock -- arriving from the guard side rather than the race side: not two hands colliding, but one hand leaving work another host cannot hold. **A third face arrived mid-lap.** Upstream moved while this round ran, and the two commits that landed left both generated pages stale -- `README.md`'s metrics block read 1,705 witnesses against a measured 1,706, and `docs-geode/libraries/README.md` read 5 and 1,698 where the tree holds 6 and 1,699. `tools/hooks/pre-commit` regenerates both and fires at `git commit`, yet `core.hooksPath` is armed **per clone** by `tools/i/install_hooks.rish`, so the wall stands only on the clones that ran it. A hook armed per clone is a promise only those clones keep, which is this row's own sentence arriving from a third direction. Both pages are regenerated here and their witnesses GREEN. **OPEN, gated.** Three shapes answer it and each is a seat: a **host tier** on the roster beside `lap` and `cadence`; a **per-star roster** in `construction/`; or a **platform gate inside the witness**, splitting its grep-only shape, boundary, and alias legs -- which run anywhere -- from the Swift leg that does not. The shape is Keaton's word.

**REDS %301 (`20260827.200419`) -- five rooms stood over the 256 bound and every meter read green, because a room is discovered by its dated files.** *What went wrong:* `tools/fixtures/room_bound_scan.sh` finds rooms by walking for **dated basenames**, so a room whose files are named for their subject rather than their day reads zero for its whole life and never appears on the meter at all. `tools/` reached **7.4x the bound** that way and was caught by a hand in August. On `20260827` the same blindness was measured again and it was not one room: **`tools/fixtures` at 649, `tools/gen/chapter` at 434, `glow/gen` at 339, `counsel/date/20260728` at 311, and `active-designing/yonder` at 296** -- five rooms, the largest **2.5x** over, with `enforced_over=0` and `advised_over=0` printed beside them. The `tools` entry on the `ENFORCE_ALL` roster counts that room's own flat entries and reaches none of its subdirectories, which is where two of the five sit. *What caught it:* reading the scan to find out why `glow/gen` was green at 340, rather than trusting that a green meter had looked. *What it taught:* **a meter that discovers rooms by a property only some rooms have will never report the room nobody thought to name.** The mark law already states the principle for the enforced set -- *a room that vanishes from a meter is not a room that passed it* -- and the same sentence reaches one step further than it was written: a room that never **arrives** at a meter has not passed it either. The general fix is to ask git for every directory rather than asking the tree for the rooms it already knows. *Repaired (`20260827.200419`):* an **undated sweep** at the end of the scan reads `git ls-files`, counts flat entries per directory, and reports every one over the bound that the rostered passes do not already cover, skipping vendored and inherited trees. All five are named on every run now. It is **advisory by design**, and the reason is worth stating: **a fold is not always mechanical.** Folding `glow/gen/` by letter would break `/+` library resolution for 31 desks, because `glow/glow_run.rye` builds the import path as the flat format string `"glow/gen/{s}.glow"` at its lines 166 and 241 -- so that room's cure is a compiler change with its own witnesses, not a `git mv`. Reporting is the honest act; each fold belongs to the round that can pay for it. **OPEN as five booked folds**, one per room, each its own lap.

**REDS %304 (`20260827.211629`) -- the TAME ban gate never read seven rooms, and 424 authored files stood outside the law they are governed by.** *What went wrong:* `tools/t/tame_style_scan_bans.rish` and its legacy shell twin both built their file set from a `find` naming twelve rooms **plus two individual Glow files** -- `glow/tokens.rye` and `glow/lower_named_cast.rye`. So `glow/` was governed two files out of 131, and `mycelium/` (100 sources), `constel/` (31), `lattice/`, `ember/`, `lantern/` and `scribble/` were never read at all. `.claude/rules/tame-guidance.md` states the bans as law over authored Rye without qualification, and the gate that enforces it covered 684 files where the tree holds 1,108. *What caught it:* an adversarial sweep reading the scan's own roster comment against the law's own scope. *What it taught:* **a gate's roster is a claim about coverage, and a roster written as a list grows stale by omission rather than by error.** Every room added since the list was written was outside the law by default, silently, and nothing in the tree compared the two -- the same shape as `%303`, where only a guard over two sets can see what one set is missing. *Repaired (`20260827.211629`):* the roster widened to all seven rooms with `! -path '*/.cache/*'` and `! -path '*/bin/*'` added, since `glow/.cache/` alone holds **157 generated `.rye` plants** that no style law governs. **Seven of the eight bans were already clean there** and gained 424 files of coverage for free. The eighth was not: `) == error.` had **8 real violations in `constel/apply.rye`, `read.rye` and `transfer.rye`**, each an `assert(call() == error.X)` whose silent `anyerror` upcast is exactly why the ban exists; all eight now read `if (call()) |_| unreachable else |err| assert(err == error.X)`, and `fora_apply`, `fora_read` and `fora_transfer` are GREEN on metal. **The eighth ban walked through its own door the same lap.** The first count read 104 compound asserts across the seven rooms and **both halves of that number were wrong**, in opposite directions. The counting regex `assert\([^)]* and ` stopped at the first `)`, so any call standing left of the `and` hid the conjunction behind it -- four real hits missed. And the gate's own `grep 'assert(.* and .*)'` cannot tell a conjunction from a **quoted string**, so six Glow lowering witnesses asserting that generated source *contains* text like `"pair.p == 5 and pair.q == 3"` read as violations and were not. `tools/fixtures/tame_style_compound_assert.sh` blanks quoted content before looking, the way the comment helper blanks comment lines, and the gate reads through it now. **Six of the seven rooms are at zero and joined the roster** -- glow, mycelium, lattice, ember, lantern, scribble -- with 25 compound asserts split into halves that name which side failed, every touched module built and its witness GREEN on metal (`glow_rune_shape`, `nock_core`, `mycelium_braid`, `_lapse`, `_pledge`, `_freight_conservation`, `_freight_bron`, `_freight_true`). **`constel` alone is held out at 130**, a truer number than the 83 the narrow regex reported. **OPEN** on that one room, and the line to delete is named in the source.

**REDS %306 (`20260828.002218`) -- one promise, two proofs, and the stricter refuses every seated name.** *What went wrong:* the Constel naming law stands in three rooms. `constel/name.rye` `check_ship`, called at `handshake.rye:117`, admits only **vowel-free** names; `pond/apps/constel_net.rye:98` and `constel_naming_scan.sh` kept the elder **digit** proof from a day earlier. All five seated names carry vowels and ten `pond/apps` modules seat them, unread by any guard. *What caught it:* the Glow-tree charter's booked defect. *What it taught:* **a promise proven twice is a promise nobody compares** -- both are sound, so safety held and every guard read green. *Repaired:* the scan mirrors `check_constellation`, gates the union, and prints `split_seated_refuses=5`; the planted fixtures caught this repair's own bug: a trailing hyphen Rye refuses `BadSegment`. **OPEN** on the redraw, Pond's lap.

**REDS %307 (`20260828.011621`) -- a living document is edited for content, and its meter reads a lap later or never.** *What went wrong:* two faces, one root. `recursion-prompts/seed/autonomous-loop.seed.md` still told an unattended lap where `N mod 5 == 0` to run `bash ~/grain/publish-seed.sh` -- the force-push custody gate %1 holds at Keaton's hand and `construction/ITINERARY.md` CUT on `20260826`. Six loops fill from that template, which the manifest scrubs public. Beside it `radiant_negation` read `NEGATION_ROSE` on three rules -- `checkpoint.md` 1.47 to 1.67, `lindy-first-crux.md` 1.17 to 1.19, `quin.md` 0.00 to 0.68 -- each raised by a correct `20260827` edit that added a clause and moved a number nobody re-read. *What caught it:* the cold roster for the second; the rota's own grid read for the first. *What it taught:* **a custody gate is only as strong as the instructions reaching the hand it gates.** *Repaired:* the section names the cut and points at gate %1; the three densities read at or under baseline, `enforce_risen=0`, claims held exactly; `radiant_negation` and `loop_prompt_parse` GREEN on metal. **OPEN** -- no guard reads an instruction file against the gate list.


**REDS %309 (`20260828.050134`) -- the refusal half of a Glow gate wall was satisfied by the runner's own exit line.** *What went wrong:* two shapes, one root -- a wall reading a substring where it meant to read an answer. `tools/g/glow_run_worker.sh` appends its own `EXIT:0` line to every successful run, and Rishi's `contains` is a plain substring test (`rishi/src/main.rye` `eval_expr`), so `assert over.out contains "0"` is satisfied by that exit line alone. Measured on metal: an ACCEPT run of `src/gate/gate-caravan-caps-bound-u32.glow` prints `1` and then `EXIT:0`, and that output passes the refusal assertion. **Twenty-eight refusal halves across twenty-three walls could not fail**, so every Glow gate in this tree was proven on its accept side and merely watched on its refusal side. Beneath it stands a deeper one: a desk whose body is a two-face cond -- `|=  [count=@u32 cap=@u32]` -- lowers through the arm at `glow/lower_shop_gate.rye:1442,1449`, which emits the gate body and the `expect` it is compared against **from the same format arguments**. The generated main reads `return if (bartis_pair(count, cap) == expect) 0 else 1`, comparing the gate to a textual copy of itself; it returns 0 for every input and prints no answer at all. Eight desks lower that way, and `tools/g/glow_vane_pair_mirrors_witness.rish` runs seven of them asserting only that the process started, closing on the line *"every edge exact."* That witness was seated onto the standing roster at tier `cadence` on `20260827.183635`, as the Glow-tree charter's own first step -- *the roster hears the language* -- so the tree has been running a guard that cannot fail and counting it green. *What caught it:* the council rota's aether row, whose fixed seat is the grain strand **a guard that cannot red guards nothing**, read against the newest Glow rung rather than the oldest; then `od -c` on the worker's real output, which showed a pair gate printing seven bytes and nothing else, and a gate planted with its arms swapped exiting 0 exactly like the true one. *What it taught:* **a wall proves only the side it can fail on, and an appended status line will quietly satisfy an assertion about content.** The tree already held the right shape one arm away -- `.fold_sumto` in the same lowering carries the comment *"the folding gate speaks its accumulator; EXIT:0 + spoken answer prove it"* and calls `emit_argv_speaking_main`. One arm speaks, its neighbour does not, and no meter compared them. *Repaired (`20260828.050134`):* the eight walls of the infrastructure half -- Comlink, Mantra, and the Caravan seams -- take `assert (over.out contains "1") == false`, exact for a 0/1 gate because the accept answer is the only `1` such a run can produce and `EXIT:0` carries none. `tools/fixtures/glow_gate_answer_scan.sh` reads every tracked `.rish` under `tools/` invoking the runner, discovered rather than named, and holds `indistinct_refusals` under a **ceiling that only falls** -- 28 before, **20** after; `silent_pair_gates=8` is **reported rather than gated**, since its cure is a change to the lowering. `tools/fixtures/glow_gate_answer_control.sh` proves eighteen cases on planted files -- the two substring facts the reading rests on, the fault bitten and the repair freed, `contains "EXIT:0"` and a non-gate run and the genuine accept assertion left free, both runner spellings read, an absent file and an absent pen told apart, and the ceiling proven from both sides on a planted corpus: twenty free, twenty-one refused, one repair bringing it back under. **What it does not reach:** whether a gate's invariant is the right one; and for a fold speaking an arbitrary number the repaired form bites on 1, 10 and 15 yet would pass on 20 -- a strengthening rather than an exact check, where reading the answer line alone is exact and costs a second process. **OPEN** on two faces that are not this bench's: the silent pair-gate **lowering**, which is Glow language custody, and the **twenty** remaining walls in Tally, Aurora, Rishi and the Glow corpus.
