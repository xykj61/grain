# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 292 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 272** -- counted from the ledger and its archives on `20260827.141931` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 292 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*Row 170 folded to [`archive/REDS-the-prevention-off-the-roster-rows-170.md`](archive/REDS-the-prevention-off-the-roster-rows-170.md) on `20260823.152322` -- REDS %83's own ASCII prevention guard standing off the roster while the card it protects carried four non-ASCII lines, CLOSED, moved here on the lap it was booked. Its lesson is carried forward by row %171, which measures the same coverage gap at a wider scale.*

*Row 171 folded to [`archive/REDS-the-sample-nobody-drew-rows-171.md`](archive/REDS-the-sample-nobody-drew-rows-171.md) on `20260823.171000`, **CLOSED** -- the random draw of 86 witnesses across 29 rooms, and the two rungs it left open, both now closed by row %173. Moved here as %173 carried the living pin past its 24,576-byte bound. **Living rows 172 onward below.***

*Row 172 folded to [`archive/REDS-the-roster-that-made-itself-red-rows-172.md`](archive/REDS-the-roster-that-made-itself-red-rows-172.md) on `20260823.174500`, **CLOSED** -- running the roster manufactured the artifact the next run reported, and the scan asks `git check-ignore` now rather than guessing which rooms are generated. Moved here as row %174 carried the living pin past its bound. **Living rows 173 onward below.***

*Row 173 folded to [`archive/REDS-the-machine-with-no-screen-rows-173.md`](archive/REDS-the-machine-with-no-screen-rows-173.md) on `20260823.174500`, **CLOSED** on the lap it was booked -- a Wayland application on a headless pier reading as a broken tree, and the shared `display_seam_probe.sh` six callers now gate on. Moved here as row %174 carried the living pin past its bound. **Living row 174 below.***

*Row 174 folded to [`archive/REDS-the-roster-ran-on-one-tree-rows-174.md`](archive/REDS-the-roster-ran-on-one-tree-rows-174.md) on `20260823.204456`, **CLOSED** -- the build cache that reached main because the roster ran before `git add -A`, and the rule it gave its final shape: stage first, then measure, then read what you staged. Moved here as row %175 carried the living pin past its bound. **Living rows 175 onward below.***

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Row 175 folded to [`archive/REDS-the-sprig-that-was-optional-rows-175.md`](archive/REDS-the-sprig-that-was-optional-rows-175.md) on `20260823.212601`, **CLOSED** -- the canonical dated test required a sprig the session-logs law makes optional, so 237 logs read as living and sat in the repointer's write set. **Eight read-only scans still carry the narrow pattern**, named there and waiting on their own lap. **Living row 176 below.***

*Row 176 folded to [`archive/REDS-the-mirror-that-matched-perfectly-rows-176.md`](archive/REDS-the-mirror-that-matched-perfectly-rows-176.md) -- CLOSED.*

*Rows 177-178 folded to [`archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md`](archive/REDS-the-printed-command-and-the-counted-remainder-rows-177-178.md).*

*Row 179 folded to [`archive/REDS-the-name-that-was-taken-rows-179.md`](archive/REDS-the-name-that-was-taken-rows-179.md) on `20260824.062207`, **CLOSED** -- a new guard written straight over a tracked guard that already carried the name, caught by ` M ` where `??` belonged in `git status`. Moved here as row %184 carried the living pin past its bound. **Living rows 180 onward below.***

*Row 180 folded to [`archive/REDS-the-rule-written-as-arithmetic-rows-180.md`](archive/REDS-the-rule-written-as-arithmetic-rows-180.md) on `20260824.071500`, **CLOSED** -- the room fold's dated test still required a sprig, written as a byte comparison rather than a regex, so the spelling guard passed truthfully over the file carrying the fault.*

*Row 181 folded to [`archive/REDS-the-citation-that-named-nothing-rows-181.md`](archive/REDS-the-citation-that-named-nothing-rows-181.md) on `20260824.072600`, **CLOSED** -- a round that widened the dated rule cited two files that exist nowhere, caught by the roster run after `git add`.*

*Row 182 folded to [`archive/REDS-the-index-that-grew-alone-rows-182.md`](archive/REDS-the-index-that-grew-alone-rows-182.md) on `20260824.075409`, **CLOSED** -- the room that folded while its index grew alone, to 2,895,849 bytes against a declared 24,576, and the index fold that seated the 39th guard. Moved here as row %187 carried the living pin past its bound. **Living rows 183 onward below.***

*Row 183 folded to [`archive/REDS-a-fold-moves-depth-rows-183.md`](archive/REDS-a-fold-moves-depth-rows-183.md) on `20260824.075409`, **CLOSED** -- the shelf writer that moved a row's text without its depth, and the rule that repaired it: fix the tool and re-run from a clean base rather than patching its output. Moved here as row %187 carried the living pin past its bound.*

*Row %221 folded to [`archive/REDS-a-cold-roster-across-its-own-edits-rows-221.md`](archive/REDS-a-cold-roster-across-its-own-edits-rows-221.md) on `20260825.144025`, **CLOSED** -- a cold reading spread across the tree it was measuring, closed by a twelve-character tree digest taken at the open and again at the close. Moved as %223 carried the living pin past its bound.*

*Row %222 folded to [`archive/REDS-a-tier-from-one-sample-rows-222.md`](archive/REDS-a-tier-from-one-sample-rows-222.md) on `20260825.162410` -- the tier chosen on one cold-cache sample multiplied by 239, CLOSED, moved here as row %224 carried the living pin past its 24,576-byte bound.*

*Rows %223-%225 folded to [`archive/REDS-a-refusal-a-ratchet-and-a-name-rows-223-225.md`](archive/REDS-a-refusal-a-ratchet-and-a-name-rows-223-225.md) on `20260825.183336`, all three **CLOSED** -- a cold roster refusing to open over an unclosed lap, the reach ratchet moving onto the property it stands for, and the seed wall learning the fund's short name. Moved here as rows %226 through %228 carried the living pin past its 24,576-byte bound.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). Most recently %233-%236 on `20260826.001630` and %237-%241 on `20260826.010645`, all **CLOSED**. One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **Row %230 stays flat: it is OPEN and gated.***

*Rows %242, %245-%248, %251 and %254 folded to [`archive/REDS-the-integration-and-its-wake-rows-242-254.md`](archive/REDS-the-integration-and-its-wake-rows-242-254.md) on `20260826.044827`, every one **CLOSED** -- the two-pier send, the debride's wake, the parked package, and a witness pinning an exact claim count. Moved because rows %253 and %254 carried the living pin to 35,380 bytes against the 24,576 it declares. **The pin keeps what is open:** %230, %249, %250, %252, and %253 stay flat.*

*Row %249 folded to [`archive/REDS-the-gate-that-guarded-nothing-rows-249.md`](archive/REDS-the-gate-that-guarded-nothing-rows-249.md) on `20260826.070513`, **CLOSED** -- the exec-bit gate that enforced a zero it could not see, repaired and proven from both sides on metal. Moved because row %255 carried the living pin to 25,885 bytes against the 24,576 it declares. **The pin keeps what is open:** %230, %250, %252, %253 and %255 stay flat.*



**REDS %289 (`20260827.024604`) -- the tamper that GNU sed plants, BSD sed silently declines to plant.** *What went wrong:* RED leg six of `tools/ca/caravan_ladder_copy_witness.rish` manufactures its control by rewriting one printed phrase in the living harness with `sed '0,/re/s//x/'` -- GNU's first-occurrence range. BSD sed exits 0 on that address and substitutes nothing, so the "hurt" file was byte-identical to the clean one, the prose scan rightly passed it, and the must-refuse assert redded the rung -- redding the choir's first full sing on this bench at rung one of a green 113. *What caught it:* the choir itself, under the fresh build lock; a diff showed the tamper never landed, and a two-line probe split the dialects -- GNU substitutes, BSD passes through, both exit 0. *What it taught:* the dialect family (%249, %250, %275) reaches the refusal legs too, and a control that no-ops is the worst dialect fault yet: the scan under test was correct on both hosts, and the guard testing it reported the fault as the scan's. A refusal leg proven on one dialect proves nothing on the other half of the fleet. *Repaired (`20260827.024604`):* the range dropped for a plain POSIX `s///` -- the phrase stands once in the file, so the tamper is identical on both dialects -- with the why written beside it in the witness; the rung re-proven GREEN on metal, welcome and refusal both, the choir re-sung; a tree-wide grep finds no other `0,/` site outside dated testimony. CLOSED.

**REDS %290 (`20260827.082646`) -- the rewritten remote met three loops that had no word for it.** *What went wrong:* two faults with one root, surfacing the morning after the Kiln deep debride. First, the Dream pier's round-open `git pull xy main` died fatal on divergent branches -- expected after a force-pushed rewrite -- and the launcher loops' own fallback, `|| git rebase --abort`, would have spun every twenty seconds forever, saying nothing; no launcher carried a recovery word for the one state the debride choreography promised every bench would meet. Second, Mind's worktree had merged rewritten main into its pre-rewrite branch, and with no honest merge-base across a rewrite, the merge resolved `context/LEXICON.md` to a rewritten *historical* blob -- the one class of blob the debride's HEAD-protection cannot reach -- swapping the deliberate lineage Ovens the working-tree half had kept on purpose (`Oven Chapter o1` read `Kiln Chapter o1`, the arc read `Kiln-to-Kiln`). *What caught it:* the pier's own fatal, and Mind's `repo-clean` refusal in `tools/l/chatgpt-mind.rish` -- the guard held the hand-staged repair un-lost and stopped the loop, exactly its job. *What it taught:* **a deep debride's cost is not paid until every bench has paid it, and a loop must know the one word for that morning: reset, never merge.** A pre-rewrite branch is never merged across a rewrite -- its unique work is adopted file-wise onto the rewritten history and the branch re-rooted. *Repaired (`20260827.082646`):* the round-open in all three shell launchers now breaks loudly on divergence, printing the `git branch keep-old && git reset --hard xy/main` recipe instead of spinning; Mind's eleven files (the `chatgpt-mind.rish` runner, its shell twin, controls, scans, the adaptation rule, and the rewritten cardinal launcher) adopted whole from the branch tip onto main, every surface GREEN or parsing on this bench; the worktree re-rooted onto rewritten main, where its staged repair dissolves because main's LEXICON already carries the correct text. CLOSED.


**REDS %291 (`20260827.122432`) -- two sessions took one booked lap in one checkout, and neither could see the other.** *What went wrong:* two Claude Code sessions ran against this working tree at once, both seated as SOUND, both reading the same ITINERARY *Now* block, and both taking the same booked lap -- the nineteen-module socket-table fold. One opened at `12:10:13` with a cold `tools/fixtures/standing_equipment_run.sh`, sized the class, and listed the nineteen files. The other, started inside a minute of it and unaware of it, rewrote eighteen of those files at `12:14:06` and `linengrow/setu65_lab_tx.rye` at `12:15:33`, carried `linux_layout_files_ceiling` from 19 to 0 in `tools/fixtures/socket_dialect_scan.sh`, and committed and pushed three commits ending `5a500864c`. Nothing in the tree said a second hand was there: `git status` names paths and never who wrote them. *What caught it:* a re-read. The first session ran `socket_dialect_scan.sh` again to size its own lap and got `linux_layout_files=0` where its listing minutes earlier had read 19, then found nineteen `M` rows it had not typed; `ListAgents` named the peer and the two sessions talked. The standing guard would have caught it too, late -- `tree_at_open=a809173fb8fb` against `ed6d7a5454de` measured after the foreign writes, so that roster was already bound to refuse under `run_verdict=tree_moved` twenty-two minutes later, having spent the run. *What it taught:* **a checkout is a single-writer resource, and this tree publishes no reading of who holds it.** REDS %281 named the same root from the build side -- two `rye build` runs deleting each other's `.zig` shadows -- and its answer, *a lock in `bridge_to_zig`, or one tree per star*, is Keaton's word and still uncrossed. This is the source-file half of that row and it is the quieter half: a shadow race makes a correct module report RED, which is loud, while a source race makes two hands do one lap, which looks exactly like work. `tree_moved` is the right reading at the wrong time -- it fires at the close of a twenty-eight-minute run rather than at the moment a foreign write lands. *What the duplication bought:* a second reader. The first session read the second's nineteen-file diff before it was committed and found three faults -- two `setsockopt` blocks sitting at eight spaces inside four-space bodies (`mantra/recall_batch_delivery.rye:209-211` and `mantra/snapshot_export_delivery.rye:143-145`, proven by running `zig fmt` on a copy rather than by eye), a shelf paragraph in `construction/archive/REDS-the-kernel-that-answered-nine-rows-282.md` stamped `20260827.094500` on a file whose mtime reads `20260827.121845`, two hours thirty-three minutes behind the one clock, and an edit to a shelf whose own header declares it never edited. All three shipped. *Repaired in part (`20260827.122432`):* the six indentation lines are dedented, both modules rebuilt, `tools/m/mantra_recall_batch_wire.rish` and `tools/m/mantra_snapshot_wire.rish` GREEN on metal, `tame_style_check`, `width-check` and `socket_dialect_witness` GREEN. The stamp is a committed dated artifact and takes an erratum rather than a quiet edit, so it is surfaced here rather than corrected. **OPEN, gated** -- one tree per star or a lock is Keaton's word, unchanged from %281. What an agent can do without it, starting now: run `ListAgents` at lap open and name any peer in the log before touching a file.

**REDS %292 (`20260827.141931`) -- the roster runner dirtied the tree it was measuring, and twenty control legs went quiet rather than red.** *What went wrong:* two faults with one root, both in the fusion standfast's new hit-rate machinery. First, `tools/fixtures/standing_equipment_run.sh` appends one row per open to `construction/standing-equipment-hitrate.kyri` with no `mkdir -p` ahead of it. A real tree always carries `construction/`, so on this pier the append works; a throwaway pen never does, so the redirect fails and ends the pass right there -- after `roster_receipt` and before a single guard runs. Second, that append lands INSIDE the tree the runner is measuring, between the open digest and the close digest. On a real tree it is invisible because `.gitignore` lines 356 and 357 hide the receipt and the ledger; the control's git pen carried no such rule, so the runner's own bookkeeping read back as a moved tree and the runner refused a tree that had stood perfectly still. *What caught it:* `tools/s/standing_equipment_witness.rish` redded at line 92, `default_is_lap`, during an unrelated Tally lap. The reading was misleading in the exact way a swallowed error always is: `run_runner` ends in `|| true`, so a runner that died at line one produced empty output, and twenty `case` legs each answered `no` with nothing naming why. The red was proven pre-existing rather than assumed -- `git stash` put the tree back to `4af925691` and the witness redded identically with the lap's work set aside. *Repaired (`20260827.141931`):* `mkdir -p "$(dirname "$hitledger")"` ahead of the append, and the control's git pen now writes the real tree's own two ignore rules into a `.gitignore` it commits with its seed, so the pen models the pier rather than a stripped-down cousin of it. Each fix was isolated from the failing side by line-number deletion: without the `mkdir` and with the ignore rules, **21 legs fail**; with the `mkdir` and without the ignore rules, **2 fail** -- `still_tree_reads_no` and `still_tree_passes`, the two the second fault owns; with both, **0**, and the witness is GREEN over forty-six behaviors with ten refusals bitten. *What it taught:* **a guard that writes into the tree it measures is only correct by grace of an ignore rule, and a pen that omits that rule is not a smaller tree -- it is a different one.** The pen existed to prove the digest honest and instead proved the runner's own footprint. Kin to %221, which named the digest, and to %289, whose lesson is the same shape one layer down: a control is only as true as the thing it plants. *And the loom, since this lantern is allowed to fire only once:* the witness that proves the roster honest -- `tools/s/standing_equipment_witness.rish` -- **was named only in a comment at line 42 of the roster and was not itself a rostered guard**, which is why a fault in the runner could stand until a hand happened to run it during an unrelated lap. It costs **4 seconds** of wall, measured, so it is seated as guard 78 at `20260827.142909` and the next such regression reds on the lap it enters. That is %152's own lesson arriving again: a rule living in a comment is a rule no program runs. **CLOSED** on metal, both directions measured. Two nearby readings stay open on this bench and neither is this row: `exec_bit` and `geode_libraries` each reported RED in a cold roster taken over a tree a peer was rewriting, so both want a re-read in a still tree before anyone books them.
