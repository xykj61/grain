# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 334 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 314** -- counted from the ledger and its archives on `20260828.222118` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 334 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*

**REDS %291 (`20260827.122432`) -- two sessions took one booked lap in one checkout, and neither could see the other.** *What went wrong:* two Claude Code sessions ran against this working tree at once, both seated as SOUND, both reading the same ITINERARY *Now* block, and both taking the same booked lap -- the nineteen-module socket-table fold. One opened at `12:10:13` with a cold `tools/fixtures/s/standing_equipment_run.sh`, sized the class, and listed the nineteen files. The other, started inside a minute of it and unaware of it, rewrote eighteen of those files at `12:14:06` and `linengrow/setu65_lab_tx.rye` at `12:15:33`, carried `linux_layout_files_ceiling` from 19 to 0 in `tools/fixtures/s/socket_dialect_scan.sh`, and committed and pushed three commits ending `5a500864c`. Nothing in the tree said a second hand was there: `git status` names paths and never who wrote them. *What caught it:* a re-read. The first session ran `socket_dialect_scan.sh` again to size its own lap and got `linux_layout_files=0` where its listing minutes earlier had read 19, then found nineteen `M` rows it had not typed; `ListAgents` named the peer and the two sessions talked. The standing guard would have caught it too, late -- `tree_at_open=a809173fb8fb` against `ed6d7a5454de` measured after the foreign writes, so that roster was already bound to refuse under `run_verdict=tree_moved` twenty-two minutes later, having spent the run. *What it taught:* **a checkout is a single-writer resource, and this tree publishes no reading of who holds it.** REDS %281 named the same root from the build side -- two `rye build` runs deleting each other's `.zig` shadows -- and its answer, *a lock in `bridge_to_zig`, or one tree per star*, is Keaton's word and still uncrossed. This is the source-file half of that row and it is the quieter half: a shadow race makes a correct module report RED, which is loud, while a source race makes two hands do one lap, which looks exactly like work. `tree_moved` is the right reading at the wrong time -- it fires at the close of a twenty-eight-minute run rather than at the moment a foreign write lands. *What the duplication bought:* a second reader. The first session read the second's nineteen-file diff before it was committed and found three faults -- two `setsockopt` blocks sitting at eight spaces inside four-space bodies (`mantra/recall_batch_delivery.rye:209-211` and `mantra/snapshot_export_delivery.rye:143-145`, proven by running `zig fmt` on a copy rather than by eye), a shelf paragraph in `construction/archive/REDS-the-kernel-that-answered-nine-rows-282.md` stamped `20260827.094500` on a file whose mtime reads `20260827.121845`, two hours thirty-three minutes behind the one clock, and an edit to a shelf whose own header declares it never edited. All three shipped. *Repaired in part (`20260827.122432`):* the six indentation lines are dedented, both modules rebuilt, `tools/m/mantra_recall_batch_wire.rish` and `tools/m/mantra_snapshot_wire.rish` GREEN on metal, `tame_style_check`, `width-check` and `socket_dialect_witness` GREEN. The stamp is a committed dated artifact and takes an erratum rather than a quiet edit, so it is surfaced here rather than corrected. **OPEN, gated** -- one tree per star or a lock is Keaton's word, unchanged from %281. What an agent can do without it, starting now: run `ListAgents` at lap open and name any peer in the log before touching a file.

**REDS %301 (`20260827.200419`) -- five rooms stood over the 256 bound and every meter read green, because a room is discovered by its dated files.** *What went wrong:* `tools/fixtures/r/room_bound_scan.sh` finds rooms by walking for **dated basenames**, so a room whose files are named for their subject rather than their day reads zero for its whole life and never appears on the meter at all. `tools/` reached **7.4x the bound** that way and was caught by a hand in August. On `20260827` the same blindness was measured again and it was not one room: **`tools/fixtures` at 649, `tools/gen/chapter` at 434, `glow/gen` at 339, `counsel/date/20260728` at 311, and `active-designing/yonder` at 296** -- five rooms, the largest **2.5x** over, with `enforced_over=0` and `advised_over=0` printed beside them. The `tools` entry on the `ENFORCE_ALL` roster counts that room's own flat entries and reaches none of its subdirectories, which is where two of the five sit. *What caught it:* reading the scan to find out why `glow/gen` was green at 340, rather than trusting that a green meter had looked. *What it taught:* **a meter that discovers rooms by a property only some rooms have will never report the room nobody thought to name.** The mark law already states the principle for the enforced set -- *a room that vanishes from a meter is not a room that passed it* -- and the same sentence reaches one step further than it was written: a room that never **arrives** at a meter has not passed it either. The general fix is to ask git for every directory rather than asking the tree for the rooms it already knows. *Repaired (`20260827.200419`):* an **undated sweep** at the end of the scan reads `git ls-files`, counts flat entries per directory, and reports every one over the bound that the rostered passes do not already cover, skipping vendored and inherited trees. All five are named on every run now. It is **advisory by design**, and the reason is worth stating: **a fold is not always mechanical.** Folding `glow/gen/` by letter would break `/+` library resolution for 31 desks, because `glow/glow_run.rye` builds the import path as the flat format string `"glow/gen/{s}.glow"` at its lines 166 and 241 -- so that room's cure is a compiler change with its own witnesses, not a `git mv`. Reporting is the honest act; each fold belongs to the round that can pay for it. **OPEN as five booked folds**, one per room, each its own lap.

**REDS %306 (`20260828.002218`) -- one promise, two proofs, and the stricter refuses every seated name.** *What went wrong:* the Constel naming law stands in three rooms. `constel/name.rye` `check_ship`, called at `handshake.rye:117`, admits only **vowel-free** names; `pond/apps/constel_net.rye:98` and `constel_naming_scan.sh` kept the elder **digit** proof from a day earlier. All five seated names carry vowels and ten `pond/apps` modules seat them, unread by any guard. *What caught it:* the Glow-tree charter's booked defect. *What it taught:* **a promise proven twice is a promise nobody compares** -- both are sound, so safety held and every guard read green. *Repaired:* the scan mirrors `check_constellation`, gates the union, and prints `split_seated_refuses=5`; the planted fixtures caught this repair's own bug: a trailing hyphen Rye refuses `BadSegment`. **OPEN** on the redraw, Pond's lap. *Redraw landed (`20260828.120225`):* each elder name with its **vowels dropped** (`xnqvel34` -> `xnqvl34`), so all five satisfy **both** readings rather than trading one away -- `check_ship`'s alphabet is consonants **and** digits, so a name never had to choose. Ten modules repointed, two Bron fixtures regenerated, 15 witnesses GREEN; `split_seated_refuses=0` and `split=0` pinned. **The other direction stays open, pinned at `split_digit_absent=6`** -- where `generate()` draws every name `name_safe` refuses. Which room moves is a seat.

**REDS %311 (`20260828.114450`) -- the wall whose needle the harness itself supplies, and 152 runs that never read an answer.** *What went wrong:* `tools/g/glow_lower_assert_witness.rish:40` asserted `run_un.out contains "EXIT:"` on a run captured as `${un_bin} >/dev/null 2>&1; echo EXIT:$?`. The `echo` fires unconditionally, so `EXIT:` stands in that output before the binary is ever consulted, and the assertion could never red. The same reading found **152 answer-blind runs** across 35 witness files -- a lowered binary proven to launch and exit with a code, nothing asserted about what it said -- and **7 files** asserting a clean exit and never once, by any spelling, a refusal. `tools/g/glow_lower_shop_gate_witness.rish` runs 34 gate binaries that way, `${argv_bin} 9` among them, which is the argv shape %310 caught exiting clean and silent. *What caught it:* the meter written for it. A hand reading those same 35 files had already counted the never-refused files by grep and missed the vacuous needle entirely; the scan named it on its first run. *What it taught:* **the framing a harness prints is not evidence, and a substring test cannot tell the two apart.** %310 is this sentence one layer down -- there the oracle was derived from the implementation it checked, here the needle is derived from the harness that runs it -- and both pass for one reason: the checker and the checked share a source. *Repaired (`20260828.114450`):* the vacuous line becomes `assert run_un.ok`, matching its two siblings in the same file, and the honest negated refusal beneath it stands untouched. `tools/fixtures/g/glow_gate_answer_scan.sh` gates `laundered_needles` and `unread_runs` at **zero** and ratchets `answer_blind_runs` at **152** and `never_refused_files` at **7** under ceilings that only fall; `tools/fixtures/g/glow_gate_answer_control.sh` proves **22** cases on real planted `.rish` files -- the real hit and the real %310 needle bitten verbatim, every honest exit-code wall freed, and both ceilings proven from both sides by planting one past them, so no override exists. `tools/g/glow_gate_answer_witness.rish` GREEN on metal. **OPEN** on the two ratchets: 152 and 7 are lowering-side repairs, and a plant must speak before a witness can read it, which is Sound's language custody.




**REDS %326 (`20260828.182855`) -- the guard written to hold every path exactly knew 17 rooms of 82, so a room rename swept six dated files and read green.** *What went wrong:* commit `9f053c662` ("src: six rooms become three") repointed `src/lib/ -> src/gate/` and `src/{sur,app,sec,til}/ -> src/shape/` as a string sweep across 139 files, and **six of them were dated testimony** -- 7 lines, every one a historical statement turned into a present one. `tools/fixtures/d/dated_classify_seam.sh`, this tree's own shared classifier, reads all six as `dated`. The charter that *records the rename* took the worst of it: its before/after table now read `| src/shape/ | **src/shape/** |` in three rows, and its cost sentence bound four distinct rooms to one token with four different counts -- `src/shape/ 39, src/shape/ 8, src/shape/ 4, src/shape/ 3`. Two lines in `expanding-prompts/archive/` are quoted `RECOMMEND:` utterances, now saying words no lap ever said. *What caught it:* a hand reading the charter to find out what was still open on a granted door -- no meter, and the reason no meter fired is this row. `tools/fixtures/c/claim_preserve_scan.sh` is the instrument written for exactly this, and replayed with `CLAIM_PRESERVE_BASE=9f053c662^` it answered **GREEN on all five** non-charter files: *claim tokens identical*. The cause sat at `claim_preserve_extract.rish:39`, where the PATH class spelled a **hardcoded roster of 17 room names**. Measured `20260828`: this tree has **82 tracked top-level rooms**, so the class saw `context/` and `tools/` and was blind to **65** -- `src/`, `construction/`, `.claude/rules/`, `caravan/`, `comlink/`, `constel/`, `mantra/`, `tally/`, `kumara/`, `expanding-prompts/`, `docs-geode/`, `pond/`, `skate/`, `aurora/` and 51 more. Every register pass ever run through that guard held paths exactly in one room in five. *What it taught:* **REDS %301's sentence reaches past rooms to instruments.** There a meter discovered rooms by dated basenames and never reported the room nobody thought to name; here a meter recognized paths by a roster of names and never reported the room nobody thought to *add*. Same root, one instrument over, and the repair is the same one: ask git for every room rather than asking the tree for the rooms already named. *Repaired (`20260828.182855`):* `claim_preserve_scan.sh` derives the roster from `git ls-files` -- longest-first so `docs-geode/` wins the alternation over `docs/`, a leading dot escaped so `.claude/` is a room rather than a wildcard -- and hands it to the extractor, which now **refuses without it** (exit 1, proven) rather than quietly guessing, the same discipline the QA card keeps when it cites `measure()`. Proven **both ways on real history rather than on a plant**: replayed at `9f053c662^` the repaired guard names all five files and the exact tokens that moved; after a byte-exact restoration the same run is GREEN at exit 0, and the unrepaired guard read GREEN on those same bytes an hour earlier. Coverage measured on six living documents: PATH tokens **86 -> 132**, and the added tokens are real paths (`.claude/rules/*`, `construction/REDS.md`, `docs-geode/*`) rather than prose noise. **Restored:** 7 lines across the 6 dated files -- five whole files `cmp`-identical to `9f053c662^` (zero commits landed on them after the sweep), the charter's 7 lines by line number so the author's own later Status update stands. **OPEN** on the seam this cannot reach: `claim_preserve` is **opt-in**, running only when a pass names its own files in `CLAIM_PRESERVE_FILES`, so nothing obliges a sweep to run it at all -- binding a path-moving commit to this guard is a seat. The elder `tools/fixtures/c/claim_preserve_extract.sh` still spells the 17 and has no live caller; it is left as the fossil it is rather than repaired in two places.

**REDS %327 (`20260828.170003`) -- the per-host config is sourced ahead of the seated room, so an adopted credential directory was rebound empty.** *What went wrong:* `tools/ag/agent-jail.sh` moves each elder state directory into the `loops/` room with `adopt_state_dir`, then resolves `CLAUDE_STATE="${CLAUDE_STATE:-$LOOPS/claude}"` -- **thirty-four lines after** `source "$CONF"`. The shipped `enclosure.conf.example` still assigned `$REPO/.claude-state` and `$REPO/.cursor-agent-state`, the paths the adoption empties, so the `:-` default was dead code, `mkdir -p` recreated the elder, and `--rw-map` bound an empty directory over the agent's HOME. Measured: `.claude-state` **0 files**, `loops/claude` **5,229**, `.credentials.json` among them. *What caught it:* reading both halves of one seam in one lap. Every guard stayed green: an empty state directory and a fresh one are alike. *What it taught:* **a file sourced ahead of a default is the answer, and the default is only a fallback nobody re-reads.** *Repaired:* the example comments all three out, so the launcher's room is the only spelling, and `prefer_adopted_room` lets an adopted room outrank a pin holding **no file**, on stderr. `pond_enclosure_state_witness.rish` gates `pinned_elders` at zero over 24 pen cases. **OPEN** on this pier's own `enclosure.conf`, still pinning both: a per-host file is Keaton's hand. *Stashed unsent; booked `20260828.180405`, the spine having spent 323.*

**REDS %328 (`20260828.180405`) -- the roster's runner writes into a room a throwaway pen has no reason to own, so 21 of its own 46 proofs failed unseen for two days.** *What went wrong:* `standing_equipment_run.sh` appends to `construction/standing-equipment-hitrate.kyri` at open and writes its receipt at close, both entering at `e1c50fcbae` (`20260826.222031`). A bare `>>` cannot create a parent directory, so in a pen -- every place the control drives it -- the append died and took the runner with it **before a single guard ran**. At `26197f6ad6`, **21 of 46 behaviors read `no`**, all runner-driven. *What caught it:* new cases for that control finding old ones already failing beside them; `git show HEAD:` confirmed the 21 were not mine. *What it taught:* **a guard nothing runs is a guard nothing tells you about** -- the witness is on no roster and has no caller, last green `20260826.134505`, eight hours before the meter landed, and it is unrostered *structurally*: the scan refuses on any red the card holds, so a self-witness sustains its own. *Repaired:* both writes are guarded and **announce the skip**; creating the directory was refused, since an untracked `construction/` moves the very digest three of those cases read. Beside it, the rung the dead-letter box was owed: the runner reads `git stash list` on line one, each entry by message and file count, **reported and never gated**, bounded at 16. *The catch the cases paid for:* `git stash show --name-only` **omits untracked files** while `fleet_round_open.sh` stashes with `-u`, so a lap of all-new files read `0 files` -- 0 without `--include-untracked`, 2 with, git 2.54.0. *Proof:* 62 behaviors, the bound shown both ways, `control_verdict=ok`, zero `=no` on metal. **OPEN** on the seat it names: whether the roster's own witness can be rostered at all.

**REDS %330 (`20260828.202405`) -- one word wears two hats: the panchanga's fourth ring, and a family of numbered witnesses that predate it.** *What went wrong:* the seated calendar vocabulary (`foundations/20260826-014901_the-panchanga.md`) names the rings orbit - quest - journey - **equinox** - chapter, an equinox being 1,875 rounds; and the tree's guard family `equinox_e102`, `equinox_e123`, `equinox_e134`... -- 98 numbered files measured `20260828` -- 57 witnesses in `tools/equinox/witness/` and 41 scans in `tools/fixtures/` -- uses the same word for something else entirely: per-round guard drops marked by an ascending `eNNN` counter, the very shape the mark law retired (%329's own class, one room over). A reader meeting `equinox_e123_living_pin_guard` cannot tell whether it belongs to the fourth ring of the calendar or to the hundred-twenty-third of anything, and the answer is neither. *What caught it:* Keaton, holding both vocabularies at once -- the same catch as %329, memory where a meter should be. *What it taught:* **a seated lexicon term is a namespace, and a code family squatting it taxes every future sentence that needs the real one** -- the Comlink tendency's safety test (collides with nothing seated) applied backward in time, since the family predates the panchanga seating and nobody re-checked the collision when the calendar landed. **OPEN, booked: the fix is its own design lap.** Its questions are named so the lap starts warm: a family name that is not a calendar ring (the Comlink tendency draws it -- the family is per-era guard drops, closer to *strata* or *rings of a trunk* than to a season); whether the `eNNN` numbers convert to stamp-and-name marks in the same molt or ride the %329 ratchet; and the mechanical shape (the room just moved once, `20260828`, so the resolver-and-repoint recipe is fresh). Until that lap lands, new guards of this family take stamp-and-name marks rather than the next `eNNN`, and the launch cards say so.

**REDS %334 (`20260828.213503`) -- a witness named its build in a comment, then ran the binary.** *What went wrong:* `comlink/bin/` is gitignored (`.gitignore:185`), and `tools/co/comlink_handshake_turn_witness.rish` and `comlink_turn_route_witness.rish` carried `# Build first: rye build ...` then `run ["comlink/bin/handshake-turn" "selftest"]`. A comment builds nothing, so a clone died on a missing file rather than a handshake fact. *What caught it:* Comlink's four witnesses, each named by no runner. *What it taught:* **a repair without a meter fixes one of three** -- `comlink_topology_witness.rish:19` already had it. *Repaired:* both take it, GREEN with binaries deleted; `tools/w/witness_own_build_witness.rish` ratchets `unbuilt_pairs` at **46** over 35 witnesses (was 48). **OPEN** on the 46.
