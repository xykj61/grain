# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**REDS %393 (`20260831.035452`) -- a rostered choir builds into a gitignored directory it never creates, so it reds on every clone that has not built Mandate before.** *What went wrong:* `tools/o/operations_conformance_witness.rish` builds five Mandate binaries with `rye build mandate/$m.rye -femit-bin=mandate/bin/$m`. `mandate/bin/` is build output and `.gitignore` names it at line 191, so a fresh clone does not carry it, and `-femit-bin` into a missing directory fails the build. The witness then asserts `operations: a Mandate binary did not build`, which names the code and blames it for an absent directory. `tools/ac/acme_dx_witness.rish` runs that witness as one leg, so the choir rostered GREEN on the pier at `20260831.014630` -- 3s, measured on a bench that had built Mandate many times -- reds on a clone that has not. *What caught it:* this seat's own `--hot` close on the macOS bench: 108 guards, 104 green, four red, where two are the custody-gated pair the card names (`rule_twin` at gate %7, `pond_enclosure_door` at gate %5) and two were not. Running the same five builds by hand outside the witness built all five, which is the reading that tells a code fault from an environment one -- and the witness's own command then succeeded the moment `mkdir -p mandate/bin` had run. *What it taught:* **a witness that writes into build output must create it, because the one machine where a gitignored directory reliably exists is the machine that has already run.** That is `%360`'s stale-pin class turned over: there, unheard guards carried expectations pinned to a tree that had moved; here a heard guard carries an expectation pinned to a tree that has *worked*, and both are invisible on the bench that wrote them. A guard rostered from one clone carries that clone's history inside it until a second clone runs it, which is the argument for the fleet reading the same roster from different trees. *Repaired (`20260831.035452`):* one `mkdir -p mandate/bin &&` at the head of the build loop, with the reason beside it. Proven from a genuinely fresh state -- `mandate/bin` removed, then `acme_dx` GREEN end to end -- so the directory the repair makes is the only thing that carried it. *The fourth red is the same class at a custody edge and is named rather than taken:* `tools/co/commit_message_guard_witness.rish` asserts `SEED_PUBLISHER_ARMS 1` by grepping `publish-seed.sh`, which `.gitignore` line 8 keeps untracked at the root by design, so the guard cannot pass on any clone lacking the publisher -- including this one, whose seat forbids ever running it. **CLOSED.**

**REDS %392 (`20260831.031048`) -- a program carries two prose settings by law, the report card reads it at one, and the same unchanged file grades C, C+, or A depending on which word a hand types.** *What went wrong:* `context/GAUGE_STYLE.md` assigns two settings inside one source file -- **Door** at the head of a module, where a reader arrives holding no context, and **Meter** at a bound or an assert, where the comment names the failure the number was chosen against and negation is the subject rather than a fault. `tools/fixtures/q/qa_report_card.sh` takes a single `--setting` for the whole artifact and feeds it to both counted readings, so a program's blended comment corpus is weighed against one ceiling. Measured this lap on `comlink/virtio_net.rye`, one file, one state, three runs: **C 72** at `door`, **C+ 77** at `field`, **A 94** at `meter` -- a 22-point spread and two letter grades decided by the hand rather than by the artifact. The `meter` reading is the sharpest, since `grade_ceiling=0` and `xref_ceiling=0` make Register and Reach report rather than score, so any module carrying comments reads 100 on both counted halves without the instrument weighing a sentence of it. *What caught it:* the carded molt the live card itself named. Rewriting that module's head as Door prose moved Register **65 -> 73** (35% -> 27%, under Field's 30% ceiling) and left Reach pinned at **60**, grade 15 against 11 -- so the repair the reading asked for could not move the reading that asked for it. Grading the two halves apart said why: the new head measures **8.8** against Door's ceiling of 9, and the 501 words of `// invariant:` comments beneath it measure **16.4** against a ceiling the same law leaves uncapped. The head is already right; the file is graded on the average. *What it taught:* **when a law assigns two settings inside one artifact, a single-setting meter reports the hand's choice rather than the artifact.** Kin to `%276`, where this card threw a program's comments away and graded its code, and to `%358`, where its extractor knew `//` and `#` and not Glow's `::` -- the same instrument, the same class, one layer in. The halves are found now and still weighed as one. The QA rule's own test settles the direction: a meter that instructs a repair which would make the artifact worse is the thing to fix, and Field over this file instructs a writer to simplify the invariant comments TAME asks to be exact. *Named, not taken:* a card that reads a module head at Door and bound comments at Meter and reports two readings rather than averaging one. `%386` is the standing warning against the naive form, where a guard built in a hurry flagged 14 and was wrong about 13; the split here needs a rule for the doc comments between the two poles, since a `///` on a public function is neither a module head nor a bound. Shared machinery every body runs, so it wants its own lap and a control proving both poles from both sides. **OPEN.**

**REDS %391 (`20260831.031509`) -- the pin that reads newest first opens on its oldest shelf, and every loop's first instruction points at that row.** *What went wrong:* `session-logs/README.md` declares its table newest-first and orders 40 day rows that way, `20260831` down to `20260722-shelf` -- with `through-20260721`, the 775-row shelf holding everything before the fold began, at the TOP. Every seat prompt in the fleet opens on *the newest day shelf named at the top of `session-logs/README.md`*, so a loop obeying its own instruction lands on the oldest shelf in the tree. The row leads `session-logs/CHAPTERS.md` correctly, whose table is **oldest-first**; it carried that page's order into a page with the opposite one. *What caught it:* a hand reading the pin's header sentence against its first row, in the gap `tools/fixtures/s/shelf_link_touch_scan.sh` already declares it does not reach -- *"or that the row is in the right order. This proves the door opens."* *What it taught:* **two pages sorting one set of rows in opposite orders will trade rows, and a link guard cannot see the trade, because both orders link correctly.** *Repaired (`20260831.031509`):* the row moves to the foot of the pin's table, where its range puts it; `CHAPTERS.md` is untouched. **BOOKED** -- a guard reading a declared order against its own rows is a lap.

**REDS %390 (`20260831.031509`) -- the roster records what every guard answered and never what it cost, so a lap sizing a pass against its own clock reads a window of it, over a per-guard cost this pass measured with a median of 2.5 seconds and a maximum of 280.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` writes one `ran <name> <stamp> <verdict> <tier>` line per guard and no elapsed time, so `construction/standing-equipment-runs.kyri` held 106 verdicts and not one cost. On `20260831.023122` a lap watched 15 guards for 30 minutes, took 2 min/guard as a rate, projected a 106-guard close at three and a half hours, and shipped without a full roster -- an honest deviation its own log named. It rested on one assumption, that guard cost is roughly uniform, and nothing here recorded the distribution that assumption waves away. *What caught it:* this lap's cold open, piped through a `while read` loop stamping each guard's line with `date +%s`. **28 guards, all green, 865 seconds:** `sow` 280, `living_card_ascii` 189, `dated_path` 90, `phantom_path` 73. Median **2.5s**, mean **31s**, three guards holding **65%** of the total. The roster's own header already documented `caravan_suite` at 8m31s beside guards finishing inside a second: the header knew the spread, the record did not. *What it taught:* **a mean fifteen times its own median is not a rate, and an instrument recording a verdict without its cost hands every reader an estimate to make and nothing to make it from.** *Repaired (`20260831.031509`):* the runner brackets each guard with two `date +%s` calls and writes elapsed seconds as a sixth field -- `ran <name> <stamp> <verdict> <tier> <seconds>` -- printing it beside the verdict with a `guards_seconds` total at the close; `standing_equipment_scan.sh` reads it into `runs_seconds_total`, `runs_seconds_absent` and `runs_slowest`, so cost is read by the guard proving the roster honest. Seconds rather than finer: the reading sizes a pass, it does not profile a guard. Elder rows carry five fields and count **absent** rather than zero -- and a guard timed AT zero still claims the slowest seat, or a card of fast guards would read exactly like a card nobody timed, which is this same fault one layer down. Twelve behaviors join the control, both ways. **BOOKED** -- whether `--scoped` may chain from a full pass whose only reds are custody-gated remains `%374`, Keaton's word.



**REDS %374 (`20260830.192524`) -- the receipt is written past the red exit, so a tree whose reds are parked at a custody gate can never earn the cheaper pass, and the refusal told it to run the roster again.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` writes `construction/standing-equipment-receipt.kyri` below the `if [ "$red" -ne 0 ]` branch that already exited 1, so a full pass carrying any red leaves no receipt at all -- and `--scoped` reads that receipt for its basis and refuses without one. On this clone both of the roster's red guards sit at custody gates the living card names, `pond_enclosure_door` at gate %5 and `rule_twin` at gate %7, so every full pass closes `guard_red`, no receipt has ever been written here, and the fusion build's cheaper pass is unreachable by construction rather than by delay. The refusal read `run the full roster once` to a hand who had run it twice that morning, at **2h27m** a pass -- the run card's open stamp `20260830.080600` against its own mtime. The same shape stands one layer up: `tools/s/standing_equipment_witness.rish`, the guard that proves the roster honest, refuses at its scan leg for `runs_red=2` on those same two gated rows, so the instrument that measures the roster is unrunnable on exactly the trees carrying a gate. *What caught it:* this lap's own cold open, which printed `roster_receipt=none` while `construction/standing-equipment-runs.kyri` held a complete 96-guard pass from that morning; both readings cannot be innocent at once. *What it taught:* **one counter carrying two meanings decides for a mechanism that never asked which one it meant.** `red` counts *this guard broke* and *this guard is parked at a gate the maintainer named* identically, and the receipt gate reads their sum -- REDS %338's own class, where OPEN carried both *live defect* and *instances repaired, remainder booked* until the ledger deadlocked on it. *Repaired (`20260830.192524`):* additive readings, no gate moved -- a full pass closing red prints `roster_receipt_write=withheld_guard_red`, and a `--scoped` without a basis prints `scoped_basis_blocked=<guards>` read from the run card, eight names then a count, with a refusal that names them rather than prescribing a pass that cannot help. Six cases in the throwaway pen, three proven to bite under a runner stripped of both lines; the fourth is written down as a companion rather than counted as a refusal it is not. **Whether a receipt may chain from a close whose only reds are custody-gated and card-named is Keaton's word**, since it loosens what a receipt promises -- until then the diagnosis is honest and the cost stands. Still **OPEN**.
**Rows: 394 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 374** -- counted from the ledger and its archives on `20260831.043810` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 394 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
