# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 443 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 423** -- counted from the ledger and its archives on `20260905.231715` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 443 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %441 (`20260905.225236`) -- the commit that turned on the proofs of a thing declared unbuilt, in the same commit that declared it.** *What went wrong:* commit `b4a647f68` did two things in one lap. It rostered twenty Mantra witnesses onto `construction/standing-equipment.kyri`, four of them at `tier lap` -- `mantra_gen_floor_a1_gate`, `mantra_glow_tend_limb1`, `limb2`, `limb3` -- whose entire job is to read `mantra/src/weave.rye` and assert `pub const Line = struct`, `text: []const u8,`, `gen: u32,`, `pos: u32,`, `pub const Weave = struct`, `pub const Diff = struct`, and `gen >= 1`. And it added `active-designing/20260905-153729_mantra-was-named-for-the-weave.md`, which concludes *"the shape it was named for still waits to be built"* and books a three-round movement, *The line, and its count*, to write "one authored Rye module holding a line, its arrival, its departure, and the integer that ticks between them." That module is `mantra/src/weave.rye`, and it has stood since this tree's root commit `7d991702f`. Its sibling `mantra/src/main.rye` opens: *"Mantra is the version-control layer of the Rye family. It holds the full history of a text file as a weave -- a list of lines each carrying a stable position and a generation count that names whether the line is present or gone."* Three rounds were booked to write again what four rostered guards prove every lap. *How the essay could not see it, and every number in it is correct:* its six-row measurement table changes scope between rows. Rows one and two read the room at every depth -- `mantra/` Rye modules **32**, lines **9,175**; the row the conclusion rests on reads it flat and says so in its own label -- ``mantra/*.rye`` files mentioning conflict, merge, three-way or diff3, **0**. Measured `20260905.225236`: flat is **27** files and **7,976** lines, recursive is **32** and **9,175**, and that same grep run recursively reads **1** -- `mantra/src/weave.rye`. The 1,199-line difference is exactly `mantra/src/`. The essay never writes the string `src/`; `mantra/README.md` line 36 does, naming *"Mantra's own seed: `main.rye`, `diff.rye`, and the Weave aspect (`weave.rye`) beneath the namespace."* The claim then travelled: `tools/l/patchouli_seat_prompt.txt` is **tracked** and carries *"a WEAVE it has yet to grow"* into every launch of this ship. *What caught it:* the inbound-reference sweep before naming a new module. This lap opened to build that second movement and swept the tree for `weave` collisions first under *references are promises*, which returned `mantra/src/weave.rye`. Counsel `20260707-025212` had read the same file and named the same collision fourteen months of laps earlier. *What it taught:* **a table that changes its glob between rows must change its subject word too.** No number was wrong and no row was mislabeled; what crossed the two scopes was the **conclusion**, which read six rows under one heading as one measurement, and no reader catches that without re-running both globs. **The same lantern has fired twice before from the other side of one gap:** `room_bound`'s dated-basename pattern matched none of `tools/`'s 1,917 entries, so it read ZERO for that room's whole life while the room stood 7.4x over bound with every guard green; and REDS %175 found 237 logs carrying a stamp and no sprig, which a pattern requiring one read as living. Pattern-blind and depth-blind are two spellings of one fault -- **the instrument's reach did not match the room it was pointed at, and the reading came back clean.** *Repaired (`20260905.225236`):* an erratum on the essay, which is dated testimony and takes facts by erratum rather than rewrite -- movement two stands **built**; movement three stands **half built** (permanent positions and `current()` in position order stand; the merge of two weaves and its commutativity proof do not exist); movements four through six stand as written, and the essay's crux is unchanged. The seat prompt, being a living path, is corrected in place. And the loom: `tools/fixtures/m/module_room_reach_scan.sh` publishes, for every room holding tracked Rye, what a flat glob reaches against what the room holds -- **44 rooms, 14 with a hidden shelf, 513 files below a top level, and 8 rooms where a flat reading reports ZERO Rye while the room holds some**, `rye/` with 117 and `tools/` with 131 among them. It gates the **instrument** rather than the structure: a subdirectory is good structure and this meter never asks a room to flatten, so it refuses only when it read nothing (`verdict=blind`) or when its three counting patterns disagree. That arithmetic gate was itself one subtraction from being decorative -- `hidden` derived as `recursive - flat` makes the check true for every input -- and it is counted by its own pattern now, with a control leg that breaks one pattern in a copy and proves the refusal. Six behaviours on real git repositories in a throwaway pen, every refusal shown from both sides. **CLOSED** on `tools/m/module_room_reach_witness.rish` GREEN.

**REDS %440 (`20260905.233435`) -- a rebase left the day shelf out of its own newest-first order, and the guard drawn for the last rebase fault on this exact file reads bytes, links, and duplicates rather than sequence.** *What went wrong:* this lap's row (`20260905.224930`) and a peer's (`20260905.224714`) landed on `session-logs/date/README-index-20260905.md` within four minutes of each other. The rebase auto-merged the shelf **cleanly** -- no conflict, no marker, nothing to resolve -- and seated the peer's OLDER row above the newer one, so the pin that promises *newest first* read `224714`, `224930`, `213047`: descending everywhere except across the seam the merge had just made. *What caught it:* reading the merged shelf by eye after `git rebase --continue` reported success, then `grep -oE` over the stamps to see the sequence rather than trust the diff, which reported one row added and said nothing about where it went. *What it taught:* **this is `%381`'s lantern firing a second time, one fault over.** That row caught a rebase re-applying a row without lifting the stale one -- 76 rows standing for 63 logs -- and built [`../tools/in/index_row_bound_witness.rish`](../tools/in/index_row_bound_witness.rish) to watch the open shelf. Measured here, that witness asserts exactly four things, `rows_over=0`, `rows_unresolved=0`, `rows_duplicate=0`, and `verdict=ok`, and its scan's three uses of the word *newest* all pick WHICH shelf is open rather than reading sequence WITHIN one. So the guard drawn for a rebase fault on this file is blind to the neighboring rebase fault on it, and the difference between them is only whether the merge produced two rows or two rows in the wrong order. **A merge that reports no conflict has still made a decision**, and an ordering law that no guard reads is a law that holds exactly as long as every hand stays careful. The order is repaired in the commit that found it; the loom -- one assert that the open shelf's stamps descend, proven from both sides -- is a booked lap. **BOOKED.**
**REDS %439 (`20260905.224714`) -- a doc comment kept the universe ten percent larger than the constant thirty lines above it, and no meter in this tree reads a number written in prose.** *What went wrong:* `comlink/topology.rye`'s doc line over `decode` read *"or null when it is past the universe's 792"*, while `universe_points` has read **720** since the inclusive breach. 792 is the count the three number spaces have when they are held **apart** -- 12 galaxies plus 60 stars plus 720 planets -- and inclusion is precisely the decision that every galaxy is also a star and a planet, so the universe became the planet count alone. The code was right the whole time and refuses 720 exactly; the one sentence a reader meets before the function told them otherwise. *What caught it:* reading the module to build a routing-metric census for a research brief -- not a guard, because no guard here compares a number spelled in a comment against the constant declared above it. The tree had already written the correction down: `external-research/20260810-020543_inclusive-address-space-and-the-outfit.md` says *"the universe is 720, not 792"* in its own third line, and has since the breach landed. *What it taught:* **a breach that changes a derived total must sweep the prose that quotes the total, and this tree meters numbers in asserts and not one written in a comment.** `borrowed_number` reaches a stale expectation inside a Rishi assert; `ascii_comment` reaches a comment's bytes and never its arithmetic. Between them sits a whole class -- a figure a comment states about the code beside it -- that only a reader finds, which is `%341`/`%357`'s borrowed-number family one room further out. The elder 792 also stands in the day-shelf row that recorded the landing, and that is testimony and correctly stays; what drifted is the living source. *Repaired (`20260905.224714`):* the line reads 720 with a **say-why** beneath it naming both counts and the breach between them, so the next reader meets the history rather than only the answer. `rishi/bin/rishi run tools/co/comlink_topology_witness.rish` **GREEN on metal** after the edit, printing its own `720/universe` from the constant. **CLOSED.**

**REDS %431 (`20260905.135216`) -- the fix lived in a file that cannot travel and the claim that it was fixed lived in one that can, and they shipped in the same commit.** *What went wrong:* `%423` closed `commit_message_guard` on the Incense tree by reconstructing `publish-seed.sh`, and lifted its `gate %1` field in the same lap on exactly the right reasoning -- *a gate standing over a green guard would excuse the NEXT red*. Only one of those two changes could travel. `publish-seed.sh` is **untracked at the root by design**, ignored by `.gitignore` line 8's root wildcard `/*`, which is the privacy clause `.claude/rules/git-signing.md` states outright: *anything a wipe would disarm is armed in the script.* `construction/standing-equipment.kyri` is tracked. So the commit carried the gate removal to all three Earth trees and carried the repair to none of them, and on every tree but the one that ran the lap the guard went from *red and explained* to *red and unexplained*. *What caught it:* running `tools/co/commit_message_guard_witness.rish` on `grain-petrichor` during an unrelated restore lap, one hour and eleven minutes after `%423` was written. Measured here `20260905.135216`: `GUARD_HOOK_PRESENT 1`, **`GUARD_PASS 25`, `GUARD_FAIL 0`**, `GUARD_TEACHES 1`, `GUARD_ARMED 1`, `SEED_ARMED_REFUSES 1`, `SEED_ARMED_WELCOMES 1`, `SEED_SHIPPED_MESSAGE_PASSES 1`, `GUARD_OK` -- **ten of eleven readings green**, and `SEED_PUBLISHER_ARMS 0` alone reds the witness at line 58. The wall itself is entirely sound on this tree; the one leg that is not reads a file `git` was told never to carry. *What it taught:* **a repair proven on one tree and a claim published to all of them must not ride in one commit -- and the tell is that the repair is untracked.** This is `%174`'s shape (the roster ran on one tree and the commit shipped another) and `%422`'s (a control that names a machine cannot be run on the fleet it guards) in a sixth material, and the sharpest of the three, because the other two shipped a *wrong reading* while this one shipped a **correct** reading with its explanation removed. The compounding cost is the one `%422` already priced: a red guard withholds the roster receipt (`roster_receipt_write=withheld_guard_red`), and `--scoped` refuses without a full green receipt, so two of three trees pay a full cold pass every lap for a file that was never meant to be in the commit. *Named rather than built, and it wants a word, because it touches the seed publisher at custody gate %1:* three shapes were weighed. **Reconstruct the publisher on each tree** is what `%423` proves is agent-doable, restores the guard honestly, and leaves the same drift waiting for the next tree that is born -- so the durable form of it belongs in `birth_a_clone` rather than in three hands. **Read the arming from a tracked proxy** is refused by the guard's own header, which promises the shipped hook is fed planted messages *never a copy*, so proving the arming against something the tree does not run is the one thing that leg exists to prevent. **A `host` or `capability` field** is refused by `%422`'s own reasoning, recorded there about `agent_jail`: it would mark the guard skippable on trees where it genuinely works. The gate field stays off; `%423` was right to lift it, and this row is the NEXT red it was lifted to stop excusing. **OPEN.**

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
