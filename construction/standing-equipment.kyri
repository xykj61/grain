# construction/standing-equipment.kyri -- the standing guards, as a list a program can read.
#
# WHY THIS FILE EXISTS. The roster of standing equipment lived in one paragraph of
# construction/ITINERARY.md, in prose. A paragraph can be read and it can be trusted; it cannot
# be run, counted, or dated. Two consecutive rounds paid for that: REDS %147 found eight
# standing witnesses each holding a count its source had moved past, and REDS %149 found
# the living-pin bound guard unrun for four laps while construction/REDS.md sat 2,050 bytes over
# the bound it measures. Both were caught by a hand that happened to run something.
#
# The tree already seats the strand this file's sibling completes. The grain says a guard
# that cannot red guards nothing (REDS row 59), and every guard here can red. The strand
# beside it, learned the hard way twice: a guard that is never run guards nothing either.
#
# WHAT A ROW MEANS. One `guard` record per standing check, each naming the path a runner
# invokes, the tier it runs on, and the stamp the guard was seated.
#
# WHAT A TIER MEANS. Guards cost wildly different amounts of time. A choir -- one witness that
# sings a whole family of rungs -- takes minutes: `caravan_suite` runs 111 rungs in 8m31s,
# `crypto_suite` runs 74 in 9m06s, and `ales_suite` runs 240 in 233s and 303s over two clean
# passes, all measured on this pier on 20260825, against a whole roster of 20m20s with one of them
# seated. A lap reads the roster twice, cold at the open and hot after `git add`, so every choir on
# one tier would cost most of an hour a lap. So a guard names its own clock:
#
# COST DECIDES THE TIER, AND SO DOES WHAT IS MOVING. `caravan_suite` measured 840 seconds on
# 20260831 and serializes 121 witnesses over shared runtime paths. Its roster bijection is cheap
# and stays on every lap; the full sing runs on cadence, when scoped work reaches Caravan, or when
# a hand asks for it by name. `ales_suite` also stands on cadence because its Lotus rungs are stable.
# Revisit a row's tier when either family reopens.
#
#   tier lap       every roster run. What a record naming no tier means, which is why none of the
#                  rows below had to be edited when the field arrived.
#   tier cadence   the cadence lap -- the fifth round, where the council rota
#                  closes its cycle -- and any lap where a hand asks for the guard by name.
#                  (The seed once shipped on this clock too; that cadence was CUT 20260826,
#                  and custody gate %1 holds every refresh at Keatons hand.)
#
# A tier is a CADENCE rather than an exemption. REDS %219 was a choir standing off this roster
# altogether, which is a refusal nobody receives; a cadence guard is still heard, on a slower
# clock, and the run card records which clock heard it. A tier word the runner does not know is
# refused at zero, because a guard seated on one would run on no lap at all, in silence. The roster is the authority for what stands;
# construction/standing-equipment-runs.kyri records when each last ran ON THIS PIER, and that card
# is deliberately untracked, so a fresh clone reads `never run here` rather than inheriting
# another machine's memory.
#
# HOW IT IS KEPT HONEST. tools/s/standing_equipment_witness.rish proves every path exists,
# refuses a run card naming a guard the roster never seated, and refuses any recorded RED.
# Add a guard by adding a record here; the meter counts the file rather than a sentence.
#
# ONE PASS AT A TIME. The runner takes a directory lock at zero wait before it measures anything,
# so a second pass in the SAME tree refuses by name rather than deleting binaries out from under
# the first (REDS %359). The lock path is relative to the repository root, so the fleet's other
# trees run freely beside each other.

format standing-equipment-v1
stamp 20260825.183336
voice Kyri
runner tools/fixtures/s/standing_equipment_run.sh
card construction/standing-equipment-runs.kyri

guard banner_room
path tools/b/banner_room_witness.rish
seated 20260822.231949
tier cadence   # banners across every room; moves when a room opens or a fossil is bannered

guard dated_path
path tools/d/dated_path_witness.rish
seated 20260821.203103
tier cadence   # a census of dated references across ~19,787 tracked files; its answer moves when a room folds

guard docs_command_path
path tools/d/docs_command_path_witness.rish
seated 20260908.193508
tier lap   # one awk over every fenced block and every inline "Run `...`" span of 5,787 tracked pages, measured 5s -- cheap enough to run every lap, and the fault it catches is spent by a reader on their first hour

guard dated_path_repoint
path tools/d/dated_path_repoint_witness.rish
seated 20260821.203103

guard documented_room
path tools/d/documented_room_witness.rish
seated 20260821.203103

guard empty_document
path tools/e/empty_document_witness.rish
seated 20260823.041442

guard equinox_e123_living_pin_guard
path tools/equinox/witness/equinox_e123_living_pin_guard_witness.rish
seated 20260817.000000

guard exec_bit
path tools/e/exec_bit_witness.rish
seated 20260823.025333

guard fetch_toolchain
path tools/f/fetch_toolchain_witness.rish
seated 20260821.203103

guard foundations_link
path tools/f/foundations_link_witness.rish
seated 20260821.230319

guard generated_page_freshness
path tools/g/generated_page_freshness_witness.rish
seated 20260823.000512

guard geode_libraries
path tools/g/geode_libraries_witness.rish
seated 20260821.203103

guard living_card_ascii
path tools/l/living_card_ascii_witness.rish
seated 20260817.000000
tier lap       # the card and the ledger, which every lap writes

guard living_docs_lint
path tools/l/living_docs_lint.rish
seated 20260821.203103
tier cadence   # ADVISE-only -- it prints advisories and never fails, so its reading is a trend

guard nib_honesty
path tools/n/nib_honesty_witness.rish
seated 20260821.211943

guard radiant_negation
path tools/r/radiant_negation_witness.rish
seated 20260821.211943

guard readme_metrics
path tools/r/readme_metrics_witness.rish
seated 20260821.203103
tier lap       # pages the pre-commit hook regenerates at every commit

guard reds_ledger_monotone
path tools/gen/chapter/reds_ledger_monotone_witness.rish
seated 20260821.235936

guard reds_spine_derive
path tools/r/reds_spine_derive_witness.rish
seated 20260827.181605

guard room_bound
path tools/r/room_bound_witness.rish
seated 20260821.203103

guard rye_bridge_cycle
path tools/r/rye_bridge_cycle_witness.rish
seated 20260821.232733

guard sha3_file
path tools/s/sha3_file_witness.rish
seated 20260821.203103

guard phantom_path
path tools/p/phantom_path_witness.rish
seated 20260823.010937

guard prose_register
path tools/p/prose_register_witness.rish
seated 20260823.045448

guard seed_link
path tools/s/seed_link_witness.rish
seated 20260823.034321


# sow_allow_reach -- the seed's allowlist proven from the SHIPPED side. sow_witness asks did
# anything private get out; seed_link asks does a shipped link land; neither can ask did the room we
# allowed actually arrive. On `20260906` a second filter by name inside sow_project.sh discarded an
# allowed room, the projection ran, and sow_witness answered GREEN -- because a room that ships no
# bytes leaks no names (REDS %485). A publish is as wrong when a room is missing as when a name
# leaks, and quieter.
# capability seated 20260906.140000 (REDS %493). The guard reads the SHIPPED side, so it needs a
# projection at `seed/` -- which is gitignored, built by `tools/s/sow.rish`, and therefore absent
# from every fresh clone. Rostered `tier lap` with no capability it went red on this tree's first
# cold pass after landing, and a red guard withholds the roster receipt, so `--scoped` refuses and
# every ship pays a FULL cold pass every lap: 778s measured here. The scan's own refusal is right
# (REDS %170) and the roster is where the environment fact belongs -- the same reading the operator
# card already gives an empty `vendor/`. Absence is announced by name rather than made silent.
guard sow_allow_reach
path tools/s/sow_allow_reach_witness.rish
tier lap
capability seed_projection
seated 20260906.122632

# sealed_digest -- a digest this tree records about its own bytes, compared to nothing, is prose. The
# algorithm split is already right and principled -- SHA3 where the choice is ours, SHA-256 where an
# outside protocol, vendor, or coreutils-only verifier fixes it -- so the open question was never
# WHICH hash but whether any seal is read. Holds unread at zero, reads past dated testimony, a bare
# fingerprint, and an outsider's quoted published value.
guard sealed_digest
path tools/s/sealed_digest_witness.rish
tier lap
seated 20260906.192644

# unshared_citation -- derived-spine rule 4 made a gate. A row's %N is a VIEW the anointed remote
# allocates, so a lap cites its own row by STAMP until that number is bound upstream. The rule stood
# ten days as a habit and the habit never formed: 28 recorded errata, six rows renumbered more than
# once, one four times in an hour -- and each renumber sweeps 20 to 36 files that spelled a number
# the stamp would have spared. Reads shared_max from the spine on every run; the ledger, testimony,
# and a five-digit colour are each read past for their own reason.
guard unshared_citation
path tools/u/unshared_citation_witness.rish
tier lap
seated 20260906.210117

# rota_declared -- the council rota's reading, counted rather than assumed. On `20260906` the record
# answered three ways across 109 logs: 20 carried a `rota` field, 53 named a row in prose alone, 36
# said nothing -- one observation carrying two claims (whether a rota was read, whether it was
# recorded), which is the single-stranded braid. The field existed in practice and in no law until
# that day. REPORTED, never gated: a lap that legitimately did no rota read is indistinguishable
# from one that skipped it, and a gate that cannot tell them apart reds on honest work.
# capability seated 20260907.003211. REPAIRED `20260907.114500`: the guard counted TODAY's logs and
# nothing else, so between 00:00 and the day's first COMMITTED log there was nothing to count and
# its scan refused -- right about the corpus (REDS %170) and wrong about the subject, which made an
# ordinary daily state read as a fault. Measured at 00:12 on `20260907`, the first cold pass of a
# day answered `guards_red=2` on one root: this guard, and `standing_equipment` reading its own
# roster's red. A red guard withholds the roster receipt, so `--scoped` refuses and every ship pays
# a FULL cold pass -- daily, fleet-wide, and healing itself an hour later, which is worse than a
# standing red because it looks like somebody's fault and then vanishes. The scan now falls back to
# the newest shelf that HOLDS a corpus, at most seven shelves back, and prints `day_source=` so an
# open-day reading and a fallback reading are told apart; a day a caller NAMES still refuses.
guard rota_declared
path tools/r/rota_declared_witness.rish
tier lap
capability day_shelf
seated 20260906.222120

# status_declared -- a lap's own verdict on its own lap, counted. `session-log-provenance` asks every
# log to finish with `scope` and `status`; measured `20260907.200326`, 21 of 95 logs carried both and
# 73 carried neither. The gap came from an incident: an incense loop cancelled at the moment its lap
# completed left its work staged and whole, claimed no send, and nothing in the record could say
# whether it had been interrupted or had finished and declined. REPORTED, never gated -- a lap killed
# mid-send cannot write a status by definition, so a gate would red hardest on the very laps it makes
# visible. `capability day_shelf` for the same reason its sibling carries it: the scan refuses when
# the open day has no shelf yet, which is honest rather than a fault.
guard status_declared
path tools/s/status_declared_witness.rish
tier lap
capability day_shelf
seated 20260907.200326

# fold_shelf_link_repoint -- the guard that names a repair can now make it. A shelf sits a level
# below the pin it was folded out of, so every climbing link must gain a level, and NOTHING writes
# these shelves -- a hand writes each one. Four different hands missed it in the week to
# `20260907`, each repairing by hand. The scan already computed the corrected path and only ever
# proposes one it has proven present on disk; this applies exactly that column, leaves a dead link
# for a reader, and writes through the original inode so a mode survives.
guard fold_shelf_link_repoint
path tools/f/fold_shelf_link_repoint_witness.rish
tier lap
seated 20260907.212612

# itinerary_account_shelf -- the shelf nothing wrote now has a writer, so its sibling above is the
# repair and this is the prevention. Every send folds a ship's live-front account one directory
# deeper, and every relative link in the block must gain a level. The filter performing exactly that
# rewrite has been general since `20260908.104232`, when its own header was widened to spell this
# invocation -- and the fault fired again the same day, from more than one ship. Measured
# `20260909`: 208 account shelves stand, all hand-written, and NOTHING under `tools/` wrote one.
# The fault was never an absent tool; it was a tool nothing calls. Its REDS sibling refuses to invent
# a header because a REDS shelf's header is judgment; an account shelf's header is a seat, a stamp,
# and one sentence that never varies, so this writer generates it rather than asking for it.
guard itinerary_account_shelf
path tools/i/itinerary_account_shelf_witness.rish
tier lap   # one control in a throwaway pen plus two readings of the standing shelves, measured 1s
seated 20260909.002517

# loom_trend -- the reader the loom line was promised and never had. `session-logs.md` records a
# measurement on a `loom` line *so a future Loom-aware reader can trend them across logs*; measured
# `20260907.222830`, 3,426 measurements stood across 1,577 logs and NOTHING read one. The convention
# was healthy -- 115 of 116 logs that day carried a loom line -- so this is a reader rather than a
# census. Its first question had been unanswerable: the roster grew 17 guards -> 173 while its wall
# time FELL.
guard loom_trend
path tools/l/loom_trend_witness.rish
tier lap
seated 20260907.222830

# convergence_census -- the tree's own idempotence claim, counted. `what-brix-infuse-is` states
# `infusion(world') -> world'`, and a tool that fails it corrupts on its SECOND run, which is what an
# unattended loop performs at three in the morning. Re-run `20260908.190452`: **10 candidates, 3
# proven**, and the census's own header names some of the ten as libraries or a remote-reading scan
# rather than writers. REPORTED, never gated -- a one-shot publisher has nothing to converge and no
# reading here tells it from a tool that never checked. The denominator was wrong FIVE times: 392
# counting pen writes, 1200 when the census read its own bound as a measurement, 30 counting
# controls, 7 counting non-writers, and 10 admitting the exec-bit write idiom it had been blind to.
# A count this unstable is why the seat's own page says to RUN it rather than read it.
#
# AND THE NUMERATOR WAS WRONG ONCE, in the other direction (`20260908.190452`): it greped every
# tracked path holding the tool's stem, the tool ITSELF included, on any line comment or not, so a
# tool writing `Idempotent: a second run changes nothing` in its own header certified itself. Two
# of the five did exactly that. The census header had stated the rule correctly all along --
# *inside a check rather than a comment* -- and nothing read it. Proven fell to **3 of 10**, and
# the control runs the elder numerator over the same plant to prove the repair moved something.
#
# AND THEN THE NUMERATOR GREW A SECOND SOURCE (`20260908.215031`), because a proof that runs every
# lap was invisible to it. `convergence_tree_prove_witness.rish`, rostered below, proves
# `tools/fixtures/r/reds_ledger_headline_write.sh` converges -- the writer `tools/hooks/pre-commit`
# runs on EVERY commit -- and that tool stood in the unproven column, since only a SIBLING
# assertion counted. A tool is proven now when a runner that is not the tool names it and a
# convergence prover on one non-comment line, and the split is printed:
# `proven_by_sibling_assertion` says a hand wrote a check, `proven_by_prover_run` says somebody ran
# the question. Proven reads **4 of 10**. Six control legs prove it from both sides, the elder
# numerator among them.
guard convergence_census
path tools/c/convergence_census_witness.rish
tier lap
seated 20260907.234808

# convergence_prove -- one prover where 27 hand-written cases were owed. The census found 30 tree
# writers with 3 proving convergence; hand-writing a case into each of the other 27 would be the
# census's own finding performed once per tool. 22 of the 27 share one invocation shape, a single
# file path, so one prover answers for all. Three verdicts, and `inert` is the load-bearing one: a
# sample that triggers no work is the quiet way a convergence proof lies.
guard convergence_prove
path tools/c/convergence_prove_witness.rish
tier lap
seated 20260908.005205

# convergence_tree_prove -- the nine of ten candidates that answer to a flag rather than a path.
# `convergence_prove` invokes a tool as `sh <tool> <one-path>`, and nine of the ten the census finds
# are whole-tree operators driven by `apply`, `write`, or `--check`, so no prover could reach them
# and the column read whether somebody wrote a check rather than whether the tool converges. Here
# the subject is a repository: the pen is a `git worktree` checkout of HEAD, the comparison is
# `git write-tree`, so a mode-only change is caught where a line diff sees nothing, and `--perturb`
# hands the pen a tree with work waiting. Six verdicts; `perturb_inert` was earned when this
# guard's own first witness passed a `sed` through Rishi and the escaping ate it.
guard convergence_tree_prove
path tools/c/convergence_tree_prove_witness.rish
tier lap
seated 20260908.204122

# rota_grid -- the twenty paths the council rota rests on, held as a set. The 5 x 3 grid in
# `recursion-prompts/seed/autonomous-loop.seed.md` returns this tree's canon to living awareness on
# a schedule, and its five thresholds are Markdown links while its fifteen seats are backticked bare
# paths no link wall follows. On `20260907` the Earth threshold still routed its reader to the seat
# Earth-Cardinal replaced two days earlier -- both paths resolving, so nothing was broken and no wall
# could see it. Three readings gated at zero: a path the grid names that is missing from disk, a
# threshold missing a modality heading, and a modality section naming a seat other than its own.
guard rota_grid
path tools/r/rota_grid_witness.rish
tier lap
seated 20260907.004329
# capability seated 20260906.140000 (REDS %493). The guard reads the SHIPPED side, so it needs a
# projection at `seed/` -- which is gitignored, built by `tools/s/sow.rish`, and therefore absent
# from every fresh clone. Rostered `tier lap` with no capability it went red on this tree's first
# cold pass after landing, and a red guard withholds the roster receipt, so `--scoped` refuses and
# every ship pays a FULL cold pass every lap: 778s measured here. The scan's own refusal is right
# (REDS %170) and the roster is where the environment fact belongs -- the same reading the operator
# card already gives an empty `vendor/`. Absence is announced by name rather than made silent.

# publish_template -- the one file this tree ships that a stranger RUNS against their own forge.
# publish-seed.sh is personal by law, so a newcomer inherits every room except a publisher;
# publish-seed.template.sh is that room. Two gates: no literal this field owns survives in it, and
# no stub lacks the GLOW_PROFILE field that answers it. Both fail QUIETLY otherwise -- a leftover
# remote reads as a working default until somebody pushes to a repository that is not theirs.
guard publish_template
path tools/p/publish_template_witness.rish
tier lap
seated 20260906.133118
guard shipped_binary_claim
path tools/s/shipped_binary_claim_witness.rish
seated 20260821.203103

guard tame_style_check
path tools/t/tame_style_check.rish
seated 20260707.164612
tier lap       # authored Rye style, named on touch by the same

# THE THREE TAME GUARDS THE LAW PAGES NAME AND NO ROSTER HEARD, seated 20260908.111848.
# `context/TAME_CORE.md` closes on a section titled "The checkable surface (the audit runs these)"
# and names five tools. Four of the five reached a lap only because somebody rostered them one at a
# time for their own reasons; the fifth, `rune_assert_sweep`, was rostered by nobody. The runner the
# page names for all five -- "the TAME Guidance Audit Quest" -- appears in exactly one place in the
# living tree, which is the sentence that names it. `context/TAME_GUIDANCE.md`'s own lint table
# names two more, `opening_lines_witness` and `tame-check`, and neither stood here either;
# `tame-check` is the tool that table cites for FIVE separate rules, unqualified assert among them.
#
# ALL THREE ARE GREEN AT HEAD and have been unheard since they were seated -- `tame-check` and
# `opening_lines` since the parity chapter, `rune_assert_sweep` since `20260810`. Nothing was
# broken; nothing was listening either, which is the second half of this file's own opening law.
#
# `unheard_guard` COUNTED THEM AND COULD NOT NAME THEM. All three stand inside its reading -- one
# in `unheard`, two in the 405 `unnamed_runners` -- under ceilings of 1,108 and 405. An aggregate
# ratchet answers "how many runners does no lap reach"; it cannot answer "is the tool this law page
# declares as its checkable surface one of them". Two questions, one number, and only the first
# was being asked.
#
# WHAT EACH ONE ACTUALLY READS, written here so a green never reads wider than it is. The TAME
# rooms in `tools/fixtures/t/tame_style_rooms.txt` hold 1,126 authored `.rye` files, 1,073 of which
# import std and are therefore hosted. At the seating: `opening_lines` read 25 hand-named files and
# said "all 25 hosted files"; `tame_check` reads 17; `rune_assert_sweep` gates 12 hand-named cores.
# Each is a regression guard over its own list rather than a reading of the tree, and a hand list
# cannot grow with the tree -- the same shape `tally_caller_map` was repaired for on `20260908`.
# Measured at the seating over the whole roster: 123 of the 1,073 hosted files are missing at least
# one line of the opening triad. Widening the lists is its own lap and was named on the card, not
# taken there: rostering a guard and changing what it reads are two acts, and doing both at once
# means no one can tell which of them moved the number.
#
# THE FIRST OF THE THREE WAS WIDENED `20260908.125347`. `opening_lines_scan.rish` now derives its
# population from the rooms roster rather than an inline list, so it reads 1,073 hosted files rather
# than 24. Its "25" was never a file count at all: the witness printed `length lines result.out`,
# which counted 24 OK lines plus the trailing empty one, and three living pages copied the number
# for a year. Two readings are walls -- `qualified_assert` at zero across all 1,073, and the elder
# twenty-four held clean on all four of their duties, so the widening lowers nothing the narrow list
# had proven. Three are ratchets whose ceilings only fall: 5 files calling `std.debug.print(`, 67
# missing the assert bind, 118 missing the print bind.
#
# `tame_check` TOOK THE SECOND LAP `20260908.133141`, and it found something the first did not.
# Its scan reads the same roster and counts every authored `.rye` rather than only the hosted ones,
# because a tab and a trailing space are faults whichever way a file opens: 1,127 rather than 16.
# Three duties are walls at zero across all of them, each on a looser predicate than the literal it
# replaced -- `std.debug.assert(` at any spacing, `Self = @This()` at any spacing, any tab. The
# fourth found three files the sixteen could not see, and all three are load-bearing: two carry a
# trailing space inside a `\\` multiline string, which is generated Zig and generated Brush rather
# than an untidy source, so they are reported under `trailing_content` and gated nowhere; the third
# is `brushstroke/font8x8_data.rye:35`, the glyph row whose `//` comment IS the space character it
# draws. That one is a ratchet at 1 rather than a named exemption, so naming the glyph in words some
# day lowers the ceiling honestly.
#
# ITS COUNT WAS THE SAME FAULT AS THE FIRST ONE'S. `tame-check` printed `length lines result.out`
# and said "all 17 files" against a list of 16, the seventeenth being the trailing empty line --
# exactly the 25-for-24 above, in the guard standing beside it, found the same day. Two of two
# widened guards had been measuring their own output, which is the argument for widening the third.
#
# `rune_assert_sweep` TOOK THE THIRD LAP `20260908.161651`, and the widening found a boundary rather
# than a count. Its scan derives the same 1,127 authored `.rye`, of which 1,100 carry functions, and
# reads TAME root rule 2 in two halves: 100 function-bearing files carry no `assert(` at all, and
# 101 more assert without naming a single `// invariant:`. Both are ratchets whose ceilings only
# fall, because a wall at zero would ask for two hundred files in one lap. What it found is that the
# elder twelve are NOT a subset of the roster -- `mandate/store.rye`, `mandate/keyed.rye`,
# `kumara/tilak.rye`, `settlement/constellation.rye` and `sundial/sundial.rye` sit in four rooms
# `tame_style_rooms.txt` does not name at all -- so deriving alone would have dropped five gated
# files while the printed population grew a hundredfold. The twelve keep a wall of their own at
# zero, each printed by name.
#
# AND ITS OWN HEADER NAMED A READING IT NEVER MADE, caught `20260908.170042` on the air rota --
# the row that feels along a boundary and presses each post. The scan opened by saying it counts
# "at least one `assert(`, each preceded by a `// invariant:` comment. This scan counts both," and
# both of its readings were FILE questions: does this source assert at all, does it name an
# invariant anywhere. The word the rule turns on is EACH, and no instrument in this tree read it.
# `unnamed_assert` reads it now: 6,619 of 24,495 asserts across the 859 asserting module sources
# name nothing. So 73 percent of ASSERTS are named, while 899 of the 1,100
# function-bearing files -- 82 percent -- already satisfied the file reading beside it.
#
# THE SPLIT IS WHAT MAKES THE NUMBER MEAN ANYTHING. A `*_witness.rye` asserts about another
# program's OUTPUT, so its 2,588 unnamed asserts are correct practice rather than debt -- reported
# here and gated nowhere. Read whole, `glow/` scores 1.1 percent named and reads as the worst room
# in the tree; read on its modules alone it scores 92 percent, and those 2,331 asserts were its
# witnesses all along. A reading that libels a room is a reading of the pattern rather than of the
# tree, which is `tame_style_app_sites`' own finding one room over on the same day.
#
# THE ELDER READING IS A STRICT SUBSET, proven by plant rather than argued: a file counted by
# `invariant_gap` necessarily holds an assert counted here, so the control's case 3 now watches two
# ratchets move together, and case 3b plants inside a file that already carries a comment, the only
# way to move the newer reading alone. A blank line breaks the pairing and a comment block opening
# on the invariant carries the run beneath it -- both shown from the failing side. Control 24 legs
# -> 37. The reading costs 2.5s of a lap pass, one `awk` per batch carrying a single flag forward,
# never one grep per file.
#
# AND ITS FIRST DRAFT PAID DIFFUSER'S TAX. Three greps per file against 1,127 sources is 3,381
# processes and 19.3s; three list-wide `grep -l` readings plus `comm` set arithmetic is six, and
# returns the same three counts in 237ms. The witness fell 47.4s -> 2.0s with the control included.
#
# `tier lap` for all three, measured on this pier `20260908`: 19,564ms, 401ms, 172ms against a lap
# pass of 1,537s. The first grew 966ms -> 19.6s at the widening, and the scan itself is 1.3s of that
# -- the other 18 is its control, which runs the scan once per planted state across a throwaway pen.
# A control that proves eighteen behaviors is what the widening is worth, and 1.3 percent of a pass
# is what it costs.
guard rune_assert_sweep
path tools/r/rune_assert_sweep.rish
tier lap   # 7.1s (3.0s scan, 4.1s control); 1,127 derived authored files, plus the twelve elder cores by name
seated 20260908.111848

guard opening_lines
path tools/o/opening_lines_witness.rish
tier lap   # 19.6s (1.3s scan, 18s control); the opening triad over 1,073 derived hosted files
seated 20260908.111848

guard tame_check
path tools/t/tame-check.rish
tier lap   # 13.4s (1.0s scan, 12s control); the four textual rules over 1,127 derived authored files
seated 20260908.111848

# A FOURTH LAP, AND IT READS THE ROSTER RATHER THAN THROUGH IT, seated 20260908.184342 on the air
# rota -- the row that feels along a boundary. The three widenings above each derived a guard's
# population from `tools/fixtures/t/tame_style_rooms.txt`, which is the right move and leaves one
# question standing: that file is itself a hand-written list of twenty rooms, and nothing has ever
# measured what it leaves out. A hand-written enumeration standing in for a population reads as
# coverage from every side, which is the shape REDS %532 booked one room over.
#
# THE ROSTER'S OWN HEAD ALREADY NAMES A TWENTY-FIRST ROOM and declines to add it, deliberately and
# with reasons -- and it sizes that deferred lap from ONE advisory ratchet: seven camelCase names,
# six after an inherited-name exemption. The bans half FAILS parity rather than printing a count,
# and nobody had read it over the rooms the roster leaves out. It stands at 736 hits across 113
# files, none in a comment: 718 `std.debug.assert(` in `rye/tests`, twelve `copyForwards` and six
# `copyBackwards` in the three tests named for those primitives. The deferral was sound. Its stated
# size came from the only number anyone had measured, and it was three orders short.
#
# THE REACH, PUBLISHED RATHER THAN REASONED ABOUT: 1,729 tracked authored `.rye`, 1,127 under a
# roster room, 551 outside -- `lotus` 238, `rye` 116, `crypto` 82. `crypto/` is absent on purpose
# and this meter never asks for it; what the meter refuses is that the number was nobody's to say.
# One gate at zero (`phantom_rooms` -- a roster line naming no tracked Rye, the law narrowing in
# silence) and two ceilings that only fall, so a room born tomorrow joins the roster or reds here.
guard tame_reach
path tools/t/tame_reach_witness.rish
tier lap   # 5.0s (0.6s scan, 4.4s control); the roster's reach over 1,729 derived authored files
seated 20260908.184342

# sow -- the projected seat is clean before it is ever public. MOVED TO `tier cadence`
# `20260904` (REDS %412): at 230s it was the single most expensive guard on the roster, and unlike
# the two repaired beside it that cost is INHERENT -- it projects the whole public seed and scrubs
# every name from it, which is real work rather than a fork loop. What makes the slower clock
# honest is what it guards: the publish is custody gate %1, a manual act at Keaton's hand, and
# `publish-seed.sh` runs this witness in its own flow. So no lap can take the act this protects,
# and running it on every lap spent 15% of the pass proving a gate that already stops everyone.
# A tier is a cadence rather than an exemption: it still runs, on the fifth round, and the run card
# records the clock that heard it.
guard sow
path tools/s/sow_witness.rish
tier cadence
seated 20260823.134057

guard tool_path
path tools/t/tool_path_witness.rish
seated 20260823.131241

guard tool_letter_room
path tools/t/tool_letter_room_witness.rish
seated 20260907.225349
tier lap
# A hand swept 34 misfiled entries on 20260906.182719 and left no guard; five had regrown a day
# later. tier lap because the fault is born with a FILE, and files are born every lap.
# The reason above stood in the comment and the FIELD stayed empty, so the ratchet counted the
# guard as undeclared and reddened the roster for eight ships. Transcribed rather than decided:
# the tier is the seater's own written word, moved into the line the meter reads.
# And the default cost what a default costs: the scan compared 2,972 filenames by forking
# `printf | cut` per tracked path and `printf | cut | tr` per entry -- about 36,000 processes, 82s
# every lap across eight ships, the slowest guard on the roster. Rewritten `20260908.011500` to
# parameter expansion and one awk pass: 78.8s -> 0.26s, output byte-identical, all 25 planted
# refusals still caught. lap is a cadence this guard can now actually keep.

guard tool_path_repoint
path tools/t/tool_path_repoint_witness.rish
seated 20260823.144100
tier cadence   # tool-path references tree-wide; moves when a tool moves, which is a deliberate act

guard tracked_link
path tools/t/tracked_link_witness.rish
seated 20260823.002511

guard vocabulary_collection
path tools/v/vocabulary_collection_witness.rish
seated 20260821.211943

guard pond_display_gate
path tools/p/pond_display_gate_witness.rish
seated 20260823.171000

guard document_mirror
path tools/d/document_mirror_witness.rish
seated 20260823.173634

guard readme_reach
path tools/r/readme_reach_witness.rish
seated 20260823.184309

guard kyri_receipt
path tools/k/kyri_receipt_witness.rish
seated 20260823.212046

guard loop_prompt_parse
path tools/l/loop_prompt_parse_witness.rish
seated 20260824.025704

guard dated_spelling
path tools/d/dated_spelling_witness.rish
seated 20260824.035758
tier cadence   # the dated-stamp spelling census across 4,705 living sources

guard dated_pattern
path tools/gen/chapter/dated_pattern_witness.rish
seated 20260824.040502

guard index_fold
path tools/i/index_fold_witness.rish
seated 20260824.052329

guard caravan_ladder_roster
path tools/ca/caravan_ladder_roster_witness.rish
seated 20260824.062207

guard mycelium_map_roster
path tools/m/mycelium_map_roster_witness.rish
seated 20260824.071500

guard living_docs_roster
path tools/l/living_docs_roster_witness.rish
seated 20260824.075409

guard image_module_roster
path tools/i/image_module_roster_witness.rish
seated 20260824.082436

guard lotus_module_roster
path tools/l/lotus_module_roster_witness.rish
seated 20260824.091754

guard chapter_allocator
path tools/c/chapter_allocator_witness.rish
seated 20260908.083050
tier lap   # two walls at zero over 1,958 authored Rye sources; a lap that writes Rye can move it

guard crushed_index
path tools/cr/crushed_index_witness.rish
seated 20260906.091330
tier lap   # the shipping shelf's indexes, which every prose lap can move

guard crypto_count_guard
path tools/cr/crypto_count_guard_witness.rish
seated 20260824.095920

guard crypto_module_roster
path tools/cr/crypto_module_roster_witness.rish
seated 20260824.095920

guard constel_module_roster
path tools/co/constel_module_roster_witness.rish
seated 20260824.104946

guard sow_lock
path tools/s/sow_lock_witness.rish
seated 20260824.104946

guard rule_twin
path tools/r/rule_twin_witness.rish
gate %7
seated 20260824.112806

# ascii_document -- the ASCII-first law names THREE subjects and only two were measured over their
# whole subject. Comments have two meters over every tracked source; commit messages have none and
# want none (0 of the last 400 bodies carry a byte above 0x7F, measured 20260906.131411); documents,
# the subject the law names FIRST, had `living_card_ascii_scan.sh` -- two pins read hard and six as
# advisory, EIGHT files of 5,529. The gap sat where it cost most: 63 of the 104 rule pages an editor
# reads as law, the pages carrying the ASCII-first law itself, held 859 non-ASCII characters, every
# one a form the rule's own substitution table spells. Swept to zero on the lap this row was
# written, the sweep proven to have moved no prose by re-deriving each page from its committed
# bytes. `tier lap`: 2s for the control's pens and 4s for the tree.
guard ascii_document
path tools/a/ascii_document_witness.rish
tier lap
seated 20260906.133344

guard width_check
path tools/w/width-check.rish
seated 20260620.210812
tier lap       # authored Rye contracts, named on touch by the TAME Definition of Done

guard declared_ceiling
path tools/d/declared_ceiling_witness.rish
seated 20260824.121445
tier lap       # the gate that stops a ratchet being loosened -- a lap can raise one

guard declared_model
path tools/d/declared_model_witness.rish
seated 20260824.161948

guard qa_report_card
path tools/q/qa_report_card_witness.rish
seated 20260824.161948

guard index_row_bound
path tools/in/index_row_bound_witness.rish
seated 20260824.183000

guard dayshelf_merge
path tools/d/dayshelf_merge_witness.rish
tier lap   # 2s on metal, and the fault it prevents is re-earned by every pull, so it belongs on the clock that follows a pull
seated 20260908.054012

guard log_has_a_row
path tools/l/log_has_a_row_witness.rish
seated 20260824.201500

guard rishi_bare_path
path tools/r/rishi_bare_path_witness.rish
seated 20260825.004500

guard fascia_home_link
path tools/f/fascia_home_link_witness.rish
seated 20260825.004500

guard ascii_comment
path tools/as/ascii_comment_witness.rish
seated 20260825.011000
tier cadence   # non-ASCII in every comment, held as a ratchet that only falls

# spoken_ascii -- the ASCII-first law names three subjects, and the tree's guards speak in a fourth.
# Both comment meters refuse to count "program content", and `ascii_comment_witness` gives the
# reason in its own GREEN line: converting a string "changes what a program prints". That one
# exclusion answers two questions. A heredoc body is what a program feeds ONWARD, and converting it
# changes behavior -- rightly excluded, here as there. A `say` line is what a program says TO A
# PERSON, on their terminal and into `session-output/`, and converting it changes register, which is
# what the law governs. Measured 20260907.075500 over the same 2,387 `.rish` sources the comment
# meter opens, under the same law, on the same run: 505 characters counted and held, against 11,154
# counted by nothing -- and 11,154 is very nearly the 10,468 the comment surface itself stood at on
# the day its own meter was built. Same files, same law, one sweep less of attention. `tier cadence`:
# 10s, and it reads 2,387 files where a lap touches a handful.
guard spoken_ascii
path tools/s/spoken_ascii_witness.rish
seated 20260907.075500
tier cadence   # non-ASCII in what a Rishi guard says out loud, a ratchet that only falls

# rye_spoken_ascii -- the sibling above proved the argument and stopped at one language. A heredoc
# body is what a program feeds ONWARD; a `say` line is what it says TO A PERSON, and converting that
# changes register, which is what the ASCII-first law governs. Rye speaks through `print`, and the
# Rye comment meter declines program content for that same one reason -- so the tree held a guard
# over what a Rishi program says and none over what a Rye program says, in the language it writes
# most of its own modules in. Measured 20260908.214712 over the same 1,730 tracked `.rye` sources
# the comment meter opens: 3,794 characters counted and held there, against 3,996 counted by
# nothing, in 809 of those files -- 45% of the tree's authored Rye printing a character to a
# person's terminal that no instrument read. A LINE-ORIENTED READING WOULD HAVE MISSED HALF ITS OWN
# SUBJECT: this tree writes a claim line as a chain of literals joined by `++` across four or five
# lines, and 2,480 characters stand on lines holding the call against 2,235 on its continuations, so
# the scan reads parenthesis depth outside string literals. The seating lap swept its own lane to
# zero -- mantra and tally, 25 files, 101 characters -- and set the ceiling at what remained.
# `tier cadence`: it reads 1,730 files where a lap touches a handful.
guard rye_spoken_ascii
path tools/r/rye_spoken_ascii_witness.rish
seated 20260908.214712
tier cadence   # non-ASCII in what a Rye program prints, a ratchet that only falls

guard comment_citation
path tools/co/comment_citation_witness.rish
seated 20260825.052000
tier cadence   # citations inside every tracked comment; a lap touches a handful of files, this reads thousands

guard witness_reach
path tools/w/witness_reach_witness.rish
seated 20260825.092953
tier cadence   # which witnesses reach what, tree-wide; moves when a witness is written or retired

guard caravan_suite
path tools/ca/caravan_suite_witness.rish
tier cadence
seated 20260825.092953

guard caravan_roster_bijection
path tools/ca/caravan_roster_bijection_witness.rish
seated 20260831.071031

guard crypto_suite
path tools/cr/crypto_suite_witness.rish
tier cadence
seated 20260825.110922

guard ales_roster
path tools/al/ales_roster_witness.rish
seated 20260825.132121

guard ales_suite
path tools/al/ales_suite_witness.rish
tier cadence
seated 20260825.132121

guard season_leaf_choir
path tools/gen/chapter/season_leaf_choir_witness.rish
tier cadence
seated 20260825.234902

guard remember_git_nib
path tools/r/remember_git_nib_witness.rish
seated 20260825.183336

guard remember_git_nib_write
path tools/r/remember_git_nib_write_witness.rish
seated 20260906.050718
tier lap       # 34 pen cases in 0.6s; the writer beside the guard above, standing free of it

guard reds_ledger_headline
path tools/r/reds_ledger_headline_witness.rish
seated 20260825.183336

guard reds_row_present
path tools/r/reds_row_present_witness.rish
seated 20260825.213128
tier lap       # the ledger a lap books rows into

guard shell_dialect
path tools/s/shell_dialect_witness.rish
seated 20260826.001630

guard shell_dialect_touch
path tools/s/shell_dialect_touch_witness.rish
tier lap
seated 20260908.040446
# THE SAME LAW, ASKED WHILE THE HAND IS STILL HERE. `shell_dialect` above reads every tracked shell
# source and holds five GNU-only families at a ceiling of zero. It is right, and it arrives late: it
# runs inside a roster pass of roughly half an hour, so the lap that breaks a family is structurally
# never the lap that hears it. Measured `20260908` -- two `sed -i` plants landed at 01:59 and were
# first heard at ANOTHER ship's cold open at 02:42, 43 minutes and one ship later, after which two
# hands cured the same two sites in parallel and one cure withdrew whole.
#
# This is rule seven of tools/hooks/pre-commit: the same five families, read off the INDEX at commit
# time. It gates only the families whose tree ceiling stands at ZERO, which it reads from the elder
# rather than respelling -- a ceiling of zero means no lawful site exists, so every staged hit is a
# rise and no author is refused for a line somebody else wrote. Replayed against commit 0f48f2c85 it
# answers `staged_gated_sites=2` and names both the file and the line.

guard one_clock
path tools/o/one_clock_witness.rish
seated 20260826.075023

guard upstream_shape
path tools/u/upstream_shape_witness.rish
seated 20260826.010645
tier cadence   # the shape of upstream refs; moves on a remote change rather than on a lap

guard ryekey
path tools/r/ryekey_witness.rish
seated 20260826.092732
tier lap       # authored Rye keys, same clause

guard skate_macos_choice
path tools/s/skate_macos_choice_witness.rish
seated 20260826.164751

guard reds_fold
path tools/r/reds_fold_witness.rish
seated 20260826.175633

# scope_trace seated 20260907.081500. THE MAP IS THE ONE BEHAVIORAL CLAIM IN THIS SYSTEM NOBODY
# CHECKED. tools/fixtures/s/standing_equipment_scope_map.sh tells a `--scoped` pass which guards a
# change can reach, and a row naming LESS than its guard gates SKIPS REAL WORK -- the map's own
# header says so, and thirteen such rows were repaired by hand on 20260906. This rung runs a guard
# under `strace -f -y -e trace=openat` and matches its observed read set against its row with the
# same tools/fixtures/s/scope_match.sh the runner skips by, so a gap named here is a skip the runner
# would actually take. Two rows were repaired by running it rather than by reading them the day it
# was seated: `radiant_negation` read 142 files against a row reaching 89, missing `.claude/rules/`
# -- its ENFORCE roster, while the rooms the row DID name are its ADVISORY tier -- and
# `tally_roster` read 844 against 28, missing the build edge. Both reproduce
# external-research/20260907-061951 file for file, on a second tree.
#
# WHAT IS GATED is the CONTROL, never a live gap. A row follows a guard's GATED readings and
# observation cannot tell a gated read from an advisory one, so a gap is a word a hand considers
# rather than a defect this rung declares. Union, never subtract.
#
# `capability trace_instrument` because strace is Linux-only; a bench without it reads its own name
# in `skipped_capability` rather than reddening for a kernel it does not have.
#
# `tier lap` at 4s: the control traces a pen guard whose whole read set is four files, so it costs
# nothing near the 58s a real trace of `tally_roster` takes. Tracing a REAL guard stays a hand's
# command, since its cost is the guard's own cost times 1.1x to 6.7x.
guard scope_trace
path tools/s/scope_trace_witness.rish
tier lap
capability trace_instrument
seated 20260907.081500

guard tally_roster
path tools/t/tally_roster_witness.rish
seated 20260826.224730

# Seated 20260908.090000. The guard stood UNHEARD: no roster row, and its one caller
# `tools/t/tensegral_arc_iv_witness.rish` is unrostered too, so nothing on any clock ran it. Widened
# the same lap from a hand-listed 19 paths tested with `-e` to a population derived from the index
# -- every tracked symlink resolving under tally/, 69 on this date -- with the named 19 kept beside
# it, since a retargeted link leaves a derived population in silence. `tier lap`: the reading is one
# `git ls-files` and 69 `readlink -f` calls, well under a second.
guard tally_caller_map
path tools/t/tally_caller_map_witness.rish
tier lap
seated 20260908.090000

guard socket_dialect
path tools/s/socket_dialect_witness.rish
seated 20260827.004535

guard rish_join_split
path tools/r/rish_join_split_witness.rish
seated 20260827.140328

# The six Glow witnesses nothing reached, seated 20260827.183635 on Keaton's word.
# All 89 glow_*_witness.rish stood off this roster; these six were reached by no runner,
# roster, or suite at all, and they carry the newest rungs. One of the six was RED and had
# been since 20260804 (REDS %299). They run on `cadence` rather than `lap`: each builds real
# plants through the Zig toolchain, and glow_shop_gate_horizon and _pair_faces each take
# minutes. Revisit the tier when the Glow ladder reopens.

guard glow_shop_gate_pair_faces
path tools/g/glow_shop_gate_pair_faces_witness.rish
tier cadence
seated 20260827.183635

guard glow_shop_gate_pair_select
path tools/g/glow_shop_gate_pair_select_witness.rish
tier cadence
seated 20260827.183635

guard glow_shop_gate_horizon
path tools/g/glow_shop_gate_horizon_witness.rish
tier cadence
seated 20260827.183635

guard glow_tally_pair_bound
path tools/g/glow_tally_pair_bound_witness.rish
tier cadence
seated 20260827.183635

guard glow_vane_pair_mirrors
path tools/g/glow_vane_pair_mirrors_witness.rish
tier cadence
seated 20260827.183635

guard glow_compose_after_inc
path tools/g/glow_compose_after_inc_witness.rish
tier cadence
seated 20260827.183635

# The desk corpus's own reach, seated 20260907.005950. glow/gen/ holds 352 generated desks and exactly one
# guard runs any of them -- tools/g/glow_run_desk_witness.rish, 1,133 lines naming 218 desks one at
# a time. A hand-written enumeration standing in for a population drifts the moment the population
# grows, and nothing here could see it: 129 runnable desks are run by nothing, 89 of which pass on
# metal today. This row does not run desks -- it reads them, so it costs 9s where the desk witness
# would cost a quarter hour of Zig builds, and it is what makes rostering that witness honest
# later. `tier lap` because it is file reading, and because the reading it holds is a ratchet that
# only works if it is read every lap.
#
# 20260907.020441 -- the 129 split in two. The witness runs every desk it names with ZERO
# arguments, and tools/g/glow_run_worker.sh permits a sample for 94 stems, 46 of them in this room.
# All 46 are uncovered and none is covered, so the boundary was the instrument's shape rather than
# anyone's oversight. Two ratchets now: 83 bare desks that cost one block each, 46 sample-taking
# desks that cost a chosen argument first. The sum is derived from them, so it cannot drift.
#
# 20260907.122532 -- the bare ratchet became a gate at zero, because `covered` learned to read both
# instruments. The derived runner below landed and was rostered, and for one lap this reading went
# on deriving `covered` from the elder hand-written witness alone -- printing 83 desks "run by
# nothing" while a guard on this very roster ran all 83. `covered` is the union of `covered_witness`
# and `covered_runner` now, the runner asked with --list rather than derived a second time. What
# `uncovered_bare` counts is a desk NOTHING runs, which makes it the gate on the two derivations
# agreeing: this scan excludes by the markers' intersection, the runner by their union.
guard glow_desk_reach
path tools/g/glow_desk_reach_witness.rish
tier lap
seated 20260907.005950

# The desk corpus actually RUN, seated 20260907.110022. The reach guard above measures how far the
# hand-written witness reaches; this one runs the room. tools/fixtures/g/glow_desk_run_scan.sh
# derives the bare-runnable set on every pass -- every .glow under glow/gen/ minus those declaring
# they must not run by EITHER marker, minus those the worker will only take with a sample -- and
# lowers, builds and runs all 301 of them. The elder tools/g/glow_run_desk_witness.rish names 218
# in 1,133 hand-written lines and is on no roster, because rostering it would publish a 218-desk
# green as coverage of a 352-desk corpus (REDS %532).
#
# Why a batch is cheaper than the blocks it replaces: tools/g/glow_run_worker.sh rebuilds
# glow/bin/glow_run from glow/glow_run.rye on EVERY invocation, which is correct for a hand running
# one desk and wasteful 301 times over. Measured on this pier 20260907 -- 3.80s per desk through the
# worker, 2.42s of it that rebuild, 1.38s the desk's own lower-build-run. The batch builds the
# compiler once, behind the same directory lock the worker takes.
#
# The failure ceiling stands at 3 rather than 0: three files under glow/gen/ are data fixtures
# rather than desks and carry no marker in either name or head, and giving them one is a language
# custody ruling (REDS %532). Run on metal they refuse in two distinct ways, not one.
#
# `cadence` rather than `lap`: 325s of Zig builds measured under a load of 14 is choir money, and
# the desk room grows by a few files a round.
guard glow_desk_run
path tools/g/glow_desk_run_witness.rish
tier cadence
seated 20260907.110022

# The constellation rehearsal's wire pen, seated 20260828.011621. comlink/rehearsal_wire.rye
# crosses three real processes over localhost UDP and was reached by no runner, roster, or suite
# until this guard -- it ran when a hand typed its name and at no other time. It is the pen FORA31,
# the socket rung where constel/ meets comlink/hosted_wire.rye, is to be proven in, so the pen
# earns a standing guard before the rung lands in it. `cadence` rather than `lap`: three spawned
# processes binding real ports is the thin edge, and the thin edge is budgeted rather than free.
# Measured on this bench 20260828: 2-3s with a warm Zig cache, build included.

guard comlink_rehearsal_wire
path tools/co/comlink_rehearsal_wire_witness.rish
tier cadence
seated 20260828.011621

# The socket rung itself, seated 20260828.174900. constel/socket.rye is FORA31 -- the FORA6
# switchboard with every mailbox swapped for a real UDP descriptor on 127.0.0.1, the one rung the
# Constel ladder named as genuinely crossing the Comlink seam. Thirty rungs of consensus stand on
# the switchboard's promise that a frame reaches exactly one named pier; until this rung that
# promise was kept by an array index the module wrote itself. `cadence` rather than `lap`, for two
# reasons said plainly: leg B spends real wall-clock on bounded waits by design, and the guard binds
# eight loopback ports (38512..38519), which two hands sharing one bench should not do every lap --
# the same one-tree-per-star question REDS %281 and %291 leave with Keaton, arriving from the port
# side. The hold releases on its own after 3000 ms, so a red here never strands a port.
# Measured on this bench 20260828: 3s wall with a warm Zig cache, both builds included.

guard fora_socket
path tools/f/fora_socket_witness.rish
tier cadence
seated 20260828.174900

# The custody-gate instruction guard, seated 20260828.033212. REDS %307 found the loop's own prompt
# template telling an unattended lap to run the seed publisher every fifth round -- a cadence the
# card cut on 20260826, and a gate Keaton holds by hand. A hand repaired that file; the same
# sentence stood on in tools/l/launch-claude-chapter.rish, the file a hand actually pastes to START
# a loop, so the orders reaching the machine were still the cut ones. This guard reads the whole
# loop-prompt surface -- recursion-prompts/ and tools/l/launch-*, discovered rather than named --
# and gates at zero any gated command standing in a clause with no refusal beside it. `lap` rather
# than `cadence`: the scan is grep over 27 files and costs 0.15s on this bench, measured 20260828,
# and a gate worth holding is worth holding every lap.

guard custody_gate_instruction
path tools/cu/custody_gate_instruction_witness.rish
seated 20260828.033212

# glow_gate_answer -- a wall whose needle the harness itself supplies is not a wall. REDS %310 found
# three argv plants computing their expected value from the same expression as the gate body, so
# every one returned zero for every input and spoke nothing, and a witness above them read
# `contains "0"` against output whose tail was EXIT:0. Sound's language custody repaired the
# emitters; this is the meter over the witness surface, the half %310 parked. Two gates at zero --
# a needle the EXIT framing already supplies, and a captured run nobody asserted -- and two ratchets
# under ceilings that only fall: 152 answer-blind runs and 7 files never seen to refuse. Both
# ceilings are proven from both sides in the pen by planting one past them, so no override exists.
# `lap` rather than `cadence`: the whole witness costs 3s on this bench, measured 20260828, and the
# fault it refuses can be written again in any lap that adds a lowering witness.

guard glow_gate_answer
path tools/g/glow_gate_answer_witness.rish
seated 20260828.114450

# gitlink_dependent -- a guard that reds on a dependency a correct clone may lack can never be
# rostered, so its refusal is one nobody will ever hear. This tree holds twelve gitlinks in two
# rooms: `vendor/` is what the tree LINKS and the operator card requires every clone to initialise,
# and `gratitude/` is a reading library the licenses law says we study and never copy -- so a clone
# that never checked out `gratitude/tigerbeetle` is a CORRECT clone. REDS %646 found the shape by
# sampling and left the class as a sample; counted `20260908.214427` it was 22 dependents, all one
# family -- 21 from `20260908.224618`, when the sixth probe was drawn and one was rostered -- against
# 32 on the required rungs. The GATE is the one row that would red the fleet -- a rostered guard
# depending on an optional gitlink and naming no `capability` -- held at zero. The population is
# reported rather than gated, because rostering a study census is a hand's decision with a probe
# attached. `lap` rather than `cadence`: 11s on this bench, and the fault can be written again by
# any lap that rosters a witness.
guard gitlink_dependent
path tools/g/gitlink_dependent_witness.rish
tier lap
seated 20260908.214427

# tigerbeetle_naming_census -- the first of the 22 optional dependents `gitlink_dependent` counted,
# rostered because the probe it wanted now stands. `capability tigerbeetle_clone` is the sixth arm
# in the runner, seated 20260908.224618 and spelling the same `test -d gratitude/tigerbeetle/src`
# the witness's own assert spells; where the held clone is absent -- which is every ship on this
# pier today, all eight gratitude gitlinks empty -- the row reads its own name in
# `skipped_capability` rather than reddening for a reading library nobody is obliged to fetch.
#
# THIS ONE ROW RATHER THAN THE FAMILY, on purpose. Forty-four witnesses read that clone (20 census
# leaves and 24 choirs wrapping them 1:1, and NOTHING ELSE IN THE TREE CALLS ANY OF THEM), so
# rostering the family would put 44 skip lines on every pass of every ship and turn 16 percent of a
# 275-row roster into a study that skips. That is a hand's decision. This row is what makes the
# probe load-bearing rather than decoration, and it is the leanest member of the family: pure
# counting, no almanac append, no nested choir, so it cannot move the tree on a pier that does hold
# the clone. `lap` rather than `cadence`: it is grep over a checkout that is usually absent.
guard tigerbeetle_naming_census
path tools/gen/chapter/tigerbeetle_naming_census_witness.rish
tier lap
capability tigerbeetle_clone
seated 20260908.224618

# reds_status_consistency -- construction/REDS.md is the first page a lap reads under reds-first,
# and the one thing it has to get right is which rows are still open. REDS %313 found `%307` reading
# open on the pin while `%308` -- folded onto a shelf the same day -- carried the only record of its
# closure. The split is structural: every fold moves a closing row into a closed stack and leaves
# the row it closed on the open shelf. Gates contradicted_rows and phantom_closures at zero, and
# reports open_rows, closed_rows, closure_claims, duplicate_row_lines and fold_blocked_rows. `lap`
# rather than `cadence`: it is awk over 128 markdown files and costs under a second, measured
# 20260828, and the page it reads is the page every lap chooses its work from.

guard reds_status_consistency
path tools/r/reds_status_consistency_witness.rish
seated 20260828.125441

# pin_bound_touch -- the seven living pins carry byte bounds so an agent reads one in a single
# breath beside its lap, and until 20260828 nothing weighed a pin at the moment a round moved it.
# REDS %293 named the mechanical repair in its own third field and left it unbuilt; the next day
# construction/REDS.md stood 3,992 bytes over and construction/ITINERARY.md 3 over, and four
# commits shipped across a red `equinox_e123_living_pin_guard`. Two readings that cannot disagree:
# e123 weighs every pin at roster time, this weighs only the ones the round's own diff names, off
# the index, where `git cat-file -s :<path>` is the size the commit will actually carry. One roster
# file and one bound reading (living_pin_max_bytes.sh) are shared, so there is no second roof.
# It also weighs EVERY touched pin before refusing -- e123 exits at its first, which is how the
# ledger's 3,992 bytes went unreported while the card's 3 were named. Armed as RULE THREE in
# tools/hooks/pre-commit, proven by 36 cases in a pen, five of them running real `git commit`s
# through the real hook. The witness gates only its own reading -- the refusals, the pen, the hook
# turning a real commit away -- and PRINTS the live worktree reading without asserting on it, so
# one fault reds in one place and e123 keeps that gate. `lap` rather than `cadence`: the witness
# costs 3s on this bench, measured 20260828, and the fault it refuses can be written by any lap
# that edits a pin.

guard pin_bound_touch
path tools/p/pin_bound_touch_witness.rish
seated 20260828.153046

# pond_enclosure_policy -- orbit one of the quest that retires ai-jail. pond/enclosure_policy.rye
# reads an enclosure as a value and marks each declaration place, hold, or refuse at receipt,
# naming the declaration that refused. The scan plants one forbidden mount and then removes it, so
# the refusal is proven from both sides; a refusal shown only in the passing direction cannot be
# told from a bypass. `lap` rather than `cadence`: the arc is under active work and the whole run
# costs one second warm, measured 20260828. Rostered here on the lap that recovered it, since an
# unrostered witness raises `unreached` and proves nothing on any tree but its author's.

guard pond_enclosure_policy
path tools/p/pond_enclosure_policy_witness.rish
tier lap
gate %5
seated 20260828.002135

# pond_enclosure_state -- the launcher's state house and the per-host config it reads, compared.
# tools/ag/agent-jail.sh seats `loops/claude`, `loops/cursor`, and `loops/codex` and adopts an
# elder directory into each once; tools/e/enclosure.conf is sourced BEFORE those defaults, so a
# path named there outranks the move and the jail binds a directory the adoption just emptied. The
# gate is one reading -- the tracked example naming an elder -- and the host's own gitignored conf
# and the directories on disk are reported without gating, since one machine's file is not the
# tree's to fail on. `lap` rather than `cadence`: the enclosure arc is under active work and the
# whole run costs under two seconds, measured 20260828. Booked as REDS %327 on the lap that found
# a credential directory already rebound empty on this pier.

guard pond_enclosure_state
path tools/p/pond_enclosure_state_witness.rish
tier lap
seated 20260828.170003

# pond_policy_launcher -- the enclosure record and the launcher it claims to describe, compared.
# pond/enclosure_policy.kyri declares what the agent-jail lap builds and tools/ag/agent-jail.sh
# builds it; two spellings of one contract stood side by side and nothing compared them, so the
# record carried `network off` for an enclosure sharing the host's own network namespace (REDS
# %329). Two readings gate at zero: a mount the launcher makes and the record never declared,
# which is the escape direction orbit two must rule out, and a closure claim with no flag behind
# it. The reverse readings and the `ai-jail --help` reading are reported, since a guard that reds
# on a machine without the jail is a guard someone turns off. `lap` rather than `cadence` for the
# same reason its two siblings chose it -- the enclosure arc is under active work and this guard's
# whole subject is the pair of files orbit two will derive its grant set from. The cost is named
# rather than hidden: 13.4s measured 20260828, of which the 25-pen control is 9.9s and the scan
# itself 0.4s. When the quest's orbit five closes and these two files stop moving, this row falls
# to `cadence` -- the control re-proves the guard rather than the tree.

guard pond_policy_launcher
path tools/p/pond_policy_launcher_witness.rish
tier lap
seated 20260828.202621

# glow_choir -- the 26 Glow-family witnesses that no runner on disk named. A survey on 20260828
# traced every tracked runner and found them reached by nothing: not this roster, not the three
# tend suites, not another witness. One of the 26 was already RED, citing a brief that had folded
# to active-designing/yonder/, and nothing was there to say so. `cadence` rather than `lap`: the
# choir costs 2m01s measured on this pier, which is choir money, and the Glow ladder's last
# advancing commit was 2026-08-04, so a slower clock matches how fast the family moves.

guard glow_choir
path tools/g/glow_choir_witness.rish
tier cadence
seated 20260828.192755

# glow_shop_core -- which road a `|%` source takes, decided before it lowers. The two modules
# that pick it, glow/rune_shop_core.rye and glow/lower_shop_core.rye, landed 20260830.224500 and
# were imported by glow/glow_run.rye alone: no runner on disk named either, so nothing ran them
# for seven days while their dispatch refused all fourteen thin core desks under glow/gen/c/ and
# the Glow step of manual/grain-os/get-started.md (REDS `20260906.195208`). `lap` rather than `cadence`: 2s
# measured on this pier, and the predicate it holds decides a road for every `|%` source the
# language will ever lower.

guard glow_shop_core
path tools/g/glow_rune_shop_core_witness.rish
tier lap
seated 20260906.195208

guard skate_native_core
path tools/s/skate_native_core_witness.rish
tier cadence
host macos
seated 20260828.194500

# fixture_depth -- the depth-proof root walk the fixtures letter fold (REDS %301's last room)
# seated across tools/fixtures/. The witness drives the pen control -- wrong root shown, right
# root from both depths, elder sibling source broken, new source landed, the 8-step bound
# refusing loudly -- and censuses the room at zero elder shapes. `lap` because the control
# costs about a second on this pier, and the census reads git rather than walking disk.

guard fixture_depth
path tools/f/fixture_depth_witness.rish
tier lap
seated 20260828.202325

# witness_own_build -- a witness that runs a gitignored artifact also builds it. comlink/bin/ is
# ignored, so a clone carries no binary: two Comlink witnesses named their build in a header
# comment and then invoked the path, and both read GREEN on a bench where an August lap had left
# binaries behind. Their sibling comlink_topology_witness.rish already carried the repair, so one
# of three was fixed and nothing counted the other two. `lap` because the scan reads 1,730
# witnesses in 4.7s on this pier, one awk and one batched check-ignore rather than a call per file.

guard witness_own_build
path tools/w/witness_own_build_witness.rish
tier lap
seated 20260828.213503

# Comlink's three pure proofs, seated 20260828.231614. comlink/topology.rye, turn_route.rye and
# handshake_turn.rye each carry a selftest this tree already trusted, and each was reached by no
# roster row, no choir and no runner -- they ran when a hand typed their name and at no other time,
# which is the sentence REDS %334 left standing when it repaired their builds. Measured cold on
# this bench 20260828 with comlink/bin/ emptied first: 2s each, its own zig build included, no
# socket bound and no process spawned. `lap` rather than `cadence` for that cost.
#
# comlink_topology READS 12s FROM 20260906.093000, and the row stays `lap` on purpose. It
# grew the point-metric proof REDS %454 asked for -- every ordered pair of both seated skies
# walked for a strictly-closer next hop -- plus its own control, which builds six pen copies
# of the module and runs each. Measured here: 12.5s against a cold pass of 875s, so 1.4%.
# The elder 2s above is kept as what the row cost when it was seated, since a number a row
# carries should say which lap measured it.
#
# Their fourth sibling tools/co/comlink_r1_dual_stack_witness.rish takes its seat here on
# 20260829, and the field it waited for is the reason it now can. Its leg B reads the loopback
# interface table and requires BOTH address families on it, so a red there is an ENVIRONMENT fact
# -- this host has not configured a family -- rather than a finding about Comlink, and an
# unconditional row would turn one bench's configuration into every body's red lap. The `host`
# field cannot carry it: `host macos` is a tier for PLACE, and this condition is one of CAPABILITY
# -- a Linux bench routing IPv6 keeps the promise a Linux bench without it breaks, so the OS word
# would encode something untrue. `capability ipv6` is that word, seated 20260829 and proven in all
# three of its answers: present runs, absent skips BY NAME and is counted, and unknown RUNS --
# since skipping on an unknown is how a tier stops being a cadence and becomes an exemption. The
# runner's probe reads `::1` on lo0 here, so the guard runs on this bench and a bench without the
# family reads its own name in `skipped_capability` rather than vanishing from the roster.
#
# `cadence` for the cost: 105s measured here 20260829.175031, its own a1 suite included, against
# the 2s each of its three pure siblings above. The elder note in this room read 151s; a witness
# timed under a lap's own load reads high, and the number a row carries should be measured by the
# lap that writes it. Either reading is two orders above the siblings, which is what `cadence`
# answers -- it sings on the fifth round rather than every lap.

guard comlink_topology
path tools/co/comlink_topology_witness.rish
tier lap
seated 20260828.231614

guard comlink_turn_route
path tools/co/comlink_turn_route_witness.rish
tier lap
seated 20260828.231614

guard comlink_handshake_turn
path tools/co/comlink_handshake_turn_witness.rish
tier lap
seated 20260828.231614

guard comlink_r1_dual_stack
path tools/co/comlink_r1_dual_stack_witness.rish
tier cadence
capability ipv6
seated 20260829.175031

# waymark_rung_drift -- the mark law (.claude/rules/stamp-and-name.md) retired the ascending rung
# as a mark for planned work, and two ladders numbered right past it for a week because nothing
# counted the shape (%329). This meter counts <seated-waymark><digits> in living tracked files
# under a ceiling that only falls. It landed 20260828 with no roster row, so nothing ran it: its
# first hearing on this pier, the lap that rostered it, read one mark over the ceiling. `lap`
# because the scan is one batched grep over git's own file list, under a second on this pier.

guard waymark_rung_drift
path tools/w/waymark_rung_drift_witness.rish
tier lap
seated 20260828.233813

# tally_bud -- the Pedersen bud vector, pinned and moved. A pinned constant earns trust only when
# a witness moves it, so this holds a passing vector and refuses a planted generator byte, a
# planted domain, and an amount that moves the answer. `lap` because it costs 6.2s on this pier
# and Tally is the arc under active work, which is the same reasoning caravan_suite stands on.

guard tally_bud
path tools/t/tally_bud_witness.rish
tier lap
seated 20260828.233813

# counsel_census -- the counsel campaign's own value reading: which of the 941 closed pieces are
# still leaned on, ordered by citer count, so Phase 1 lifts by measurement rather than by memory.
# `cadence` because it costs 32.2s on this pier and the room it reads is closed -- a census of a
# room that no longer grows changes on the laps that lift from it, not on every lap.

guard counsel_census
path tools/co/counsel_census_witness.rish
tier cadence
seated 20260828.233813

# pond_enclosure_built -- the record against what the enclosure actually BUILDS. Its sibling
# pond_policy_launcher reads the record against the launcher and says in its own header that it
# compares what the launcher SPELLS; ai-jail supplies twenty-one more mounts from its own defaults,
# and the record named six of twenty-seven until this row's lap. The scan reads `ai-jail --dry-run`
# -- the bwrap plan printed without starting anything -- gates `undeclared_builds` at zero against
# a pinned measurement so a clone with no jail still reads it, and reports drift between the pinned
# plan and a live one. `lap` because the scan is 0.12s and the whole witness 2.4s on this pier.

guard pond_enclosure_built
path tools/p/pond_enclosure_built_witness.rish
tier lap
seated 20260828.233813

# reds_pin_capacity -- the four guards above all ask whether construction/REDS.md is CONSISTENT;
# none asked whether anything more could be WRITTEN. On 20260829 the pin stood at 24,571 of its
# 24,576 bytes with all nine rows marked OPEN, so reds_fold.sh had no lawful move, four reds went
# unbooked on 20260828, and three more were born onto single-row shelves -- one of them, %337,
# still live, which is the exact state the fold tool refuses a fold to prevent. Every one of those
# four guards read green throughout (REDS %338). Gates phantom_recital_shelves at zero, ratchets
# unrecorded_shelves at 62 and shelf_open_rows at 2, and REPORTS the capacity table and
# pin_deadlocked -- reported rather than gated because raising a page's bound is Keaton's word, and
# a gate that reds on ordinary work is a gate someone turns off. `lap` rather than `cadence`: awk
# over the spine and a pen control, 1s for the scan and 3s for the whole witness measured on this
# pier 20260829, and the page it reads is the page every lap chooses its work from.

guard reds_pin_capacity
path tools/r/reds_pin_capacity_witness.rish
tier lap
seated 20260829.031804

# pond_enclosure_ephemeral -- the two guards above ask what the enclosure lets an agent SEE, one
# from the launcher's flags and one from the jail's own plan. Neither asks how long what it WRITES
# lasts, which is orbit three's whole question: the memory that forgets. This classifies every mount
# row by lifetime -- a tmpfs dissolves, a read-only bind keeps nothing, a read-write bind hands the
# byte to the host -- gates the record's `ephemeral` claims against that reading at zero, and holds
# the writable paths outliving the close outside the pier at a ceiling of 3. Its `--probe` leg plants
# one marker per candidate inside a real enclosure, lets it close, and reads the host: measured
# 20260829, the private home and /tmp came back forgotten and /dev/shm, /tmp/.X11-unix and
# /run/user/<uid> came back carrying the marker, with zero disagreements against the plan. `lap`
# because the whole witness including the live probe is 3.0s on this pier, and the probe is the only
# reader of the private home's own claim, which the pinned plan drops.

guard pond_enclosure_ephemeral
path tools/p/pond_enclosure_ephemeral_witness.rish
tier lap
gate %5
seated 20260829.054303

# pond_enclosure_door -- the three guards above read the ROOM: the flags the launcher spells, the plan
# the jail builds, and how long a written byte lasts. None reads the DOOR -- which program walks in,
# carrying which environment, as which user -- which is the launcher's own last exec line and nothing
# the record has a key for. This derives reachability from the record (`map`, `persist`, and `rw-map`
# destinations declare; a closer `mask` wins; `ephemeral` and `fresh` start bare), gates the ENTRY at
# zero unreachable, and holds undeclared search-path elements at a ceiling of 1. Its `--probe` leg
# starts one enclosure and asks it the same questions, writing no byte on either side: measured
# 20260829, two of the launcher's four search-path elements are absent inside -- one the host lacks,
# one the private home dissolves -- with zero disagreements against the derivation, and the three
# door duties (`entry`, `env`, `user`) have no key in the record's grammar at all. `lap` because the
# whole witness including the live probe is 8s on this pier.

guard pond_enclosure_door
path tools/p/pond_enclosure_door_witness.rish
tier lap
gate %5
seated 20260829.064107

# lattice_suite -- forty small arithmetic witnesses heard as one bounded choir. Each lap builds
# Lattice and proves an admitted operation beside its shape refusal; the suite also refuses when
# a newborn lap witness stands outside its forty-seat roster. `cadence` keeps the stable arithmetic
# family heard without adding forty repeated builds to both halves of every systems lap.

guard lattice_suite
path tools/l/lattice_suite_witness.rish
tier cadence
seated 20260829.061739

# macos_app_bundle -- the Finder door, proven from both sides on a Mac. The witness builds
# tools/rye/macos_app_shell_probe.rye with Cocoa and CoreGraphics, assembles Grain.app in a /tmp
# pen around the tracked plist fixture, strips Info.plist from a copy and proves `open -W` refuses
# it, then opens the whole bundle through LaunchServices and reads the receipt the probe wrote from
# a process that never had a terminal -- `mode=bundle window=5308 main_menu_items=1`, measured on
# this bench 20260829.104233, GREEN in 4s. It stood in tools/fixtures/w/witness_reach_scan.sh's own
# ceiling comment as the one debt a Linux pier could not pay, and this is the Mac bench that pays
# it. `host macos` because `open` needs a window server, and `cadence` beside its macOS-surface
# sibling skate_native_core, because the guard opens a real window and a hand may call it by name
# on any lap.

guard macos_app_bundle
path tools/m/macos_app_bundle_witness.rish
tier cadence
host macos
seated 20260829.104233

# pond_seal_gate -- the custody gate for the season flip, read wherever it is spelled. Three tracked
# launchers admit ENCLOSURE=pond only behind pond_exit_bron_master_seal.sh --require, and
# pond_policy_launcher reads exactly one of them because that is the one Pond's record describes.
# This discovers the launchers with `git grep` rather than listing them (%301, %326) and gates three
# readings at zero: an ungated pond, a seal call whose exit code nothing reads (%311's vacuous wall,
# one room over), and a caller the seal's own header does not name. `lap` because scan plus control
# is 9s on this pier, the same weight as pond_enclosure_door beside it.

guard pond_seal_gate
path tools/p/pond_seal_gate_witness.rish
tier lap
seated 20260829.123529

# The wire-lab fn macro stays one macro: fifteen orchestrators each carry three fn lines hoisted
# from 135 repeated template sites (20260829), and Rishi has no cross-file include, so the lines
# are deliberate copies read against the teaching head. Readings at zero: fn_drift (one file's
# macro wandering) and raw_sites (a template returning outside the fns). `lap` because scan plus
# control run under ten seconds -- the happy zone holds daily; the real qemu boots stay with the
# labs' own gating witnesses.

guard wire_lab_fn_drift
path tools/w/wire_lab_fn_drift_witness.rish
tier lap
seated 20260829.153150

# Tablecloth's first Glow desk, seated 20260829. Every other infrastructure vane carrying a bound
# worth naming already stood in the shape museum -- Aurora four pedestals, Mantra four, Caravan
# three, Tally six, Comlink one -- and Tablecloth, the content-addressed store the Brix manifest
# and the four comlink/guest_tablecloth_query_* wires both lean on, stood there not at all.
#
# The guard is shaped against the elder Tend habit rather than after it. Those witnesses grep for
# the LITERAL number in both rooms, which writes the value a third time inside the guard itself,
# so raising a bound honestly in the Rye AND on the pedestal still reds until a hand edits the
# witness. This scan reads both numbers and compares them, carrying none of its own; the control
# proves that welcome -- a bound raised in both rooms walking free -- beside its refusals.
#
# Widened 20260830.001122 to Tablecloth's second pedestal, `max_content_bytes`, which is a
# DERIVATION -- `= beading.max_resin_bytes` -- so tablecloth.rye names the bound and
# mantra/beading.rye decides it. The scan compares the desk against the deciding room and reads
# the derivation as a fact of its own, because respelling it as a literal leaves every number
# agreeing while the link is gone. Nineteen control cases now: three welcomes, sixteen refusals.
#
# `cadence` because the metal leg lowers the desk through Glow and builds it with the Zig
# toolchain, the same clock the six Glow witnesses beside it keep. The pure scan and control run
# in under a second, so a hand asking for the guard by name pays almost nothing.

guard tablecloth_glow_tend
path tools/t/tablecloth_glow_tend_witness.rish
tier cadence
seated 20260829.210056

# lantern_face -- the asking voice's face, landed by a peer `20260830.000539` with no roster row
# beside it. `witness_reach` caught the gap on the very next hot pass: a witness nothing enumerates
# is a witness family with no clock, and that reading stood at 221 wholly-unreached families
# against a ceiling of 220. The guard's own refusal names the repair -- give it a roster row rather
# than raise the ceiling -- so this is the row rather than a raised number.
#
# `lap` because the whole witness, including the Glow wall and the two-language parity anchors,
# runs in 9.5s measured on this pier `20260830.011217`, which is inside the cost every lap already
# pays for its Pond siblings. It builds and runs the face's own legs, proves the `.brush`-themed
# cascade recolors one settledness, walks the Glow budget wall at 38 lawful against 39 past it,
# and closes on one number read in two languages.

guard lantern_face
path tools/l/lantern_face_witness.rish
tier lap
seated 20260830.011217

# lantern_face_claim -- the face's sibling, landed in the same peer arc `20260830` and unrostered
# with it. `witness_reach` counts a FAMILY rather than a file, so rostering one of the two left
# `lantern_face` wholly unreached all the same; a peer's own card names both, which is how this
# second row was found. `lap` on 4.4s measured here `20260830.021200`.

guard lantern_face_claim
path tools/l/lantern_face_claim_witness.rish
tier lap
seated 20260830.021200

# borrowed_number -- the loom REDS %357 named and left unbuilt. The class had fired twice: a
# witness typing another document's byte count or SHA-256 into an assertion, and that document
# moving underneath it. %341 repaired two instances, %357 found three more in the same family the
# next day, all red since the day SECURITY.md and context/TWO_ROOMS.md were edited and none of
# them rostered. The reading catches a pin while it is still TRUE, which is the only moment
# catching it is cheap; a pin that has already gone stale is a red the witness itself declares.
# `lap` on 40s measured here `20260830.072125` -- four scan passes and a fourteen-behaviour pen.

guard borrowed_number
path tools/b/borrowed_number_witness.rish
tier lap
seated 20260830.072125

# unheard_guard -- the other half of this file's own law, written at its head and unmeasured until
# now: a guard that cannot red guards nothing, AND a guard that is never run guards nothing either.
# The reading takes every tracked witness and choir outside the fixture pens, closes the set this
# roster can reach -- rostered, or named on a non-comment line of something already reached -- and
# holds the remainder under a ceiling that only falls. REDS %357 measured what the gap costs: three
# witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass
# read 95 green. Measured here `20260830.095947`: 1,756 guards, 603 heard, 1,153 unheard, 41 of
# those choirs that sing families of their own; re-measured `20260830.154940` after the five
# rule-named rows below landed: 614 heard, 1,144 unheard, 40 choirs. The git hooks were checked as
# a second standing runner and name no guard at all, so this roster is the whole seed.
# `lap` on 5s measured here -- one batched awk pass over the tree, plus a six-pen control.
# WIDENED `20260906` (REDS %465): the population above is drawn by BASENAME, so a runner wearing
# neither `witness` nor `suite` was not counted unheard -- it was absent, and absence looks like
# health. Four readings join the elder two: `unnamed_population` 451 asserting runners wearing
# neither word, `unnamed_heard` 43, `unnamed_runners` 408 REPORTED and never gated, and
# `unnamed_choirs` 12 GATED at what was measured. `elder_reach_gap` 2 gives the narrow rule's own
# blind spot a number. The largest silent choir in this tree is `tools/p/parity_ch01.rish`, which
# calls itself a witness suite on its own first line, builds and runs 116 Rye programs, sings 281
# guards, and which this census could not see at all. `unnamed_runners` is reported rather than
# gated from measurement: all ten arrivals into that set over the fourteen days to `20260906` were
# ACTORS -- seven `launch-*-chapter.rish`, `birth_a_clone.rish` which copies a tree's `.ssh` -- so a
# ceiling there would red on ordinary growth and point a hand at custody gate 3 through a number.
# Still `lap`, measured 21s on metal here against the elder 5s: the control grew 6 pens to 11 and 27
# behaviours to 52, and the scan itself runs FASTER than HEAD's despite carrying a second closure
# (0.88s against 0.60s), because both closures now read each source once.

guard unheard_guard
path tools/u/unheard_guard_witness.rish
tier lap
seated 20260830.095947

# comlink_glow_tend -- Comlink's payload ceiling, displayed and tied. The vane stood in the shape
# museum with one desk, a dual-stack POLICY, while thirty-five bounds went undisplayed, so the
# widest wire surface in the tree was the least shown. `max_wire_payload` = 340 sizes real
# buffers, gates asserts, and two further bounds derive from it -- and it is DERIVED upstream, as
# `wire_capacity - off_cipher`, where four fifths of the header is decided inside the crypto
# library. It was right by arithmetic rather than by a rule: 188 is 188 only because Ed25519 signs
# in 64 bytes, and the card books Kumara moving to SLH-DSA-SHAKE-256s. Three modules spelled the
# answer as a bare literal and named no upstream room.
#
# The scan compares rooms and carries no value of its own, so an honest raise walks free (control
# case 23). What a scan cannot do is evaluate the library widths, so the tie is handed to the
# compiler: `max_wire_payload <= wf.max_message` at comptime, proven both ways on metal in a
# throwaway pen -- as written it builds, inverted it stops the build. Twenty-six control cases,
# five welcomes and twenty-one refusals. The tie count is a FLOOR, so amphora/ tying its own room
# later is welcomed rather than punished.
#
# `cadence` because two legs reach metal -- the tie probe builds two modules and the desk lowers
# through Glow and builds with the Zig toolchain, the same clock the Glow witnesses beside it
# keep. Measured here `20260830.164837`: 10s whole, of which the pure scan and control are under a second.

guard comlink_glow_tend
path tools/co/comlink_glow_tend_witness.rish
tier cadence
seated 20260830.164837

# comlink_carriage -- what a whole is cut into, and whether the pieces can carry it. Three families
# in this tree cut a whole and carry the pieces, each publishing a whole's ceiling, a piece's size,
# and a piece count -- and the relation `whole <= pieces * piece` was stated in no room, no comment,
# and no guard. It held in all three by arithmetic nobody had written down, and the bead family held
# it by EXACTLY ZERO margin: 512 <= 8 * 64, where the 64 is a chunker tuning constant filed beside a
# hash mask rather than beside a carriage bound. Measured before the guard was written: raising
# `max_resin_bytes` 512 -> 576 and changing nothing else refuses 9 of 3,999 pseudorandom wholes with
# TooManyBeads -- a legal input refused by the module that accepted it two lines earlier, on 1 in 444
# contents, which is why a single-fixture test would ship it. The two mantra/ rooms now carry the
# relation at comptime, and amphora/vessel_fetch_wire.rye tied the third on `20260906`, taking
# ties_wired to 3 of 3. That landing turned the gate over: a FLOOR of two had been single-drop
# detection only while two rooms were tied, so the gate now asks each room for its OWN tie and
# wants all three, and the control's case 19 is Amphora dropping its tie rather than adding it
# (REDS %455). Twenty control cases on real files in a throwaway pen, seventeen
# refusals and three welcomes, and a probe that asks the compiler seven times -- four inverted ties
# and the three plausible real edits that were measured to produce a wrong answer. Measured here
# `20260830.200925`: 12s whole, of which the pure scan and control are under a second. At tier cadence
# because it builds two Rye modules eight times over and lowers a desk through Glow, the same clock
# the Glow witnesses beside it keep.

guard comlink_carriage
path tools/co/comlink_carriage_witness.rish
tier cadence
seated 20260830.200925

# comlink_nesting -- whether a frame this tree sends holds the datagram it seals. Every layer of a
# network stack wraps the layer above it, so each container must hold its own header plus the whole
# payload: `container >= header + payload`. Comlink has two such layers and BOTH stand at zero
# slack, and only one of them is safe. The datagram layer is zero because wire_format.rye DERIVES
# it -- `max_message = wire_capacity - off_cipher` -- so raising the capacity raises the message in
# the same expression. The frame layer is zero because it is SPELLED: `max_frame` is the literal 554
# in comlink/virtio_net.rye, `wire_capacity` is the literal 528 in comlink/wire_format.rye, 554 is
# 12 + 14 + 528 exactly, and a doc comment was the only thing joining them. The two rooms cannot
# import each other -- virtio_net.rye is freestanding and imports nothing, wire_format.rye reaches
# std and kumara -- so the arithmetic had nowhere to live but prose. Measured before the guard was
# written: raising `wire_capacity` 528 -> 576 and changing nothing else still BUILDS GREEN for
# riscv64-freestanding, and the guests' own frame arithmetic run hosted reads `index out of bounds:
# index 602, len 554`. Twenty-seven of fifty-nine guests lay a datagram in at that offset, and
# dg_len is `off_cipher + message.len`, so a short message never reaches the ceiling and a full one
# lands on it exactly -- content-dependent, which is why a fixture carrying a short message passes
# forever. virtio_net.rye publishes `frame_carries` and device_wire.rye evaluates it at comptime.
# Sixteen control cases on real files in a throwaway pen, twelve refusals and four welcomes, and a
# probe that asks the compiler six times -- the tie inverted, the three plausible real edits
# (capacity raised, ethernet header grown a VLAN tag, virtio header widened by a field), and an
# honest raise of `max_frame` that must walk free. Measured here `20260830.204814`: 6s whole, of
# which the pure scan and control are 2s. At tier cadence because the probe builds a Rye module six
# times over, the same clock its carriage sibling keeps.

guard comlink_nesting
path tools/co/comlink_nesting_witness.rish
tier cadence
seated 20260830.204814

# THE GUARDS OUR OWN RULES ALREADY DEMAND (REDS %360). The census that row books found 1,153
# tracked guards this roster reaches on no lap at all, and five of them are not merely unrun --
# each is named by a LIVING RULE that instructs a lap to run it. A rule that names a check and a
# roster that never runs it are one sentence disagreeing with itself, and the disagreement is not
# free: the sixth of that set, `mechanism_sentence_witness`, reds on metal right now because the
# commit wall's one named seed exemption moved out from under both the publisher and the wall's own
# fixture, and nothing was listening. It stays off this roster until that spelling is Keaton's word
# (custody gate %1); these five stood GREEN on metal before their rows were written, because
# rostering a red guard reds every cold pass in the fleet.
#
# WHAT THEY COST, measured here 20260830 on this pier, each run alone: waymark_derive 1s,
# commit_message_guard 2s, waymark_registry 3s, sel4_userlevel_license 0s, crypto_vendored_parity
# 12s -- 18s together against a cold pass of 2,425s, which is why every one of them is `lap` rather
# than `cadence`. The cheapest of them was already the most overdue: `gratitude-licenses.md` calls
# the seL4 sweep "a standing check, not a one-time read," and it had never been run by a standing
# pass at all.
#
# WHAT ROSTERING THEM BUYS, stated honestly against the alternative. `unheard` falls 1,153 -> 1,144 (nine)
# and `unheard_choirs` 41 -> 40. That is HALF what the single largest silent choir would have
# bought, and the count is not the value: what a rostered guard is worth is what breaks while it is
# silent. The larger choirs live in Glow, STOA, SUNN and the equinox family, where REDS %360
# measured a 46% red rate in lanes this seat does not hold, and rostering another lane's red guard
# would red every body's cold pass.

# instrument_refusal -- a guard that cannot run its instrument refuses, and names it. Three scans in
# two days reached for a helper they did not write, could not run it, and reported a clean tree --
# and one of those readings was a ratchet, which passes on a low number, so a broken instrument and
# a healthy tree printed the same green (REDS %416). Gated at ZERO from birth rather than ratcheted
# down, because the two known faults were repaired on the lap this was written.
guard instrument_refusal
path tools/i/instrument_refusal_witness.rish
tier lap
seated 20260905.072118

# instrument_absence -- an absent tool must not wear that tool's answer. The sibling above gates an
# output pass redirected into a FILE; this gates the capture into a VARIABLE whose emptiness is the
# pass branch, which that reading is blind to by construction. Measured with ripgrep genuinely off
# PATH -- not a shim answering 127, which `command -v` still finds -- two of 117 tracked scans
# naming `rg` printed their GREEN line byte for byte and exited 0, and a Rishi witness over one of
# them read GREEN with no ripgrep on the host (REDS %442). Gated at ZERO from birth: both sites were
# repaired on the lap this was written, and there is no honest reason to author a new one. It makes
# no claim about tiers -- the tool-grant roster stays the yonder design's, and rightly comes first
# there.
guard instrument_absence
path tools/i/instrument_absence_witness.rish
tier lap
seated 20260905.223102

# fleet_roster -- the fleet's one seat table, and the readers that must not disagree with it. The
# binding seat -> tree -> engine stood in six independent places across two executables and two had
# already drifted: fleet-loop.sh admitted six seat names while fleet_rearm.sh reported nine, and the
# elder-name remap seated on Keaton's word `20260904` lived in the loop and never reached the
# re-arm. A table two files disagree about is one table plus a waiting red (REDS %409).
guard fleet_roster
path tools/f/fleet_roster_witness.rish
tier lap
seated 20260904.190000

# fleet_lap_verdict -- a lap that dies instantly is either an agent the loop could not reach or a
# window the account has spent, and the two answers are opposite: stop and fetch a person, against
# hold and try again. On `20260906` the loop knew only the first, so six ships stopped themselves
# between 07:21 and 07:28 against a limit that reset at 07:30 (REDS %471). This guard holds the
# distinction, and holds `%414`'s own wound still stopping the loop at three.
guard fleet_lap_verdict
path tools/f/fleet_lap_verdict_witness.rish
tier lap
seated 20260906.082415

# fleet_watch -- a loop that has exited cannot hold, so something above it has to notice. The watch
# reads tmux window NAMES every pass and re-arms any live seat whose loop process is gone; the two
# things it must never do are type into a keyboard somebody else is using and start a second writer
# on one checkout (%291). Both are proven on a real tmux session in a pen, every refusal planted and
# then lifted (REDS %471, the level above the lap classifier).
guard fleet_watch
path tools/f/fleet_watch_witness.rish
tier lap
seated 20260906.090000

# fleet_drain -- two stops with two owners want two files. `.loop-gates-only` is the AGENT's,
# set during its own lap and cleared at the top of the next so last night's stop never blocks
# tonight; `.loop-clockout` is a HAND's, read before a lap opens AND after one finishes, and removed
# by the hand that set it. Until `20260908.165143` the loop cleared the only sentinel at the top of
# every iteration, so a stop landing in the twenty-second sleep between laps was erased before
# anything read it and the loop worked on -- failing silently toward *keep running*, and leaving a
# kill as the only reliable hand-stop, which cuts a lap mid-send. Nine behaviors on a real loop
# body in a pen, every sentinel planted and lifted, the two proven distinguishable.
guard fleet_clockout
path tools/f/fleet_clockout_witness.rish
tier lap
seated 20260908.165143

# sunn_front_doors -- the ten witnesses guarding this tree's front doors ran in NO pass until
# `20260908.183744`. They assert the shape of README, CLAUDE, SOURCE, CONTRIBUTING, STEWARDS,
# llms.txt, keys/README and the grain-os overview -- careful, specific, and completely inert,
# because a guard's reach is the roster rather than its own quality. Four stood RED when first run
# by hand, and `sunn8` had been red since `2026-08-23`: a link sweep had removed the front door's
# `**Voice:** [Kyri](context/KYRI.md)` line as collateral, so the tree's README stopped naming its
# own voice for sixteen days while a 190-green roster ran beside it every twenty minutes. The
# repairs were mostly the guards' own: assertions pinned to a private remote the one-page decision
# retired, to Radiant where Gauge is seated, to an en dash the ASCII sweep replaced, to `e49` where
# the tree reads `e125`, and to a lowercase `record` the rule spells `Record`. A guard bolted to a
# moving counter reds on every ordinary advance; these now read the fact rather than the wording.
# The choir sings all ten, so one roster row reaches every front door.
guard sunn_front_doors
path tools/gen/chapter/sunn15_journey_close_witness.rish
tier cadence
seated 20260908.183744

# font5x7_choir -- 45 witnesses covered this font's glyph tables and NOT ONE was reached by any
# pass; `witness_reach_scan.sh` counted every one `unheard`. One choir row rather than 45 roster
# rows, and the family is DISCOVERED rather than listed, since a hand-written list goes stale the
# first lap somebody adds a glyph table and goes stale silently. Two faults met while writing it are
# recorded in the shared scan, both because each read exactly like a pass: a glob assuming a middle
# segment ran 44 of 45, and an untrimmed `wc` newline reached `sh` as two lines. The scan is generic
# from `20260908.191500` and `hunk_choir` is its second caller. Eight behaviors proven in a
# pen, every refusal planted and then lifted, the bound shown from both sides at the bound itself.
#
# ONE HONEST LIMIT, measured rather than assumed: `witness_reach_scan.sh` counts a witness reached
# when a rostered file NAMES it, and this choir discovers its family by glob, so the meter moved by
# ONE when 45 witnesses began running. The 45 genuinely run -- the scan prints `members=45` and
# fails on any red -- and the meter cannot see a glob. Naming all 45 would satisfy the number and
# reintroduce the stale-list fault the discovery exists to prevent, so the run is kept and the
# number is explained here. A meter that would be improved by making the code worse is a meter to
# read alongside the thing, never instead of it.
guard font5x7_choir
path tools/f/font5x7_choir.rish
tier cadence
seated 20260908.185500

# fleet_rearm -- the seat printer and its sentinel wall, proven on a real pen. It was unheard by
# every pass until `20260908.185500`, and this lap had already repaired its shared constant pen, so
# a guard proven green the same hour was still one nothing ran.
guard fleet_rearm
path tools/f/fleet_rearm_witness.rish
tier cadence
seated 20260908.185500

# hunk_choir -- 86 witnesses cover the HUNK ladder, Chapter A's open image module and what grew
# beside it, and NOT ONE was reached by any pass. One choir row rather than 86, over the GENERIC
# `witness_family_scan.sh`: the font choir written one lap earlier was bolted to its own family, and
# the very next family wanted the same thirty lines with one word changed, so the scan took a
# `--dir` and a `--prefix` and both choirs now call it. A third family costs a roster row rather
# than a file. Eleven behaviors proven in a pen, every refusal planted and then lifted, the bound
# shown from both sides at the bound itself, and the two required arguments proven to refuse rather
# than default -- since a silent default picks a family nobody chose.
guard hunk_choir
path tools/h/hunk_choir.rish
tier cadence
seated 20260908.191500

# mycelium_choir -- 81 witnesses cover Mycelium, this tree's own `.myc` notation, and 69 of them
# were reached by no pass. THIRD caller of the generic `witness_family_scan.sh`, and the one that
# shows what generalizing bought: a file of comments and this row, with no scan and no control of
# its own. It declares a `# reach-list:` enumerator like its siblings, so the meter counts what it
# actually sings rather than inferring from a glob.
guard mycelium_choir
path tools/m/mycelium_choir.rish
tier cadence
seated 20260908.192900

# fora_choir -- 32 witnesses cover the FORA ladder, the Constel test-network names that can never
# parse as a live `@p`, and none was reached by any pass. Fourth caller of the generic
# `witness_family_scan.sh`.
guard fora_choir
path tools/f/fora_choir.rish
tier cadence
seated 20260908.203500

# buhr_choir -- 49 witnesses cover BUHR, Compass Equinox 3's Realidream surface and the Quin
# inference vane, and none was reached by any pass. Fifth caller of the same scan.
#
# THE FAMILY NOT TAKEN, named here so the next lap does not reach for it: `equinox` is the largest
# unreached prefix at 112, and it is the one family a prefix choir would get WRONG. The reach scan's
# own header records why -- inferring from the season choir's glob overstated by 111, because the
# pattern selects 144 equinox witnesses while the choir sings the 33 that chain nobody. A family
# whose members chain each other wants its chain read, never its prefix globbed.
guard buhr_choir
path tools/b/buhr_choir.rish
tier cadence
seated 20260908.203500

# equinox_choir_census -- 82 choirs stand in `tools/equinox/witness` and a census on `20260908` read
# them 50 green, 30 red, 2 hung. All 50 green ones were reached by NO pass, so a green choir that
# stopped being green had nothing to say so. A RATCHET rather than a wall: a wall would refuse the
# roster on 30 reds that stood before anyone looked, and a guard that reds on ordinary standing
# state is one somebody turns off. Both ceilings only fall.
#
# Each choir is PROBED rather than run, through `tools/fixtures/w/witness_probe_scan.sh`, so a HANG
# is told from a RED -- one is a claim about the run, the other about the tree, and two members of
# this room genuinely hang. `tier cadence`: the census costs about fifteen minutes on this pier.
guard equinox_choir_census
path tools/e/equinox_choir_census_witness.rish
tier cadence
seated 20260908.230500

# fleet_call -- a signal is not a file, so one-writer-per-checkout never reached it. Eight ships run
# one program name from eight trees on one pier, and `pkill -f standing_equipment_run` therefore
# reaches the fleet rather than the lap: measured live on this pier the same hour, sixteen peer
# processes across four trees stood behind that one pattern, and the calling shell's own command
# line carried it too (exit 144, three firings in two laps -- REDS %541). tools/f/fleet_call.sh
# resolves each candidate's working directory and signals only what sits under this tree's own
# root, refusing a peer and refusing the caller's ancestry OUT LOUD -- a kill that misses is silent
# on both sides. Twenty-two behaviors on real processes in a pen, every refusal planted and lifted.
guard fleet_call
path tools/f/fleet_call_witness.rish
tier lap
seated 20260907.073406

# process_reach -- the helper above made the safe form near; this makes the unsafe form loud. Every
# wall in this tree watches FILES, and a pattern kill touches none, so the two firings on `20260907`
# (%569, %587) were invisible to every standing guard. This one reads tracked `.sh` and `.rish`
# sources for a command-position `pkill`, `killall`, or `pgrep` whose selector is a command-line or
# program-name pattern -- the READ counted beside the kill, since `pgrep -f standing_equipment_run`
# answered 22 processes across seven peer trees on this pier the hour it was written. `-P` is
# bounded by parent and free; a comment discusses rather than calls; `ripgrep` can never open a
# fragment; and a site whose boundary is real DECLARES it in words on one of the three lines above,
# so the judgment stays readable instead of drifting inside a hand-kept path list. Ratchet at 2, both
# named in the scan and both in lanes this seat does not hold. Twenty-one behaviors in a pen.
guard process_reach
path tools/p/process_reach_witness.rish
tier lap
seated 20260907.214518

# fleet_key_locality -- one jail binds one tree, so every path a ship's git config names must live
# inside that ship's own tree. grain-pheromone was born naming petrichor's keyring and petrichor's
# deploy keys, and could neither sign nor push from inside its own enclosure while every check at a
# host prompt passed (REDS %427). This reads UNTRACKED files -- `.git/config` and
# `.git/ssh_config_jail` carry custody paths and are untracked by design -- which is why no tracked
# scan, the commit hook, or the link duty could ever have reached it.
guard fleet_key_locality
path tools/f/fleet_key_locality_witness.rish
tier lap
seated 20260905.152000

# fleet_round_open -- the script whose one verb is `reset --hard`, run on three live trees roughly
# every twenty minutes, and which had no control at all until REDS %428. It modeled no interrupted
# rebase, so a conflicted worktree read dirty and got stashed, a detached HEAD got classified, and
# the reset abandoned the rebase leaving the real branch behind. Against the elder script the
# control fails 5 of its 14 legs. Destructive authority earns a standing guard, not on-touch care.
guard fleet_round_open
path tools/f/fleet_round_open_witness.rish
tier lap
seated 20260905.153500

# stash_record -- the round open FILLS a dead-letter box and, until REDS %464, nothing ever looked
# inside it. Measured on this tree: five stashes, and three of them held a complete session log no
# branch carried -- one recording a lap whose CODE had landed, so the work shipped and its
# reasoning did not, and the next lap paid to rediscover it. The gate is `unlanded=0`: a session
# log parked at an open is carried by the worktree, a branch, or a remote-tracking branch. It reads
# the RECORD rather than the box, so it goes green by reading a log back and never by dropping a
# stash. The obvious probe would have been `git log --all`, which reaches refs/stash and reports a
# staged record's own stash index commit as history.
# Widened `20260906.194500` (REDS %510): a record is not the work. The session log is the proxy
# because every lap writes one, and the proxy breaks when a lap's log lands while its code does not
# -- `unlanded=0 verdict=ok` over a box still holding the files. Four stood here across three
# stashes, 258 lines, none of them raising `records` by one. The scan now reads every NON-record
# path too: `orphans` is what nothing outside the box carries, `unread` is what something carries
# and whose stashed EDIT no path probe can judge. Reported rather than gated, because a COUNT does
# not travel between checkouts the way `unlanded=0` does; the round open names both drawers.
guard stash_record
path tools/s/stash_record_witness.rish
tier lap
seated 20260906.073939

# announced_length -- a living page announcing a ladder's length is a forecast, and this counts how
# far each one actually got. The mark law names four such ladders, all found by hand and typed into
# the rule; five more stood in `context/LEXICON.md` unnoticed, where the Compass Chapter's four
# equinoxes announced 256 rungs between them and had reached six. It reports the ladders that MET
# their announcement as loudly as the ones that fell short, because a reading that only accuses is
# one nobody believes.
guard announced_length
path tools/a/announced_length_witness.rish
tier lap
seated 20260905.164500

# captain_view -- the captain reads a peer's work and never a peer's keys. Every peer tree arrives
# read-only and the three credential paths are denied outright, proven by entering a real enclosure
# and trying the thing rather than by reading the flags. `cadence` because the enclosure's shape
# changes on a deliberate edit rather than on a lap, and each run costs a real sandbox launch.
guard captain_view
path tools/ca/captain_view_witness.rish
tier cadence
seated 20260905.181500

# THE MANTRA WITNESSES -- twenty proofs that ran only when a hand remembered. Measured `20260905`:
# `mantra/` holds 32 Rye modules and 9,175 lines, **120 Rye files outside it reach into it** --
# Brushstroke's photo library, Pond's apps, the editor's undo -- and NOT ONE of its ~26 witnesses
# stood on this roster. The tree's most-depended-upon module had no standing guard. All twenty run
# GREEN, timed here, and they are seated by what each one proves: the module's own contracts at
# `lap`, and the surface built on top of it at `cadence`. Core cost, measured: 39s.

guard mantra_bolt_apply_step
path tools/m/mantra_bolt_apply_step_witness.rish
tier lap
seated 20260905.161500

guard mantra_a2_list_fold
path tools/m/mantra_a2_list_fold_witness.rish
tier lap
seated 20260905.161500

guard mantra_gen_floor_a1_gate
path tools/m/mantra_gen_floor_a1_gate_witness.rish
tier lap
seated 20260905.161500

guard mantra_weave_merge
path tools/m/mantra_weave_merge_witness.rish
tier lap
seated 20260906.001728

guard mantra_weave_annotate
path tools/m/mantra_weave_annotate_witness.rish
tier lap
seated 20260906.115458

# The elder record, lifted rather than converted. `mantra/src/main.rye` writes a three-field
# `mantra-weave-v1` blob and this module widened past it, so the open question (REDS %589) was
# whether closing that gap breaks the record already on disk. It does not: the record was
# written by one hand, so site and run are CONSTANT across it rather than absent from it, and a
# constant run beside a constant site reduces `Place.less_than` to `pos <` -- the elder reader's
# own comparison. `Weave.from_v1` is that sentence as code; the witness compares the two readers
# line for line and proves four refusals the elder reader never had. It ports nothing and moves
# no byte on disk; it makes the port mechanical and the record's safety checkable.
guard mantra_weave_v1_lift
path tools/m/mantra_weave_v1_lift_witness.rish
tier lap
seated 20260908.051232

# The elder record, written back. The lift guard above answered the READING half of REDS %589:
# an elder `mantra-weave-v1` record reads the same after the widening, since site and run are
# constant across a record one hand wrote. `mantra/src/main.rye` WRITES that record on every
# commit, so until a weave the module has advanced can be written back as three fields, "the
# port moves no byte on disk" was a claim about reading wearing the clothes of a claim about
# the record. `Weave.to_v1` is the other half, and the round trip is identity on the DOCUMENT
# rather than on the weave -- the runs come back collapsed to `v1_run`, and the document is
# unmoved because inside ONE site `next_run` and `next_pos` both only rise at the same apply,
# so a higher run always carries a higher position. What the format cannot hold it refuses by
# name: a second site, since the record has no field to tell two hands apart; and a merge whose
# position order has parted from its document order, which the witness BUILDS rather than
# argues. It ports nothing and moves no byte on disk.
guard mantra_weave_v1_write
path tools/m/mantra_weave_v1_write_witness.rish
tier lap
seated 20260908.123338

# rish_report_bound -- a guard's success report has a ceiling and its refusal does not, and until
# 20260907 nothing read the difference. Rishi composes an interpolated string literal through
# `StrBuf`, a bounded 4,096-byte buffer in rishi/src/main.rye; a bare `say <value>` writes straight
# out under no ceiling. `reds_spine_derive_witness.rish` passed BOTH its gates -- `verdict=ok`,
# `rebindings=0` -- and then ended the run at `say "label ${scan.out}"`, because the reading had
# grown to 4,250 bytes as the ledger grew. Eight ships paid a red every lap for a green reading,
# and the message named the guard's own reporting line rather than the tree. The asymmetry is
# deliberate on Rishi's side and is the finding: an `assert ... else` message that overflows falls
# back to its raw literal, so the REFUSAL path degrades rather than dies -- the unprotected path is
# the one that fires when everything is right. Ratchet at 38 sites across 26 files, never a gate:
# every site is correct today and fails only when its own reading grows, so a gate would refuse
# ordinary work. Narrowed from 504 interpolating sites across 467 files by requiring the file to
# read the same capture with `contains` -- a verdict-bearing report rather than a probe's one line;
# counting the rest would push 466 files toward worse prose to reach the 26 carrying the hazard.
# Three claims proven against the real interpreter on metal in the control, not read out of source.
# `lap` rather than `cadence`: the elder lap measured 19s and this pier re-proved it at 31.7s on
# the day it was rostered, and the shape it counts arrives in any lap that writes a guard.
guard rish_report_bound
path tools/r/rish_report_bound_witness.rish
tier lap
seated 20260907.233349

# The name a blob carries, spent on every read. `mantra/src/store.rye` named a blob by its
# SHA3-256 digest and then never checked it again: `Sha3.hash` stood in `write_blob` alone, so
# the whole content-addressing claim rested on the bytes on disk never changing. Beside it the
# read used `Dir.readFile`, whose `readSliceShort` returns `buffer.len` the moment the buffer
# fills, so a blob past the ceiling came back as its own first megabyte with no error raised.
# Together those are silent corruption rather than two faults: a truncated `mantra-weave-v1`
# record is a VALID PREFIX, so it lifts as a shorter weave and the next commit writes it back
# under its own honest digest. Proven on metal before this row was written -- the elder CLI
# reported a diff against a truncated history and exited 0; the ported one refuses
# `BlobNameMismatch`. Four numeric ceilings stood unnamed across two files, two of them the
# same function at two heights (1 MiB here, 4 MiB in `main.rye`, over one `.mantra/blobs/`);
# `max_blob_bytes` is one number at 128 MiB, so folding them RAISED the shipped capacity --
# which is why the port was declined once already (`20260908.143034`). It builds two binaries
# and writes a 128 MiB file, so it is 21s and a lap guard rather than a cadence one.
guard mantra_store
path tools/m/mantra_store_witness.rish
tier lap
seated 20260908.191500

guard mantra_diff
path tools/m/mantra_diff_witness.rish
tier lap
seated 20260906.061556

# Mantra's own width reading, beside the tree-wide one at width_check above. It
# had run green by hand for weeks and stood on no roster, so nothing heard it --
# which is `%360`'s shape, a proof nobody runs. It reads one room and takes
# under a second, so it is a lap guard rather than a cadence one.
guard width_check_th3
path tools/w/width_check_th3.rish
tier lap
seated 20260907.172608

guard mantra_declaration_walk
path tools/m/mantra_declaration_walk_witness.rish
tier lap
seated 20260907.081500

guard mantra_a1_equality
path tools/m/mantra_a1_equality_witness.rish
tier cadence
seated 20260906.094442

guard conflict_marker
path tools/c/conflict_marker_witness.rish
tier lap
seated 20260906.105500

guard mantra_glow_tend_limb1
path tools/m/mantra_glow_tend_limb1_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb2
path tools/m/mantra_glow_tend_limb2_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb3
path tools/m/mantra_glow_tend_limb3_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb4
path tools/m/mantra_glow_tend_limb4_witness.rish
tier lap
seated 20260905.161500

# WHAT THE LIMB GUARDS ABOVE CANNOT SEE. Each reads ONE file, and Mantra's weave model is declared
# in three: `mantra/src/weave.rye`, the CLI seed `mantra/src/main.rye`, and the test that inlines
# it. `bc37657e8` widened the module -- `Line` gained `site`, `Diff` gained `site` -- and neither
# copy moved, so the lock on widening `Line` held over a file nobody else was reading. This guard
# reads every tracked copy against the module. It welcomes the two known laggards under a ceiling
# that only falls, since reconciling them rewrites `.mantra/`'s on-disk record and stays a seam at
# Keaton's word, and it refuses any copy arriving disagreeing.
guard mantra_weave_model
path tools/m/mantra_weave_model_witness.rish
tier lap
seated 20260907.210124

guard tube5_mantra_revision
path tools/t/tube5_mantra_revision_witness.rish
tier lap
seated 20260905.161500

# THE CENSUS THAT READS A ROOM AS DEEP AS THE ROOM GOES (REDS %441). The lap that rostered the
# twenty guards above also published, in the same commit, that Mantra's weave "still waits to be
# built" -- while four of those twenty read `mantra/src/weave.rye` every lap and assert the exact
# fields the essay booked three rounds to write. Every number in that essay's table is correct; the
# conclusion crossed two scopes, because `mantra/*.rye` cannot see `mantra/src/`. This publishes the
# trap for every room rather than that one: 44 rooms, 14 carrying a hidden shelf, 513 files below a
# top level, 8 rooms where a flat glob reports zero Rye while the room holds some. It gates the
# INSTRUMENT rather than the tree's shape -- a subdirectory is good structure -- so it refuses only
# when it read nothing or when its three counting patterns disagree. Measured here: 3s.
guard module_room_reach
path tools/m/module_room_reach_witness.rish
tier lap
seated 20260905.225236

# The Realidream surface built ON Mantra, at `cadence` -- the fifth round. These prove an editor
# and a browser rather than the module's own contracts, so they answer on the slower clock. A
# cadence is a clock, never an exemption. Surface cost, measured: 23s.

guard buhr_mantra_browser
path tools/b/buhr_mantra_browser_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_heads
path tools/b/buhr_mantra_browser_heads_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_view
path tools/b/buhr_mantra_browser_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_theme
path tools/b/buhr_mantra_browser_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_theme_stack
path tools/b/buhr_mantra_browser_theme_stack_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_view_theme
path tools/b/buhr_mantra_browser_view_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_full_theme
path tools/b/buhr_mantra_browser_full_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor
path tools/b/buhr_mantra_editor_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_view
path tools/b/buhr_mantra_editor_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_undo
path tools/b/buhr_mantra_editor_undo_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_undo_view
path tools/b/buhr_mantra_editor_undo_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_fork
path tools/b/buhr_mantra_editor_fork_witness.rish
tier cadence
seated 20260905.161500

# agent_jail -- the launcher every unattended Linux lap runs through, and the guard that proves it.
# It stood UNROSTERED and RED at its first leg while the launcher worked perfectly: it grepped the
# dry-run plan for `.claude-state` and `.cursor-agent-state`, the directories the `20260827`
# one-room adoption moved under `loops/`. Nothing was listening, so three stale pins stood for eight
# days and the launcher's real fault -- a `~/.claude.json` mount conditional on a file only the
# jailed Claude could write -- rode in beside them (REDS %408, %360's unheard-guard class). Rostered
# the lap it was repaired, since a guard proved green once and heard by nothing guards nothing.
# agent_jail -- SPLIT `20260906.010500` (REDS %455). The whole guard read RED on eight of eight
# ships because four of its legs launch the jail and `bwrap` will not nest, so a ship inside the
# enclosure answered `Failed to make / slave: Operation not permitted` at a leg that could never
# have run there. A red guard withholds the roster receipt, `--scoped` refuses without a full green
# receipt, and every ship therefore paid a full cold pass every lap for an environment fact. The
# legs that read plans, paths and seeded files run everywhere and stay here; the legs that launch
# the jail moved to the row below, behind a capability the runner PROBES by attempting a trivial
# bwrap. Not the `host` field %422 declined -- a declaration can be wrong about a bench, and an
# attempt cannot.
guard agent_jail
path tools/ag/agent_jail_witness.rish
tier lap
host linux
seated 20260904.182600

guard agent_jail_enclosure
path tools/ag/agent_jail_enclosure_witness.rish
tier lap
host linux
capability jail_nesting
seated 20260906.010500

# commit_message_guard -- GATE FIELD LIFTED `20260905.115500`. It carried `gate %1` because
# publish-seed.sh was absent from this pier, so SEED_PUBLISHER_ARMS read 0 and the guard reported
# that a fresh seed/.git would publish with the message wall unarmed. The publisher is reconstructed
# and the guard is GREEN, so the gate comes off in the same lap -- a gate left standing over a green
# guard would excuse the NEXT red, which is the one thing %374's field must never do.
guard commit_message_guard
path tools/co/commit_message_guard_witness.rish
tier lap
seated 20260830.154940

guard waymark_derive
path tools/w/waymark_derive_witness.rish
tier lap
seated 20260830.154940

guard waymark_registry
path tools/w/waymark_registry_witness.rish
tier lap
seated 20260830.154940

guard sel4_userlevel_license
path tools/s/sel4_userlevel_license_witness.rish
tier lap
seated 20260830.154940

guard crypto_vendored_parity
path tools/cr/crypto_vendored_parity_suite.rish
tier lap
seated 20260830.154940

# AND THE SIXTH, one lap later (REDS %360, %380's own round). The comment above says this guard
# stays off the roster until the seed subject is Keaton's word. That word came on `20260830.190407`
# and the living card carries it: the subject is `crashed-meteor`, spoken by the wall, by the
# field's publisher, and by both rules, and the real publish shipped on it. What remained was a
# tracked file nobody had swept -- `tools/fixtures/m/mechanism_wall_scan.sh` still planted the
# retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed`
# as the borrow attempt beside it, so the fixture the witness trusts was refused by the wall it
# tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling, and the borrower
# keeps its teeth: fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches
# the whole subject rather than a suffix. GREEN on metal before this row was written, 1s alone.
# `unheard` fell 1,144 -> 1,143 as this row landed, and came back to 1,144 in the same window as two
# unrostered witnesses landed upstream. The ceiling stands where it was published; the reading is
# what moved, and this row is still the last of the six our own rules demanded.

guard mechanism_sentence
path tools/m/mechanism_sentence_witness.rish
tier lap
seated 20260830.202400

# glow_preset_offset -- Sound's nesting law landed `20260830` carrying its own witness and no clock,
# so the first pass after the fast-forward read `witness_reach` at 1,094 unreached against a ceiling
# of 1,093, and `unheard_guard` rose with it. That guard's own refusal names the answer: put the new
# one on a clock rather than raise the ceiling. Measured 31s on metal here and GREEN before this row
# was written. `lap` rather than `cadence` because Glow's composition is the arc under active change
# -- the same reason `caravan_suite` pays its minutes every pass.

guard glow_preset_offset
path tools/g/glow_preset_offset_witness.rish
tier lap
seated 20260830.233618

# shelf_link_touch -- the commit-time half of the day-shelf link reading (REDS %383). `readme_reach`
# already gates every broken link in a living file at zero, and `reds_fold` runs the same scan as its
# third duty, so one bad shelf row reds two rostered guards -- 45 minutes after it was written. This
# one reads only the shelves a commit stages, off the index, and `tools/hooks/pre-commit` carries it
# as rule four, so a row is refused on the lap that writes it. The two readings cannot disagree about
# the gated class: the pen plants ONE fault and drives BOTH scans over it, requiring both to refuse
# and then both to pass. 28 cases in a throwaway pen, five arming the real hook. 11s on metal, GREEN
# before this row was written. `lap` rather than `cadence` because a shelf row is written on nearly
# every lap this tree runs, which is exactly when a wall this cheap should be listening.

guard shelf_link_touch
path tools/s/shelf_link_touch_witness.rish
tier lap
seated 20260831.003448

# acme_dx and drey -- two choirs the roster could not hear, and the last two this seat can reach
# alone (REDS %360). The census reads a choir as a witness that runs three or more siblings, and a
# silent choir takes its whole family down with it (REDS %219). Forty stood unheard; these two are
# the pair whose families sit in no other body's lane, so rostering them crosses nobody. `acme_dx`
# sings the Acme developer-arc conformance witnesses, one of which -- `operations_conformance` --
# is a choir in its own right, so the pair reaches further than its two rows suggest. `drey` sings
# the Mikrophone firmware rungs, all pure Rye on the bench: no disk, no network, no key, no funds.
# NEITHER FAMILY'S SIZE IS TYPED HERE. Both grow, and a count written beside a thing that grows
# stops the day it is typed -- which is REDS %386, found in `drey`'s own header on this same lap.
# The census counts them: `sh tools/fixtures/u/unheard_guard_scan.sh choirs`. What it read as the
# two rows landed, 20260831: heard 660 -> 688, unheard 1,144 -> 1,116, unheard choirs 40 -> 37.
#
# COST IS WHY BOTH SIT ON `lap` RATHER THAN `cadence`. Measured on this pier 20260831, each run
# alone and GREEN before its row was written: acme_dx 3s, drey 5s -- 8s together, against a cold
# roster pass of 63 minutes. A tier is a cadence rather than an exemption, and eight seconds buys
# no exemption worth taking.

guard acme_dx
path tools/ac/acme_dx_witness.rish
tier lap
seated 20260831.014630

guard drey
path tools/d/drey_witness.rish
tier lap
seated 20260831.014630

# Spool Cloth's name bound, seated 20260831.045858 -- the second half of one room's naming layer arriving
# in the shape museum. Tablecloth's four desks landed 20260829 and 20260830 and the last of them
# left the invitation on its own placard: pond/apps/spool_cloth.rye took the same wall in the same
# shape on the same day. The two modules are housemates rather than neighbours, since
# brushstroke/tablecloth.rye is a symlink into pond/apps/, so this is one room finishing its
# thought rather than a new vane arriving.
#
# The guard is shaped like its sibling: the scan reads the number the desk shows and the number the
# Rye publishes and compares them, holding no value of its own, so raising the bound honestly in
# both rooms walks free -- proven as a welcome beside the refusals rather than assumed.
#
# IT CARRIES TWO READINGS ITS SIBLING DOES NOT, and both were found by reading rather than by
# copying. First, this module reads max_name at TWO call sites: store_large refuses a long name
# with NameTooLong, and parse_manifest refuses a long field with BadManifest -- and the second
# guards an array's end rather than a policy, since a parsed name lands in a fixed [max_name]u8.
# They fire apart, so the control reads the survivor by name while its neighbour refuses. Second,
# the placard's SIXTH line is read by VALUE: PLACARD.md gives the nib its reason -- a pedestal
# displays a value AT a nib -- and every placard reading in the tree counts that keyword and stops,
# so across the 37 residents standing before this desk the stamp after it was compared to nothing.
# It is compared against spool_cloth_version now, so a module whose version moves while its desk
# keeps the elder nib reds here.
#
# A SECOND PEDESTAL JOINED IT 20260831.073652, and it carries a THIRD reading of its own kind.
# max_large_artifacts is shown beside the ceiling artifacts the shared bead store can pay for --
# four seats against a guarantee of two, measured on metal by the module's own selftest, since the
# catalog counts NAMES and the store counts BYTES. That guarantee is derived two levels deep out of
# mantra/beading.rye and mantra/spool.rye, so the scan RECOMPUTES it rather than reading a copy and
# reads the module's own derivation as a fact beside it. And the bound is the length of three fixed
# arrays in TWO modules: the third, KeyedLargeCatalog.owners in pond/apps/spool_keyed.rye, has no
# wall of its own and rides on store_large's through a `try` -- so the scan reads the bound where it
# CROSSES a module boundary, which no desk in this room had done before.
#
# Forty-seven control cases -- seven welcomes, forty refusals -- and thirteen named readings.
#
# `cadence` because the metal legs lower both desks through Glow and build them with the Zig
# toolchain, the same clock its Tablecloth sibling and the six Glow witnesses beside it keep.
# Re-measured on this pier 20260831.073652, GREEN before this row moved: 24s whole -- 17s of it the
# forty-seven-case control, the rest two Zig builds -- against a cold pass of 2,317s. The pure scan
# still runs in under a second, so a hand asking it by name pays almost nothing.

guard pond_spool_cloth_glow_tend
path tools/p/pond_spool_cloth_glow_tend_witness.rish
tier cadence
seated 20260831.045858

# living_pin_near_bound -- the near-bound advisory reads the set the pin law names. Duty 6 of the
# docs lint is the one reading in this tree that speaks BEFORE a pin crosses its bound: e123 and
# pin_bound_touch both ask whether a pin is OVER, and a pin three bytes under is not. On 20260831
# the card and the ledger stood at 3 and 21 bytes free with all three of those guards green, and
# the advisory that exists for exactly this said nothing about the ledger -- because it kept a docs
# roster of its own and had never weighed four of the seven seated pins (REDS %396).
#
# `lap` rather than `cadence`: nine pen cases, 1s measured on this pier before the row was written,
# and the reading it guards is the one that decides whether the next lap can write to the card at
# all. Both faults are proven from the side where the reading goes silent -- five of the nine bite
# against the departing scan -- since a repair proven only in the speaking direction cannot be told
# from a rewording.

guard living_pin_near_bound
path tools/l/living_pin_near_bound_witness.rish
seated 20260831.080221

# glow_core -- the first Glow core form, and the one witness of four that landed unheard. The
# `|%` core landed 20260830.224500 under the growth law: three proven Lantern laws -- the 38-byte
# text wall, the unknown-carrying-an-answer refusal, the feed's kind law -- gathered into one
# source with zero new gate grammar. Its witness proves all three arms answer as their standing
# desks and that an unnamed arm, an unclosed core, and an empty core refuse by name.
#
# It arrived with three siblings on the same night and was the only one no runner named, which put
# the unheard reading at 1117 against a ceiling of 1116 -- the ceiling doing exactly the work a
# ratchet is for. A landed law whose guard nobody runs is a law nobody is checking.
#
# `cadence` rather than `lap`: 66s measured on this pier 20260831.114124, GREEN before this row
# moved, which is choir money on the same clock glow_choir and the six Glow witnesses beside it
# already keep. A hand asking it by name pays the same 66s and pays it deliberately.

guard glow_core
path tools/g/glow_core_witness.rish
tier cadence
seated 20260831.121153

# provenance_self_reference -- a page may not be named as its own elder. A molt seats a page at a
# fresh stamp, so the mutant's header carries a provenance line naming the elder it grew from; the
# repoint pass that follows treats that line as a citation and rewrites it to the mutant's own
# name, and the page becomes its own ancestor (REDS %435, twice on `20260826`). Nothing standing
# could see it, because a self-link RESOLVES: `tracked_link_scan` and the broken-link duty test
# with `[ -e ]`, and the report card scores Truth by counting cited paths that FAIL to resolve --
# so `single-stranded` read `truth=100` and `A+/98` with its own first sentence false.
#
# Bounded to this tree's own provenance vocabulary rather than to self-links at large: of the 35
# tracked documents carrying a self-link, the other 33 are honest sibling rosters and dated logs.
guard provenance_self_reference
path tools/p/provenance_self_reference_witness.rish
tier lap
seated 20260905.160212

# amphora -- the vessel family's eight witnesses, and not one of them stood on a clock. How large
# the room is now reads off an instrument rather than off this line: `sh
# tools/fixtures/m/module_room_reach_scan.sh list | grep '^room=amphora '` answers `own=` and
# `linked=` on every run, so a peer's linked module is never counted as this lane's. The elder
# spelling here said 7 and 3,603 lines, corrected by hand on `20260906.063124` from a reading of 10
# that had counted three symlinks into `tally/` and `comlink/`; one day later the line count had
# drifted 273 and nothing was listening (the row booked `20260907.215529`). This roster named no
# guard over any of it. A lap that
# broke a seal, a stamp, or the chunk assembler would have shipped green, because nothing was
# listening -- and the chunk assembler stayed that way until the crossing rows landed. That is
# REDS %360's population exactly -- a guard that is never run guards nothing -- one family at a time.
#
# WHAT THE EIGHT PROVE, in the pairs they were written as. `pour` fills a vessel from the Cellar
# ring-1 season, seals the cargo, then stamps the canonical body; `carry` moves it to a far dock and
# proves the far bytes equal the near ones; `restore` scrubs cold and lands four files at the same
# parent; `grand_round` runs pour, carry and restore end to end and re-restores; `first_resident`
# pours a real counsel season and proves the out-home byte-equal. Each of the first three carries a
# NEGATIVE twin that plants at the wall it names -- a forged manifest line, a forked-season re-pour,
# an absent season, a tampered `seal_cargo`, a truncated vessel, a forged resin body, a torn chunk --
# and asserts the refusal by its own error name, with the standing vessel proven untouched after
# each. So the family already carries the half of the law a green reading can never show on its own:
# every one of these guards is proven able to red.
#
# `lap` rather than `cadence`: all eight GREEN on this pier 20260905, 11 seconds together against a
# lap pass of 838. They build the three binaries they drive rather than assuming a previous lap left
# them behind -- `amphora/bin/` is gitignored, which killed them on a missing file once already
# (REDS %334, and %337 for the absent -femit-bin directory) -- so a fresh clone pays that build and
# the reading stays a fact about Amphora rather than about the last hand.
#
# The two `tools/k/kumara_*` witnesses read UNHEARD beside these and are NOT seated here: both are
# green on this pier, and both drive `kumara/` over `tally/kumara.rye` rather than `amphora/`, so
# they belong to the lane that owns Tally. Named rather than taken.

guard amphora_pour
path tools/am/amphora_pour_witness.rish
tier lap
seated 20260905.225551

guard amphora_pour_negative
path tools/am/amphora_pour_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_carry
path tools/am/amphora_carry_witness.rish
tier lap
seated 20260905.225551

guard amphora_carry_negative
path tools/am/amphora_carry_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_restore
path tools/am/amphora_restore_witness.rish
tier lap
seated 20260905.225551

guard amphora_restore_negative
path tools/am/amphora_restore_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_grand_round
path tools/am/amphora_grand_round_witness.rish
tier lap
seated 20260905.225551

guard amphora_first_resident
path tools/am/amphora_first_resident_witness.rish
tier lap
seated 20260905.225551

# amphora_roster -- every authored module in the vessel room answers to a rostered guard, or is
# counted. The Sound lane has carried this reading for `tally/` since `20260826`
# (`tools/fixtures/t/tally_roster_scan.sh`); the vessel room had no twin, so its coverage lived in
# prose a hand typed and re-typed. The block below carried a module and line count corrected in
# place on `20260906`, and the line count drifted 273 by the next morning with nothing listening
# (row `20260907.215529`); `amphora/README.md` read *twelve standing witnesses run every lap* while
# fifteen rows stood here, thirteen of them on the lap clock. REDS %360's family, one room over: a
# claim with no instrument. The repair is the instrument rather than a better-typed number, so this
# roster's own vessel counts are now answered by
# `sh tools/fixtures/am/amphora_roster_scan.sh` on every run.
#
# WHAT IT GATES at zero: `uncovered`, a module no rostered guard names -- a break a lap would not
# catch; `orphan_rows`, a row naming a guard file the room lacks -- the roster promising a run
# nobody can make; and `readme_unnamed`, a rostered guard the room's front door never names.
# `singly_covered` is REPORTED and never gated, because a module born today is
# named by exactly one guard on the day it lands, and a ceiling there would red on the most ordinary
# work in the room. Two stand there now, `manifest_entry.rye` and `purchase_delivery.rye`.
#
# THE THIRD GATE LANDED `20260908`, and it closes the half the first repair left open. This block's
# own counts came off an instrument from `20260907.223214` forward, while the guard LIST on
# `amphora/README.md` stayed hand-typed and drifted the same way the counts had: it named 11 of the
# 16 rows seated here, passing over `amphora_roster` -- the guard this scan serves -- and
# `amphora_mark_wreck`, both seated after the list was last written. A count read off an instrument
# beside a list typed by hand is half a repair. It has teeth where `singly_covered` does not for a
# plain reason: seating a guard is a roster row a hand writes, and naming it at the room's door is
# the same hand, the same lap, one line -- so the wall refuses nothing anyone does on purpose. It
# was reached by REPAIR rather than by decree; both missing names were written the lap it landed.
# An absent front door answers the word `absent` rather than zero and gates nothing, so the witness
# asserts this room's door BY PATH.
#
# THE FOURTH GATE LANDED THE SAME DAY, over the numbers still living in that page's prose.
# `readme_spelled_lines` holds the door at zero line counts spelled in digits, and `own_lines` with
# its `detail_lines` listing gives the door something to name instead. The door read
# *`purchase_delivery.rye`, `vessel_fetch_wire.rye` and `vessel_fetch_delivery.rye` are 1,160
# lines*: true at `0ee5c8171`, written into `05c87d3d0` -- the commit that grew
# `vessel_fetch_delivery.rye` by 109 changed lines -- so it shipped 77 short inside the commit that
# made it short, and stood 161 short two days on. The hand measured and then kept working, which no
# amount of care prevents and one reading does.
#
# AND A COUNT SPELLED IN LETTERS DRIFTED UNDER THAT GATE THE SAME DAY. The row closing the fourth
# gate let the word form walk free, saying a number spelled as a word cannot go stale in silence.
# Two paragraphs down the same page it read *Those eight build three modules* while nine of the
# sixteen rostered guards built all three -- written when twelve stood over the room, and read again
# after `amphora_mark_wreck` made sixteen. `readme_spelled_words` reads a number word from `one` to
# `fifty` beside `line`, `module`, `guard` or `witness`, the four nouns this instrument measures,
# and REPORTS rather than gates: the same door carries `three more modules` in a sentence that
# enumerates what it counts, and a wall refusing that would refuse honest prose. Measured across the
# 113 living module front doors outside the closed stacks, 36 pages carry 63 such lines.
#
# A SYMLINK IS ANOTHER LANE'S MODULE. The room is read with `find ! -type l`, so `kumara.rye`,
# `tally_copy.rye` and `wire_format.rye` are counted apart rather than held to this lane's account
# -- the same distinction that corrected the room's own reading from 10 to 7. A SHIM IS FOLLOWED ONE
# HOP, bounded at four, since nine files under `tools/am/` run a target under `tools/gen/amphora/`
# and the module names live in the target.
#
# `tier lap`: shell and awk only, no build, measured under a second against a lap pass of 1,248.
# Proven by 77 planted behaviors on throwaway rooms in
# `tools/fixtures/am/amphora_roster_control.sh` -- every refusal shown from the failing side and
# then lifted, the vacuum leg proving that zero rostered guards over a room holding modules refuses
# rather than reading covered, and the pen proven innocent against a scan patched to always say ok.

guard amphora_roster
path tools/am/amphora_roster_witness.rish
tier lap
seated 20260907.223214

# elf_machine -- a binary's architecture read from its own ELF header rather than from `file`'s
# prose. Glow's two cross-target witnesses asked `file` for a substring and never checked that the
# call had run; this pier has no `file(1)`, so the call exited 127 with empty output and the assert
# fired saying the binary was not AArch64 -- of a binary that carried e_machine=183. Ten sites
# repaired, three left in the Android and APK ladders whose toolchains this pier cannot run.
# `tier lap`: 1s, after the census became one awk pass over ~2,900 runners rather than two
# processes per file, which had cost 17 of an elder 18.

guard elf_machine
path tools/e/elf_machine_witness.rish
tier lap
seated 20260906.040355

# rye_compile_reach -- which authored Rye does anything ever compile? %449 found `mantra/src/weave.rye`
# had not compiled since the toolchain moved, while four rostered guards read it every lap with
# `grep` and stayed green, and booked this census. It walks the corpus, resolves the 227 symlinks
# through `git ls-files -s` and `git cat-file` rather than a non-POSIX `readlink`, gathers every
# build root, and closes the `@import` graph. The GATED reading is %449's own shape: a tracked Rye
# file a runner makes a claim about while no build reaches it, ceiling 0. Files nothing compiles and
# nobody claims are reported rather than gated. That reading read 178 when the row was seated, and
# 114 of the 178 were programs `tools/p/parity_ch01.rish` compiles every parity pass -- it assembles
# their paths from a directory and a bare stem, so none is written anywhere and a census reading for
# filenames called them all unbuilt (%466). The census asks `rye_harness_roster_scan.sh --paths` for
# those now, crediting them only when the assembling script compiles anything at all: `never` reads
# **64** at `20260906.113000`, and `harness_unresolved` publishes what the asking still cannot reach.
# `tier lap`: seated at 5s over sixteen pens; **13s** measured `20260906.113000` over twenty-one.

guard rye_compile_reach
path tools/r/rye_compile_reach_witness.rish
tier lap
seated 20260906.045200

# rye_witness_walker -- a witness proves what it CALLS, and claims the module. Zig analyses lazily,
# so a witness importing a module and calling two of its functions forces exactly those two bodies
# through analysis and walks past every other declaration. Proven on metal `20260906`: a `pub fn`
# assigning a `u32` to a `[]const u8` was appended to `mantra/src/weave.rye` and
# `tools/m/mantra_weave_merge_witness.rish` read GREEN, its own five-break control included. That is
# %470's fault in the file next door and the instrument %463 asked for. A four-line comptime loop
# over `@typeInfo(mod).@"struct".decls` reading each back with `@field` forces every body through;
# the same plant reds once it is present. The GATED reading is a `*_witness.rye` naming its subject
# and not walking it -- 121 subjects, `walked` **2**, ceiling **119**, no slack. `pairs` (284) and
# `ambiguous` (9, a witness whose stem names nothing and which imports several siblings) are
# reported rather than gated, since guessing accuses a file of a promise nobody can show it made.
# The credit carries its condition: `unreached` counts walkers the compile-reach census places in
# `never`, gated at zero, and reads **-1** where that resolver cannot answer -- unknown published as
# unknown (%488's lesson one census over). Control: **37 behaviors**, the ceiling both ways by
# planting 120 pairs rather than by an override the scan does not offer. `tier lap`: **24s**.

guard rye_witness_walker
path tools/r/rye_witness_walker_witness.rish
tier lap
seated 20260906.131501

# amphora_bounds_agree -- the ninth, and the meter that finds unheard guards could not see it.
#
# WHAT IT PROVES. Amphora names the same bound in more than one roof, and this guard proves those
# copies agree. Three readings: same-name declarations across module roofs; value groups aliased
# under three names and two widths; and twelve declared `/// couples:` markers, where a bound in
# one file must equal a named partner in another -- `max_vessel_len` against `vessel_seal` and
# `vessel_core` at u32=8192, `max_resin_bytes` against `vessel_seal.max_seal_plain` at u32=1024,
# and `max_wire_payload` against main's chunk datagram at u32=340 beside u16=340, the width
# difference carried in a note rather than swallowed. Coupling is DECLARED rather than inferred,
# so a partner is named by a hand and never guessed from a matching number.
#
# Nothing else in this tree checks that a bound raised in one roof was raised in the other, and
# REDS %453 (`20260906.010047`) is what that costs: two Amphora bounds tied by hand at comptime,
# a lap after a raise to one of them refused 64 of 2,200 legal lengths with its own selftest
# still GREEN. This guard is the standing form of that repair.
#
# A FOURTH READING, and the class the first three could not state (REDS `20260906.193823`, `20260906.193823`).
# Every relation above compares for EQUALITY -- same name, same value, same declared partner -- and
# the relations that decide whether Amphora refuses a pour are ORDERS. Amphora carried two: its
# season door welcomed a 512-character name into a listing contract holding 200, and its manifest
# roof was declared `= max_cargo_bytes` while the manifest is the same catalog under a three-byte-
# longer prefix. Both refused honestly and both refused LATE, at a wall nothing had declared, and
# this guard read GREEN across the gap because no marker could express `>=`. `/// covers:
# <module>.<name>` says it now -- this bound must be at least that one -- printing src, dst, and a
# SIGNED slack so a reader sees the direction rather than a clamped zero (`%498`). One declaration
# stands today, `max_manifest_name_len` covering `max_season_name_len` at `slack=0`: the wall
# exactly at the door, which is the honest state and visible rather than assumed.
#
# PROVEN THREE WAYS, because a leg proven only in the passing direction cannot be told from a
# bypass. Two plants carry the order both ways -- `covers_hold` walks free, `covers_short` is bitten
# at `slack=-312`, which is Amphora's own elder gap rather than an invented number -- and the guard
# was then pointed at the LIVING tree with the elder 512 restored, where it answered
# `verdict=misread` and exit 1, and went green again when the repair returned. A plant proves the
# grammar; the living tree proves the subject.
#
# IT ALREADY CARRIES THE HALF A GREEN READING CANNOT SHOW. Six plants run before any total is
# believed -- same-name, alias and couples -- each proven to FAIL on a divergent plant and PASS
# on an agreeing one, so the refusal is proven from the failing side rather than assumed.
#
# `tier lap`, and the reading carries the pier's state rather than a bare number. Exit 0 on three
# consecutive runs at 1.755s, 1.782s and 1.780s on a quiet pier (`20260906.034025`), and at
# 3.153s, 2.869s and 2.687s at load average 10.2 with all eight ships opening a lap at once
# (`20260906.034409`) -- against full lap passes of 658 and 808 seconds measured the same two
# ways. A wall-clock figure on a shared pier is a reading of the pier as much as of the guard, so
# a lap that meets the slower number has met the expected one. Under two seconds quiet and under
# four loaded, this stays lap-tier under either sky. The affordable witness is the one that keeps
# being run.
#
# WHY IT STOOD HEARD BY NOTHING FOR 36 DAYS, booked as REDS (`20260906.034025`). It has been
# GREEN and cited in counsel since `20260801`, and `tools/fixtures/u/unheard_guard_scan.sh` never
# named it -- not because it was heard, but because that census draws its population by basename,
# `(witness|suite)`. This guard wears neither word, so it was never unheard there; it was ABSENT.
# 77 asserting runners stand in that blind spot, and the census's own header names the shape
# without ever measuring its size.

guard amphora_bounds_agree
path tools/am/amphora_bounds_agree.rish
tier lap
seated 20260906.034025
# harness_roster, seated 20260906.051706. tools/p/parity_ch01.rish builds and runs 116 Rye programs
# whose paths are written nowhere in this tree: it holds `let dir = "rye/tests"` and a list of 116
# bare stems, and reaches each as `${dir}/${s}.rye`. Two quiet consequences. A census that reads for
# filenames calls all 116 unbuilt -- one did this morning, and its count reached the operator card
# as a fact. And a program added to rye/tests/ and left out of the list is never run while the suite
# still reports GREEN, because the suite asserts over the list it holds rather than over the
# directory it names. The tree already keeps this rule for crypto/ and for module rosters; this
# carries it to the largest assembled site it has. It also ANSWERS that census now: `--paths` prints
# each harness path with the script that assembles it, so the two share one resolver rather than
# each learning to read a harness. That mode skips the build-site awk, which is very nearly the
# whole of the scan's wall time -- 3.8s against 0.5s, measured `20260906.113000`. `tier lap`: seated
# at 4s over 3,073 scripts; **11s** measured `20260906.113000` over twenty-seven control cases --
# one awk over batched files and a candidate grep before the per-file work, the cure %460 applied to
# the runner census, which took this from 35s.

guard harness_roster
path tools/r/rye_harness_roster_witness.rish
tier lap
seated 20260906.051706


# compass_rose -- the return habit, walked. This guard has stood in the tree since the compass was
# seated and stood on NO roster, so nothing ever read its green. Meanwhile its now/order check asked
# `test -f construction/TASKS.md && test -f construction/ROADMAP.md`, and both had been pointer stubs
# since 20260823.103804, when three cards fused into `construction/ITINERARY.md`. Presence resolved,
# the guard printed GREEN, and for fourteen days the rose sent readers to a redirect to learn the now
# (REDS %467). It reads the stations out of the rose document itself now and refuses a station that
# forwards, and `tools/fixtures/c/compass_station_control.sh` proves eighteen behaviors in a pen --
# both refusals and, as hard, the welcomes. `tier lap`: 4s, and it is the walk every ship is told to
# take when direction frays.

guard compass_rose
path tools/co/compass_rose.rish
tier lap
seated 20260906.055500

# standing_equipment -- THE ROSTER'S OWN GUARD, which stood on no roster (REDS %468). This file's
# header says how it is kept honest: `tools/s/standing_equipment_witness.rish` proves every path
# exists, refuses a run card naming a guard never seated, and refuses any recorded RED. No row ran
# it, so on 20260906 it went red and STAYED red -- the scan held `known_capabilities="ipv6"` by hand
# while a peer taught the runner a second probe, `jail_nesting`, and a whole-roster refusal sat
# unread through a cold pass that reported `run_verdict=ok`. The note under `comlink_nesting`
# already wrote the lesson: a rule that names a check and a roster that never runs it are one
# sentence disagreeing with itself. That sentence was at this roster's own front door. The scan
# derives the capability list off the runner now, so the drift cannot recur, and this row is what
# makes the next one audible. `tier lap`: 16s, most of it the control's pens over real repositories.
# GREEN on metal before the row was written, since rostering a red guard reds every cold pass in the
# fleet.

guard standing_equipment
path tools/s/standing_equipment_witness.rish
tier lap
seated 20260906.060000

# copy_lag, seated 20260906.070240. Zig admits an import only from the root file's own directory,
# so one implementation reaches two rooms here by symlink -- 220 of the 414 paths under a shared
# basename are mode 120000. Where a room keeps a REGULAR file under a name its siblings link, that
# file is the one path that can fall behind the implementation with no diff anyone reads, and the
# tree guarded exactly ONE such name: copy_sameness_scan.sh compares every tally_copy.rye against
# tally/copy.rye, canon spelled in the script. This carries the same rule to all 108 basenames held
# both ways, deriving each canon by resolving its own symlinks rather than by naming it. Gated:
# `behind` under a ceiling of 2 that only falls, and `split_canon` at zero. Reported: `sibling`,
# where a copy publishes something its namesake lacks, which is two modules sharing a word rather
# than one drifting. `tier lap`: 8s, one git ls-files and 5 comparisons.

guard copy_lag
path tools/co/copy_lag_witness.rish
tier lap
seated 20260906.070240

# tlb_reach -- a table is bounded twice, and only one bound appears in `@sizeOf`. The elder
# footprint study measured cost against bytes touched; a load's address is translated first, and
# that structure's capacity is counted in PAGES. Measured on this pier 20260906: 64 pages of span
# cost 1.65 ns per dependent read and 65 pages cost 3.90 -- same bytes, same instructions, same
# L1d sets, one more page -- and 2 MiB pages return the ratio to 1.00, which is how the cause is
# known. This rung proves the CENSUS rather than the reading: 38 planted behaviors, eight legs
# from the failing side, the clean baseline asserted before and after every plant, and each plant
# checked to have changed the reading it touched. The eighth leg, `elder_arm_ran`, gates the arm
# that reads the elder study's own three sizes under both page sizes -- gated for PRESENCE, since
# an arm that quietly did not run prints no attribution and reads exactly like one that ran and
# found nothing. The census itself stays UNROSTERED on purpose --
# 64 pages is a property of the board, so a guard asserting it would red the day the fleet moves
# part, and a guard that reds on ordinary work is one somebody turns off. The build leg is here
# because %449 and %463 measured what an uncompiled authored `.rye` costs. `tier lap`: 22s, of
# which 19 is that build.

guard tlb_reach
path tools/t/tlb_reach_witness.rish
tier lap
seated 20260906.061229
# amphora, the crossing -- the three modules the nine guards above never compile.
#
# WHAT WAS MEASURED (`20260906.063124`), and how. Every one of the 177 rostered paths standing that
# hour was read together with its own
# fixtures two levels deep (`awk '/^path /{print $2}'` over this file, then each guard's
# `tools/fixtures/...` references, then theirs). The eight amphora witnesses build exactly three
# modules -- `amphora/src/main.rye`, `vessel_core.rye`, `vessel_seal.rye` -- whose `@import`
# closure adds `manifest_entry.rye` and the two symlinked bounds files. `purchase_delivery.rye`,
# `vessel_fetch_wire.rye` and `vessel_fetch_delivery.rye` were compiled by NOTHING a lap runs:
# 1,160 lines, 32% of the 3,603 this lane owns.
#
# THE BLOCK ABOVE NAMED THIS GAP IN ITS OWN WORDS AND DID NOT CLOSE IT. It reads *a lap that
# broke a seal, a stamp, or the chunk assembler would have shipped green, because nothing was
# listening*, offering that as the state its eight rows cured -- and the chunk assembler is
# exactly these two wire modules, which `grep -l chunk amphora/*.rye` answers and no rostered
# guard compiled. Three comlink guards READ `vessel_fetch_wire.rye` as text for a bound tie
# (`tools/fixtures/c/comlink_carriage_scan.sh:68` binds it to `vessel=`) and none of the three
# builds it. That is REDS %449's shape one room over -- grep-reach standing in for compile-reach
# -- and in `mantra/` it had already bitten. Here it had not: all three build today, measured
# before these rows were written, so this is a coverage repair rather than a repair of rot.
# Booked as REDS %477 (`20260906.063124`). That row read `%466` while it sat unsent in a stash and
# `%472` while it sat in an unpushed commit; `xy` spent both in the meantime, and an unshared row
# renumbers -- the derived spine's own law, twice in one lap.
#
# `rye_compile_reach` READS ALL THREE AS `built`, and is right to. It asks whether ANY runner in
# the tree compiles a module; this roster asks whether a RUN one does. Its `built=1535` therefore
# counts runners nobody runs -- REDS %360's population seen from the other side -- so a green
# reading there settles the compile question and leaves the coverage question open. Two meters,
# two questions, and the second is the one that decides whether a lap would catch a break.
#
# THE HEADLINE ABOVE IS CORRECTED IN PLACE. *10 tracked Rye modules and 3,861 lines* counts three
# paths this lane does not own: `kumara.rye` and `tally_copy.rye` are symlinks into `tally/`, and
# `wire_format.rye` into `comlink/`. Seven modules and 3,603 lines is the lane's own figure. REDS
# %463's *resolution before accusation*, arriving in a roster comment rather than in a census.
#
# THE OTHER FIVE RUNNERS ARE MEASURED AND DECLINED, each by name rather than by omission.
# `amphora_lap1`, `amphora_lap2`, `amphora_vessel_seal` and `amphora_vessel_stamp` build only
# modules the nine above already compile, so they would add claims rather than reach, and the
# family already proves pour, carry, restore and three tamper refusals. `amphora_device_wire`
# drives `comlink/run_vessel_fetch_wire_lab.sh` under virtio and this pier holds no qemu. It
# REFUSES HONESTLY -- exit 1, and the one GREEN in its output is the substring an assert was
# looking for rather than a claim it made -- which is the good half of REDS %460, whose open
# question is whether such a witness may read GREEN with a named gap. Rostering it here would put
# an environment fact on a lap clock.
#
# `tier lap`, with the pier's state beside the number. GREEN at 3s, 3s and 2s -- 8 seconds
# together -- at load average 8.3 with the emitted binaries deleted first, so each rebuilt from
# source against a warm Zig cache (`20260906.063124`), against a full lap pass of 704 seconds
# measured the same hour. Re-measured warm at 2s, 2s and 1s three times each (`20260906.093709`)
# against a pass of 840. All twelve amphora rows together now cost roughly 16 of those 840.
#
# AND THE RUNNING FOUND WHAT THE READING COULD NOT. These three were rostered on a measurement of
# COVERAGE -- which modules a lap compiles -- and the first lap to actually run them repeatedly
# found `amphora/vessel_fetch_delivery.rye` stalling forever, one run in six to eight, alone on a
# quiet pier. Both halves of the module's socket habit were races: it opened, bound, received and
# closed a socket per datagram, so the receiver was unbound in every gap, and a UDP datagram
# arriving at an unbound port is discarded with no trace on either side; and `recvfrom` carried no
# `SO_RCVTIMEO`, so the receive that followed waited forever. The elder form did red -- at the
# selftest, naming a selftest that was fine -- because a failed `rye build` overwrites the emitted
# binary with a 5,547-byte artifact, which the warm-binary fallback beside it could never have
# survived. Measured, elder against repaired, same pier and same hour at load 8.5: 2 stalls in 20
# against 0 in 40. REDS %478 (`20260906.093709`).
#
# THREE THINGS STAND BEHIND THESE ROWS NOW, and they are different instruments. The module binds
# before it sends and holds one socket across an exchange, with every receive bounded and named --
# that is the repair. `tools/fixtures/a/amphora_vessel_port_lock.sh` holds UDP 38494/38495 at a
# HOST path around every step that speaks them, because a port belongs to the machine and eight
# trees reach for one pair -- that is what keeps two lawful runs from meeting. And the fixtures
# bound their two long-lived processes with `timeout`, so whatever slips past both reds in a minute
# rather than stopping the pass. Proven from both sides: two concurrent `chunkdemo` runs abort at
# 134 in 31 seconds unlocked, and pass in 1 to 2 seconds through the lock, three trials each.
#
# THE REFUSAL REACHES THE READER THROUGH THE SHIM. `tools/am/amphora_*.rish` are accrete shims onto
# `tools/gen/amphora/`, and they forwarded `r.out` alone -- so a planted break made the direct run
# say *no longer compiles* and the roster's own path say nothing at all, filing an evidence page
# naming the guard and no reason. The exit code always travelled; the sentence did not. These three
# forward `r.err` now, and the other five took the same two lines on `20260906.124500` -- all nine
# amphora shims carry their target's reason, and `shim_reason` below holds the habit for the tree.

guard amphora_lap3
path tools/am/amphora_lap3.rish
tier lap
seated 20260906.063124

guard amphora_resin_chunk
path tools/am/amphora_resin_chunk.rish
tier lap
seated 20260906.063124

guard amphora_purchase_delivery
path tools/am/amphora_purchase_delivery.rish
tier lap
seated 20260906.063124

# amphora_asker_reply -- a source answers the asker, and the kernel names the asker's port.
#
# WHAT WAS WRONG. `amphora/vessel_fetch_delivery.rye` held two UDP port numbers in its own source:
# `fetcher_port` 38494 and `source_port` 38495. The source sent every answer to the first, and every
# asker bound that same number, so the pair agreed with itself and nothing it did alone could tell
# *answers the asker* from *answers 38494*. Eight lines above the constants the same file says THE
# HOST NUMBERS THESE, NEVER THIS FILE about `SOL_SOCKET`, and eight lines above that THE KERNEL LAYS
# THIS OUT, NEVER THIS FILE about `sockaddr_in` -- both taken back from the source and handed to the
# host after REDS %282. The port stood between them, unread. REDS %485 (`20260906.113014`).
#
# WHAT IS TRUE NOW. `recv_on` hands the caller the address `recvfrom` already filled in and the
# elder code discarded; `send_resin_response` answers that address; `fetch_one` binds
# `ephemeral_port` -- zero, the ask rather than the port -- and the kernel returns a free one
# atomically, which no probe-then-bind can do. `bound_port` reads it back with `getsockname` and the
# asker prints it. `fetcher_port` is gone. `source_port` stays, because a service answers on a
# number its askers can name.
#
# THE SECOND FAULT, FOUND BY THE FIRST REPAIR REFUSING. Aiming the answer at the asker made the demo
# fail at once: `send_wire` opened a fresh socket per send, so a request's return address named an
# ephemeral port the asker was not listening on and closed a moment later. The source answered
# honestly and answered nobody. An exchange speaks and listens through ONE fd now -- `send_from`
# takes the caller's socket, and `send_wire`, `send_to` and `recv_wire` are gone rather than left
# uncalled.
#
# WHAT THE FIVE LEGS PROVE, and the leg that corrected the round's own expectation. The control
# plants both elder faults into a pen copy by transform rather than by git ref, so it survives a
# history rewrite. PORT plants the written reply number on both ends; SPLIT plants the two-socket
# asker. Crossed against the repair: a PORT source cannot answer a repaired asker, and a repaired
# source cannot answer a SPLIT asker -- both refusals bitten. A repaired source DOES answer a PORT
# asker, which the first draft expected to refuse and measured otherwise: a fixed-port asker that
# speaks and listens through one socket is still answerable, so the repair is a superset of the
# elder rather than merely different. Each planted pair talking to itself is answered, which is what
# proves the pen, the pour, and the fixture innocent.
#
# WHAT WAS TRIED AND DISCARDED. Two askers at once against one source was the obvious control and it
# discriminates nothing -- both binaries passed it in half a second, because two short exchanges
# stagger by milliseconds and never overlap. It stands as a claim on the repaired bytes, where it
# can still red, and never as the control. A test both bytes pass proves only that the test ran.
#
# `tier cadence`, and the reason is the bound rather than the cost. Two crossed legs each spend the
# module's own three attempts of ten seconds, because what they establish is that nothing arrives,
# and nothing takes exactly as long to prove as the bound says it does. Measured `20260906.113014`
# on this pier: 68s for the whole guard, of which 62 are those two legs and two plant builds, against
# a full lap pass of 840s. The three lap-tier amphora rows already build and run this module every
# lap; this one asks the question they cannot, and it changes only when the module does.
#
# WHAT THIS DOES NOT REACH. The port lock stands. `source_port` is still one number the machine owns
# and eight trees reach for, so `tools/fixtures/a/amphora_vessel_port_lock.sh` still keeps two lawful
# runs from meeting. Halving the shared resource is not removing it; the source's own port discovery
# is the booked lap after this one.

guard amphora_asker_reply
path tools/am/amphora_asker_reply.rish
tier cadence
seated 20260906.113014
# signal_trap -- a POSIX trap handler runs and then RESUMES the script, so
# `trap 'rm -rf "$pen"' EXIT INT TERM` reads as "clean up and stop" and behaves as "clean up and
# carry on". Measured at `15f99e1fe0` before this round's repair: 143 command-position traps
# naming INT or TERM across tracked shell sources, and exactly ONE called exit -- because
# `shell_portable.sh`'s own header taught the broken form beside `lock_acquire`, so the shape had
# one source and spread from it. GATED at `lock_releasing 0`: no handler may free a checkout's
# mutual exclusion and let the script run on, which is the severe subset because another writer
# then enters a tree that believes it is alone. `files_without_set_e` (48) is REPORTED rather than
# gated -- those run to completion and publish counts computed from a deleted directory, observed
# on two of this tree's own fixtures at exit 0 with every count collapsed to zero. Both were
# caught, because the witness above them asserts on the counts that collapsed; a witness asserting
# only a ceiling would not be, since a ceiling claim reads MORE true when the evidence is gone.
# Study: external-research/20260906-102443_the-severity-lives-in-the-witness.md. `tier lap`: 6s.

guard signal_trap
path tools/s/signal_trap_witness.rish
tier lap
seated 20260906.120137

# fold_shelf_link -- a link that kept the depth of the file it was folded out of (REDS %491, closing
# %474's booked loom). The card sits in `construction/`, so a link it writes as `](archive/X)`,
# `](../Y)`, or a bare `](Z)` is correct there and broken the moment that text is folded onto
# `construction/archive/`.
# FOUR link guards read this tree and none reaches it, three of them for one shared reason: a fold
# shelf's basename carries a one-clock stamp, stamped means testimony, and testimony is where
# `tracked_link` reads past and `readme_reach` stops gating. The fold's own output wears the exact
# property that makes every guard let it go. Gating testimony is lawful here and nowhere else,
# because a depth-lost link was never correct in the file it sits in -- the fold broke it in the
# same act that created the file -- and it is told from a stale reference mechanically: it resolves
# under exactly ONE card-to-shelf correction where a stale one resolves under neither. Ambiguity is
# impossible by construction: `archive/`, `../`, and neither-of-those partition every relative
# target, and an absolute path takes no arm. That third arm was MISSING until `20260908.072125`,
# when the gate was found reading zero over nine bare siblings sitting in its own reported column --
# the correction block had enumerated the two spellings a hand had just repaired rather than
# deriving them from the rule those two spell. 5s on metal; `lap`
# rather than `cadence` because the live front folds by hand on nearly every lap, and this fault
# fired three times in one lap under the hand that had just written it down.

guard fold_shelf_link
path tools/f/fold_shelf_link_witness.rish
tier lap
seated 20260906.120905

# shim_reason -- a pass-through shim hands on its target's reason, not only its verdict.
#
# WHAT WAS WRONG, measured on a real file with no plant. `tools/am/*.rish` and their kin are accrete
# shims: each runs a target under `tools/gen/` and exits that target's code, so either path may be
# named and the answer is the same. Rishi's `run` captures the target's stderr into `r.err`, and a
# shim that says only `r.out` writes nothing to its own stderr. `amphora_device_wire` refuses
# honestly on this pier -- it drives a virtio lab and there is no qemu here -- so the two readings
# stand side by side without breaking anything: the target run directly says `rishi: assertion
# failed -- Amphora vessel fetch device wire lab failed`, with the line number; the shim run the same
# second exits 1 with ZERO bytes on stderr, and all a reader receives is `amphora-device-wire:
# virtio fetch-by-digest lab...`, a progress line printed BEFORE the failure, which reads like
# success. This runner captures a guard with `> out 2>&1`, so the merge was already there and caught
# nothing: the shim emitted nothing to merge.
#
# A GATE AND A RATCHET, because this is a trap rather than a live fault. Zero swallowing shims stand
# on this roster, so nothing is lost today; it springs on the lap that ROSTERS one, and %360's
# standing pressure is to roster more. The amphora lane walked into it on `20260906.063124` -- three
# shims onto a lap clock, the swallow found by planting a break and repaired in the same breath. The
# gate is what spares the next lane that discovery. The ratchet is the residue off the roster, and it
# only falls; the scan's own `unrostered_ceiling` line carries the number so no page can drift from
# it. Nine of 52 shims forward the reason today, and five of those nine were repaired in this lap.
#
# THE GATE IS PROVEN ABLE TO BITE, which matters more than usual here: `swallow_rostered` reads zero
# on this field and always has, so nothing in the tree could show it working. The pen supplies the
# case -- one swallowing shim named by a `path` row, refused; the same shim with one line added,
# freed. The ceiling is proven from both sides on ONE plant, two unrostered swallowers read at a
# ceiling of two and then of one.
#
# THREE MARKS, TOGETHER. A shim is the `run ["rishi/bin/rishi" "run" ...]` call, the `say r.out`
# forward, and the `exit r.code` tail, all three -- each alone is ordinary, since witnesses run
# witnesses and plenty of scripts say a captured stdout. A seat is a `path` row at column zero,
# never a path mentioned in a roster comment; unanchored, a guard could count as rostered because
# somebody wrote about it, and the pen plants that sentence. Two tracked files exit a captured run's
# code under another variable name and are invisible to the reading; neither is a shim, and both are
# named every pass as `exit_alias_sites`, so the floor carries a number (%466).
#
# `tier lap`, and cheap because the corpus arrives through one `git grep` whose status is classified
# rather than swallowed (%473, %484). Three greps over 2,362 tracked `.rish` files cost 22 seconds
# measured on this pier at load 10.1; the pre-filter reads the identical counts in 2, and the whole
# guard including its 40-case pen runs in 5 against a full lap pass of 729.
#
# A SECOND SHAPE OF THE SAME LOSS, seated `20260906.233111` -- and found by a red rather than by a
# search. `fleet_watch` reddened a cold pass, filed seven lines that could not name a behavior, then
# passed 16 of 16 on six consecutive re-runs: the one reading that could have diagnosed the flake
# was the reading its witness threw away. `tools/fixtures/f/fleet_watch_control.sh:24` prints
# `FAIL <behavior> -- wanted X, got Y` to STDERR so a passing run stays quiet, and
# `tools/f/fleet_watch_witness.rish:17` interpolated `${ctl.out}` and never `${ctl.err}`. The exit
# code travelled, `pass=15 fail=1` travelled, and the clause naming WHICH behavior did not.
#
# Measured the same hour: EIGHT controls print FAIL to stderr, FIVE are run by a witness, and all
# five of those witnesses stood on this roster interpolating `.out` alone -- `fleet_watch`,
# `fleet_lap_verdict`, `publish_template`, `unshared_citation`, `sealed_digest`. All five carry
# `${ctl.err}` now, and the repair was proven on metal by planting one wrong expected value in
# `sealed_digest_control.sh`: the evidence went from `pass=8 fail=1` alone to that line plus
# `FAIL an unread seal is counted -- wanted PLANTED_WRONG, got yes`.
#
# THE THREE-MARK READING ABOVE COULD NOT SEE IT. That reading requires `say r.out` and
# `exit r.code`, which is a pass-through shim; these are witnesses that assert. One law -- hand on
# your target's reason -- and two shapes, so the instrument grew a second reading rather than the
# tree growing a second instrument. `reason_lost_rostered` is GATED at zero, `reason_lost_unrostered`
# is a ratchet opening at zero, and `stderr_controls` names the population so a zero is visible
# rather than assumed (%463).
#
# TWO REFUSALS IT DELIBERATELY DOES NOT MAKE, each proven in the pen. A control keeping its FAIL
# line on STDOUT is not in the population at all -- its reason already travels through whatever the
# witness interpolates, and counting it would red a witness that is already correct. And a witness
# forwarding some OTHER run's stderr is not credited for the control's: the reading takes the
# variable the control's `run` is bound to and reads only that variable's spellings, because the
# blindest version of this check is *does the file mention `.err` anywhere*.
#
# AND THE READING CAUGHT ITSELF, which is the case worth keeping. The control's pen helper WRITES a
# stderr-reporting control, so its own source carries that line inside single quotes -- and a first
# pattern read 9 where the tree holds 8, counting the instrument as one of its own subjects. The
# population is anchored now: a real call stands after start-of-line or whitespace, a quoted mention
# stands after the quote that opens its string, and a comment is dropped in the same pass. Both
# spellings of a mention are planted, and the quoted plant reproduces the real continuation-line
# shape byte for byte rather than something near it.
#
# Twenty-one new pen readings, six new repositories, every refusal planted and then lifted: 61
# cases over 14 repositories, and the guard runs in 8 seconds measured `20260907.000030` against a
# full lap pass of 983.

guard shim_reason
path tools/s/shim_reason_witness.rish
tier lap
seated 20260906.124500
# rye_struct_fields -- the reader four guards needed and none of them had (REDS %500). The four
# Mantra Glow Tend limbs each carry a placard declaring that a structure "carries exactly N
# fields", and each proved it with N `grep -Fq` calls for N field lines. Presence is not a count.
# A fourth field passed all four; a reorder passed all four; and all four refused in the words
# "missing or reordered". Two named movements of the Mantra arc -- the identity gap, wanting a
# fourth field on `Line`, and the diff anchor, wanting one on `Diff` -- were recorded on the
# operator card and in a commit body as waiting on that pin, which had no bolt in it. This rung
# proves the reader that replaced it: a count and an order out of the Rye source, three refusals
# by name, and TWO phases that run the elder predicate verbatim on the same mutated pen and watch
# it wave a fourth field and a reorder through. Those two passes are the finding itself. 19
# behaviors; `tier lap` at 1s, because the four consumers it feeds all run every lap.

guard rye_struct_fields
path tools/r/rye_struct_fields_witness.rish
tier lap
seated 20260906.140000
# The diameter floor, seated 20260906.143000. `comlink/topology.rye` publishes hop counts, and
# `topology_graph_census.sh` now reports the smallest diameter any graph of that many points and
# that maximum degree could hold -- the Moore bound solved for the DIAMETER rather than for the
# node count, which is the direction a designer's constraint actually binds. The reading exists
# because a paper asked the same bound the other way, answered nine billion nodes against our 720,
# and concluded a diameter argument decided nothing.
#
# WHY `cadence` RATHER THAN `lap`. The control runs the census seven times over 682,425 ordered
# pairs -- 151s measured, against 26s for one census pass. Its answer moves only when the census
# or the module moves, both deliberate acts, so a fifth-round reading is honest where a per-lap
# one would spend 151s a lap on eight ships watching a number that did not change.

guard topology_floor
path tools/t/topology_floor_witness.rish
tier cadence
seated 20260906.143000

# reds_citation -- a number used as a link is a promise about where the link goes (REDS row of
# `20260906.143706`). A row is cited two ways at once, as a number a reader recognises and as a path
# a reader clicks, and the two halves move at different moments: the number when the derived spine
# renumbers an unshared row, the path when a row folds onto a shelf. Nothing read them together.
# Four citations went out in one day naming one row and linking another, and each repair fixed the
# half the eye was on -- the number moved and the path stayed, then the path moved and the number
# stayed.
#
# EXISTENCE AND AGREEMENT ARE TWO QUESTIONS, and only one had an instrument. Measured on the tree
# that morning, `readme_reach` named ONE of the four -- the citation whose file was absent -- and
# read green over the other three, which open a real shelf holding somebody else's row. That is a
# misdirection with no expiry: it resolves, so it never reds, and a reader following it lands
# somewhere the sentence never meant. `tracked_link` asks existence the same way; `reds_row_present`
# asks whether the ledger HOLDS a row and never asks who cites it.
#
# TWO FORMS, AND THE THIRTY-TWO THAT TAUGHT THE NARROWING. A window around a link picks up every
# `%N` in the neighbouring prose, and a sentence may lawfully name one row while linking another --
# %220's own shelf cites %218's. A nearest-preceding-%N reading over 1,001 living documents called
# 32 honest sentences wrong, so the scan reads only the two shapes that are promises to a reader:
# the anchor text that IS the number, and the shelf word whose sentence just named a row. One line
# of lookback, never past the link, because this tree hard-wraps its pins and the break that shipped
# fell exactly across that wrap.
#
# PROVEN ON HISTORY AS WELL AS IN A PEN. Run against `b441f97b2`, this tree as it stood when the
# defect was published, the scan reads `numbered_disagree=3` and names all three sites. The pen adds
# 23 cases over 12 real repositories, every refusal planted and then lifted, and it caught a defect
# in the scan's own counting before it shipped -- `grep -c ''` prints 0 and exits 1 on an empty file,
# so the fallback beside it yielded two lines and the emptiness branch was skipped entirely.
#
# `tier lap`: one `git grep` over tracked markdown then awk over the two dozen documents that cite a
# shelf, about a second. Every fold moves a path and every renumbering moves a number, so ordinary
# work is what writes this fault, and a cadence clock would let one ride four laps.

guard reds_citation
path tools/r/reds_citation_witness.rish
tier lap
seated 20260906.143706
# reds_shelf_name -- a fold shelf's own name is a promise about what it holds (REDS row of
# `20260907.151832`). A folded row spells its number FOUR times: in the shelf's filename, in its H1
# title, in a `**Rows:**` header, and in the row headline. `reds_spine_derive` is the only
# instrument in this tree that reads a row's number at all, and it reads the headline alone -- so a
# renumber sweep reaching three of the four leaves a shelf disagreeing with itself, and the two
# spellings it leaves behind are the filename, which 291 living citations open by path, and the
# title, which is the first line a reader sees.
#
# EXISTENCE IS NOT AGREEMENT, and this is that pair one room further in than `reds_citation`. That
# guard reads a citation's number against the shelf it links, from OUTSIDE the shelf; `readme_reach`
# and `tracked_link` ask whether a path opens, and a shelf whose title names the wrong row opens
# perfectly. Nothing read a shelf against itself. Measured `20260907.151832`: one shelf of 291
# disagreed, and it was this ship's own row from the lap before, renumbered `%568 -> %566 -> %572`
# on the anointed spine while its title kept `%568`.
#
# THE READING IS NARROW ON PURPOSE, and its three welcomes are proven as hard as its refusals. A
# title may name a row as its SUBJECT mid-sentence (*the %364 collision*); a `**Rows:**` header may
# recite a renumber history after its declaration; a filename span may be WIDER than the rows held,
# because a range is not a set and the endpoints folded elsewhere. All three stand in this archive
# today, and a looser reading calls every one of them wrong.
#
# `tier lap` because the derived spine renumbers unshared rows as routine work, so the fault is
# written by ordinary laps rather than by a deliberate act -- and the scan costs four seconds over
# `git ls-files` and 291 small files.
guard reds_shelf_name
path tools/r/reds_shelf_name_witness.rish
tier lap
seated 20260907.151832
# topology_attained -- what diameter a builder can actually STAND on, at 720 points.
# Its sibling `topology_floor` binds the MOORE floor, a lower bound over all graphs that almost
# nothing attains. This one binds what is REACHABLE: a second floor for abelian Cayley graphs
# derived from the lattice ball and checked against enumeration at every radius, six graphs built
# here and walked from all 720 vertices, and a sweep of 63,903 circulants. `tier cadence` because
# the scan walks 3,106,080 ordered pairs and the control runs eight plants -- 192s measured
# `20260906.152821` -- and its answer moves only when the scan moves, a deliberate act.

guard topology_attained
path tools/t/topology_attained_witness.rish
tier cadence
seated 20260906.152821

# amphora_udp_reuseaddr -- the option one line above the port %485 took back, carried for a reason
# UDP does not have.
#
# WHAT WAS WRONG. `amphora/vessel_fetch_delivery.rye` opened every UDP socket under
# `setsockopt(SO_REUSEADDR)`, the invariant reading *SO_REUSEADDR is what lets a bounded run rebind
# its own port*. That is TCP: a closed TCP socket holds its port in TIME_WAIT and the option binds
# through it. UDP has no TIME_WAIT, so the option bought nothing it was added for -- and what it
# bought instead was a DUPLICATE BIND, two sockets holding one port while the kernel splits the
# datagrams between them with no error either side. The per-tree lock beside it was built to catch
# the collision this option had made silent. REDS `20260906.154105`.
#
# WHY IT GATES SOURCE AND PROBES KERNEL. The source reading (`source_reuseaddr_sites`, zero) is the
# regression guard: it is what keeps a plausible comment from walking the option back in. The
# kernel probe is what makes the repair a measurement -- four binds of one port answering, in
# order, that the stated reason needed nothing, that the collision is nameable without the option,
# that the option is what silenced it, and that a mixed pair still refuses. The probe asks the
# kernel for a FREE port rather than naming one, because a hardcoded probe port is this row's own
# fault one level up, and it never touches Amphora's 38494/38495, so no peer's live exchange is
# disturbed. Where `python3` is absent the probe reports itself absent by name and the source gate
# stands alone -- a guard that cannot run its instrument must not describe its subject (%460).
#
# THE THIRD READING IS A CENSUS AND NEVER GATES. `open_socket` was copied module to module with its
# comment intact, so 19 further modules carry the same call -- 1 comlink, 1 granary, 10 linengrow,
# 7 mantra, every one of them SOCK_DGRAM and not one SOCK_STREAM, counted again on this tree at
# `20260906.200000`. Those files belong to other seats, and a guard that reds Amphora over a module
# in Mantra is a guard the fleet turns off, so the count is printed and the verdict ignores it.
#
# `tier cadence`, AND THE TIER IS THE ONE THING HERE NOBODY HAD MEASURED. This block read
# `tier lap`: 1s, no build until `20260906.200000`, while the row it belongs to was parked. Timed
# on this pier: the scan alone **16.8s**, the control **1m57s** over 32 pens, the witness **2m8s**
# together. All three claims in that sentence were wrong -- it is not 1s, it does build (each pen
# is a tree the scan is run against), and at `tier lap` across eight ships it would spend roughly
# seventeen pier-minutes a fleet lap against a whole-roster average near 7s. So it takes the clock
# `topology_attained` takes for the same reason at 192s. What the cadence costs is honest and
# small: the gate catches a walk-back within five rounds rather than one, on a line no lane is
# currently editing. **The elder claim is this row's own subject one level up** -- a number carried
# for a plausible reason that nobody measured -- which is why it is corrected here with three
# timings rather than quietly restated.
#
# ITS OWN NEXT LAP, named rather than taken: the scan's 16.8s is a whole-tree `find` over every
# `.rye`, and the control pays it again on the legs that read the living tree. `git ls-files` and
# one grep pass would cut it; that is a change to the instrument, and this lap's keystone was
# getting the instrument out of the box at all.

guard amphora_udp_reuseaddr
path tools/am/amphora_udp_reuseaddr_witness.rish
tier cadence
seated 20260906.154105

# amphora_mark_wreck -- the vessel named the wreck rule for eight weeks and kept it nowhere.
#
# WHAT WAS WRONG (`20260907.015042`). `foundations/20260703-202312_the-marked-value.md` vows that
# an unknown mark is refused whole -- *there is no partial trust of cargo whose kind cannot be
# named* -- and hardcodes exactly two roots, the plain-bytes mark and the manifest mark. Pond, one
# of the two sites that page names, kept the vow: `pond/customs.rye` asserts
# `verdict_for_mark("stranger-mark") == .refuse`. Amphora, the OTHER site -- the crossing vessel
# whose shoulder line is the whole image the page is built on -- carried the mark field and checked
# it nowhere. `parse_manifest_line` validated the mark's SHAPE (nonempty, at or under
# `max_mark_len`) and never its IDENTITY, and every reading site threw the parsed entry away:
# `vessel_core.rye:160` and `:167`, `vessel_seal.rye:115` and `:229` each wrote
# `_ = me.parse_manifest_line(...) catch return error.InvalidVessel`, keeping the refusal and
# discarding the mark, while `vessel_fetch_delivery.rye` -- the far side of a UDP fetch, which is
# *receipt* in the vow's own word -- stepped over the token with `_ = parts.next(); // mark`
# without parsing at all. Probed on metal: `plain-bytes`, `manifest`, `zzz-unknown-stranger`, and
# `executable` were all ACCEPTED. The module's own header line claimed `wreck rule` and its
# selftest printed `refuse whole`.
#
# WHY NO GUARD SAW IT. The eight elder amphora witnesses plant six tampers -- an absent season, a
# tampered `seal_cargo`, a truncated vessel, a forged resin body, a torn chunk, a reordered pair --
# and every one is an INTEGRITY plant. Not one plants a TYPE. The mark was the only field of the
# three-field line with no negative case anywhere in the room.
#
# WHY THE REFUSAL HAD TO BE READ BY REASON. Editing a sealed vessel breaks its Kumara signature, so
# a planted stranger refuses either way and the exit code proves nothing about marks. Restoring the
# elder behavior, the planted vessel still refuses -- as `error: VerifyFailed`, the signature, with
# `parse_manifest_line` nowhere in the trace. So a correctly sealed vessel carrying `executable`
# rode straight through, and only a tampered one ever refused. This witness asserts the FRAME:
# `parse_manifest_line` must be what turns it away, which is the one assertion the elder tree fails.
#
# WHAT IT COST TO FIX: nothing real. Measured before the gate went in -- exactly one mark rides in
# this whole tree, `plain-bytes`, six occurrences -- so the two named roots break no vessel that
# exists. The check sits in the parser rather than at five call sites, because a rule written five
# times is a rule four places can forget.
#
# THE CENSUS IN THAT PARAGRAPH WAS SHORT BY TWO, counted `20260908` rather than recalled. Six
# callers route a whole line through the parser, not five: `take_listed_line` at
# `vessel_core.rye:234` is a third site in that roof and was named in neither this note nor the
# contract's own comment. And a SEVENTH reader never reached the parser at all --
# `amphora/src/main.rye:append_cargo_line`, which walks decrypted seal plain into a catalog slot
# and so cannot route a whole line. It carried its own tokenizer and its own mark rule,
# `eql(mark, "plain-bytes")`, NARROWER than the two roots every sibling welcomes: a vessel
# carrying the `manifest` root would have passed six readers and died here as `CargoUnproven`.
#
# WHY IT WAS UNREACHABLE, which is why it stood. That door writes what it reads -- `pour` builds
# the cargo plain with its own `mark_plain_bytes`, seals it, and `restore` opens the same plain
# back -- so no argument, file, or vessel edit puts a foreign mark into the plain without
# breaking the AEAD seal first. A leg that cannot be reached is proven where it lives: the
# reader borrows `mark_verdict` and bounds the mark field ahead of it, so an over-long mark now
# REFUSES rather than tripping that function's own precondition assert, and the written root is
# held against the contract's at comptime. A `couples:` marker was tried first and refused --
# `amphora_bounds_agree` reads a bound's spelled NUMERIC value and answered `marker_on_derived`
# on a string, which is the right refusal and pointed at the better home: a marker is checked
# when a guard runs, a comptime assert every time the door is built. Three legs, each proven to
# BITE -- the elder narrower rule restored in a source pen, the bound line deleted, and a
# divergent literal planted, whose build refusal is asserted to name the comptime tie rather
# than merely to be non-zero. REDS `20260908.004856`. GREEN at 7.4s; still `tier lap`.
guard amphora_mark_wreck
path tools/am/amphora_mark_wreck_witness.rish
tier lap
seated 20260907.015042

# topology_routing -- what a node HOLDS and COMPUTES to pick the next hop, at 720 points.
# Its sibling `topology_attained` binds the WALK -- how many hops a shape takes. This binds the
# DECISION: the exact shared next-hop table (719 entries, 3 bits each, 270 bytes, the same 270 at
# every node), what a table-free local rule costs instead, and whether more lookahead recovers the
# difference. Coordinate subtraction and permutation sorting are exact and free; the circulant's
# ring-gap rule reaches 609 of 719 and three depths of lookahead move nothing. `tier cadence`
# because the scan walks 517,680 ordered pairs three times over and the control runs ten plants --
# 52s for the scan, measured `20260906.195719` -- and its answer moves only when the scan moves.

guard topology_routing
path tools/t/topology_routing_witness.rish
tier cadence
seated 20260906.195719
# TWO-ROOMS DOORWAY -- seated 20260705, rostered 20260906, and RED for the whole gap between.
#
# `context/TWO_ROOMS.md` asks every forward-facing page to name its register in its own Status
# line, and this rung reads that token. It stood on no roster, so nothing ran it: it was RED at
# 45 pages when a hand finally did. That is REDS %360's family -- a refusal nobody receives --
# with an actual refusal waiting behind it.
#
# It was also blind. Its roster was a shell glob over three rooms; `active-designing/` folded to
# `date/YYYYMMDD/` and a shell `*` stops at `/`, so 607 pages left the subject with nothing to
# say so. A broken path reds; a narrowed glob goes green. It reads `git ls-files` now, whose
# pathspec `*` crosses `/`, and the scan floors its own reach on a folded page so the same
# narrowing refuses rather than reporting a smaller clean tree.
#
# `tier cadence`: 972 pages, one subprocess each, 28s on this pier -- and eight ships read the
# roster twice a lap on four cores, so a lap clock would spend 448 contended seconds a round on a
# reading that moves when a room folds. That is `dated_path`'s reasoning verbatim, one room over.
guard two_rooms_doorway
path tools/t/two_rooms_doorway.rish
tier cadence
seated 20260906.161500
# TWO-ROOMS DOORWAY, AT THE MOMENT THE PAGE IS WRITTEN -- seated 20260907.092522.
#
# The rung above is whole and slow: 978 pages, one subprocess each, 28s, `tier cadence`. So it
# speaks on the fifth round, hours after a page was written, to a hand who has moved on. On
# 20260907 FOUR pages landed in one day whose Status named `proposed` and no room, three of them
# AFTER the canon settled that question (TWO_ROOMS.md, 20260907.015907) and the room's own front
# door taught the token the same morning. The ratchet went 44 to 48 in a day and a hand swept it
# back by reading four sentences already on the pages. The repair rate matched the writing rate,
# which is a lantern rather than a loom.
#
# This is the loom, and it gates a NARROWER class on purpose: only a page the commit ADDS. A new
# page has no accreted testimony to protect and exactly one person who knows its register -- the
# hand writing it, right now -- so the ratchet's RISE is held at zero while its FALL stays with
# the rung above. A page the commit MODIFIES is reported and walks free, because 44 elder pages
# name no room and three are dated testimony; refusing an unrelated commit for touching one would
# be a wall somebody turns off.
#
# `tier lap`: 2s, and it reads one `git diff --cached` plus the doorway roster. It is the cheap
# gate that a lap adding one page must actually run -- COPAL's %550 sentence, answered here.
guard two_rooms_doorway_touch
path tools/t/two_rooms_doorway_touch_witness.rish
tier lap
seated 20260907.092522
# TAME'S 70-LINE LEDGER -- the instrument behind the tidy ratchet could not see its own subject.
#
# `tools/fixtures/t/tame_style_long_fn_one.sh` ended a function at the first line matching `^}$`
# OR `^    }$`. The second alternative fits a struct method, whose body closes at four spaces --
# applied to a free function it matches that function's FIRST `if` block, which closes at four
# spaces too. Its start pattern matched any INDENTED `fn `, so an inline comparator restarted the
# count inside its parent. Both fail OPEN: over 611 authored files it reported 15 functions past
# 70 lines where 321 stand, its longest reading 180 against a true 809.
#
# The witness gates the INSTRUMENT, never the count: the control's twenty planted legs, the two
# elder patterns' absence from the code, and a floor of one on the tree's own reading -- a ledger
# answering zero across 611 files has gone blind, and blindness prints what health prints. The
# count itself stays a ratchet that prints, as TAME's own tidy rules seat this family, since 321
# standing functions belong to six lanes and a ceiling would refuse other ships' ordinary work.
#
# `tier cadence`: 611 files, one awk each, 19s under fleet load -- and the number it reads moves
# when a hand splits a function, which is a deliberate act rather than a per-lap event.
guard tame_style_long_fn
path tools/t/tame_style_long_fn_witness.rish
tier cadence
seated 20260906.173013

# THE RATCHET THAT COUNTS A LINE WHERE IT MEANT A CALL. Two of TAME's advise ratchets ask a lap
# to MIGRATE what they count -- `parseInt(` to `tally/parse_int.rye`, `Ed25519` to
# `tally/kumara.rye` -- and each was one `grep -hF <pattern> | wc -l`, which counts a LINE. A
# line is not a call, and `glow/` is this tree's own language implementation, so its source holds
# the TEXT of the Rye it emits: `append_print(out, &used, "... std.fmt.parseInt(u32, argv[2], 10)
# ...")` is a compiler writing a call, and the witnesses beside it assert `indexOf(rye_argv,
# "parseInt(u32")` on that output. Measured across the twenty rooms: `parseInt(` printed 125 of
# which 79 are emitted text, all in `glow/`; `Ed25519` printed 26, of which 29 counted lines are
# `//` prose and 10 more are `X25519.KeyPair.fromEd25519` conversions. The true remainders are
# 46 and 1 -- and reading that 1 rather than counting it found it is a TRAILING comment, so the
# genuine Ed25519 debt outside the kumara seam is zero.
#
# The predicate is written ONCE, in `tools/fixtures/t/tame_style_app_sites.sh`, called by both
# halves. That costs the parity selftest nothing that existed -- it compares only the zero-assert
# and memcpy numbers -- and `@memcpy(` is deliberately left on its own two independent readings,
# since it measures identically under both rules (0 emitted, 0 comment) and is the one count
# under numeric parity.
#
# The guard gates the PREDICATE, never the counts: sixteen planted control cases in a throwaway
# pen, both parity traps (`\"` and `'"'`) shown, the one documented overcount asserted so it
# cannot move in silence, and both halves confirmed still saying `program position`. `tier lap`:
# under four seconds, and the fault it prevents is written by ordinary work -- a lap widening a
# room or a grep is exactly when a line count quietly becomes a call count again.
guard tame_style_app_sites
path tools/t/tame_style_app_sites_witness.rish
tier lap
seated 20260908.115028

# shared_pen -- a scratch path named by a constant is a path eight ships share.
# FOUR witnesses have reddened a cold pass across three laps while each was GREEN run alone --
# signal_trap, tlb_reach, amphora_restore_negative, and tlb_reach a SECOND time -- because a peer's
# `rm -rf /tmp/<a chosen name>` reached their fixture mid-run. Each sweep that repaired one counted
# the class by the spellings it could read: the first read a shell `TMPDIR=` assignment and missed a
# Rishi `let home = "/tmp/name"`; the second read four spellings and missed `${TMPDIR:-/tmp}/<name>`,
# whose text holds no literal `/tmp/` at all -- which is the one tlb_reach was written in, and why
# it came back. This reads all five. `tier lap`: one awk over 3,102 sources, under two seconds, and
# ordinary work is exactly what writes the fault -- a new witness reaches for a pen name on the lap
# it is born, so a cadence clock would let four laps of them through. The scan alone is under
# two seconds; the witness runs 14s measured 20260907.000549, since its control builds real
# git repositories for all 27 behaviors.
guard shared_pen
path tools/s/shared_pen_witness.rish
tier lap
seated 20260906.225150
# bron_resins_catalog -- the cellar's catalog names every resin it holds, and now something reads it.
#
# WHAT WAS MEASURED (`20260907.000030`). `bron-resins/` is the resin cellar and
# `bron-resins/manifest.bron` is its catalog, one `entry <basename> <note>` line per resin. It named
# THREE of the room's SEVENTEEN. Its own `note` line states the habit that keeps it -- *commit with
# the resin* -- and that habit was kept for the first three files of the first morning, `20260712`,
# and dropped. Nothing in the tree read the catalog: three scans name `bron-resins/` only to skip it
# as testimony (`counsel_census_scan.sh`, `waymark_rung_drift_scan.sh`, and `template-manifest.bron`'s
# `personal` verdict), and no witness, runner, or roster row touched it. A catalog covering a fifth
# of its room reads exactly like a whole one -- `%514`'s lesson standing in a room where the silence
# had lasted eighty-seven days.
#
# THE LAW IT KEEPS is `foundations/20260703-202312_the-marked-value.md`, whose Amber clause reads
# *every resin records the marks of what it seals, so a reader in another decade knows what the
# cellar held without opening it.* The council rota's Earth-Dual seat is that document, which is how
# this was found: the row that breathes in reads the concrete fact at the door, and the fact at this
# door was a catalog four fifths short.
#
# A LANTERN RATHER THAN A LOOM, measured before the instrument was built. Every tracked file in a
# living room whose name carries `manifest`, `catalog`, or `roster` was crossed with its readers:
# the mycelium corpora each have a witness and a Rye reader, `forge_cellar_manifest.bron` is read by
# `pond/apps/drawn_terminal.rye`, and the seat rosters are read by twenty, nine, and eight sites.
# `bron-resins/manifest.bron` was the ONE enumeration of a room with no reader at all. So this is a
# scan for one cellar rather than a general instrument, and the near-miss is named:
# `cellar/ring1_manifest_shape.bron` is cited only by `cellar/README.md`, and it is a SHAPE rather
# than an enumeration, in a lane this seat does not hold.
#
# THREE GATES AND ONE REPORTED READING, and they are separate because they fail in separate
# directions: `uncatalogued` is a resin nobody named, `orphan_entries` is a line promising a file
# the room lacks, and `duplicate_entries` is a catalog disagreeing with itself.
# `noteless_entries` is REPORTED -- a bare name is a weak line and still a true one, and a gate that
# reds on an honest half-measure is one somebody turns off. An absent room or catalog answers
# `misread` and exits 2, never zero, since zero is the reading a healthy cellar gives.
#
# The fourteen notes were written from each resin's OWN first header line rather than from a
# summary. The one `.md` file in a room whose `law` line says Bron only is named in the catalog
# rather than swept, because a catalog states its exception instead of hiding it.
#
# `tier lap`, 4s measured `20260907.000030` against a full lap pass of 983. The control proves 34
# behaviors on throwaway cellars: every refusal shown from the failing side and then LIFTED, the
# reported reading proven not to gate, the detail listing's bound proven by planting 75 resins
# against a ceiling of 64, and the pen proven innocent against a scan patched to always answer ok --
# with that patch's own landing proven by `cmp -s` and by refusing an EMPTY copy, which the control
# caught in itself (an unreadable source leaves a copy that differs from the original and reads as a
# landed plant).

guard bron_resins_catalog
path tools/b/bron_resins_catalog_witness.rish
tier lap
seated 20260907.000030
# topology_churn -- what ONE DEPARTURE costs a table everybody shares, at 720 points.
# Its sibling `topology_routing` binds the DECISION on a whole graph -- 719 entries, 3 bits each,
# 270 bytes, the same 270 at every node. That claim rests on the graph being the whole GROUP, which
# a live network breaks the first afternoon it runs. This binds the same state on a punctured graph:
# the graph's own damage (84 pairs of 516,242 on the circulant, nothing disconnected, no diameter
# moved), the stale table's damage (3,811 drops -- 45x the graph's, and predicted to within six
# pairs by (mean hops - 1)/(n - 1)), how many of the 719 entries change (925, of which a DERIVED
# n-1-degree = 713 are a dead first hop held by the hole's six neighbours), and the curve under two
# named removal patterns, where a SCATTERED loss of 64 points hurts more than a clustered one.
# `tier cadence` because the scan walks 516,242 pairs three ways on four shapes plus 56 curve
# configurations -- 135s measured `20260907.000309` -- and the control runs ten plants across four
# leg words; its answer moves only when the scan moves.

guard topology_churn
path tools/t/topology_churn_witness.rish
tier cadence
seated 20260907.000309

# topology_growth -- what an address space costs WHILE IT IS STILL FILLING, at the same 720 points.
# Its two siblings price a FULL space: `topology_routing` binds the decision on a whole graph, and
# `topology_churn` binds it on a graph that has lost a point. Both rest on a table computed for the
# whole group, and a growing network holds that table while most of the group is still dark. This
# binds the filling regime: five allocation policies at six occupancies over four shapes, with the
# stale table routed over every live ordered pair two ways from 24 sampled destinations.
#
# The load-bearing pair is one counter over two numberings. A lowest-free allocator leaves the star
# graph in 36 components of ONE POINT EACH at 5% occupancy, all 840 ordered pairs unreachable; the
# same counter over a breadth-first numbering leaves it whole, with 85% of pairs arriving. The
# mechanism is a theorem the scan checks by counting edges rather than trusting: the 120 lexicographic
# ranks with a1 = 1 are an independent set, since every star generator swaps position 1 with some j.
# The three coordinate shapes read prefix_edgeless_max=1 and that mirror is load-bearing -- a leg
# that found the star failing and never found a shape passing could not be told from a broken one.
#
# WHY IT IS `tier cadence`. The full pass is 21s measured `20260907.045627` and its answer moves
# only when the scan moves, which is a deliberate act; a per-lap row would spend those seconds on
# eight ships to watch a number that did not change. The control runs eleven plants across four leg
# words, nine breaking a named reading and two passing on purpose under a leg that cannot read them.

guard topology_growth
path tools/t/topology_growth_witness.rish
tier cadence
seated 20260907.045627


# THE PLANT LAW -- a control's break is a claim about a file, and nothing was checking it.
#
# A control breaks a module on purpose by naming one literal line of the real source in a `sed`.
# When that line moves, the sed matches nothing, the pen is copied byte for byte, and the phase
# builds and runs the UNMUTATED module -- exit 0, which is what a law that holds reads like
# (REDS %519). `tools/fixtures/p/plant.sh` answers that once, and this rung proves it can red:
# twenty-eight behaviors, each function shown from both sides, and one leg that strips the byte
# comparison out of a copy and asserts the copy calls a no-op plant LANDED.
#
# Adoption is a FLOOR that only rises, which is the mirror of this tree's usual ratchet, because
# the population counted is the good one. Seated at 4; `sh tools/fixtures/p/plant_adoption_scan.sh
# --list` names the rest, and each falls on touch.
#
# `tier lap`: the control writes pen text and runs no compiler, so the whole rung costs under a
# second -- and what it guards is every other control's honesty.

guard plant
path tools/p/plant_witness.rish
tier lap
seated 20260907.004838
# seat_prompt_figure -- a charter says what a ship is FOR, and a count in it can carry no date.
#
# WHAT WAS MEASURED (`20260907.011258`). A seat prompt is the first thing a ship reads on every lap
# it will ever run -- `tools/f/fleet-loop.sh` prepends the baton to it and hands the pair to the
# agent. Eight live seats, eight prompts, and TWO carried tree measurements: copal's `10 Rye modules
# and 3,861 lines` with `16 witnesses`, and patchouli's `32 Rye modules and 9,175 lines` with `four
# rostered witnesses`. All six had parted from the tree.
#
# THE TWO WAYS A FIGURE GOES WRONG, and this pair showed both. Copal's witness figures were EXACT at
# their seating -- 16 tracked paths named both `amphora` and `witness` on `20260905.164639`, and not
# one stood on this roster -- and went stale on ordinary work, reading 17 with 9 rostered by
# `20260907`. The line figure was wrong the day it was written: `3,861` against the **3,582** the
# room's ten `.rye` paths read at that same commit, and against **4,058** today. A figure nothing
# reads cannot be corrected, only replaced.
#
# THE LAW IS ALREADY SEATED ONE ROOM OVER. This file's own directive on `construction/ITINERARY.md`
# reads *Counts come from the scan, never here*, and Gauge asks every figure to carry unit, date,
# and source. A charter carries none of the three: nothing runs it, and no reader can tell a figure
# written this morning from one written in June. This carries that law to the room beside it.
#
# A RATE IS A LAW; A TOTAL IS A MEASUREMENT. `two asserts a function` is TAME's rule and can go
# stale only on a word, so it is counted under `rates` and excluded -- and PRINTED, because an
# exclusion nobody can see is an exclusion nobody can check. `10 Rye modules` claims what the tree
# holds this morning. That one distinction is the whole discriminator.
#
# THE READING IS A FLOOR, DELIBERATELY. Only nouns that can name a code census are read -- module,
# line, witness, guard, assert, function, scan, control. `files`, `rows`, and `pages` are left out
# because a charter says `read its top three rows` as an INSTRUCTION, and a guard that reds on an
# instruction is a guard somebody turns off. So patchouli's `120 files outside it reaching in` is a
# measurement this scan cannot see, named here rather than hidden.
#
# `figures` is a RATCHET opening at **3** -- what stands after copal repaired its own charter, so it
# lands green on the tree it publishes to rather than the tree it was built in. `missing` is GATED
# at zero for a reason of its own: a live seat whose prompt file has gone shrinks the population, and
# a ratchet over a shrinking population reads as progress while nothing improved.
#
# `tier lap`, 2s measured `20260907.011258`; the scan alone is under a second over eight small files.
# The control proves 30 behaviors on planted fleets -- every refusal shown from the failing side and
# then LIFTED, a stamp and a row number and a port proven NOT to count, a possessive and an
# instruction quantity proven not to count, the parked seats proven unread, and the pen proven
# innocent against a scan patched to always answer zero. The ceiling was also proven both ways on the
# LIVE tree: one planted figure in copal's own prompt read `figures=4 verdict=over_ceiling` and
# exit 1, and the restore was checked byte-identical with `cmp` before the green was believed.

guard seat_prompt_figure
path tools/s/seat_prompt_figure_witness.rish
tier lap
seated 20260907.011258


# grain_strand_count -- the grain's strand count, derived from the page that holds the strands.
# `foundations/20260826-024942_the-grain-and-the-crossing.md` seated ten strands `20260702` and
# three more `20260802` under *Strands Seated at the Fusion*, "beside the elders" in its own words.
# It has held thirteen since; ten living sentences across six documents still said ten thirty-six
# days later -- the foundations front door, the day-one compass rose, the compass brief in four
# places, the aether threshold the rota opens with, the singularity history, and a manual tutorial.
# Every one of them carried a total rather than deriving one, which `.claude/rules/stamp-and-name.md`
# rule 3 already forbids: count, never number. Nothing read living prose for a carried total.
#
# The scan derives the count off the page's own structure and holds every disagreeing living
# sentence at zero; the control grows the pen's page by one strand and watches a pointer that was
# right a moment ago go stale, which is what proves the count is read rather than spelled.
#
# `tier lap`: one awk pass over 849 living documents, under two seconds.

guard grain_strand_count
path tools/g/grain_strand_count_witness.rish
tier lap
seated 20260907.062544
# scope_rank, seated 20260907.045528. Nothing but the runner read
# tools/fixtures/s/standing_equipment_scope_map.sh until this rung, so three faults in that file
# had no reader at all, and each fails in the safe-LOOKING direction: an orphan row (a typo buys a
# full run and reads like coverage), a second row under one name (the runner's lookup stops at the
# first), and a row naming LESS than its guard gates (the one direction that SKIPS real work -- the
# map's header records thirteen repaired by a hand on 20260906, and this rung found a fourteenth on
# its first run). All three at zero, each planted and lifted in a real git pen. Beside them it
# prices what each row saves -- cost from the run card, touch rate from git log -- which is the
# ranking external-research/20260907-020817 named and left unbuilt. `tier lap`: 11s at the
# witness's own `--window 40`, and the gates read no history at all.

guard scope_rank
path tools/s/standing_equipment_scope_rank_witness.rish
tier lap
seated 20260907.045528

# topology_stretch -- whether the diameter a shape PUBLISHES is one a table-free router can reach.
# Its sibling `topology_routing` prices the shared next-hop TABLE at 720 points on four shapes; this
# binds the RULE, on seven shapes across two sizes, and the pair divides the subject rather than
# repeating it. Two of the seven route worse than they walk: the circulant that walks 9 is routed at
# 12 by the better of two rules reading only the two addresses, so three of the five hops it seemed
# to buy are bought by a 719-entry table; and pancake, which beats star on mean at one group and one
# degree, finds a shortest path for 296 of 719 where star finds one for all 719. Bubble-sort is the
# counterweight -- table-free, exactly optimal, and still worse than the torus at 15.
# It also reads its OWN source, refusing when a routing function mentions the breadth-first distance
# arrays -- the one fault no hop count can report, since a rule that consults the table prints the
# table-free answer. `tier lap` because vertex-transitivity is CHECKED rather than paid for: one walk
# stands for 720 once six sources spread by a stride coprime to 720 agree, so scan and control
# together are 29s measured `20260907.095458`.
# Named `topology_stretch` rather than the draft's `topology_routed`, which stood three letters from
# the seated `topology_routing` in a roster of 220 -- a grep that cannot separate two guards is a
# collision whatever the filesystem thinks. It is named for the column it alone reports.

guard topology_stretch
path tools/t/topology_stretch_witness.rish
tier lap
seated 20260907.095458

# topology_relaxed -- what the two families that RELAX the star graph's factorial keep, and what
# they give back. Its sibling `topology_stretch` found the one shape that reaches a low diameter and
# routes optimally with no table, and named that shape's wall in the same breath: a star graph has
# n! vertices, so between 720 and 5,040 points the family offers no size at all while a supervision
# tree takes its member count from its membership. This walks the two doors that paper left open.
# Thirteen (n,k)-star graphs from 120 to 6,720 vertices and four arrangement graphs, each built,
# walked breadth-first, and routed from EVERY vertex by named rules. Eight relaxed sizes stand in
# the window the star family leaves empty, two of them at degree 6, and S(7,4) walks 7 on degree 6
# at 840 points where the best three-ring torus of that size walks 14 and every degree-6 commutative
# shape walks 9 or more. The finding is the RULE: the star's rule read literally is exact at k = n-1
# and short at all eleven sizes below it, a one-step correction reaches the true diameter and leaves
# a residue at two sizes, and a rule choosing by where a symbol's CHAIN ends -- in junk, which is a
# free eviction, or at symbol 1, which wastes a move -- routes every vertex of every size by a
# shortest path. Its two anchors are the sibling's own readings at 720 and 5,040, so a builder that
# had drifted would stop reproducing them rather than print confident numbers about the wrong graph.
# It reads its OWN source too, refusing when a routing function mentions the breadth-first distance
# array -- the one fault no hop count can report. `tier lap`: scan 10s, control 30s, witness 43s
# measured `20260907.111058`, because vertex-transitivity is CHECKED from five spread sources rather
# than paid for, and the control reads its plants at one small size through the scan's `SCAN_SIZES`
# knob rather than at all thirteen.

guard topology_relaxed
path tools/t/topology_relaxed_witness.rish
tier lap
seated 20260907.111058

# topology_occupied -- the free-routing claim meets a real membership. Its sibling
# `topology_relaxed` found a table-free rule that routes every vertex of seventeen graphs by a
# shortest path, and closed by naming its own second falsifier unfired: every reading assumed all
# n!/(n-k)! vertices occupied, and a membership never lands on a falling factorial. This fires it.
# Two (n,k)-star graphs are holed in four named geometries across sixteen configurations, and every
# live pair is routed to three sampled targets by three rules, with stretch measured against a walk
# through LIVE members only. Three readings carry the finding. The published rule, which cannot see
# liveness, loses 390 of 2,391 packets at 95 percent occupancy -- one in six. Filtering by neighbour
# liveness stops every hole loss and starts cycling instead, so what the rule lacks is MEMORY rather
# than a table. And a packet carrying the vertices it has already stood on, retreating one hop when
# every ranked neighbour is dead or visited, delivers every reachable pair of every configuration at
# a maximum packet state of 25 entries on an 840-vertex shape -- bounded by the path, not by the
# membership. The geometry reading is bound from BOTH ends because only one is free: a far-end
# symbol class, 120 of 840, costs zero stretch and does not move the diameter, while a DOOR class of
# the same size splits the shape into four components. That partition read 837 of 837 delivered, so
# the count of pairs it made unreachable stands on the same line -- excluding them silently is how a
# partition reads as perfection. Its anchor is the sibling's own two readings, since this file
# carries its own copy of the builder and a drifted copy would measure the wrong graph. It reads its
# OWN source too, refusing when a routing function mentions the breadth-first distance array,
# INCLUDING on the declaration line -- the control planted it there and the first draft passed.
# `tier cadence`: scan 57s, control 35s, witness 95s measured 20260907.145954 and 125s re-measured
# at the landing 20260907.153000 on the same pier -- report the range rather than the luckier run --
# because every live
# pair of every configuration is routed exhaustively rather than sampled, which is heavy for a lap
# and ordinary for a cadence.

guard topology_occupied
path tools/t/topology_occupied_witness.rish
tier cadence
seated 20260907.145954

# topology_partial -- a membership never lands on a whole class either. Its sibling
# `topology_occupied` found that a whole far-end symbol class of S(7,4) -- 120 of 840 vertices --
# costs nothing measurable while the same 120 scattered cost a sixth of the traffic, and closed by
# naming its own falsifier unfired: a membership landing between whole classes, 800 on 840, leaves
# 40 holes with no class to hide them in. This fires it, and refutes the tidy hypothesis that rode
# on it. The classes NEST -- 840 splits 120, 20, 4, 1, derived by the falling factorial and counted
# by grouping the vertices that share each suffix -- yet the freedom does NOT descend the ladder:
# 42 whole level-2 classes, every one 20 vertices and every one leaving the shape in one piece,
# cost between 3 and 491 lost packets of 9,828. Which whole class is the choice that pays. Two
# findings survive. The leftover addresses are nearly free: at 800 live on 840 an arrangement taken
# in far-end-major order loses 429 of 9,588 against 1,156 scattered, and two holes more than a
# whole class can hold cost 457 rather than 429 -- a rounding error rather than a cliff. And the
# SAME contiguous run partitions or does not depending which end of the address is significant:
# 20 holes of S(6,3) taken door-major leave three components and 600 unreachable pairs, where the
# same 20 taken far-end-major leave one component and an unmoved diameter. The naive heuristic --
# keep the holes inside one class, taken door-major -- reads WORST of the five arrangements, behind
# scattering, so the finding is bound from that end too. One method correction is bound with them:
# targets are sampled from the whole address space and held FIXED across arrangements, because
# scored the sibling's way at three live-sampled targets `class_partial` ranked best of five and at
# fifteen it ranked worst by a factor of fourteen. It reads its OWN source, refusing when a routing
# function mentions the breadth-first distance array, and closes a function body at a column-zero
# comment -- which it learned by refusing itself on its first run over a line of prose describing
# that array while touching none of it.
# `tier cadence`: scan 314s, control 102s, witness 416s measured 20260907.173400 on this pier,
# because every live pair of 117 configurations is routed exhaustively rather than sampled and 42
# whole classes are each emptied in turn, which is heavy for a lap and ordinary for a cadence.

guard topology_partial
path tools/t/topology_partial_witness.rish
tier cadence
seated 20260907.173400

# STORE SHAPE -- the tree keeps two data shapes in one notation, and a store fitted to one is wrong
# for the other. The census walks every tracked `.kyri` and `.bron` session-log record plus four
# named registries, reading field names at line start, and prints how many records carry a field
# more than once. Measured `20260907`: journal 4,337 records, 91% multi-valued; registries 413 rows,
# zero. The witness binds the SEPARATION rather than any count, since every count grows daily and a
# guard reding on ordinary writing is a guard somebody turns off -- a floor at 50% where "mostly"
# stops meaning mostly, and a zero on the registry side because a row gaining a second copy of one
# field is a table becoming a document. Paper:
# `external-research/20260907-191657_two-shapes-one-notation.md`.
# `tier cadence`: the census walks 4,337 files with two passes each, 153s measured `20260907.191257`
# on this pier under fleet load -- heavy for a lap and ordinary for a cadence.

guard store_shape
path tools/s/store_shape_witness.rish
tier cadence
seated 20260907.191657

# JOURNAL QUERY -- the journal is asked for records, never for fields. The store-shape census above
# measured the DATA and recommended an index; it named its own second falsifier in plain words --
# count the queries the tree's tools actually ask, and if nearly none asks a field predicate the
# plan shrinks to a sorted room. This census is that count. It classes every non-comment line in a
# tracked tool source that names `session-logs/` into five non-query classes (exclude, construct,
# message, roster, and `rule` for a line reading the LAW rather than the room) and six query
# classes (field, recency, by_path, index, enumerate, exists). Measured `20260907`: 234 sites, 168
# of them naming the room without reading a record, 52 real queries, and `class_field=0`. The
# file-level upper bound `field_capable_files` reads 3, of which one queries the journal and two
# query the equipment registry. The witness binds `verdict=sorted_room`, which is a SEPARATION and
# not a count: field-capable sources under a tenth of the querying sites. A red here says the
# workload changed and the index recommendation went live -- a tripwire on a research claim, never
# a wall against a commit. Paper:
# `external-research/20260907-201914_the-workload-and-the-index.md`.
# `tier cadence`: the census greps 3,359 tool sources line by line, 40s measured `20260907.204600`,
# and its control runs the census 26 more times over a pen.

guard journal_query
path tools/j/journal_query_witness.rish
tier cadence
seated 20260907.203000

# log_file_claim -- tools/hooks/commit-msg has refused a commit BODY citing an absent path since
# REDS %202, on the reasoning that a commit message is permanent testimony. The session log rides in
# that same commit, closes with `file <path> <why>` lines making the same claim, is exactly as
# permanent, and nothing read it. Measured 20260907: 16,924 path-shaped `file` fields across 4,352
# logs, and 228 name a basename that exists nowhere -- 32 written that day. Twelve share one shape:
# a lap folds its live front to a shelf and writes the path from its OWN log's stamp rather than the
# shelf's, and `git log --all` finds not one of the twelve ever existed. Gates `staged_unwritten` at
# zero -- this commit's own logs, read against tree and index together, so a shelf staged beside the
# log naming it passes free and a peer's record can never refuse this lap. Reports `unwritten_total`
# and `unwritten_today`, never gated: that population grows ~26 a day across eight ships and is
# testimony no lap may repair, so a ceiling over it would fire at a stranger's desk. `lap` rather
# than `cadence`: 0.4s measured 20260907, down from 55s read log by log, and the claim it refuses
# can be written again in any lap that closes a session log.

guard log_file_claim
path tools/l/log_file_claim_witness.rish
tier lap
seated 20260907.221800

# say_compose_bound -- a guard must not fail for having something to say. Rishi composes an
# interpolated string into `StrBuf`, a fixed 4,096-byte buffer declared in `rishi/src/main.rye`, so
# `say "reading -- ${scan.out}"` promises that a scan's output will stay under that width forever
# while the output grows with the tree it reads. On 20260907.232343 reds_spine_derive passed every
# assert it makes and then died reporting, `rishi: line 46: StringTooLong`. The same fault has been
# met four times by four hands -- reds_spine_derive_witness.rish, readme_metrics_splice.sh, and
# tame_style_long_fn twice -- each repaired where it landed, each lesson written into a different
# file's comments, so one class read as four accidents. This is that lantern's loom.
#
# Two shapes counted apart, since they fail at different moments: `eager` composes on EVERY run and
# would red a GREEN tree the day its scan grows, and `deferred` composes only in an assert's `else`,
# which is to say exactly when an explanation is owed. `safe` reports the population already spelling
# the repair, a bare `say <name>.out`, which never opens a StrBuf at all. Ten sites of each kind are
# named on every pass with `list` printing them all, so the repair is one line away (%592). The
# width is READ off Rishi's own declaration rather than carried as a copy.
#
# THE CEILINGS ARE SHARES, IN PER MILLE, and that was learned inside the round that wrote them. The
# first draft held raw ceilings at exactly what stood -- 502 and 1,741 -- and the rebase closing that
# same round brought two peers' new guards in and refused. Eight ships write here and every one lands
# witnesses; a ceiling on the count reds on the most ordinary act on this pier. A share asks about
# the habit instead, so a tree that doubles without changing how it writes reads where it stood.
#
# `lap` rather than `cadence`: one awk pass over 2,415 tracked scripts costs 5.7s measured
# 20260908, and the shape it counts can be written again in any lap that adds a witness.

guard say_compose_bound
path tools/s/say_compose_bound_witness.rish
tier lap
seated 20260908.014200

# `lap` rather than `cadence`: the whole pass costs 3.1s measured 20260908 -- one awk read of 108
# named law pages, one derivation from `rishi/src/main.rye`, and five one-line probes through the
# interpreter. The surface it reads is law prose, which any lap may edit, so the clock is the lap.

guard rishi_run_record
path tools/r/rishi_run_record_witness.rish
tier lap
seated 20260908.101500

# glow_literal_law -- one language, one answer for a leading zero. Glow reads a decimal literal in
# fourteen places and on 20260908 those fourteen gave two answers: glow/rune_shop_gate.rye refuses
# `007` and calls the refusal "the house parse law" in its own comment, and the other thirteen read
# it as seven. Proven on metal through both public entries the same day. The gate is the MINORITY,
# min(strict, permissive), so it closes under either lawful ruling rather than choosing one; the
# language question itself is Keaton's. `lap` rather than `cadence`: the pass costs 2.5s measured
# 20260908.170542 on this pier, and the surface it reads is glow/ source, which the language lane
# edits every round. Authored 20260908.143733 in a lap that never landed; restored from its stash
# and re-proven on metal before this row was written.

guard glow_literal_law
path tools/g/glow_literal_law_witness.rish
tier lap
seated 20260908.170542

# energy_instrument -- a joule this host will not let us read is not a joule we have. The tree's
# energy arc runs several papers deep and not one figure in it was read from a counter; every one is
# a CPU-second proxy or a vendor budget. This names the tier any energy figure taken here was
# measured at -- `joules`, `counters`, or `cpu_seconds` -- and it reads by OPENING each counter and
# requiring digits back, because since PLATYPUS (CVE-2020-8694) Linux ships intel-rapl `energy_uj`
# at mode 0400, so a path that exists can refuse every unprivileged read. A second fixture asks the
# question sysfs cannot answer -- whether a declared instrument is one a process actually reaches --
# by running tools/rye/perf_self_count.rye and requiring a count. A host is not a fault:
# `tier=cpu_seconds` passes free. What the control gates is the probe's own ability to say `joules`
# when joules are there, over planted sysfs pens, since a probe that cannot say `joules` proves
# nothing by saying `none`. `lap` at a measured 1.0s on this pier: a ship moves between machines,
# and a lap is where a figure gets written down. Authored 20260908.131900 in a lap that never
# landed; re-proven on metal before this row was written.

guard energy_instrument
path tools/e/energy_instrument_witness.rish
tier lap
seated 20260908.234221

# One literal, two verdicts, seated 20260909.001908. glow/rune_shop_gate.rye:522 carries a comment
# naming a "house parse law" -- refuses empty, leading zero, overflow -- and it is one of FOURTEEN
# source-decimal readers in the language, the only one holding it. Proven on metal the day this was
# seated, on three paths in one tree: `?:  (eq a b)  01  0` refuses with MalformedBody through the
# shop gate, while `=/  amount  007` lowers GREEN through glow/lower_multi.rye to
# `const amount: u32 = 7;` and `=/  amount  03` lowers GREEN through glow/lower_compose.rye to 3.
#
# The lexer is neutral, which is why the split could hide: glow/tokens.rye scan_digits takes any run
# of digits and emits one `.decimal` token, so nothing about a leading zero reaches the token stream.
# The disagreement lives entirely in the fourteen functions that turn those bytes into a u32, and no
# two of them are called from one place, so no test ever sat them side by side. Each is internally
# consistent and correct on its own terms; the fault exists only in the relation between them, which
# is how fourteen careful hands produced it.
#
# WHY THE RATCHET COUNTS VERDICTS RATHER THAN LAW-BREAKING READERS. Which way the law falls -- Glow
# refuses a leading zero everywhere, or accepts one everywhere -- is a language custody ruling that
# stays Keaton's. A ceiling on `law_absent` would presume it: rule that Glow accepts, and the repair
# deletes the check from its one holder, taking 13 to 14 and reddening this guard for landing the
# answer. The count of DISTINCT verdicts falls to one under either ruling, so the meter holds the
# fault still without deciding it. Two control legs carry exactly that shape -- a room refusing
# everywhere and a room accepting everywhere both read one verdict and both pass.
#
# `law_claimed_absent` is the gate, and it is the copy this split was made of.
# glow/rune_assert.rye parse_decimal is the shop gate's function byte for byte, minus the loop
# variable's name and minus the one line that refuses. Copying a law's shape without its check is
# how the split reached fourteen readers; copying its COMMENT too is the same move one step louder,
# decidable today, and held at zero.
#
# `tier lap` because it is file reading at 0.6s and the whole guard costs 4s, and because a ratchet
# only works if it is read every lap. The population is DERIVED from the room on every pass -- this
# lap's own first reading, taken by hand with grep, found 5 of the 14 (REDS %532's lesson landing on
# the hand that cited it).
guard glow_decimal_law
path tools/g/glow_decimal_law_witness.rish
tier lap
seated 20260909.001908
