# construction/standing-equipment.kyri -- the standing guards, as a list a program can read.
#
# WHY THIS FILE EXISTS. The roster of standing equipment lived in one paragraph of
# construction/ITINERARY.md, in prose. A paragraph can be read and it can be trusted; it cannot
# be run, counted, or dated. Two consecutive rounds paid for that: REDS %147 found eight
# standing witnesses each holding a count its source had moved past, and REDS %149 found
# the living-pin bound guard unrun for four laps while construction/REDS.md sat 2,050 bytes over
# the bound it measures. Both were caught by a hand that happened to run something.
#
# The tree already seats the strand this file's sibling completes. The grain says a guard
# that cannot red guards nothing (REDS row 59), and every guard here can red. The strand
# beside it, learned the hard way twice: a guard that is never run guards nothing either.
#
# WHAT A ROW MEANS. One `guard` record per standing check, each naming the path a runner
# invokes, the tier it runs on, and the stamp the guard was seated.
#
# WHAT A TIER MEANS. Guards cost wildly different amounts of time. A choir -- one witness that
# sings a whole family of rungs -- takes minutes: `caravan_suite` runs 111 rungs in 8m31s,
# `crypto_suite` runs 74 in 9m06s, and `ales_suite` runs 240 in 233s and 303s over two clean
# passes, all measured on this pier on 20260825, against a whole roster of 20m20s with one of them
# seated. A lap reads the roster twice, cold at the open and hot after `git add`, so every choir on
# one tier would cost most of an hour a lap. So a guard names its own clock:
#
# COST DECIDES THE TIER, AND SO DOES WHAT IS MOVING. `caravan_suite` measured 840 seconds on
# 20260831 and serializes 121 witnesses over shared runtime paths. Its roster bijection is cheap
# and stays on every lap; the full sing runs on cadence, when scoped work reaches Caravan, or when
# a hand asks for it by name. `ales_suite` also stands on cadence because its Lotus rungs are stable.
# Revisit a row's tier when either family reopens.
#
#   tier lap       every roster run. What a record naming no tier means, which is why none of the
#                  rows below had to be edited when the field arrived.
#   tier cadence   the slower clock -- sung whole by `--all` or `--tier cadence`, turned one guard
#                  at a time by `--cadence-slice N` on an ordinary lap pass, and reached by name
#                  on any lap where a hand asks for the guard.
#                  (This line read "the fifth round" until `20260910.221225`, and nothing in this
#                  tree counts rounds, so no pass was ever the fifth: `cadence_never_run_here` read
#                  74 of 74 on this pier. The rotation reads the run card instead, so the guards
#                  that have waited longest ride the next lap and need no round number at all.)
#                  (The seed once shipped on this clock too; that cadence was CUT 20260826,
#                  and custody gate %1 holds every refresh at Keatons hand.)
#
# A tier is a CADENCE rather than an exemption. REDS %219 was a choir standing off this roster
# altogether, which is a refusal nobody receives; a cadence guard is still heard, on a slower
# clock, and the run card records which clock heard it. A tier word the runner does not know is
# refused at zero, because a guard seated on one would run on no lap at all, in silence. The roster is the authority for what stands;
# construction/standing-equipment-runs.kyri records when each last ran ON THIS PIER, and that card
# is deliberately untracked, so a fresh clone reads `never run here` rather than inheriting
# another machine's memory.
#
# HOW IT IS KEPT HONEST. tools/s/standing_equipment_witness.rish proves every path exists,
# refuses a run card naming a guard the roster never seated, and refuses any recorded RED.
# Add a guard by adding a record here; the meter counts the file rather than a sentence.
#
# ONE PASS AT A TIME. The runner takes a directory lock at zero wait before it measures anything,
# so a second pass in the SAME tree refuses by name rather than deleting binaries out from under
# the first (REDS %359). The lock path is relative to the repository root, so the fleet's other
# trees run freely beside each other.

format standing-equipment-v1
stamp 20260825.183336
voice Kyri
runner tools/fixtures/s/standing_equipment_run.sh
card construction/standing-equipment-runs.kyri

guard banner_room
path tools/b/banner_room_witness.rish
seated 20260822.231949
tier cadence   # banners across every room; moves when a room opens or a fossil is bannered

guard dated_path
path tools/d/dated_path_witness.rish
seated 20260821.203103
tier lap   # affordable after %622; gates only broken living Markdown promises, while testimony remains reported

guard docs_command_path
path tools/d/docs_command_path_witness.rish
seated 20260908.193508
tier lap   # one awk over every fenced block and every inline "Run `...`" span of 5,787 tracked pages, measured 5s -- cheap enough to run every lap, and the fault it catches is spent by a reader on their first hour

guard door_home
path tools/d/door_home_witness.rish
seated 20260917.172626
tier lap   # 117 living doors found by one grep -l prefilter and read a block each, measured 13.1s against 23.1s before the prefilter -- and the fault it catches is a first-hour reader clicking home and landing in a room, which every link guard here calls fine because the target does resolve

guard dated_path_repoint
path tools/d/dated_path_repoint_witness.rish
seated 20260821.203103

guard documented_room
path tools/d/documented_room_witness.rish
seated 20260821.203103

guard empty_document
path tools/e/empty_document_witness.rish
seated 20260823.041442

guard equinox_e123_living_pin_guard
path tools/equinox/witness/equinox_e123_living_pin_guard_witness.rish
seated 20260817.000000

guard exec_bit
path tools/e/exec_bit_witness.rish
seated 20260823.025333

guard fetch_toolchain
path tools/f/fetch_toolchain_witness.rish
seated 20260821.203103

guard foundations_link
path tools/f/foundations_link_witness.rish
seated 20260821.230319

guard foundations_reach
path tools/f/foundations_reach_witness.rish
tier lap
seated 20260910.004858

# front_door_claim -- a page that says a front door names it, checked against that front door. A
# foundation telling its reader where it is reached from makes a claim about ANOTHER page's
# contents, and that is the one claim a page cannot keep alone: the link out belongs to the author,
# the link back to whoever edits the front door next. The Lindy foundation carried that sentence
# from 20260811; commit 7191a938b rewrote README on 20260823 and the word left the page; the claim
# stood eighteen days and nothing in the tree could hear it. foundations_link reads the links a page
# WRITES, foundations_reach reads whether the ROOM's index names a page, and neither reads the root
# front door. The key is DECLARED, the way Style and Room are, so opting in is the whole population
# filter and no reading guesses which sentence meant to be a promise.
guard front_door_claim
path tools/f/front_door_claim_witness.rish
tier lap
seated 20260910.234454

guard generated_page_freshness
path tools/g/generated_page_freshness_witness.rish
seated 20260823.000512

guard geode_libraries
path tools/g/geode_libraries_witness.rish
seated 20260821.203103

guard living_card_ascii
path tools/l/living_card_ascii_witness.rish
seated 20260817.000000
tier lap       # the card and the ledger, which every lap writes

guard living_docs_lint
path tools/l/living_docs_lint.rish
seated 20260821.203103
tier cadence   # ADVISE-only -- it prints advisories and never fails, so its reading is a trend

guard nib_honesty
path tools/n/nib_honesty_witness.rish
seated 20260821.211943

guard radiant_negation
path tools/r/radiant_negation_witness.rish
seated 20260821.211943

guard readme_metrics
path tools/r/readme_metrics_witness.rish
seated 20260821.203103
tier lap       # pages the pre-commit hook regenerates at every commit

guard reds_ledger_monotone
path tools/gen/chapter/reds_ledger_monotone_witness.rish
seated 20260821.235936

guard reds_spine_derive
path tools/r/reds_spine_derive_witness.rish
seated 20260827.181605

guard reds_row_shrink
path tools/r/reds_row_shrink_witness.rish
seated 20260917.071327
tier lap       # 9s on this pier: one git log -p over the spine, plus its pen
# A ledger row is written once and never edited, and that law was held by hands alone. A rebase is
# not a hand: it resolves one row's text in favour of the side that booked a DIFFERENT number, and
# reds_ledger_monotone reads only the spine's ORDER, so a row losing a paragraph keeps its gaps,
# numbers, stamps and file count intact. This walks the anointed spine's whole history in one
# `git log -p` and reports a record whose WORD count fell -- words, because the ascii-first sweeps
# change a row's bytes and not one word of its text. It GATES NOTHING about the ledger, by the
# booking row's own ruling: two ships lawfully edit this file each lap, an open row accreted to
# BOOKED sheds its tail, and a wall that reds on honest work is a wall somebody turns off.

guard room_bound
path tools/r/room_bound_witness.rish
seated 20260821.203103
# room_enumeration -- a page that lists the rooms beside it, checked against the rooms on disk. A
# door naming its neighbors so a reader learns a shelf without walking it makes a claim about a
# POPULATION, and a population grows. docs-geode/etc/README.md, the room held open for a genre that
# does not exist yet, named eleven neighbors by count and by name while twelve stood: lessons/
# landed 20260910 in e40799aba, so the one room whose job is noticing a new genre missed one, and
# its closing line about the day a twelfth arrives had come true a day early. crushed_index was
# built for this exact fault -- its header records three typed counts wrong at once on 20260906 --
# and it reads MEMBERSHIP, table rows against the room, so a count or a list a page types in PROSE
# is read by nothing on any page, including the four it guards. The key is DECLARED, the way Style,
# Room and Front door are, so opting in is the whole population filter.
guard room_enumeration
path tools/r/room_enumeration_witness.rish
tier lap
seated 20260911.025732

guard rye_bridge_cycle
path tools/r/rye_bridge_cycle_witness.rish
seated 20260821.232733

guard rye_toolchain_resolve
path tools/r/rye_toolchain_resolve_witness.rish
seated 20260911.115552
tier lap       # 13s: three pen builds through the real toolchain plus three mutant rebuilds

guard sha3_file
path tools/s/sha3_file_witness.rish
seated 20260821.203103

guard phantom_path
path tools/p/phantom_path_witness.rish
seated 20260823.010937

guard prose_register
path tools/p/prose_register_witness.rish
seated 20260823.045448

guard seed_link
path tools/s/seed_link_witness.rish
seated 20260823.034321


# demo_output -- the shipping shelf's demos page prints five commands AND the answer each one
# gave, and until `20260916` nothing in this tree ran one of them. docs_command_path proves a
# printed PATH resolves; a path that resolves says nothing about what the command behind it
# prints today. Same class as the front-door census that went false in a fortnight one room
# over, which is the second firing that earned this loom.
# tier cadence -- 40s, because the guard RUNS what the page prints and two of those five
# commands walk the whole tree. A demos page goes stale over weeks rather than over laps, so
# the slower clock matches the thing being watched; `--cadence-slice 1` is what turns it.
guard demo_output
path tools/d/demo_output_witness.rish
seated 20260916.211732
tier cadence   # 40s: five real commands from the page, plus 20 pen legs and three mutations


# sow_allow_reach -- the seed's allowlist proven from the SHIPPED side. sow_witness asks did
# anything private get out; seed_link asks does a shipped link land; neither can ask did the room we
# allowed actually arrive. On `20260906` a second filter by name inside sow_project.sh discarded an
# allowed room, the projection ran, and sow_witness answered GREEN -- because a room that ships no
# bytes leaks no names (REDS %485). A publish is as wrong when a room is missing as when a name
# leaks, and quieter.
# capability seated 20260906.140000 (REDS %493). The guard reads the SHIPPED side, so it needs a
# projection at `seed/` -- which is gitignored, built by `tools/s/sow.rish`, and therefore absent
# from every fresh clone. Rostered `tier lap` with no capability it went red on this tree's first
# cold endurance run after landing, and a red guard withholds the roster receipt, so `--scoped` refuses and
# every ship pays a FULL cold endurance run every lap: 778s measured here. The scan's own refusal is right
# (REDS %170) and the roster is where the environment fact belongs -- the same reading the operator
# card already gives an empty `vendor/`. Absence is announced by name rather than made silent.
guard sow_allow_reach
path tools/s/sow_allow_reach_witness.rish
tier lap
capability seed_projection
seated 20260906.122632

# The recovered control runs small local projections on every checkout. It proves
# receipt transitions even where the actual seed projection is absent.
guard sow_allow_reach_control
path tools/s/sow_reach_inputs_witness.rish
tier lap
seated 20260909.071647


# sealed_digest -- a digest this tree records about its own bytes, compared to nothing, is prose. The
# algorithm split is already right and principled -- SHA3 where the choice is ours, SHA-256 where an
# outside protocol, vendor, or coreutils-only verifier fixes it -- so the open question was never
# WHICH hash but whether any seal is read. Holds unread at zero, reads past dated testimony, a bare
# fingerprint, and an outsider's quoted published value.
#
# A SECOND WAY OF BEING READ, `20260910.014500`. The proxy was *some file under `tools/` SPELLS
# this digest*, which is how a one-off pin is checked and was the whole reading while every seal in
# the tree was a one-off pin. The resin cellar then sealed seventeen files, each recomputed every
# lap by a rostered witness, and a guard that opens a catalog and checks every digest in it spells
# none of them -- so this census answered `unread=17` and its own gate would have reddened on a
# wall. A wall read as a silence is the dangerous direction for a proxy to fail in. It now also
# reads GENERIC: a document declaring a `witness ` path under `tools/` that EXISTS and is ROSTERED
# here, reported apart as `read_generically` so neither number absorbs the other. Three conditions,
# none satisfiable by prose, each planted and lifted separately; control 9 -> 16 behaviors.
guard sealed_digest
path tools/s/sealed_digest_witness.rish
tier lap
seated 20260906.192644

# unshared_citation -- derived-spine rule 4 made a gate. A row's %N is a VIEW the anointed remote
# allocates, so a lap cites its own row by STAMP until that number is bound upstream. The rule stood
# ten days as a habit and the habit never formed: 28 recorded errata, six rows renumbered more than
# once, one four times in an hour -- and each renumber sweeps 20 to 36 files that spelled a number
# the stamp would have spared. Reads shared_max from the spine on every run; the ledger, testimony,
# and a five-digit colour are each read past for their own reason.
guard unshared_citation
path tools/u/unshared_citation_witness.rish
tier lap
seated 20260906.210117

# itinerary_list -- a living pin's numbered list is a spine, and nothing read its numbering. REDS
# %789 and the row of 20260916.220730 are one defect forty minutes apart, from two hands: both replaced item 7 of the
# INNER LOOP in construction/ITINERARY.md -- the directive telling every lap to run the endurance
# runs -- with a duplicate Git nib line, leaving the list reading 6, nib, 8. The first stood ten
# hours and roughly 120 commits on the page an eight-ship fleet reads at every lap open, and every
# standing guard passed both: nib_honesty reads the nib by its KEY and finds it whichever line
# carries it, and the byte and link meters read bytes rather than meaning. Holds every ordered run
# in the seven rostered pins to 1..N at zero, reading the pin roster rather than the tree so
# testimony keeps every number it wrote. %789 named the cure and left it unbuilt because the
# lantern had fired once; the row of 20260916.220730 is what one prose finding reaches.
guard itinerary_list
path tools/i/itinerary_list_witness.rish
tier lap
seated 20260916.220730

# link_counted -- a count of tracked paths is not a count of things. `git ls-files` lists a symlink
# beside its target, and this tree links a module into every room that imports it by bare name,
# because Zig refuses an import escaping the root file's directory -- so one module stands at up to
# four tracked paths. Measured `20260910.004524`: 230 of 1,964 `.rye` paths are symlinks (11.7%), 4
# of amphora's 11 (36%). The bill: README.md published 1964 modules where 1,734 exist, a 13.3%
# overstatement on the front door in the number a reader divides the witness count by; and two
# equinox scans asserted floors of 9 and 10 amphora files against populations of 7 and 8, calibrated
# on the inflated reading, so repairing the count made them refuse the truth. The cure was already
# written twice with its reason in a comment, in the two Rye ASCII meters -- a lantern that fires
# twice becomes a loom. Held at ZERO: every instance was repaired on the seating lap, and a glob no
# symlink answers is read past, so it becomes a finding on the lap a link lands under it.
guard link_counted
path tools/l/link_counted_witness.rish
tier lap
seated 20260910.004524

# link_text_promise -- a link makes two promises and every elder guard reads one. The target promise
# is *this opens*; the text promise is *this is where it lives*, which is the path a reader takes in
# at the door and copies into a grep long before anyone clicks. A fold repoints the target and leaves
# the anchor at its pre-move spelling, so the two drift apart and every standing guard stays green:
# proven on `20260828` commit d3ce030b4, which rewrote targets inside context/TAME_GUIDANCE.md and
# touched no anchor on the same line. RATCHET under a ceiling that only falls -- 69 living anchors
# across 15 pages stood on the seating lap, and a wall would red the ordinary.
guard link_text_promise
path tools/l/link_text_promise_witness.rish
tier lap
seated 20260915.183000

# backtick_path -- the third promise, and the one nothing read. A link's two halves are both held:
# `tracked_link` gates the target, `link_text_promise` walls the anchor at zero. A path written in
# BARE backticks -- a Kin line, a Machinery line, a table cell, a sentence naming the witness that
# binds a claim -- makes the same promise to the same reader and was held by nothing, while
# tools/hooks/commit-msg has refused exactly that shape in a commit BODY since REDS %202. So the
# promise was checked where it can never be edited and unchecked where it can. Three spellings are
# honest and tried -- root, page, and room relative; placeholders, the two append-only ledgers, and
# every anchor text are read past, each for a named reason. RATCHET under a ceiling that only falls:
# 65 living citations across 38 pages on the seating lap. Three genres stand in that listing and
# each wants a reader: a room this tree renamed (six name work-in-progress/), a path in a teacher's
# own tree (four name TigerBeetle's docs/TIGER_STYLE.md), and a file a tutorial asks a reader to
# write. Only the first is a repair, which is why no wall was seated.
guard backtick_path
path tools/b/backtick_path_witness.rish
tier lap
seated 20260917.020000

# comment_path -- the same promise, in the room where this tree writes its programs. backtick_path
# above holds Markdown and named its own remainder on its face: a path in bare backticks standing in
# a .rish witness header, a .kyri roster comment or a .rye module head is read by nothing. This is
# that population, read by ONE meter rather than one per language, because the recognition rule is
# one rule and a rule written twice is a rule two files may come to disagree about. The sibling
# comment_citation reads Markdown LINK targets on these same comment lines and reads a backticked
# span PAST by name, so the two are the target half and the anchor half of one promise -- the
# division tracked_link and link_text_promise already make for pages. THE FINDING is that this
# population is not the Markdown one. In a prose page a backticked path is nearly always a citation;
# in a scan or a control it is as often a description of a room the script ITSELF BUILDS in a
# throwaway pen. They are told apart by a checkable question rather than by judgment -- is the
# span's parent directory one this tree carries? -- and on the seating lap that question split 113
# spans into 61 living promises and 52 pen rooms. RATCHET under a ceiling that only falls. Three
# genres stand in the living listing and each wants its own lane: an elder pre-fold spelling a
# resolver quotes as its own input, a phantom a guard names because it must not exist, and a
# genuinely stale citation wanting repair. Two of the third kind were repaired on the seating lap,
# which is why the ceiling reads 61 rather than 63.
guard comment_path
path tools/c/comment_path_witness.rish
tier lap
seated 20260917.034900

# living_pin_fence -- every guard over a living pin reads its BYTES; none reads it as MARKDOWN.
# `ascii_document` counts characters, `tracked_link` and `link_text_promise` read links, the pin
# bound counts length, `nib_honesty` reads one key -- and on `20260916` commit 99d4948e6 wrapped a
# sentence in construction/ITINERARY.md so a bare triple-backtick fence opener landed at column 0 in
# prose. CommonMark runs an unclosed fence to the end of the document, so 30,791 of that card's
# 38,970 bytes rendered as one code block for every markdown reader, GitHub included, while the
# whole roster stayed green -- each of them reading bytes that had not changed. Sibling of the
# numbered-list reading seated the same hour: a list and a fence are both STRUCTURE. WALL at zero
# across living pages, which a population already at zero earns; testimony reported, never gated.
guard living_pin_fence
path tools/l/living_pin_fence_witness.rish
tier lap
seated 20260916.231943

# section_citation -- a section named beside a path is the third promise, and the two link guards
# read the other two. `tracked_link` asks whether the target resolves in a fresh clone;
# `link_text_promise` asks whether the anchor names the path it opens. Both read a PATH, so a name
# standing beside it -- `context/GAUGE_STYLE.md` -> *Quality assurance -- the report card* -- was
# read by nothing. The operator card molted on `20260912` and renamed its sections, and the compass
# rose kept sending a reader to three stations that no longer stood, every link green. WALL at zero
# for the three precise joins (an arrow, the word `section`, a link close then italic). The bare
# `--` join is COUNTED AND PRINTED, never gated: 21 candidates on the seating lap, 10 naming a real
# place and 11 not, and nine of those eleven are a writer emphasising a word.
guard section_citation
path tools/s/section_citation_witness.rish
tier lap
seated 20260916.190000

# rota_declared -- the council rota's reading, counted rather than assumed. On `20260906` the record
# answered three ways across 109 logs: 20 carried a `rota` field, 53 named a row in prose alone, 36
# said nothing -- one observation carrying two claims (whether a rota was read, whether it was
# recorded), which is the single-stranded braid. The field existed in practice and in no law until
# that day. REPORTED, never gated: a lap that legitimately did no rota read is indistinguishable
# from one that skipped it, and a gate that cannot tell them apart reds on honest work.
# capability seated 20260907.003211. REPAIRED `20260907.114500`: the guard counted TODAY's logs and
# nothing else, so between 00:00 and the day's first COMMITTED log there was nothing to count and
# its scan refused -- right about the corpus (REDS %170) and wrong about the subject, which made an
# ordinary daily state read as a fault. Measured at 00:12 on `20260907`, the first cold endurance run of a
# day answered `guards_red=2` on one root: this guard, and `standing_equipment` reading its own
# roster's red. A red guard withholds the roster receipt, so `--scoped` refuses and every ship pays
# a FULL cold endurance run -- daily, fleet-wide, and healing itself an hour later, which is worse than a
# standing red because it looks like somebody's fault and then vanishes. The scan now falls back to
# the newest shelf that HOLDS a corpus, at most seven shelves back, and prints `day_source=` so an
# open-day reading and a fallback reading are told apart; a day a caller NAMES still refuses.
guard rota_declared
path tools/r/rota_declared_witness.rish
tier lap
capability day_shelf
seated 20260906.222120

# status_declared -- a lap's own verdict on its own lap, counted. `session-log-provenance` asks every
# log to finish with `scope` and `status`; measured `20260907.200326`, 21 of 95 logs carried both and
# 73 carried neither. The gap came from an incident: an incense loop cancelled at the moment its lap
# completed left its work staged and whole, claimed no send, and nothing in the record could say
# whether it had been interrupted or had finished and declined. REPORTED, never gated -- a lap killed
# mid-send cannot write a status by definition, so a gate would red hardest on the very laps it makes
# visible. `capability day_shelf` for the same reason its sibling carries it: the scan refuses when
# the open day has no shelf yet, which is honest rather than a fault.
guard status_declared
path tools/s/status_declared_witness.rish
tier lap
capability day_shelf
seated 20260907.200326

# fold_shelf_link_repoint -- the guard that names a repair can now make it. A shelf sits a level
# below the pin it was folded out of, so every climbing link must gain a level, and NOTHING writes
# these shelves -- a hand writes each one. Four different hands missed it in the week to
# `20260907`, each repairing by hand. The scan already computed the corrected path and only ever
# proposes one it has proven present on disk; this applies exactly that column, leaves a dead link
# for a reader, and writes through the original inode so a mode survives.
guard fold_shelf_link_repoint
path tools/f/fold_shelf_link_repoint_witness.rish
tier lap
seated 20260907.212612

# itinerary_account_shelf -- the shelf nothing wrote now has a writer, so its sibling above is the
# repair and this is the prevention. Every send folds a ship's live-front account one directory
# deeper, and every relative link in the block must gain a level. The filter performing exactly that
# rewrite has been general since `20260908.104232`, when its own header was widened to spell this
# invocation -- and the fault fired again the same day, from more than one ship. Measured
# `20260909`: 208 account shelves stand, all hand-written, and NOTHING under `tools/` wrote one.
# The fault was never an absent tool; it was a tool nothing calls. Its REDS sibling refuses to invent
# a header because a REDS shelf's header is judgment; an account shelf's header is a seat, a stamp,
# and one sentence that never varies, so this writer generates it rather than asking for it.
guard itinerary_account_shelf
path tools/i/itinerary_account_shelf_witness.rish
tier lap   # one control in a throwaway pen plus two readings of the standing shelves, measured 1s
seated 20260909.002517

# loom_trend -- the reader the loom line was promised and never had. `session-logs.md` records a
# measurement on a `loom` line *so a future Loom-aware reader can trend them across logs*; measured
# `20260907.222830`, 3,426 measurements stood across 1,577 logs and NOTHING read one. The convention
# was healthy -- 115 of 116 logs that day carried a loom line -- so this is a reader rather than a
# census. Its first question had been unanswerable: the roster grew 17 guards -> 173 while its wall
# time FELL.
guard loom_trend
path tools/l/loom_trend_witness.rish
tier lap
seated 20260907.222830

# convergence_census -- the tree's own idempotence claim, counted. `what-brix-infuse-is` states
# `infusion(world') -> world'`, and a tool that fails it corrupts on its SECOND run, which is what an
# unattended loop performs at three in the morning. Re-run `20260908.190452`: **10 candidates, 3
# proven**, and the census's own header names some of the ten as libraries or a remote-reading scan
# rather than writers. REPORTED, never gated -- a one-shot publisher has nothing to converge and no
# reading here tells it from a tool that never checked. The denominator was wrong FIVE times: 392
# counting pen writes, 1200 when the census read its own bound as a measurement, 30 counting
# controls, 7 counting non-writers, and 10 admitting the exec-bit write idiom it had been blind to.
# A count this unstable is why the seat's own page says to RUN it rather than read it.
#
# AND THE NUMERATOR WAS WRONG ONCE, in the other direction (`20260908.190452`): it greped every
# tracked path holding the tool's stem, the tool ITSELF included, on any line comment or not, so a
# tool writing `Idempotent: a second run changes nothing` in its own header certified itself. Two
# of the five did exactly that. The census header had stated the rule correctly all along --
# *inside a check rather than a comment* -- and nothing read it. Proven fell to **3 of 10**, and
# the control runs the elder numerator over the same plant to prove the repair moved something.
#
# AND THEN THE NUMERATOR GREW A SECOND SOURCE (`20260908.215031`), because a proof that runs every
# lap was invisible to it. `convergence_tree_prove_witness.rish`, rostered below, proves
# `tools/fixtures/r/reds_ledger_headline_write.sh` converges -- the writer `tools/hooks/pre-commit`
# runs on EVERY commit -- and that tool stood in the unproven column, since only a SIBLING
# assertion counted. A tool is proven now when a runner that is not the tool names it and a
# convergence prover on one non-comment line, and the split is printed:
# `proven_by_sibling_assertion` says a hand wrote a check, `proven_by_prover_run` says somebody ran
# the question. Proven reads **4 of 10**. Six control legs prove it from both sides, the elder
# numerator among them.
guard convergence_census
path tools/c/convergence_census_witness.rish
tier lap
seated 20260907.234808

# convergence_prove -- one prover where 27 hand-written cases were owed. The census found 30 tree
# writers with 3 proving convergence; hand-writing a case into each of the other 27 would be the
# census's own finding performed once per tool. 22 of the 27 share one invocation shape, a single
# file path, so one prover answers for all. Three verdicts, and `inert` is the load-bearing one: a
# sample that triggers no work is the quiet way a convergence proof lies.
guard convergence_prove
path tools/c/convergence_prove_witness.rish
tier lap
seated 20260908.005205

# convergence_tree_prove -- the nine of ten candidates that answer to a flag rather than a path.
# `convergence_prove` invokes a tool with ONE path, and nine of the ten the census finds
# are whole-tree operators driven by `apply`, `write`, or `--check`, so no prover could reach them
# and the column read whether somebody wrote a check rather than whether the tool converges. Here
# the subject is a repository: the pen is a `git worktree` checkout of HEAD, the comparison is
# `git write-tree`, so a mode-only change is caught where a line diff sees nothing, and `--perturb`
# hands the pen a tree with work waiting. Six verdicts; `perturb_inert` was earned when this
# guard's own first witness passed a `sed` through Rishi and the escaping ate it.
guard convergence_tree_prove
path tools/c/convergence_tree_prove_witness.rish
tier lap
seated 20260908.204122

# rota_grid -- the twenty paths the council rota rests on, held as a set. The 5 x 3 grid in
# `recursion-prompts/seed/autonomous-loop.seed.md` returns this tree's canon to living awareness on
# a schedule, and its five thresholds are Markdown links while its fifteen seats are backticked bare
# paths no link wall follows. On `20260907` the Earth threshold still routed its reader to the seat
# Earth-Cardinal replaced two days earlier -- both paths resolving, so nothing was broken and no wall
# could see it. Three readings gated at zero: a path the grid names that is missing from disk, a
# threshold missing a modality heading, and a modality section naming a seat other than its own.
guard rota_grid
path tools/r/rota_grid_witness.rish
tier lap
seated 20260907.004329
# capability seated 20260906.140000 (REDS %493). The guard reads the SHIPPED side, so it needs a
# projection at `seed/` -- which is gitignored, built by `tools/s/sow.rish`, and therefore absent
# from every fresh clone. Rostered `tier lap` with no capability it went red on this tree's first
# cold endurance run after landing, and a red guard withholds the roster receipt, so `--scoped` refuses and
# every ship pays a FULL cold endurance run every lap: 778s measured here. The scan's own refusal is right
# (REDS %170) and the roster is where the environment fact belongs -- the same reading the operator
# card already gives an empty `vendor/`. Absence is announced by name rather than made silent.

# publish_template -- the one file this tree ships that a stranger RUNS against their own forge.
# publish-seed.sh is personal by law, so a newcomer inherits every room except a publisher;
# publish-seed.template.sh is that room. Two gates: no literal this field owns survives in it, and
# no stub lacks the GLOW_PROFILE field that answers it. Both fail QUIETLY otherwise -- a leftover
# remote reads as a working default until somebody pushes to a repository that is not theirs.
guard publish_template
path tools/p/publish_template_witness.rish
tier lap
seated 20260906.133118
guard shipped_binary_claim
path tools/s/shipped_binary_claim_witness.rish
seated 20260821.203103

guard tame_style_check
path tools/t/tame_style_check.rish
seated 20260707.164612
tier lap       # authored Rye style, named on touch by the same

# THE THREE TAME GUARDS THE LAW PAGES NAME AND NO ROSTER HEARD, seated 20260908.111848.
# `context/TAME_CORE.md` closes on a section titled "The checkable surface (the audit runs these)"
# and names five tools. Four of the five reached a lap only because somebody rostered them one at a
# time for their own reasons; the fifth, `rune_assert_sweep`, was rostered by nobody. The runner the
# page names for all five -- "the TAME Guidance Audit Quest" -- appears in exactly one place in the
# living tree, which is the sentence that names it. `context/TAME_GUIDANCE.md`'s own lint table
# names two more, `opening_lines_witness` and `tame-check`, and neither stood here either;
# `tame-check` is the tool that table cites for FIVE separate rules, unqualified assert among them.
#
# ALL THREE ARE GREEN AT HEAD and have been unheard since they were seated -- `tame-check` and
# `opening_lines` since the parity chapter, `rune_assert_sweep` since `20260810`. Nothing was
# broken; nothing was listening either, which is the second half of this file's own opening law.
#
# `unheard_guard` COUNTED THEM AND COULD NOT NAME THEM. All three stand inside its reading -- one
# in `unheard`, two in the 405 `unnamed_runners` -- under ceilings of 1,108 and 405. An aggregate
# ratchet answers "how many runners does no lap reach"; it cannot answer "is the tool this law page
# declares as its checkable surface one of them". Two questions, one number, and only the first
# was being asked.
#
# WHAT EACH ONE ACTUALLY READS, written here so a green never reads wider than it is. The TAME
# rooms in `tools/fixtures/t/tame_style_rooms.txt` hold 1,126 authored `.rye` files, 1,073 of which
# import std and are therefore hosted. At the seating: `opening_lines` read 25 hand-named files and
# said "all 25 hosted files"; `tame_check` reads 17; `rune_assert_sweep` gates 12 hand-named cores.
# Each is a regression guard over its own list rather than a reading of the tree, and a hand list
# cannot grow with the tree -- the same shape `tally_caller_map` was repaired for on `20260908`.
# Measured at the seating over the whole roster: 123 of the 1,073 hosted files are missing at least
# one line of the opening triad. Widening the lists is its own lap and was named on the card, not
# taken there: rostering a guard and changing what it reads are two acts, and doing both at once
# means no one can tell which of them moved the number.
#
# THE FIRST OF THE THREE WAS WIDENED `20260908.125347`. `opening_lines_scan.rish` now derives its
# population from the rooms roster rather than an inline list, so it reads 1,073 hosted files rather
# than 24. Its "25" was never a file count at all: the witness printed `length lines result.out`,
# which counted 24 OK lines plus the trailing empty one, and three living pages copied the number
# for a year. Two readings are walls -- `qualified_assert` at zero across all 1,073, and the elder
# twenty-four held clean on all four of their duties, so the widening lowers nothing the narrow list
# had proven. Three are ratchets whose ceilings only fall: 5 files calling `std.debug.print(`, 67
# missing the assert bind, 118 missing the print bind.
#
# `tame_check` TOOK THE SECOND LAP `20260908.133141`, and it found something the first did not.
# Its scan reads the same roster and counts every authored `.rye` rather than only the hosted ones,
# because a tab and a trailing space are faults whichever way a file opens: 1,127 rather than 16.
# Three duties are walls at zero across all of them, each on a looser predicate than the literal it
# replaced -- `std.debug.assert(` at any spacing, `Self = @This()` at any spacing, any tab. The
# fourth found three files the sixteen could not see, and all three are load-bearing: two carry a
# trailing space inside a `\\` multiline string, which is generated Zig and generated Brush rather
# than an untidy source, so they are reported under `trailing_content` and gated nowhere; the third
# is `brushstroke/font8x8_data.rye:35`, the glyph row whose `//` comment IS the space character it
# draws. That one is a ratchet at 1 rather than a named exemption, so naming the glyph in words some
# day lowers the ceiling honestly.
#
# ITS COUNT WAS THE SAME FAULT AS THE FIRST ONE'S. `tame-check` printed `length lines result.out`
# and said "all 17 files" against a list of 16, the seventeenth being the trailing empty line --
# exactly the 25-for-24 above, in the guard standing beside it, found the same day. Two of two
# widened guards had been measuring their own output, which is the argument for widening the third.
#
# `rune_assert_sweep` TOOK THE THIRD LAP `20260908.161651`, and the widening found a boundary rather
# than a count. Its scan derives the same 1,127 authored `.rye`, of which 1,100 carry functions, and
# reads TAME root rule 2 in two halves: 100 function-bearing files carry no `assert(` at all, and
# 101 more assert without naming a single `// invariant:`. Both are ratchets whose ceilings only
# fall, because a wall at zero would ask for two hundred files in one lap. What it found is that the
# elder twelve are NOT a subset of the roster -- `mandate/store.rye`, `mandate/keyed.rye`,
# `kumara/tilak.rye`, `settlement/constellation.rye` and `sundial/sundial.rye` sit in four rooms
# `tame_style_rooms.txt` does not name at all -- so deriving alone would have dropped five gated
# files while the printed population grew a hundredfold. The twelve keep a wall of their own at
# zero, each printed by name.
#
# AND ITS OWN HEADER NAMED A READING IT NEVER MADE, caught `20260908.170042` on the air rota --
# the row that feels along a boundary and presses each post. The scan opened by saying it counts
# "at least one `assert(`, each preceded by a `// invariant:` comment. This scan counts both," and
# both of its readings were FILE questions: does this source assert at all, does it name an
# invariant anywhere. The word the rule turns on is EACH, and no instrument in this tree read it.
# `unnamed_assert` reads it now: 6,619 of 24,495 asserts across the 859 asserting module sources
# name nothing. So 73 percent of ASSERTS are named, while 899 of the 1,100
# function-bearing files -- 82 percent -- already satisfied the file reading beside it.
#
# THE SPLIT IS WHAT MAKES THE NUMBER MEAN ANYTHING. A `*_witness.rye` asserts about another
# program's OUTPUT, so its 2,588 unnamed asserts are correct practice rather than debt -- reported
# here and gated nowhere. Read whole, `glow/` scores 1.1 percent named and reads as the worst room
# in the tree; read on its modules alone it scores 92 percent, and those 2,331 asserts were its
# witnesses all along. A reading that libels a room is a reading of the pattern rather than of the
# tree, which is `tame_style_app_sites`' own finding one room over on the same day.
#
# THE ELDER READING IS A STRICT SUBSET, proven by plant rather than argued: a file counted by
# `invariant_gap` necessarily holds an assert counted here, so the control's case 3 now watches two
# ratchets move together, and case 3b plants inside a file that already carries a comment, the only
# way to move the newer reading alone. A blank line breaks the pairing and a comment block opening
# on the invariant carries the run beneath it -- both shown from the failing side. Control 24 legs
# -> 37. The reading costs 2.5s of a lap pass, one `awk` per batch carrying a single flag forward,
# never one grep per file.
#
# AND ITS FIRST DRAFT PAID DIFFUSER'S TAX. Three greps per file against 1,127 sources is 3,381
# processes and 19.3s; three list-wide `grep -l` readings plus `comm` set arithmetic is six, and
# returns the same three counts in 237ms. The witness fell 47.4s -> 2.0s with the control included.
#
# `tier lap` for all three, measured on this pier `20260908`: 19,564ms, 401ms, 172ms against a lap
# pass of 1,537s. The first grew 966ms -> 19.6s at the widening, and the scan itself is 1.3s of that
# -- the other 18 is its control, which runs the scan once per planted state across a throwaway pen.
# A control that proves eighteen behaviors is what the widening is worth, and 1.3 percent of a pass
# is what it costs.
guard rune_assert_sweep
path tools/r/rune_assert_sweep.rish
tier lap   # 7.1s (3.0s scan, 4.1s control); 1,127 derived authored files, plus the twelve elder cores by name
seated 20260908.111848

guard rune_assert_arrival
path tools/r/rune_assert_arrival_witness.rish
tier lap   # 20.5s (11s control over real git repositories, 9s replaying the %752 window on real history)
seated 20260916.025734
# LAP RATHER THAN CADENCE, and the budget question is worth stating. This guard proves a tool that
# is reached for only when a sibling ratchet breaches, which argues for the slower clock. It sits on
# the lap clock anyway, because the moment it is consulted is the moment it must work: a finder
# proven once every 58 laps can be broken on exactly the lap somebody needs it, and the breach it
# answers is already costing that lap a red. Its 20.5s stands beside `opening_lines` at 19.6s.

guard opening_lines
path tools/o/opening_lines_witness.rish
tier lap   # 19.6s (1.3s scan, 18s control); the opening triad over 1,073 derived hosted files
seated 20260908.111848

guard tame_check
path tools/t/tame-check.rish
tier lap   # 13.4s (1.0s scan, 12s control); the four textual rules over 1,127 derived authored files
seated 20260908.111848

# AND THE REPAIR OF THOSE THREE WAS HELD BY NOTHING, seated 20260909.152920 on the air rota -- the
# row that feels along a boundary and presses each post to see whether it still stands. The note
# above names the instrument that was missing in its own words: an aggregate ratchet answers "how
# many runners does no lap reach", and cannot answer "is the tool this law page declares as its
# checkable surface one of them". Three tools were seated by hand that day and the answer became
# yes; nothing since then has been able to tell whether it stayed yes. A row added to the table
# tomorrow can be unrostered exactly as those three were, and every standing guard reads green --
# which is a fence post firm to the eye and loose to the hand.
#
# `tame_core_surface` asks that second question directly, three walls at zero over the NINE tools
# `context/TAME_CORE.md` names today: each exists on disk, each carries a `path` row here, and each
# declared Clock matches the roster's own `tier`. A tier absent reads `lap`, which is this file's
# own rule rather than a convenience. Measured at the seating: 9 declared, 281 roster guards, all
# three readings zero -- the hand repair of `20260908.111848` held, and is held now by construction.
#
# WHAT IT DOES NOT READ, said plainly so a green never reads wider than it is: whether a tool reads
# what its Reads column claims, whether the table names every tool it ought to, and whether a
# rostered guard is green. Those are three other questions with three other instruments. This one
# reads the page's own declaration against the roster -- the claim, pressed where the claim is made.
guard tame_core_surface
path tools/t/tame_core_surface_witness.rish
tier lap   # 3.5s (0.2s scan, 3.3s control); the nine tools TAME Core declares, against 281 roster guards
seated 20260909.152920

# A FOURTH LAP, AND IT READS THE ROSTER RATHER THAN THROUGH IT, seated 20260908.184342 on the air
# rota -- the row that feels along a boundary. The three widenings above each derived a guard's
# population from `tools/fixtures/t/tame_style_rooms.txt`, which is the right move and leaves one
# question standing: that file is itself a hand-written list of twenty rooms, and nothing has ever
# measured what it leaves out. A hand-written enumeration standing in for a population reads as
# coverage from every side, which is the shape REDS %532 booked one room over.
#
# THE ROSTER'S OWN HEAD ALREADY NAMES A TWENTY-FIRST ROOM and declines to add it, deliberately and
# with reasons -- and it sizes that deferred lap from ONE advisory ratchet: seven camelCase names,
# six after an inherited-name exemption. The bans half FAILS parity rather than printing a count,
# and nobody had read it over the rooms the roster leaves out. It stands at 736 hits across 113
# files, none in a comment: 718 `std.debug.assert(` in `rye/tests`, twelve `copyForwards` and six
# `copyBackwards` in the three tests named for those primitives. The deferral was sound. Its stated
# size came from the only number anyone had measured, and it was three orders short.
#
# THE REACH, PUBLISHED RATHER THAN REASONED ABOUT: 1,729 tracked authored `.rye`, 1,127 under a
# roster room, 551 outside -- `lotus` 238, `rye` 116, `crypto` 82. `crypto/` is absent on purpose
# and this meter never asks for it; what the meter refuses is that the number was nobody's to say.
# One gate at zero (`phantom_rooms` -- a roster line naming no tracked Rye, the law narrowing in
# silence) and two ceilings that only fall, so a room born tomorrow joins the roster or reds here.
guard tame_reach
path tools/t/tame_reach_witness.rish
tier lap   # 5.0s (0.6s scan, 4.4s control); the roster's reach over 1,729 derived authored files
seated 20260908.184342

# sow -- the projected seat is clean before it is ever public. MOVED TO `tier cadence`
# `20260904` (REDS %412): at 230s it was the single most expensive guard on the roster, and unlike
# the two repaired beside it that cost is INHERENT -- it projects the whole public seed and scrubs
# every name from it, which is real work rather than a fork loop. THAT LAST CLAUSE WAS WRONG, AND
# `20260917.004532` MEASURED IT (REDS %642): most of the cost WAS a fork loop. The projector called
# grep once per candidate for the armor blocks, once for the identity list, and once on each copy
# for an ssh key. Batched, the identity pass over 9,096 candidates reads 0.74s and the armor pass
# 0.24s, against the 103.10s and 56.69s `sow_phase` priced for the same corpus. The projection's
# output is byte-identical -- 8,569 file digests, 239 symlink targets and 9,239 tree entries with
# type and mode, all unchanged. The row stays on `tier cadence` on its OTHER reason, which holds:
# what makes the slower clock honest is what it guards: the publish is custody gate %1, a manual
# act at Keaton's hand, and `publish-seed.sh` runs this witness in its own flow. So no lap can take the act this protects,
# and running it on every lap spent 15% of the pass proving a gate that already stops everyone.
# A tier is a cadence rather than an exemption: it still runs, on the rotation `--cadence-slice`
# turns, and the run card records the clock that heard it.
guard sow
path tools/s/sow_witness.rish
tier cadence
seated 20260823.134057

# sow_phase -- where a publish spends its minutes, read without spending them. The `sow` note above
# records 230s on `20260904`; the same witness read **354.09s** on `20260916`, half again as much,
# and 99.3 percent of that is a SECOND full projection run as duty 2 rather than any proof it
# performs -- the four scans cost 2.39s together. This guard counts that structurally, by call site
# rather than by run, so asking costs two seconds instead of twelve minutes. One ceiling that only
# falls: a publish projects the whole seed twice today, and a third site reds on the lap it lands.
# The seconds themselves stay FREE and ungated, behind `--time`, since they rise with the tree.
guard sow_phase
path tools/s/sow_phase_witness.rish
tier lap   # about 3s -- the scan reads scripts and git history and projects nothing
seated 20260916.121517

guard tool_path
path tools/t/tool_path_witness.rish
seated 20260823.131241

guard tool_letter_room
path tools/t/tool_letter_room_witness.rish
seated 20260907.225349
tier lap
# A hand swept 34 misfiled entries on 20260906.182719 and left no guard; five had regrown a day
# later. tier lap because the fault is born with a FILE, and files are born every lap.
# The reason above stood in the comment and the FIELD stayed empty, so the ratchet counted the
# guard as undeclared and reddened the roster for eight ships. Transcribed rather than decided:
# the tier is the seater's own written word, moved into the line the meter reads.
# And the default cost what a default costs: the scan compared 2,972 filenames by forking
# `printf | cut` per tracked path and `printf | cut | tr` per entry -- about 36,000 processes, 82s
# every lap across eight ships, the slowest guard on the roster. Rewritten `20260908.011500` to
# parameter expansion and one awk pass: 78.8s -> 0.26s, output byte-identical, all 25 planted
# refusals still caught. lap is a cadence this guard can now actually keep.

guard tool_path_repoint
path tools/t/tool_path_repoint_witness.rish
seated 20260823.144100
tier cadence   # tool-path references tree-wide; moves when a tool moves, which is a deliberate act

guard tracked_link
path tools/t/tracked_link_witness.rish
seated 20260823.002511

guard vocabulary_collection
path tools/v/vocabulary_collection_witness.rish
seated 20260821.211943

guard pond_display_gate
path tools/p/pond_display_gate_witness.rish
seated 20260823.171000

guard document_mirror
path tools/d/document_mirror_witness.rish
seated 20260823.173634

guard readme_reach
path tools/r/readme_reach_witness.rish
seated 20260823.184309

guard kyri_receipt
path tools/k/kyri_receipt_witness.rish
seated 20260823.212046

guard loop_prompt_parse
path tools/l/loop_prompt_parse_witness.rish
seated 20260824.025704

guard dated_spelling
path tools/d/dated_spelling_witness.rish
seated 20260824.035758
tier cadence   # the dated-stamp spelling census across 4,705 living sources

guard dated_pattern
path tools/gen/chapter/dated_pattern_witness.rish
seated 20260824.040502

guard index_fold
path tools/i/index_fold_witness.rish
seated 20260824.052329

guard caravan_ladder_roster
path tools/ca/caravan_ladder_roster_witness.rish
seated 20260824.062207

guard mycelium_map_roster
path tools/m/mycelium_map_roster_witness.rish
seated 20260824.071500

guard living_docs_roster
path tools/l/living_docs_roster_witness.rish
seated 20260824.075409

guard image_module_roster
path tools/i/image_module_roster_witness.rish
seated 20260824.082436

guard lotus_module_roster
path tools/l/lotus_module_roster_witness.rish
seated 20260824.091754

guard chapter_allocator
path tools/c/chapter_allocator_witness.rish
seated 20260908.083050
tier lap   # two walls at zero over 1,958 authored Rye sources; a lap that writes Rye can move it

guard crushed_index
path tools/cr/crushed_index_witness.rish
seated 20260906.091330
tier lap   # the shipping shelf's indexes, which every prose lap can move

guard crypto_count_guard
path tools/cr/crypto_count_guard_witness.rish
seated 20260824.095920

guard crypto_module_roster
path tools/cr/crypto_module_roster_witness.rish
seated 20260824.095920

guard constel_module_roster
path tools/co/constel_module_roster_witness.rish
seated 20260824.104946

guard sow_lock
path tools/s/sow_lock_witness.rish
seated 20260824.104946

guard sow_project
path tools/s/sow_project_witness.rish
seated 20260917.004532
tier lap   # 28 branches of the seed projection on a pen field, about five seconds; what the
           # public seed contains is worth reading on every pass rather than on a slow clock

guard rule_twin
path tools/r/rule_twin_witness.rish
gate %7
seated 20260824.112806

# law_tool_citation -- the room every ship reads at its cold open names its instruments in prose,
# and half of those names were outside every guard. Measured 20260910.170340 on the air rota, the
# row that feels along a boundary: `.claude/rules/` prints 99 distinct `tools/` file paths across
# 54 pages, and 50 of them appear nowhere in the room as a Markdown link. `tracked_link` reads
# links, so a backticked name is invisible to it; `docs_command_path` reads a path a page tells a
# reader to RUN and passes an absent one FREE on purpose, because tree-wide such a path is usually
# one the reader is being asked to create. That reasoning is right for a tutorial and wrong for a
# law page, where naming a guard is a promise about a file that already exists -- so the SCOPE is
# what makes a gate honest here, and the wall stands at zero over this one room.
#
# It asks the REPOSITORY rather than the filesystem, which is the lesson tracked_link was built
# after: a file present on one machine and absent from every clone reads fine to `[ -e ]`.
#
# Two readings are printed and gated nowhere. `cited_bare` is the reason the gate exists rather
# than a fault. `runners_unrostered` belongs to `witness_reach`, which traces reachability
# tree-wide; one is standing today, `tools/b/bat_fleet_witness.rish`, cited as a swept file rather
# than as a proof. An absent roster reads `unread`, never zero. The `.cursor/rules/*.mdc` twin room
# is read the same way and reported, since `rule_twin` above stands behind gate %7 and a second
# gate there would push a lane into the merge that gate reserves.
guard law_tool_citation
path tools/l/law_tool_citation_witness.rish
seated 20260910.180224
tier lap   # 5.1s (2.0s scan, 3.2s control); 99 cited paths over 54 law pages, 39 readings on real git pens

# ascii_document -- the ASCII-first law names THREE subjects and only two were measured over their
# whole subject. Comments have two meters over every tracked source; commit messages have none and
# want none (0 of the last 400 bodies carry a byte above 0x7F, measured 20260906.131411); documents,
# the subject the law names FIRST, had `living_card_ascii_scan.sh` -- two pins read hard and six as
# advisory, EIGHT files of 5,529. The gap sat where it cost most: 63 of the 104 rule pages an editor
# reads as law, the pages carrying the ASCII-first law itself, held 859 non-ASCII characters, every
# one a form the rule's own substitution table spells. Swept to zero on the lap this row was
# written, the sweep proven to have moved no prose by re-deriving each page from its committed
# bytes. THE WALL REACHED PAST THE LAW on 20260910: `docs/*.md`, the compressor shelf MAP.md sends
# a newcomer to, carried 452 characters across 12 of its 15 pages -- 14 percent of the tree's whole
# remaining ratchet in one small room, held by nothing, since no rule page cites a compressor and
# the derived roster could therefore never reach it. Widening found what only widening could: three
# readers each answered *is this page walled?* by TYPING the roster again, so the first widened run
# priced one page twice. The set is enumerated once now, from the tracked listing; and the wall and
# the SEED are two variables, because a rule room's citations are canon where a teaching room's
# links are links. `tier lap`: 2s for the control's pens and 4s for the tree.
guard ascii_document
path tools/a/ascii_document_witness.rish
tier lap
seated 20260906.133344

# ascii_resident_agree -- the tree's most expensive scan, priced by building the cheap one beside it.
# `external-research/date/20260910/20260910-072912_the-seconds-this-pier-actually-spends.md` projected that a
# resident reader -- one process that opens the tree once and answers many questions -- would remove
# the kernel half of this pier's assurance bill, and named its own falsifier: reimplement one
# representative guard as a single process and compare CPU seconds. Run `20260910.083451`, the shell
# scan cost 25.99 CPU seconds over 7,741 processes and the one-process probe cost 1.49 over 6 -- a
# factor of 17.4 whole, 466 in the kernel, and 8.8 in USER time, which is the half the projection had
# called real reading. THIS GUARD HOLDS THE AGREEMENT RATHER THAN THE FIGURE: the seconds are free and
# move with the pier's load, while the claim that the two readers answer one question is checkable and
# is the only thing that makes the figure mean anything. The two agreed on the living tree at the
# first run and PARTED the moment a planted page broke the wall -- `enforce=broken` against
# `enforce=failed`, no detail lines, exit 0 against exit 1 -- so the pen carries the refusals.
# `tier lap`: 10 pens plus one reading of this tree, about 8s.
guard ascii_resident_agree
path tools/a/ascii_resident_agree_witness.rish
tier lap
seated 20260910.083451

guard width_check
path tools/w/width-check.rish
seated 20260620.210812
tier lap       # authored Rye contracts, named on touch by the TAME Definition of Done

guard declared_ceiling
path tools/d/declared_ceiling_witness.rish
seated 20260824.121445
tier lap       # the gate that stops a ratchet being loosened -- a lap can raise one

guard declared_model
path tools/d/declared_model_witness.rish
seated 20260824.161948

guard qa_report_card
path tools/q/qa_report_card_witness.rish
seated 20260824.161948

# qa_genre_census -- the whole program population read at BOTH settings, which settles a standing
# ask the card itself had raised and left at a sample of forty. qa_report_card.sh reads every
# program HEAD at Door, while Gauge's own setting table names a witness header, a scan comment and
# a ledger row Meter -- uncapped on register, grade and cross-references, because refusal is the
# subject. The card already derives program_genre from the basename, prints program_genre_meter
# named for a witness and a scan, and prints meter_shadow beside every composite; all of it is
# reported and none of it is scored.
# THE COUNT, 20260917 over all 2,914 tracked files the genre table reaches: Door carries 43
# distinct composites across a 44-point span, 50 to 94, with 1,176 below B -- 906 witnesses, 147
# scans, 123 controls. The Meter shadow carries ONE value, 94, on every last file. At Meter both
# of the card's counted readings are freed to 100, so the composite is fixed by Truth and Service
# alone, and the counted half of Truth reads 100 across all 2,914. Every figure here is FREE and
# rises with the tree; the scan prints its own.
# SO THE ASK ANSWERS ITSELF: Door is not mis-grading these heads, Door is the only reading that
# tells them apart. The card refused exactly this shape once already, one family over, when a
# notation carrying a comment block was refused Meter and read at Field for the same constant
# (REDS %402). The program family carries it at 153 times the population and nothing had counted it.
# WHAT IS GATED: the census's own integrity -- every candidate answered, the rows balancing against
# the candidate list, and genre_unnamed under a ceiling that only falls, at 1 today. The below-B
# counts are REPORTED: a grade is not a gate, and a head grows as it teaches more, so a wall there
# would red a lane for writing a longer header.
# The genre is ASKED OF THE CARD rather than copied, since a roster spelled twice is a roster that
# can disagree with itself. 27 control legs, every refusal planted and then lifted, two mutations
# bitten and a third named as neutral rather than claimed as a bite.
guard qa_genre_census
path tools/q/qa_genre_census_witness.rish
seated 20260917.000933
tier cadence   # 2,914 real card runs, 14m35s of wall on a pier carrying eight ships

guard index_row_bound
path tools/in/index_row_bound_witness.rish
seated 20260824.183000

guard dayshelf_merge
path tools/d/dayshelf_merge_witness.rish
tier lap   # 2s on metal, and the fault it prevents is re-earned by every pull, so it belongs on the clock that follows a pull
seated 20260908.054012

guard log_has_a_row
path tools/l/log_has_a_row_witness.rish
seated 20260824.201500

guard rishi_bare_path
path tools/r/rishi_bare_path_witness.rish
seated 20260825.004500

guard fascia_home_link
path tools/f/fascia_home_link_witness.rish
seated 20260825.004500

guard ascii_comment
path tools/as/ascii_comment_witness.rish
seated 20260825.011000
tier cadence   # non-ASCII in every comment, held as a ratchet that only falls

# spoken_ascii -- the ASCII-first law names three subjects, and the tree's guards speak in a fourth.
# Both comment meters refuse to count "program content", and `ascii_comment_witness` gives the
# reason in its own GREEN line: converting a string "changes what a program prints". That one
# exclusion answers two questions. A heredoc body is what a program feeds ONWARD, and converting it
# changes behavior -- rightly excluded, here as there. A `say` line is what a program says TO A
# PERSON, on their terminal and into `session-output/`, and converting it changes register, which is
# what the law governs. Measured 20260907.075500 over the same 2,387 `.rish` sources the comment
# meter opens, under the same law, on the same run: 505 characters counted and held, against 11,154
# counted by nothing -- and 11,154 is very nearly the 10,468 the comment surface itself stood at on
# the day its own meter was built. Same files, same law, one sweep less of attention. `tier cadence`:
# 10s, and it reads 2,387 files where a lap touches a handful.
guard spoken_ascii
path tools/s/spoken_ascii_witness.rish
seated 20260907.075500
tier cadence   # non-ASCII in what a Rishi guard says out loud, a ratchet that only falls

# rye_spoken_ascii -- the sibling above proved the argument and stopped at one language. A heredoc
# body is what a program feeds ONWARD; a `say` line is what it says TO A PERSON, and converting that
# changes register, which is what the ASCII-first law governs. Rye speaks through `print`, and the
# Rye comment meter declines program content for that same one reason -- so the tree held a guard
# over what a Rishi program says and none over what a Rye program says, in the language it writes
# most of its own modules in. Measured 20260908.214712 over the same 1,730 tracked `.rye` sources
# the comment meter opens: 3,794 characters counted and held there, against 3,996 counted by
# nothing, in 809 of those files -- 45% of the tree's authored Rye printing a character to a
# person's terminal that no instrument read. A LINE-ORIENTED READING WOULD HAVE MISSED HALF ITS OWN
# SUBJECT: this tree writes a claim line as a chain of literals joined by `++` across four or five
# lines, and 2,480 characters stand on lines holding the call against 2,235 on its continuations, so
# the scan reads parenthesis depth outside string literals. The seating lap swept its own lane to
# zero -- mantra and tally, 25 files, 101 characters -- and set the ceiling at what remained.
# `tier cadence`: it reads 1,730 files where a lap touches a handful.
guard rye_spoken_ascii
path tools/r/rye_spoken_ascii_witness.rish
seated 20260908.214712
tier cadence   # non-ASCII in what a Rye program prints, a ratchet that only falls

# rye_written_ascii -- every meter in this family reads what a source COMMENTS or what a program
# PRINTS. None read what a program ASSEMBLES INTO A BUFFER and hands onward -- to a file, a wire
# frame, or a generated page -- and that is the surface the ASCII-first law was born on, since
# REDS %83 was a persisted document a tool rewrote into mojibake. THE EXCLUSION COVERED TWO
# POPULATIONS: the sibling above steps past every literal outside a `print` call and gives its
# reason -- a module testing a decoder must contain the character it decodes -- which is exactly
# right about a decoder's own fixture and says nothing about a header a program writes into every
# artifact it pours. This meter reads the half that reason does not cover: a `bufPrint`,
# `bufPrintZ`, `allocPrint` or `writeFile` region, by parenthesis depth outside string literals,
# every call name read as a WHOLE identifier -- the trap is sharper here than in the sibling,
# because the names this one opens on END IN the name that one opens on. Measured 20260911.215028
# over the same 1,743 tracked `.rye` sources: 292 characters in 49 files, ALL 292 forms the rule's
# own table spells. TWO stood in a file that also writes one, and both were repaired on the seating
# lap -- amphora/src/main.rye assembles the header inside every sealed vessel this tree pours, and
# tools/rye/enrich/enrich_file.rye assembles Markdown and writes it into documentation pages, a
# generator feeding the gated document meter from outside every meter. So the second reading is a
# WALL at zero rather than a ratchet, and the first fell to 290 in the same commit. The persisted
# reading is a file-level PROXY named as one in the scan's header, since where a buffer lands is
# dataflow a scanner does not follow.
# `tier cadence`: it reads 1,743 files where a lap touches a handful.
guard rye_written_ascii
path tools/r/rye_written_ascii_witness.rish
seated 20260911.215028
tier cadence   # non-ASCII in what a Rye program assembles, a ratchet and a wall at zero

# shell_written_ascii -- the other half of the subject above, and the half its own header priced
# from the two fixture files it could see: "the residue is 2 characters in 2 files." Measured
# 20260912.023000 over 3,556 tracked `.sh` and `.rish` sources, the real population is 1,419
# characters across 122 files -- seven hundred times the guess, and 1,417 of them stand in
# tools/equinox/almanac/ generators whose heredocs append entries to
# rye-learning-process/GLOW_ALMANAC.md, the page a hand swept from 1,437 to zero on 20260910.042550.
# THREE SIBLINGS NAMED THIS BODY AND ALL STEPPED PAST IT, each on the one reason that converting a
# heredoc changes what a program feeds onward. That reason is right about a heredoc a PARSER
# consumes and covers a second population it was never asked about: a heredoc handed to an appender
# that writes Markdown is prose. So this meter CLASSIFIES rather than excluding -- `program` names
# the subset a bare interpreter consumes as code, read from the command word standing alone or
# carrying -c/-f rather than a script path, and `sweepable` is the remainder a lane may convert.
# NOTHING IS WALLED and `written` ratchets: the almanac stubs are DATED generators whose engine
# exits 0 on a seat already present, so they are inert rather than pending, and whether a dated
# generator is swept, re-poured or retired governs a family and wants Keaton's word. What it still
# cannot see is a `printf` argument, which needs to know whether a quote sits inside another quote,
# so it undercounts on purpose the way all four siblings do.
# `tier cadence`: it reads 3,556 files where a lap touches a handful.
guard shell_written_ascii
path tools/s/shell_written_ascii_witness.rish
seated 20260912.023000
tier cadence   # non-ASCII in what a shell script assembles in a heredoc, a ratchet at 1,419

# shell_emit_ascii -- the sixth ASCII subject, and the one the fifth NAMED and never priced. That
# meter's own closing line says what it cannot see: "a `printf` argument assembling one line",
# because finding it needs to know whether a quote sits inside another quote. Measured
# 20260916.183114 over the same 3,682 tracked sources, the residue outside heredoc bodies and
# outside `#` comments is 11,030 characters; 10,416 sit in `.rish` where the spoken meter already
# counts them, and the `.sh` half is 614 that NOTHING reads -- `echo` 360 against `printf` 113, so
# the family named the smaller half of its own gap by three to one. THE STRUCTURAL REASON no
# sharper reading of the five would have found it: a POSIX `.sh` file has no `say` verb, since
# `say` is Rishi's, so the spoken meter opens `.rish` alone and the whole `.sh` emit surface stands
# outside the family BY CONSTRUCTION. Every file this meter names is `.sh`; the split is measured
# each run. It reads an emit's OPERAND REGION by a quote walk -- a `#` inside a string does not end
# the line, a `>` inside a quoted argument is prose rather than a redirect -- and a command begins
# after an unquoted separator, so an emit after a pipe, inside `$( )`, or after `then` is read; the
# keyword step alone carries 21 characters. 509 across 77 files, 464 spoken and 45 written by the
# line's own redirect, 412 in forms the rule's table spells. IT RATCHETS RATHER THAN WALLS for a
# population that must never be swept: tools/fixtures/p/prin_matrix.sh draws a terminal frame out of
# box-drawing characters, which is the rule's own named exception, so only `emit_named` is a lane's
# to act on. 41 legs, four mutations bitten, the keyword step and the quote walk each proven
# load-bearing by planting their absence.
# `tier cadence`: it reads 3,682 files where a lap touches a handful.
guard shell_emit_ascii
path tools/s/shell_emit_ascii_witness.rish
seated 20260916.183114
tier cadence   # non-ASCII in what a shell script speaks through echo or printf, a ratchet at 509

# listing_census -- whether a scan that CAPS a listing says a row was dropped. The nine ASCII-first
# meters above all answered `--list` by piping their report through `sort -rn | head -40`, and none
# of the eleven listings said so: 2,972 of 3,321 rows went unprinted in silence, 89 percent, while
# seven of the nine headers promised `--list   # name each file and its count`. An absent row and a
# dropped row then read the same, and the reader hurt worst is the one asking the narrowest
# question -- eighteen non-ASCII characters stood in five tools/am/amphora_* witnesses and this
# lane, reading the listing and seeing no amphora row, took it for a clean room.
# THE CLASS FIRED TWICE IN ONE DAY, which is what earns a meter: petrichor's seed_link_scan.sh
# printed a ratchet site only while its counter stood at or under five, so 847 sites showed five
# (20260916.210332). A lantern that fires twice becomes a loom.
# A CAP is a sort piped into head, printed rather than redirected, taking more than one row. Each
# exclusion is a boundary drawn where counting would be wrong: `head -1` is a scalar read, a
# redirected cap bounds an INPUT set the way TAME asks every collection to, and a comment naming
# the shape is not the shape -- without that last one the meter refuses its own header first. A
# CENSUS is an EMIT carrying a key ending `hidden=` or `shown=`; counting the assignments beneath
# it read one honest block as three, so a verbose file earned slack.
# The nine ASCII scans are a WALL at zero, repaired on the seating lap; 13 caps across nine files in
# nine other lanes RATCHET under a ceiling that only falls. The claim that opened the lap said four,
# counted by a grep wanting digits after `head`; the meter reads `head -n "$var"` too, and the
# correction rides with the reading. 38 legs, five mutations bitten, the wall shown red and green on
# metal by planting a cap in glow_comment_ascii_scan.sh and lifting it.
guard listing_census
path tools/l/listing_census_witness.rish
seated 20260916.214947
tier lap   # 378 scan sources, one awk pass each, under a second

guard comment_citation
path tools/co/comment_citation_witness.rish
seated 20260825.052000
tier cadence   # citations inside every tracked comment; a lap touches a handful of files, this reads thousands

guard witness_reach
path tools/w/witness_reach_witness.rish
seated 20260825.092953
tier cadence   # which witnesses reach what, tree-wide; moves when a witness is written or retired

guard caravan_suite
path tools/ca/caravan_suite_witness.rish
tier cadence
seated 20260825.092953

guard caravan_roster_bijection
path tools/ca/caravan_roster_bijection_witness.rish
seated 20260831.071031

guard crypto_suite
path tools/cr/crypto_suite_witness.rish
tier cadence
seated 20260825.110922

guard ales_roster
path tools/al/ales_roster_witness.rish
seated 20260825.132121

guard ales_suite
path tools/al/ales_suite_witness.rish
tier cadence
seated 20260825.132121

guard season_leaf_choir
path tools/gen/chapter/season_leaf_choir_witness.rish
tier cadence
seated 20260825.234902

guard remember_git_nib
path tools/r/remember_git_nib_witness.rish
seated 20260825.183336

guard remember_git_nib_write
path tools/r/remember_git_nib_write_witness.rish
seated 20260906.050718
tier lap       # 34 pen cases in 0.6s; the writer beside the guard above, standing free of it

guard reds_ledger_headline
path tools/r/reds_ledger_headline_witness.rish
seated 20260825.183336

guard reds_row_present
path tools/r/reds_row_present_witness.rish
seated 20260825.213128
tier lap       # the ledger a lap books rows into

# rishi_quoted_program -- Rishi leaves `\"` in a string literal alone, so the backslash reaches
# `sh`; inside a single-quoted region -- how every awk program in this tree is written -- both
# characters land in the program text and awk REFUSES a string literal spelled that way. It
# writes a syntax error to stderr and prints nothing, a `wc -l` downstream answers 0, and the
# pipeline's exit status is the last command's, so `.ok` reads true. A guard above it reads a
# silence it never earned. Three stood on exactly that (the row of `20260910.031353`), and one
# of them is `reds_row_present` directly above: its gap arithmetic answered 0 missing of 7 where
# 5 were missing, so "every spine row found" was a sentence no run had checked. `gen_home` was
# rostered `tier lap` and left a planted stray desk GREEN on metal; `bricks_exist` read 0 of 20
# brick paths while saying all 20 resolve. All three repaired by handing the literal in with
# `awk -v k=file` rather than spelling it inside the program.
#
# WHICH INTERPRETER REFUSES WAS MEASURED, never assumed: grep and sed read `\"` as a plain `"`
# and match correctly, and awk warns inside a regex and works. Only the awk STRING LITERAL is
# fatal, so `awk_fatal` is walled at zero and the 18 tolerated grep/sed/printf sites stand under
# a ceiling that only falls. A wall drawn around the whole spelling would have refused eighteen
# living witnesses that work.
#
# One `grep -l` narrows 2,458 sources to the handful that could carry the spelling before any
# per-file walk -- 19.9s -> 0.63s for identical counts, Diffuser's tax one room over. The
# control proves 16 behaviors in a pen, every refusal planted then lifted, and its first two
# legs ask AWK ITSELF whether the plant breaks and the repair reads, so the fault is proven real
# rather than pattern-matched. 1.5s including the control.
guard rishi_quoted_program
path tools/r/rishi_quoted_program_witness.rish
seated 20260910.031353
tier lap       # 1.5s; it walls the class that made three green guards vacuous

guard shell_dialect
path tools/s/shell_dialect_witness.rish
seated 20260826.001630

guard shell_dialect_touch
path tools/s/shell_dialect_touch_witness.rish
tier lap
seated 20260908.040446
# THE SAME LAW, ASKED WHILE THE HAND IS STILL HERE. `shell_dialect` above reads every tracked shell
# source and holds five GNU-only families at a ceiling of zero. It is right, and it arrives late: it
# runs inside a roster pass of roughly half an hour, so the lap that breaks a family is structurally
# never the lap that hears it. Measured `20260908` -- two `sed -i` plants landed at 01:59 and were
# first heard at ANOTHER ship's cold open at 02:42, 43 minutes and one ship later, after which two
# hands cured the same two sites in parallel and one cure withdrew whole.
#
# This is rule seven of tools/hooks/pre-commit: the same five families, read off the INDEX at commit
# time. It gates only the families whose tree ceiling stands at ZERO, which it reads from the elder
# rather than respelling -- a ceiling of zero means no lawful site exists, so every staged hit is a
# rise and no author is refused for a line somebody else wrote. Replayed against commit 0f48f2c85 it
# answers `staged_gated_sites=2` and names both the file and the line.

guard one_clock
path tools/o/one_clock_witness.rish
seated 20260826.075023

guard upstream_shape
path tools/u/upstream_shape_witness.rish
seated 20260826.010645
tier cadence   # the shape of upstream refs; moves on a remote change rather than on a lap

guard ryekey
path tools/r/ryekey_witness.rish
seated 20260826.092732
tier lap       # authored Rye keys, same clause

guard rye_key
path tools/r/rye_key_witness.rish
seated 20260916.101500
tier lap       # the same key asked without building -- one computation, so it cannot drift

guard skate_macos_choice
path tools/s/skate_macos_choice_witness.rish
seated 20260826.164751

guard reds_fold
path tools/r/reds_fold_witness.rish
seated 20260826.175633

# scope_trace seated 20260907.081500. THE MAP IS THE ONE BEHAVIORAL CLAIM IN THIS SYSTEM NOBODY
# CHECKED. tools/fixtures/s/standing_equipment_scope_map.sh tells a `--scoped` pass which guards a
# change can reach, and a row naming LESS than its guard gates SKIPS REAL WORK -- the map's own
# header says so, and thirteen such rows were repaired by hand on 20260906. This rung runs a guard
# under `strace -f -y -e trace=openat` and matches its observed read set against its row with the
# same tools/fixtures/s/scope_match.sh the runner skips by, so a gap named here is a skip the runner
# would actually take. Two rows were repaired by running it rather than by reading them the day it
# was seated: `radiant_negation` read 142 files against a row reaching 89, missing `.claude/rules/`
# -- its ENFORCE roster, while the rooms the row DID name are its ADVISORY tier -- and
# `tally_roster` read 844 against 28, missing the build edge. Both reproduce
# external-research/20260907-061951 file for file, on a second tree.
#
# WHAT IS GATED is the CONTROL, never a live gap. A row follows a guard's GATED readings and
# observation cannot tell a gated read from an advisory one, so a gap is a word a hand considers
# rather than a defect this rung declares. Union, never subtract.
#
# `capability trace_instrument` because strace is Linux-only; a bench without it reads its own name
# in `skipped_capability` rather than reddening for a kernel it does not have.
#
# `tier lap` at 4s: the control traces a pen guard whose whole read set is four files, so it costs
# nothing near the 58s a real trace of `tally_roster` takes. Tracing a REAL guard stays a hand's
# command, since its cost is the guard's own cost times 1.1x to 6.7x.
guard scope_trace
path tools/s/scope_trace_witness.rish
tier lap
capability trace_instrument
seated 20260907.081500

guard tally_roster
path tools/t/tally_roster_witness.rish
seated 20260826.224730

guard tally_receipt_offer_bounds
path tools/t/tally_receipt_offer_bounds_witness.rish
tier lap
seated 20260913.171959

# Seated 20260915.180000. A named error says what KIND of refusal happened and
# nothing else, so twelve bounded receipt fields all refused as one word `TooLong`
# and acceptance case 6 -- refuse by the missing field's own name -- could not be
# written. This guard holds the refusal record that carries field, value, ceiling,
# unit, and reason, and holds the record and the elder named error to ONE reading,
# so the two faces of a refusal cannot drift. Its control plants five drifts a
# reason-only test would pass: a wrong field name, a crossed error mapping, a
# crossed unit, a reordered line, and a dropped field-name bound. 14s on this pier.
guard tally_receipt_refusal
path tools/t/tally_receipt_refusal_witness.rish
tier lap
seated 20260915.180000

# Seated 20260908.090000. The guard stood UNHEARD: no roster row, and its one caller
# `tools/t/tensegral_arc_iv_witness.rish` is unrostered too, so nothing on any clock ran it. Widened
# the same lap from a hand-listed 19 paths tested with `-e` to a population derived from the index
# -- every tracked symlink resolving under tally/, 69 on this date -- with the named 19 kept beside
# it, since a retargeted link leaves a derived population in silence. `tier lap`: the reading is one
# `git ls-files` and 69 `readlink -f` calls, well under a second.
# Seated 20260917.024441. REDS %767 recorded its own gap in its second field: nothing
# in this tree asked whether every field a contract publishes carries a ceiling that
# contract declares. Four receipt fields refused at max_identifier_bytes=96, the number
# the contract named for a POPULATION and for none of them, so a reader met ceiling=96
# on a refusal line and could not find it. This guard holds three documents to one
# answer -- the contract's ceiling table, the bounds module's constants, and every
# ceiling site in the admission module -- gating unnamed_ceiling and value_disagrees at
# zero, and reporting population_undeclared, borrowed_rows and row_unenforced, each a
# thing a lawful contract does today. Three awk passes over four files, no build.
guard receipt_contract_ceiling
path tools/r/receipt_contract_ceiling_witness.rish
tier lap
seated 20260917.024441

# Seated 20260917.090914. The accepted receipt contract carries one falsifier whose second clause
# is a condition on code -- either product importing the other's projection type makes the contract
# wrong -- and acceptance case 8 restates it as a build condition. Measured this lap, that sentence
# stood on no instrument: `linengrow/` and `dimeroll/` named each other zero times across 175
# tracked Rye sources, so the boundary read clean and nothing held it clean. The contract is
# ACCEPTED for implementation, which is when a braid becomes tempting. The guard reads the product
# pair off the contract's OWN residence table -- one room and one projection type per arrow row,
# exactly two wanted -- so a rename on the page moves the guard, and walls `cross_import` and
# `cross_type` at zero while reporting `cross_mention` and `peer_word`, since a module head
# explaining the boundary it keeps is doing the right thing. `tier lap`: 11 seconds of pen and 1 of
# live reading.
guard receipt_product_braid
path tools/r/receipt_product_braid_witness.rish
tier lap
seated 20260917.090914

# Seated 20260918.043437. The receipt contract's Dimeroll projection stood at zero lines: the
# product owing half the falsifier had not begun. `dimeroll/receipt_offer.rye` publishes
# `DimerollReceiptIntake` and `from_snapshot`, reading a settled offer's own fields -- receipt_id,
# product_digest, value_amount, value_unit, and whether it has expired -- and reporting
# recognition_status=unrecognized-offer, journal_entry_count=0. It imports no other room: Zig's
# module boundary refuses an @import reaching outside its root file's directory (REDS %589), so an
# `OfferSnapshot` rather than Mantra's own `ReceiptState` type crosses that boundary, and the
# module head says why. This guard builds the witness, proves the mapping field for field before
# and after expiry against the contract's own fixture values, and reads the product braid guard
# beside it. One `rye build`, one binary run, one awk pass -- no pen, no control.
guard dimeroll_receipt_offer
path tools/d/dimeroll_receipt_offer_witness.rish
tier lap
seated 20260918.043437

# linengrow/receipt_offer.rye is the receipt contract's other projection -- case 2's
# holder-readable view, `ReceiptState -> LinengrowReceipt`, and case 5's Linengrow half
# (expiration moves status alone). Same shape as its Dimeroll sibling above: an
# `OfferSnapshot` crosses the Zig module boundary rather than Mantra's own `ReceiptState`,
# and this guard proves the mapping field for field before and after expiry, then reads
# the product braid guard beside it.
guard linengrow_receipt_offer
path tools/l/linengrow_receipt_offer_witness.rish
tier lap
seated 20260918.100854

# The first receipt's Still rows and accessibility entries share the published
# eleven-field order. The source control plants four drifts on hosts without Swift.
guard receipt_still_order
path tools/r/receipt_still_order_witness.rish
tier lap
seated 20260924.224012

# The receipt contract's last mile: mantra/src/receipt_offer_snapshot.rye carries one replayed
# ReceiptState into each product's own OfferSnapshot, through two renamed symlinks that cross
# Zig's module boundary the same way tally_receipt_offer_bounds.rye and tally_receipt_refusal.rye
# already do in this room. This guard proves acceptance cases 1-3 chained together from ONE
# admitted fact and ONE replay -- rather than three separately-fed fixtures that merely agree --
# both before and after expiry, and reads the product braid guard beside it.
guard mantra_receipt_offer_snapshot
path tools/m/mantra_receipt_offer_snapshot_witness.rish
tier lap
seated 20260918.105100

# Acceptance cases 6 and 7 as CHAIN properties: every required text field, emptied one at a time,
# refuses AND blocks the replay neither product could otherwise project from; a wrong-length
# digest or an invalid signature refuse "false authority" the same way. The admission module's own
# field-level refusal tests already prove the field; this proves the consequence the contract's
# own words ask for.
guard mantra_receipt_offer_refusal_chain
path tools/m/mantra_receipt_offer_refusal_chain_witness.rish
tier lap
seated 20260918.111200

guard tally_caller_map
path tools/t/tally_caller_map_witness.rish
tier lap
seated 20260908.090000

guard socket_dialect
path tools/s/socket_dialect_witness.rish
seated 20260827.004535

guard rish_join_split
path tools/r/rish_join_split_witness.rish
seated 20260827.140328

# The six Glow witnesses nothing reached, seated 20260827.183635 on Keaton's word.
# All 89 glow_*_witness.rish stood off this roster; these six were reached by no runner,
# roster, or suite at all, and they carry the newest rungs. One of the six was RED and had
# been since 20260804 (REDS %299). They run on `cadence` rather than `lap`: each builds real
# plants through the Zig toolchain, and glow_shop_gate_horizon and _pair_faces each take
# minutes. Revisit the tier when the Glow ladder reopens.

guard glow_shop_gate_pair_faces
path tools/g/glow_shop_gate_pair_faces_witness.rish
tier cadence
seated 20260827.183635

guard glow_shop_gate_pair_select
path tools/g/glow_shop_gate_pair_select_witness.rish
tier cadence
seated 20260827.183635

guard glow_shop_gate_horizon
path tools/g/glow_shop_gate_horizon_witness.rish
tier cadence
seated 20260827.183635

guard glow_tally_pair_bound
path tools/g/glow_tally_pair_bound_witness.rish
tier cadence
seated 20260827.183635

guard glow_vane_pair_mirrors
path tools/g/glow_vane_pair_mirrors_witness.rish
tier cadence
seated 20260827.183635

guard glow_compose_after_inc
path tools/g/glow_compose_after_inc_witness.rish
tier cadence
seated 20260827.183635

# The desk corpus's own reach, seated 20260907.005950. glow/gen/ holds 352 generated desks and exactly one
# guard runs any of them -- tools/g/glow_run_desk_witness.rish, 1,133 lines naming 218 desks one at
# a time. A hand-written enumeration standing in for a population drifts the moment the population
# grows, and nothing here could see it: 129 runnable desks are run by nothing, 89 of which pass on
# metal today. This row does not run desks -- it reads them, so it costs 9s where the desk witness
# would cost a quarter hour of Zig builds, and it is what makes rostering that witness honest
# later. `tier lap` because it is file reading, and because the reading it holds is a ratchet that
# only works if it is read every lap.
#
# 20260907.020441 -- the 129 split in two. The witness runs every desk it names with ZERO
# arguments, and tools/g/glow_run_worker.sh permits a sample for 94 stems, 46 of them in this room.
# All 46 are uncovered and none is covered, so the boundary was the instrument's shape rather than
# anyone's oversight. Two ratchets now: 83 bare desks that cost one block each, 46 sample-taking
# desks that cost a chosen argument first. The sum is derived from them, so it cannot drift.
#
# 20260907.122532 -- the bare ratchet became a gate at zero, because `covered` learned to read both
# instruments. The derived runner below landed and was rostered, and for one lap this reading went
# on deriving `covered` from the elder hand-written witness alone -- printing 83 desks "run by
# nothing" while a guard on this very roster ran all 83. `covered` is the union of `covered_witness`
# and `covered_runner` now, the runner asked with --list rather than derived a second time. What
# `uncovered_bare` counts is a desk NOTHING runs, which makes it the gate on the two derivations
# agreeing: this scan excludes by the markers' intersection, the runner by their union.
guard glow_desk_reach
path tools/g/glow_desk_reach_witness.rish
tier lap
seated 20260907.005950

# A Glow gate desk decides a module's law at the door by MEMORIZING its number, while the Rye
# module holds the declaration -- three copies of one number in two languages. Every sibling
# witness runs the desk twice and proves it DECIDES on both sides of the wall it carries, which
# stays true after the Rye constant moves and the desk does not (`20260915.185309`). This one reads the
# two together: a desk names its law once in its head, `::  law <file> <const> below|atmost|exact`,
# and the constant's value, the head's restatement, and the body literal are derived from it.
# Four readings gated at zero; `unlinked` ratchets at 18 and falls as each lane links its own.
#
# Cost: the scan reads 451 tracked .glow heads and a handful of Rye declarations -- no Zig build,
# no lowering, no desk run -- so it sits at lap tier beside its sibling rather than at cadence.

guard glow_gate_law_agree
path tools/g/glow_gate_law_agree_witness.rish
tier lap
seated 20260915.185309

# The desk corpus actually RUN, seated 20260907.110022. The reach guard above measures how far the
# hand-written witness reaches; this one runs the room. tools/fixtures/g/glow_desk_run_scan.sh
# derives the bare-runnable set on every pass -- every .glow under glow/gen/ minus those declaring
# they must not run by EITHER marker, minus those the worker will only take with a sample -- and
# lowers, builds and runs all 301 of them. The elder tools/g/glow_run_desk_witness.rish names 218
# in 1,133 hand-written lines and is on no roster, because rostering it would publish a 218-desk
# green as coverage of a 352-desk corpus (REDS %532).
#
# Why a batch is cheaper than the blocks it replaces: tools/g/glow_run_worker.sh rebuilds
# glow/bin/glow_run from glow/glow_run.rye on EVERY invocation, which is correct for a hand running
# one desk and wasteful 301 times over. Measured on this pier 20260907 -- 3.80s per desk through the
# worker, 2.42s of it that rebuild, 1.38s the desk's own lower-build-run. The batch builds the
# compiler once, behind the same directory lock the worker takes.
#
# The failure ceiling stands at 3 rather than 0: three files under glow/gen/ are data fixtures
# rather than desks and carry no marker in either name or head, and giving them one is a language
# custody ruling (REDS %532). Run on metal they refuse in two distinct ways, not one.
#
# `cadence` rather than `lap`: 325s of Zig builds measured under a load of 14 is choir money, and
# the desk room grows by a few files a round.
guard glow_desk_run
path tools/g/glow_desk_run_witness.rish
tier cadence
seated 20260907.110022

# glow_run's own exit contract, seated 20260911.230925. The guard above reads glow_run's exit status
# by number and maps it onto its own stage codes; two more doors recite the same numbers in prose.
# Not one of them could cite a statement of the contract, because the module returning the number
# carried none -- so the reading was taken by hand off metal and rewritten twice, once at REDS %532
# and once on 20260908.234354, each correction making the same shape of error at a different size.
#
# Two codes each carried two meanings. `readFileAlloc` was reached with `try`, so FileNotFound,
# IsDir and StreamTooLong each returned through Zig's `!u8` main -- exit 1 -- and downstream named
# that *lowering failed*, a claim about a desk's CONTENT for a file whose bytes were never read. A
# usage slip exited 2, so *glow_run declined this file* was the answer when no file had been named.
# `3 unreadable` and `4 usage` are seated with the guard, each naming itself on stderr.
#
# Three gates at zero, never a ratchet: a contract is not a backlog. The head table and main's own
# return sites must name one set of codes, and the built binary must answer them -- the metal half
# is what a source-versus-table reading can never reach, and the pen's `metal_disagrees` legs plant
# exactly the elder collision to prove it.
#
# `tier lap` at 5s: the probes run against a shell stub in the pen and against the standing binary
# once, so the guard costs no Zig build of its own.
guard glow_run_contract
path tools/g/glow_run_contract_witness.rish
tier lap
seated 20260911.230925

# glow_desk_arity -- three files state one desk's arity, and nothing compared them.
#
# How many sample arguments a Glow desk takes is written in the desk's own `::  Sample:` head
# line, in tools/g/glow_run_worker.sh's accepted-count list, and in the argv gates that
# glow/glow_run.rye --sample-argv emits. The third is DERIVED; the first two are hand-written.
# REDS %532 repaired two of this corpus's three enumerations and named arity as the one left
# underived, then predicted the fourth in its own words -- a sampled desk needs a value somebody
# chooses "and then a fourth hand-written enumeration to hold the answers." The `Sample:` line is
# that fourth.
#
# Measured 20260910: 46 sampled desks, three statements each, zero disagreement -- the same
# perfect agreement %532 found among its own three, stored in nobody's instrument.
#
# `lap` rather than `cadence`: 9s, because it reads and lowers rather than building. The sibling
# that RUNS these desks costs 325s of Zig and sits on the slower clock; this one answers the
# contract question every lap, which is when a desk lands.
guard glow_desk_arity
path tools/g/glow_desk_arity_witness.rish
tier lap
seated 20260910.223000

# gate_example -- a gate desk writes its own answers on its face, and nothing ever read them.
#
# Every desk in `src/gate/` opens with the six placard lines `src/shape/PLACARD.md` seats, and on a
# GATE the `example` line is a table of input and answer -- `3 5 -> 1 - 3 2 -> 0 - 3 3 -> 1`. On a
# shape pedestal that same line is one literal, and two rungs seated `20260910` hold each of those
# numbers against the Rye it describes. The gate room's tables had no such reader at all.
#
# Measured `20260911` over the 43 gate desks: 40 carry an example, 39 parse as machine tables, and
# they hold 104 cases. The count is a reading rather than a claim -- those 104 plus the 3 arrows of
# the one prose desk account for all 107 arrows standing on example lines in the room.
#
# NOT A SECOND COPY OF `glow_desk_arity`. That rung compares a `::  Sample:` head line, the
# worker's accepted-count list, and the argv gates `--sample-argv` derives, over 46 desks in
# `glow/gen`. Measured the same day: ZERO desks in `src/gate/` carry a `Sample:` line, so its first
# statement does not exist in this room. Here the desk's own statement of its arity is the shape of
# its example table -- and that table carries the ANSWER beside the inputs, which no `Sample:` line
# does.
#
# The arity is read PER CASE. `gate-lantern-face-core` carries cases of two and of three inputs and
# the worker accepts both, so a reading taken from the first case alone calls that desk arity 2 and
# passes its three-input cases unseen. The control plants exactly that shape and it bites.
#
# WHAT RUNS ON THIS CLOCK, AND WHAT DOES NOT. The free readings -- parse, and every case's arity
# against the worker's own `--arity` answer -- cost no build. The run half is opt-in: each case
# lowers and builds its desk with Zig at 5.5 to 6.0 seconds, so the live 104 take about eleven
# minutes. Spending that every lap would re-prove 104 answers that moved for nobody, so it is
# proven on miniature desk rooms in a pen instead, and the live sweep is one command a hand takes:
# `sh tools/fixtures/g/gate_example_scan.sh --run`.
#
# 30 control legs, every refusal planted and then lifted, and two mutations of the scan bitten:
# reading arity from the first case alone fell one leg, reading the answer by substring fell three.
# That second is REDS %310's own shape -- `EXIT:0` contains the digits an answer of `0` is -- so the
# scan reads the last line before the tail exactly, and a program that says nothing answers
# `<silence>` rather than passing for zero.
#
# `lap` rather than `cadence`: 10.5s measured on this pier `20260911.093000`, a text reading plus a
# pen, no toolchain. A desk lands in this room by hand a few a round, and the lap it lands is when
# its table wants reading.
guard gate_example
path tools/g/gate_example_witness.rish
tier lap
seated 20260911.093000

# gen_home -- the instrument stood green, on no clock, while the door it answers drifted 34 desks.
#
# WHAT WENT WRONG. `src/gen/README.md` is the door onto the desks' future home, and it pinned
# *Three hundred eighteen `.glow` desks stand today* with *eighty-four witnesses* beside it. The
# witness that measures both readings lives in the same room: `tools/g/gen_home_witness.rish`
# prints the desk total rather than pinning it, and its own header says why -- *a count carried in
# prose drifts the first lap nobody edits both (REDS %93); this witness pinned "317" desks for
# exactly as long as that was true and not one day less.* Run on `20260908.145510` it answered
# **352 desks and 29 tool files**. The door was wrong by 34 and by 55, and the guard that knew had
# never been on this roster at all -- it ran when a hand typed its name and at no other time.
#
# THE COUNT AT THE DOOR WAS THE SYMPTOM. Nothing this witness gates had failed: zero flat desks,
# every desk in the letter room its own basename names. What had failed is that nobody heard it.
# A witness whose totals are measured rather than pinned still teaches nobody while nothing runs
# it, which is the `unheard guard` shape (%482, %568) in the one room whose whole argument is that
# a printed number beats a written one.
#
# HOW IT REACHES THE ROSTER. The runner invokes every guard as `rishi run <path>` with no
# arguments, and this witness read `args[0]` outright, so seating it needed one accretion:
# `home` defaults to `glow/gen` and an explicit argument still wins. Both forms and the empty
# refusal were proven on metal before the seat.
#
# `lap` rather than `cadence`: 254 ms measured, no build, three `find`/`grep` passes over one room.
guard gen_home
path tools/g/gen_home_witness.rish
tier lap
seated 20260908.145510

# The constellation rehearsal's wire pen, seated 20260828.011621. comlink/rehearsal_wire.rye
# crosses three real processes over localhost UDP and was reached by no runner, roster, or suite
# until this guard -- it ran when a hand typed its name and at no other time. It is the pen FORA31,
# the socket rung where constel/ meets comlink/hosted_wire.rye, is to be proven in, so the pen
# earns a standing guard before the rung lands in it. `cadence` rather than `lap`: three spawned
# processes binding real ports is the thin edge, and the thin edge is budgeted rather than free.
# Measured on this bench 20260828: 2-3s with a warm Zig cache, build included.

guard comlink_rehearsal_wire
path tools/co/comlink_rehearsal_wire_witness.rish
tier cadence
seated 20260828.011621

# The socket rung itself, seated 20260828.174900. constel/socket.rye is FORA31 -- the FORA6
# switchboard with every mailbox swapped for a real UDP descriptor on 127.0.0.1, the one rung the
# Constel ladder named as genuinely crossing the Comlink seam. Thirty rungs of consensus stand on
# the switchboard's promise that a frame reaches exactly one named pier; until this rung that
# promise was kept by an array index the module wrote itself. `cadence` rather than `lap`, for two
# reasons said plainly: leg B spends real wall-clock on bounded waits by design, and the guard binds
# eight loopback ports (38512..38519), which two hands sharing one bench should not do every lap --
# the same one-tree-per-star question REDS %281 and %291 leave with Keaton, arriving from the port
# side. The hold releases on its own after 3000 ms, so a red here never strands a port.
# Measured on this bench 20260828: 3s wall with a warm Zig cache, both builds included.

guard fora_socket
path tools/f/fora_socket_witness.rish
tier cadence
seated 20260828.174900

# The custody-gate instruction guard, seated 20260828.033212. REDS %307 found the loop's own prompt
# template telling an unattended lap to run the seed publisher every fifth round -- a cadence the
# card cut on 20260826, and a gate Keaton holds by hand. A hand repaired that file; the same
# sentence stood on in tools/l/launch-claude-chapter.rish, the file a hand actually pastes to START
# a loop, so the orders reaching the machine were still the cut ones. This guard reads the whole
# loop-prompt surface -- recursion-prompts/ and tools/l/launch-*, discovered rather than named --
# and gates at zero any gated command standing in a clause with no refusal beside it. `lap` rather
# than `cadence`: the scan is grep over 27 files and costs 0.15s on this bench, measured 20260828,
# and a gate worth holding is worth holding every lap.

guard custody_gate_instruction
path tools/cu/custody_gate_instruction_witness.rish
seated 20260828.033212

# glow_gate_answer -- a wall whose needle the harness itself supplies is not a wall. REDS %310 found
# three argv plants computing their expected value from the same expression as the gate body, so
# every one returned zero for every input and spoke nothing, and a witness above them read
# `contains "0"` against output whose tail was EXIT:0. Sound's language custody repaired the
# emitters; this is the meter over the witness surface, the half %310 parked. Two gates at zero --
# a needle the EXIT framing already supplies, and a captured run nobody asserted -- and two ratchets
# under ceilings that only fall: 152 answer-blind runs and 7 files never seen to refuse. Both
# ceilings are proven from both sides in the pen by planting one past them, so no override exists.
# `lap` rather than `cadence`: the whole witness costs 3s on this bench, measured 20260828, and the
# fault it refuses can be written again in any lap that adds a lowering witness.

guard glow_gate_answer
path tools/g/glow_gate_answer_witness.rish
seated 20260828.114450

# gitlink_dependent -- a guard that reds on a dependency a correct clone may lack can never be
# rostered, so its refusal is one nobody will ever hear. This tree holds twelve gitlinks in two
# rooms: `vendor/` is what the tree LINKS and the operator card requires every clone to initialise,
# and `gratitude/` is a reading library the licenses law says we study and never copy -- so a clone
# that never checked out `gratitude/tigerbeetle` is a CORRECT clone. REDS %646 found the shape by
# sampling and left the class as a sample; counted `20260908.214427` it was 22 dependents, all one
# family -- 21 from `20260908.224618`, when the sixth probe was drawn and one was rostered -- against
# 32 on the required rungs. The GATE is the one row that would red the fleet -- a rostered guard
# depending on an optional gitlink and naming no `capability` -- held at zero. The population is
# reported rather than gated, because rostering a study census is a hand's decision with a probe
# attached. `lap` rather than `cadence`: 14s on this bench, and the fault can be written again by
# any lap that rosters a witness.
#
# A SECOND GATE FROM `20260911`, and the reason it had to be added. Everything above reads a SHAPE.
# The reading called its population `optional_unrostable` and said of each member *reds when an
# optional submodule is absent*, and nothing had ever run one: with `gratitude/tigerbeetle` empty --
# how every ship on this pier stands -- 38 of 38 exit 0, each printing an honest SKIP. The reading
# is `optional_unrostered` now, which is what it counts, and `probe` RUNS each dependent whose
# gitlinks stand empty and gates `probe_red` at zero. That gate is what turns REDS %646's 5-in-40
# sample into a wall: a runner written with a hard assert over a reading library reds on the lap it
# lands rather than being found by the next census. The probe costs about 3s of the 14.
guard gitlink_dependent
path tools/g/gitlink_dependent_witness.rish
tier lap
seated 20260908.214427

# tigerbeetle_naming_census -- the first of the 22 optional dependents `gitlink_dependent` counted,
# rostered because the probe it wanted now stands. `capability tigerbeetle_clone` is the sixth arm
# in the runner, seated 20260908.224618 and spelling the same `test -d gratitude/tigerbeetle/src`
# the witness's own assert spells; where the held clone is absent -- which is every ship on this
# pier today, all eight gratitude gitlinks empty -- the row reads its own name in
# `skipped_capability` rather than reddening for a reading library nobody is obliged to fetch.
#
# THIS ONE ROW RATHER THAN THE FAMILY, on purpose. Forty-four witnesses read that clone (20 census
# leaves and 24 choirs wrapping them 1:1, and NOTHING ELSE IN THE TREE CALLS ANY OF THEM), so
# rostering the family would put 44 skip lines on every pass of every ship and turn 16 percent of a
# 275-row roster into a study that skips. That is a hand's decision. This row is what makes the
# probe load-bearing rather than decoration, and it is the leanest member of the family: pure
# counting, no almanac append, no nested choir, so it cannot move the tree on a pier that does hold
# the clone. `lap` rather than `cadence`: it is grep over a checkout that is usually absent.
guard tigerbeetle_naming_census
path tools/gen/chapter/tigerbeetle_naming_census_witness.rish
tier lap
capability tigerbeetle_clone
seated 20260908.224618

# precondition_dependent -- the other half of REDS %646, and the half nothing had counted. That row
# sampled 40 unreached witnesses, ran 5 red, and found two PRECONDITIONS among them. One was an
# optional submodule, which `gitlink_dependent` walled above. The other was `build wayland_seed
# first`, which is no submodule at all -- a built artifact this tree does not track -- and it stood
# uncounted for three days. This reads that family and the one beside it: a DISPLAY a headless host
# lacks however much it builds.
#
# THE CURE STANDS THREE TIMES ALREADY, which is what a census could see and a sample could not. REDS
# %173 seated `pond_display_gate` for the screenless case. The `capability` field above is the tier
# for what a host CAN DO. And `tools/equinox/witness/equinox_season_e0_witness.rish` BUILDS its own
# `glow/bin/mod-clock` rather than requiring one. Read `builds_own`: on this tree it is ONE, against
# ten dependents still refusing, so the cure is a habit that reached one room and stopped.
#
# THE GATE is `rostered_undeclared` at zero -- the one row that would red the fleet on every machine
# lacking the artifact or the screen. `probe_red` is a CEILING THAT ONLY FALLS rather than a wall,
# because 10 of 10 refuse today and a wall refusing ordinary work on its landing lap is a wall
# somebody turns off.
#
# THE SIBLING ANSWERED THE OPPOSITE WAY, which is the argument for probing rather than reading:
# `gitlink_dependent` ran its 38 and found 38 of 38 exit clean; this ran its 10 and found 10 of 10
# refuse. Neither could be guessed from the other. `lap` rather than `cadence`: 58s, and the fault
# can be written again by any lap that adds a witness -- the same reasoning its sibling carries. The
# probe costs 55 of the 58 and every dependent refuses before it reaches anything expensive.
guard precondition_dependent
path tools/p/precondition_dependent_witness.rish
tier lap
seated 20260911.202909

# reds_status_consistency -- construction/REDS.md is the first page a lap reads under reds-first,
# and the one thing it has to get right is which rows are still open. REDS %313 found `%307` reading
# open on the pin while `%308` -- folded onto a shelf the same day -- carried the only record of its
# closure. The split is structural: every fold moves a closing row into a closed stack and leaves
# the row it closed on the open shelf. Gates contradicted_rows and phantom_closures at zero, and
# reports open_rows, closed_rows, closure_claims, duplicate_row_lines and fold_blocked_rows. `lap`
# rather than `cadence`: it is awk over 128 markdown files and costs under a second, measured
# 20260828, and the page it reads is the page every lap chooses its work from.

guard reds_status_consistency
path tools/r/reds_status_consistency_witness.rish
seated 20260828.125441

# pin_bound_touch -- the seven living pins carry byte bounds so an agent reads one in a single
# breath beside its lap, and until 20260828 nothing weighed a pin at the moment a round moved it.
# REDS %293 named the mechanical repair in its own third field and left it unbuilt; the next day
# construction/REDS.md stood 3,992 bytes over and construction/ITINERARY.md 3 over, and four
# commits shipped across a red `equinox_e123_living_pin_guard`. Two readings that cannot disagree:
# e123 weighs every pin at roster time, this weighs only the ones the round's own diff names, off
# the index, where `git cat-file -s :<path>` is the size the commit will actually carry. One roster
# file and one bound reading (living_pin_max_bytes.sh) are shared, so there is no second roof.
# It also weighs EVERY touched pin before refusing -- e123 exits at its first, which is how the
# ledger's 3,992 bytes went unreported while the card's 3 were named. Armed as RULE THREE in
# tools/hooks/pre-commit, proven by 36 cases in a pen, five of them running real `git commit`s
# through the real hook. The witness gates only its own reading -- the refusals, the pen, the hook
# turning a real commit away -- and PRINTS the live worktree reading without asserting on it, so
# one fault reds in one place and e123 keeps that gate. `lap` rather than `cadence`: the witness
# costs 3s on this bench, measured 20260828, and the fault it refuses can be written by any lap
# that edits a pin.

guard pin_bound_touch
path tools/p/pin_bound_touch_witness.rish
seated 20260828.153046

# pond_enclosure_policy -- orbit one of the quest that retires ai-jail. pond/enclosure_policy.rye
# reads an enclosure as a value and marks each declaration place, hold, or refuse at receipt,
# naming the declaration that refused. The scan plants one forbidden mount and then removes it, so
# the refusal is proven from both sides; a refusal shown only in the passing direction cannot be
# told from a bypass. `lap` rather than `cadence`: the arc is under active work and the whole run
# costs one second warm, measured 20260828. Rostered here on the lap that recovered it, since an
# unrostered witness raises `unreached` and proves nothing on any tree but its author's.

guard pond_enclosure_policy
path tools/p/pond_enclosure_policy_witness.rish
tier lap
gate %5
seated 20260828.002135

# pond_enclosure_state -- the launcher's state house and the per-host config it reads, compared.
# tools/ag/agent-jail.sh seats `loops/claude`, `loops/cursor`, and `loops/codex` and adopts an
# elder directory into each once; tools/e/enclosure.conf is sourced BEFORE those defaults, so a
# path named there outranks the move and the jail binds a directory the adoption just emptied. The
# gate is one reading -- the tracked example naming an elder -- and the host's own gitignored conf
# and the directories on disk are reported without gating, since one machine's file is not the
# tree's to fail on. `lap` rather than `cadence`: the enclosure arc is under active work and the
# whole run costs under two seconds, measured 20260828. Booked as REDS %327 on the lap that found
# a credential directory already rebound empty on this pier.

guard pond_enclosure_state
path tools/p/pond_enclosure_state_witness.rish
tier lap
seated 20260828.170003

# pond_policy_launcher -- the enclosure record and the launcher it claims to describe, compared.
# pond/enclosure_policy.kyri declares what the agent-jail lap builds and tools/ag/agent-jail.sh
# builds it; two spellings of one contract stood side by side and nothing compared them, so the
# record carried `network off` for an enclosure sharing the host's own network namespace (REDS
# %329). Two readings gate at zero: a mount the launcher makes and the record never declared,
# which is the escape direction orbit two must rule out, and a closure claim with no flag behind
# it. The reverse readings and the `ai-jail --help` reading are reported, since a guard that reds
# on a machine without the jail is a guard someone turns off. `lap` rather than `cadence` for the
# same reason its two siblings chose it -- the enclosure arc is under active work and this guard's
# whole subject is the pair of files orbit two will derive its grant set from. The cost is named
# rather than hidden: 13.4s measured 20260828, of which the 25-pen control is 9.9s and the scan
# itself 0.4s. When the quest's orbit five closes and these two files stop moving, this row falls
# to `cadence` -- the control re-proves the guard rather than the tree.

guard pond_policy_launcher
path tools/p/pond_policy_launcher_witness.rish
tier lap
seated 20260828.202621

# glow_choir -- the 26 Glow-family witnesses that no runner on disk named. A survey on 20260828
# traced every tracked runner and found them reached by nothing: not this roster, not the three
# tend suites, not another witness. One of the 26 was already RED, citing a brief that had folded
# to active-designing/yonder/, and nothing was there to say so. `cadence` rather than `lap`: the
# choir costs 2m01s measured on this pier, which is choir money, and the Glow ladder's last
# advancing commit was 2026-08-04, so a slower clock matches how fast the family moves.
# 26 -> 32 members on 20260909: a second reading of the same `unheard` band answered eight more
# Glow-family files, six of which joined after each ran GREEN alone. Cost 2m01s -> 4m47s, still the
# cadence clock. The two held back are the cross-target pair, green in 0s by skipping an absent
# qemu -- REDS `%460`, Keaton's word.

guard glow_choir
path tools/g/glow_choir_witness.rish
tier cadence
seated 20260828.192755

# glow_shop_core -- which road a `|%` source takes, decided before it lowers. The two modules
# that pick it, glow/rune_shop_core.rye and glow/lower_shop_core.rye, landed 20260830.224500 and
# were imported by glow/glow_run.rye alone: no runner on disk named either, so nothing ran them
# for seven days while their dispatch refused all fourteen thin core desks under glow/gen/c/ and
# the Glow step of manual/grain-os/get-started.md (REDS `20260906.195208`). `lap` rather than `cadence`: 2s
# measured on this pier, and the predicate it holds decides a road for every `|%` source the
# language will ever lower.

guard glow_shop_core
path tools/g/glow_rune_shop_core_witness.rish
tier lap
seated 20260906.195208

guard skate_native_core
path tools/s/skate_native_core_witness.rish
tier cadence
host macos
seated 20260828.194500

# fixture_depth -- the depth-proof root walk the fixtures letter fold (REDS %301's last room)
# seated across tools/fixtures/. The witness drives the pen control -- wrong root shown, right
# root from both depths, elder sibling source broken, new source landed, the 8-step bound
# refusing loudly -- and censuses the room at zero elder shapes. `lap` because the control
# costs about a second on this pier, and the census reads git rather than walking disk.

guard fixture_depth
path tools/f/fixture_depth_witness.rish
tier lap
seated 20260828.202325

# witness_own_build -- a witness that runs a gitignored artifact also builds it. comlink/bin/ is
# ignored, so a clone carries no binary: two Comlink witnesses named their build in a header
# comment and then invoked the path, and both read GREEN on a bench where an August lap had left
# binaries behind. Their sibling comlink_topology_witness.rish already carried the repair, so one
# of three was fixed and nothing counted the other two. `lap` because the scan reads 1,730
# witnesses in 4.7s on this pier, one awk and one batched check-ignore rather than a call per file.

guard witness_own_build
path tools/w/witness_own_build_witness.rish
tier lap
seated 20260828.213503

# built_tool_freshness -- the freshness half of the question the row above answers for PRESENCE.
# witness_own_build passes rye/bin/rye and rishi/bin/rishi free on a reason that is exactly right
# about what it checks: if either is absent nothing runs at all, so their presence is a bootstrap
# fact. Both rooms are gitignored, so a pull carries no rebuild, and a present binary arbitrarily
# far behind its own source runs every guard on the ship and its errors are attributed to whatever
# it was running (REDS %746). Measured when this row was seated: rishi stood 877,180 seconds --
# ten days -- behind rishi/src/main.rye, and a healthy peer witness died with NoSuchField on a
# field the source declares. `lap` because the fault is re-earned by every pull, and because the
# reading is one git ls-files and one find -newer per tool: 0.5s scan, 2s with its 36-leg pen.
# The reading lives in plain shell so a rishi too stale to parse this witness cannot silence it.
guard built_tool_freshness
path tools/b/built_tool_freshness_witness.rish
tier lap
seated 20260915.221313

# Comlink's three pure proofs, seated 20260828.231614. comlink/topology.rye, turn_route.rye and
# handshake_turn.rye each carry a selftest this tree already trusted, and each was reached by no
# roster row, no choir and no runner -- they ran when a hand typed their name and at no other time,
# which is the sentence REDS %334 left standing when it repaired their builds. Measured cold on
# this bench 20260828 with comlink/bin/ emptied first: 2s each, its own zig build included, no
# socket bound and no process spawned. `lap` rather than `cadence` for that cost.
#
# comlink_topology READS 12s FROM 20260906.093000, and the row stays `lap` on purpose. It
# grew the point-metric proof REDS %454 asked for -- every ordered pair of both seated skies
# walked for a strictly-closer next hop -- plus its own control, which builds six pen copies
# of the module and runs each. Measured here: 12.5s against a cold endurance run of 875s, so 1.4%.
# The elder 2s above is kept as what the row cost when it was seated, since a number a row
# carries should say which lap measured it.
#
# Their fourth sibling tools/co/comlink_r1_dual_stack_witness.rish takes its seat here on
# 20260829, and the field it waited for is the reason it now can. Its leg B reads the loopback
# interface table and requires BOTH address families on it, so a red there is an ENVIRONMENT fact
# -- this host has not configured a family -- rather than a finding about Comlink, and an
# unconditional row would turn one bench's configuration into every body's red lap. The `host`
# field cannot carry it: `host macos` is a tier for PLACE, and this condition is one of CAPABILITY
# -- a Linux bench routing IPv6 keeps the promise a Linux bench without it breaks, so the OS word
# would encode something untrue. `capability ipv6` is that word, seated 20260829 and proven in all
# three of its answers: present runs, absent skips BY NAME and is counted, and unknown RUNS --
# since skipping on an unknown is how a tier stops being a cadence and becomes an exemption. The
# runner's probe reads `::1` on lo0 here, so the guard runs on this bench and a bench without the
# family reads its own name in `skipped_capability` rather than vanishing from the roster.
#
# `cadence` for the cost: 105s measured here 20260829.175031, its own a1 suite included, against
# the 2s each of its three pure siblings above. The elder note in this room read 151s; a witness
# timed under a lap's own load reads high, and the number a row carries should be measured by the
# lap that writes it. Either reading is two orders above the siblings, which is what `cadence`
# answers -- it sings on the rotation's turn rather than every lap.

guard comlink_topology
path tools/co/comlink_topology_witness.rish
tier lap
seated 20260828.231614

guard comlink_turn_route
path tools/co/comlink_turn_route_witness.rish
tier lap
seated 20260828.231614

guard comlink_handshake_turn
path tools/co/comlink_handshake_turn_witness.rish
tier lap
seated 20260828.231614

guard comlink_r1_dual_stack
path tools/co/comlink_r1_dual_stack_witness.rish
tier cadence
capability ipv6
seated 20260829.175031

# waymark_rung_drift -- the mark law (.claude/rules/stamp-and-name.md) retired the ascending rung
# as a mark for planned work, and two ladders numbered right past it for a week because nothing
# counted the shape (%329). This meter counts <seated-waymark><digits> in living tracked files
# under a ceiling that only falls. It landed 20260828 with no roster row, so nothing ran it: its
# first hearing on this pier, the lap that rostered it, read one mark over the ceiling. `lap`
# because the scan is one batched grep over git's own file list, under a second on this pier.

guard waymark_rung_drift
path tools/w/waymark_rung_drift_witness.rish
tier lap
seated 20260828.233813

# tally_bud -- the Pedersen bud vector, pinned and moved. A pinned constant earns trust only when
# a witness moves it, so this holds a passing vector and refuses a planted generator byte, a
# planted domain, and an amount that moves the answer. `lap` because it costs 6.2s on this pier
# and Tally is the arc under active work, which is the same reasoning caravan_suite stands on.

guard tally_bud
path tools/t/tally_bud_witness.rish
tier lap
seated 20260828.233813

# counsel_census -- the counsel campaign's own value reading: which of the 941 closed pieces are
# still leaned on, ordered by citer count, so Phase 1 lifts by measurement rather than by memory.
# `cadence` because it costs 32.2s on this pier and the room it reads is closed -- a census of a
# room that no longer grows changes on the laps that lift from it, not on every lap.

guard counsel_census
path tools/co/counsel_census_witness.rish
tier cadence
seated 20260828.233813

# pond_enclosure_built -- the record against what the enclosure actually BUILDS. Its sibling
# pond_policy_launcher reads the record against the launcher and says in its own header that it
# compares what the launcher SPELLS; ai-jail supplies twenty-one more mounts from its own defaults,
# and the record named six of twenty-seven until this row's lap. The scan reads `ai-jail --dry-run`
# -- the bwrap plan printed without starting anything -- gates `undeclared_builds` at zero against
# a pinned measurement so a clone with no jail still reads it, and reports drift between the pinned
# plan and a live one. `lap` because the scan is 0.12s and the whole witness 2.4s on this pier.

guard pond_enclosure_built
path tools/p/pond_enclosure_built_witness.rish
tier lap
seated 20260828.233813

# reds_pin_capacity -- the four guards above all ask whether construction/REDS.md is CONSISTENT;
# none asked whether anything more could be WRITTEN. On 20260829 the pin stood at 24,571 of its
# 24,576 bytes with all nine rows marked OPEN, so reds_fold.sh had no lawful move, four reds went
# unbooked on 20260828, and three more were born onto single-row shelves -- one of them, %337,
# still live, which is the exact state the fold tool refuses a fold to prevent. Every one of those
# four guards read green throughout (REDS %338). Gates phantom_recital_shelves at zero, ratchets
# unrecorded_shelves at 62 and shelf_open_rows at 2, and REPORTS the capacity table and
# pin_deadlocked -- reported rather than gated because raising a page's bound is Keaton's word, and
# a gate that reds on ordinary work is a gate someone turns off. `lap` rather than `cadence`: awk
# over the spine and a pen control, 1s for the scan and 3s for the whole witness measured on this
# pier 20260829, and the page it reads is the page every lap chooses its work from.

guard reds_pin_capacity
path tools/r/reds_pin_capacity_witness.rish
tier lap
seated 20260829.031804

# pond_enclosure_ephemeral -- the two guards above ask what the enclosure lets an agent SEE, one
# from the launcher's flags and one from the jail's own plan. Neither asks how long what it WRITES
# lasts, which is orbit three's whole question: the memory that forgets. This classifies every mount
# row by lifetime -- a tmpfs dissolves, a read-only bind keeps nothing, a read-write bind hands the
# byte to the host -- gates the record's `ephemeral` claims against that reading at zero, and holds
# the writable paths outliving the close outside the pier at a ceiling of 3. Its `--probe` leg plants
# one marker per candidate inside a real enclosure, lets it close, and reads the host: measured
# 20260829, the private home and /tmp came back forgotten and /dev/shm, /tmp/.X11-unix and
# /run/user/<uid> came back carrying the marker, with zero disagreements against the plan. `lap`
# because the whole witness including the live probe is 3.0s on this pier, and the probe is the only
# reader of the private home's own claim, which the pinned plan drops.

guard pond_enclosure_ephemeral
path tools/p/pond_enclosure_ephemeral_witness.rish
tier lap
gate %5
seated 20260829.054303

# pond_enclosure_door -- the three guards above read the ROOM: the flags the launcher spells, the plan
# the jail builds, and how long a written byte lasts. None reads the DOOR -- which program walks in,
# carrying which environment, as which user -- which is the launcher's own last exec line and nothing
# the record has a key for. This derives reachability from the record (`map`, `persist`, and `rw-map`
# destinations declare; a closer `mask` wins; `ephemeral` and `fresh` start bare), gates the ENTRY at
# zero unreachable, and holds undeclared search-path elements at a ceiling of 1. Its `--probe` leg
# starts one enclosure and asks it the same questions, writing no byte on either side: measured
# 20260829, two of the launcher's four search-path elements are absent inside -- one the host lacks,
# one the private home dissolves -- with zero disagreements against the derivation, and the three
# door duties (`entry`, `env`, `user`) have no key in the record's grammar at all. `lap` because the
# whole witness including the live probe is 8s on this pier.

guard pond_enclosure_door
path tools/p/pond_enclosure_door_witness.rish
tier lap
gate %5
seated 20260829.064107

# lattice_suite -- forty small arithmetic witnesses heard as one bounded choir. Each lap builds
# Lattice and proves an admitted operation beside its shape refusal; the suite also refuses when
# a newborn lap witness stands outside its forty-seat roster. `cadence` keeps the stable arithmetic
# family heard without adding forty repeated builds to both halves of every systems lap.

guard lattice_suite
path tools/l/lattice_suite_witness.rish
tier cadence
seated 20260829.061739

# macos_app_bundle -- the Finder door, proven from both sides on a Mac. The witness builds
# tools/rye/macos_app_shell_probe.rye with Cocoa and CoreGraphics, assembles Grain.app in a /tmp
# pen around the tracked plist fixture, strips Info.plist from a copy and proves `open -W` refuses
# it, then opens the whole bundle through LaunchServices and reads the receipt the probe wrote from
# a process that never had a terminal -- `mode=bundle window=5308 main_menu_items=1`, measured on
# this bench 20260829.104233, GREEN in 4s. It stood in tools/fixtures/w/witness_reach_scan.sh's own
# ceiling comment as the one debt a Linux pier could not pay, and this is the Mac bench that pays
# it. `host macos` because `open` needs a window server, and `cadence` beside its macOS-surface
# sibling skate_native_core, because the guard opens a real window and a hand may call it by name
# on any lap.

guard macos_app_bundle
path tools/m/macos_app_bundle_witness.rish
tier cadence
host macos
seated 20260829.104233

# pond_seal_gate -- the custody gate for the season flip, read wherever it is spelled. Three tracked
# launchers admit ENCLOSURE=pond only behind pond_exit_kyri_master_seal.sh --require, and
# pond_policy_launcher reads exactly one of them because that is the one Pond's record describes.
# This discovers the launchers with `git grep` rather than listing them (%301, %326) and gates three
# readings at zero: an ungated pond, a seal call whose exit code nothing reads (%311's vacuous wall,
# one room over), and a caller the seal's own header does not name. `lap` because scan plus control
# is 9s on this pier, the same weight as pond_enclosure_door beside it.

guard pond_seal_gate
path tools/p/pond_seal_gate_witness.rish
tier lap
seated 20260829.123529

# The wire-lab fn macro stays one macro: fifteen orchestrators each carry three fn lines hoisted
# from 135 repeated template sites (20260829), and Rishi has no cross-file include, so the lines
# are deliberate copies read against the teaching head. Readings at zero: fn_drift (one file's
# macro wandering) and raw_sites (a template returning outside the fns). `lap` because scan plus
# control run under ten seconds -- the happy zone holds daily; the real qemu boots stay with the
# labs' own gating witnesses.

guard wire_lab_fn_drift
path tools/w/wire_lab_fn_drift_witness.rish
tier lap
seated 20260829.153150

# Tablecloth's first Glow desk, seated 20260829. Every other infrastructure vane carrying a bound
# worth naming already stood in the shape museum -- Aurora four pedestals, Mantra four, Caravan
# three, Tally six, Comlink one -- and Tablecloth, the content-addressed store the Brix manifest
# and the four comlink/guest_tablecloth_query_* wires both lean on, stood there not at all.
#
# The guard is shaped against the elder Tend habit rather than after it. Those witnesses grep for
# the LITERAL number in both rooms, which writes the value a third time inside the guard itself,
# so raising a bound honestly in the Rye AND on the pedestal still reds until a hand edits the
# witness. This scan reads both numbers and compares them, carrying none of its own; the control
# proves that welcome -- a bound raised in both rooms walking free -- beside its refusals.
#
# Widened 20260830.001122 to Tablecloth's second pedestal, `max_content_bytes`, which is a
# DERIVATION -- `= beading.max_resin_bytes` -- so tablecloth.rye names the bound and
# mantra/beading.rye decides it. The scan compares the desk against the deciding room and reads
# the derivation as a fact of its own, because respelling it as a literal leaves every number
# agreeing while the link is gone. Nineteen control cases now: three welcomes, sixteen refusals.
#
# `cadence` because the metal leg lowers the desk through Glow and builds it with the Zig
# toolchain, the same clock the six Glow witnesses beside it keep. The pure scan and control run
# in under a second, so a hand asking for the guard by name pays almost nothing.

guard tablecloth_glow_tend
path tools/t/tablecloth_glow_tend_witness.rish
tier cadence
seated 20260829.210056

# lantern_face -- the asking voice's face, landed by a peer `20260830.000539` with no roster row
# beside it. `witness_reach` caught the gap on the very next hot endurance run: a witness nothing enumerates
# is a witness family with no clock, and that reading stood at 221 wholly-unreached families
# against a ceiling of 220. The guard's own refusal names the repair -- give it a roster row rather
# than raise the ceiling -- so this is the row rather than a raised number.
#
# `lap` because the whole witness, including the Glow wall and the two-language parity anchors,
# runs in 9.5s measured on this pier `20260830.011217`, which is inside the cost every lap already
# pays for its Pond siblings. It builds and runs the face's own legs, proves the `.brush`-themed
# cascade recolors one settledness, walks the Glow budget wall at 38 lawful against 39 past it,
# and closes on one number read in two languages.

guard lantern_face
path tools/l/lantern_face_witness.rish
tier lap
seated 20260830.011217

# lantern_face_claim -- the face's sibling, landed in the same peer arc `20260830` and unrostered
# with it. `witness_reach` counts a FAMILY rather than a file, so rostering one of the two left
# `lantern_face` wholly unreached all the same; a peer's own card names both, which is how this
# second row was found. `lap` on 4.4s measured here `20260830.021200`.

guard lantern_face_claim
path tools/l/lantern_face_claim_witness.rish
tier lap
seated 20260830.021200

# borrowed_number -- the loom REDS %357 named and left unbuilt. The class had fired twice: a
# witness typing another document's byte count or SHA-256 into an assertion, and that document
# moving underneath it. %341 repaired two instances, %357 found three more in the same family the
# next day, all red since the day SECURITY.md and context/TWO_ROOMS.md were edited and none of
# them rostered. The reading catches a pin while it is still TRUE, which is the only moment
# catching it is cheap; a pin that has already gone stale is a red the witness itself declares.
# `lap` on 40s measured here `20260830.072125` -- four scan passes and a fourteen-behaviour pen.

guard borrowed_number
path tools/b/borrowed_number_witness.rish
tier lap
seated 20260830.072125

# unheard_guard -- the other half of this file's own law, written at its head and unmeasured until
# now: a guard that cannot red guards nothing, AND a guard that is never run guards nothing either.
# The reading takes every tracked witness and choir outside the fixture pens, closes the set this
# roster can reach -- rostered, or named on a non-comment line of something already reached -- and
# holds the remainder under a ceiling that only falls. REDS %357 measured what the gap costs: three
# witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold endurance run
# read 95 green. Measured here `20260830.095947`: 1,756 guards, 603 heard, 1,153 unheard, 41 of
# those choirs that sing families of their own; re-measured `20260830.154940` after the five
# rule-named rows below landed: 614 heard, 1,144 unheard, 40 choirs. The git hooks were checked as
# a second standing runner and name no guard at all, so this roster is the whole seed.
# `lap` on 5s measured here -- one batched awk pass over the tree, plus a six-pen control.
# WIDENED `20260906` (REDS %465): the population above is drawn by BASENAME, so a runner wearing
# neither `witness` nor `suite` was not counted unheard -- it was absent, and absence looks like
# health. Four readings join the elder two: `unnamed_population` 451 asserting runners wearing
# neither word, `unnamed_heard` 43, `unnamed_runners` 408 REPORTED and never gated, and
# `unnamed_choirs` 12 GATED at what was measured. `elder_reach_gap` 2 gives the narrow rule's own
# blind spot a number. The largest silent choir in this tree is `tools/p/parity_ch01.rish`, which
# calls itself a witness suite on its own first line, builds and runs 116 Rye programs, sings 281
# guards, and which this census could not see at all. `unnamed_runners` is reported rather than
# gated from measurement: all ten arrivals into that set over the fourteen days to `20260906` were
# ACTORS -- seven `launch-*-chapter.rish`, `birth_a_clone.rish` which copies a tree's `.ssh` -- so a
# ceiling there would red on ordinary growth and point a hand at custody gate 3 through a number.
# Still `lap`, measured 21s on metal here against the elder 5s: the control grew 6 pens to 11 and 27
# behaviours to 52, and the scan itself runs FASTER than HEAD's despite carrying a second closure
# (0.88s against 0.60s), because both closures now read each source once.

# AND A THIRD READING JOINED IT `20260911`, over the proofs rather than the guards. Both populations
# above end `grep -v '^tools/fixtures/'` -- an exclusion written for pen material a control plants,
# measured and correct for its own question, and `tools/fixtures/` holds the tree's whole INSTRUMENT
# layer beside that material. So the census asking who is listening could not see a control at all.
# A control is what proves a scan reads what its header says; when one stops running, nothing reds,
# and the first drift in a classifier lands in a number a hand then quotes. Measured here
# `20260911`: **258 tracked controls, 240 reached by this roster, 18 not** -- among them
# `invariant_coverage_control.sh`, whose 37 legs prove the classifier behind three figures published
# the day before. The remainder splits by CAUSE, reported apart from the gate: an ORPHAN is named by
# no runner anywhere and wants one written or rostered, where an UNRUN control HAS a runner that is
# itself unheard, so rostering that witness closes both and one fault pays once.
#
# ITS FIRST DRAFT READ `control_heard=0`. Both elder naming rules end `\.(rish|rye)`, so neither can
# match a `.sh` at all, while a plain grep finds 248 of the 258 named somewhere. A rule is paired
# with the population it can see, or it reads zero and calls it news. The third reading carries
# `names_sh.awk` for that reason and REUSES the first closure rather than growing a second, since
# two answers to *what the roster reaches* would be free to drift.
#
# THE SOURCE SET KEEPS THE WITNESS and drops the scan and the control -- the two files that could
# ever ENUMERATE a control path. Dropping the witness would report its own control unrun, a false
# RED, which is worse than the false green `%486` booked; a control leg holds the witness to naming
# no control but its own, so a hand asserting a foreign path cannot quietly credit it. Ceiling opens
# at 18 and only falls; the population floor sits at 200 against 258, because a glob that
# stopped matching reads zero unheard and a ratchet passes a low number (`%416`).
#
# `tier lap` still, and the reading is not free: the scan runs 0.9s -> 2.3s for its two `.sh` passes,
# and the witness 21s -> 50s, since each both-sides leg runs the scan twice and the pen grew 52
# behaviours -> 75. That is 29s against a lap pass of 2,999s, and the two causes are worth it: a
# reading that cannot name which repair a finding wants sends a hand to read eighteen files.

guard unheard_guard
path tools/u/unheard_guard_witness.rish
tier lap
seated 20260830.095947

# law_guard_heard -- the sibling above asked of the loudest population there is. A rule page in this
# tree does not merely state a boundary; it names the instrument holding it still, which is the
# fifth thing Gauge asks a figure to carry -- pinned, walled, or free, where walled means a guard
# reds the lap the number moves. A reader meeting `held by tools/x/foo_witness.rish` reads a wall,
# and it is a wall only while this roster reaches that guard. Measured `20260910.191124` at
# 2410630a80: 137 law pages across the two rule rooms and the canon they cite name 100 distinct
# authored tool paths; all 100 are carried by the repository; 66 wear `witness` or `suite`, and
# **25 of those 66** stand outside every reach this roster has. Four were run by hand and all four
# read GREEN, so the finding is about reach rather than a standing red -- REDS %357's shape before
# it fires. `absent` is WALLED at zero and `unheard` ratchets, because an uncarried path is
# decidable with no judgment while an unheard one has several lawful repairs (roster it, have a
# rostered choir sing it, or find the citation was never a wall claim) and a hard gate there would
# refuse the law's ordinary growth. 63 of the 100 citations stand outside a Markdown link, so
# `tracked_link` could never see them; this is the only reading that does.
# `lap` on 7.4s measured here -- two closure passes of the sibling census plus a twelve-pen control.

guard law_guard_heard
path tools/l/law_guard_heard_witness.rish
tier lap
seated 20260910.191124

# comlink_glow_tend -- Comlink's payload ceiling, displayed and tied. The vane stood in the shape
# museum with one desk, a dual-stack POLICY, while thirty-five bounds went undisplayed, so the
# widest wire surface in the tree was the least shown. `max_wire_payload` = 340 sizes real
# buffers, gates asserts, and two further bounds derive from it -- and it is DERIVED upstream, as
# `wire_capacity - off_cipher`, where four fifths of the header is decided inside the crypto
# library. It was right by arithmetic rather than by a rule: 188 is 188 only because Ed25519 signs
# in 64 bytes, and the card books Kumara moving to SLH-DSA-SHAKE-256s. Three modules spelled the
# answer as a bare literal and named no upstream room.
#
# The scan compares rooms and carries no value of its own, so an honest raise walks free (control
# case 23). What a scan cannot do is evaluate the library widths, so the tie is handed to the
# compiler: `max_wire_payload <= wf.max_message` at comptime, proven both ways on metal in a
# throwaway pen -- as written it builds, inverted it stops the build. Twenty-six control cases,
# five welcomes and twenty-one refusals. The tie count is a FLOOR, so amphora/ tying its own room
# later is welcomed rather than punished.
#
# `cadence` because two legs reach metal -- the tie probe builds two modules and the desk lowers
# through Glow and builds with the Zig toolchain, the same clock the Glow witnesses beside it
# keep. Measured here `20260830.164837`: 10s whole, of which the pure scan and control are under a second.

guard comlink_glow_tend
path tools/co/comlink_glow_tend_witness.rish
tier cadence
seated 20260830.164837

# comlink_carriage -- what a whole is cut into, and whether the pieces can carry it. Three families
# in this tree cut a whole and carry the pieces, each publishing a whole's ceiling, a piece's size,
# and a piece count -- and the relation `whole <= pieces * piece` was stated in no room, no comment,
# and no guard. It held in all three by arithmetic nobody had written down, and the bead family held
# it by EXACTLY ZERO margin: 512 <= 8 * 64, where the 64 is a chunker tuning constant filed beside a
# hash mask rather than beside a carriage bound. Measured before the guard was written: raising
# `max_resin_bytes` 512 -> 576 and changing nothing else refuses 9 of 3,999 pseudorandom wholes with
# TooManyBeads -- a legal input refused by the module that accepted it two lines earlier, on 1 in 444
# contents, which is why a single-fixture test would ship it. The two mantra/ rooms now carry the
# relation at comptime, and amphora/vessel_fetch_wire.rye tied the third on `20260906`, taking
# ties_wired to 3 of 3. That landing turned the gate over: a FLOOR of two had been single-drop
# detection only while two rooms were tied, so the gate now asks each room for its OWN tie and
# wants all three, and the control's case 19 is Amphora dropping its tie rather than adding it
# (REDS %455). Twenty control cases on real files in a throwaway pen, seventeen
# refusals and three welcomes, and a probe that asks the compiler seven times -- four inverted ties
# and the three plausible real edits that were measured to produce a wrong answer. Measured here
# `20260830.200925`: 12s whole, of which the pure scan and control are under a second. At tier cadence
# because it builds two Rye modules eight times over and lowers a desk through Glow, the same clock
# the Glow witnesses beside it keep.

guard comlink_carriage
path tools/co/comlink_carriage_witness.rish
tier cadence
seated 20260830.200925

# comlink_nesting -- whether a frame this tree sends holds the datagram it seals. Every layer of a
# network stack wraps the layer above it, so each container must hold its own header plus the whole
# payload: `container >= header + payload`. Comlink has two such layers and BOTH stand at zero
# slack, and only one of them is safe. The datagram layer is zero because wire_format.rye DERIVES
# it -- `max_message = wire_capacity - off_cipher` -- so raising the capacity raises the message in
# the same expression. The frame layer is zero because it is SPELLED: `max_frame` is the literal 554
# in comlink/virtio_net.rye, `wire_capacity` is the literal 528 in comlink/wire_format.rye, 554 is
# 12 + 14 + 528 exactly, and a doc comment was the only thing joining them. The two rooms cannot
# import each other -- virtio_net.rye is freestanding and imports nothing, wire_format.rye reaches
# std and kumara -- so the arithmetic had nowhere to live but prose. Measured before the guard was
# written: raising `wire_capacity` 528 -> 576 and changing nothing else still BUILDS GREEN for
# riscv64-freestanding, and the guests' own frame arithmetic run hosted reads `index out of bounds:
# index 602, len 554`. Twenty-seven of fifty-nine guests lay a datagram in at that offset, and
# dg_len is `off_cipher + message.len`, so a short message never reaches the ceiling and a full one
# lands on it exactly -- content-dependent, which is why a fixture carrying a short message passes
# forever. virtio_net.rye publishes `frame_carries` and device_wire.rye evaluates it at comptime.
# Sixteen control cases on real files in a throwaway pen, twelve refusals and four welcomes, and a
# probe that asks the compiler six times -- the tie inverted, the three plausible real edits
# (capacity raised, ethernet header grown a VLAN tag, virtio header widened by a field), and an
# honest raise of `max_frame` that must walk free. Measured here `20260830.204814`: 6s whole, of
# which the pure scan and control are 2s. At tier cadence because the probe builds a Rye module six
# times over, the same clock its carriage sibling keeps.

guard comlink_nesting
path tools/co/comlink_nesting_witness.rish
tier cadence
seated 20260830.204814

# THE GUARDS OUR OWN RULES ALREADY DEMAND (REDS %360). The census that row books found 1,153
# tracked guards this roster reaches on no lap at all, and five of them are not merely unrun --
# each is named by a LIVING RULE that instructs a lap to run it. A rule that names a check and a
# roster that never runs it are one sentence disagreeing with itself, and the disagreement is not
# free: the sixth of that set, `mechanism_sentence_witness`, reds on metal right now because the
# commit wall's one named seed exemption moved out from under both the publisher and the wall's own
# fixture, and nothing was listening. It stays off this roster until that spelling is Keaton's word
# (custody gate %1); these five stood GREEN on metal before their rows were written, because
# rostering a red guard reds every cold endurance run in the fleet.
#
# WHAT THEY COST, measured here 20260830 on this pier, each run alone: waymark_derive 1s,
# commit_message_guard 2s, waymark_registry 3s, sel4_userlevel_license 0s, crypto_vendored_parity
# 12s -- 18s together against a cold endurance run of 2,425s, which is why every one of them is `lap` rather
# than `cadence`. The cheapest of them was already the most overdue: `gratitude-licenses.md` calls
# the seL4 sweep "a standing check, not a one-time read," and it had never been run by a standing
# pass at all.
#
# WHAT ROSTERING THEM BUYS, stated honestly against the alternative. `unheard` falls 1,153 -> 1,144 (nine)
# and `unheard_choirs` 41 -> 40. That is HALF what the single largest silent choir would have
# bought, and the count is not the value: what a rostered guard is worth is what breaks while it is
# silent. The larger choirs live in Glow, STOA, SUNN and the equinox family, where REDS %360
# measured a 46% red rate in lanes this seat does not hold, and rostering another lane's red guard
# would red every body's cold endurance run.

# instrument_refusal -- a guard that cannot run its instrument refuses, and names it. Three scans in
# two days reached for a helper they did not write, could not run it, and reported a clean tree --
# and one of those readings was a ratchet, which passes on a low number, so a broken instrument and
# a healthy tree printed the same green (REDS %416). Gated at ZERO from birth rather than ratcheted
# down, because the two known faults were repaired on the lap this was written.
guard instrument_refusal
path tools/i/instrument_refusal_witness.rish
tier lap
seated 20260905.072118

# instrument_absence -- an absent tool must not wear that tool's answer. The sibling above gates an
# output pass redirected into a FILE; this gates the capture into a VARIABLE whose emptiness is the
# pass branch, which that reading is blind to by construction. Measured with ripgrep genuinely off
# PATH -- not a shim answering 127, which `command -v` still finds -- two of 117 tracked scans
# naming `rg` printed their GREEN line byte for byte and exited 0, and a Rishi witness over one of
# them read GREEN with no ripgrep on the host (REDS %442). Gated at ZERO from birth: both sites were
# repaired on the lap this was written, and there is no honest reason to author a new one. It makes
# no claim about tiers -- the tool-grant roster stays the yonder design's, and rightly comes first
# there.
guard instrument_absence
path tools/i/instrument_absence_witness.rish
tier lap
seated 20260905.223102

# fleet_roster -- the fleet's one seat table, and the readers that must not disagree with it. The
# binding seat -> tree -> engine stood in six independent places across two executables and two had
# already drifted: fleet-loop.sh admitted six seat names while fleet_rearm.sh reported nine, and the
# elder-name remap seated on Keaton's word `20260904` lived in the loop and never reached the
# re-arm. A table two files disagree about is one table plus a waiting red (REDS %409).
guard fleet_roster
path tools/f/fleet_roster_witness.rish
tier lap
seated 20260904.190000

# fleet_claim -- absence is checkable and intent is not. `path_absence_scan.sh` answers about a
# remote AS IT STOOD WHEN ASKED, and a file that does not exist yet is on no remote, in no ref, in
# no tree -- so no sharper reading could reach it. On `20260911` that gap cost two ships a whole
# build apiece inside one day: incense withdrew `port_registry` (35 constants, 21 files, 44 pen
# legs, GREEN on metal) against a peer's `port_band` landed two hours earlier, and bakery dropped a
# 29-leg port census into that same peer's scan. Both absence readings were honest and both were
# hours stale. `construction/fleet-claims.kyri` is the declaration a peer can read before anything
# exists; this proves the reader reads the ANOINTED REMOTE's copy rather than local bytes, which is
# REDS %457 one layer down and the one fault that would pass every other leg in a pen.
guard fleet_claim
path tools/f/fleet_claim_witness.rish
tier lap
seated 20260911.222157

# fleet_claim_form -- the same board, read for its SHAPE rather than its sentences (REDS %787). The
# content reader above is line-oriented and forgiving by construction: every field rule in its awk
# program guards on `if (name != "")`, so a field above the first header is dropped in silence and a
# record whose own header is eaten donates its five fields to the record before it. On `20260916` a
# hand resolving a rebase conflict glued three records into one 1,547-byte line, ate a header, and
# `--check` answered `verdict=clear` with `claims_live=8` -- so every ship opening a lap in those
# minutes was told the board was well. The damage came from the one operation no tool performs, a
# conflict resolved by a hand on a shared append-only file, which `%291`'s one-writer law does not
# reach because both writers sat in their own checkouts and both obeyed it. Findings split by kind:
# CORRUPTING puts a field in another record and makes the content reader refuse; CONFINED stays
# inside one record and is held at zero here, so an untidy record never reds the fleet's coordination.
guard fleet_claim_form
path tools/f/fleet_claim_form_witness.rish
tier lap
seated 20260916.211440

# fleet_lap_verdict -- a lap that dies instantly is either an agent the loop could not reach or a
# window the account has spent, and the two answers are opposite: stop and fetch a person, against
# hold and try again. On `20260906` the loop knew only the first, so six ships stopped themselves
# between 07:21 and 07:28 against a limit that reset at 07:30 (REDS %471). This guard holds the
# distinction, and holds `%414`'s own wound still stopping the loop at three.
guard fleet_lap_verdict
path tools/f/fleet_lap_verdict_witness.rish
tier lap
seated 20260906.082415

# fleet_watch -- a loop that has exited cannot hold, so something above it has to notice. The watch
# reads tmux window NAMES every pass and re-arms any live seat whose loop process is gone; the two
# things it must never do are type into a keyboard somebody else is using and start a second writer
# on one checkout (%291). Both are proven on a real tmux session in a pen, every refusal planted and
# then lifted (REDS %471, the level above the lap classifier).
guard fleet_watch
path tools/f/fleet_watch_witness.rish
tier lap
seated 20260906.090000

# fleet_clockout -- two stops with two owners want two files. The guard was seated as `fleet_drain`
# and molted to `fleet_clockout` on `20260909.013844`; this header carried the elder word for a day,
# and a receipt written before the molt then read `unrostered` and reddened `standing_equipment` on
# every ship until each hand pruned its own untracked runs ledger. `.loop-gates-only` is the AGENT's,
# set during its own lap and cleared at the top of the next so last night's stop never blocks
# tonight; `.loop-clockout` is a HAND's, read before a lap opens AND after one finishes, and removed
# by the hand that set it. Until `20260908.165143` the loop cleared the only sentinel at the top of
# every iteration, so a stop landing in the twenty-second sleep between laps was erased before
# anything read it and the loop worked on -- failing silently toward *keep running*, and leaving a
# kill as the only reliable hand-stop, which cuts a lap mid-send. Nine behaviors on a real loop
# body in a pen, every sentinel planted and lifted, the two proven distinguishable.
guard fleet_clockout
path tools/f/fleet_clockout_witness.rish
tier lap
seated 20260908.165143

# sunn_front_doors -- the ten witnesses guarding this tree's front doors ran in NO pass until
# `20260908.183744`. They assert the shape of README, CLAUDE, SOURCE, CONTRIBUTING, STEWARDS,
# llms.txt, keys/README and the grain-os overview -- careful, specific, and completely inert,
# because a guard's reach is the roster rather than its own quality. Four stood RED when first run
# by hand, and `sunn8` had been red since `2026-08-23`: a link sweep had removed the front door's
# `**Voice:** [Kyri](context/KYRI.md)` line as collateral, so the tree's README stopped naming its
# own voice for sixteen days while a 190-green roster ran beside it every twenty minutes. The
# repairs were mostly the guards' own: assertions pinned to a private remote the one-page decision
# retired, to Radiant where Gauge is seated, to an en dash the ASCII sweep replaced, to `e49` where
# the tree reads `e125`, and to a lowercase `record` the rule spells `Record`. A guard bolted to a
# moving counter reds on every ordinary advance; these now read the fact rather than the wording.
# The choir sings all ten, so one roster row reaches every front door.
guard sunn_front_doors
path tools/gen/chapter/sunn15_journey_close_witness.rish
tier cadence
seated 20260908.183744

# font5x7_choir -- 45 witnesses covered this font's glyph tables and NOT ONE was reached by any
# pass; `witness_reach_scan.sh` counted every one `unheard`. One choir row rather than 45 roster
# rows, and the family is DISCOVERED rather than listed, since a hand-written list goes stale the
# first lap somebody adds a glyph table and goes stale silently. Two faults met while writing it are
# recorded in the shared scan, both because each read exactly like a pass: a glob assuming a middle
# segment ran 44 of 45, and an untrimmed `wc` newline reached `sh` as two lines. The scan is generic
# from `20260908.191500` and `hunk_choir` is its second caller. Eight behaviors proven in a
# pen, every refusal planted and then lifted, the bound shown from both sides at the bound itself.
#
# ONE HONEST LIMIT, measured rather than assumed: `witness_reach_scan.sh` counts a witness reached
# when a rostered file NAMES it, and this choir discovers its family by glob, so the meter moved by
# ONE when 45 witnesses began running. The 45 genuinely run -- the scan prints `members=45` and
# fails on any red -- and the meter cannot see a glob. Naming all 45 would satisfy the number and
# reintroduce the stale-list fault the discovery exists to prevent, so the run is kept and the
# number is explained here. A meter that would be improved by making the code worse is a meter to
# read alongside the thing, never instead of it.
guard font5x7_choir
path tools/f/font5x7_choir.rish
tier cadence
seated 20260908.185500

# fleet_rearm -- the seat printer and its sentinel wall, proven on a real pen. It was unheard by
# every pass until `20260908.185500`, and this lap had already repaired its shared constant pen, so
# a guard proven green the same hour was still one nothing ran.
guard fleet_rearm
path tools/f/fleet_rearm_witness.rish
tier cadence
seated 20260908.185500

# hunk_choir -- 86 witnesses cover the HUNK ladder, Chapter A's open image module and what grew
# beside it, and NOT ONE was reached by any pass. One choir row rather than 86, over the GENERIC
# `witness_family_scan.sh`: the font choir written one lap earlier was bolted to its own family, and
# the very next family wanted the same thirty lines with one word changed, so the scan took a
# `--dir` and a `--prefix` and both choirs now call it. A third family costs a roster row rather
# than a file. Eleven behaviors proven in a pen, every refusal planted and then lifted, the bound
# shown from both sides at the bound itself, and the two required arguments proven to refuse rather
# than default -- since a silent default picks a family nobody chose.
guard hunk_choir
path tools/h/hunk_choir.rish
tier cadence
seated 20260908.191500

# mycelium_choir -- 81 witnesses cover Mycelium, this tree's own `.myc` notation, and 69 of them
# were reached by no pass. THIRD caller of the generic `witness_family_scan.sh`, and the one that
# shows what generalizing bought: a file of comments and this row, with no scan and no control of
# its own. It declares a `# reach-list:` enumerator like its siblings, so the meter counts what it
# actually sings rather than inferring from a glob.
guard mycelium_choir
path tools/m/mycelium_choir.rish
tier cadence
seated 20260908.192900

# fora_choir -- 32 witnesses cover the FORA ladder, the Constel test-network names that can never
# parse as a live `@p`, and none was reached by any pass. Fourth caller of the generic
# `witness_family_scan.sh`.
guard fora_choir
path tools/f/fora_choir.rish
tier cadence
seated 20260908.203500

# buhr_choir -- 49 witnesses cover BUHR, Compass Equinox 3's Realidream surface and the Quin
# inference vane, and none was reached by any pass. Fifth caller of the same scan.
#
# THE FAMILY NOT TAKEN, named here so the next lap does not reach for it: `equinox` is the largest
# unreached prefix at 112, and it is the one family a prefix choir would get WRONG. The reach scan's
# own header records why -- inferring from the season choir's glob overstated by 111, because the
# pattern selects 144 equinox witnesses while the choir sings the 33 that chain nobody. A family
# whose members chain each other wants its chain read, never its prefix globbed.
guard buhr_choir
path tools/b/buhr_choir.rish
tier cadence
seated 20260908.203500

# equinox_choir_census -- 82 choirs stand in `tools/equinox/witness` and a census on `20260908` read
# them 50 green, 30 red, 2 over bound. All 50 green ones were reached by NO pass, so a green choir that
# stopped being green had nothing to say so. A RATCHET rather than a wall: a wall would refuse the
# roster on 30 reds that stood before anyone looked, and a guard that reds on ordinary standing
# state is one somebody turns off. Both ceilings only fall.
#
# Each choir is PROBED rather than run, through `tools/fixtures/w/witness_probe_scan.sh`, so an OVER-BOUND run
# is told from a RED -- one is a claim about the run, the other about the tree, and two members of
# this room run past the bound this census sets. `tier cadence`: the census costs about fifteen minutes on this pier.
guard equinox_choir_census
path tools/e/equinox_choir_census_witness.rish
tier cadence
seated 20260908.230500

# wrap_ring -- first witness for the bounded-torus moonshot: working memory as a ring of fixed
# circumference C, where the TAME bound and the period are ONE number. It exists because an
# index-only assert proves nothing -- `(i + C) % C == i` holds for every C and says nothing about
# memory -- so every page carries the lap that wrote it and a reader holding a spent lap is refused.
# Two faults were found by running it rather than by reading it: the selftest first expected a page
# to be recycled after ONE lap, when a page is overwritten only when the ring reaches it again; and
# the witness first read stdout, where `std.debug.print` writes to stderr, so a healthy program read
# as a red. `tier cadence`: it compiles a Rye binary, which is choir money rather than lap money.
# THE PRICE WAS READ ON `20260916.025923` AND IT IS SMALL: **2.78 seconds** including the compile
# from source, **1.70** on a second run. It had **0** `ran` receipts in the six days since seating,
# because nothing turned the cadence rota until `20260915`. Whether that price still buys a cadence
# seat is a roster question this reading leaves open rather than answers.
guard wrap_ring
path tools/w/wrap_ring_witness.rish
tier cadence
seated 20260910.062400

# cyclic_witness -- first witness for the cyclic-topos moonshot: a proof that RETURNS to its start
# state is a loop with a period rather than a line that lengthens. The scan digests `git status
# --porcelain` before and after a witness runs; equal digests mean it cycled, and different ones
# print the paths. Porcelain rather than the whole tree because hashing tens of thousands of files
# would cost more than the witness under test, and porcelain already names the three ways residue
# arrives -- an edit, an addition, a removal.
#
# WHAT IT DELIBERATELY CANNOT SEE is named in the scan: a write into a gitignored path, which is
# where laps are SUPPOSED to write; a write outside this tree, which `fleet_call` owns; and a file
# written then restored to identical bytes, which IS a cycle by this definition.
#
# Measured on the seating lap: nine rostered witnesses sampled, eight cycled and one ran PAST THE BOUND
# THIS LAP CHOSE -- `glow_desk_run_witness`, later measured at 455 seconds and exit 0. It builds Glow
# desks with a real toolchain and passes; the reading was a fact about a 280-second bound.
# READ ON `20260916.025923`: **1.25 seconds**, nine control behaviors green, and **0** `ran`
# receipts in the six days since seating. Row 11's own erratum had named it unrun from the other
# side, while the moonshot page still read its row as untouched.
guard cyclic_witness
path tools/c/cyclic_witness_witness.rish
tier cadence
seated 20260910.063900

# cyclic_state -- a second reading of the cyclic-topos moonshot, built on `wrap_ring`'s OWN state
# rather than on git porcelain across the rostered fleet. `cyclic_witness` above asks whether
# RUNNING a witness leaves residue in the tree; this asks whether the DATA STRUCTURE definition
# one's own ring names as cyclic actually returns to a prior state, hashed two ways.
#
# TWO DIGESTS, TWO ANSWERS. Over page BYTES alone, the ring reaches a FIXED POINT at the end of the
# first lap under a constant fill and holds it -- a genuine period-one cycle, though not the
# entry-equals-exit shape the moonshot's own wording first suggested (entry is zeroed; exit is
# filled; they never match). Over bytes PLUS the per-page lap tag -- the field `wrap_ring`'s own
# refusal mechanism depends on -- the state is NEVER periodic: `Ring.lap()` is `written /
# circumference`, `written` only grows, and no later full state can equal an earlier one.
#
# WHY THIS MATTERS BESIDE THE WHITEPAPER. `20260917-204915_bounded-cyclic-computing.md`'s
# definition one reads `wrap_ring` as "a bound is a period" and its definition two reads
# `cyclic_witness` as "a proof that returns is a cycle," treating the two as one shape measured
# twice. This witness shows the SAME ring satisfies definition one's period claim on its content
# while failing definition two's return claim on its full state -- the two definitions can diverge
# on one structure, which the paper's own falsifier names as the case that would matter.
guard cyclic_state
path tools/c/cyclic_state_witness.rish
tier cadence
seated 20260917.214608

# retired_count -- first witness for the joules-as-a-unit moonshot, reshaped by the host read that
# row was ranked on and nobody had taken. `tools/fixtures/e/energy_instrument_scan.sh` answers
# `joule_source=none`, `powercap_domains=0`, `msr_device=no`, `tier=counters` on this pier: no joule
# is readable here by anybody, so the millijoule witness the moonshot named cannot be built at all.
# What the same scan reads is `hw_instructions=yes` and `perf_event_paranoid=2`, and paranoid 2
# permits a process to count its OWN user space -- reachable from Rye through `perf_event_open(2)`
# without the `perf` binary this host also lacks.
#
# The claim it holds: retired instructions is steady where wall time is not. Measured
# `20260911.080700` at load 10.11 over sixteen runs, five times: instruction spread 356-357 ppm,
# wall spread 195,724-1,422,106 ppm -- three orders of magnitude apart, and the wall figure's own
# range is 7.3x while the counter's is one part per million.
#
# A HOST IS NOT A FAULT (REDS %646). Where `perf_event_open` refuses, the program prints
# `counter=unavailable` and reaches GREEN, so a machine without a counter keeps this guard green.
# What IS gated is the distinction -- exactly one branch named, every run. The control mutates both
# measured gates in a throwaway pen and requires each to bite, and builds the unmutated copy beside
# them. `tier cadence`: it compiles a Rye binary three times, which is choir money rather than lap
# money.
guard retired_count
path tools/r/retired_count_witness.rish
tier cadence
seated 20260911.081019

# retired_rank -- the counter's own falsifier, run. `retired_count` above proved retired
# instructions is STEADY where wall time is not; steadiness says nothing about whether the unit
# ORDERS two implementations the way a lane cares about, and row 3 of
# `active-designing/date/20260917/20260917-105154_the-refusal-that-can-fire.md` states that question as its own
# falsifier: the counter ranks two implementations in the opposite order from the clock.
#
# Neither elder program can measure a COMMAND -- both count a loop compiled into themselves -- so
# `tools/rye/retired_exec.rye` forks, arms a hardware counter on the dependent with
# `enable_on_exec` and `inherit`, releases it through a pipe to exec, and reports instructions
# beside wall nanoseconds for that dependent and everything it starts.
#
# What it holds: nineteen behaviors over planted arm pairs whose right answer is arranged before the
# run -- five refusals bitten, the agreeing case named from BOTH positions so a scan that always
# names its first arm reds, the DISAGREEING case shown firing on a sleeper-against-worker pair, two
# arms that are one arm declining to be ranked, and an order-function mutation shown to bite. The
# disagreement leg is the load-bearing one: a reading that cannot say `agree=no` says nothing when
# it says `agree=yes`.
#
# `tier cadence`: nineteen legs each run two shell arms several times, which is choir money.
guard retired_rank
path tools/r/retired_rank_witness.rish
tier cadence
seated 20260917.121546

# fleet_call -- a signal is not a file, so one-writer-per-checkout never reached it. Eight ships run
# one program name from eight trees on one pier, and `pkill -f standing_equipment_run` therefore
# reaches the fleet rather than the lap: measured live on this pier the same hour, sixteen peer
# processes across four trees stood behind that one pattern, and the calling shell's own command
# line carried it too (exit 144, three firings in two laps -- REDS %541). tools/f/fleet_call.sh
# resolves each candidate's working directory and signals only what sits under this tree's own
# root, refusing a peer and refusing the caller's ancestry OUT LOUD -- a kill that misses is silent
# on both sides. Twenty-two behaviors on real processes in a pen, every refusal planted and lifted.
guard fleet_call
path tools/f/fleet_call_witness.rish
tier lap
seated 20260907.073406

# process_reach -- the helper above made the safe form near; this makes the unsafe form loud. Every
# wall in this tree watches FILES, and a pattern kill touches none, so the two firings on `20260907`
# (%569, %587) were invisible to every standing guard. This one reads tracked `.sh` and `.rish`
# sources for a command-position `pkill`, `killall`, or `pgrep` whose selector is a command-line or
# program-name pattern -- the READ counted beside the kill, since `pgrep -f standing_equipment_run`
# answered 22 processes across seven peer trees on this pier the hour it was written. `-P` is
# bounded by parent and free; a comment discusses rather than calls; `ripgrep` can never open a
# fragment; and a site whose boundary is real DECLARES it in words on one of the three lines above,
# so the judgment stays readable instead of drifting inside a hand-kept path list. Ratchet at 2, both
# named in the scan and both in lanes this seat does not hold. Twenty-one behaviors in a pen.
guard process_fanout
path tools/p/process_fanout_witness.rish
tier cadence
seated 20260917.200026

guard process_reach
path tools/p/process_reach_witness.rish
tier lap
seated 20260907.214518

# fleet_key_locality -- one jail binds one tree, so every path a ship's git config names must live
# inside that ship's own tree. grain-pheromone was born naming petrichor's keyring and petrichor's
# deploy keys, and could neither sign nor push from inside its own enclosure while every check at a
# host prompt passed (REDS %427). This reads UNTRACKED files -- `.git/config` and
# `.git/ssh_config_jail` carry custody paths and are untracked by design -- which is why no tracked
# scan, the commit hook, or the link duty could ever have reached it.
guard fleet_key_locality
path tools/f/fleet_key_locality_witness.rish
tier lap
seated 20260905.152000

# fleet_round_open -- the script whose one verb is `reset --hard`, run on three live trees roughly
# every twenty minutes, and which had no control at all until REDS %428. It modeled no interrupted
# rebase, so a conflicted worktree read dirty and got stashed, a detached HEAD got classified, and
# the reset abandoned the rebase leaving the real branch behind. Against the elder script the
# control fails 5 of its 14 legs. Destructive authority earns a standing guard, not on-touch care.
guard fleet_round_open
path tools/f/fleet_round_open_witness.rish
tier lap
seated 20260905.153500

# stash_record -- the round open FILLS a dead-letter box and, until REDS %464, nothing ever looked
# inside it. Measured on this tree: five stashes, and three of them held a complete session log no
# branch carried -- one recording a lap whose CODE had landed, so the work shipped and its
# reasoning did not, and the next lap paid to rediscover it. The gate is `unlanded=0`: a session
# log parked at an open is carried by the worktree, a branch, or a remote-tracking branch. It reads
# the RECORD rather than the box, so it goes green by reading a log back and never by dropping a
# stash. The obvious probe would have been `git log --all`, which reaches refs/stash and reports a
# staged record's own stash index commit as history.
# Widened `20260906.194500` (REDS %510): a record is not the work. The session log is the proxy
# because every lap writes one, and the proxy breaks when a lap's log lands while its code does not
# -- `unlanded=0 verdict=ok` over a box still holding the files. Four stood here across three
# stashes, 258 lines, none of them raising `records` by one. The scan now reads every NON-record
# path too: `orphans` is what nothing outside the box carries, `unread` is what something carries
# and whose stashed EDIT no path probe can judge. Reported rather than gated, because a COUNT does
# not travel between checkouts the way `unlanded=0` does; the round open names both drawers.
# Widened again `20260911.230630`: a fourth kind, `renamed`. The scan's own header named this shape
# in prose -- *a file landed under a different NAME reads as an orphan* -- and counted it in the one
# column a reader is told to act on. All three of this tree's `orphans_work` were the draft-named
# `topology_routed_*` instrument that landed two days later as `topology_stretch_*`. The reading is
# the orphan's own lines asked of a sibling in its own room, with the name MAPPED first: raw, the
# three read 99, 97 and 42 percent; mapped, 100 with nothing unheld. Claimed only above a floor of
# 90 and four times the runner-up, named with its path and its remainder, and the round open now
# prints the work drawer by path. Study: `active-designing/date/20260911/20260911-230630_the-orphan-that-had-already-landed-under-another-name.md`.
guard stash_record
path tools/s/stash_record_witness.rish
tier lap
seated 20260906.073939

# announced_length -- a living page announcing a ladder's length is a forecast, and this counts how
# far each one actually got. The mark law names four such ladders, all found by hand and typed into
# the rule; five more stood in `context/LEXICON.md` unnoticed, where the Compass Chapter's four
# equinoxes announced 256 rungs between them and had reached six. It reports the ladders that MET
# their announcement as loudly as the ones that fell short, because a reading that only accuses is
# one nobody believes.
guard announced_length
path tools/a/announced_length_witness.rish
tier lap
seated 20260905.164500

# captain_view -- the captain reads a peer's work and never a peer's keys. Every peer tree arrives
# read-only and the three credential paths are denied outright, proven by entering a real enclosure
# and trying the thing rather than by reading the flags. `cadence` because the enclosure's shape
# changes on a deliberate edit rather than on a lap, and each run costs a real sandbox launch.
guard captain_view
path tools/ca/captain_view_witness.rish
tier cadence
seated 20260905.181500

# THE MANTRA WITNESSES -- twenty proofs that ran only when a hand remembered. Measured `20260905`:
# `mantra/` holds 32 Rye modules and 9,175 lines, **120 Rye files outside it reach into it** --
# Brushstroke's photo library, Pond's apps, the editor's undo -- and NOT ONE of its ~26 witnesses
# stood on this roster. The tree's most-depended-upon module had no standing guard. All twenty run
# GREEN, timed here, and they are seated by what each one proves: the module's own contracts at
# `lap`, and the surface built on top of it at `cadence`. Core cost, measured: 39s.

guard mantra_bolt_apply_step
path tools/m/mantra_bolt_apply_step_witness.rish
tier lap
seated 20260905.161500

guard mantra_a2_list_fold
path tools/m/mantra_a2_list_fold_witness.rish
tier lap
seated 20260905.161500

guard mantra_gen_floor_a1_gate
path tools/m/mantra_gen_floor_a1_gate_witness.rish
tier lap
seated 20260905.161500

guard mantra_weave_merge
path tools/m/mantra_weave_merge_witness.rish
tier lap
seated 20260906.001728

guard mantra_weave_annotate
path tools/m/mantra_weave_annotate_witness.rish
tier lap
seated 20260906.115458

guard mantra_weave_apply
path tools/m/mantra_weave_apply_witness.rish
tier lap
seated 20260912.041507

# The elder record, lifted rather than converted. `mantra/src/main.rye` writes a three-field
# `mantra-weave-v1` blob and this module widened past it, so the open question (REDS %589) was
# whether closing that gap breaks the record already on disk. It does not: the record was
# written by one hand, so site and run are CONSTANT across it rather than absent from it, and a
# constant run beside a constant site reduces `Place.less_than` to `pos <` -- the elder reader's
# own comparison. `Weave.from_v1` is that sentence as code; the witness compares the two readers
# line for line and proves four refusals the elder reader never had. It ports nothing and moves
# no byte on disk; it makes the port mechanical and the record's safety checkable.
guard mantra_weave_v1_lift
path tools/m/mantra_weave_v1_lift_witness.rish
tier lap
seated 20260908.051232

# The elder record, written back. The lift guard above answered the READING half of REDS %589:
# an elder `mantra-weave-v1` record reads the same after the widening, since site and run are
# constant across a record one hand wrote. `mantra/src/main.rye` WRITES that record on every
# commit, so until a weave the module has advanced can be written back as three fields, "the
# port moves no byte on disk" was a claim about reading wearing the clothes of a claim about
# the record. `Weave.to_v1` is the other half, and the round trip is identity on the DOCUMENT
# rather than on the weave -- the runs come back collapsed to `v1_run`, and the document is
# unmoved because inside ONE site `next_run` and `next_pos` both only rise at the same apply,
# so a higher run always carries a higher position. What the format cannot hold it refuses by
# name: a second site, since the record has no field to tell two hands apart; and a merge whose
# position order has parted from its document order, which the witness BUILDS rather than
# argues. It ports nothing and moves no byte on disk.
guard mantra_weave_v1_write
path tools/m/mantra_weave_v1_write_witness.rish
tier lap
seated 20260908.123338

# The record a merged history fits in. The two guards above named this gap in their own headers
# and neither could close it: `to_v1` refuses a second site by name, and a second site is exactly
# and only what `merge` produces -- so Mantra could merge two histories and had nowhere to put
# the result, since `mantra/src/main.rye` writes the elder record on every commit. `Weave.to_v2`
# and `Weave.from_v2` are the five-field record, and the round trip is identity on the WEAVE
# rather than only on the document: the runs come back as they went. THE COUNTERS ARE CARRIED
# AND DID NOT HAVE TO BE, which this round measured rather than argued -- a first draft asserted
# they cannot be derived, and they can, since `merge` maxes two counters while dropping no line,
# so the side holding the higher counter also contributes the line below it. Claim 3 measures
# that equality on a lifted, an edited and a merged weave; the record carries the two numbers
# anyway because the weave's DECLARED invariant is an upper bound rather than an equality, and
# claim 4 holds a record standing above the accident. Four refusals, three of which an elder
# record could not get wrong. WHAT IS NOT PROVEN: that the CLI writes this record -- it still
# serializes v1, so the format has no writer yet, which is the next lap and its own round for the
# reason the lift and the write were two. `tier lap` beside its sibling at 14.4s: the control
# builds sixteen pens for 20.9s measured under a contended pier, the same order rather than a
# new one.
guard mantra_weave_v2
path tools/m/mantra_weave_v2_witness.rish
tier lap
seated 20260909.233534

# mantra_cli_record -- the record the CLI actually writes, which its sibling above named as the
# thing it did NOT prove. Every other guard in the weave family reads a function inside
# mantra/src/weave.rye; none read a byte the CLI wrote, and the module could build a wide record
# while mantra/src/main.rye kept serializing the elder one. This guard drives the built binary in a
# pen and reads the blobs off disk, including a real elder store frozen at
# tools/fixtures/m/mantra_elder_v1_store/ -- blobs whose names are the SHA3-256 digests the elder
# binary computed, so the compatibility claim is tested against a store rather than a forgery.
guard mantra_cli_record
path tools/m/mantra_cli_record_witness.rish
tier lap
seated 20260910.001500

# mantra_weave_order_record -- the record every store written since `20260916` holds, read whole.
# Its sibling `mantra_weave_v2` proves the ELDER record's lift and lower from both sides, and the
# order-key record that replaced it stood unproven at the record level: `to_order_record` and
# `from_order_record` were read by mantra/src/weave.rye and mantra/src/main.rye and by no guard.
# Three readings did stand over the newer record and each is blind to one thing -- `cli_record`
# asserts the header and the row's field COUNT, `document_roundtrip` compares rendered text -- so
# a writer and a reader agreeing on arity and disagreeing on ASSIGNMENT passes all three. The
# mapping is hand-spelled four times over six fields. `row_eql` walks the row type's own field
# list, `order_record_eql` compares both counters and every field of every row, and a comptime
# assert binds each published count to its struct's own field list. THE MEASUREMENT IS A CONTROL
# LEG: with the reader parsing `site` where `run` was written, `mantra_weave_merge` and
# `mantra_weave_apply` both still reach GREEN, asserted as `blindness_held=yes`. `tier lap` at 30s
# beside its sibling's 14.4s guard and 20.9s control -- the same order rather than a new one.
guard mantra_weave_order_record
path tools/m/mantra_weave_order_record_witness.rish
tier lap
seated 20260917.130215

# mantra_record_column -- which column is which, in the record on disk. A record's columns carry
# meaning by POSITION alone, so moving the writer's tuple and the reader's row TOGETHER changes
# the format's meaning while every round trip stays clean, and every store written before that
# day is misread forever in silence. Measured before the guard was built: planting all ten
# pairwise column swaps on both sides at once, the standing `mantra_cli_record` scan is
# BYTE-IDENTICAL on all ten. Its pen is why -- a fresh store commits once, so its rows read
# `1 0 0 0 0 alpha`, three columns a constant zero and two the same rising sequence, which also
# lets four of the ten ONE-SIDED swaps travel free on each side. The cure is data rather than a
# rule: a second commit carrying a REPLACEMENT gives gen a value above one, pos six distinct
# values and ord a non-decreasing run with a repeat -- four profiles over five columns, read off
# the record's own bytes. TWO HALVES, EACH PRICED BY ITS OWN MUTATION: the column profiles are
# the only reading that sees a consistent swap, the round trip the only one that sees a one-sided
# one, and the control strikes each out and aims a swap only that half catches. THE BOUND IS
# ASSERTED RATHER THAN STATED: site and run share a profile and no store this pen can write will
# part them, since the CLI exposes no merge, so all three families read `blind` on that pair and
# the witness asserts each. `tier lap` beside its siblings' 30s, thirty swap legs at roughly
# four seconds a build.
guard mantra_record_column
path tools/m/mantra_record_column_witness.rish
tier lap
seated 20260917.150224

# mantra_multifile -- a second tracked file keeps the first one's history. A commit blob named ONE
# weave for a whole directory, so `cmd_add` diffed the next file against the file before it and
# applied the difference, deleting every line of the earlier one -- while the CLI printed
# `wove 2/2 file(s).` The .brix add-all route is the tool's own advertised way to track a project
# of more than one source, and it fired hardest there. This guard drives the built binary down
# exactly that route and asks the tool the user's question -- does an untouched file read clean --
# rather than counting blobs, since a record is free to change shape and a promise is not.
guard mantra_multifile
path tools/m/mantra_multifile_witness.rish
tier lap
seated 20260910.003046

# mantra_idempotent_add -- a second `mantra add` over a file nobody edited changes nothing. The
# weave is content-addressed and `commit_files_with` returned the prior list entry for entry, yet
# `cmd_add` wrote a commit blob and moved HEAD on every run, so the record grew with the polling
# rather than with the work; `mantra log` walks at most 1,000 commits, so a watcher calling `add`
# on a timer pushes the real first commit past the end of the log inside a day. `files_agree`
# compares the two lists and `cmd_add` returns without writing when they agree. The scan hashes
# every byte under `.mantra/` after three runs, and reads the opposite direction too -- a real edit
# and a new path must still land -- because a skip is as wrong when it fires too eagerly.
guard mantra_idempotent_add
path tools/m/mantra_idempotent_add_witness.rish
tier lap
seated 20260910.024500

# mantra_record_ceiling -- a record read off disk stops at a line the reader names. `store.rye`
# bounds a blob in BYTES (max_blob_bytes, 1 << 27) and `weave.rye` bounds a weave in LINES
# (max_weave_lines, 1 << 20), and the conversion between the two units had never been taken: a v2
# row costs nine bytes at its shortest, so a blob standing UNDER the store's own ceiling carries
# roughly fourteen times the lines a weave may hold. `read_v2_record` appended every row and read
# its ceiling afterwards with an assert, so a 15,666,516-byte blob -- an eighth of the store's
# ceiling -- made `mantra status` panic at exit 134 after allocating all 1,048,600 rows, where
# `Weave.from_v2` would have refused it by name. `read_commit_v2` sixty lines below already made
# the right move against max_commit_files. Both readers refuse at the edge now; the scan builds
# the reader at two lowered ceilings and has the wide binary write the stores the narrow one
# reads, so no store is forged and no external hash tool is needed.
guard mantra_record_ceiling
path tools/m/mantra_record_ceiling_witness.rish
tier lap
seated 20260910.034822

# mantra_snapshot_batch_count -- what follows the last declared batch? A catalog snapshot's header
# carries a leaf claim and a BATCH COUNT, and `import_catalog` in `snapshot_export.rye` walked
# exactly that many batches and never compared the offset it stopped at against the length of the
# blob. `SnapshotError.BatchCountMismatch` had named that refusal since the file was written and
# no line ever returned it -- a refusal named and unreachable. Measured on metal `20260910.140122`
# in a pen before a line changed: eight bytes of junk appended to an honest 1,292-byte snapshot,
# and both `import_catalog` and `import_catalog_horizon` answered 5 leaves and success, so two
# byte strings imported as one catalog. Lowering the declared count then imported a PREFIX as
# whole -- `drop=1 claim=3` and `drop=2 claim=2` accepted -- since the leaf claim is a field the
# same hand writes; the horizon path refused those with HeadDigestMismatch, which is where the
# defense had actually been standing. The repair is one comparison at the edge, ahead of the leaf
# claim, and the selftest sweeps every lowered count at every claim up to `max_prefix_claim`.
guard mantra_snapshot_batch_count
path tools/m/mantra_snapshot_batch_count_witness.rish
tier lap
seated 20260910.140122

# mantra_document_roundtrip -- does the store return the bytes it was handed? `split_lines` in
# `diff.rye` drops the empty token a text ending in `\n` produces, and a text ending without one
# produces no such token at all, so `x\ny` and `x\ny\n` split to the same two lines. `cmd_add`
# stores that split and nothing else -- a weave and a commit, never the file's own bytes. Measured
# `20260910.043900`: two pens, two files one byte apart, both answered `HEAD -> 441c3c6fa8b8` and
# both weaves carried the digest `7695a361c00e...`, so a content-addressed store gave one address
# to two contents. `status` calls the changed file clean and `add` answers `unchanged`, in both
# directions. Four readings are GATED -- an ordinary edit is still woven and still reported, and
# each file still reads back clean against its own weave -- and seven REPORT the loss, because the
# repair is a seam with three doors and a gate that reds on what no lap may fix gets turned off.
# The control prices door 1 in a pen: keeping the token parts the digests, makes `add` see the
# appended newline, and costs one line per terminated file (4 where the tree reads 3).
guard mantra_document_roundtrip
path tools/m/mantra_document_roundtrip_witness.rish
tier lap
seated 20260910.043900

# mantra_annotate_cli -- the showing reaches a person. `Weave.annotate` in mantra/src/weave.rye has
# read two weaves side by side since the merge landed, one note per position carrying what both
# sides did; every guard over it called that function from Rye. The CLI offered `status`, which
# answers how many lines moved, and nothing that answers what each line DID -- so the showing the
# module was named for stopped at the library's edge. `mantra annotate <file>` carries it across:
# the stored weave is the left side, the working file's edit applied to a second load is the right,
# and each line comes back marked two spaces, `+` or `-` from the same two parities the merge reads.
# This guard is the THIN EDGE beside the happy zone of mantra_weave_annotate -- a real binary, a
# real directory, a file edited between runs, and the bytes a person would see. The water reading is
# three legs rather than one: the store's bytes, HEAD, and the printed answer are each unchanged by
# a second run, since `infusion(world') -> world'` is strictest for a reading. Its edits APPEND and
# DROP on purpose -- `%680` stands OPEN over a replacement -- so it measures the reading rather than
# a seam awaiting Keaton's word. Three control phases each bite their own leg: the two weaves
# swapped tells the story backwards, a blanked mark leaves the count standing, and removing the
# tombstone skip shows a line deleted a commit ago.
guard mantra_annotate_cli
path tools/m/mantra_annotate_cli_witness.rish
tier lap
seated 20260911.053330

# rish_report_bound -- a guard's success report has a ceiling and its refusal does not, and until
# 20260907 nothing read the difference. Rishi composes an interpolated string literal through
# `StrBuf`, a bounded 4,096-byte buffer in rishi/src/main.rye; a bare `say <value>` writes straight
# out under no ceiling. `reds_spine_derive_witness.rish` passed BOTH its gates -- `verdict=ok`,
# `rebindings=0` -- and then ended the run at `say "label ${scan.out}"`, because the reading had
# grown to 4,250 bytes as the ledger grew. Eight ships paid a red every lap for a green reading,
# and the message named the guard's own reporting line rather than the tree. The asymmetry is
# deliberate on Rishi's side and is the finding: an `assert ... else` message that overflows falls
# back to its raw literal, so the REFUSAL path degrades rather than dies -- the unprotected path is
# the one that fires when everything is right. Ratchet at 38 sites across 26 files, never a gate:
# every site is correct today and fails only when its own reading grows, so a gate would refuse
# ordinary work. Narrowed from 504 interpolating sites across 467 files by requiring the file to
# read the same capture with `contains` -- a verdict-bearing report rather than a probe's one line;
# counting the rest would push 466 files toward worse prose to reach the 26 carrying the hazard.
# Three claims proven against the real interpreter on metal in the control, not read out of source.
# `lap` rather than `cadence`: the elder lap measured 19s and this pier re-proved it at 31.7s on
# the day it was rostered, and the shape it counts arrives in any lap that writes a guard.
guard rish_report_bound
path tools/r/rish_report_bound_witness.rish
tier lap
seated 20260907.233349

# The name a blob carries, spent on every read. `mantra/src/store.rye` named a blob by its
# SHA3-256 digest and then never checked it again: `Sha3.hash` stood in `write_blob` alone, so
# the whole content-addressing claim rested on the bytes on disk never changing. Beside it the
# read used `Dir.readFile`, whose `readSliceShort` returns `buffer.len` the moment the buffer
# fills, so a blob past the ceiling came back as its own first megabyte with no error raised.
# Together those are silent corruption rather than two faults: a truncated `mantra-weave-v1`
# record is a VALID PREFIX, so it lifts as a shorter weave and the next commit writes it back
# under its own honest digest. Proven on metal before this row was written -- the elder CLI
# reported a diff against a truncated history and exited 0; the ported one refuses
# `BlobNameMismatch`. Four numeric ceilings stood unnamed across two files, two of them the
# same function at two heights (1 MiB here, 4 MiB in `main.rye`, over one `.mantra/blobs/`);
# `max_blob_bytes` is one number at 128 MiB, so folding them RAISED the shipped capacity --
# which is why the port was declined once already (`20260908.143034`). It builds two binaries
# and writes a 128 MiB file, so it is 21s and a lap guard rather than a cadence one.
guard mantra_store
path tools/m/mantra_store_witness.rish
tier lap
seated 20260908.191500

guard mantra_diff
path tools/m/mantra_diff_witness.rish
tier lap
seated 20260906.061556

# THE TABLECLOTH QUERY OVER THE WIRE, AND THE ANSWER IT HANDS BACK. This witness has stood since
# the query wire landed and was on no roster at all, so nothing heard it -- `%360`'s shape, a
# proof nobody runs, in a module `%664` had just found returning views into a dead frame. It
# builds two binaries, runs both selftests, and drives the answer-slot control's six pens: 15s
# measured `20260909.170000` on this pier, so it is a lap guard rather than a cadence one.
guard mantra_tablecloth_query_wire
path tools/m/mantra_recall_tablecloth_query_wire.rish
tier lap
seated 20260909.170000

# AND THE COUNTER THAT READS THAT GUARD MORE THAN ONCE. The row above answered red on one cold
# pass and green on a re-run minutes later over a tree that had not moved (`%700`), and every
# instrument in this room reads ONE run -- so a law that holds and a law that holds most of the
# time read alike, and a fortnight of greens never raised the question. This guard proves the
# repeat counter that closes that gap: it plants three stub guards with known answers in a pen --
# always green, always red, and refusing on every odd run -- and watches the counter report each
# correctly. The leg that earns the roster seat is `alternating_flaky`, since `flaky=no` is worth
# nothing from an instrument nobody has seen say `flaky=yes`. 3.5s measured `20260911.081500`,
# because the stubs are cheap where their subject is not.
guard mantra_query_wire_flap
path tools/m/mantra_query_wire_flakiness_witness.rish
tier lap
seated 20260911.081500

# THE PORT THE WHOLE PIER WAS SHARING, and the option that made the sharing silent. The counter
# above proved `mantra_tablecloth_query_wire` is flaky under load; this guard holds the cause it
# turned out to have. Seven `mantra/*_delivery.rye` modules opened their UDP socket under
# `setsockopt(SO_REUSEADDR)` whose invariant read *SO_REUSEADDR is what lets a bounded run rebind
# its own port* -- TCP's sentence, since a closed TCP socket sits in TIME_WAIT holding its port.
# UDP has no TIME_WAIT, so the option bought nothing it was named for and bought instead a
# DUPLICATE BIND: two sockets holding one address and port while the kernel splits the datagrams
# between them, with no error on either side. A port belongs to the MACHINE and this pier runs
# eight trees, so the collision was ordinary; two of the seven bind one pair, 38490/38491, so it
# stands inside a single tree too.
#
# TWO GATES AND A LIVE LEG. `reuseaddr_sites` is the regression guard, held at zero.
# `dgram_unproven` asks for more than silence: every module opening SOCK_DGRAM reads the option
# back and asserts it zero, so the property is stated rather than inferred from an absent line. The
# live leg runs the BUILT binary against a foreign holder of 38491 and reads what it says --
# `refused_by_name`, first stderr line `BindFailed`. Measured `20260911` both ways: the elder
# module, built from the commit before the repair, bound beside the holder and printed `GREEN`.
# The live leg takes the pair through `tools/fixtures/m/mantra_delivery_port_lock.sh` first,
# because a probe that binds a machine-wide port bare would red a peer's lap -- the exact fault it
# exists to name. The witness wiring count is REPORTED rather than gated: five of the thirteen
# witnesses driving these binaries belong to the caravan lane. 7.9s measured `20260911.104500`,
# most of it the Zig build the live leg needs.
guard mantra_udp_reuseaddr
path tools/m/mantra_udp_reuseaddr_witness.rish
tier lap
seated 20260911.104500

# THE HAPPY ZONE THAT STOOD BEHIND A THIN EDGE. Measured `20260910.171423`: 26 witnesses in this
# tree reach a wire lab under `comlink/run_*_wire_lab.sh`, and **0 of 26** stood on this roster.
# The cause is one shape repeated -- each welds hosted legs, which want a Zig toolchain and stop
# there, to one device leg spawning `qemu-system-riscv64`. `%646` seats what follows: a leg that
# cannot run here makes the whole row unrostable everywhere, so the hosted asserts go unheard.
# Eleven of the 26 carry **71 such asserts**, every one passing on this bench for an audience of
# nobody.
#
# `mantra_snapshot_wire` is the worked example, split into the two rows below rather than gated as
# one. Gating the welded witness would keep the capability word honest and leave its four hosted
# asserts exactly as unheard as before; splitting hands them a bench they run on. That is the happy
# zone and the thin edge drawn as two roster rows
# (`foundations/20260826-194850_the-happy-zone-and-the-thin-edge.md`). The remaining ten welded
# witnesses are a sweep, named on the card rather than swept here.
#
# The hosted row: build, selftest, import and replay. **2s** measured `20260910.171423` on this
# pier, cold and warm alike -- the Zig build cache holds -- so it is a lap guard. Its sibling below runs this same file as its first leg, so one body proves
# the hosted half and the two can never come to disagree.
guard mantra_snapshot_hosted
path tools/m/mantra_snapshot_hosted.rish
tier lap
seated 20260910.171423

# The wire row: the hosted leg above, then the device crossing under Comlink seal. `capability
# qemu_riscv` is the tier for what a host CAN DO, seated in `capability_state()` beside `ipv6`,
# `jail_nesting` and `trace_instrument`. A bench carrying the emulator runs this row whole; a bench
# without it reads `skipped_capability mantra_snapshot_wire wants=qemu_riscv here=absent` on every
# pass, which is a guard named rather than a guard lost. This pier lacks the emulator today, so the
# row's own value here is the naming.
guard mantra_snapshot_wire
path tools/m/mantra_snapshot_wire.rish
tier lap
capability qemu_riscv
seated 20260910.171423

# Mantra's own width reading, beside the tree-wide one at width_check above. It
# had run green by hand for weeks and stood on no roster, so nothing heard it --
# which is `%360`'s shape, a proof nobody runs. It reads one room and takes
# under a second, so it is a lap guard rather than a cadence one.
#
# IT READS THE ROOM FROM `20260910.025942`, and until then it read four files. The elder body
# opened `let files = ["mantra/src/diff.rye" ... ]` -- four paths -- while `mantra/src/` held
# thirteen tracked sources and `mantra/` held forty-one. `mantra/src/parse_int.rye` landed
# `20260713` and stood eight weeks outside a reading whose own header said it gates the room.
# The verdict was true the whole time and the reach was four files, which is REDS %532's shape
# one room over: a hand-written enumeration standing in for a population reads as coverage from
# every side. The list is derived from `git ls-files` now, the wall widened src/ -> the whole
# room (which costs nothing, since all 41 already read zero), the count of symlinked sources is
# printed so one number cannot stand for two things, and the reading PROVES IT CAN RED on two
# plants under `.lap/` before its silence is trusted -- the grain's own strand (REDS row 59).
# Proven on metal both ways: a usize appended to `mantra/parse_int.rye`, a file the elder list
# never named, reds it.
guard width_check_th3
path tools/w/width_check_th3.rish
tier lap
seated 20260907.172608

guard mantra_declaration_walk
path tools/m/mantra_declaration_walk_witness.rish
tier lap
seated 20260907.081500

guard mantra_a1_equality
path tools/m/mantra_a1_equality_witness.rish
tier cadence
seated 20260906.094442

guard conflict_marker
path tools/c/conflict_marker_witness.rish
tier lap
seated 20260906.105500

guard mantra_glow_tend_limb1
path tools/m/mantra_glow_tend_limb1_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb2
path tools/m/mantra_glow_tend_limb2_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb3
path tools/m/mantra_glow_tend_limb3_witness.rish
tier lap
seated 20260905.161500

guard mantra_glow_tend_limb4
path tools/m/mantra_glow_tend_limb4_witness.rish
tier lap
seated 20260905.161500

# WHAT THE LIMB GUARDS ABOVE CANNOT SEE. Each reads ONE file, and Mantra's weave model is declared
# in three: `mantra/src/weave.rye`, the CLI seed `mantra/src/main.rye`, and the test that inlines
# it. `bc37657e8` widened the module -- `Line` gained `site`, `Diff` gained `site` -- and neither
# copy moved, so the lock on widening `Line` held over a file nobody else was reading. This guard
# reads every tracked copy against the module. It welcomes the two known laggards under a ceiling
# that only falls, since reconciling them rewrites `.mantra/`'s on-disk record and stays a seam at
# Keaton's word, and it refuses any copy arriving disagreeing.
guard mantra_weave_model
path tools/m/mantra_weave_model_witness.rish
tier lap
seated 20260907.210124

guard tube5_mantra_revision
path tools/t/tube5_mantra_revision_witness.rish
tier lap
seated 20260905.161500

# THE CENSUS THAT READS A ROOM AS DEEP AS THE ROOM GOES (REDS %441). The lap that rostered the
# twenty guards above also published, in the same commit, that Mantra's weave "still waits to be
# built" -- while four of those twenty read `mantra/src/weave.rye` every lap and assert the exact
# fields the essay booked three rounds to write. Every number in that essay's table is correct; the
# conclusion crossed two scopes, because `mantra/*.rye` cannot see `mantra/src/`. This publishes the
# trap for every room rather than that one: 44 rooms, 14 carrying a hidden shelf, 513 files below a
# top level, 8 rooms where a flat glob reports zero Rye while the room holds some. It gates the
# INSTRUMENT rather than the tree's shape -- a subdirectory is good structure -- so it refuses only
# when it read nothing or when its three counting patterns disagree. Measured here: 3s.
guard module_room_reach
path tools/m/module_room_reach_witness.rish
tier lap
seated 20260905.225236

# The Realidream surface built ON Mantra, at `cadence` -- the slower clock. These prove an editor
# and a browser rather than the module's own contracts, so they answer on the slower clock. A
# cadence is a clock, never an exemption. Surface cost, measured: 23s.

guard buhr_mantra_browser
path tools/b/buhr_mantra_browser_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_heads
path tools/b/buhr_mantra_browser_heads_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_view
path tools/b/buhr_mantra_browser_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_theme
path tools/b/buhr_mantra_browser_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_theme_stack
path tools/b/buhr_mantra_browser_theme_stack_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_view_theme
path tools/b/buhr_mantra_browser_view_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_browser_full_theme
path tools/b/buhr_mantra_browser_full_theme_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor
path tools/b/buhr_mantra_editor_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_view
path tools/b/buhr_mantra_editor_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_undo
path tools/b/buhr_mantra_editor_undo_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_undo_view
path tools/b/buhr_mantra_editor_undo_view_witness.rish
tier cadence
seated 20260905.161500

guard buhr_mantra_editor_fork
path tools/b/buhr_mantra_editor_fork_witness.rish
tier cadence
seated 20260905.161500

# agent_jail -- the launcher every unattended Linux lap runs through, and the guard that proves it.
# It stood UNROSTERED and RED at its first leg while the launcher worked perfectly: it grepped the
# dry-run plan for `.claude-state` and `.cursor-agent-state`, the directories the `20260827`
# one-room adoption moved under `loops/`. Nothing was listening, so three stale pins stood for eight
# days and the launcher's real fault -- a `~/.claude.json` mount conditional on a file only the
# jailed Claude could write -- rode in beside them (REDS %408, %360's unheard-guard class). Rostered
# the lap it was repaired, since a guard proved green once and heard by nothing guards nothing.
# agent_jail -- SPLIT `20260906.010500` (REDS %455). The whole guard read RED on eight of eight
# ships because four of its legs launch the jail and `bwrap` will not nest, so a ship inside the
# enclosure answered `Failed to make / slave: Operation not permitted` at a leg that could never
# have run there. A red guard withholds the roster receipt, `--scoped` refuses without a full green
# receipt, and every ship therefore paid a full cold endurance run every lap for an environment fact. The
# legs that read plans, paths and seeded files run everywhere and stay here; the legs that launch
# the jail moved to the row below, behind a capability the runner PROBES by attempting a trivial
# bwrap. Not the `host` field %422 declined -- a declaration can be wrong about a bench, and an
# attempt cannot.
guard agent_jail
path tools/ag/agent_jail_witness.rish
tier lap
host linux
seated 20260904.182600

guard agent_jail_enclosure
path tools/ag/agent_jail_enclosure_witness.rish
tier lap
host linux
capability jail_nesting
seated 20260906.010500

# commit_message_guard -- GATE FIELD LIFTED `20260905.115500`. It carried `gate %1` because
# publish-seed.sh was absent from this pier, so SEED_PUBLISHER_ARMS read 0 and the guard reported
# that a fresh seed/.git would publish with the message wall unarmed. The publisher is reconstructed
# and the guard is GREEN, so the gate comes off in the same lap -- a gate left standing over a green
# guard would excuse the NEXT red, which is the one thing %374's field must never do.
guard commit_message_guard
path tools/co/commit_message_guard_witness.rish
tier lap
seated 20260830.154940

guard waymark_derive
path tools/w/waymark_derive_witness.rish
tier lap
seated 20260830.154940

guard waymark_registry
path tools/w/waymark_registry_witness.rish
tier lap
seated 20260830.154940

guard sel4_userlevel_license
path tools/s/sel4_userlevel_license_witness.rish
tier lap
seated 20260830.154940

guard crypto_vendored_parity
path tools/cr/crypto_vendored_parity_suite.rish
tier lap
seated 20260830.154940

# AND THE SIXTH, one lap later (REDS %360, %380's own round). The comment above says this guard
# stays off the roster until the seed subject is Keaton's word. That word came on `20260830.190407`
# and the living card carries it: the subject is `crashed-wave`, spoken by the wall, by the
# field's publisher, and by both rules, and the real publish shipped on it. What remained was a
# tracked file nobody had swept -- `tools/fixtures/m/mechanism_wall_scan.sh` still planted the
# retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed`
# as the borrow attempt beside it, so the fixture the witness trusts was refused by the wall it
# tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling, and the borrower
# keeps its teeth: fed `caravan: crashed-wave` the wall still refuses, since the exemption matches
# the whole subject rather than a suffix. GREEN on metal before this row was written, 1s alone.
# `unheard` fell 1,144 -> 1,143 as this row landed, and came back to 1,144 in the same window as two
# unrostered witnesses landed upstream. The ceiling stands where it was published; the reading is
# what moved, and this row is still the last of the six our own rules demanded.

guard mechanism_sentence
path tools/m/mechanism_sentence_witness.rish
tier lap
seated 20260830.202400

# glow_preset_offset -- Sound's nesting law landed `20260830` carrying its own witness and no clock,
# so the first pass after the fast-forward read `witness_reach` at 1,094 unreached against a ceiling
# of 1,093, and `unheard_guard` rose with it. That guard's own refusal names the answer: put the new
# one on a clock rather than raise the ceiling. Measured 31s on metal here and GREEN before this row
# was written. `lap` rather than `cadence` because Glow's composition is the arc under active change
# -- the same reason `caravan_suite` pays its minutes every pass.

guard glow_preset_offset
path tools/g/glow_preset_offset_witness.rish
tier lap
seated 20260830.233618

# shelf_link_touch -- the commit-time half of the day-shelf link reading (REDS %383). `readme_reach`
# already gates every broken link in a living file at zero, and `reds_fold` runs the same scan as its
# third duty, so one bad shelf row reds two rostered guards -- 45 minutes after it was written. This
# one reads only the shelves a commit stages, off the index, and `tools/hooks/pre-commit` carries it
# as rule four, so a row is refused on the lap that writes it. The two readings cannot disagree about
# the gated class: the pen plants ONE fault and drives BOTH scans over it, requiring both to refuse
# and then both to pass. 28 cases in a throwaway pen, five arming the real hook. 11s on metal, GREEN
# before this row was written. `lap` rather than `cadence` because a shelf row is written on nearly
# every lap this tree runs, which is exactly when a wall this cheap should be listening.

guard shelf_link_touch
path tools/s/shelf_link_touch_witness.rish
tier lap
seated 20260831.003448

# acme_dx and drey -- two choirs the roster could not hear, and the last two this seat can reach
# alone (REDS %360). The census reads a choir as a witness that runs three or more siblings, and a
# silent choir takes its whole family down with it (REDS %219). Forty stood unheard; these two are
# the pair whose families sit in no other body's lane, so rostering them crosses nobody. `acme_dx`
# sings the Acme developer-arc conformance witnesses, one of which -- `operations_conformance` --
# is a choir in its own right, so the pair reaches further than its two rows suggest. `drey` sings
# the Mikrophone firmware rungs, all pure Rye on the bench: no disk, no network, no key, no funds.
# NEITHER FAMILY'S SIZE IS TYPED HERE. Both grow, and a count written beside a thing that grows
# stops the day it is typed -- which is REDS %386, found in `drey`'s own header on this same lap.
# The census counts them: `sh tools/fixtures/u/unheard_guard_scan.sh choirs`. What it read as the
# two rows landed, 20260831: heard 660 -> 688, unheard 1,144 -> 1,116, unheard choirs 40 -> 37.
#
# COST IS WHY BOTH SIT ON `lap` RATHER THAN `cadence`. Measured on this pier 20260831, each run
# alone and GREEN before its row was written: acme_dx 3s, drey 5s -- 8s together, against a cold
# roster pass of 63 minutes. A tier is a cadence rather than an exemption, and eight seconds buys
# no exemption worth taking.

guard acme_dx
path tools/ac/acme_dx_witness.rish
tier lap
seated 20260831.014630

guard drey
path tools/d/drey_witness.rish
tier lap
seated 20260831.014630

# Spool Cloth's name bound, seated 20260831.045858 -- the second half of one room's naming layer arriving
# in the shape museum. Tablecloth's four desks landed 20260829 and 20260830 and the last of them
# left the invitation on its own placard: pond/apps/spool_cloth.rye took the same wall in the same
# shape on the same day. The two modules are housemates rather than neighbours, since
# brushstroke/tablecloth.rye is a symlink into pond/apps/, so this is one room finishing its
# thought rather than a new vane arriving.
#
# The guard is shaped like its sibling: the scan reads the number the desk shows and the number the
# Rye publishes and compares them, holding no value of its own, so raising the bound honestly in
# both rooms walks free -- proven as a welcome beside the refusals rather than assumed.
#
# IT CARRIES TWO READINGS ITS SIBLING DOES NOT, and both were found by reading rather than by
# copying. First, this module reads max_name at TWO call sites: store_large refuses a long name
# with NameTooLong, and parse_manifest refuses a long field with BadManifest -- and the second
# guards an array's end rather than a policy, since a parsed name lands in a fixed [max_name]u8.
# They fire apart, so the control reads the survivor by name while its neighbour refuses. Second,
# the placard's SIXTH line is read by VALUE: PLACARD.md gives the nib its reason -- a pedestal
# displays a value AT a nib -- and every placard reading in the tree counts that keyword and stops,
# so across the 37 residents standing before this desk the stamp after it was compared to nothing.
# It is compared against spool_cloth_version now, so a module whose version moves while its desk
# keeps the elder nib reds here.
#
# A SECOND PEDESTAL JOINED IT 20260831.073652, and it carries a THIRD reading of its own kind.
# max_large_artifacts is shown beside the ceiling artifacts the shared bead store can pay for --
# four seats against a guarantee of two, measured on metal by the module's own selftest, since the
# catalog counts NAMES and the store counts BYTES. That guarantee is derived two levels deep out of
# mantra/beading.rye and mantra/spool.rye, so the scan RECOMPUTES it rather than reading a copy and
# reads the module's own derivation as a fact beside it. And the bound is the length of three fixed
# arrays in TWO modules: the third, KeyedLargeCatalog.owners in pond/apps/spool_keyed.rye, has no
# wall of its own and rides on store_large's through a `try` -- so the scan reads the bound where it
# CROSSES a module boundary, which no desk in this room had done before.
#
# Forty-seven control cases -- seven welcomes, forty refusals -- and thirteen named readings.
#
# `cadence` because the metal legs lower both desks through Glow and build them with the Zig
# toolchain, the same clock its Tablecloth sibling and the six Glow witnesses beside it keep.
# Re-measured on this pier 20260831.073652, GREEN before this row moved: 24s whole -- 17s of it the
# forty-seven-case control, the rest two Zig builds -- against a cold endurance run of 2,317s. The pure scan
# still runs in under a second, so a hand asking it by name pays almost nothing.

guard pond_spool_cloth_glow_tend
path tools/p/pond_spool_cloth_glow_tend_witness.rish
tier cadence
seated 20260831.045858

# living_pin_near_bound -- the near-bound advisory reads the set the pin law names. Duty 6 of the
# docs lint is the one reading in this tree that speaks BEFORE a pin crosses its bound: e123 and
# pin_bound_touch both ask whether a pin is OVER, and a pin three bytes under is not. On 20260831
# the card and the ledger stood at 3 and 21 bytes free with all three of those guards green, and
# the advisory that exists for exactly this said nothing about the ledger -- because it kept a docs
# roster of its own and had never weighed four of the seven seated pins (REDS %396).
#
# `lap` rather than `cadence`: nine pen cases, 1s measured on this pier before the row was written,
# and the reading it guards is the one that decides whether the next lap can write to the card at
# all. Both faults are proven from the side where the reading goes silent -- five of the nine bite
# against the departing scan -- since a repair proven only in the speaking direction cannot be told
# from a rewording.

guard living_pin_near_bound
path tools/l/living_pin_near_bound_witness.rish
seated 20260831.080221

# glow_core -- the first Glow core form, and the one witness of four that landed unheard. The
# `|%` core landed 20260830.224500 under the growth law: three proven Lantern laws -- the 38-byte
# text wall, the unknown-carrying-an-answer refusal, the feed's kind law -- gathered into one
# source with zero new gate grammar. Its witness proves all three arms answer as their standing
# desks and that an unnamed arm, an unclosed core, and an empty core refuse by name.
#
# It arrived with three siblings on the same night and was the only one no runner named, which put
# the unheard reading at 1117 against a ceiling of 1116 -- the ceiling doing exactly the work a
# ratchet is for. A landed law whose guard nobody runs is a law nobody is checking.
#
# `cadence` rather than `lap`: 66s measured on this pier 20260831.114124, GREEN before this row
# moved, which is choir money on the same clock glow_choir and the six Glow witnesses beside it
# already keep. A hand asking it by name pays the same 66s and pays it deliberately.

guard glow_core
path tools/g/glow_core_witness.rish
tier cadence
seated 20260831.121153

# provenance_self_reference -- a page may not be named as its own elder. A molt seats a page at a
# fresh stamp, so the mutant's header carries a provenance line naming the elder it grew from; the
# repoint pass that follows treats that line as a citation and rewrites it to the mutant's own
# name, and the page becomes its own ancestor (REDS %435, twice on `20260826`). Nothing standing
# could see it, because a self-link RESOLVES: `tracked_link_scan` and the broken-link duty test
# with `[ -e ]`, and the report card scores Truth by counting cited paths that FAIL to resolve --
# so `single-stranded` read `truth=100` and `A+/98` with its own first sentence false.
#
# Bounded to this tree's own provenance vocabulary rather than to self-links at large: of the 35
# tracked documents carrying a self-link, the other 33 are honest sibling rosters and dated logs.
guard provenance_self_reference
path tools/p/provenance_self_reference_witness.rish
tier lap
seated 20260905.160212

# amphora -- the vessel family's eight witnesses, and not one of them stood on a clock. How large
# the room is now reads off an instrument rather than off this line: `sh
# tools/fixtures/m/module_room_reach_scan.sh list | grep '^room=amphora '` answers `own=` and
# `linked=` on every run, so a peer's linked module is never counted as this lane's. The elder
# spelling here said 7 and 3,603 lines, corrected by hand on `20260906.063124` from a reading of 10
# that had counted three symlinks into `tally/` and `comlink/`; one day later the line count had
# drifted 273 and nothing was listening (the row booked `20260907.215529`). This roster named no
# guard over any of it. A lap that
# broke a seal, a stamp, or the chunk assembler would have shipped green, because nothing was
# listening -- and the chunk assembler stayed that way until the crossing rows landed. That is
# REDS %360's population exactly -- a guard that is never run guards nothing -- one family at a time.
#
# WHAT THE EIGHT PROVE, in the pairs they were written as. `pour` fills a vessel from the Cellar
# ring-1 season, seals the cargo, then stamps the canonical body; `carry` moves it to a far dock and
# proves the far bytes equal the near ones; `restore` scrubs cold and lands four files at the same
# parent; `grand_round` runs pour, carry and restore end to end and re-restores; `first_resident`
# pours a real counsel season and proves the out-home byte-equal. Each of the first three carries a
# NEGATIVE twin that plants at the wall it names -- a forged manifest line, a forked-season re-pour,
# an absent season, a tampered `seal_cargo`, a truncated vessel, a forged resin body, a torn chunk --
# and asserts the refusal by its own error name, with the standing vessel proven untouched after
# each. So the family already carries the half of the law a green reading can never show on its own:
# every one of these guards is proven able to red.
#
# `lap` rather than `cadence`: all eight GREEN on this pier 20260905, 11 seconds together against a
# lap pass of 838. They build the three binaries they drive rather than assuming a previous lap left
# them behind -- `amphora/bin/` is gitignored, which killed them on a missing file once already
# (REDS %334, and %337 for the absent -femit-bin directory) -- so a fresh clone pays that build and
# the reading stays a fact about Amphora rather than about the last hand.
#
# The two `tools/k/kumara_*` witnesses read UNHEARD beside these and are NOT seated here: both are
# green on this pier, and both drive `kumara/` over `tally/kumara.rye` rather than `amphora/`, so
# they belong to the lane that owns Tally. Named rather than taken.

guard amphora_pour
path tools/am/amphora_pour_witness.rish
tier lap
seated 20260905.225551

guard amphora_pour_negative
path tools/am/amphora_pour_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_carry
path tools/am/amphora_carry_witness.rish
tier lap
seated 20260905.225551

guard amphora_carry_negative
path tools/am/amphora_carry_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_restore
path tools/am/amphora_restore_witness.rish
tier lap
seated 20260905.225551

guard amphora_restore_negative
path tools/am/amphora_restore_negative_witness.rish
tier lap
seated 20260905.225551

guard amphora_grand_round
path tools/am/amphora_grand_round_witness.rish
tier lap
seated 20260905.225551

guard amphora_first_resident
path tools/am/amphora_first_resident_witness.rish
tier lap
seated 20260905.225551

# amphora_roster -- every authored module in the vessel room answers to a rostered guard, or is
# counted. The Sound lane has carried this reading for `tally/` since `20260826`
# (`tools/fixtures/t/tally_roster_scan.sh`); the vessel room had no twin, so its coverage lived in
# prose a hand typed and re-typed. The block below carried a module and line count corrected in
# place on `20260906`, and the line count drifted 273 by the next morning with nothing listening
# (row `20260907.215529`); `amphora/README.md` read *twelve standing witnesses run every lap* while
# fifteen rows stood here, thirteen of them on the lap clock. REDS %360's family, one room over: a
# claim with no instrument. The repair is the instrument rather than a better-typed number, so this
# roster's own vessel counts are now answered by
# `sh tools/fixtures/am/amphora_roster_scan.sh` on every run.
#
# WHAT IT GATES at zero: `uncovered`, a module no rostered guard names -- a break a lap would not
# catch; `orphan_rows`, a row naming a guard file the room lacks -- the roster promising a run
# nobody can make; and `readme_unnamed`, a rostered guard the room's front door never names.
# `singly_covered` is REPORTED and never gated, because a module born today is
# named by exactly one guard on the day it lands, and a ceiling there would red on the most ordinary
# work in the room. Two stand there now, `manifest_entry.rye` and `purchase_delivery.rye`.
#
# THE THIRD GATE LANDED `20260908`, and it closes the half the first repair left open. This block's
# own counts came off an instrument from `20260907.223214` forward, while the guard LIST on
# `amphora/README.md` stayed hand-typed and drifted the same way the counts had: it named 11 of the
# 16 rows seated here, passing over `amphora_roster` -- the guard this scan serves -- and
# `amphora_mark_wreck`, both seated after the list was last written. A count read off an instrument
# beside a list typed by hand is half a repair. It has teeth where `singly_covered` does not for a
# plain reason: seating a guard is a roster row a hand writes, and naming it at the room's door is
# the same hand, the same lap, one line -- so the wall refuses nothing anyone does on purpose. It
# was reached by REPAIR rather than by decree; both missing names were written the lap it landed.
# An absent front door answers the word `absent` rather than zero and gates nothing, so the witness
# asserts this room's door BY PATH.
#
# THE FOURTH GATE LANDED THE SAME DAY, over the numbers still living in that page's prose.
# `readme_spelled_lines` holds the door at zero line counts spelled in digits, and `own_lines` with
# its `detail_lines` listing gives the door something to name instead. The door read
# *`purchase_delivery.rye`, `vessel_fetch_wire.rye` and `vessel_fetch_delivery.rye` are 1,160
# lines*: true at `0ee5c8171`, written into `05c87d3d0` -- the commit that grew
# `vessel_fetch_delivery.rye` by 109 changed lines -- so it shipped 77 short inside the commit that
# made it short, and stood 161 short two days on. The hand measured and then kept working, which no
# amount of care prevents and one reading does.
#
# AND A COUNT SPELLED IN LETTERS DRIFTED UNDER THAT GATE THE SAME DAY. The row closing the fourth
# gate let the word form walk free, saying a number spelled as a word cannot go stale in silence.
# Two paragraphs down the same page it read *Those eight build three modules* while nine of the
# sixteen rostered guards built all three -- written when twelve stood over the room, and read again
# after `amphora_mark_wreck` made sixteen. `readme_spelled_words` reads a number word from `one` to
# `fifty` beside `line`, `module`, `guard` or `witness`, the four nouns this instrument measures,
# and REPORTS rather than gates: the same door carries `three more modules` in a sentence that
# enumerates what it counts, and a wall refusing that would refuse honest prose. Measured across the
# 113 living module front doors outside the closed stacks, 36 pages carry 63 such lines.
#
# A SYMLINK IS ANOTHER LANE'S MODULE. The room is read with `find ! -type l`, so `kumara.rye`,
# `tally_copy.rye` and `wire_format.rye` are counted apart rather than held to this lane's account
# -- the same distinction that corrected the room's own reading from 10 to 7. A SHIM IS FOLLOWED ONE
# HOP, bounded at four, since nine files under `tools/am/` run a target under `tools/gen/amphora/`
# and the module names live in the target.
#
# `tier lap`: shell and awk only, no build, measured under a second against a lap pass of 1,248.
# Proven by 77 planted behaviors on throwaway rooms in
# `tools/fixtures/am/amphora_roster_control.sh` -- every refusal shown from the failing side and
# then lifted, the vacuum leg proving that zero rostered guards over a room holding modules refuses
# rather than reading covered, and the pen proven innocent against a scan patched to always say ok.

guard amphora_roster
path tools/am/amphora_roster_witness.rish
tier lap
seated 20260907.223214

# amphora_manifest_agrees -- the readable listing checked against the cargo it names. Amphora's
# `cargo ` digests were already checked twice, exactly as `the-marked-value` asks: the fetch wire
# recomputes on both sides of the UDP seam, and restore proves every file it writes. The
# `manifest ` listing -- the half a dock reader reads WITHOUT opening the seal, and the one that
# foundation calls "the shoulder line speaks the cargo's marks in plain words" -- was checked for
# SHAPE alone: a known mark, a well-formed digest, a nonempty name. Three readers of those lines
# stood in the tree and not one compared a listed digest to anything.
#
# MEASURED ON METAL `20260910` before a line was written. A vessel whose listing gave `hello.txt` a
# digest of sixty-four zeros passed the Kumara stamp, passed the AEAD seal, and passed a whole
# restore printing `restore files proven count=4`. So did a vessel with one listing line deleted,
# and one carrying a ghost line naming a file no cargo holds. The stamp covers the listing, which
# is what made this look guarded: it proves the head is the head somebody signed, never that the
# head tells the truth about the block beneath it -- and the vessel stamp seed is a witness
# constant in the tree, so re-signing after a tamper costs one command.
#
# WHAT IT GATES: `vessel-seal manifest-check` refuses `ManifestDisagrees` when a listing line and
# its cargo twin differ in any of the three fields, when the listing names more than the cargo, or
# when it names fewer. `carry_verify_walls` runs it, so `carry` and `restore` both refuse and the
# CLI names the wall. ZERO listing lines is lawful and answers `no listing to check`: vessels
# poured before the Q11 listing carry none and their bytes are frozen, so a head making no claim is
# no disagreement -- whether a listing is REQUIRED stays the far-manifest door's question.
#
# PROVEN ABLE TO RED, which a witness owes before its green means anything: build the elder
# `vessel_seal.rye` and it answers `manifest-check` by falling through to its selftest and exiting
# 0, so every refusal leg here fires. Measured that way `20260910` rather than argued.
guard amphora_manifest_agrees
path tools/am/amphora_manifest_agrees_witness.rish
tier lap
seated 20260910.052000

# amphora_contained_name -- a cargo name lands inside the home it was restored into, or it does not
# land. The digests were checked twice over and the NAMES not at all: a digest answers what the
# bytes are, and nothing answered where they may be written. Measured on metal `20260910` before a
# line changed -- a vessel whose cargo named `../escaped.txt` passed the Kumara stamp, passed the
# AEAD seal, passed the listing-agrees walk seated hours earlier, and printed `restore files proven
# count=2`, because the content was honest and only the destination was not. The file landed one
# directory ABOVE its out-home and `restore` refused afterward on the parent compare, with the write
# already done. The rule lives once, in `amphora/manifest_entry.rye` beside the wreck rule it reads
# like, and four doors inherit it -- the seal writer, the stamp writer, the listing check, and the
# cargo reader restore walks -- so the earliest door a vessel meets is the one that speaks. The pen
# REMOVES the wall, watches the escape happen, and returns it, since a refusal proven only from the
# passing side cannot be told from a door with nothing to refuse; and it reaches the cargo door the
# one honest way, with an elder-sealed vessel, because every earlier door refuses first.
# `tier lap`: four rye builds plus three in the elder pen.

guard amphora_contained_name
path tools/am/amphora_contained_name_witness.rish
tier lap
seated 20260910.061500

# amphora_framed_name -- a cargo name is one RECORD. `amphora_contained_name` one row up closed
# what a digest cannot answer: where the bytes land. This closes the question one field further
# in, about the line the name is written on. A cargo line and a manifest line each end at a
# newline, so a name carrying one is written as two records and the second is whatever the name
# said. Measured on metal `20260910` before a line changed: a season of two ordinary files, one
# named `b.txt<LF>manifest plain-bytes <64 hex> evil.txt<LF>cargo plain-bytes <64 hex> evil.txt`,
# poured clean and the sealed, Kumara-stamped head listed FOUR names for those two files, two of
# them a file the season never held at a digest it never produced. The pour printed `pour
# complete`. The rule joins `name_verdict` as a third verdict, `unframed`, reached through an
# exhaustive switch so a verdict added later stops the build rather than riding through; the POUR
# asks it directly, because the pour writes names and parses none. Bytes at or above 0x80 walk
# free -- a season may hold a UTF-8 filename. The pen removes the wall, watches the forged listing
# appear under a real stamp, and returns it. `tier lap`: four rye builds plus three in the pen.

guard amphora_framed_name
path tools/am/amphora_framed_name_witness.rish
tier lap
seated 20260910.064044

# amphora_prove_before_write -- a restore proves every resin while its out-home does not yet
# exist. The two rows above ask WHERE a cargo name may land and which LINE it is written on;
# this asks WHEN the proof runs. `restore_write_prove` read each resin, wrote it, and then
# hashed -- and the hash was taken over the buffer it had just read, so the write sat between a
# read and its own proof for nothing. Measured on metal `20260910.073234` before a line changed:
# a four-file season with one resin body overwritten refused with `cargo unproven` and ALL FOUR
# files standing in the out-home, the forged bytes among them at `nested/leaf.txt`, because that
# entry sorted last and every earlier write had landed. A hand reading the directory rather than
# the exit code sees a season that looks whole. `restore_prove_resins` now walks the whole
# catalog first and `restore_prove_write` proves each body again as it lands, so a resin
# tampered with BETWEEN the walks still writes no wrong byte. The pen strikes the pre-pass out,
# watches three honest files land ahead of the refusal, and returns it. `tier lap`: three rye
# builds plus one in the elder pen.

guard amphora_prove_before_write
path tools/am/amphora_prove_before_write_witness.rish
tier lap
seated 20260910.074214

# amphora_pour_atomic -- the same question one verb over. `prove_before_write` asks WHEN a
# RESTORE proves; this asks what a refused POUR leaves at the target. `pour_ship` wrote the
# vessel unsealed at the target FIRST and ran the seal and the stamp over it afterward, so
# every refusal between those steps destroyed whatever vessel already stood there. Measured
# on metal `20260911` before a line changed: a good 1,565-byte vessel, then a re-pour with
# `AMPHORA_VESSEL_SEAL` absent, and 956 bytes stand at that path carrying the manifest and
# cargo listing in the CLEAR with neither seal nor stamp -- `vessel-core verify` and
# `vessel-seal open-check` both answering InvalidVessel over the wreck of a vessel that had
# passed both minutes earlier. With `AMPHORA_VESSEL_CORE` absent instead, the leavings are
# SEALED and unstamped, which reads legitimate to anyone checking the seal alone. The room's
# own `amphora_pour` witness asserts that a same-season re-pour "welcomes" and never asks what
# the second pour left, so this stood green over a verb that could eat a vessel. The fill, the
# seal and the stamp land on a `.pouring` scratch beside the target now, and `rename(2)` -- one
# filesystem, so POSIX makes it atomic -- replaces the target only once the whole rite has
# passed; every refusal sweeps its own scratch, because a scratch left behind is a clear-cargo
# copy of the season lying beside the vessel. The pen reconstructs the elder shape by making
# the scratch the target and removing the landing, watches it lose the vessel over the same
# plant, and returns the wall. `tier lap`: three rye builds plus one in the elder pen.
#
# AND THE ACCEPTED POUR WAS THE OTHER HALF OF THAT SENTENCE (`20260916.031248`). The paragraph
# above ends on *the room's own pour witness asserts that a same-season re-pour welcomes and
# never asks what the second pour left* -- and this witness asked it only of the REFUSED pour.
# `amphora/vessel_seal.rye` has claimed since the seal landed that the nonce is derived from
# stamp and parent, so *same pour yields the same seal*, and `write_unsealed_vessel` puts the
# constant `amphora_version` in the vessel's `stamp` field rather than a clock. Both halves say
# a second pour owes the first every byte, and nothing in the tree ran it twice. Measured on
# metal: a re-pour of the ring-1 season lands **1,564 bytes byte-identical**, and the same
# season poured into an EMPTY path lands those same bytes -- the second reading being what
# rules out the cheap explanation, since a pour that saw the standing vessel and declined to
# work would compare equal for a reason having nothing to do with determinism. The pen then
# removes the determinism: a copy of `vessel_seal.rye` whose nonce mixes in the process id,
# built and driven through `AMPHORA_VESSEL_SEAL`, makes two pours of one season differ, so the
# byte comparisons have something behind them. Legs **8 to 10**; `tier lap` holds, at 23.5
# seconds measured on metal -- four rye builds now, one of them in the elder pen and one in the
# drift pen.

guard amphora_pour_atomic
path tools/am/amphora_pour_atomic_witness.rish
tier lap
seated 20260911.133000

# amphora_carry_atomic -- the same question a third time, at the dock's own scale.
# `pour_atomic` asks what a refused POUR leaves at the target path; this asks what a refused
# CARRY leaves at the far dock. `carry_cross` assembled the far vessel at the dock's own
# `vessel.kyri` and ferried the resin bodies AFTER it, so a refusal between those steps left a
# dock holding a listing whose bodies had yet to arrive. Measured on metal `20260911.183720` on
# the ring-1 fixture, before a line changed, with one near resin body forged so
# `ferry_prove_resins` answers `cargo unproven` -- the production path, needing no env seam and
# no permission game: a FIRST carry into a fresh dock refuses at the near end and leaves 1,565
# bytes at `dock/vessel.kyri`, byte-equal to the near vessel, with `vessel-core verify` GREEN
# and `vessel-seal open-check` GREEN, standing beside ZERO of the four bodies its manifest
# names. A far `amphora restore` over that dock passes the carry walls, passes the restore
# walls, reads its catalog whole -- `files=4 plain_bytes=378` -- and only then answers `resin
# absent`. So every wall the room offers says yes, and the reader who came for the cargo meets
# the gap at the last step.
#
# The unit that must land whole is the DOCK rather than the file: a directory listing is the
# promise a far reader trusts, so the vessel is the last thing that may appear in it. The
# assembly lands on a `.crossing` scratch inside the dock now, the far walls and the far
# manifest read that scratch, the resins ferry beside it into the dock's own content-addressed
# `resins/`, and `rename(2)` -- one filesystem, so POSIX makes it atomic -- puts `vessel.kyri`
# in place once every body is across. Every refusal after the scratch exists sweeps it, since a
# scratch left standing is a vessel under another name.
#
# What was ALREADY right, and why the first carry is the sharp case: a resin body is addressed
# by its own digest and `guard_identity_fork` holds a dock to one identity for its life, so a
# RE-carry writes the bodies already standing there -- measured, a refused re-carry on the elder
# binary left every body in place. The loss lived where the dock had nothing yet. The witness
# reads both, because a rule holding only in the case someone happened to test is a rule waiting
# for the other case. `tier lap`: three rye builds plus one in the elder pen.

guard amphora_carry_atomic
path tools/am/amphora_carry_atomic_witness.rish
tier lap
seated 20260911.193818

# amphora_pour_agrees -- the three rules above each examine ONE verb, and every witness either
# room owns reads ONE writer. This tree pours Amphora vessels two ways: `amphora/src/main.rye`
# in Rye, and `tools/fixtures/a/amphora_pour.sh` in shell, which feeds ten witnesses. Both
# stamp `format amphora-v1` on what they produce, and until `20260911` they computed `parent`
# over different preimages -- the Rye pour over the cargo LISTING the vessel carries, the shell
# pour over `manifest.kyri`, a sibling FILE that stays at the near dock. One season poured both
# ways: same four names, same four digests, two parents.
#
# Two consequences, the second worse than the first. A vessel arriving alone at a far dock could
# never have its shell-written parent checked, because the preimage stayed home. And `amphora
# restore` over a shell-poured vessel proved every resin, WROTE ALL FOUR FILES, and refused
# afterward at `cargo unproven`, exit 2 -- a refusal after the write, which is `%694`'s own
# lesson one field over. Ten shell witnesses were green throughout, because not one runs restore,
# and `vessel-core verify` carries `parent` into the signed canonical bytes without ever
# recomputing it, so the signature covered a claim nobody checked.
#
# The repair is two mechanisms for one root: the shell pour hashes the listing it writes, and
# `restore_open_catalog` proves that listing against the declared parent while the out-home
# still does not exist -- measured, 4 files left before and 0 after. `restore_same_parent` stays,
# since what LANDED and what the vessel CLAIMS are two guarantees.
#
# Both mutations are proven to bite: the elder shell rule planted, where the two parents MUST
# differ and the forged vessel must still verify AND still open (or the plant proves nothing),
# and the pre-write wall struck out in a source pen, where the same vessel writes its files and
# refuses afterward. `tier lap`: three rye builds plus one in the no-wall pen.
#
# Self-consistency is not agreement. A format with two implementations wants a guard that reads
# one against the other, and that guard belongs to neither implementation's own suite.

guard amphora_pour_agrees
path tools/am/amphora_pour_agrees_witness.rish
tier lap
seated 20260911.231348

# amphora_named_once -- a listing names each place once. Every wall the vessel family carries reads
# ONE cargo line at a time: `mark_verdict` against the engine's two roots, `name_verdict` against
# the containment and framing rules, and each resin body against the digest that addresses it. A
# listing naming `a.txt` on two lines, each carrying an honest digest for a DIFFERENT body, passes
# every one of them, because the fault is the relation between the lines rather than anything on
# either line.
#
# Measured on metal `20260910.090000` in a pen, before a line changed: such a vessel answered
# `listing agrees count=2` at the readable door, crossed a dock whole (`carry complete chunks=3`),
# and restored through `restore resins proven count=2` and `restore files proven count=2` before
# refusing on the parent compare -- with the out-home already standing, holding ONE file named
# `a.txt` whose bytes belonged to the other. Every digest was honest; only the mapping was not.
# Three doors welcomed it and the fourth refused after the mutation, which is the shape
# `20260910.073234` closed one clause over: a refusal after the write is not a wall.
#
# The rule is a bounded roster published once in `amphora/manifest_entry.rye` -- `NameRoster`,
# `roster_admit`, and the three-way `NameAdmit` verdict -- and borrowed by both readers, the way
# they already borrow the wreck rule and the containment rule. A judgment written twice is a
# judgment two files may come to disagree about.
#
# THE TWO WALLS STAND IN SERIES, and the witness says so rather than implying both are proven by
# one path: the listing door refuses first, so the listed plant proves the outer wall alone. The
# ELDER vessel shape -- a head carrying no `manifest ` line, which `verify_manifest_agrees`
# answers as no claim -- gives the restore reader's own wall a leg of its own, refusing at
# `cargo unproven` before `restore resins proven` is reached. Both were proven to bite by removing
# each wall in turn and watching the named leg fall.
guard amphora_named_once
path tools/am/amphora_named_once_witness.rish
tier lap
seated 20260910.094705

# amphora_ferried_body_proven -- a body proves its address before it crosses. `resins/` is
# content-addressed: a resin's FILENAME is the SHA3-256 of its own bytes, and that equality is the
# whole of what makes the directory an address book rather than a pile of files named in hex. The
# restore reader proved it TWICE -- once before writing and once as each body lands
# (`amphora_prove_before_write`). The ferry, which carries those same bodies across a crossing,
# proved it NOT ONCE: it checked only that the filename was digest-LENGTH.
#
# WHAT WAS WRONG. Measured on metal `20260910.114040` in a pen before a line changed: one resin
# overwritten with bytes hashing to `42cd36cf...` while its filename still read `7cab931a...` was
# ferried whole. `carry` printed `resins ferried count=4` and `carry complete`, exit **0**, and the
# forged body stood in the far home under a name that lied about it -- where the far Return reads
# it as the file the listing names. Every digest in the vessel was honest; one body behind them
# was not.
#
# WHY NO GUARD SAW IT. Eleven amphora witnesses plant tampers, and every one plants at a wall that
# EXISTS. `amphora_prove_before_write` proves the restore path's two readings exactly, and the
# ferry is a different reader of the same directory -- so the family's coverage was a statement
# about doors rather than about the bodies passing through them. The comparison had been written
# once and skipped once, which is why the repair PUBLISHES it: `body_addresses` in
# `amphora/src/main.rye`, borrowed by both readers the way they already borrow the wreck rule.
#
# THE FERRY NOW PROVES IN TWO WALKS, like the restore reader beside it: every body before the
# first byte crosses, and each again as it lands, so a resin tampered with BETWEEN the walks writes
# nothing either. The first repair proved as it wrote, and two honest bodies landed before the
# forgery refused -- a refusal after the write is a report, which is the lesson
# `amphora_prove_before_write` seated one reader over.
#
# BOTH WALLS CARRY THEIR OWN LEG, and the second one needed a seam to get it. The walls stand in
# series and the outer refuses first, so every honest input proves the outer alone -- removing the
# inner check left the witness GREEN, measured. `AMPHORA_CARRY_SKIP_PROVE` declines the first pass
# so the writing walk answers alone; it forges nothing and production carries never set it, the
# same shape `AMPHORA_CARRY_TRUST_DOCK` already holds in this file. Each wall was then removed in
# turn and its named leg watched to fall, and the welcome shown returning.
guard amphora_ferried_body_proven
path tools/am/amphora_ferried_body_proven_witness.rish
tier lap
seated 20260910.114040

# amphora_receipt_bundle -- the accepted synthetic receipt crosses as opaque, authenticated
# cargo in the existing vessel. A changed encrypted byte fails both the Kumara stamp and the
# AEAD seal before restore. An unknown vessel version refuses before the local signer can bless
# its meaning. The final leg restores byte-identical receipt bytes and reads the accepted schema
# once. The fixture carries no real identity, data, key, money, network, or production signature.
guard amphora_receipt_bundle
path tools/am/amphora_receipt_bundle_witness.rish
tier lap
seated 20260913.152951

# elf_machine -- a binary's architecture read from its own ELF header rather than from `file`'s
# prose. Glow's two cross-target witnesses asked `file` for a substring and never checked that the
# call had run; this pier has no `file(1)`, so the call exited 127 with empty output and the assert
# fired saying the binary was not AArch64 -- of a binary that carried e_machine=183. Ten sites
# repaired, three left in the Android and APK ladders whose toolchains this pier cannot run.
# `tier lap`: 1s, after the census became one awk pass over ~2,900 runners rather than two
# processes per file, which had cost 17 of an elder 18.

guard elf_machine
path tools/e/elf_machine_witness.rish
tier lap
seated 20260906.040355

# rye_compile_reach -- which authored Rye does anything ever compile? %449 found `mantra/src/weave.rye`
# had not compiled since the toolchain moved, while four rostered guards read it every lap with
# `grep` and stayed green, and booked this census. It walks the corpus, resolves the 227 symlinks
# through `git ls-files -s` and `git cat-file` rather than a non-POSIX `readlink`, gathers every
# build root, and closes the `@import` graph. The GATED reading is %449's own shape: a tracked Rye
# file a runner makes a claim about while no build reaches it, ceiling 0. Files nothing compiles and
# nobody claims are reported rather than gated. That reading read 178 when the row was seated, and
# 114 of the 178 were programs `tools/p/parity_ch01.rish` compiles every parity pass -- it assembles
# their paths from a directory and a bare stem, so none is written anywhere and a census reading for
# filenames called them all unbuilt (%466). The census asks `rye_harness_roster_scan.sh --paths` for
# those now, crediting them only when the assembling script compiles anything at all: `never` reads
# **64** at `20260906.113000`, and `harness_unresolved` publishes what the asking still cannot reach.
# `tier lap`: seated at 5s over sixteen pens; **13s** measured `20260906.113000` over twenty-one.

guard rye_compile_reach
path tools/r/rye_compile_reach_witness.rish
tier lap
seated 20260906.045200

# rye_witness_walker -- a witness proves what it CALLS, and claims the module. Zig analyses lazily,
# so a witness importing a module and calling two of its functions forces exactly those two bodies
# through analysis and walks past every other declaration. Proven on metal `20260906`: a `pub fn`
# assigning a `u32` to a `[]const u8` was appended to `mantra/src/weave.rye` and
# `tools/m/mantra_weave_merge_witness.rish` read GREEN, its own five-break control included. That is
# %470's fault in the file next door and the instrument %463 asked for. A four-line comptime loop
# over `@typeInfo(mod).@"struct".decls` reading each back with `@field` forces every body through;
# the same plant reds once it is present. The GATED reading is a `*_witness.rye` naming its subject
# and not walking it -- 121 subjects, `walked` **2**, ceiling **119**, no slack. `pairs` (284) and
# `ambiguous` (9, a witness whose stem names nothing and which imports several siblings) are
# reported rather than gated, since guessing accuses a file of a promise nobody can show it made.
# The credit carries its condition: `unreached` counts walkers the compile-reach census places in
# `never`, gated at zero, and reads **-1** where that resolver cannot answer -- unknown published as
# unknown (%488's lesson one census over). Control: **37 behaviors**, the ceiling both ways by
# planting 120 pairs rather than by an override the scan does not offer. `tier lap`: **24s**.

guard rye_witness_walker
path tools/r/rye_witness_walker_witness.rish
tier lap
seated 20260906.131501

# amphora_bounds_agree -- the ninth, and the meter that finds unheard guards could not see it.
#
# WHAT IT PROVES. Amphora names the same bound in more than one roof, and this guard proves those
# copies agree. Three readings: same-name declarations across module roofs; value groups aliased
# under three names and two widths; and twelve declared `/// couples:` markers, where a bound in
# one file must equal a named partner in another -- `max_vessel_len` against `vessel_seal` and
# `vessel_core` at u32=8192, `max_resin_bytes` against `vessel_seal.max_seal_plain` at u32=1024,
# and `max_wire_payload` against main's chunk datagram at u32=340 beside u16=340, the width
# difference carried in a note rather than swallowed. Coupling is DECLARED rather than inferred,
# so a partner is named by a hand and never guessed from a matching number.
#
# Nothing else in this tree checks that a bound raised in one roof was raised in the other, and
# REDS %453 (`20260906.010047`) is what that costs: two Amphora bounds tied by hand at comptime,
# a lap after a raise to one of them refused 64 of 2,200 legal lengths with its own selftest
# still GREEN. This guard is the standing form of that repair.
#
# A FOURTH READING, and the class the first three could not state (REDS `20260906.193823`, `20260906.193823`).
# Every relation above compares for EQUALITY -- same name, same value, same declared partner -- and
# the relations that decide whether Amphora refuses a pour are ORDERS. Amphora carried two: its
# season door welcomed a 512-character name into a listing contract holding 200, and its manifest
# roof was declared `= max_cargo_bytes` while the manifest is the same catalog under a three-byte-
# longer prefix. Both refused honestly and both refused LATE, at a wall nothing had declared, and
# this guard read GREEN across the gap because no marker could express `>=`. `/// covers:
# <module>.<name>` says it now -- this bound must be at least that one -- printing src, dst, and a
# SIGNED slack so a reader sees the direction rather than a clamped zero (`%498`). One declaration
# stands today, `max_manifest_name_len` covering `max_season_name_len` at `slack=0`: the wall
# exactly at the door, which is the honest state and visible rather than assumed. A second landed
# since -- `max_cargo_mirror` covering `max_season_files` at `slack=4` -- and the scan prints
# `covers_declarations`, so read that rather than this sentence.
#
# THE FIRST READING READ A TYPED ROSTER, AND THE TREE GROWS PAST A TYPED ROSTER (REDS
# `20260911.022245`). `NAMES="max_vessel_len max_cargo digest_hex_len"` named the three same-name
# families a hand had noticed, and reading (1) exists to DISCOVER the law it states -- when two
# roofs carry one name, either they agree or the name is doing two jobs. Proven by mutation on a
# planted pen: two roofs declaring `max_frame_len` at 4096 and 2048 answered `verdict=ok` and exit
# 0, because nobody had typed that name in. The roster is derived now -- every identifier declared
# as a spelled `u<N>` const in two or more files under the root -- unioned with the seated three,
# which stay seated so a seated name VANISHING from the room still reds. `same_name_seated`,
# `same_name_discovered`, `same_name_read` and `same_name_unseated` print on every run, so a family
# nobody typed is named out loud rather than merely folded in. Measured `20260911.022245`: derived
# and seated both answer **three** and `same_name_unseated=none`, so the widening lowers nothing
# today and holds the door for what lands next. Two new plants prove it from both sides, taking the
# family to twelve planted pairs. This is the shape three TAME-core guards were widened out of on
# `20260908` -- a list holds what it was typed to hold.
#
# THE SECOND READING COULD NOT SEE ITS OWN MEMBERS (REDS `20260911.055500`). An alias group is a
# CLAIM ABOUT MEMBERS -- these differently spelled names mean one quantity -- and the reading summed
# const LINES across the whole group before comparing the numbers it happened to find. So one member
# declared twice at one value answered `declarations=2 signatures=1 status=agree` with `verdict=ok`
# and exit 0, its comparison made entirely out of reading (1)'s own subject read twice, while the
# other two names stood nowhere in the room. Proven on metal against the landed
# `alias_incomplete` plant, both readers in turn. Membership and agreement are two questions now:
# `members` is what the group names, `present` counts the distinct names the room declares, `missing`
# names the rest, a group no member of which stands here reads `absent` and is skipped, and a group
# with one name present says `thin` rather than `agree`. The living room reads `membership=whole`
# for both groups, so the widening lowers nothing today. This is the same fault the FIRST reading
# was widened out of one lap earlier (`20260911.022245`) wearing its other face: that roster was
# typed and could not grow, this one was declared and could not shrink. The `alias_disagree` plant
# gained its third member in the same commit, so its only fault stays the divergence it was planted
# for. Thirteen planted pairs.
#
# PROVEN THREE WAYS, because a leg proven only in the passing direction cannot be told from a
# bypass. Two plants carry the order both ways -- `covers_hold` walks free, `covers_short` is bitten
# at `slack=-312`, which is Amphora's own elder gap rather than an invented number -- and the guard
# was then pointed at the LIVING tree with the elder 512 restored, where it answered
# `verdict=misread` and exit 1, and went green again when the repair returned. A plant proves the
# grammar; the living tree proves the subject.
#
# IT ALREADY CARRIES THE HALF A GREEN READING CANNOT SHOW. Six plants run before any total is
# believed -- same-name, alias and couples -- each proven to FAIL on a divergent plant and PASS
# on an agreeing one, so the refusal is proven from the failing side rather than assumed.
#
# `tier lap`, and the reading carries the pier's state rather than a bare number. Exit 0 on three
# consecutive runs at 1.755s, 1.782s and 1.780s on a quiet pier (`20260906.034025`), and at
# 3.153s, 2.869s and 2.687s at load average 10.2 with all eight ships opening a lap at once
# (`20260906.034409`) -- against full lap passes of 658 and 808 seconds measured the same two
# ways. A wall-clock figure on a shared pier is a reading of the pier as much as of the guard, so
# a lap that meets the slower number has met the expected one. Under two seconds quiet and under
# four loaded, this stays lap-tier under either sky. The affordable witness is the one that keeps
# being run.
#
# A ROOF WAS A PATH, AND THREE FAMILIES WERE ONE FILE READ TWICE (`20260915.174535`). A recursive
# grep follows a symlink, and `amphora/src/manifest_entry.rye` links to `amphora/manifest_entry.rye`
# -- Zig forbids `src/` importing its parent, so the link is how the two roofs share one module.
# Three of the six families reading (1) discovered were therefore one file compared with itself:
# `max_listed_names`, `max_mark_len` and `max_name_len` each answered `declarations=2 signatures=1
# status=agree` about an agreement no divergence could break, and `digest_hex_len` reported five
# declarations across four real files. That is the `alias_incomplete` fault exactly, one reading
# over. Every family prints `roofs` now -- distinct files by inode, since `ls -Li` follows the link
# to the serial number two paths share -- and a family standing on one file reads `one_file`. The
# state is lawful, so the repair is a NAME rather than a refusal, and `same_name_one_file=3` is
# printed so nobody mistakes it for a proof again.
#
# AND THE LAW REACHED A POPULATION EVERY REGEX STEPPED OVER (`20260915.174535`). Reading (1) states
# its law over every same-name const and matches `const <name>: u<N> = <digits>;`, so a const whose
# value is a quoted WORD -- spelling no width -- was outside all four readings. Those are the WIRE
# WORDS: `vessel_format` is `amphora-v1` in `amphora/vessel_core.rye`, the name `parse_vessel`
# returns `UnknownVersion` on, and again in `amphora/src/main.rye`, the name a pour writes;
# `shoulder_ring1_wire` is the seal's associated data, spelled in `vessel_seal.rye` and `main.rye`;
# `mark_plain_bytes` is the cargo mark. Drift in any one writes a vessel this room's own verifier
# refuses, and the version wall proven on `20260913` stood on a word held still by nothing. Reading
# (5) is wholly derived, counts roofs before agreement, and reads `words_read=5 words_agree=3
# words_one_file=2` on the living room -- so the widening lowers nothing today and holds the door
# for the word that lands next. Three plants joined: `word_agree`, `word_diverge`, and
# `roof_symlink`, whose `b.rye` is a real symlink to its `a.rye`. Both repairs proven by mutation --
# counting paths rather than files reds `plant_roof_symlink`, emptying the word roster reds
# `plant_word_positive` -- so sixteen planted pens now stand. The two readings cost nothing a lap
# would notice: 1.599s, 1.545s and 2.038s on three consecutive runs after the widening
# (`20260915.175358`), against the 1.755s, 1.782s and 1.780s seated below. Still lap-tier.
#
# WHY IT STOOD HEARD BY NOTHING FOR 36 DAYS, booked as REDS (`20260906.034025`). It has been
# GREEN and cited in counsel since `20260801`, and `tools/fixtures/u/unheard_guard_scan.sh` never
# named it -- not because it was heard, but because that census draws its population by basename,
# `(witness|suite)`. This guard wears neither word, so it was never unheard there; it was ABSENT.
# 77 asserting runners stand in that blind spot, and the census's own header names the shape
# without ever measuring its size.

guard amphora_bounds_agree
path tools/am/amphora_bounds_agree.rish
tier lap
seated 20260906.034025
# harness_roster, seated 20260906.051706. tools/p/parity_ch01.rish builds and runs 116 Rye programs
# whose paths are written nowhere in this tree: it holds `let dir = "rye/tests"` and a list of 116
# bare stems, and reaches each as `${dir}/${s}.rye`. Two quiet consequences. A census that reads for
# filenames calls all 116 unbuilt -- one did this morning, and its count reached the operator card
# as a fact. And a program added to rye/tests/ and left out of the list is never run while the suite
# still reports GREEN, because the suite asserts over the list it holds rather than over the
# directory it names. The tree already keeps this rule for crypto/ and for module rosters; this
# carries it to the largest assembled site it has. It also ANSWERS that census now: `--paths` prints
# each harness path with the script that assembles it, so the two share one resolver rather than
# each learning to read a harness. That mode skips the build-site awk, which is very nearly the
# whole of the scan's wall time -- 3.8s against 0.5s, measured `20260906.113000`. `tier lap`: seated
# at 4s over 3,073 scripts; **11s** measured `20260906.113000` over twenty-seven control cases --
# one awk over batched files and a candidate grep before the per-file work, the cure %460 applied to
# the runner census, which took this from 35s.

# Widened `20260911.234500`: the corpus is a `find`, so it read every scratch room a ship keeps and
# none of them ships. A lap's normalized copies under `.lap/norm/` put a SECOND copy of one real
# unresolved site into the corpus, `unresolved` read 2 against a ceiling of 1, and this guard plus
# `standing_equipment` downstream both went red over bytes no clone will ever hold. The corpus is
# filtered through `git check-ignore` now, asked only where a repository answers -- the pens are
# plain directories, so every plant still reads. Measured that stamp on this tree: 7,148 scripts
# found, **3,606 of them ignored** (3,557 under `.lap/`, 33 `session-output/`, 19 `loops/`, 1
# `.gnupg-rye/`), leaving 3,542 that ship -- a FREE figure, read before that lap cleared its own
# `.lap/norm/`, after which the same reading answers 127. `ignored_read_past=N` prints the drop,
# since a filter that swallowed the corpus would answer every ratchet with zero and read like
# health. The size is per ship; the fault was structural. And the probe ANSWERS OR REFUSES: the
# first draft hedged it with `2>/dev/null ... || true`, which would have written an empty ignored
# list for a `check-ignore` that could not run at all -- the scratch back in the reading, in
# silence. `instrument_refusal` caught that on the same lap, and a pen leg with a `git` shim
# answering 128 now holds the refusal.
guard harness_roster
path tools/r/rye_harness_roster_witness.rish
tier lap
seated 20260906.051706


# compass_rose -- the return habit, walked. This guard has stood in the tree since the compass was
# seated and stood on NO roster, so nothing ever read its green. Meanwhile its now/order check asked
# `test -f construction/TASKS.md && test -f construction/ROADMAP.md`, and both had been pointer stubs
# since 20260823.103804, when three cards fused into `construction/ITINERARY.md`. Presence resolved,
# the guard printed GREEN, and for fourteen days the rose sent readers to a redirect to learn the now
# (REDS %467). It reads the stations out of the rose document itself now and refuses a station that
# forwards, and `tools/fixtures/c/compass_station_control.sh` proves eighteen behaviors in a pen --
# both refusals and, as hard, the welcomes. `tier lap`: 4s, and it is the walk every ship is told to
# take when direction frays.

guard compass_rose
path tools/co/compass_rose.rish
tier lap
seated 20260906.055500

# standing_equipment -- THE ROSTER'S OWN GUARD, which stood on no roster (REDS %468). This file's
# header says how it is kept honest: `tools/s/standing_equipment_witness.rish` proves every path
# exists, refuses a run card naming a guard never seated, and refuses any recorded RED. No row ran
# it, so on 20260906 it went red and STAYED red -- the scan held `known_capabilities="ipv6"` by hand
# while a peer taught the runner a second probe, `jail_nesting`, and a whole-roster refusal sat
# unread through a cold endurance run that reported `run_verdict=ok`. The note under `comlink_nesting`
# already wrote the lesson: a rule that names a check and a roster that never runs it are one
# sentence disagreeing with itself. That sentence was at this roster's own front door. The scan
# derives the capability list off the runner now, so the drift cannot recur, and this row is what
# makes the next one audible. `tier lap`: 16s, most of it the control's pens over real repositories.
# GREEN on metal before the row was written, since rostering a red guard reds every cold endurance run in the
# fleet.

guard standing_equipment
path tools/s/standing_equipment_witness.rish
tier lap
seated 20260906.060000

# standing_equipment_redleg, seated 20260909.152755. This file's own eleventh line says every guard on
# this roster can red, and until this row nothing read that sentence back. Its sibling half was
# already walled -- `standing_equipment` gates `guards_path_missing` at zero and counts the guards
# that have never run here -- so REDS row 59 stood behind prose while REDS %149's strand stood
# behind a gate. The rung gates a rostered witness carrying NO assertion at zero, which is the
# vacuum row 59 was booked for, and reports under a falling ceiling the guards that assert and
# demonstrate no refusal of their own. All 53 standing at seating DELEGATE -- each asserts on a
# real run and reds through its child -- so the number is a proxy read for direction rather than a
# wall, and gating it would refuse every choir this tree owns. `tier lap`: under a second for the
# roster read, a few more for the control's pen. GREEN on metal before the row was written.
# AMENDED 20260915.212000: the marker set widened from four spellings to six, after a hand read all
# fifty-three by eye. Two guards demonstrated a refusal the four could not see -- an asserted
# refusal VALUE (`glow_vane_pair_mirrors` calls a gate over its bound and asserts the digit "0")
# and an asserted refusal EXIT (`comlink_rehearsal_wire` hands a child a stranger role and asserts
# it exits 2). The value form reads past a line naming a captured stream, which keeps
# `gen_home`'s census of the live tree out of the marker set; that exclusion carries its own two
# control legs. The count fell 53 to 51 and the ceiling with it; the control stands at 24 legs, six
# of them new, and three mutations were proven to bite each on its own leg.

guard standing_equipment_redleg
path tools/s/standing_equipment_redleg_witness.rish
tier lap
seated 20260909.152755

# copy_lag, seated 20260906.070240. Zig admits an import only from the root file's own directory,
# so one implementation reaches two rooms here by symlink -- 220 of the 414 paths under a shared
# basename are mode 120000. Where a room keeps a REGULAR file under a name its siblings link, that
# file is the one path that can fall behind the implementation with no diff anyone reads, and the
# tree guarded exactly ONE such name: copy_sameness_scan.sh compares every tally_copy.rye against
# tally/copy.rye, canon spelled in the script. This carries the same rule to all 108 basenames held
# both ways, deriving each canon by resolving its own symlinks rather than by naming it. Gated:
# `behind` under a ceiling of 2 that only falls, and `split_canon` at zero. Reported: `sibling`,
# where a copy publishes something its namesake lacks, which is two modules sharing a word rather
# than one drifting. `tier lap`: 8s, one git ls-files and 5 comparisons.
#
# A PLANT IS BUILT TO DIFFER (`20260915.184500`). Every `.rye` under `tools/fixtures/` is a
# control's own planted material -- `word_agree/b.rye` agrees with its `a.rye` on purpose and
# `word_diverge/b.rye` disagrees on purpose, because a refusal proven only in the passing direction
# cannot be told from a bypass. Those rooms also name their plants `a.rye` and `b.rye`, so every
# pen shares a basename with every other pen, and the first SYMLINKED plant to land nominated a
# canon for all of them: two pen files read `behind` a third pen's file they have no relation to,
# and `basenames_both_ways` rose 108 -> 110 over a header that names 108. The 64 fixture paths are
# read past now and COUNTED as `fixtures_read_past`, since an exclusion nobody can see is a door --
# the same exclusion `ascii_document_scan.sh` makes, for the same stated reason. Proven from both
# sides: the identical lagging file reads `behind=0` under `tools/fixtures/` and `behind=1` in an
# ordinary room, so the filter reads a PATH rather than a shape. Pen 31 -> 36.

guard copy_lag
path tools/co/copy_lag_witness.rish
tier lap
seated 20260906.070240

# tlb_reach -- a table is bounded twice, and only one bound appears in `@sizeOf`. The elder
# footprint study measured cost against bytes touched; a load's address is translated first, and
# that structure's capacity is counted in PAGES. Measured on this pier 20260906: 64 pages of span
# cost 1.65 ns per dependent read and 65 pages cost 3.90 -- same bytes, same instructions, same
# L1d sets, one more page -- and 2 MiB pages return the ratio to 1.00, which is how the cause is
# known. This rung proves the CENSUS rather than the reading: 38 planted behaviors, eight legs
# from the failing side, the clean baseline asserted before and after every plant, and each plant
# checked to have changed the reading it touched. The eighth leg, `elder_arm_ran`, gates the arm
# that reads the elder study's own three sizes under both page sizes -- gated for PRESENCE, since
# an arm that quietly did not run prints no attribution and reads exactly like one that ran and
# found nothing. The census itself stays UNROSTERED on purpose --
# 64 pages is a property of the board, so a guard asserting it would red the day the fleet moves
# part, and a guard that reds on ordinary work is one somebody turns off. The build leg is here
# because %449 and %463 measured what an uncompiled authored `.rye` costs. `tier lap`: 22s, of
# which 19 is that build.

guard tlb_reach
path tools/t/tlb_reach_witness.rish
tier lap
seated 20260906.061229
# amphora, the crossing -- the three modules the nine guards above never compile.
#
# WHAT WAS MEASURED (`20260906.063124`), and how. Every one of the 177 rostered paths standing that
# hour was read together with its own
# fixtures two levels deep (`awk '/^path /{print $2}'` over this file, then each guard's
# `tools/fixtures/...` references, then theirs). The eight amphora witnesses build exactly three
# modules -- `amphora/src/main.rye`, `vessel_core.rye`, `vessel_seal.rye` -- whose `@import`
# closure adds `manifest_entry.rye` and the two symlinked bounds files. `purchase_delivery.rye`,
# `vessel_fetch_wire.rye` and `vessel_fetch_delivery.rye` were compiled by NOTHING a lap runs:
# 1,160 lines, 32% of the 3,603 this lane owns.
#
# THE BLOCK ABOVE NAMED THIS GAP IN ITS OWN WORDS AND DID NOT CLOSE IT. It reads *a lap that
# broke a seal, a stamp, or the chunk assembler would have shipped green, because nothing was
# listening*, offering that as the state its eight rows cured -- and the chunk assembler is
# exactly these two wire modules, which `grep -l chunk amphora/*.rye` answers and no rostered
# guard compiled. Three comlink guards READ `vessel_fetch_wire.rye` as text for a bound tie
# (`tools/fixtures/c/comlink_carriage_scan.sh:68` binds it to `vessel=`) and none of the three
# builds it. That is REDS %449's shape one room over -- grep-reach standing in for compile-reach
# -- and in `mantra/` it had already bitten. Here it had not: all three build today, measured
# before these rows were written, so this is a coverage repair rather than a repair of rot.
# Booked as REDS %477 (`20260906.063124`). That row read `%466` while it sat unsent in a stash and
# `%472` while it sat in an unpushed commit; `xy` spent both in the meantime, and an unshared row
# renumbers -- the derived spine's own law, twice in one lap.
#
# `rye_compile_reach` READS ALL THREE AS `built`, and is right to. It asks whether ANY runner in
# the tree compiles a module; this roster asks whether a RUN one does. Its `built=1535` therefore
# counts runners nobody runs -- REDS %360's population seen from the other side -- so a green
# reading there settles the compile question and leaves the coverage question open. Two meters,
# two questions, and the second is the one that decides whether a lap would catch a break.
#
# THE HEADLINE ABOVE IS CORRECTED IN PLACE. *10 tracked Rye modules and 3,861 lines* counts three
# paths this lane does not own: `kumara.rye` and `tally_copy.rye` are symlinks into `tally/`, and
# `wire_format.rye` into `comlink/`. Seven modules and 3,603 lines is the lane's own figure. REDS
# %463's *resolution before accusation*, arriving in a roster comment rather than in a census.
#
# THE OTHER FIVE RUNNERS ARE MEASURED AND DECLINED, each by name rather than by omission.
# `amphora_lap1`, `amphora_lap2`, `amphora_vessel_seal` and `amphora_vessel_stamp` build only
# modules the nine above already compile, so they would add claims rather than reach, and the
# family already proves pour, carry, restore and three tamper refusals. `amphora_device_wire`
# drives `comlink/run_vessel_fetch_wire_lab.sh` under virtio and this pier holds no qemu. It
# REFUSES HONESTLY -- exit 1, and the one GREEN in its output is the substring an assert was
# looking for rather than a claim it made -- which is the good half of REDS %460, whose open
# question is whether such a witness may read GREEN with a named gap. Rostering it here would put
# an environment fact on a lap clock.
#
# `tier lap`, with the pier's state beside the number. GREEN at 3s, 3s and 2s -- 8 seconds
# together -- at load average 8.3 with the emitted binaries deleted first, so each rebuilt from
# source against a warm Zig cache (`20260906.063124`), against a full lap pass of 704 seconds
# measured the same hour. Re-measured warm at 2s, 2s and 1s three times each (`20260906.093709`)
# against a pass of 840. All twelve amphora rows together now cost roughly 16 of those 840.
#
# AND THE RUNNING FOUND WHAT THE READING COULD NOT. These three were rostered on a measurement of
# COVERAGE -- which modules a lap compiles -- and the first lap to actually run them repeatedly
# found `amphora/vessel_fetch_delivery.rye` stalling forever, one run in six to eight, alone on a
# quiet pier. Both halves of the module's socket habit were races: it opened, bound, received and
# closed a socket per datagram, so the receiver was unbound in every gap, and a UDP datagram
# arriving at an unbound port is discarded with no trace on either side; and `recvfrom` carried no
# `SO_RCVTIMEO`, so the receive that followed waited forever. The elder form did red -- at the
# selftest, naming a selftest that was fine -- because a failed `rye build` overwrites the emitted
# binary with a 5,547-byte artifact, which the warm-binary fallback beside it could never have
# survived. Measured, elder against repaired, same pier and same hour at load 8.5: 2 stalls in 20
# against 0 in 40. REDS %478 (`20260906.093709`).
#
# THREE THINGS STAND BEHIND THESE ROWS NOW, and they are different instruments. The module binds
# before it sends and holds one socket across an exchange, with every receive bounded and named --
# that is the repair. `tools/fixtures/a/amphora_vessel_port_lock.sh` holds UDP 38494/38495 at a
# HOST path around every step that speaks them, because a port belongs to the machine and eight
# trees reach for one pair -- that is what keeps two lawful runs from meeting. And the fixtures
# bound their two long-lived processes with `timeout`, so whatever slips past both reds in a minute
# rather than stopping the pass. Proven from both sides: two concurrent `chunkdemo` runs abort at
# 134 in 31 seconds unlocked, and pass in 1 to 2 seconds through the lock, three trials each.
#
# THE REFUSAL REACHES THE READER THROUGH THE SHIM. `tools/am/amphora_*.rish` are accrete shims onto
# `tools/gen/amphora/`, and they forwarded `r.out` alone -- so a planted break made the direct run
# say *no longer compiles* and the roster's own path say nothing at all, filing an evidence page
# naming the guard and no reason. The exit code always travelled; the sentence did not. These three
# forward `r.err` now, and the other five took the same two lines on `20260906.124500` -- all nine
# amphora shims carry their target's reason, and `shim_reason` below holds the habit for the tree.

guard amphora_lap3
path tools/am/amphora_lap3.rish
tier lap
seated 20260906.063124

guard amphora_resin_chunk
path tools/am/amphora_resin_chunk.rish
tier lap
seated 20260906.063124

guard amphora_purchase_delivery
path tools/am/amphora_purchase_delivery.rish
tier lap
seated 20260906.063124

# amphora_asker_reply -- a source answers the asker, and the kernel names the asker's port.
#
# WHAT WAS WRONG. `amphora/vessel_fetch_delivery.rye` held two UDP port numbers in its own source:
# `fetcher_port` 38494 and `source_port` 38495. The source sent every answer to the first, and every
# asker bound that same number, so the pair agreed with itself and nothing it did alone could tell
# *answers the asker* from *answers 38494*. Eight lines above the constants the same file says THE
# HOST NUMBERS THESE, NEVER THIS FILE about `SOL_SOCKET`, and eight lines above that THE KERNEL LAYS
# THIS OUT, NEVER THIS FILE about `sockaddr_in` -- both taken back from the source and handed to the
# host after REDS %282. The port stood between them, unread. REDS %485 (`20260906.113014`).
#
# WHAT IS TRUE NOW. `recv_on` hands the caller the address `recvfrom` already filled in and the
# elder code discarded; `send_resin_response` answers that address; `fetch_one` binds
# `ephemeral_port` -- zero, the ask rather than the port -- and the kernel returns a free one
# atomically, which no probe-then-bind can do. `bound_port` reads it back with `getsockname` and the
# asker prints it. `fetcher_port` is gone. `source_port` stays, because a service answers on a
# number its askers can name.
#
# THE SECOND FAULT, FOUND BY THE FIRST REPAIR REFUSING. Aiming the answer at the asker made the demo
# fail at once: `send_wire` opened a fresh socket per send, so a request's return address named an
# ephemeral port the asker was not listening on and closed a moment later. The source answered
# honestly and answered nobody. An exchange speaks and listens through ONE fd now -- `send_from`
# takes the caller's socket, and `send_wire`, `send_to` and `recv_wire` are gone rather than left
# uncalled.
#
# WHAT THE FIVE LEGS PROVE, and the leg that corrected the round's own expectation. The control
# plants both elder faults into a pen copy by transform rather than by git ref, so it survives a
# history rewrite. PORT plants the written reply number on both ends; SPLIT plants the two-socket
# asker. Crossed against the repair: a PORT source cannot answer a repaired asker, and a repaired
# source cannot answer a SPLIT asker -- both refusals bitten. A repaired source DOES answer a PORT
# asker, which the first draft expected to refuse and measured otherwise: a fixed-port asker that
# speaks and listens through one socket is still answerable, so the repair is a superset of the
# elder rather than merely different. Each planted pair talking to itself is answered, which is what
# proves the pen, the pour, and the fixture innocent.
#
# WHAT WAS TRIED AND DISCARDED. Two askers at once against one source was the obvious control and it
# discriminates nothing -- both binaries passed it in half a second, because two short exchanges
# stagger by milliseconds and never overlap. It stands as a claim on the repaired bytes, where it
# can still red, and never as the control. A test both bytes pass proves only that the test ran.
#
# `tier cadence`, and the reason is the bound rather than the cost. Two crossed legs each spend the
# module's own three attempts of ten seconds, because what they establish is that nothing arrives,
# and nothing takes exactly as long to prove as the bound says it does. Measured `20260906.113014`
# on this pier: 68s for the whole guard, of which 62 are those two legs and two plant builds, against
# a full lap pass of 840s. The three lap-tier amphora rows already build and run this module every
# lap; this one asks the question they cannot, and it changes only when the module does.
#
# WHAT THIS DOES NOT REACH. The port lock stands. `source_port` is still one number the machine owns
# and eight trees reach for, so `tools/fixtures/a/amphora_vessel_port_lock.sh` still keeps two lawful
# runs from meeting. Halving the shared resource is not removing it; the source's own port discovery
# is the booked lap after this one.
#
# AND IT HAD NOT RUN ONCE IN THE FOUR DAYS SINCE, on this pier (`20260910.234500`). The cost argument
# above is sound and the tier it chose has no clock: REDS %568 booked `--tier cadence` with no caller
# on `20260907.113635` and stands OPEN, while the tier grew from 58 guards to 74 in the three days
# after. Measured on this tree the same stamp: of those 74, `construction/standing-equipment-runs.kyri`
# holds a run row for exactly ONE, and that row records `lap` -- so the cadence tier has produced
# zero cadence runs here. Asked by hand, this guard answers GREEN in 69s, matching the 68s its
# seating measured; its sibling `amphora_udp_reuseaddr` answers GREEN in 104s. Both silences were
# honest, and only the asking established that. A lap pass of 251 guards runs 2,940s, so moving the
# pair to `tier lap` would spend 173s twice a lap to reprove what changes only when
# `amphora/vessel_fetch_delivery.rye` changes -- which is why the rows below stay where they are and
# the clock, rather than the tier, is what wants Keaton's word.

guard amphora_asker_reply
path tools/am/amphora_asker_reply.rish
tier cadence
seated 20260906.113014
# signal_trap -- a POSIX trap handler runs and then RESUMES the script, so
# `trap 'rm -rf "$pen"' EXIT INT TERM` reads as "clean up and stop" and behaves as "clean up and
# carry on". Measured at `15f99e1fe0` before this round's repair: 143 command-position traps
# naming INT or TERM across tracked shell sources, and exactly ONE called exit -- because
# `shell_portable.sh`'s own header taught the broken form beside `lock_acquire`, so the shape had
# one source and spread from it. GATED at `lock_releasing 0`: no handler may free a checkout's
# mutual exclusion and let the script run on, which is the severe subset because another writer
# then enters a tree that believes it is alone. `files_without_set_e` (48) is REPORTED rather than
# gated -- those run to completion and publish counts computed from a deleted directory, observed
# on two of this tree's own fixtures at exit 0 with every count collapsed to zero. Both were
# caught, because the witness above them asserts on the counts that collapsed; a witness asserting
# only a ceiling would not be, since a ceiling claim reads MORE true when the evidence is gone.
# Study: external-research/date/20260906/20260906-102443_the-severity-lives-in-the-witness.md. `tier lap`: 6s.

guard signal_trap
path tools/s/signal_trap_witness.rish
tier lap
seated 20260906.120137

# fold_shelf_link -- a link that kept the depth of the file it was folded out of (REDS %491, closing
# %474's booked loom). The card sits in `construction/`, so a link it writes as `](archive/X)`,
# `](../Y)`, or a bare `](Z)` is correct there and broken the moment that text is folded onto
# `construction/archive/`.
# FOUR link guards read this tree and none reaches it, three of them for one shared reason: a fold
# shelf's basename carries a one-clock stamp, stamped means testimony, and testimony is where
# `tracked_link` reads past and `readme_reach` stops gating. The fold's own output wears the exact
# property that makes every guard let it go. Gating testimony is lawful here and nowhere else,
# because a depth-lost link was never correct in the file it sits in -- the fold broke it in the
# same act that created the file -- and it is told from a stale reference mechanically: it resolves
# under exactly ONE card-to-shelf correction where a stale one resolves under neither. Ambiguity is
# impossible by construction: `archive/`, `../`, and neither-of-those partition every relative
# target, and an absolute path takes no arm. That third arm was MISSING until `20260908.072125`,
# when the gate was found reading zero over nine bare siblings sitting in its own reported column --
# the correction block had enumerated the two spellings a hand had just repaired rather than
# deriving them from the rule those two spell. 5s on metal; `lap`
# rather than `cadence` because the live front folds by hand on nearly every lap, and this fault
# fired three times in one lap under the hand that had just written it down.

guard fold_shelf_link
path tools/f/fold_shelf_link_witness.rish
tier lap
seated 20260906.120905

# shim_reason -- a pass-through shim hands on its target's reason, not only its verdict.
#
# WHAT WAS WRONG, measured on a real file with no plant. `tools/am/*.rish` and their kin are accrete
# shims: each runs a target under `tools/gen/` and exits that target's code, so either path may be
# named and the answer is the same. Rishi's `run` captures the target's stderr into `r.err`, and a
# shim that says only `r.out` writes nothing to its own stderr. `amphora_device_wire` refuses
# honestly on this pier -- it drives a virtio lab and there is no qemu here -- so the two readings
# stand side by side without breaking anything: the target run directly says `rishi: assertion
# failed -- Amphora vessel fetch device wire lab failed`, with the line number; the shim run the same
# second exits 1 with ZERO bytes on stderr, and all a reader receives is `amphora-device-wire:
# virtio fetch-by-digest lab...`, a progress line printed BEFORE the failure, which reads like
# success. This runner captures a guard with `> out 2>&1`, so the merge was already there and caught
# nothing: the shim emitted nothing to merge.
#
# A GATE AND A RATCHET, because this is a trap rather than a live fault. Zero swallowing shims stand
# on this roster, so nothing is lost today; it springs on the lap that ROSTERS one, and %360's
# standing pressure is to roster more. The amphora lane walked into it on `20260906.063124` -- three
# shims onto a lap clock, the swallow found by planting a break and repaired in the same breath. The
# gate is what spares the next lane that discovery. The ratchet is the residue off the roster, and it
# only falls; the scan's own `unrostered_ceiling` line carries the number so no page can drift from
# it. Nine of 52 shims forward the reason today, and five of those nine were repaired in this lap.
#
# THE GATE IS PROVEN ABLE TO BITE, which matters more than usual here: `swallow_rostered` reads zero
# on this field and always has, so nothing in the tree could show it working. The pen supplies the
# case -- one swallowing shim named by a `path` row, refused; the same shim with one line added,
# freed. The ceiling is proven from both sides on ONE plant, two unrostered swallowers read at a
# ceiling of two and then of one.
#
# THREE MARKS, TOGETHER. A shim is the `run ["rishi/bin/rishi" "run" ...]` call, the `say r.out`
# forward, and the `exit r.code` tail, all three -- each alone is ordinary, since witnesses run
# witnesses and plenty of scripts say a captured stdout. A seat is a `path` row at column zero,
# never a path mentioned in a roster comment; unanchored, a guard could count as rostered because
# somebody wrote about it, and the pen plants that sentence. Two tracked files exit a captured run's
# code under another variable name and are invisible to the reading; neither is a shim, and both are
# named every pass as `exit_alias_sites`, so the floor carries a number (%466).
#
# `tier lap`, and cheap because the corpus arrives through one `git grep` whose status is classified
# rather than swallowed (%473, %484). Three greps over 2,362 tracked `.rish` files cost 22 seconds
# measured on this pier at load 10.1; the pre-filter reads the identical counts in 2, and the whole
# guard including its 40-case pen runs in 5 against a full lap pass of 729.
#
# A SECOND SHAPE OF THE SAME LOSS, seated `20260906.233111` -- and found by a red rather than by a
# search. `fleet_watch` reddened a cold endurance run, filed seven lines that could not name a behavior, then
# passed 16 of 16 on six consecutive re-runs: the one reading that could have diagnosed the flake
# was the reading its witness threw away. `tools/fixtures/f/fleet_watch_control.sh:24` prints
# `FAIL <behavior> -- wanted X, got Y` to STDERR so a passing run stays quiet, and
# `tools/f/fleet_watch_witness.rish:17` interpolated `${ctl.out}` and never `${ctl.err}`. The exit
# code travelled, `pass=15 fail=1` travelled, and the clause naming WHICH behavior did not.
#
# Measured the same hour: EIGHT controls print FAIL to stderr, FIVE are run by a witness, and all
# five of those witnesses stood on this roster interpolating `.out` alone -- `fleet_watch`,
# `fleet_lap_verdict`, `publish_template`, `unshared_citation`, `sealed_digest`. All five carry
# `${ctl.err}` now, and the repair was proven on metal by planting one wrong expected value in
# `sealed_digest_control.sh`: the evidence went from `pass=8 fail=1` alone to that line plus
# `FAIL an unread seal is counted -- wanted PLANTED_WRONG, got yes`.
#
# THE THREE-MARK READING ABOVE COULD NOT SEE IT. That reading requires `say r.out` and
# `exit r.code`, which is a pass-through shim; these are witnesses that assert. One law -- hand on
# your target's reason -- and two shapes, so the instrument grew a second reading rather than the
# tree growing a second instrument. `reason_lost_rostered` is GATED at zero, `reason_lost_unrostered`
# is a ratchet opening at zero, and `stderr_controls` names the population so a zero is visible
# rather than assumed (%463).
#
# TWO REFUSALS IT DELIBERATELY DOES NOT MAKE, each proven in the pen. A control keeping its FAIL
# line on STDOUT is not in the population at all -- its reason already travels through whatever the
# witness interpolates, and counting it would red a witness that is already correct. And a witness
# forwarding some OTHER run's stderr is not credited for the control's: the reading takes the
# variable the control's `run` is bound to and reads only that variable's spellings, because the
# blindest version of this check is *does the file mention `.err` anywhere*.
#
# AND THE READING CAUGHT ITSELF, which is the case worth keeping. The control's pen helper WRITES a
# stderr-reporting control, so its own source carries that line inside single quotes -- and a first
# pattern read 9 where the tree holds 8, counting the instrument as one of its own subjects. The
# population is anchored now: a real call stands after start-of-line or whitespace, a quoted mention
# stands after the quote that opens its string, and a comment is dropped in the same pass. Both
# spellings of a mention are planted, and the quoted plant reproduces the real continuation-line
# shape byte for byte rather than something near it.
#
# Twenty-one new pen readings, six new repositories, every refusal planted and then lifted: 61
# cases over 14 repositories, and the guard runs in 8 seconds measured `20260907.000030` against a
# full lap pass of 983.
#
# THE FOURTH SHAPE, `20260910.203444`: the reason is never said at all. The three readings above
# each watch a witness forward its target's reason badly -- wrong stream, wrong field, one line too
# late. Under them sits the plain case: `let r = run [...]` followed by `assert r.ok else "the
# build failed"`, mentioning neither `.out` nor `.err`, so a refusal hands the reader the witness's
# own sentence and discards the compiler's. REDS %700 is this shape: a rostered guard answering red
# on one cold endurance run and green on a re-run over one unchanged tree, with an evidence page naming
# which LEG refused and nothing about why. All five of its bindings wore it.
#
# THE REPAIR IS QUIET, which is what tells it from the third shape. Rishi interpolates a capture
# into an `assert ... else` message, so the reason travels only when the door closes -- where a
# `say` moved above the assert prints on every green run forever.
#
# A RATCHET ON BOTH SIDES RATHER THAN A GATE, because 953 rostered bindings wore this shape across
# 170 of this roster's 326 guards on the lap it was seated (7,110 tree-wide). A gate at zero would
# red every lap on a tree no lap can repair, which is a gate somebody turns off. The rostered
# ceiling opened at 948 -- five lower, the motivating witness having taken its own move in the same
# commit. Sixteen new pen readings over three new repositories, both ratchets proven from both
# sides on one plant, and the PARTITION against the third shape proven too: a binding that says its
# run late is counted by `late_say` and not here, so one fault never pays twice. 128 cases over 28
# repositories; the guard runs in 60 seconds measured `20260910.203444`.

guard shim_reason
path tools/s/shim_reason_witness.rish
tier lap
seated 20260906.124500
# rye_struct_fields -- the reader four guards needed and none of them had (REDS %500). The four
# Mantra Glow Tend limbs each carry a placard declaring that a structure "carries exactly N
# fields", and each proved it with N `grep -Fq` calls for N field lines. Presence is not a count.
# A fourth field passed all four; a reorder passed all four; and all four refused in the words
# "missing or reordered". Two named movements of the Mantra arc -- the identity gap, wanting a
# fourth field on `Line`, and the diff anchor, wanting one on `Diff` -- were recorded on the
# operator card and in a commit body as waiting on that pin, which had no bolt in it. This rung
# proves the reader that replaced it: a count and an order out of the Rye source, three refusals
# by name, and TWO phases that run the elder predicate verbatim on the same mutated pen and watch
# it wave a fourth field and a reorder through. Those two passes are the finding itself. 19
# behaviors; `tier lap` at 1s, because the four consumers it feeds all run every lap.

guard rye_struct_fields
path tools/r/rye_struct_fields_witness.rish
tier lap
seated 20260906.140000
# rye_enum_variants -- the sibling reader, for the declaration a struct reader cannot see. The
# Glow gate law grammar in tools/fixtures/g/glow_gate_law_agree_scan.sh could point a `law` line
# only at a plain-integer const, so a desk memorizing a count of ENUM VARIANTS stood unlinked and
# that scan's own header named the shape as one of three the grammar would have to learn. This
# rung proves the reader that home needed: a count and an order out of the Rye source across all
# three spellings this tree writes -- one-line bare, one-line tagged `enum(u8)` with `= <n>`
# values, and the many-line form with comments between variants -- and four refusals by name,
# each exiting non-zero so a caller never reads a zero as an answer. The comment clause is
# planted BOTH ways: one clause serves `///` and `//`, and the first draft carried two that
# overlapped, which is how the redundancy was found -- removing one changed no reading at all.
# 26 behaviors; `tier lap` at 1s, beside the consumer it feeds.

guard rye_enum_variants
path tools/r/rye_enum_variants_witness.rish
tier lap
seated 20260915.205116
# The diameter floor, seated 20260906.143000. `comlink/topology.rye` publishes hop counts, and
# `topology_graph_census.sh` now reports the smallest diameter any graph of that many points and
# that maximum degree could hold -- the Moore bound solved for the DIAMETER rather than for the
# node count, which is the direction a designer's constraint actually binds. The reading exists
# because a paper asked the same bound the other way, answered nine billion nodes against our 720,
# and concluded a diameter argument decided nothing.
#
# WHY `cadence` RATHER THAN `lap`. The control runs the census seven times over 682,425 ordered
# pairs -- 151s measured, against 26s for one census pass. Its answer moves only when the census
# or the module moves, both deliberate acts, so a fifth-round reading is honest where a per-lap
# one would spend 151s a lap on eight ships watching a number that did not change.

guard topology_floor
path tools/t/topology_floor_witness.rish
tier cadence
seated 20260906.143000

# reds_citation -- a number used as a link is a promise about where the link goes (REDS row of
# `20260906.143706`). A row is cited two ways at once, as a number a reader recognises and as a path
# a reader clicks, and the two halves move at different moments: the number when the derived spine
# renumbers an unshared row, the path when a row folds onto a shelf. Nothing read them together.
# Four citations went out in one day naming one row and linking another, and each repair fixed the
# half the eye was on -- the number moved and the path stayed, then the path moved and the number
# stayed.
#
# EXISTENCE AND AGREEMENT ARE TWO QUESTIONS, and only one had an instrument. Measured on the tree
# that morning, `readme_reach` named ONE of the four -- the citation whose file was absent -- and
# read green over the other three, which open a real shelf holding somebody else's row. That is a
# misdirection with no expiry: it resolves, so it never reds, and a reader following it lands
# somewhere the sentence never meant. `tracked_link` asks existence the same way; `reds_row_present`
# asks whether the ledger HOLDS a row and never asks who cites it.
#
# TWO FORMS, AND THE THIRTY-TWO THAT TAUGHT THE NARROWING. A window around a link picks up every
# `%N` in the neighbouring prose, and a sentence may lawfully name one row while linking another --
# %220's own shelf cites %218's. A nearest-preceding-%N reading over 1,001 living documents called
# 32 honest sentences wrong, so the scan reads only the two shapes that are promises to a reader:
# the anchor text that IS the number, and the shelf word whose sentence just named a row. One line
# of lookback, never past the link, because this tree hard-wraps its pins and the break that shipped
# fell exactly across that wrap.
#
# PROVEN ON HISTORY AS WELL AS IN A PEN. Run against `b441f97b2`, this tree as it stood when the
# defect was published, the scan reads `numbered_disagree=3` and names all three sites. The pen adds
# 23 cases over 12 real repositories, every refusal planted and then lifted, and it caught a defect
# in the scan's own counting before it shipped -- `grep -c ''` prints 0 and exits 1 on an empty file,
# so the fallback beside it yielded two lines and the emptiness branch was skipped entirely.
#
# `tier lap`: one `git grep` over tracked markdown then awk over the two dozen documents that cite a
# shelf, about a second. Every fold moves a path and every renumbering moves a number, so ordinary
# work is what writes this fault, and a cadence clock would let one ride four laps.

guard reds_citation
path tools/r/reds_citation_witness.rish
tier lap
seated 20260906.143706
# reds_shelf_name -- a fold shelf's own name is a promise about what it holds (REDS row of

# reds_shelf_resolve -- a refold renames a shelf, and the past's citations of the departed name
# must still be answerable (REDS row of `20260916.103855`). A fold shelf is named
# `REDS-<title>-rows-<N>.md`, and a later refold RENAMES it -- absorbing the row into a wider range
# or retitling the same rows. Living citers repoint in that pass; dated testimony keeps every word
# it wrote. Measured at seating: 564 distinct shelf basenames cited, 88 on no path, and 80 of those
# 88 cited ONLY by testimony accrete-never-break forbids rewriting. The resolver recovers 82.
#
# THE GATED READING IS `unrecoverable`, NEVER `absent`, and that is the whole shape of the guard: a
# refold RAISES `absent` every time it runs, so gating it would red on the tree's own tending and
# become a gate somebody turns off. `unrecoverable` a refold leaves exactly where it stood, because
# a refold moves a row between shelves and never off its number. Proven the lap it was seated --
# folding `%770` took `shelves` 489 to 490 and left all three of `absent`, `recovered` and
# `unrecoverable` unmoved.
#
# THE SIX IT CANNOT ANSWER ARE NAMED RATHER THAN LUMPED. Four are `ambiguous`, every one a
# PUBLISHED DOUBLE (`%512`, `%530`, `%675`) that `.claude/rules/derived-spine.md` states no lap may
# repair, so refusing there is the law working. Two are `missing`, both pen plants written by a
# living witness rather than a fixture -- the census reads past `tools/fixtures/` as a proxy for
# *planted*, and this measurement is what showed that proxy incomplete. They are counted rather
# than excluded, because widening an exclusion to make a number smaller is how a census stops
# measuring its subject.
#
# `tier cadence`, chosen by measurement rather than by habit. The witness runs 75s (read
# `20260916` under a concurrent roster pass): one map built across 490 shelves, then 82 lookups,
# then a 32-leg pen that reads the census three more times. Eight ships paying that every lap is a
# real spend, and the fault it watches for CANNOT arrive between laps -- the gated reading is
# invariant under the only ordinary work that touches this population. `--cadence-slice` turns the
# clock now (`%701`), so a cadence seat is a slower clock rather than silence.

guard reds_shelf_resolve
path tools/r/reds_shelf_resolve_witness.rish
tier cadence
seated 20260916.103855
# `20260907.151832`). A folded row spells its number FOUR times: in the shelf's filename, in its H1
# title, in a `**Rows:**` header, and in the row headline. `reds_spine_derive` is the only
# instrument in this tree that reads a row's number at all, and it reads the headline alone -- so a
# renumber sweep reaching three of the four leaves a shelf disagreeing with itself, and the two
# spellings it leaves behind are the filename, which 291 living citations open by path, and the
# title, which is the first line a reader sees.
#
# EXISTENCE IS NOT AGREEMENT, and this is that pair one room further in than `reds_citation`. That
# guard reads a citation's number against the shelf it links, from OUTSIDE the shelf; `readme_reach`
# and `tracked_link` ask whether a path opens, and a shelf whose title names the wrong row opens
# perfectly. Nothing read a shelf against itself. Measured `20260907.151832`: one shelf of 291
# disagreed, and it was this ship's own row from the lap before, renumbered `%568 -> %566 -> %572`
# on the anointed spine while its title kept `%568`.
#
# THE READING IS NARROW ON PURPOSE, and its three welcomes are proven as hard as its refusals. A
# title may name a row as its SUBJECT mid-sentence (*the %364 collision*); a `**Rows:**` header may
# recite a renumber history after its declaration; a filename span may be WIDER than the rows held,
# because a range is not a set and the endpoints folded elsewhere. All three stand in this archive
# today, and a looser reading calls every one of them wrong.
#
# `tier lap` because the derived spine renumbers unshared rows as routine work, so the fault is
# written by ordinary laps rather than by a deliberate act -- and the scan costs four seconds over
# `git ls-files` and 291 small files.
guard reds_shelf_name
path tools/r/reds_shelf_name_witness.rish
tier lap
seated 20260907.151832

# pre_push_spine -- the derived spine's own two gates, asked at the moment that publishes.
# `reds_spine_derive` exits 1 on `rebindings` and on `double_booked`, and both are exactly the
# state that becomes a PUBLISHED DOUBLE the instant a push lands. It runs at the cold open and at
# the hot close, and both come BEFORE the send's final rebase -- the one step that brings a peer's
# row into this tree. Nothing ran the reading between that rebase and the push, and seven published
# doubles stand upstream, six of them arriving in the three days after the law declared the class
# stood "exactly once". `tools/hooks/pre-push` is the placement; this proves it in a pen of real
# repositories with real pushes, and proves the ARMING too, since a hook the tree ships and no
# clone reaches is a claim rather than a wall. `tier lap` -- eight seconds, and the fault it
# catches is written by two ships booking one next-free number inside the same hour.
guard pre_push_spine
path tools/p/pre_push_spine_witness.rish
tier lap
seated 20260910.015328

# pre_push_marker -- the conflict-marker reading, at the moment of publication.
# `conflict_marker` is correct and rostered at `tier lap`, and `tools/hooks/pre-commit` rule nine
# asks the same scan of the staged set. Neither could see the fault: `git rebase --continue` COMMITS
# WITHOUT RUNNING `pre-commit`, proven on metal `20260915` on git 2.54.0 and reproduced by case 12
# of this guard's own pen. The roster runs cold at a lap's open and hot at its close, and both come
# BEFORE the send's final rebase -- which on `20260915` wrote three marker lines into
# `construction/ITINERARY.md`, the card every ship reads whole at every lap, and they stood on the
# anointed remote for two commits (REDS %731). `tools/hooks/pre-push` is the placement, beside the
# spine rule already there. It refuses a marker in the push's own paths and REPORTS one outside
# them, since a gate that reds on what this lap cannot repair is a gate somebody turns off.
# `tier lap` -- about ten seconds: real repositories, real pushes, and one real rebase.
guard pre_push_marker
path tools/p/pre_push_marker_witness.rish
tier lap
seated 20260915.190000
# topology_attained -- what diameter a builder can actually STAND on, at 720 points.
# Its sibling `topology_floor` binds the MOORE floor, a lower bound over all graphs that almost
# nothing attains. This one binds what is REACHABLE: a second floor for abelian Cayley graphs
# derived from the lattice ball and checked against enumeration at every radius, six graphs built
# here and walked from all 720 vertices, and a sweep of 63,903 circulants. `tier cadence` because
# the scan walks 3,106,080 ordered pairs and the control runs eight plants -- 192s measured
# `20260906.152821` -- and its answer moves only when the scan moves, a deliberate act.

guard topology_attained
path tools/t/topology_attained_witness.rish
tier cadence
seated 20260906.152821

# amphora_udp_reuseaddr -- the option one line above the port %485 took back, carried for a reason
# UDP does not have.
#
# WHAT WAS WRONG. `amphora/vessel_fetch_delivery.rye` opened every UDP socket under
# `setsockopt(SO_REUSEADDR)`, the invariant reading *SO_REUSEADDR is what lets a bounded run rebind
# its own port*. That is TCP: a closed TCP socket holds its port in TIME_WAIT and the option binds
# through it. UDP has no TIME_WAIT, so the option bought nothing it was added for -- and what it
# bought instead was a DUPLICATE BIND, two sockets holding one port while the kernel splits the
# datagrams between them with no error either side. The per-tree lock beside it was built to catch
# the collision this option had made silent. REDS `20260906.154105`.
#
# WHY IT GATES SOURCE AND PROBES KERNEL. The source reading (`source_reuseaddr_sites`, zero) is the
# regression guard: it is what keeps a plausible comment from walking the option back in. The
# kernel probe is what makes the repair a measurement -- four binds of one port answering, in
# order, that the stated reason needed nothing, that the collision is nameable without the option,
# that the option is what silenced it, and that a mixed pair still refuses. The probe asks the
# kernel for a FREE port rather than naming one, because a hardcoded probe port is this row's own
# fault one level up, and it never touches Amphora's 38494/38495, so no peer's live exchange is
# disturbed. Where `python3` is absent the probe reports itself absent by name and the source gate
# stands alone -- a guard that cannot run its instrument must not describe its subject (%460).
#
# THE THIRD READING IS A CENSUS AND NEVER GATES. `open_socket` was copied module to module with its
# comment intact, so 19 further modules carry the same call -- 1 comlink, 1 granary, 10 linengrow,
# 7 mantra, every one of them SOCK_DGRAM and not one SOCK_STREAM, counted again on this tree at
# `20260906.200000`. Those files belong to other seats, and a guard that reds Amphora over a module
# in Mantra is a guard the fleet turns off, so the count is printed and the verdict ignores it.
#
# `tier cadence`, AND THE TIER IS THE ONE THING HERE NOBODY HAD MEASURED. This block read
# `tier lap`: 1s, no build until `20260906.200000`, while the row it belongs to was parked. Timed
# on this pier: the scan alone **16.8s**, the control **1m57s** over 32 pens, the witness **2m8s**
# together. All three claims in that sentence were wrong -- it is not 1s, it does build (each pen
# is a tree the scan is run against), and at `tier lap` across eight ships it would spend roughly
# seventeen pier-minutes a fleet lap against a whole-roster average near 7s. So it takes the clock
# `topology_attained` takes for the same reason at 192s. What the cadence costs is honest and
# small: the gate catches a walk-back within five rounds rather than one, on a line no lane is
# currently editing. **The elder claim is this row's own subject one level up** -- a number carried
# for a plausible reason that nobody measured -- which is why it is corrected here with three
# timings rather than quietly restated.
#
# ITS OWN NEXT LAP, named rather than taken: the scan's 16.8s is a whole-tree `find` over every
# `.rye`, and the control pays it again on the legs that read the living tree. `git ls-files` and
# one grep pass would cut it; that is a change to the instrument, and this lap's keystone was
# getting the instrument out of the box at all.
#
# ITS SILENCE, MEASURED `20260910.234500`: this pier's run card held one row for it, stamped
# `20260906.181204` and recording `lap`, and no cadence pass has run since. Asked by hand it answers
# GREEN in 104s. The reading for the whole tier, and why the pair stays on `tier cadence`, sits above
# `guard amphora_asker_reply`.

guard amphora_udp_reuseaddr
path tools/am/amphora_udp_reuseaddr_witness.rish
tier cadence
seated 20260906.154105

# amphora_device_wire -- the vessel room's own instance of REDS %646, rostered under the capability
# that answers it.
#
# WHAT WAS WRONG. `amphora/README.md` named this witness under *Hand witnesses (run by name, on no
# clock)*, saying it *drives a virtio lab this pier has no qemu for, and refuses honestly at exit 1
# rather than pretending*. Every word true, and the consequence is %646's sentence exactly: a hard
# refusal on an absent OPTIONAL dependency makes a witness unrostable, and an unrostered witness
# runs nowhere -- so the honest refusal reached no reader on any bench, including the benches that
# carry the emulator and would have run it green.
#
# WHY IT CAN BE ROSTERED NOW. `capability qemu_riscv` was seated `20260910.171423` for
# `mantra_snapshot_wire`, and its probe's own comment measures the population this row belongs to:
# **26 witnesses in this tree reach a wire lab and 0 of 26 stand on this roster**. This is one of
# the 26, and the first from this lane. `tools/gen/amphora/amphora_device_wire.rish` is the whole
# body -- `comlink/run_vessel_fetch_wire_lab.sh` into `run_wire_lab.sh vessel_fetch` into
# `tools/co/comlink_vessel_fetch_wire_lab.rish`, which spawns `qemu-system-riscv64 -machine virt`
# -- so the capability is the whole of what it needs and nothing beside.
#
# WHAT IT COSTS HERE. Nothing: `command -v qemu-system-riscv64` answers absent on this pier, so the
# pass prints `skipped_capability amphora_device_wire wants=qemu_riscv here=absent` and moves on.
# That line is the point. A guard named is a guard a reader can ask for; a guard off the roster is
# one nobody knows to ask.
#
# `tier lap`, matching its `mantra_snapshot_wire` sibling: a row whose capability is absent costs a
# probe, and on a bench that HAS the emulator the lab is what that bench is for.
#
# The shim carries its reason already (`tools/am/amphora_device_wire.rish` says `r.err` as well as
# `r.out`), which `shim_reason` gates for every shim this roster seats -- so this row arrives under
# that wall already paying it.
guard amphora_device_wire
path tools/am/amphora_device_wire.rish
tier lap
capability qemu_riscv
seated 20260911.083000

# amphora_mark_wreck -- the vessel named the wreck rule for eight weeks and kept it nowhere.
#
# WHAT WAS WRONG (`20260907.015042`). `foundations/20260703-202312_the-marked-value.md` vows that
# an unknown mark is refused whole -- *there is no partial trust of cargo whose kind cannot be
# named* -- and hardcodes exactly two roots, the plain-bytes mark and the manifest mark. Pond, one
# of the two sites that page names, kept the vow: `pond/customs.rye` asserts
# `verdict_for_mark("stranger-mark") == .refuse`. Amphora, the OTHER site -- the crossing vessel
# whose shoulder line is the whole image the page is built on -- carried the mark field and checked
# it nowhere. `parse_manifest_line` validated the mark's SHAPE (nonempty, at or under
# `max_mark_len`) and never its IDENTITY, and every reading site threw the parsed entry away:
# `vessel_core.rye:160` and `:167`, `vessel_seal.rye:115` and `:229` each wrote
# `_ = me.parse_manifest_line(...) catch return error.InvalidVessel`, keeping the refusal and
# discarding the mark, while `vessel_fetch_delivery.rye` -- the far side of a UDP fetch, which is
# *receipt* in the vow's own word -- stepped over the token with `_ = parts.next(); // mark`
# without parsing at all. Probed on metal: `plain-bytes`, `manifest`, `zzz-unknown-stranger`, and
# `executable` were all ACCEPTED. The module's own header line claimed `wreck rule` and its
# selftest printed `refuse whole`.
#
# WHY NO GUARD SAW IT. The eight elder amphora witnesses plant six tampers -- an absent season, a
# tampered `seal_cargo`, a truncated vessel, a forged resin body, a torn chunk, a reordered pair --
# and every one is an INTEGRITY plant. Not one plants a TYPE. The mark was the only field of the
# three-field line with no negative case anywhere in the room.
#
# WHY THE REFUSAL HAD TO BE READ BY REASON. Editing a sealed vessel breaks its Kumara signature, so
# a planted stranger refuses either way and the exit code proves nothing about marks. Restoring the
# elder behavior, the planted vessel still refuses -- as `error: VerifyFailed`, the signature, with
# `parse_manifest_line` nowhere in the trace. So a correctly sealed vessel carrying `executable`
# rode straight through, and only a tampered one ever refused. This witness asserts the FRAME:
# `parse_manifest_line` must be what turns it away, which is the one assertion the elder tree fails.
#
# WHAT IT COST TO FIX: nothing real. Measured before the gate went in -- exactly one mark rides in
# this whole tree, `plain-bytes`, six occurrences -- so the two named roots break no vessel that
# exists. The check sits in the parser rather than at five call sites, because a rule written five
# times is a rule four places can forget.
#
# THE CENSUS IN THAT PARAGRAPH WAS SHORT BY TWO, counted `20260908` rather than recalled. Six
# callers route a whole line through the parser, not five: `take_listed_line` at
# `vessel_core.rye:234` is a third site in that roof and was named in neither this note nor the
# contract's own comment. And a SEVENTH reader never reached the parser at all --
# `amphora/src/main.rye:append_cargo_line`, which walks decrypted seal plain into a catalog slot
# and so cannot route a whole line. It carried its own tokenizer and its own mark rule,
# `eql(mark, "plain-bytes")`, NARROWER than the two roots every sibling welcomes: a vessel
# carrying the `manifest` root would have passed six readers and died here as `CargoUnproven`.
#
# WHY IT WAS UNREACHABLE, which is why it stood. That door writes what it reads -- `pour` builds
# the cargo plain with its own `mark_plain_bytes`, seals it, and `restore` opens the same plain
# back -- so no argument, file, or vessel edit puts a foreign mark into the plain without
# breaking the AEAD seal first. A leg that cannot be reached is proven where it lives: the
# reader borrows `mark_verdict` and bounds the mark field ahead of it, so an over-long mark now
# REFUSES rather than tripping that function's own precondition assert, and the written root is
# held against the contract's at comptime. A `couples:` marker was tried first and refused --
# `amphora_bounds_agree` reads a bound's spelled NUMERIC value and answered `marker_on_derived`
# on a string, which is the right refusal and pointed at the better home: a marker is checked
# when a guard runs, a comptime assert every time the door is built. Three legs, each proven to
# BITE -- the elder narrower rule restored in a source pen, the bound line deleted, and a
# divergent literal planted, whose build refusal is asserted to name the comptime tie rather
# than merely to be non-zero. REDS `20260908.004856`. GREEN at 7.4s; still `tier lap`.
guard amphora_mark_wreck
path tools/am/amphora_mark_wreck_witness.rish
tier lap
seated 20260907.015042

# topology_routing -- what a node HOLDS and COMPUTES to pick the next hop, at 720 points.
# Its sibling `topology_attained` binds the WALK -- how many hops a shape takes. This binds the
# DECISION: the exact shared next-hop table (719 entries, 3 bits each, 270 bytes, the same 270 at
# every node), what a table-free local rule costs instead, and whether more lookahead recovers the
# difference. Coordinate subtraction and permutation sorting are exact and free; the circulant's
# ring-gap rule reaches 609 of 719 and three depths of lookahead move nothing. `tier cadence`
# because the scan walks 517,680 ordered pairs three times over and the control runs ten plants --
# 52s for the scan, measured `20260906.195719` -- and its answer moves only when the scan moves.

guard topology_routing
path tools/t/topology_routing_witness.rish
tier cadence
seated 20260906.195719
# TWO-ROOMS DOORWAY -- seated 20260705, rostered 20260906, and RED for the whole gap between.
#
# `context/TWO_ROOMS.md` asks every forward-facing page to name its register in its own Status
# line, and this rung reads that token. It stood on no roster, so nothing ran it: it was RED at
# 45 pages when a hand finally did. That is REDS %360's family -- a refusal nobody receives --
# with an actual refusal waiting behind it.
#
# It was also blind. Its roster was a shell glob over three rooms; `active-designing/` folded to
# `date/YYYYMMDD/` and a shell `*` stops at `/`, so 607 pages left the subject with nothing to
# say so. A broken path reds; a narrowed glob goes green. It reads `git ls-files` now, whose
# pathspec `*` crosses `/`, and the scan floors its own reach on a folded page so the same
# narrowing refuses rather than reporting a smaller clean tree.
#
# `tier cadence`: 972 pages, one subprocess each, 28s on this pier -- and eight ships read the
# roster twice a lap on four cores, so a lap clock would spend 448 contended seconds a round on a
# reading that moves when a room folds. That is `dated_path`'s reasoning verbatim, one room over.
guard two_rooms_doorway
path tools/t/two_rooms_doorway.rish
tier cadence
seated 20260906.161500
# TWO-ROOMS DOORWAY, AT THE MOMENT THE PAGE IS WRITTEN -- seated 20260907.092522.
#
# The rung above is whole and slow: 978 pages, one subprocess each, 28s, `tier cadence`. So it
# speaks on the fifth round, hours after a page was written, to a hand who has moved on. On
# 20260907 FOUR pages landed in one day whose Status named `proposed` and no room, three of them
# AFTER the canon settled that question (TWO_ROOMS.md, 20260907.015907) and the room's own front
# door taught the token the same morning. The ratchet went 44 to 48 in a day and a hand swept it
# back by reading four sentences already on the pages. The repair rate matched the writing rate,
# which is a lantern rather than a loom.
#
# This is the loom, and it gates a NARROWER class on purpose: only a page the commit ADDS. A new
# page has no accreted testimony to protect and exactly one person who knows its register -- the
# hand writing it, right now -- so the ratchet's RISE is held at zero while its FALL stays with
# the rung above. A page the commit MODIFIES is reported and walks free, because 44 elder pages
# name no room and three are dated testimony; refusing an unrelated commit for touching one would
# be a wall somebody turns off.
#
# `tier lap`: 2s, and it reads one `git diff --cached` plus the doorway roster. It is the cheap
# gate that a lap adding one page must actually run -- COPAL's %550 sentence, answered here.
guard two_rooms_doorway_touch
path tools/t/two_rooms_doorway_touch_witness.rish
tier lap
seated 20260907.092522
# TAME'S 70-LINE LEDGER -- the instrument behind the tidy ratchet could not see its own subject.
#
# `tools/fixtures/t/tame_style_long_fn_one.sh` ended a function at the first line matching `^}$`
# OR `^    }$`. The second alternative fits a struct method, whose body closes at four spaces --
# applied to a free function it matches that function's FIRST `if` block, which closes at four
# spaces too. Its start pattern matched any INDENTED `fn `, so an inline comparator restarted the
# count inside its parent. Both fail OPEN: over 611 authored files it reported 15 functions past
# 70 lines where 321 stand, its longest reading 180 against a true 809.
#
# The witness gates the INSTRUMENT, never the count: the control's twenty planted legs, the two
# elder patterns' absence from the code, and a floor of one on the tree's own reading -- a ledger
# answering zero across 611 files has gone blind, and blindness prints what health prints. The
# count itself stays a ratchet that prints, as TAME's own tidy rules seat this family, since 321
# standing functions belong to six lanes and a ceiling would refuse other ships' ordinary work.
#
# `tier cadence`: 611 files, one awk each, 19s under fleet load -- and the number it reads moves
# when a hand splits a function, which is a deliberate act rather than a per-lap event.
guard tame_style_long_fn
path tools/t/tame_style_long_fn_witness.rish
tier cadence
seated 20260906.173013

# THE RATCHET THAT COUNTS A LINE WHERE IT MEANT A CALL. Two of TAME's advise ratchets ask a lap
# to MIGRATE what they count -- `parseInt(` to `tally/parse_int.rye`, `Ed25519` to
# `tally/kumara.rye` -- and each was one `grep -hF <pattern> | wc -l`, which counts a LINE. A
# line is not a call, and `glow/` is this tree's own language implementation, so its source holds
# the TEXT of the Rye it emits: `append_print(out, &used, "... std.fmt.parseInt(u32, argv[2], 10)
# ...")` is a compiler writing a call, and the witnesses beside it assert `indexOf(rye_argv,
# "parseInt(u32")` on that output. Measured across the twenty rooms: `parseInt(` printed 125 of
# which 79 are emitted text, all in `glow/`; `Ed25519` printed 26, of which 29 counted lines are
# `//` prose and 10 more are `X25519.KeyPair.fromEd25519` conversions. The true remainders are
# 46 and 1 -- and reading that 1 rather than counting it found it is a TRAILING comment, so the
# genuine Ed25519 debt outside the kumara seam is zero.
#
# The predicate is written ONCE, in `tools/fixtures/t/tame_style_app_sites.sh`, called by both
# halves. That costs the parity selftest nothing that existed -- it compares only the zero-assert
# and memcpy numbers -- and `@memcpy(` is deliberately left on its own two independent readings,
# since it measures identically under both rules (0 emitted, 0 comment) and is the one count
# under numeric parity.
#
# The guard gates the PREDICATE, never the counts: sixteen planted control cases in a throwaway
# pen, both parity traps (`\"` and `'"'`) shown, the one documented overcount asserted so it
# cannot move in silence, and both halves confirmed still saying `program position`. `tier lap`:
# under four seconds, and the fault it prevents is written by ordinary work -- a lap widening a
# room or a grep is exactly when a line count quietly becomes a call count again.
guard tame_style_app_sites
path tools/t/tame_style_app_sites_witness.rish
tier lap
seated 20260908.115028

# shared_pen -- a scratch path named by a constant is a path eight ships share.
# FOUR witnesses have reddened a cold endurance run across three laps while each was GREEN run alone --
# signal_trap, tlb_reach, amphora_restore_negative, and tlb_reach a SECOND time -- because a peer's
# `rm -rf /tmp/<a chosen name>` reached their fixture mid-run. Each sweep that repaired one counted
# the class by the spellings it could read: the first read a shell `TMPDIR=` assignment and missed a
# Rishi `let home = "/tmp/name"`; the second read four spellings and missed `${TMPDIR:-/tmp}/<name>`,
# whose text holds no literal `/tmp/` at all -- which is the one tlb_reach was written in, and why
# it came back. This reads all five. `tier lap`: one awk over 3,102 sources, under two seconds, and
# ordinary work is exactly what writes the fault -- a new witness reaches for a pen name on the lap
# it is born, so a cadence clock would let four laps of them through. The scan alone is under
# two seconds; the witness runs 14s measured 20260907.000549, since its control builds real
# git repositories for all 27 behaviors.
guard shared_pen
path tools/s/shared_pen_witness.rish
tier lap
seated 20260906.225150
# build_lock_ignore -- a lock this tree takes is a lock `.gitignore` names.
#
# WHAT WAS MEASURED (`20260917`). `lock_acquire` holds a lock as the DIRECTORY and writes the
# holder's pid inside it, so a lock is an untracked directory standing in the tree while its holder
# runs. `git stash push -u` sweeps untracked files and passes over ignored ones, and
# `fleet_round_open.sh` stashes that way at every lap open -- so a lock `.gitignore` does not name
# is one a round-open RELEASES mid-hold, after which the next acquirer enters the room the lock
# exists to serialize. The tree took four in-tree locks and named three; the build lock born at
# REDS %734, taken in whatever room a build writes, was named by nothing. Three of its pids stood in
# this ship's own dead-letter box on the seating lap, swept there by a round-open.
# `tier lap`: the scan is one pass over the tracked runner roster, well under a second, and
# ordinary work is what writes the fault -- a new lock is taken on the lap its tool is born, so a
# cadence clock would let laps of them through. The witness runs longer than the scan because its
# control builds real git repositories and runs real stashes for all 29 behaviors.
guard build_lock_ignore
path tools/b/build_lock_ignore_witness.rish
tier lap
seated 20260917.065842
# kyri_resins_catalog -- the cellar's catalog names every resin it holds, and now something reads it.
#
# WHAT WAS MEASURED (`20260907.000030`). `kyri-resins/` is the resin cellar and
# `kyri-resins/manifest.kyri` is its catalog, one `entry <basename> <note>` line per resin. It named
# THREE of the room's SEVENTEEN. Its own `note` line states the habit that keeps it -- *commit with
# the resin* -- and that habit was kept for the first three files of the first morning, `20260712`,
# and dropped. Nothing in the tree read the catalog: three scans name `kyri-resins/` only to skip it
# as testimony (`counsel_census_scan.sh`, `waymark_rung_drift_scan.sh`, and `template-manifest.kyri`'s
# `personal` verdict), and no witness, runner, or roster row touched it. A catalog covering a fifth
# of its room reads exactly like a whole one -- `%514`'s lesson standing in a room where the silence
# had lasted eighty-seven days.
#
# THE LAW IT KEEPS is `foundations/20260703-202312_the-marked-value.md`, whose Amber clause reads
# *every resin records the marks of what it seals, so a reader in another decade knows what the
# cellar held without opening it.* The council rota's Earth-Dual seat is that document, which is how
# this was found: the row that breathes in reads the concrete fact at the door, and the fact at this
# door was a catalog four fifths short.
#
# A LANTERN RATHER THAN A LOOM, measured before the instrument was built. Every tracked file in a
# living room whose name carries `manifest`, `catalog`, or `roster` was crossed with its readers:
# the mycelium corpora each have a witness and a Rye reader, `forge_cellar_manifest.kyri` is read by
# `pond/apps/drawn_terminal.rye`, and the seat rosters are read by twenty, nine, and eight sites.
# `kyri-resins/manifest.kyri` was the ONE enumeration of a room with no reader at all. So this is a
# scan for one cellar rather than a general instrument, and the near-miss is named:
# `cellar/ring1_manifest_shape.kyri` is cited only by `cellar/README.md`, and it is a SHAPE rather
# than an enumeration, in a lane this seat does not hold.
#
# THREE GATES AND ONE REPORTED READING, and they are separate because they fail in separate
# directions: `uncatalogued` is a resin nobody named, `orphan_entries` is a line promising a file
# the room lacks, and `duplicate_entries` is a catalog disagreeing with itself.
# `noteless_entries` is REPORTED -- a bare name is a weak line and still a true one, and a gate that
# reds on an honest half-measure is one somebody turns off. An absent room or catalog answers
# `misread` and exits 2, never zero, since zero is the reading a healthy cellar gives.
#
# The fourteen notes were written from each resin's OWN first header line rather than from a
# summary. The one `.md` file in a room whose `law` line says Kyri only is named in the catalog
# rather than swept, because a catalog states its exception instead of hiding it.
#
# SEALED `20260910.014500`, and the elder reading above had two of a manifest line's three fields.
# `foundations/20260703-202312_the-marked-value.md` states all three -- *its type-mark, its digest
# at the tiers the resins law fixes, and its name* -- and the vow beneath them, *the digest is
# checked twice*. The catalog carried the name and the note. A resin edited in place would have
# passed `uncatalogued`, `orphan_entries` and `duplicate_entries` in silence, which is the room's
# own lesson standing one field over: a catalog covering part of its subject reads exactly like a
# whole one. The strand is the grain's own -- **the content address is the wall**.
#
# Each resin now carries `seal <basename> <sha3-256>`, computed by `tools/fixtures/s/sha3.sh` over
# `crypto/sha3_digest.rye`, authored Keccak, so a reader in another decade checks one with the tree
# and no external binary. FOUR MORE GATES, separate because they fail separately: `unsealed` a
# resin no seal addresses, `seal_mismatch` bytes that moved under their digest, `orphan_seals` a
# seal naming a departed file, `duplicate_seals` a catalog stating one fact twice. `unsealed` GATES
# where the sibling `noteless_entries` only reports, and the parting is deliberate: a bare name is
# a weak claim, while an absent seal is no claim at all.
#
# WHAT THE SEVENTEEN DIGESTS ARE, measured rather than assumed. Every resin in the room has exactly
# one commit in the tree's whole history, and its blob there is byte-identical to its blob at HEAD,
# checked one file at a time. So each seal is the seal of the bytes that LANDED.
#
# THE REFUSAL WAS SHOWN ON THE REAL ROOM FIRST: run against the unsealed cellar the gate answered
# `unsealed=17`, `verdict=drifted`, naming all seventeen, and the seal lines lifted it.
#
# AND IT MOVED A SIBLING GUARD, which is the part worth reading. `sealed_digest` asks whether a
# recorded digest is ever compared against its bytes, and its proxy was *does a file under `tools/`
# SPELL this digest*. Seventeen digests recomputed every lap by a rostered witness spell nowhere,
# so that census read `unread=17` and its own gate would have reddened -- a WALL READ AS A SILENCE,
# the dangerous direction for a proxy to fail in. It reads a second mechanism now: a document that
# declares a `witness ` path under `tools/` which EXISTS and is ROSTERED here is read generically,
# counted apart as `read_generically` so no number quietly absorbs the other. Three conditions,
# none satisfiable by prose, each planted and lifted separately in that guard's own control
# (9 -> 16 behaviors), so no single one can pass for the whole test.
#
# `tier lap`, 4s measured `20260907.000030` against a full lap pass of 983; 20s measured
# `20260910.014500`, the seals and the control's own cellars being what it now buys. The control
# proves 65 behaviors on throwaway cellars (34 before the seal): every refusal shown from the
# failing side and then LIFTED, the
# reported reading proven not to gate, a malformed seal proven to fall into `unsealed` rather than
# to pass as a claim, the detail listing's bound proven by planting 75 resins
# against a ceiling of 64, and the pen proven innocent against a scan patched to always answer ok --
# with that patch's own landing proven by `cmp -s` and by refusing an EMPTY copy, which the control
# caught in itself (an unreadable source leaves a copy that differs from the original and reads as a
# landed plant).

guard kyri_resins_catalog
path tools/k/kyri_resins_catalog_witness.rish
tier lap
seated 20260907.000030

# kyri_resins_landed -- a resin's bytes are the bytes that landed, read out of git rather than out
# of the working tree. `kyri-resins/manifest.kyri` states the room's law in its own prose: *every
# resin in this room has exactly one commit in the tree's whole history, and its blob at that
# commit is byte-identical to its blob at HEAD -- checked one file at a time, all seventeen equal.*
# A hand took that reading on `20260910` and nothing held it afterwards, which is Gauge's fifth
# question about a figure -- what holds it still -- answered `free`.
#
# WHAT THE SEAL BESIDE IT CANNOT SEE. `kyri_resins_catalog` recomputes each resin's SHA3-256 and
# compares it to the `seal` line, so a resin edited in place reds. Both halves of that comparison
# live in the WORKING TREE, so an edit landing together with an updated seal is self-consistent and
# every gate in this room stays green. Git's history is the one copy an edit cannot reach, and this
# guard is the reading that asks it. Two walls over one room, parting on which copy they trust.
#
# WHAT IT GATES at zero: `moved`, a resin whose bytes differ from its adding commit's blob. Three
# readings REPORT instead, each for its own reason -- `uncommitted`, a resin written and not yet
# committed, which is the ordinary state of the lap that adds one; `commits_multi`, a second commit
# touching the path, which changes no byte on its own and is exactly what a lawful room fold does;
# and `readded`, a delete and restore, which the bytes reading already answers for. The living
# catalog is excluded by name, since it gains a line whenever a resin lands and its own bytes move
# by design -- measured `20260916`, three commits and moved bytes against one and unmoved for all
# seventeen resins.
#
# `tier lap`, 6s measured `20260916.234020` on a pier already running a cold roster pass, of which
# the scan over the real room is 1s. It reads the whole history TWICE rather than twice per file --
# one `git log --diff-filter=A` and one plain `git log`, both `--no-renames` so a fold lands as an
# add at the new path -- then batches the object readings through one `git cat-file --batch-check`
# and one `git hash-object --stdin-paths` and joins the five files in a single awk pass. Asked once
# per resin instead, the same reading cost 4.5s over seventeen files and would have priced the
# guard off the lap clock. The control proves 42 behaviors on REAL git repositories in a throwaway
# pen, because a plant built without history would prove the scan can count files and nothing about
# the claim it holds: every refusal shown from the failing side and then LIFTED, the detail
# listing's bound proven by planting 70 resins against a ceiling of 64, and three mutations asserted
# to bite -- reading the newest adding commit rather than the earliest, which lets an unfaithful
# re-add read `same`; dropping the catalog exclusion, which reds a healthy cellar; and reporting the
# move while exiting clean, which is how a guard fails most quietly.

guard kyri_resins_landed
path tools/k/kyri_resins_landed_witness.rish
tier lap
seated 20260916.234020
# topology_churn -- what ONE DEPARTURE costs a table everybody shares, at 720 points.
# Its sibling `topology_routing` binds the DECISION on a whole graph -- 719 entries, 3 bits each,
# 270 bytes, the same 270 at every node. That claim rests on the graph being the whole GROUP, which
# a live network breaks the first afternoon it runs. This binds the same state on a punctured graph:
# the graph's own damage (84 pairs of 516,242 on the circulant, nothing disconnected, no diameter
# moved), the stale table's damage (3,811 drops -- 45x the graph's, and predicted to within six
# pairs by (mean hops - 1)/(n - 1)), how many of the 719 entries change (925, of which a DERIVED
# n-1-degree = 713 are a dead first hop held by the hole's six neighbours), and the curve under two
# named removal patterns, where a SCATTERED loss of 64 points hurts more than a clustered one.
# `tier cadence` because the scan walks 516,242 pairs three ways on four shapes plus 56 curve
# configurations -- 135s measured `20260907.000309` -- and the control runs ten plants across four
# leg words; its answer moves only when the scan moves.

guard topology_churn
path tools/t/topology_churn_witness.rish
tier cadence
seated 20260907.000309

# topology_growth -- what an address space costs WHILE IT IS STILL FILLING, at the same 720 points.
# Its two siblings price a FULL space: `topology_routing` binds the decision on a whole graph, and
# `topology_churn` binds it on a graph that has lost a point. Both rest on a table computed for the
# whole group, and a growing network holds that table while most of the group is still dark. This
# binds the filling regime: five allocation policies at six occupancies over four shapes, with the
# stale table routed over every live ordered pair two ways from 24 sampled destinations.
#
# The load-bearing pair is one counter over two numberings. A lowest-free allocator leaves the star
# graph in 36 components of ONE POINT EACH at 5% occupancy, all 840 ordered pairs unreachable; the
# same counter over a breadth-first numbering leaves it whole, with 85% of pairs arriving. The
# mechanism is a theorem the scan checks by counting edges rather than trusting: the 120 lexicographic
# ranks with a1 = 1 are an independent set, since every star generator swaps position 1 with some j.
# The three coordinate shapes read prefix_edgeless_max=1 and that mirror is load-bearing -- a leg
# that found the star failing and never found a shape passing could not be told from a broken one.
#
# WHY IT IS `tier cadence`. The full pass is 21s measured `20260907.045627` and its answer moves
# only when the scan moves, which is a deliberate act; a per-lap row would spend those seconds on
# eight ships to watch a number that did not change. The control runs eleven plants across four leg
# words, nine breaking a named reading and two passing on purpose under a leg that cannot read them.

guard topology_growth
path tools/t/topology_growth_witness.rish
tier cadence
seated 20260907.045627


# THE PLANT LAW -- a control's break is a claim about a file, and nothing was checking it.
#
# A control breaks a module on purpose by naming one literal line of the real source in a `sed`.
# When that line moves, the sed matches nothing, the pen is copied byte for byte, and the phase
# builds and runs the UNMUTATED module -- exit 0, which is what a law that holds reads like
# (REDS %519). `tools/fixtures/p/plant.sh` answers that once, and this rung proves it can red:
# twenty-eight behaviors, each function shown from both sides, and one leg that strips the byte
# comparison out of a copy and asserts the copy calls a no-op plant LANDED.
#
# Adoption is a FLOOR that only rises, which is the mirror of this tree's usual ratchet, because
# the population counted is the good one. Seated at 4; `sh tools/fixtures/p/plant_adoption_scan.sh
# --list` names the rest, and each falls on touch.
#
# `tier lap`: the control writes pen text and runs no compiler, so the whole rung costs under a
# second -- and what it guards is every other control's honesty.

guard plant
path tools/p/plant_witness.rish
tier lap
seated 20260907.004838
# seat_prompt_figure -- a charter says what a ship is FOR, and a count in it can carry no date.
#
# WHAT WAS MEASURED (`20260907.011258`). A seat prompt is the first thing a ship reads on every lap
# it will ever run -- `tools/f/fleet-loop.sh` prepends the baton to it and hands the pair to the
# agent. Eight live seats, eight prompts, and TWO carried tree measurements: copal's `10 Rye modules
# and 3,861 lines` with `16 witnesses`, and patchouli's `32 Rye modules and 9,175 lines` with `four
# rostered witnesses`. All six had parted from the tree.
#
# THE TWO WAYS A FIGURE GOES WRONG, and this pair showed both. Copal's witness figures were EXACT at
# their seating -- 16 tracked paths named both `amphora` and `witness` on `20260905.164639`, and not
# one stood on this roster -- and went stale on ordinary work, reading 17 with 9 rostered by
# `20260907`. The line figure was wrong the day it was written: `3,861` against the **3,582** the
# room's ten `.rye` paths read at that same commit, and against **4,058** today. A figure nothing
# reads cannot be corrected, only replaced.
#
# THE LAW IS ALREADY SEATED ONE ROOM OVER. This file's own directive on `construction/ITINERARY.md`
# reads *Counts come from the scan, never here*, and Gauge asks every figure to carry unit, date,
# and source. A charter carries none of the three: nothing runs it, and no reader can tell a figure
# written this morning from one written in June. This carries that law to the room beside it.
#
# A RATE IS A LAW; A TOTAL IS A MEASUREMENT. `two asserts a function` is TAME's rule and can go
# stale only on a word, so it is counted under `rates` and excluded -- and PRINTED, because an
# exclusion nobody can see is an exclusion nobody can check. `10 Rye modules` claims what the tree
# holds this morning. That one distinction is the whole discriminator.
#
# THE READING IS A FLOOR, DELIBERATELY. Only nouns that can name a code census are read -- module,
# line, witness, guard, assert, function, scan, control. `files`, `rows`, and `pages` are left out
# because a charter says `read its top three rows` as an INSTRUCTION, and a guard that reds on an
# instruction is a guard somebody turns off. So patchouli's `120 files outside it reaching in` is a
# measurement this scan cannot see, named here rather than hidden.
#
# `figures` is a RATCHET opening at **3** -- what stands after copal repaired its own charter, so it
# lands green on the tree it publishes to rather than the tree it was built in. `missing` is GATED
# at zero for a reason of its own: a live seat whose prompt file has gone shrinks the population, and
# a ratchet over a shrinking population reads as progress while nothing improved.
#
# `tier lap`, 2s measured `20260907.011258`; the scan alone is under a second over eight small files.
# The control proves 30 behaviors on planted fleets -- every refusal shown from the failing side and
# then LIFTED, a stamp and a row number and a port proven NOT to count, a possessive and an
# instruction quantity proven not to count, the parked seats proven unread, and the pen proven
# innocent against a scan patched to always answer zero. The ceiling was also proven both ways on the
# LIVE tree: one planted figure in copal's own prompt read `figures=4 verdict=over_ceiling` and
# exit 1, and the restore was checked byte-identical with `cmp` before the green was believed.

guard seat_prompt_figure
path tools/s/seat_prompt_figure_witness.rish
tier lap
seated 20260907.011258


# grain_strand_count -- the grain's strand count, derived from the page that holds the strands.
# `foundations/20260826-024942_the-grain-and-the-crossing.md` seated ten strands `20260702` and
# three more `20260802` under *Strands Seated at the Fusion*, "beside the elders" in its own words.
# It has held thirteen since; ten living sentences across six documents still said ten thirty-six
# days later -- the foundations front door, the day-one compass rose, the compass brief in four
# places, the aether threshold the rota opens with, the singularity history, and a manual tutorial.
# Every one of them carried a total rather than deriving one, which `.claude/rules/stamp-and-name.md`
# rule 3 already forbids: count, never number. Nothing read living prose for a carried total.
#
# The scan derives the count off the page's own structure and holds every disagreeing living
# sentence at zero; the control grows the pen's page by one strand and watches a pointer that was
# right a moment ago go stale, which is what proves the count is read rather than spelled.
#
# `tier lap`: one awk pass over 849 living documents, under two seconds.

guard grain_strand_count
path tools/g/grain_strand_count_witness.rish
tier lap
seated 20260907.062544
# scope_rank, seated 20260907.045528. Nothing but the runner read
# tools/fixtures/s/standing_equipment_scope_map.sh until this rung, so three faults in that file
# had no reader at all, and each fails in the safe-LOOKING direction: an orphan row (a typo buys a
# full run and reads like coverage), a second row under one name (the runner's lookup stops at the
# first), and a row naming LESS than its guard gates (the one direction that SKIPS real work -- the
# map's header records thirteen repaired by a hand on 20260906, and this rung found a fourteenth on
# its first run). All three at zero, each planted and lifted in a real git pen. Beside them it
# prices what each row saves -- cost from the run card, touch rate from git log -- which is the
# ranking external-research/20260907-020817 named and left unbuilt. `tier lap`: 11s at the
# witness's own `--window 40`, and the gates read no history at all.

guard scope_rank
path tools/s/standing_equipment_scope_rank_witness.rish
tier lap
seated 20260907.045528

# topology_stretch -- whether the diameter a shape PUBLISHES is one a table-free router can reach.
# Its sibling `topology_routing` prices the shared next-hop TABLE at 720 points on four shapes; this
# binds the RULE, on seven shapes across two sizes, and the pair divides the subject rather than
# repeating it. Two of the seven route worse than they walk: the circulant that walks 9 is routed at
# 12 by the better of two rules reading only the two addresses, so three of the five hops it seemed
# to buy are bought by a 719-entry table; and pancake, which beats star on mean at one group and one
# degree, finds a shortest path for 296 of 719 where star finds one for all 719. Bubble-sort is the
# counterweight -- table-free, exactly optimal, and still worse than the torus at 15.
# It also reads its OWN source, refusing when a routing function mentions the breadth-first distance
# arrays -- the one fault no hop count can report, since a rule that consults the table prints the
# table-free answer. `tier lap` because vertex-transitivity is CHECKED rather than paid for: one walk
# stands for 720 once six sources spread by a stride coprime to 720 agree, so scan and control
# together are 29s measured `20260907.095458`.
# Named `topology_stretch` rather than the draft's `topology_routed`, which stood three letters from
# the seated `topology_routing` in a roster of 220 -- a grep that cannot separate two guards is a
# collision whatever the filesystem thinks. It is named for the column it alone reports.

guard topology_stretch
path tools/t/topology_stretch_witness.rish
tier lap
seated 20260907.095458

# topology_relaxed -- what the two families that RELAX the star graph's factorial keep, and what
# they give back. Its sibling `topology_stretch` found the one shape that reaches a low diameter and
# routes optimally with no table, and named that shape's wall in the same breath: a star graph has
# n! vertices, so between 720 and 5,040 points the family offers no size at all while a supervision
# tree takes its member count from its membership. This walks the two doors that paper left open.
# Thirteen (n,k)-star graphs from 120 to 6,720 vertices and four arrangement graphs, each built,
# walked breadth-first, and routed from EVERY vertex by named rules. Eight relaxed sizes stand in
# the window the star family leaves empty, two of them at degree 6, and S(7,4) walks 7 on degree 6
# at 840 points where the best three-ring torus of that size walks 14 and every degree-6 commutative
# shape walks 9 or more. The finding is the RULE: the star's rule read literally is exact at k = n-1
# and short at all eleven sizes below it, a one-step correction reaches the true diameter and leaves
# a residue at two sizes, and a rule choosing by where a symbol's CHAIN ends -- in junk, which is a
# free eviction, or at symbol 1, which wastes a move -- routes every vertex of every size by a
# shortest path. Its two anchors are the sibling's own readings at 720 and 5,040, so a builder that
# had drifted would stop reproducing them rather than print confident numbers about the wrong graph.
# It reads its OWN source too, refusing when a routing function mentions the breadth-first distance
# array -- the one fault no hop count can report. `tier lap`: scan 10s, control 30s, witness 43s
# measured `20260907.111058`, because vertex-transitivity is CHECKED from five spread sources rather
# than paid for, and the control reads its plants at one small size through the scan's `SCAN_SIZES`
# knob rather than at all thirteen.

guard topology_relaxed
path tools/t/topology_relaxed_witness.rish
tier lap
seated 20260907.111058

# topology_occupied -- the free-routing claim meets a real membership. Its sibling
# `topology_relaxed` found a table-free rule that routes every vertex of seventeen graphs by a
# shortest path, and closed by naming its own second falsifier unfired: every reading assumed all
# n!/(n-k)! vertices occupied, and a membership never lands on a falling factorial. This fires it.
# Two (n,k)-star graphs are holed in four named geometries across sixteen configurations, and every
# live pair is routed to three sampled targets by three rules, with stretch measured against a walk
# through LIVE members only. Three readings carry the finding. The published rule, which cannot see
# liveness, loses 390 of 2,391 packets at 95 percent occupancy -- one in six. Filtering by neighbour
# liveness stops every hole loss and starts cycling instead, so what the rule lacks is MEMORY rather
# than a table. And a packet carrying the vertices it has already stood on, retreating one hop when
# every ranked neighbour is dead or visited, delivers every reachable pair of every configuration at
# a maximum packet state of 25 entries on an 840-vertex shape -- bounded by the path, not by the
# membership. The geometry reading is bound from BOTH ends because only one is free: a far-end
# symbol class, 120 of 840, costs zero stretch and does not move the diameter, while a DOOR class of
# the same size splits the shape into four components. That partition read 837 of 837 delivered, so
# the count of pairs it made unreachable stands on the same line -- excluding them silently is how a
# partition reads as perfection. Its anchor is the sibling's own two readings, since this file
# carries its own copy of the builder and a drifted copy would measure the wrong graph. It reads its
# OWN source too, refusing when a routing function mentions the breadth-first distance array,
# INCLUDING on the declaration line -- the control planted it there and the first draft passed.
# `tier cadence`: scan 57s, control 35s, witness 95s measured 20260907.145954 and 125s re-measured
# at the landing 20260907.153000 on the same pier -- report the range rather than the luckier run --
# because every live
# pair of every configuration is routed exhaustively rather than sampled, which is heavy for a lap
# and ordinary for a cadence.

guard topology_occupied
path tools/t/topology_occupied_witness.rish
tier cadence
seated 20260907.145954

# topology_partial -- a membership never lands on a whole class either. Its sibling
# `topology_occupied` found that a whole far-end symbol class of S(7,4) -- 120 of 840 vertices --
# costs nothing measurable while the same 120 scattered cost a sixth of the traffic, and closed by
# naming its own falsifier unfired: a membership landing between whole classes, 800 on 840, leaves
# 40 holes with no class to hide them in. This fires it, and refutes the tidy hypothesis that rode
# on it. The classes NEST -- 840 splits 120, 20, 4, 1, derived by the falling factorial and counted
# by grouping the vertices that share each suffix -- yet the freedom does NOT descend the ladder:
# 42 whole level-2 classes, every one 20 vertices and every one leaving the shape in one piece,
# cost between 3 and 491 lost packets of 9,828. Which whole class is the choice that pays. Two
# findings survive. The leftover addresses are nearly free: at 800 live on 840 an arrangement taken
# in far-end-major order loses 429 of 9,588 against 1,156 scattered, and two holes more than a
# whole class can hold cost 457 rather than 429 -- a rounding error rather than a cliff. And the
# SAME contiguous run partitions or does not depending which end of the address is significant:
# 20 holes of S(6,3) taken door-major leave three components and 600 unreachable pairs, where the
# same 20 taken far-end-major leave one component and an unmoved diameter. The naive heuristic --
# keep the holes inside one class, taken door-major -- reads WORST of the five arrangements, behind
# scattering, so the finding is bound from that end too. One method correction is bound with them:
# targets are sampled from the whole address space and held FIXED across arrangements, because
# scored the sibling's way at three live-sampled targets `class_partial` ranked best of five and at
# fifteen it ranked worst by a factor of fourteen. It reads its OWN source, refusing when a routing
# function mentions the breadth-first distance array, and closes a function body at a column-zero
# comment -- which it learned by refusing itself on its first run over a line of prose describing
# that array while touching none of it.
# `tier cadence`: scan 314s, control 102s, witness 416s measured 20260907.173400 on this pier,
# because every live pair of 117 configurations is routed exhaustively rather than sampled and 42
# whole classes are each emptied in turn, which is heavy for a lap and ordinary for a cadence.

guard topology_partial
path tools/t/topology_partial_witness.rish
tier cadence
seated 20260907.173400

# STORE SHAPE -- the tree keeps two data shapes in one notation, and a store fitted to one is wrong
# for the other. The census walks every tracked `.kyri` and `.kyri` session-log record plus four
# named registries, reading field names at line start, and prints how many records carry a field
# more than once. Measured `20260907`: journal 4,337 records, 91% multi-valued; registries 413 rows,
# zero. The witness binds the SEPARATION rather than any count, since every count grows daily and a
# guard reding on ordinary writing is a guard somebody turns off -- a floor at 50% where "mostly"
# stops meaning mostly, and a zero on the registry side because a row gaining a second copy of one
# field is a table becoming a document. Paper:
# `external-research/date/20260907/20260907-191657_two-shapes-one-notation.md`.
# `tier cadence`: the census walks 4,337 files with two passes each, 153s measured `20260907.191257`
# on this pier under fleet load -- heavy for a lap and ordinary for a cadence.

guard store_shape
path tools/s/store_shape_witness.rish
tier cadence
seated 20260907.191657

# JOURNAL QUERY -- the journal is asked for records, never for fields. The store-shape census above
# measured the DATA and recommended an index; it named its own second falsifier in plain words --
# count the queries the tree's tools actually ask, and if nearly none asks a field predicate the
# plan shrinks to a sorted room. This census is that count. It classes every non-comment line in a
# tracked tool source that names `session-logs/` into five non-query classes (exclude, construct,
# message, roster, and `rule` for a line reading the LAW rather than the room) and six query
# classes (field, recency, by_path, index, enumerate, exists). Measured `20260907`: 234 sites, 168
# of them naming the room without reading a record, 52 real queries, and `class_field=0`. The
# file-level upper bound `field_capable_files` reads 3, of which one queries the journal and two
# query the equipment registry. The witness binds `verdict=sorted_room`, which is a SEPARATION and
# not a count: field-capable sources under a tenth of the querying sites. A red here says the
# workload changed and the index recommendation went live -- a tripwire on a research claim, never
# a wall against a commit. Paper:
# `external-research/date/20260907/20260907-201914_the-workload-and-the-index.md`.
# `tier cadence`: the census greps 3,359 tool sources line by line, 40s measured `20260907.204600`,
# and its control runs the census 26 more times over a pen.

guard journal_query
path tools/j/journal_query_witness.rish
tier cadence
seated 20260907.203000

# log_file_claim -- tools/hooks/commit-msg has refused a commit BODY citing an absent path since
# REDS %202, on the reasoning that a commit message is permanent testimony. The session log rides in
# that same commit, closes with `file <path> <why>` lines making the same claim, is exactly as
# permanent, and nothing read it. Measured 20260907: 16,924 path-shaped `file` fields across 4,352
# logs, and 228 name a basename that exists nowhere -- 32 written that day. Twelve share one shape:
# a lap folds its live front to a shelf and writes the path from its OWN log's stamp rather than the
# shelf's, and `git log --all` finds not one of the twelve ever existed. Gates `staged_unwritten` at
# zero -- this commit's own logs, read against tree and index together, so a shelf staged beside the
# log naming it passes free and a peer's record can never refuse this lap. Reports `unwritten_total`
# and `unwritten_today`, never gated: that population grows ~26 a day across eight ships and is
# testimony no lap may repair, so a ceiling over it would fire at a stranger's desk. `lap` rather
# than `cadence`: 0.4s measured 20260907, down from 55s read log by log, and the claim it refuses
# can be written again in any lap that closes a session log.

guard log_file_claim
path tools/l/log_file_claim_witness.rish
tier lap
seated 20260907.221800

# say_compose_bound -- a guard must not fail for having something to say. Rishi composes an
# interpolated string into `StrBuf`, a fixed 4,096-byte buffer declared in `rishi/src/main.rye`, so
# `say "reading -- ${scan.out}"` promises that a scan's output will stay under that width forever
# while the output grows with the tree it reads. On 20260907.232343 reds_spine_derive passed every
# assert it makes and then died reporting, `rishi: line 46: StringTooLong`. The same fault has been
# met four times by four hands -- reds_spine_derive_witness.rish, readme_metrics_splice.sh, and
# tame_style_long_fn twice -- each repaired where it landed, each lesson written into a different
# file's comments, so one class read as four accidents. This is that lantern's loom.
#
# Two shapes counted apart, since they fail at different moments: `eager` composes on EVERY run and
# would red a GREEN tree the day its scan grows, and `deferred` composes only in an assert's `else`,
# which is to say exactly when an explanation is owed. `safe` reports the population already spelling
# the repair, a bare `say <name>.out`, which never opens a StrBuf at all. Ten sites of each kind are
# named on every pass with `list` printing them all, so the repair is one line away (%592). The
# width is READ off Rishi's own declaration rather than carried as a copy.
#
# THE CEILINGS ARE SHARES, IN PER MILLE, and that was learned inside the round that wrote them. The
# first draft held raw ceilings at exactly what stood -- 502 and 1,741 -- and the rebase closing that
# same round brought two peers' new guards in and refused. Eight ships write here and every one lands
# witnesses; a ceiling on the count reds on the most ordinary act on this pier. A share asks about
# the habit instead, so a tree that doubles without changing how it writes reads where it stood.
#
# `lap` rather than `cadence`: one awk pass over 2,415 tracked scripts costs 5.7s measured
# 20260908, and the shape it counts can be written again in any lap that adds a witness.
#
# IT REDDENED THIS PIER ON 20260910 AT deferred=538 AGAINST 537, AND THE ROOT WAS THE READING.
# The classifier read from the start of a line, so a one-line `if COND then ACTION` was invisible
# whatever the action did: 21 hazardous guarded tails went uncounted, and 155 guarded bare says --
# sites already spelling the repair -- counted nowhere at all. That deflated `safe`, left the
# shared denominator 258 low, and carried the deferred share 26 per mille above where the tree
# actually stood. Run the widened classifier against the pre-sweep tree in a worktree pen and it
# answers 512, `verdict=ok`. The classifier reads past a `then` now, and reads the CONDITION
# apart from the action, because a condition interpolating a capture composes on every run
# whatever follows -- that case reads zero on this tree and is planted rather than trusted.
#
# The widening also unbraids the two shares from each other. Repairing a deferred site the
# guarded way used to remove it from the population, shrinking the denominator and raising the
# EAGER share -- so a lane doing exactly the right thing pushed a peer's reading toward its
# ceiling, 165 repairs from reddening it. A guarded repair lands in `safe` now, and only the
# numerator moves. `tools/am/` then paid its whole share: 93 deferred sites and one eager to
# zero, sixteen witnesses re-run GREEN, the new refusal path proven by breaking a build in a pen.
# Ceilings fell 155 -> 144 and 537 -> 489, keeping the two and three per mille of slack the first
# draft chose. Control 29 -> 41 legs, each of the two new walls removed in turn and its own legs
# watched to fall.

guard say_compose_bound
path tools/s/say_compose_bound_witness.rish
tier lap
seated 20260908.014200

# `lap` rather than `cadence`: the whole pass costs 3.1s measured 20260908 -- one awk read of 108
# named law pages, one derivation from `rishi/src/main.rye`, and five one-line probes through the
# interpreter. The surface it reads is law prose, which any lap may edit, so the clock is the lap.

guard rishi_run_record
path tools/r/rishi_run_record_witness.rish
tier lap
seated 20260908.101500

# glow_literal_law -- one language, one answer for a leading zero. Glow reads a decimal literal in
# fourteen places and on 20260908 those fourteen gave two answers: glow/rune_shop_gate.rye refuses
# `007` and calls the refusal "the house parse law" in its own comment, and the other thirteen read
# it as seven. Proven on metal through both public entries the same day. The gate is the MINORITY,
# min(strict, permissive), so it closes under either lawful ruling rather than choosing one; the
# language question itself is Keaton's. `lap` rather than `cadence`: the pass costs 2.5s measured
# 20260908.170542 on this pier, and the surface it reads is glow/ source, which the language lane
# edits every round. Authored 20260908.143733 in a lap that never landed; restored from its stash
# and re-proven on metal before this row was written.

guard glow_literal_law
path tools/g/glow_literal_law_witness.rish
tier lap
seated 20260908.170542

# energy_instrument -- a joule this host will not let us read is not a joule we have. The tree's
# energy arc runs several papers deep and not one figure in it was read from a counter; every one is
# a CPU-second proxy or a vendor budget. This names the tier any energy figure taken here was
# measured at -- `joules`, `counters`, or `cpu_seconds` -- and it reads by OPENING each counter and
# requiring digits back, because since PLATYPUS (CVE-2020-8694) Linux ships intel-rapl `energy_uj`
# at mode 0400, so a path that exists can refuse every unprivileged read. A second fixture asks the
# question sysfs cannot answer -- whether a declared instrument is one a process actually reaches --
# by running tools/rye/perf_self_count.rye and requiring a count. A host is not a fault:
# `tier=cpu_seconds` passes free. What the control gates is the probe's own ability to say `joules`
# when joules are there, over planted sysfs pens, since a probe that cannot say `joules` proves
# nothing by saying `none`. `lap` at a measured 1.0s on this pier: a ship moves between machines,
# and a lap is where a figure gets written down. Authored 20260908.131900 in a lap that never
# landed; re-proven on metal before this row was written.

guard energy_instrument
path tools/e/energy_instrument_witness.rish
tier lap
seated 20260908.234221

# Test mutation liveness is separate from adoption. The scan owns the safe-sed
# capability probe; hosts lacking it get a named skip. Unresolved inputs remain
# reported gaps, and failed programs refuse alongside dead mutations.
guard plant_liveness
path tools/p/plant_liveness_witness.rish
tier lap
capability sed_sandbox
seated 20260909.025521

# THE TWO ROSTERS OF THE SESSION-LOGS ROOM, held to one derived reading. `session-logs/README.md`
# and `session-logs/CHAPTERS.md` each spell every day's lap count as a literal constant, and only
# the pin is named by a close duty -- so the roster drifted for eleven days, six closed days wrong
# and six days missing a row entirely, all twelve found by a hand on 20260909 rather than by any
# instrument. This guard derives each count off the shelf's own rows and holds both pages to it.
# `lap` rather than `cadence`: the pass costs 5s measured 20260909.012700 on this pier, and the
# surface it reads is written by EVERY lap of every ship, which is the clock a day-close runs on.

guard session_roster_agree
path tools/s/session_roster_agree_witness.rish
tier lap
seated 20260909.012700

# One literal, two verdicts, seated 20260909.001908. glow/rune_shop_gate.rye:522 carries a comment
# naming a "house parse law" -- refuses empty, leading zero, overflow -- and it is one of FOURTEEN
# source-decimal readers in the language, the only one holding it. Proven on metal the day this was
# seated, on three paths in one tree: `?:  (eq a b)  01  0` refuses with MalformedBody through the
# shop gate, while `=/  amount  007` lowers GREEN through glow/lower_multi.rye to
# `const amount: u32 = 7;` and `=/  amount  03` lowers GREEN through glow/lower_compose.rye to 3.
#
# The lexer is neutral, which is why the split could hide: glow/tokens.rye scan_digits takes any run
# of digits and emits one `.decimal` token, so nothing about a leading zero reaches the token stream.
# The disagreement lives entirely in the fourteen functions that turn those bytes into a u32, and no
# two of them are called from one place, so no test ever sat them side by side. Each is internally
# consistent and correct on its own terms; the fault exists only in the relation between them, which
# is how fourteen careful hands produced it.
#
# WHY THE RATCHET COUNTS VERDICTS RATHER THAN LAW-BREAKING READERS. Which way the law falls -- Glow
# refuses a leading zero everywhere, or accepts one everywhere -- is a language custody ruling that
# stays Keaton's. A ceiling on `law_absent` would presume it: rule that Glow accepts, and the repair
# deletes the check from its one holder, taking 13 to 14 and reddening this guard for landing the
# answer. The count of DISTINCT verdicts falls to one under either ruling, so the meter holds the
# fault still without deciding it. Two control legs carry exactly that shape -- a room refusing
# everywhere and a room accepting everywhere both read one verdict and both pass.
#
# `law_claimed_absent` is the gate, and it is the copy this split was made of.
# glow/rune_assert.rye parse_decimal is the shop gate's function byte for byte, minus the loop
# variable's name and minus the one line that refuses. Copying a law's shape without its check is
# how the split reached fourteen readers; copying its COMMENT too is the same move one step louder,
# decidable today, and held at zero.
#
# `tier lap` because it is file reading at 0.6s and the whole guard costs 4s, and because a ratchet
# only works if it is read every lap. The population is DERIVED from the room on every pass -- this
# lap's own first reading, taken by hand with grep, found 5 of the 14 (REDS %532's lesson landing on
# the hand that cited it).
guard glow_decimal_law
path tools/g/glow_decimal_law_witness.rish
tier lap
seated 20260909.001908

# query_wire_retention -- what a kept answer costs per read, measured rather than argued. The
# retention arc proved a decoded QueryWireResponse BORROWS its text from the payload, so a caller
# who keeps the answer keeps those bytes; it left unmeasured whether that caller should also keep
# the decoded struct. comlink/query_wire_retention_cost.rye brackets two workloads between reads of
# a self-scoped retired-instruction counter -- pid 0, no root, no `perf` binary -- alternating them
# sample by sample so host drift moves both together, and publishes the median of seventeen samples.
# WHY THE CONTROL CARRIES THE GATE: the cached-read loop is loop-invariant when it reads one fixed
# hit, and the harness's first draft read exactly that -- under -OReleaseFast twenty thousand reads
# compiled to six instructions and it published an elimination as a saving. The rotating hit index
# is the cure, and tools/fixtures/q/query_wire_retention_control.sh proves the cure load-bearing by
# planting the fault back and requiring the planted copy to collapse. A host refusing the counter
# says `tier=cpu_seconds` and passes free, since a host is not a fault. `tier lap` at a measured
# 1.5s on this pier with the builds cached: this is a performance claim, and a ship moves between
# machines, so the lap is where the figure gets checked against the metal under it.
guard query_wire_retention
path tools/q/query_wire_retention_witness.rish
tier lap
seated 20260909.152110

# THE GUARD THAT WALLS THE RUNE TABLE, HEARD FOR THE FIRST TIME (`20260909.155028`). It has stood
# GREEN and rostered nowhere since STOA90, which is the shape the aether page names outright: a
# witness must be proven able to make a sound before its silence means anything. Unrostered, its
# silence meant nothing at all -- and the fault it was built to catch had already walked past it,
# because `?&` and `?|` entered `glow/tokens.rye` `match_rune2` on `20260830` and never entered the
# pronunciation roll beside it.
#
# `tier lap` at 0.6s, which is 0.04 percent of a 1,537s pass. It reads five files and forks no
# compiler; there is no cadence cheap enough to be worth the drift.
#
# A FOURTH BINDING, AND THE FIRST THAT ASKS WHETHER A RUNE IS TAUGHT (`20260911.202958`). The three
# bindings above each hold the lexer against a NAME -- the pronunciation roll, three G1 briefs, the
# TAME family index -- so a rune with a name answers all three, and whether a reader could learn it
# went unasked. `|+` barlus was named `20260822`, carries STOA332-336, is parsed in
# `glow/rune_shop_gate.rye:parse_body` and folded by seven gate sources under `src/gate/`, and no
# page of the Glow Book taught it. The worker walks the same derived heads against
# `active-designing/docs/glow/runes.md` now, publishing `book_named` and `book_unnamed_glyphs`, and
# refuses an untaught head that stands on no exemption. `?&` and `?|` keep the exemption they
# already hold one binding over, for the same cause: an entry leads with its spoken name.
#
# A FIFTH BINDING, AND THE FIRST THAT READS THE WHOLE LEXER (`20260915.225111`). The four bindings
# above each derive their head roster from the `const pairs` constant declared inside the
# `match_rune2` function, and `lex_one` reaches two rune-shaped two-byte heads on branches BEFORE
# that call -- `::`, routed to `skip_comment_line`, and `==`, pushed as the `.double_equals` token
# kind. Thirty heads read where the lexer accepts thirty-two, and the two outside were unreachable
# by construction: no plant inside the pairs table can touch them, and no reading derived from it
# can count them. `==` is what the hole cost -- consumed by `glow/rune_shape.rye` at two call sites
# as the tall-form terminator, standing in 105 tracked `.glow` sources, and named in NONE of the
# three documents the guard binds, while `--` closes the same kind of form one line below it in the
# same lexer and is named in all three. The worker walks `lex_one` itself now and publishes
# `outside_heads`, `outside_roll_named`, `outside_book_named` and `outside_unnamed_glyphs`: the
# reading is 2, 0, 1 and `==`. Both are exempt BY NAME on the cause the two exemptions above stand
# on -- whether a terminator and a comment head are runes wanting a pronunciation row is Keaton's
# word -- and a third reds wherever it arrives.
guard glow_rune_alphabet
path tools/g/glow_rune_alphabet_witness.rish
tier lap   # 0.6s; 32 lexer heads -- 30 derived from match_rune2 against 28 pronunciation rows and 28 taught entries, plus 2 matched in lex_one above that table; four exempt by name
seated 20260909.155028

# mantra_tablecloth_hit_census -- how large an answer can be, enumerated rather than sampled. Two
# retention papers ended on one request, count a real caller's hit distribution, and the number does
# not live in a caller: a Tablecloth query is a conjunction of exact equalities, so an answer is an
# agreement class of the CATALOG. Five fields make 31 query shapes and `max_bindings` caps a catalog
# at 16 leaves, so mantra/recall_tablecloth_hit_census.rye walks the space whole -- a saturated
# catalog per shape, the worst case proven rather than projected. It reads 2 of 31 shapes bounded to
# one hit, exactly those naming the uniqueness key `append_leaf` enforces, and 29 reaching past the
# wire's 8. WHAT IT FOUND ON THE WAY: `max_wire_hits` declares a ceiling in HITS where the wire's
# bound is 340 BYTES, so `build_response` builds three hits inside the declared eight and
# `encode_response` refuses them on the next call (REDS, stamp `20260909.213236`). WHY THE CONTROL
# CARRIES THE GATE:
# every finding is an assert inside the program, and an assert never made false reads like a comment
# -- tools/fixtures/m/mantra_tablecloth_hit_census_control.sh plants five, each read off the bytes
# and each required to reach its own named line before it refuses. Its own first draft carried a
# tautological comptime assert, and the plant that starts the walk at the empty mask is what found
# it. `tier lap` at a measured 4.9s on this pier with the builds cached: it forks a compiler, and a
# ship moves between machines, so the lap is where the figure meets the metal under it.
guard mantra_tablecloth_hit_census
path tools/m/mantra_tablecloth_hit_census_witness.rish
tier lap   # 4.9s; 31 shapes over saturated catalogs, five plants, 21 control checks
seated 20260909.215647

# A TUTORIAL'S QUOTED OUTPUT IS A CLAIM ABOUT BEHAVIOR, and until this row nothing in the tree read
# one. `qa_report_card` scores Truth by whether cited PATHS resolve, `document_mirror_scan` proves
# two homes hold identical bytes, and a fenced output block holds neither a path nor a mirror -- so
# docs-geode/tutorials/the-first-hour.md quoted a three-line ending for its toolchain fetch where
# the command prints four, and the page read Truth 100 the whole time it was wrong
# (20260909.174000). This runs the command above each quoted block and compares. It reads the
# convention the docs-geode room already writes -- an `sh` fence, then an unlabelled fence -- which
# is 6 of the 17 `sh` fences across its 38 tracked pages. WHAT RUNS is narrow on purpose: one plain
# invocation of `rishi/bin/rishi` or `sh` naming a TRACKED script, no redirect, pipe, semicolon,
# ampersand, backtick, dollar or glob. The test is on what EXECUTES rather than on what it is
# handed, so the demos page keeps its deliberately stale resolver argument and still checks. A fence
# that makes a directory or names a script the reader writes is HELD, a ratchet at 2. A block that
# honestly moves declares itself -- `<!-- volatile: why -->` above its fence -- reports, and never
# gates; a reasonless declaration excuses nothing. NOT `docs_command_path`, one row up, which proves
# a printed path RESOLVES: a page can name paths that all resolve and still promise output no
# command produces. `tier lap` at a measured 1.0s: it runs four short commands, and the fault it
# catches is spent by a reader on their first hour.
guard tutorial_output
path tools/t/tutorial_output_witness.rish
tier lap   # 72s (was 1.0s); 17 pairs read, 10 run, 55 control legs with every refusal bitten and
           # lifted. The cost rose with the corpus on `20260911.171657`: the operator manual and
           # SOURCE.md joined docs-geode, and three more pairs now invoke real witnesses that build
           # binaries. It stays `tier lap` on purpose -- `cadence` is the clock 69 of 83 guards have
           # never been turned on here (%568), and a drift guard nobody runs is the fault it exists
           # to catch, one room over.
seated 20260909.235500

# tutorial_bare_fence counts what `tutorial_output` cannot see. That guard opens a pair only on a
# fence marked ```sh or ```bash; an UNLABELLED fence sits in the same room, above the same kind of
# block, promising the same thing to the same reader, and stands in no pair -- neither checked nor
# counted nor held, which reads from outside exactly like clean.
# `docs-geode/tutorials/running-the-fleet.md` carried sixteen bare fences and none tagged while
# declaring on its own face that every command below was run against this tree.
# GATED at zero: `bare_runnable_lone` -- roster-passing commands with no block beneath, where
# tagging creates no pair, runs nothing, and the repair is one word. Four stood at seating and all
# four were taken. RATCHET at 2: `bare_runnable_paired`, where tagging CLAIMS the block is that
# command's output and only a reader of the page knows -- both standing cases prove it, one whose
# block beneath is the next command and one whose first line watches until a hand stops it.
# The refusal classes name the ROSTER'S own word -- `off_roster`, `metacharacter`, `untracked` --
# rather than the block's genre, which is a judgment the scan declines.
# ONE RULE, TWO READERS: the roster predicate moved to
# `tools/fixtures/t/tutorial_run_roster.sh`, sourced as a sibling by this scan and by
# `tutorial_output_scan.sh`, which held it inline; the extraction moved no count and the elder
# report was captured before and after byte-identical.
# `tier lap` at a measured 7s: the scan runs no command at all -- it counts what the elder guard
# cannot reach -- and the pen builds ten small git repositories.
guard tutorial_bare_fence
path tools/t/tutorial_bare_fence_witness.rish
tier lap   # 7s; 79 untagged fences read, 23 control legs, three mutations bitten.
seated 20260917.034323

# prose_count_table reads the sentence ABOVE a table against the table itself. A teaching page
# introduces a table by saying how long it is -- `Four tutorials ship from this room`,
# `## The Seven Rooms` -- and that number is a copy of something three lines below it: the table
# grows when a hand adds a row, and the sentence does not. This shelf has paid twice and written
# both receipts on its own face -- `docs-geode/README.md` said ten rooms while its table listed
# twelve, from the very commit that added the twelfth, and stood sixteen days; `etc/README.md`
# named eleven neighbors while twelve stood. Nothing else reads the class: `qa_report_card` scores
# Truth by whether cited PATHS resolve, so a stale count reads Truth 100; `tutorial_output` reads
# what a command PRINTS and a prose count is inside no fence; `crushed_index` reads one page's one
# table. GATED at zero: `drift`. REPORTED: `near_block`, a count whose noun the table header does
# not name, five standing, four of them counts of something the table does not tabulate and the
# fifth an honest synonym past what a header test can reach; and `listform`, the same shape above a
# LIST, nineteen with sixteen agreeing, ungated because a list carries no header and the three that
# differ are a duration, a TAME rate, and a section reference.
guard prose_count_table
path tools/p/prose_count_table_witness.rish
tier lap   # 1s; 84 pages, 14 sites all agreeing, 37 control legs, four mutations bitten.
seated 20260917.072438

# self_matching_assert reads the command FORM and the output channel before gating an operand
# match. A simple default-stdout run carrying one literal path is gated; options, stderr,
# compounds, other utilities, and unresolved bindings stay REPORTED, since a label is not a
# reading -- `stat --format=%s path` prints the size alone and names no operand (`%685`).
# `lap` rather than `cadence`: the scan is 1.3s over 2,456 runners and the control 16s, and the
# corpus it reads is every tracked runner, which any lap may add to.
guard self_matching_assert
path tools/s/self_matching_assert_witness.rish
tier lap
seated 20260910.022156

# silent_leg reads the OTHER borrowed-evidence source, one room over from its sibling above:
# `tools/g/glow_run_worker.sh` echoes a trailer of its own, `EXIT:$?`, and a witness reads the
# desk's answer out of that same stream -- so `assert over.out contains "0"` reads true off
# `EXIT:0` however the desk answered. That is almost always the leg proving a bound REFUSES, so
# the refusing side of a Glow gate was the one side no witness could hear. Proven on metal
# `20260910`: a gen-floor desk answering 7 where its invariant says 0 kept the elder leg green.
# Eleven mantra and tally legs carry the exact list read now and the lane is gated at zero; 28
# stand in seven other rooms under a ceiling that only falls, each its own room's hand to repair.
# The sibling cannot see this family -- its `min_needle` of 3 passes over the one-character needle
# that carries almost all of it. `lap` rather than `cadence`: the scan is ~2s over 2,470 runners
# and the control ~4s, and the corpus is every tracked runner, which any lap may add to.
guard silent_leg
path tools/s/silent_leg_witness.rish
tier lap
seated 20260910.081300

# THE COUNT IN FRONT OF THE BYTE CEILING. A module writing items into a fixed buffer can name two
# maxima -- how many items it carries, and how many bytes it has -- and only one of them is real.
# The encoder decides which, never the declaration. `mantra/recall_tablecloth_query_wire.rye`
# declares `max_wire_hits = 8` six lines above `max_wire_payload = 340`, where one hit at maximum
# name lengths encodes to 119 bytes, so the attainable count is 2. Its sibling
# `recall_sync_wire.rye` declares 8 entries against the same payload where one worst-case entry
# costs 676 bytes -- attainable count zero, under every input. Both were found by hand a lap apart
# (20260909.213236, 20260910.001454), and a lantern that fires twice becomes a loom. Census and
# arithmetic: external-research/date/20260910/20260910-001454_the-count-in-front-of-the-byte-ceiling.md.
#
# THE CLASS IS QUIET, which is why it wants an instrument rather than another hand. A bound too
# SMALL refuses honest work and is found within a day. A count too LARGE refuses nothing, passes
# every test built from short names, and reports its wrong number to every reader who asks.
#
# IT REACHES THE TREE where the hand reached four modules: 29 distinct tracked sources declare both
# shapes, against the 4 the hand-walked census over mantra, caravan, tally and aurora could see.
#
# AND IT DOES NOT JUDGE, which is the finding this guard was built out of. Four lexical
# discriminators were measured against the four cases the paper walked, and every one leaked --
# names in one file (48 candidates, 2 real), names in one function (admits `recall_lap1`, two
# independent guards three lines apart), the byte name checked after a loop (admits
# `glow_storage_scope`, a per-item check inside a loop), and the byte name checked against a
# variable the loop advances (admits it again, on a partial field-name match). The real test is
# whether the loop ACCUMULATES into the bounded buffer, which is dataflow rather than text. A guard
# that guessed would red on honest work and be turned off. So it asks the file to answer instead --
# one `// ceilings: independent|derived|literal` line -- which is the move `declared_ceiling` makes
# for a page and `tutorial_output` for a command: where a judgment cannot be mechanized, mechanize
# the DECLARATION of it. ONE GATE at zero, a verdict no tool can read. TWO RATCHETS that only fall:
# `pairs_undeclared` 25, which every file leaves on the lap a hand touches it, and `pairs_literal`
# 2, the real unrepaired class. A DERIVED ceiling leaves the population by construction, so the
# repair is rewarded by the reading rather than merely permitted -- which is why `caravan/`, whose
# rung bounds are chained, is absent for the right reason. `tier lap` at a measured 3.4s: two
# batched greps over 1,964 sources, then 29 files read again.
guard ceiling_pair
path tools/c/ceiling_pair_witness.rish
tier lap   # 3.4s; 29 pairs read, 14 control behaviors with 5 refusals bitten and lifted
seated 20260910.020000

# room_braid -- the single-stranded claim, performed rather than believed. The foundation says a
# part stands free and "you can check it by trying to pull one part out"; the air row repeats the
# pull as its own sense; nothing had ever performed it. Zig refuses an import escaping its
# directory, so every cross-room reach is a SYMLINK and the symlink set IS the dependency graph --
# 226 cross-room links, one escaping `@import` in the tree, no root build.zig. Twenty of the 226
# are imported by nothing and are counted dead rather than as edges. The GATED reading is
# `braid_max`, the largest set of rooms that cannot be lifted out separately: 9 at seating
# (amphora, brushstroke, comlink, granary, kumara, linengrow, mantra, pond, settlement), ceiling 9,
# meant only to fall. `tier lap`: 10s, the closure being one awk relaxation over tens of nodes.

guard room_braid
path tools/r/room_braid_witness.rish
tier lap
seated 20260910.050619

# ceiling_teeth -- a declared ceiling is read where it can refuse. Its sibling above asks whether
# two ceilings in one file agree; this asks whether ONE ceiling is read anywhere a caller can be
# turned away. Six classes, decided by reading the file rather than judging it: refused,
# structural, cut, derived, asserted_only, unread. ONE GATE at zero -- `unread`, a constant its
# own file declares and nothing in any tracked `.rye` ever reads, which needs no judgment and is
# how `max_mirror_pairs = 4` stood four lines above the struct it was drawn for while
# `poll_one_cycle` walked `pairs.len` with no ceiling and cast it to u32 unchecked. ONE RATCHET
# that only falls: `asserted_only`, where a repair lands, so a wall there would refuse the fix in
# the same breath as the fault. The other four report. Rooms are arguments, seated at mantra and
# tally -- the lane a hand walked constant by constant -- and another lane adopts by naming itself
# here. GLOW ADOPTED `20260917` at its own `asserted_only <= 3`, held beside mantra and tally's 1
# rather than merged, so a repair in one room can never pay for a regression in another. THE
# REFUSAL IS READ BY BLOCK, NEVER BY A LINE COUNT (`20260917.040203`): the elder three-line window missed a
# refusal record filled between the test and the return -- `glow/tokens.rye`'s four -- and admitted
# a refusal spelled in a comment or inside an emitted string literal. `tier lap`: two readings.
guard ceiling_teeth
path tools/c/ceiling_teeth_witness.rish
tier lap   # 9.5s; 110 ceilings read over two rooms, 31 assertions over 23 behaviors, three mutations bitten
seated 20260910.054031

# glow_shared_bound -- one bound, one value, however many Glow modules publish it. `max_name_len`
# stood FOURTEEN times -- tokens.rye, rune_shape.rye, expr.rye and eleven rune modules -- each
# spelling 64 on its own line, compared by nothing, and three of the agreements were stated in a
# `///` comment, which is a wish rather than a wall. That name found an OWNER on `20260910.083221`:
# the lexer declares it and thirteen modules alias it, so it carries one value by construction and
# has left this census. The front end now publishes 184 numeric bounds across its 134 sources and
# shares SIX names, every one a pair of PEERS where no module owns the rule and an alias would
# invent an arbitrary dependency between equals. There the comparison is the owner. Every shared
# name AGREES, so the gate is zero rather than a ceiling -- no residue to ratchet down, and a
# ceiling above zero would buy room for the first break. Sibling to `ceiling_pair`, which reads two
# bounds standing in ONE file where this reads one bound across MANY: that one is depth, this one
# is breadth. The instrument moved to `tools/fixtures/s/shared_bound_scan.sh` on `20260910.093000`
# and took a `--room` parameter, since `mantra/` carries the same shape and a second copy of the
# reading would be the fault it refuses; this guard names its room explicitly and owns the shared
# 53-leg control. `tier lap`: 13s, one grep over 134 sources plus that control on real git
# repositories.
guard glow_shared_bound
path tools/g/glow_shared_bound_witness.rish
tier lap   # 13s; 184 declarations read, 53 control behaviors with 5 scan mutations bitten
seated 20260910.073606

# mantra_shared_bound -- one bound, one value, across the version store. `mantra/` publishes 31
# numeric bounds across its 41 tracked sources, and TWO of those names stand in more than one
# module. `max_resin_bytes` is 512 in `beading.rye` and `recall_lap1.rye` -- one resin's byte
# ceiling, which the beader divides and the catalog stores, so a resin one module accepts is a
# resin the other must be able to hold. `max_wire_payload` is 340 in `recall_sync_wire.rye` and
# `recall_tablecloth_query_wire.rye` -- the sealed datagram's own body budget, one physical fact
# spelled twice. `beading.rye` states its agreement in a comment, which is a wish rather than a
# wall; `max_wire_payload` carried no comment at all, so its agreement was held by nothing. Both
# agree today, so the gate is zero rather than a ceiling. Shares one instrument with
# `glow_shared_bound` -- `tools/fixtures/s/shared_bound_scan.sh --room mantra` -- whose 53-leg
# control runs once per pass under that guard rather than twice. `tier lap`: one grep over 41
# sources, no control of its own.
guard mantra_shared_bound
path tools/m/mantra_shared_bound_witness.rish
tier lap   # 31 declarations read across 41 sources; the shared control runs under glow_shared_bound
seated 20260910.093000
# qa_setting_declared -- Gauge is one style on one DIAL, and 110 living pages decline to set it.
# `qa_report_card.sh` reads the setting from a `--setting` flag the CALLER hands it and reports the
# page's own declaration apart, as `qa_declared_setting`; when a page declares nothing those two
# facts stop agreeing, and nothing counted how often. Measured `20260910.104833`: of 330 living
# pages, 158 declare a style and 48 of those name a setting. Priced over 27 of the 110 unnamed at
# stride 4, 20 scored differently at Door than at Field, mean 3.29 points, and FOUR crossed the B
# door at 80 -- `MAP.md` 79/84, the page the baton tells every ship to read instead of walking the
# root, and `construction/REDS.md` 77/82. The lantern fired twice in one lane in one morning (eight
# points on `glow/nock/README.md` at `20260910.082330`), which is what earns an instrument rather
# than a repair: 110 separate asks become one ruling with a population attached. REPORTED, never
# gated -- Gauge's own law says most pages know their own setting, so a page that declares none may
# be perfectly honest, and a gate here would red on ordinary work. The count is cheap and reads
# headers alone; the PRICE costs two card runs a page and is a hand's `price` subcommand.
guard qa_setting_declared
path tools/q/qa_setting_declared_witness.rish
tier lap
seated 20260910.104833
# tilak_root_count -- the marked value says the engine hardcodes exactly TWO type-marks, and until
# `20260910` nothing in the tree compared that claim to the engine. It is written down three times:
# `foundations/20260703-202312_the-marked-value.md` argues it, `amphora/manifest_entry.rye` restates
# it in a doc comment above `mark_plain_bytes` and `mark_manifest`, and
# `src/shape/tilak-root-count.glow` displays it as `example    2`. The pedestal's own leg inside
# `src_first_resident_witness` greps that pedestal for `plain-bytes`, `manifest` and `%tile` --
# three strings the file already holds -- and opens the engine never, so a third root wired into
# `mark_verdict` passed every guard standing. Measured the same lap: of the 17 pedestals that leg
# holds, THREE are genuinely tied to their Rye source (the `ParseError`, `BrushSurface` and
# `BrushFrame` desks, a repair REDS %358 bought), one is tied to a `.brush` fixture, and THIRTEEN
# are checked against literals typed into the witness beside them. This row moves one of the
# thirteen; the other twelve are named on the card rather than swept, since each wants its own
# reading of its own source.
#
# Five readings, each able to fail while the other four agree: the desk against its own list, the
# count against the marks `mark_verdict` answers on, the count against the roots the engine
# publishes, the desk's NAMES against the arms' names resolved to values, and the arms against the
# published constants. That fifth was argued away in the scan's header as one that could never fire
# alone, and the pen found the case the argument missed -- a bare-literal arm whose constant is
# later renamed, where every other reading agrees while the engine answers on a mark it no longer
# publishes. 21 control cases, 5 welcomes and 16 refusals, each planted and then lifted; the
# granted growth (a third root in engine, verdict, and pedestal) walks free, because the scan
# carries no number of its own and the pair is Keaton's word to widen.
#
# `lap` because the whole witness is a pure text reading plus a pen, 3.0s measured on this pier
# `20260910.152558`. It drives no toolchain: `src_first_resident` already lowers and builds this
# pedestal through Glow and Zig, and repeating that would buy a second copy of one fact at the
# price of a toolchain launch.
guard tilak_root_count
path tools/t/tilak_root_count_witness.rish
tier lap
seated 20260910.152558

# ratchet_slack -- a ceiling nothing compares is decoration, and the family's resting state is zero
# slack. Every ratchet in this tree writes the same rule in its own header, `the ceiling only
# falls`, and followed exactly that rule parks the ceiling at whatever the reading was the day
# somebody last lowered it. Measured `20260910.172957`, the first reading anybody had taken of the
# family as a family: 81 ceilings across 55 scans, 35 at ZERO SLACK, 21 walls already at zero, 13
# carrying slack, and THREE over -- `mantra_weave_model`, whose own witness passes a higher ceiling;
# `dated_path` at 105 against 85, which is REDS %626 at `tier cadence` where no lap hears it; and
# `say_compose_bound` at 538 per mille against 537, `tier lap`, red across the fleet today. The last
# two are one shape, so %626 is a class rather than an instance. This guard holds the CHEAP half, which is structural: every
# declared ceiling is compared inside its own scan, and the pairing table naming each ceiling's
# reading names only ceilings the tree still declares. The slack itself is `--live`, three minutes
# because it runs every scan, so it is hand-run and prints no verdict line at all -- a census a
# roster could seat would cost every lap three minutes to learn what one reading a day teaches.
# `lap` rather than `cadence`: the static half is 6s and the control 3s, and the corpus is every
# fixture scan, which any lap may add to.
guard ratchet_slack
path tools/r/ratchet_slack_witness.rish
tier lap
seated 20260910.172957
# shape_constant_pedestal -- the shape museum displays a number on each pedestal, and until
# `20260910` twelve of those numbers were held by a literal typed into the witness beside them:
# `grep -q 'example    16384' <desk>` proves the desk still says 16384 and asks the engine nothing.
# Change the constant in Rye and the pedestal stays green over a bound the engine stopped keeping.
# This row takes the five whose number is a published constant of one module,
# `brushstroke/brush_parse.rye`, and reads each one out of the source: `max_frame_lines`,
# `brush_skate_cols`, `brush_skate_rows`, `max_brush_bytes`, `max_pin_bytes`.
#
# TWO OF THE FIVE ARE NOT LITERALS, which is why the reading resolves rather than greps.
# `brush_skate_rows` is an alias to `max_frame_lines`, so its desk shows the frame ceiling under a
# second name; `max_brush_bytes` is `16 * 1024`, so 16384 stands nowhere a grep could find it. A
# form the reading has never met is NAMED as unresolved and reds, never counted as zero.
#
# The lap's own finding: the frame ceiling is written FOUR times -- published once as
# `max_frame_lines`, again as a private `const max_lines: u32 = 8` in `brushstroke/seed.rye` and
# once more in `brushstroke/wayland_seed.rye` (which also reaches for the published constant at
# three other sites, both spellings in one file), and displayed a fourth time on the desk. Each
# private copy asserts against its own number, so lowering the published ceiling leaves both
# modules letting eight lines through in silence. The scan gathers every site and refuses when they
# part; a site publishing NO ceiling is dropped rather than counted, since deleting a private copy
# in favour of the published constant is the repair this reading wants. The desk was repaired in
# the same lap to name `max_frame_lines`, which it never did.
#
# 18 control cases, 4 welcomes and 14 refusals, each planted and then lifted, mutation-proven. A
# bound doubled in both rooms walks free: the scan carries no number of its own.
#
# `lap` because the whole witness is a pure text reading plus a pen, 9.1s measured on this pier
# `20260910.173003`. It drives no toolchain: `src_first_resident` already lowers and builds these
# five pedestals through Glow and Zig.
guard shape_constant_pedestal
path tools/s/shape_constant_pedestal_witness.rish
tier lap
seated 20260910.173003

# shape_member_pedestal -- the sibling row to `shape_constant_pedestal`, and the one that closes
# the class. Five museum pedestals display a published constant; SIX display a COUNT -- struct
# fields, constructor parameters, sibling modules, refusal sites, fixture lines -- and a count has
# no published name to read. There is no `pub const manifest_field_count`: the three fields of
# `ManifestEntry` are the fact, and the desk's `3` is a reading of them. So this row names the
# KIND of member per pedestal, five kinds for six desks, each a reading a visitor can rerun by hand
# at the declaration. With it seated, every desk in `src/shape/` showing a figure stands on its own
# engine; the three showing words rather than a number have no pair to compare.
#
# The lap's own finding: FOUR of the six named no source at all. `shape-manifest-field-count` cited
# a brief, two cited `docs/TUBE.md`, and `shape-mand-ring-count` recited its rings in prose --
# honest provenance, and none of it a path to an engine, so a visitor had a number and nothing to
# check it against. All four carry a `Source:` line now, and the scan refuses if one leaves.
#
# The CONTROL's own finding, which rewrote the scan: the grant-family selector first ENUMERATED the
# three mold modules by name, so a fourth family in the room could never be counted -- a reading
# able to answer only its own length, green forever. It names the family's SHAPE now,
# `glow_*_grant.rye` and `glow_*_scope.rye`, and case 8 plants the fourth mold and is refused.
#
# 19 control cases, 4 welcomes and 15 refusals, each planted and then lifted, mutation-proven --
# dropping the names reading reds one leg, dropping the value comparison reds six. A mold grown in
# Rye AND on its desk walks free, and so does a grant witness added beside its mold: a proof of a
# mold is no new family.
#
# `lap` because the whole witness is a pure text reading plus a pen, 9.0s measured on this pier
# `20260910.193433`. It drives no toolchain: `src_first_resident` already lowers and builds these
# six pedestals through Glow and Zig.
guard shape_member_pedestal
path tools/s/shape_member_pedestal_witness.rish
tier lap
seated 20260910.193433

# card_pin_claim -- reds_pin_capacity derives whether construction/REDS.md can still accept a row;
# construction/ITINERARY.md is the page every lap of every ship reads WHOLE to choose its work, and
# a hand may write that same fact there in prose. Two spellings of one reading are two files that
# may come to disagree (%231), and when they do the prose wins, because the card is read first and
# read whole. The disagreement has fired twice in one direction: on 20260910.140033 the capacity
# scan's own doors line read "no lawful fold exists here" from a state that had passed and four
# ships carried "yours, Keaton" onto the card in one morning; on 20260911.025632 the card said no
# new red could be booked until Keaton raised the bound while the instrument read pin_deadlocked=0
# and pin_foldable_rows=1, one reds_fold.sh from headroom. Gates claim_disagreements at zero and
# REPORTS card_blocked_claims, silent_deadlock and card_cites_scan -- the converse reported rather
# than gated because its cure is a line somebody must WRITE, and a gate there would ask every ship
# to edit the card. The deadlock reading is QUOTED from the capacity scan rather than recomputed, so
# this guard adds no third spelling of the reading it exists to hold to two. `lap` rather than
# `cadence`: it reads one card and one scan, and the page it reads is the page every lap chooses
# its work from.

guard card_pin_claim
path tools/ca/card_pin_claim_witness.rish
tier lap
seated 20260911.034352

# rye_build_lock_reach -- the build lock is scoped to a working directory and the shadows it guards
# are scoped to the tree (found `20260911.041926` on the air rota row, pressing REDS %700's fence).
# `rye build` and `rye run` bridge a `.rye` root and every local `.rye` module it imports to
# adjacent `.zig` shadow files, hand those to the toolchain, and delete them on the way out. Two
# builds staging one shadow delete each other's bridge mid-compile, which is the race %281 booked
# and `rye/src/main.rye` answered on `20260827.023156` with one lock held from the first shadow
# write to the last delete, cache-hit path included.
#
# THE TWO SCOPES PART, read off the source rather than inferred. The lock directory is
# `.rye-build.lock` opened against `std.Io.Dir.cwd()`, so the lock's scope is THE CALLER'S WORKING
# DIRECTORY. A shadow's path is its source's path with `.rye` traded for `.zig`, so the shadow's
# scope is THE TREE. Two callers standing in two directories take two locks over one namespace and
# neither waits for the other.
#
# WHAT HOLDS IT TODAY IS A COINCIDENCE, WHICH IS WHY IT EARNS A GUARD. Two working directories reach
# this tree's builds -- the repository root and `glow/.cache`, the second used by two Glow lowering
# witnesses whose closures stay inside that directory. Measured `20260911`: 1,314 build roots, 1,696
# shadow paths, 907 of them shared between two or more roots under the ONE root lock, and zero
# claimed across the two scopes. The shared 907 are safe by construction -- one lock stands over
# them, which is the lock's whole purpose -- so they are reported and gated at nothing. The gate is
# `cross_scope_collisions` alone, and it is free while the coincidence holds and loud the day a
# witness reaches for `cd <dir> && rye build ...` to shorten a path. Twenty-two control legs on real
# repositories prove it both ways, four mutations biting 8, 2, 3 and 2 of them.
#
# ONE FILE IS READ PAST, NAMED AND PRINTED: this scan's own control, whose whole content is planted
# material. It writes its pens with literal `cd <dir>` lines rather than shell variables, so the
# elder pen reading could not tell them from tree invocations -- and the guard refused its own pen
# the moment the control was staged, `lock_scopes` reading 5 and naming three pen directories. The
# same trap then closed on the scan's own head comment, whose example spelled a plant out in full;
# an illustration is written from placeholders for exactly this reason. Four further legs prove the
# exclusion is load-bearing rather than decorative.
#
# WHY `tier lap`. One `git ls-files` over `tools/` and an awk walk of 1,314 import closures, about a
# second measured `20260911`. Ordinary work writes this fault in the line it is typed, so a cadence
# clock would let one ride four laps before anything said so.

guard rye_build_lock_reach
path tools/r/rye_build_lock_reach_witness.rish
tier lap
seated 20260911.041926

# rye_build_lock_holder -- a live holder keeps the build lock it took (found `20260915.211500` on the
# aether rota row, whose instruction is to listen for the claim a page keeps repeating). The sibling
# above asks whether the lock's SCOPE reaches the shadows it covers, and reads zero. This one asks
# whether the holder keeps it, and the elder answer was no: a lock with perfect reach that can be
# taken from a live holder guards nothing. Reach and tenure are two questions.
#
# THE FAULT, AND WHY BOTH HALVES WERE REASONABLE ALONE. `rye/src/main.rye` created the lock with an
# atomic `createDir` and wrote the holder's pid in a SECOND call, so between the two it stood with
# nothing inside. The waiter's own rule read an unreadable pid twice, one 50ms poll apart, as a
# corpse and cleared it -- correct by its own lights, since a lock naming nobody is what a killed
# build leaves behind. Together they let a waiter clear a LIVE holder's lock and bridge beside it,
# and the loser reports `unable to load '<name>.zig'` about correct code.
#
# THE CLAIM THE PAGE KEPT REPEATING was the elder comment's own: "one wrong clear only when a maker
# hangs inside a two-line window". The code beside it says one 50ms poll, and this pier runs eight
# parallel builds at load average 16.8, measured the lap this was found -- where a stall of that size
# between two syscalls is ordinary. A second reading needs no timing luck at all: the pid write's
# refusal was swallowed, so a write that simply failed left the lock anonymous forever.
#
# THE REPAIR IS THAT THE STATE NO LONGER EXISTS. `build_lock_claim` assembles the lock complete --
# pid already inside -- in a staging directory and `rename`s it into place; `rename` onto a directory
# already holding a pid refuses with `DirNotEmpty`, so the move is both the claim and the test of it.
# The WAITER is untouched, because clearing a genuine corpse is still its job, and under this
# protocol an anonymous lock can only ever be one. Reading the pid back after writing it was the
# first repair tried and it does not work -- a robbed holder writes its own pid over the thief's and
# reads back exactly what it just wrote. Writing the control is what found that.
#
# WHAT IT READS. There is no census to take, because a race has no population. The pen opens the one
# window on demand -- `RYE_BUILD_LOCK_STALL_MS` holds a claim still between staging and rename -- and
# looks through it: 28 legs, the elder compiler built from the commit before the repair walking into
# a planted anonymous lock, and the repaired one never once observed anonymous through a two-second
# claim. The pen also proves it SAW the window open, since a reading of nothing is indistinguishable
# from a healthy one (%463). A mutation that publishes the lock before its pid -- the elder's shape
# rebuilt under the new structure -- is observed anonymous twice where the repair reads zero.
#
# WHAT IT DOES NOT REACH, named rather than implied. `rye/bin/rye` is gitignored, so the repair lands
# on a ship only when that ship rebuilds its compiler, and nothing in this tree reads whether an
# installed binary matches its source. That gap is proposed on the card rather than built here.
#
# WHY `tier cadence`. The pen builds the compiler twice and runs a dozen small builds through them,
# which is minutes rather than seconds on a loaded pier. The fault is written once into a protocol
# and then lived with, so a daily reading is the right clock for it.

guard rye_build_lock_holder
path tools/r/rye_build_lock_holder_witness.rish
tier cadence
seated 20260915.211500

# rye_build -- the wrapper that holds one build at a time per module directory, and the control
# that was proven and unrun (`20260915.202715`). `tools/fixtures/r/rye_build.sh` answers REDS %734:
# `rye/src/main.rye` bridges each `.rye` source to an adjacent `.zig` shadow and clears those files
# when a build ends, so two builds over one module delete each other's inputs mid-compile. The
# wrapper derives the module from the source path's directory, takes a lock inside that directory
# through `lock_acquire` in `tools/fixtures/s/shell_portable.sh`, runs the real build, and releases
# the lock from a trap.
#
# WHY IT IS HERE. The wrapper and its control landed together and nothing ran the control.
# `tools/fixtures/u/unheard_guard_scan.sh` reads every control on disk and asks which one a
# rostered runner reaches; its `control_unheard` rose to 19 against a ceiling of 18 and the scan
# refused. A proof nothing runs passes review, reads like coverage, and says nothing on the day the
# thing it proves changes -- which is the same shape REDS %357 booked when three witnesses of one
# family stood red at HEAD for days beneath a green endurance run.
#
# THE LEG THAT CARRIES THE SUBJECT is `two_modules_concurrent`. A lock serializing EVERY build
# would satisfy a naive no-collision reading while costing the tree its parallelism, so the control
# holds one room shut and watches a build in a second room walk straight through. One leg was added
# on the seating lap: the held-lock refusal stood proven only in the affirmative direction, so the
# control now copies the wrapper with its `lock_acquire` call traded for `true` and asserts the
# refusal STOPS. Without it a wrapper that had lost the one call it exists to make would pass every
# leg. The control stands at sixteen legs, five of them the cross-room and room-bound readings
# `10fcae2878` landed the same day -- count them off the control's own `legs=` line rather than
# from this comment, since only that number is read on a run.
#
# WHAT IT DOES NOT REACH. The control plants and removes `lotus/.rye-build.lock`,
# `image/.rye-build.lock`, `dimeroll/.rye-build.lock`, and `pond/apps/.rye-build.lock` by name, so a
# hand running it beside a live build over any of those modules removes a lock it never took. A
# roster pass runs its guards one at a time, which is why the row sits on the lap clock rather than
# beside a parallel launcher.
#
# WHY `tier lap`. Three real module builds and two bounded waits -- 19s measured `20260915`. The fault
# it catches is written by ordinary work on the wrapper or on the lock helper beneath it.

guard rye_build
path tools/r/rye_build_witness.rish
tier lap
seated 20260915.202715

# error_member_reach -- can this tree produce every refusal it declares? (found `20260911.060527`
# on the air rota row, whose instruction is to close a hand on a boundary and pull). TAME asks that
# a bound fail with a NAMED error, and a named error is a promise to the caller: this function can
# refuse in this particular way, so switch on it. Zig enforces one direction of that promise -- a
# returned error must belong to the declared set -- and nothing asks the other, whether a declared
# member can ever arrive. So a member left behind by a widening keeps standing in the set and reads
# as a refusal a caller may plan for.
#
# MEASURED `20260911`: 1,965 tracked authored sources, 4,213 declared member sites, 1,472 distinct
# names, and NINE sites naming a refusal nothing in this tree can make -- four in Glow
# (`MissingTagged` and `TooFewLines` in `rune_shape`, `TooFewLines` in `lower_named_cast`,
# `NotBarePayload` in `rune_core`) and five across Caravan, Mycelium, Rishi's matcher and the vault.
# Three of the four Glow names were left deliberately: `session-logs/date/20260720/
# 20260720-032713_stoa97-token-mold-spec.kyri` records a lap deciding "remain in ParseError set (no
# removal); only their emission paths change". That decision has stood in no instrument since, which
# is what this row changes -- the removal itself is a language-surface question and stays Keaton's.
#
# THE GATE IS THE TREE-WIDE QUESTION, NEVER THE PER-FILE ONE. Zig error names are global, so a
# ladder may declare one shared vocabulary across many rungs and produce it in a couple:
# `OweMisrecorded` stands in 47 Caravan rungs and is returned in two. 261 sites read unreached in
# their own file and 165 of them are that shape, so the per-file number is reported with its cause
# named and gated at nothing. A guard that reds on a sound habit is one somebody turns off.
#
# WHY A CEILING RATHER THAN ZERO. The nine were not made today and five belong to other lanes. A
# ceiling that only falls means the tenth reds on the lap it arrives, and it costs no lane a forced
# edit. Twenty-three control legs on miniature sources in a pen, every refusal planted and lifted;
# five mutations of the scan bite 2, 3, 2, 1 and 2 of them.
#
# WHY `tier lap`. ONE awk pass over 1,965 sources, 7.6s measured `20260911` -- it was three walks at
# 11.7s until the declared, produced and per-file readings were folded into one reader.
# Ordinary work writes this fault -- a widening that makes a check moot leaves its name behind in
# the line above it, and nothing else in this tree would say so.

guard error_member_reach
path tools/e/error_member_reach_witness.rish
tier lap
seated 20260911.060527

# THE ROW `context/TAME_GUIDANCE.md` CALLED ENFORCED AND NOBODY HELD, seated 20260911.114500.
# That page's lint table stands under the heading "Enforced now" and one of its rows asks for
# exactly one top-level `#` per markdown page, fenced code ignored. It named no instrument. The
# only other page carrying the duty, `tools/fixtures/r/radiant_lint_scan.sh` duty 3, prints
# `deferred (TAME one-# Title / tame-check owns it)` -- and `tools/t/tame-check.rish` spells no
# heading anywhere. Two pages pointing at each other, and a reader checking either end finds a
# citation rather than a hole.
#
# The rule was kept by habit at 389 of 390 living pages before the guard existed, which is the
# cheapest moment to seat one: the ceiling costs a single page of slack rather than a migration.
# 0.23s over 390 pages and 2.7s for its pen, so it rides every lap.
guard one_title
path tools/o/one_title_witness.rish
tier lap
seated 20260911.114500

# lint_surface_census -- which rows of the lint table does a lap actually run?
#
# WHY IT IS ROSTERED. `context/TAME_GUIDANCE.md` carries the tree's lint surface as twenty rule rows
# under the heading **Enforced now**, and beneath it stood a hand census reading twenty rows, eight
# leaning on five rostered tools, six naming a tool no lap runs. It was exact on the morning it was
# typed, `20260911.112513`, and stale by nightfall: the guide's own history reads 11 tools named and
# 5 rostered at `050903`, 13 and 6 at `140923`, 14 and 7 the next day, as two rows seated later that
# same day each arrived with a rostered guard beside it. COUNT, NEVER NUMBER, one room over.
#
# AND THE HEADLINE NEVER SIGNALLED THE DRIFT. Rows read twenty at every one of those commits and
# reads twenty today, so the cheapest figure to spot-check agrees while the breakdown beneath it has
# stopped earning that agreement -- which is why this is a guard rather than a repaired sentence.
#
# THE WALL is `tools_missing` at zero: a row citing a tool absent from disk tells a reader to run
# something that cannot run. The RATCHET is `rows_unheld`, since a row no lap runs is often correct
# and reasoned, and a wall there would red on rows nobody may repair.
#
# 0.6s over two files and 1.5s for its 46-leg pen, measured on the seating lap, so it rides every lap.
guard lint_surface_census
path tools/l/lint_surface_census_witness.rish
tier lap
seated 20260912.002402

# port_band -- the room's port numbers, counted off the sources rather than typed in a sentence.
#
# WHY IT IS ROSTERED. REDS %700 cost a fortnight: a rostered witness read red on one cold endurance run and
# green on a re-run over an unchanged tree, and the cause was two lawful runs on eight checkouts
# reaching for one compiled-in UDP port. A port is a name on the MACHINE rather than in a checkout,
# and this pier stands eight checkouts deep whose passes overlap almost entirely.
#
# THE ROOM ALREADY HAD AN ANSWER AND WROTE IT IN PROSE. Two lock scripts serialize the runs that
# share a pair, and each spelled its room's roster in its own header. Both sentences were true when
# typed and both went stale the moment a module was repaired -- the tablecloth delivery module took
# the bind-to-zero handshake on `20260911.105415` and let 38490/38491 go, and the amphora fetcher
# let 38494 go, which `linengrow/neth_serial_core_delivery.rye` now declares. COUNT, NEVER NUMBER
# (`.claude/rules/stamp-and-name.md`): the roster is read off the declarations and the sentences
# point at the reading.
#
# WHAT IT HOLDS. `ports_outside_band` at zero, so a module reaching for 8080 at random reds on the
# lap it arrives; `lock_band_uncovered` at zero, so the next module past a lock's numeric edge is
# caught here rather than by the lock turning its own caller away. `ports_double_claimed` ratchets
# at THREE -- 38495, 38496 and 38497, six declarations across four files, each lawful for its own
# module and a collision for the pier, each repair a choice of number inside another lane.
# `lock_claims_unbound` reports the one stale sentence. 46 control legs in a pen, two mutations
# each biting their own leg.
#
# WHAT IT DOES NOT REACH. The host. Read `20260911`,
# `/proc/sys/net/ipv4/ip_local_port_range` answers `32768 60999` and `ip_local_reserved_ports` is
# EMPTY, so every number in the seated band is one the kernel may hand any client socket here. The
# band orders our own modules and buys nothing against the host; a reservation is a `nixos/` change
# and so Keaton's.
#
# WHY `tier lap`. One `git grep` over 1,972 Rye sources plus a pen of small git repositories, 8s
# measured `20260911`. Ordinary work writes this fault: a module is repaired to bind-to-zero and a
# sentence naming its old pair stays behind.

guard port_band
path tools/p/port_band_witness.rish
tier lap
seated 20260911.145000

# port_runner_lock -- which standing guards BIND a port, and which of those runs holds a lock.
#
# WHY IT IS ROSTERED, BESIDE port_band RATHER THAN INSIDE IT. port_band counts the NUMBERS our
# modules declare and names the ones two modules share, which answers *can two modules meet*. A
# fleet pass asks the other half: *does the roster RUN a binder, and is that run held apart from the
# seven other ships running the same guard on the same machine at the same second*. The shared
# number was never the whole exposure -- of the three doubles, only one side of one pair is
# rostered, so a roster pass never runs both claimants of any of them. What it does run is one
# guard, on eight checkouts, against one machine.
#
# WHAT IT HOLDS, and why two instruments rather than one. Unlocked with no SO_REUSEADDR, the kernel
# refuses the second bind: the pass reds, a re-run passes, and the bill is a re-run -- a RATCHET at
# FOUR that only falls. Unlocked WITH SO_REUSEADDR, both sockets bind and the kernel splits the
# datagrams between them with no refusal at all: every reading of it is plausible, which is what
# made REDS %700 take a fortnight -- a WALL at zero. The silent cell is empty today and the loud
# cell holds four, both in `tools/co/comlink_rehearsal_wire_witness.rish` and
# `tools/f/fora_socket_witness.rish`, each repair a choice inside another lane.
#
# WHAT THE PEN PROVES. 38 legs on real git repositories, every refusal planted and then lifted, the
# ceiling shown from both sides, and two mutations biting their own legs: dropping the bare-variable
# resolution loses both Rishi spellings, and dropping the path boundary charges `${bin}.out` as an
# execution of `${bin}`.
#
# WHY `tier lap`. 8s measured `20260911`, one pass over the roster's own `.rish` files plus a pen of
# ten small git repositories. Ordinary work writes this fault: a witness gains a second run, or a
# module gains a setsockopt, and neither hand is thinking about the other seven ships.

guard port_runner_lock
path tools/p/port_runner_lock_witness.rish
tier lap
seated 20260911.203000

# THE THIRD CELL OF A THREE-CELL GATE (`20260911.111917`). Glow's `|-` trap takes a required bound as its
# first child, so an unbounded loop is a compile-time refusal rather than a runtime surprise. That
# claim had two proofs standing beside it and a hole between them, read `20260911`: the MODULE's
# refusal is proven by `glow/rune_bounded_trap_witness.rye` -- nine claims, GREEN in 2.2s -- which
# stood outside every roster, here and at the anointed head; and the DRIVER's acceptance by
# `glow_desk_run`, whose derived selection lowers, builds and runs 347 desks (its own roster
# comment records 301 on `20260907`), two of them traps. The DRIVER's refusal
# was proven by nothing, ever. `MissingBound` appeared in exactly two files in the whole tree, both
# Rye sources.
#
# A refusal proven only in the passing direction cannot be told from a bypass, which is this tree's
# own sentence one meter over in `.claude/rules/ascii-first.md`. The control drives
# `glow/bin/glow_run` itself -- nine probes into a throwaway pen, the bare trap planted and refused
# BY NAME, the same trap lifted to `|-  32` and required to pass, and the four other refusals each
# in the compiler's own word. Both mutations bite: flipping the plant's expectation fails it, and
# changing only the expected error word from `MissingBound` to `LiteralOverflow` fails it too.
#
# It builds the driver into its OWN pen rather than over `glow/bin/glow_run`. Those bytes are
# shared by `tools/g/glow_run_worker.sh` and the batch desk runner, which take a directory lock
# against each other; a control needing that lock could never run beside a desk pass.
#
# The ninth leg records rather than praises: `|-  0` is ACCEPTED -- a loop declared with a
# circumference of zero, which parses, lowers and runs clean. Whether a language should let a
# reader write that is a question for a hand; the leg makes a change to the answer audible.
#
# `tier lap` at 4.1s, of which 2.4s is the driver build. This is the family's third firing in three
# days -- `glow_rune_alphabet` was rostered `20260909.155028` after standing GREEN and unheard since
# STOA90 -- and a lantern that fires twice becomes a loom.
#
# Paper: `active-designing/date/20260911/20260911-111917_the-loop-that-already-refused.md`
guard glow_trap_bound
path tools/g/glow_trap_bound_witness.rish
tier lap   # 4.1s; nine probes through a pen-built driver, plant refused by name and lifted
seated 20260911.111917
# THE LINE-LENGTH CENSUS, seated 20260911. `context/TAME_GUIDANCE.md`'s lint table carries the row
# *Line length <= 100 columns* under the heading **Enforced now**, and REDS %714 found that row held
# by nothing at all. The remainder it booked was one lap: measure the population, then decide
# whether the row earns a guard or a retirement.
#
# The measurement decided it. Across 4,915 tracked sources, **102,153 lines run past a hundred
# columns** -- and 54,940 of those are own-line comments while 18,495 more are a Rishi `say` or
# `assert` carrying a sentence, so 72 percent of the population is prose rather than code a reader
# follows across a terminal. RUN the guard for today's figures rather than
# reading these. The rest concentrates where width is structural: a `run [...]` array, a
# chained `.inner` access whose depth is the ladder's, a published test vector.
#
# So this guard REPORTS its census and gates one thing -- its own reach. A ratchet over a hundred
# thousand lines that rises whenever a rung is generated is a guard that reds on ordinary work, and
# four laws here already name that as how a guard gets turned off. 1.0s over 4,915 sources and 1.3s
# for its pen, so it rides every lap.
guard line_length_census
path tools/l/line_length_census_witness.rish
tier lap
seated 20260911.172644

# aether_falloff -- the falloff-field moonshot's own witness, and it publishes a NEGATIVE. Row 4 of
# the bounded-torus page proposes waking only the guards within path-distance R of a touched file,
# intensity falling with distance. `tools/fixtures/a/aether_falloff_scan.sh` measures the curve over
# real commits and the row's own falsifier fires at radius ZERO: 288 of 345 seated guards carry no
# coordinate at all, so 88.1 pct of the roster wakes before any radius is opened, and the 57 the map
# does place saturate in two hops.
#
# WHY THE ROSTER GATES THE CONTROL RATHER THAN THE CURVE. Every number the scan prints is a fact
# about the roster rather than a defect in it, and gating a floor share would red the tree for
# carrying guards nobody has mapped -- a backlog, priced already by
# `tools/fixtures/s/standing_equipment_scope_rank.sh` at `absent_cost_share=0.920`. What must never
# go quiet is the instrument: a scan answering "no gradient" for every input would have produced
# this finding on a tree that had a perfect one. So the control proves the negative from BOTH sides,
# on real git repositories in a pen -- a roster with real locality read as having it, saturation at
# two; the same roster with no map read as having none and no saturation at all.
#
# The anchor rule is the scan's one piece of judgment and it is mutated: breaking the glob-truncation
# line moves `g_beta`'s mean distance from 0.00 to 1.00 on the same pen, the plant proven landed by
# `cmp` before the reading is believed (%519).
#
# It spends no seconds counting seconds. The ranking beside it prices guards in wall time, and two
# meters over one population is the waste this tree names.
#
# Paper: `active-designing/date/20260911/20260911-195059_the-field-with-no-distance.md`
guard aether_falloff
path tools/a/aether_falloff_witness.rish
tier lap   # 3s; 38 behaviors in a pen, eleven refusals and one mutation
seated 20260911.195059

# WHY THIS GUARD. `context/TAME_GUIDANCE.md`'s lint table holds the tree's whole textual lint
# surface, and REDS %714 found twenty rows under one heading keeping four different promises. That
# repair named the split in a paragraph, and the paragraph went stale inside a day by the size of
# its own repair -- it read "eight lean on five tools a roster pass runs every lap" while the round
# writing it rostered two more of the rows it counted. A count typed into prose is held by nobody.
#
# So the split moved into a Runs column, one word per row, and this guard holds each word to THIS
# FILE's own reading: `every lap` for a named tool at `tier lap`, `on cadence` at `tier cadence`,
# `by hand` for a row no roster carries. Three walls at zero -- a word that disagrees, a row with no
# word, and a row naming no instrument at all.
#
# A row is judged by what it NAMES rather than by what a reader could work out. Three rows read the
# bans scan alone while `tame_style_check` drives it every lap, so their own text could not say what
# ran them; naming the driver in the row is the repair, where a private driver map inside the scan
# would put the relationship in a second place two files could disagree about.
#
# Paper: `active-designing/date/20260912/20260912-011500_the-column-the-paragraph-could-not-keep.md`
guard lint_table_runs
path tools/l/lint_table_runs_witness.rish
tier lap   # 1s; 32 behaviors in a pen, nine refusals and two mutations
seated 20260912.011500
# ignored_walk -- a guard that finds its population by WALKING reads whatever stands under the root,
# and this tree's own laws send a lap to work in rooms git ignores: `.lap/` for a lap's scratch,
# `session-output/` for a transcript, `loops/` for launcher state, and a `git worktree` parked under
# any of them (`read-scope.md`). REDS %722 caught two such guards by living through them -- a
# worktree at `.lap/verify` took `rye_harness_roster` from `unresolved=1` to `2`, one past a ceiling
# with no slack, naming ONE file counted twice; a copy of `mantra/` under `.lap/probe/` took
# `copy_sameness` from `ok` to `drift`. Both readings were true about that disk and false about the
# tree, which is the direction a guard can least afford: a red on every ship for a file no clone
# holds is how a gate gets turned off. That row repaired the two that fired and named the rest a lap.
#
# THIS READS THE CLASS, so the walk written tomorrow is counted the day it lands rather than probed
# by hand some later week. Its own corpus comes from `git ls-files`, for exactly that reason.
#
# TWO NUMBERS BECAUSE THEY ASK TWO THINGS. `unfiltered` -- a tree walk in a file that never calls
# `git check-ignore` -- is a property of TRACKED BYTES, identical on every ship, so a ceiling over it
# refuses the same work everywhere. `exposed` counts the walks whose root holds an ignored path on
# THIS disk, where a peer with an empty `.lap/` answers lower for no tree reason, so it is printed by
# name and gated by nothing.
#
# THE PROXY AND THE INERT MEMBERS, BOTH NAMED. A static reading says a walk CAN reach an ignored
# path, never that one would MOVE a published number; two of the ten probed by hand at %722 did.
# And seven `.` walks sit in files that `cd` to a variable: three land on the repository root, four
# land in a pen. Reclassifying all seven drops three real findings, and spelling `ROOT` as the
# root-ish name is the same claim-about-remembered-names this reading exists to retire -- so all
# seven stay counted and all seven are printed under `cd_relocated`, four of them inert members the
# ceiling can never bring to zero. Sixty-four variable roots are a named gap rather than a guess.
#
# The repair that rode with it: `tools/fixtures/t/tools_py_ban_scan.sh` pruned `tools/.cache` and
# `tools/.build` by name, having already paid for this fault once, and git's own rules disown SIX
# paths under `tools/`. Proven on metal -- a file at `tools/bin/probe.py`, a room `.gitignore` names
# on its own line, false-REDs the elder spelling and reads clean under the cure. Its git-free
# fallback stays for the pen it is built to run in, and prints which branch ran.
#
# Paper: `active-designing/date/20260912/20260912-003102_the-walk-and-the-listing.md`
guard ignored_walk
path tools/i/ignored_walk_witness.rish
tier lap   # 8s; 32 behaviors in a pen, every refusal lifted and two mutations bitten
seated 20260912.003102
# capability_lattice -- the polar moonshot's own witness, and it publishes a REFUSAL. Row 2 of the
# bounded-torus page reads Caravan's process graph in polar coordinates, radius standing for
# privilege, and rests on one written assumption: privilege in Caravan is already ordered.
# `caravan/capabilities.rye` declares five rights as bits of a `u8` mask and compares them in
# `Rights.contains(have, need)`, returning `(have & need) == need` -- subset containment, which is
# the Boolean lattice rather than a line. `tools/fixtures/c/capability_lattice_scan.sh` derives the
# rights from their own declarations, reads the ten masks the `caravan/` room actually constructs,
# and finds 32 of their 45 unordered pairs incomparable. The row's own falsifier fires on seated
# code.
#
# THE HALF WORTH KEEPING IS THE SIGN. A radius-only checker admits 48 of the seated conferrals the
# lattice refuses and refuses none it allows, because popcount is monotone under subset. So every
# error a ring model can make is an OVER-GRANT -- a supervisor handing a dependent a right nobody
# conferred, silently, since from inside the ring model the hop is legal. That is a reason to
# decline the coordinate rather than to refine it.
#
# WHY THE ROSTER GATES THE CONTROL RATHER THAN THE COUNTS. Every figure the scan prints is a fact
# about a capability model that is CORRECT, so a gate on the incomparable count would red the tree
# for holding a good one. What must never go quiet is the instrument: a scan answering `lattice` for
# every input would have produced this finding on a module whose privileges genuinely sat on a line.
# So the control proves the verdict from BOTH sides -- a chain pen reads `line` with the two models
# agreeing pair for pair, and one incomparable mask flips it on a single changed line.
#
# The full-lattice figures are closed forms rather than a 4^n walk, so the reading costs n^2 and
# stays available at any mask width. A closed form is a claim, so the control enumerates all 4^n
# ordered pairs at three and five rights and compares every figure, including the under-admission
# zero the scan asserts rather than measures. Three mutations bite: the declaration-line filter
# inverted, the verdict hard-wired, and containment read backwards. Each plant is proven landed by
# `cmp` before its reading is believed (%519).
#
# A DECLARATION IS NOT A CONSTRUCTION, so the mask harvest reads past `pub const right_x: u8 = ...`
# lines. Measured on the seating lap the filter changes nothing here -- every right Caravan declares
# is also used as a mask somewhere, `right_net` at `caravan/capabilities.rye:284` -- and what it
# buys is a right declared tomorrow and left unused, which would otherwise seat a privilege nobody
# built. The pen's declaration sits inside its room, as Caravan's does, or the filter goes
# unexercised on a geometry the tree does not have.
#
# The rights bound is DERIVED from the declared mask type rather than typed here: `u8` caps Caravan
# at eight rights and it holds five, so `rights_headroom=3` and a ninth right is a type change in a
# published struct.
#
# Paper: `active-designing/date/20260912/20260912-021711_the-privilege-that-was-never-a-line.md`
guard capability_lattice
path tools/c/capability_lattice_witness.rish
tier lap   # 2s; 59 behaviors in a pen, six refusals each lifted, three mutations
seated 20260912.021711
# WHERE A GUARD'S BUILD OUTPUT LANDS -- the question nobody had asked. 60 of 353 rostered guards
# pass `-femit-bin=` to `rye build` across 125 emit sites, and 110 of those name a fixed path in the
# tree rather than a directory the run made for itself. Of 51 distinct fixed paths, 6 are written by
# two or more DISTINCT guards, three of them by 18 apiece.
#
# THE COST IS MEASURED RATHER THAN ARGUED. `rye build` writes THROUGH its output -- the inode
# survives a rebuild -- so a concurrent reader executes a partial binary. Over 400 executions on
# this pier: a reader whose own path nobody writes fails 0, one other build writing it fails 335.
#
# TWO LOCKS STAND AND NEITHER IS ON THIS AXIS. This runner refuses a second roster pass; `rye build`
# holds `.rye-build.lock` so two builds cannot stage one shadow (%281). Pass against pass, build
# against build -- and nothing serializes a build against a RUN, which is the only pairing that
# needs it. So the single-writer reading, with no build race at all, is the worst of the three.
#
# TWO WALLS AT ZERO -- a build output the repository carries, and one git would show, which would
# move this runner's own tree digest. TWO RATCHETS under ceilings that only fall, so a NEW guard
# building into the tree reds on the lap it lands; the escape is `d=$(mktemp -d)`.
#
# Paper: `active-designing/date/20260912/20260912-020039_the-binary-two-guards-are-writing.md`
guard build_target
path tools/b/build_target_witness.rish
tier lap   # 2s; 33 legs in a pen on real git repositories, two mutations bitten
seated 20260912.020039

# torus_fold -- the storage moonshot's own witness, and it publishes a DECLINE. Row 5 of the
# bounded-torus moonshots proposes folding Tablecloth's hash space onto a 2-torus so that names
# near each other in the fold sit near each other in storage, with clustering as its falsifier.
# `tools/fixtures/t/torus_fold_scan.sh` reads both halves over the tree's own bytes: it hashes a
# strided sample of tracked files with SHA3-512, reads two digest bytes as coordinates on a
# 256 x 256 torus, and reports a chi-squared over an adaptive grid beside the toroidal pair
# distance of three populations -- random, same-directory, and a file against itself with one byte
# changed -- each against the closed form 128.
#
# THE FALSIFIER FAILS AND THE ROW DIES ANYWAY, which is why this rung exists. A cryptographic hash
# is built to avalanche, so the fold is even by construction -- and that same avalanche is what
# leaves fold-adjacency carrying no relation. Two documents differing in one byte land as far apart
# as two strangers. Evenness and adjacency are one property read twice, so row 5's two halves
# cannot both be answered the way it wants.
#
# WHAT IS GATED. The control, and the live reading's SHAPE -- never the live verdict. A three-sigma
# test over six populations refuses roughly one run in sixty by construction, so a gate on the
# verdict would red this tree at the rate its own instrument publishes.
#
# Paper: `active-designing/date/20260912/20260912-042053_the-fold-that-had-nothing-to-hold.md`
# awk_lcg_exact -- a pen's generator makes TWO promises and this tree had a word for one. awk
# carries every number as an IEEE-754 double, exact to 2^53, so a linear congruential generator's
# largest intermediate -- multiplier times modulus-minus-one -- decides whether the stream that
# comes out is the stream the line spells. `torus_fold_control.sh` spelled a product of 2.37e18,
# 263x past the range: measured on metal 20260916, gawk 5.4.1, the rounded map has a tail of 3,253
# draws and a cycle of 10,466 -- 13,719 states against the exact 2^31 -- and the consumed byte
# differed from the exact stream in 4,079 of 4,096 draws. That pen drew 2,048 and stood inside the
# tail, which is why nothing had ever reddened.
#
# DETERMINISM WAS NEVER THE FAULT, and the guard says so rather than scolding. IEEE-754 specifies
# multiply, add and fmod exactly, so the rounded stream is reproducible on every awk carrying
# doubles -- the comment beside that line was true. Reproducibility is two runs agreeing and passes
# forever; VARIETY wants a count of states rather than a comparison of runs, and only the second
# reading catches this.
#
# THREE CLASSES, ONE GATED. A FED-BACK site, where the variable assigned is the variable
# multiplied, is bounded by its own modulus, so the largest intermediate follows from the line
# alone and no judgment enters -- held at ZERO past 2^53/(modulus-1), a WALL rather than a ratchet
# since the class was two sites wide and both were repaired on the seating lap. An INDEX HASH is
# bounded by the range of its input, which a scanner cannot know: 17 stand in the tree, reported
# and gated by nothing, and the number is printed so a reader can tell an empty blind spot from a
# large one. The tightest EXACT site, key_trade_control.sh, stands at 1.260x of headroom -- named
# in the scan's header so a zero cannot read as slack.
guard awk_lcg_exact
path tools/a/awk_lcg_exact_witness.rish
tier lap   # 6s; 57 behaviors in a pen, three mutations bitten, the control proven outside its own population
seated 20260916.184200

guard torus_fold
path tools/t/torus_fold_witness.rish
tier lap   # 5s; 28 behaviors in a pen, both verdicts reachable, three mutations bitten
seated 20260912.042053

# torus_place -- the second reading of the same moonshot row, and it answers the question the first
# one names as untouched: whether a torus is a good PLACEMENT scheme. Every property row 5 had left
# is compared against a RING of the same cell count, because a property a line also has is not a
# property a second axis supplies.
#
# THREE ANSWERS, AND THE SHAPE LOSES ALL THREE. Evenness belongs to SHA3-512: two folds reading
# different bits of one digest read equally even and agree on where to put a name at the chance
# rate. Uniformity belongs to vertex-transitivity, which a ring has too. And SPREAD -- the shortest
# contiguous run of storage indices whose loss destroys every copy of some cell -- comes out
# AGAINST the grid: at a 64-cell grid the torus survives 17, a ring taking its nearest four
# survives 5, and a ring free to choose offsets of one and two rows survives 33. All three match
# their closed forms of 2g+1, 5 and 4g+1. What a torus supplies is a FIXED offset set handed down
# by its shape; declustering needs no torus.
#
# WHAT IS GATED. The control, and the live reading's ARITHMETIC -- the run-kill lengths against
# their closed forms, and the wide ring out-surviving the torus. Those cannot move under this
# tree. The live EVENNESS verdict is reported rather than gated: a p=0.001 test taken twice refuses
# about one run in five hundred on a digest that is behaving correctly.
#
# A FOURTH RULE ADDED 20260918: `evenspread` spaces its five copies evenly around the ring
# (round(i*C/5), i=0..4) and survives 52 against the free ring's 33 -- arithmetic beats the search
# opened a step later. Closed form C-ceil(C/5)+1.
#
# Paper: `active-designing/date/20260915/20260915-175000_the-axis-that-carried-nothing.md` --
#        `active-designing/20260918-043308_evenly-spaced-offsets-close-opening-one.md`
guard torus_place
path tools/t/torus_place_witness.rish
tier lap   # 4s; 31 behaviors in a pen, both verdicts reachable, three mutations bitten
seated 20260915.175000

# torus_offset_search -- round two's proposal 1, and the answer is the family's own maximum. Round
# one's reading 3 compared exactly two hand-picked members of one offset family, {d,-d,2d,-2d} mod
# C, at d=1 (run-kill 5) and d=g (run-kill 33). Sweeping every d from 1 to C/2 at the same founding
# grid finds d=13, run-kill 52 -- which equals C - ceil(C/5) + 1, the theoretical ceiling for five
# points splitting a ring as evenly as the integers allow. The search did not find a better guess;
# it found the criterion's own optimum. A second grid (g=32) repeats the same shape.
#
# WHAT IS GATED. Both elder closed forms (5 and 4g+1) reproduce under this scan's own algorithm;
# the search never reads below the hand-picked point it already knows; an aliasing grid (below the
# five-distinct-copies precondition) refuses rather than guessing. REPORTED: whether the found
# optimum beats round one's 33 -- the falsifier itself, a fact about geometry no lap repairs.
#
# Paper: `active-designing/20260918-002526_round-two-three-moonshots-grounded-in-round-ones-refusals.md`
guard torus_offset_search
path tools/t/torus_offset_search_witness.rish
tier lap   # <1s; 13 behaviors, two grids, one mutation bitten, no digest population needed
seated 20260918.005052

# torus_offset_generic_search -- the open door torus_offset_search's own essay named: does a
# FULLY GENERIC four-offset set (no +-pairing, no shape borrowed from a torus/ring's own axis)
# beat the constrained {d,-d,2d,-2d} family's own ceiling? Enumerating every 4-subset of the
# offset range directly, at C=9 (hand-checkable) and C=64 (round one and two's own founding grid),
# both reach the theoretical ceiling exactly -- 52 at C=64, matching the constrained family's own
# number bit for bit. The conjecture the elder essay left open now stands checked at two grids.
#
# WHAT IS GATED. The exhaustive best never exceeds the theoretical ceiling (a true arithmetic
# upper bound on any five-point circular split), proven load-bearing by a planted mutation that
# lies about the ceiling and is caught; a too-small grid and an oversized grid (45 billion
# candidates) each refuse by name rather than guess or hang. REPORTED: whether the generic best
# reaches the ceiling exactly -- the conjecture this rung exists to check.
#
# Paper: `active-designing/date/20260918/20260918-040155_the-generic-search-found-the-same-ceiling.md`
guard torus_offset_generic_search
path tools/t/torus_offset_generic_search_witness.rish
tier lap   # ~3s at grid 8 (595,665 candidates); 16 behaviors, two grids, one mutation bitten
seated 20260918.040155

# locality_key -- the door BOTH torus readings left open, and it turns out to be a price rather than
# a yes. Each of those closes names the same remaining idea in the same words: a key built to carry
# locality on purpose. This guard opens it and prices the crudest member of that family -- the file's
# own leading two bytes -- because the trade belongs to the FAMILY, and the crudest member shows it
# at full size with no parameter a reader must take on trust.
#
# WHAT IT BUYS, AND WHAT IT SELLS, read over the tree's own tracked bytes at a 64-cell grid. It BUYS
# locality: the prefix key survives a one-byte edit at offset 32 for every probe while the digest
# survives none, and two files from one room sit 0.410 cells apart against the 15.693 the digest
# gives and the 16.000 two uniform keys give. It SELLS evenness: chi-squared 13134.05 against a
# p=0.001 critical 103.51, on a population where the digest reads 79.05. And it SELLS the room with
# it: an observer holding keys alone places 0.5353 of files in the right room against a no-key
# plurality baseline of 0.3471 over 44 rooms.
#
# THE FINDING. Locality, evenness and confidentiality are one quantity read three ways -- a key that
# lets a reader find neighbours groups documents, and a key that groups documents tells an observer
# which group each is in. The control proves the last two are separable rather than one restated, by
# planting prefixes clustered exactly as in the real case with rooms assigned round robin. So the
# avalanche both torus readings ran into is what BUYS the other two properties.
#
# WHAT IS GATED. The control, and the live reading's ARITHMETIC -- no digest survives a deep edit,
# every prefix does, and both recovery figures lie between their own no-key baseline and one. The
# three TRADE verdicts and every chi-squared and mean are REPORTED rather than gated: the statistic
# refuses at its own stated error rate, and the means move with the tree's contents.
#
# Paper: `active-designing/date/20260915/20260915-181000_the-key-that-carries-locality.md`
guard locality_key
path tools/l/locality_key_witness.rish
tier lap   # 43 behaviors in a pen, both verdicts reachable, four mutations bitten
seated 20260915.181000

# rye_module_door -- Gauge sets its dial by reader and the dial runs through the code: METER beside
# a bound, DOOR at the head of a module where somebody arriving cold deserves a plain sentence about
# what the thing is for. Zig, and Rye after it, writes that setting into the grammar -- `//!` is a
# module doc comment the compiler accepts only at the top of a file -- so the door is a POSITION the
# language reserves rather than a habit a reader infers.
#
# WHAT WAS UNREAD. `tools/fixtures/c/comment_dial_scan.sh` has counted the form since `20260824` and
# gates nothing, deliberately: its header says a measurement is finished when its question is
# answered. So the reading stood for eighteen days and no lap met it.
#
# THE SILENT MODULES WERE NOT DOORLESS, which is the finding. Of the ten that opened outside the
# form, eight carried real Door prose in the ordinary `//` block -- `comlink/query_wire_retention_cost.rye`
# opens on a WHY paragraph naming the design question it settles; `linengrow/retting_timer.rye` names
# its two commands and the append-only log behind them -- so a reader met a door and every tool read
# a wall. One more, `rye/tests/version_test.rye`, carried its prose INSIDE `main`, where the sentence
# explains the statement below it and answers nobody arriving at the file. The reading counts PATHS,
# so the three `font8x8_data.rye` entries are one file reached by two symlinks -- right for a meter
# asking whether a reader arriving at a path meets a door. All ten paths stand repaired, nine files,
# and the four that build as programs were rebuilt on metal through `rye/bin/rye build` to prove the
# compiler accepts the form at the head of each.
#
# A WALL RATHER THAN A RATCHET, on the two-rooms doorway guard's own line: nothing structural holds
# a living module's head silent, so a floor above zero would be slack rather than a fact. Two
# exemptions stand, each named and each COUNTED -- dated testimony by the mark law, and a `fixtures/`
# module because a planted control must stay free to carry the fault it plants. A reach floor sits
# under both, since a gate held at zero cannot tell a clean collection from a dead instrument.
#
# WHAT IT CANNOT SAY. Whether the sentence behind the door welcomes anybody. Position is checkable;
# the welcome is graded by `sh tools/fixtures/q/qa_report_card.sh`.
#
# Paper: `active-designing/date/20260912/20260912-041500_the-door-the-grammar-reserves.md`
guard rye_module_door
path tools/r/rye_module_door_witness.rish
tier lap   # 5s; 45 behaviors in a pen, every refusal lifted and two mutations bitten
seated 20260912.041500

# fleet_moved_proof -- the send fetch can move xy/main after this lap packages its work. The helper
# maps the lap and upstream change sets through the same static watch rows the roster runner reads,
# then chooses scoped proof only when those closures are readable, known, and disjoint. Its control
# proves the one scoped decision and all four conservative full-roster decisions in a real Git pen.
guard fleet_moved_proof
path tools/f/fleet_moved_proof_witness.rish
tier lap
seated 20260914.015559

guard rye_compiled_reach
path tools/r/rye_compiled_reach_witness.rish
tier lap   # 5s; 31 behaviors in a pen, six refusal classes bitten
seated 20260914.025746

# THE GUARDS THAT SHARE A BUILD PATH, COUNTED (REDS %700). A witness emitting its binary to a
# literal path in the tree shares that name with every run in the same checkout: one run executes
# the binary while another's linker opens the same path, and the linker refuses with ETXTBSY. The
# guard then answers differently on bytes nobody changed. The reading resolves `let bin = "..."`
# to its value, which is the whole reason it is a scan rather than a grep -- the dominant spelling
# is `femit-bin=${bin}`, and two hand counts that stopped at the dollar sign read 304 and then 51
# where the tree holds 1,853 sites across 1,224 files. A ratchet that only falls; 3s measured.
# Lowered to 1,813 then 1,574 on `20260915` as the lattice forty and the lotus 239 took pens.
guard shared_build_path
path tools/s/shared_build_path_witness.rish
tier lap
seated 20260915.185705

# THE PEN A RUNNER NEVER ENTERED (REDS %729). A control built each case with an unchecked `cd` into
# a directory `mktemp -d` had failed to make, so the case bodies ran with the working directory
# still at the repository root: a `printf` redirect truncated the tracked front door, and three
# `git config` calls rewrote this checkout's identity. Twelve commits on main carry that pen
# identity, unsigned. `shared_pen` asks whether a pen is SHARED across eight checkouts; nothing
# asked whether it was ENTERED, and contention and containment are two questions. A WALL at zero --
# the five sites standing at seating were repaired in the same commit -- with `unchecked_mktemp`
# reported beside it and gated by nothing, since an empty pen variable is a different cure. A `#`
# line and a heredoc body are read past, the first learned by first residency when the scan counted
# its own header. 30s: 449 runners, 1,140 pen sites, and a 45-leg control building eighteen real
# repositories in a pen.
guard pen_entry
path tools/p/pen_entry_witness.rish
tier lap
seated 20260915.191655

# THE THIRD QUESTION ABOUT ONE PEN (REDS %745). `shared_pen` reads whether a pen is CONTENDED and
# `pen_entry` whether it is ENTERED; nothing read whether it is RELEASED, and one rostered witness
# put 1,312 pens and 32G into the shared /tmp over nine days until the pier read 990M free. The two
# standing guards also read ONE LANGUAGE -- a shell name or a shell shebang -- and the leaker,
# `tools/am/amphora_mark_wreck_witness.rish`, is Rishi and carries neither, so it stood outside the
# family's whole population. This opens both: 461 shell runners and 338 Rishi sources carrying a
# pen. `never_removed` is a RATCHET at 16 rather than a wall, since fourteen of the sixteen are
# amphora and caravan witnesses whose repair belongs to their own lanes. `unreleased_on_refusal`
# (336) is reported and gated by nothing: in shell the cure is one EXIT trap. The RISHI half of
# that sentence is closed -- `make-pen` below seats a runtime-owned pen, and `runtime_pens` counts
# the sources that have taken it, in NEITHER class, so a conversion moves a file between readings
# rather than out of the census. 22s: one `git grep -l` narrows both populations before a file is
# opened, and a 46-leg control builds real repositories in a pen.
guard pen_release
path tools/p/pen_release_witness.rish
tier lap
seated 20260915.230045

# rishi_make_pen -- the language question that half of %745 was waiting on, answered. `make-pen
# <label>` is a Rishi expression builtin: the runtime composes the path under TMPDIR, creates the
# directory, registers it in the CleanupRegistry that `acquire-lock` already used, and releases it
# RECURSIVELY after a normal return, a failing assert, an `exit`, HUP, INT, or TERM. The elder
# claim on three living pages -- *Rishi has no trap at all* -- was false when it was written: the
# registry has stood since the lock, and what it lacked was a pen kind.
# THE BOUNDARY IS THE SAFETY ARGUMENT. A recursive removal is the operation worth being frightened
# of, so a caller never supplies a path: it supplies a LABEL of letters, digits, `_` and `-`,
# bounded at 64 bytes, and a label that cannot spell `/`, `.` or a null byte cannot leave one path
# component. The release re-reads the runtime's own `rishi-pen-` prefix off the basename before it
# removes anything, and refuses a path that stopped being a directory out loud.
# 29 legs on metal, every wall shown from both sides by INPUT rather than by a mutation switch --
# the subject is a compiled runtime, so flipping a predicate costs a rebuild a rostered guard
# cannot afford. 64-byte label accepted, 65 refused; four pens accepted, the fifth refused at
# max_cleanup_paths; a pen released empty and, separately, holding a nested tree.
guard rishi_make_pen
path tools/r/rishi_make_pen_witness.rish
tier lap
seated 20260916.005245

# bearing_quorum -- the last measurable row of the bounded-torus page, and it splits in two. Row 8
# of active-designing/date/20260910/20260910-060204_the-bounded-torus-moonshots.md says a node announcing along a
# meridian and confirming along a parallel makes "consensus routing" cost the perimeter rather than
# the area. `tools/fixtures/b/bearing_quorum_scan.sh` builds a g x g torus in arithmetic at four
# grids and reads all three halves. THE COST HALF STANDS: an announce plus a query is 2g-2 messages
# against a dissemination floor of N-1, the ratio falls 0.400 to 0.061 across g=4..32, and the
# exponent over the two largest grids reads 0.5237. THE DELIVERY HALF DOES NOT: one announce reaches
# g of N, a share of 0.031 at g=32, while the intersection of an announce row with a query column
# reads exactly ONE for all 1,360 cases -- so the scheme is a RENDEZVOUS, and it costs the square
# root of N because its guarantee is intersection rather than coverage. Row 8 priced a quorum and
# described a broadcast. AND THE WRAP IS WORTH EXACTLY ONE CUT, the one finding belonging to the
# torus rather than the grid: a cycle loses coverage in zero cut positions and answers nobody
# silently, a path of the same length loses 1,300 and answers 372,368 queriers silently, and two
# cuts split the cycle in all 650 cases. Row 8's own falsifier fires on the path and is extinguished
# by the wrap. Everything is GATED because the scan opens no file, so no figure can move under this
# tree; the finite-size exponent is fitted on the tail and the all-grid fit is printed beside it,
# since 2g-2 against g*g approaches 0.5 from above. `tier lap`: scan 1s, control 4s, with five
# mutations bitten -- the one that matters most makes the querier read its own row, since two
# perpendicular lines meeting once is exactly the fact a scan can assert without checking.
guard bearing_quorum
path tools/b/bearing_quorum_witness.rish
tier lap
seated 20260915.211724

# THE BOUNDED CAPTURE FIELDS, AND THE CLIFF THEY EXIST FOR (REDS %740). Composing a WHOLE capture
# into an assert message drops it in silence past 4,096 bytes -- the composed string emits the
# literal placeholder rather than the value, a truncation, or a refusal, so the reason is lost
# exactly when there is most of it. `out_brief` and `err_brief` are the same captures bounded to
# 512 bytes and marked where they cut. The witness asserts the cliff too, since a tree that forgot
# it would go back to composing whole captures. 13 legs; 2s measured.
guard rishi_brief
path tools/r/rishi_brief_witness.rish
tier lap
seated 20260915.212007

# whitepaper_definitions -- row 11 of the bounded-torus page is the only one of the twelve whose
# subject is the other eleven: "one paper binds the three definitions the other eleven rows lean
# on." Nine rows now carry landed readings, so its assumption and its falsifier are both checkable,
# and this rung checks them by RUNNING the sibling instruments and reading their emitted keys rather
# than by reading the errata that describe them -- so the answer moves when an instrument moves.
#
# ONE DEFINITION OF THREE STANDS. The bound-when-space-wraps definition holds on bearing_quorum's
# cost_half and wrap_worth_one_cut. The radius-when-privilege-is-distance definition is refuted on
# three keys across two instruments -- capability_lattice reads 32 incomparable unordered pairs and
# 48 over-admissions where a line reads zero of each, and aether_falloff reads a mapped saturation
# radius of 2 where a gradient needs three. The topos definition is left UNRUN, since its instrument
# is the cadence-tier cyclic_witness, and the scan proves the answer cannot turn on it by
# recomputing with that definition forced to stand.
#
# THE SURVIVORS NEED A RING. Each definition states how many independent cycles the space must
# carry; the maximum among the supported ones reads 1 against a page titled for a torus.
#
# AND THE FALSIFIER CANNOT FIRE. Each definition bounds the space from below, and lower bounds
# compose by maximum, so the scan enumerates all 27 verdict assignments and counts the conflicting
# ones under both readings of that hinge: 0 under `atleast` and 5 under `exact`. The hinge is an
# inference rather than an observation and both answers are printed, so a reader who disagrees has
# one number to move.
#
# EVERYTHING IS GATED: every figure is arithmetic over the assignment space or a key from a sibling
# instrument that is itself gated, and the thresholds are the values a definition HOLDING would
# produce rather than counts of this tree's population that ordinary work moves.
#
# `tier cadence` rather than lap: the scan runs four sibling scans and takes 11s, of which
# torus_place is 8s, and the control runs the scan about twenty times for 19s more.
guard whitepaper_definitions
path tools/w/whitepaper_definitions_witness.rish
tier cadence
seated 20260915.212827
# workload_trial -- row 12 of the bounded-torus page proposes a METHOD rather than an idea: "the
# smallest trial that touches metal goes first, and its result funds the ordering of everything
# after it," falsified when "the trial's number sits inside the run-to-run spread of the baseline."
# Ten rows now carry landed errata, so the method can be held against what the ordering actually
# did. A falsifier of that shape needs three things, and they fail separately.
#
# THE METAL CLAUSE FIRED, AND NO TRIAL RAN IT. Every tools path named inside an erratum line is
# classified by what its own EXECUTABLE lines open, one delegation hop followed, since every
# witness in this family hands its work to a control and reads arithmetic when read alone. Read
# 20260915 over ten errata naming nine instruments: four metal, two bytes, two arithmetic, one
# host, none absent, seven re-ranks or re-aims standing. Every one of the four metal readings
# produced a REFUSAL -- a compiler declining a trap with no bound, a digest avalanching two
# near-identical files apart, a tick loop proving three closed forms -- where row 12 proposes a
# comparison of DEGREE.
#
# AND DEGREE HAS NO OPERAND. Across all twelve row bodies the count of numeric effect claims reads
# ZERO; three rows say "saving" and none says how much. So the falsifier cannot fire in either
# direction, which is row 11's finding arriving along a second road.
#
# AND THE PIER PUTS A FLOOR UNDER ANY SINGLE TRIAL. A deterministic CPU-bound workload measuring a
# change of exactly zero read a baseline spread of 16.7 to 54.8 percent of its median across eight
# readings at load average 8 to 13, with 40 to 100 percent of zero-change trials landing inside the
# band. Any improvement under roughly a fifth is invisible to one trial here.
#
# THE SCAN READS PAST ITS OWN PATH, because its classifier holds every marker it looks for as a
# grep argument -- it classified itself `host` on the lap its own erratum landed.
#
# WHAT IS GATED IS SHORT ON PURPOSE. The page's shape is held by tracked bytes and is walled: twelve
# rows, zero effect sizes, every named instrument present. The funding distribution is reported and
# never gated, since it rightly moves as rows land. Every timing figure is FREE -- eight ships share
# this pier -- so only the arithmetic's self-consistency is asserted.
#
# `tier cadence` rather than lap: the live reading takes about 14s of deliberate wall time and the
# control runs the scan around twenty times beside it.
guard workload_trial
path tools/w/workload_trial_witness.rish
tier cadence
seated 20260915.221500

# ladder_order -- the twelve rows of the bounded-torus page were ranked once, on the day they were
# written, by what a lane could start on this pier that month. Ten errata have landed since, each
# reporting a measurement on metal, and several recommend a re-rank or a re-aim in their own words.
# The ranking table still reads as first written, correctly, since dated testimony keeps every word
# -- so the order the twelve measurements support stands scattered across ten separate errata.
#
# THIS RUNG ASKED WHETHER THAT ORDER EXISTS BEFORE WRITING IT DOWN, and it does not. Nine rows
# speak (row 5 carries two clauses and the later one wins), six recommend anything at all, and only
# FOUR name a seat -- two of them the same seat, since rows 2 and 4 were both sent last for the
# same stated cause. Four named positions of twelve, two collisions, `permutation=no`.
#
# THE MECHANISM: an erratum speaks in a CLASS and the table speaks in a POSITION. Two rows may both
# be demoted; only one may be twelfth. Transcribing a class into a table demands a tiebreak that
# would be invented rather than measured, which is what this reading exists to refuse.
#
# WHAT STANDS IS A PARTITION -- 3 unread, 3 reported, 1 re-aimed, 2 seated, 2 demoted, 1 superseded
# -- whose six counts are gated against the roster, since disjointness is the one property making
# the weaker statement true.
#
# WHAT IS GATED: the roster against the ranking table, parse failures at zero, and the class total
# against the roster. The COLLISION count is reported rather than walled -- it is a reading of a
# dated page, and an erratum landing tomorrow may move it honestly.
#
# `tier lap`: the scan opens one page and the control plants pages in a pen it releases on both
# exits, which the control proves rather than trusting the trap (REDS %745 read on this instrument).
guard ladder_order
path tools/l/ladder_order_witness.rish
tier lap
seated 20260916.003332

# claim_preserve_control -- `claim_preserve` has stood since 20260724 as the safety that makes a
# broad register pass honest: it extracts claim tokens before and after, counts eighteen modal
# terms, and refuses any drift. A hand invokes it with CLAIM_PRESERVE_FILES, so it sits on no
# roster, and until 20260915 it had no control -- every claim it made about itself was a claim
# about code nobody exercised. The control builds its own pen and needs no environment, so the
# reading a roster CAN carry is this one.
#
# WHAT THE READING IS. The modality diff carries two populations and named neither. OBLIGATION
# terms -- must, shall, should, may, require, recommend, propose, seat, hold, parked -- are what
# the tree owes, and moving one is the drift this guard was seated for. REGISTER terms -- never,
# none, always, every, all -- are the words the register law asks a lap to RECAST, so a lawful
# sweep moves them by doing as it is told. The scan sorts each drifted term and prints
# obligation_drift and register_drift beside the failure. The REFUSAL is unchanged: any drift
# still exits 1. Whether a register-only drift may ever pass is a door for Keaton.
#
# MEASURED ON THIS TREE'S OWN HISTORY, and re-read on metal 20260916.003300: commit f258e5f58
# restated fourteen negatives in skate/README.md under that law and moved `may` 5 -> 4 and
# `all` 10 -> 12, the second purely from the restatement idiom "A, B and C all stay outside".
#
# THE REGISTER CLASS IS DERIVED from tools/fixtures/p/prose_register_scan.sh's own `neg`
# vocabulary, so the word a sweep is asked to recast and the word this guard calls register are
# one list; a term added there is classified here on the next run.
#
# 15 legs, 3 mutations bitten, 3s. tier lap.
guard claim_preserve_control
path tools/cl/claim_preserve_control_witness.rish
tier lap
seated 20260916.004500

# aurora_placement -- first witness for the small-torus-of-cores moonshot, row 7 of
# active-designing/date/20260910/20260910-060204_the-bounded-torus-moonshots.md, and the last of the twelve
# rows that carried no erratum. The row proposes Aurora on a 4-core or 16-core network-on-chip
# "whose topology Grain knows to be a torus", with a placement map as its first witness and this
# falsifier: the reachable boards are mesh rather than torus.
#
# THE GEOMETRY HALF IS ARITHMETIC AND OPENS NO FILE. At the 4-core grid the row names FIRST, a
# 2 x 2 torus IS a 2 x 2 mesh: i+1 mod 2 and i-1 mod 2 name the same node, so every wrap link
# duplicates a link the mesh already has. Equal diameter 2, equal distinct link count 4, equal
# mean pair distance 1.3333 -- one graph under two names, so a mesh board takes nothing away and
# the row's own falsifier cannot fire where the row starts. At 16 cores it fires with something
# behind it: diameter 6 to 4, mean 2.6667 to 2.1333, a fifth of the average hop for 8 more links.
#
# THE OPERAND HALF READS THE TREE. A placement map minimizes sum w(i,j) x hops(i,j) and wants a
# module-to-module communication WEIGHT. Measured 20260916.042700: 44 rooms carry Rye, 1,084
# import sites resolve through the hand-filed symlinks into 75 directed room pairs over 225
# edges -- and that graph is STRUCTURE, one edge per importing source file, a startup read and a
# hot loop indistinguishable. Of 9,188 loom keys in the tracked journal, 242 name a room and 4
# name two, all four counting files or symlinks. 44 rooms also do not sit on 4 nodes one apiece,
# so the map needs a coarsening the row never states. Every operand figure is FREE; the geometry
# is HELD.
#
# 54 legs, 4 mutations bitten, one real git repository in a throwaway pen, 12s. tier lap.
guard aurora_placement
path tools/a/aurora_placement_witness.rish
tier lap
seated 20260916.042700

# aurora_file_placement -- the step aurora_placement's second erratum named and nobody took. That
# reading found every assignment of MODULES to nodes overflows a node at either grid row 7 names,
# and closed on one sentence: name which FILES sit on which node, since the coarsening is forced
# rather than optional. This guard grades the unit that replaces it, in three readings.
#
# WHERE THE UNIT STOPS FITTING. Divide tracked non-symlink Rye bytes by the node count and compare
# the largest single file against that equal share. Measured 20260917: 35,113,670 bytes over 1,761
# files, largest caravan/farewell.rye at 568,249 -- 6.5 percent of a quarter, 25.9 percent of a
# sixteenth, and 103.6 percent of a sixty-fourth. So the file unit fits at 4 and 16 nodes and
# OVERFLOWS at 64, one grid above the largest the row names. The erratum's "one level down" has a
# ceiling of its own, and 4 of 44 rooms already stand over a sixteenth.
#
# WHAT THE FINER GRAPH HOLDS. Imports resolved at FILE granularity through the hand-filed symlinks
# give 7,549 unordered edges over 1,561 endpoint files, against the room graph's 67 pairs -- 112.7
# edges per pair. And 6,389 of the 7,549, a share of 0.8463, sit INSIDE one room, so a room-level
# graph cannot see them at all and a module placement would have had every one of them free.
#
# WHAT THE FORCED UNIT COSTS. The capacity-constrained greedy layout keeps 0.2184 of edges on one
# node at 16 nodes where a room layout would keep 0.8463 -- 0.6279 of the graph turning from free
# traffic into real hops. That is the price of a coarsening the constraint forbids, and it is the
# finding. The layout still beats a COUNT-MATCHED random floor by 0.3651, which is the reading that
# matters: a free-spreading floor loses to any layout at all, so the matched one is what says the
# graph was consulted rather than the occupancy.
#
# WHAT THE BORROWED TOLERANCE IS WORTH, added 20260917.145116. The layout rests on the static
# import graph read as a lower bound on coupling, and the paper naming the file unit BORROWED its
# tolerance from the room-granularity sibling, calling the borrowing its own weakest joint. Reading
# 4 re-measures it by the sibling's own method at the sibling's own threshold: with probability p a
# real edge goes silent and the same count of unseen pairs carry traffic, the FIXED layout costed
# under the distorted edges against a count-matched floor costed under the same. The borrowing was
# sound. The file graph's gain crosses the keep threshold at 71.90 percent against the room graph's
# 77.37 interpolated from its own live ladder -- lower, as a thinner layout should be, and by five
# points rather than by the collapse its 0.2184-against-0.8463 locality would suggest. The sibling's
# 20-point ladder reported 60 for both, which is a readout coarser than the effect.
#
# AND THE DECAY IS STRAIGHT, so the tolerance has a closed form. Gain falls as g0(1 - p) to within
# 0.0046 over eight rungs, giving 1 - keep/g0 = 72.70 against a measured 71.90. The tolerance is
# set by the layout's own undistorted gain and the threshold, and granularity enters only through
# g0. That law holds on ~7,500 edges and fails on 67 room pairs and on the pen's 32, where the same
# key reads drift_decay_linear=no -- which is what proves the reading a measurement rather than a
# constant, and it is asserted from both sides.
#
# Every figure here is FREE -- the tree grows -- so run the scan rather than reading them. 78 legs,
# 8 mutations bitten, three real git repositories in a throwaway pen, 38s. tier lap.
guard aurora_file_placement
path tools/a/aurora_file_placement_witness.rish
tier lap
seated 20260917.142006

# key_trade -- the elder locality-key reading names its own next step in its own last section:
# "whether a similarity-preserving sketch lands better on the same trade. It will land better in
# magnitude. Whether it changes a sign is a different key, a different reading." This rung is that
# reading, and it finds a sign changed -- by a third key the elder page never proposed.
#
# THREE KEYS OVER ONE POPULATION, each a 16-bit value whose high byte is x and whose low byte is
# y, so no key gets a fold of its own. sha3 takes bytes 0 and 1 of SHA3-512 over the file, the
# name a Tablecloth here would carry. path takes FNV-1a over the directory in the high byte and
# FNV-1a over the basename in the low, so two files of one room share x exactly. simhash votes 16
# accumulators up or down by each word token's FNV-1a and reads their signs.
#
# THREE METRICS, because a key read only in the torus's metric reports a fact about the FOLD as a
# fact about the KEY. Toroidal Manhattan on (x, y), closed form 128. Circular distance on the whole
# 16-bit key, closed form 16384. Hamming distance, closed form 8. All three derivations are proven
# against a planted uniform population in both directions.
#
# AND EVERY RELATED POPULATION IS READ TWICE -- against the closed form, which asks whether the key
# is uniform, and against the OWN random baseline of that key, which asks whether it carries the
# relation. Only the second is what locality means. The path key's random pairs read a torus mean
# near 95 where a uniform key reads 128, so a same-directory mean under 128 would count that key's
# global concentration as locality. The control proves the two readings disagree on a planted
# population that is concentrated and unrelated, and the adjacency verdict follows the second.
#
# THE ROOM-RECOVERY READING is carried over from the elder page unchanged, so the two may be set
# side by side: the share of files an observer holding keys alone places in the right top-level
# room by guessing the plurality of each cell, against a no-key baseline.
#
# READ 20260916.051607 over 512 tracked files in 215 directories and 41 top-level rooms: chi-squared
# 65.25 sha3, 98.00 path, 1087.50 simhash, against a p=0.001 critical 103.51 at 63 degrees of
# freedom. sha3 carries adjacency in none of six own-baseline readings; path in all six; simhash in
# four. Room recovery 0.4082, 0.4473, 0.4473 against a no-key baseline of 0.3516. Every tree figure
# is FREE. The live VERDICT is reported rather than gated -- eighteen tests at p=0.001 refuse about
# one run in fifty-five on keys behaving correctly, and the path key's evenness sits at 95 percent
# of its critical value.
#
# 42 legs, 4 mutations bitten under a sham leg proving a bite is a bite rather than a root walk
# failing, planted key populations and one real directory tree in a throwaway pen, 30s. tier lap.
guard key_trade
path tools/k/key_trade_witness.rish
tier lap
seated 20260916.051607

# glow_shape_capacity -- CAN GLOW WRITE THE PRODUCT'S OWN TYPES? Glow's multi-field `$:` shape
# holds at most `max_fields` faces, and that constant reads NINE in glow/rune_shape.rye. The
# receipt contract Keaton accepted on `20260913` declares four public types, and TWO of them stand
# above nine: `ReceiptOfferFact` at fifteen fields and `LinengrowReceipt` at eleven. So this seat's
# own ladder rung -- express the receipt facts in the smallest Glow form already owned -- cannot be
# walked, and one constant is why.
#
# THE NINE IS A SEATED FREEZE rather than an oversight: the `20260720` capacity ruling stopped the
# ladder at nona on the reason that another face name taught no new nest law, which was true when
# nothing asked for a tenth. Its own escape clause reserves a widening for Keaton's word, so this
# reading MEASURES the gap and widens nothing.
#
# WHAT IS GATED: that the instrument answered -- both halves read from their own files, a capacity
# found, and the contract's block parsed to its four types. A reader that silently parses zero
# types prints a flawless `types_over_capacity=0` about a page it never opened. The GAP itself is
# reported and gated nowhere, since closing it is a ruling no lap may take.
#
# `tier lap`: two file reads and a pen of planted files released on every exit path.
guard glow_shape_capacity
path tools/g/glow_shape_capacity_witness.rish
tier lap
seated 20260916.033517

# chemical_formula -- a formula names an operation, and the tree either declares it or does not.
# context/CHEMICAL_FORMULAS.md writes this tree's operations as reactions and says on its own face
# that two guards read it and a wrong formula travels past both: the mirror guard proves the page
# and biochemistry/README.md byte-identical, so a false line reaches both homes intact and green,
# and qa_report_card scores Truth by whether cited PATHS resolve, where a formula cites a module and
# no path at all. `weave` stood in that page seventeen days under both, and six days longer in the
# beginner door at foundations/20260825-211056_what-mantra-is.md, which sat outside the declared
# mirror -- a declared mirror guarantees two files and claims nothing about a third page that wrote
# the same sentence. The reading is the call-form token `name(` on an arrow line inside an untagged
# fence, checked against `fn name(` in tracked .rye; a block under an Erratum heading is a quoted
# elder and is read past, since an erratum keeps every word by law. RATCHET at 3 -- every one the
# noun `infusion` where brix/infuse.rye declares `fn infuse`, in the formulas page, its mirror, and
# the water-cardinal rota seat; whether those three take `infuse` is a seated word rather than a
# lap's choice. A ceiling at the standing count is what makes the next arrival red.
guard chemical_formula
path tools/c/chemical_formula_witness.rish
tier lap
seated 20260916.052706
# glow_refusal -- the refusal record the receipt contract asks for, and its one rendering.
#
# The contract asks a refusal to name field, value, ceiling, unit and reason where a ceiling
# applies, and field and reason where none does. `glow/rune_shape.rye` answered with a bare Zig
# error name, and one of those names covered two different ceilings: a nine-face `$:` tuple and a
# three-face `$%` payload both return `error.TooManyFields`, so a caller could not tell which of
# the two refused while the face name, the count and the ceiling all stood in locals the return
# threw away.
#
# SHAPED AFTER `tally/receipt_refusal.rye`, which landed this form first for the receipt side --
# the same `Measure` struct holding value, ceiling and unit together, and the same rendering. Two
# records rather than one, because a Glow parse refusal is not a receipt admission refusal and
# borrowing that vocabulary would braid the language into the product.
#
# WHAT IS GATED: both renderings byte for byte; distinct printable space-free reason and unit
# words; a 64-byte field name admitted and 65 refused; an eight-byte buffer refused by name; the
# tuple ceiling naming its tenth face in units of field; the payload ceiling naming its tag in
# units of payload-face, told apart from the tuple one behind their shared error name; a refusal
# no record names leaving the slot null; and `parse_source` keeping its published bytes. Beside
# them four asserts grepping BOTH format strings out of BOTH records, so two files rendering one
# line drift only by reddening this guard on the lap it happens.
#
# `tier lap`: one small Zig module built through the toolchain, 1.6s warm.
guard glow_refusal
path tools/g/glow_refusal_witness.rish
tier lap
seated 20260916.062546

# rank_outcome -- DID THE RANKING PREDICT ANYTHING? active-designing/20260910-060204_the-bounded-
# torus-moonshots.md carries twelve speculative rows and ranks all twelve at its close. That
# ranking states its own operand in one line -- "Ranked by what a lane can start on this pier, with
# no new hardware, this month" -- so it forecasts STARTABILITY and never truth, and it was written
# before a single row had been read. Every one of the twelve now carries a dated erratum, which is
# the first moment the forecast is gradeable. Grading it against a claim it declined to make is the
# commonest way to be unfair to a forecast, so the three readings are kept apart.
#
# READING 1 -- RANK AGAINST READ ORDER, the thing it claimed. Each erratum carries a one-clock
# stamp, so the read order stands in the page's own bytes, recorded by a habit nobody adopted for
# this purpose. Over all twelve rows Spearman rho reads 0.3007 and Kendall tau 0.4242. Over the TEN
# rows the ranking actually governed, rho reads 0.9879 and tau 0.9556 -- ONE discordant pair in
# forty-five. The rank in read order is 3,4,5,6,7,8,9,11,10,1,2,12: ranks three through nine read in
# exact rank order, one adjacent inversion, then the two rows ranked FIRST and SECOND at read
# positions TEN and ELEVEN.
#
# WHY THE TWO NUMBERS PART, in the errata's own words: row 1's witness landed 22 minutes after the
# page and row 3's 37 minutes after, and both errata are stamped six days later, each reporting 0
# receipts because a cadence guard waited on a rota nothing was turning. A row cheap enough to build
# inside the hour is a row whose READING nobody schedules -- and the ranking could not see that,
# because building and reading are one column in it. Proven in closed form rather than told as a
# story: plant ten perfectly ranked rows, move one prebuilt row from rank one to last in the read
# order, and rho falls 1.0000 to 0.4545 while lifting that row restores 1.0000 over 36 pairs.
#
# READING 2 -- RANK AGAINST SURVIVAL, which it never claimed. Three rows stand, at ranks 1, 2 and 4;
# one is blocked on this pier at rank 3; eight are altered, at ranks 5 through 12. That reads as
# foresight until the confound is COUNTED rather than argued: claim_stands_preexisting=3, so every
# surviving row either had a witness standing before its erratum or is described by its own erratum
# as already standing -- row 10's required-bound rune since 20260716, two months before the page
# named it. Survival here measures PRE-EXISTENCE, and the reading declines to call it prediction.
# Survival is read by a CONJUNCTION rather than a keyword, and that came from a fault found while
# building: "GREEN on metal" names a row's own witness in one erratum and the GRADING instrument in
# another, so row 7 counted as standing on the green word alone while its own erratum re-aimed it.
#
# READING 3 -- WHICH HALF OF A ROW FAILED, and this is the finding worth carrying off the page. A
# Gauge Field row carries a claim and a falsifier. TEN of twelve errata find the FALSIFIER
# structurally faulty -- 6 incapable of firing at all, 3 already settled so firing discriminates
# nothing, 1 aimed at the wrong subject, 2 unmentioned -- a faulted share of 0.8333. And the page
# counts this class itself, inside row 12's erratum, and counts TWO. The class was six. Nobody was
# careless: an erratum can only count the rows already read beside it, so a class distributed one
# row at a time across six days is invisible to every writer of it and plain to the first reader of
# the whole set. The classification is a KEYWORD PROXY over prose and says so on its own face;
# `--explain` quotes the sentence behind each verdict, and all ten were checked by hand.
#
# Every page figure is FREE and free in ONE DIRECTION ONLY -- an erratum is never rewritten, so each
# count can rise as errata land and none can fall. A refusal here means a thirteenth erratum arrived
# and the reading is owed a re-read, rather than that an instrument broke. The ordering arithmetic
# is HELD: identity reads rho 1 and tau 1, a full reversal reads -1 and -1, one adjacent swap in ten
# reads 0.9879 with 44 concordant against 1 discordant, each proven from both sides.
#
# 62 legs, 4 mutations bitten under a sham leg proving a bite is a bite rather than a pen failing,
# planted Markdown pages in a throwaway pen, 2s. tier lap.
guard rank_outcome
path tools/r/rank_outcome_witness.rish
tier lap
seated 20260916.070656

# precondition_skip -- A WITNESS THAT CANNOT RUN HERE SHOULD SAY SO AND EXIT 0. REDS %646 booked
# this sweep from a SAMPLE -- five reds in forty unreached witnesses, two of them preconditions --
# so the crux was never the sweeping; it was building the count the sample stood in for. Counted,
# the submodule half was already honest: 40 of 43 gratitude/tigerbeetle dependents skip, and the
# three that do not are a say line, a presence report satisfied either way, and a witness retired
# from the living suite by name on 20260711. The live half wore a different shape one directory
# over: nine tools/gen/chapter witnesses hard-assert test -x on brushstroke/bin/brushstroke-wayland-
# seed, which git check-ignore names a build output, so a fresh clone lacks it by construction --
# and none of the nine was rostered, which is %646's shape exactly.
#
# The scan reads the class by STRUCTURE: a let binding resolved in one pass, the resolved literal
# asked of git check-ignore, and an unresolved name REFUSED rather than guessed. Its first honest
# reading was 25 hard sites and most were false -- this tree's .gitignore denies the root with /*,
# so check-ignore answers yes to every bare word it has never heard of, and an unresolved variable
# name read as a build output. Requiring a resolved literal carrying a slash took it to 7; leg 7
# removes that guard and bites. 18 sites stay UNRESOLVED and are printed rather than guessed, since
# resolution is one pass over one file -- the same honesty the four ASCII siblings write into their
# own headers. The ceiling is the measured 7 across 5 files, none of them in this sweep's claim.
#
# A skip that always fires proves nothing, so leg 11 plants an executable where the artifact
# belongs, exports WAYLAND_DISPLAY, and asserts a swept witness walks PAST the skip and refuses on
# its own first real claim. 11 legs, 2 mutations bitten, real repositories in a throwaway pen.
guard precondition_skip
path tools/p/precondition_skip_witness.rish
tier lap
seated 20260916.083000
# glow_ceiling_refusal -- two ceilings behind one error name, counted as a class.
#
# `glow_refusal` above closed ONE instance: `glow/rune_shape.rye` answered its field ceiling and
# its payload-face ceiling with the one word `error.TooManyFields`. The next lap found the same
# defect in `glow/tokens.rye`, the lexer every Glow parse passes through, where `error.BadToken`
# answers four ceilings -- a name too long, a cord literal too long, a hex literal with too many
# digits, a source too long -- among nineteen sites that also carry fifteen shape faults. A lantern
# that fires twice becomes a loom, so this guard measures the class.
#
# WHAT IT COUNTS: a relational comparison against a named `max_` constant whose block returns a
# named error, over tracked `glow/` sources; error names grouped by how many DISTINCT constants
# they refuse; and the sites under a shared name that fill no `glow/refusal.rye` record. That last
# number is the gate, under a ceiling that only falls -- 44 at seating, from 48 ambiguous sites of
# which four already carry records.
#
# THE ERROR NAME IS NEVER THE CURE. A published error set is a contract, so the repair fills a
# record beside the return and leaves the word alone (accrete-never-break). Coverage is credited by
# the filled record, and a mutant crediting it by name is proven to bite.
#
# WHAT MAKES THE READING HONEST, each planted: string literals stripped first, since
# `glow/lower_shop_gate.rye` emits Zig text carrying a comparison beside a print-buffer refusal --
# seven false sites; an `assert(` line read past, since an assert crashes and can never be a named
# refusal -- `unpaired` 69 to 16; and the return found by brace depth rather than a line count,
# which is what sees the founding case at all. `unpaired` is printed, so the undercount is a
# measurement rather than a memory.
#
# `tier lap`: two shell readings, 0.5s for the tree and 1.1s for the pen.
guard glow_ceiling_refusal
path tools/g/glow_ceiling_refusal_witness.rish
tier lap
seated 20260916.090645

# glow_ident -- the Zig-identifier rule the lowering room lowers a face by, in one place.
#
# `glow_ceiling_refusal` above reads 35 of its 40 uncovered sites under the one name
# `error.BadIdent`, and the reason is structural rather than a missing repair: `fn zig_safe_ident`
# implemented that rule in 29 separate `glow/lower_*.rye` modules, so a refusal record could not
# land once while its function was copied 29 times. The character test itself -- first byte a
# letter or an underscore, every later byte a letter, a digit or an underscore, hyphen mapped to
# underscore -- was byte-for-byte identical in all 29.
#
# WHAT A RULE WRITTEN 29 TIMES COMES TO DISAGREE ABOUT, measured `20260917` before the first
# conversion: 23 returned `usize` where TAME asks `u32`; five carried the `// invariant:` assert
# the other 24 omitted; `glow/lower_shop_nest.rye` answered a LENGTH ceiling with
# `error.MissingFace` where 28 peers answered `error.BadIdent`; and `glow/lower_alias.rye` mapped
# the dot as well as the hyphen.
#
# WHAT IT COUNTS: files declaring `fn zig_safe_ident` at column one whose body does NOT reach
# `zig_ident.safe_ident`. That number is the gate, under a ceiling that only falls -- 23 at
# seating, from 29 copies of which six became stubs with their own six guards GREEN. A doc comment
# naming the function is prose rather than a declaration, and the column-one anchor that tells them
# apart is proven to bite.
#
# THE SIBLING'S BLIND SPOT, WHICH IS WHY BOTH GUARDS STAND. `glow_ceiling_refusal` reads a ceiling
# site as a comparison against a named `max_` constant, so moving a ceiling into a PARAMETER takes
# the site off that meter whether or not a record was ever written -- `uncovered` fell 40 to 34 on
# the six conversions while `recorded` held at 8, because the record moved into `glow/zig_ident.rye`
# where the sibling cannot see it. That number can therefore be lowered by parameterizing a ceiling
# and writing nothing. This guard holds the other half, and five ident legs inside
# `glow/refusal_witness.rye` prove the record behind it: three faults -- an empty face, a face past
# its ceiling, a byte Zig will not take -- arriving as the one word `error.BadIdent` and told apart
# by record. The published error name is unmoved.
#
# `tier lap`: two shell readings and one Zig build, ~6s.
guard glow_ident
path tools/g/glow_ident_witness.rish
tier lap
seated 20260917.003000

# control_leg_heard -- a control emits a named leg and the witness beside it hears that leg only
# by quoting its name in an assert. So a leg nobody quotes may flip from `yes` to `no` on any lap
# while every named assert passes and every gate stays green; the count merely stops rising. The
# class is booked twice -- `ascii_document_control.sh` spoke 82 readings to a gate that heard 46,
# and `qa_report_card_witness.rish` carried the same hole until `20260916`. A lantern that fires
# twice becomes a loom.
#
# READ `20260916` over 283 tracked control-witness pairs: `generic_wall=41`, `unheard=35` across
# two pairs, and 48 controls carrying no sibling witness at all. Thirty-three of the 35 stand in
# `standing_equipment_control.sh`, the control for the guard that runs every rostered guard.
#
# TWO WALL SPELLINGS ARE CREDITED, and reading for one alone was the fault first residency found:
# the witness may derive the control's `=no` lines and assert that set empty, or the control may
# tally its own parted legs and the witness assert the tally is zero. Reading for the first alone,
# this scan answered `generic_wall=1` and called forty standing walls holes.
#
# THE WALL IS NOT A UNIVERSAL CURE, which is why this ratchets rather than gating at zero.
# `standing_equipment_witness.rish` asserts several legs read `no` on a GREEN run -- a refusal that
# must not fire -- so a wall demanding no `=no` line would red that pair every lap. The per-pair
# cure is naming the leg, and it belongs to the lane that owns each control.
#
# `tier cadence`: the scan reads 283 controls and their witnesses at ~30s. An awk rewrite ran 20s
# and answered 38 where the proven shape answers 35, so the shape was put back and the tier pays
# the cost instead. `--cadence-slice 1` is what turns it.
guard control_leg_heard
path tools/c/control_leg_heard_witness.rish
tier cadence
seated 20260916.223000

# THE ROOT-FINDER CENSUS -- can this tree's own scripts find their root in a checkout of tracked
# bytes alone? 189 of them cannot: they walk up until a directory holds `rishi/bin`, which holds
# the built `rishi` binary and nothing tracked, so a fresh clone before its first build and every
# detached worktree refuse before reading a file (REDS %788).
#
# WHY IT IS ROSTERED AT ALL. No standing guard could have caught this, because every standing
# guard runs in a tree somebody has already built in -- the one condition that hides it. The probe
# that found it reaches the class only by running a candidate twice in a detached worktree at two
# to three minutes each, and met it on 2 of 9 candidates. This reading is static: it extracts each
# root-finder's sentinel directory names and asks `git ls-files` whether each holds tracked
# content, so the whole population is counted in seconds with no build and no worktree.
#
# `tier lap`: 24s, a 9s scan over 3,723 tracked shell sources and a 15s control over real git
# repositories in a pen. It rides the lap clock because the number moves when a NEW script is
# written carrying the copied block, and new instrument files land in this fleet at tens a day --
# so the 190th copy should red on the lap it arrives rather than a fortnight later.
#
# IT RATCHETS RATHER THAN WALLING. The repair is one directory name per file and the files are
# spread across every lane, so the sweep's timing belongs to the fleet rather than to one ship,
# which is `%788`'s own closing sentence. Lower the ceiling in the same commit that sweeps a file.
guard root_finder
path tools/r/root_finder_witness.rish
tier lap
seated 20260917.013000

# `rish_dangling_else` -- in `if COND then assert X else MSG`, `do_if` takes the `else` first and
# the assert is handed a bare condition, so the message the author wrote never arrives. That has
# two effects and REDS %806 recorded one: a FALSE condition refuses `UnknownStatement`, which is
# what a hot pass found on one pier, and a TRUE condition reports the BARE CONDITION as the
# reason, silently, on every pier, in the branch the author did run. The gated class is an else
# arm that is a bare string literal -- never a Rishi statement, so it can only have been the
# assert's message. An arm that IS a statement is reported apart, since whether that was the
# intent is a judgment. Ratchet rather than wall: `%806` names two repair doors, one of them a
# grammar change, and neither is a lap's to take. Lower the ceiling in the commit that sweeps a
# site, which the seating lap did once: `ryekey_witness.rish` was `%806`'s own live instance,
# red on this pier an hour before, and the ceiling fell 7 to 6 with it.
guard rish_dangling_else
path tools/r/rish_dangling_else_witness.rish
tier lap
seated 20260917.035317

# `assert_evidence` -- a Rishi assertion that refuses without printing the command own words is a
# refusal nobody can read. REDS %734 captured thirteen failures under parallel load; ten carried
# their reason -- `unable to load 'pan.zig'` -- and those ten identified the cause, while three
# said only `Lattice build failed` and left nothing. Every `.ok` comes from a record the
# interpreter builds at `rishi/src/main.rye:1563` with `err` and `err_brief` beside it, so the
# evidence is always in hand and the cure is one interpolation. The scan counts the five answers
# apart -- the cure, the wrong stream, a file on disk, a `say` line above, and silence. It gates
# ZERO silence in `tools/l/lattice_*_witness.rish`, the room %734 names and the seating lap swept,
# and PRINTS the tree-wide count beside its share per ten thousand. That split was decided by
# measurement inside the first hour: an absolute ceiling over the tree-wide count reddened every
# ship as peers landed new witnesses, 7,403 to 7,412, while the share moved two parts, 7,234 to
# 7,236. A population that grows with ordinary work wants a share rather than a total. Lap tier: it
# reads tracked bytes alone in about three seconds.
# A SECOND WALL JOINED IT `20260917`. Not every silence costs the same: a mute refusal over a
# `mkdir` stands above a command a reader re-runs in a second, where one over a RYE COMPILATION
# discards a compiler diagnostic produced under a load that may never reproduce -- which is what
# %734 paid a fortnight for. The same binding trace answers it, so `build_mute` is the silence over
# `tools/fixtures/r/rye_build.sh`, or over the `rye` binary asked for `build`, `build-lib` or
# `run`: 1,123 of 7,448 on the seating lap, out of 2,070 compilations. `tools/ca/` was swept -- 64
# sites across 63 files, one `err_brief` interpolation each -- and `build_cohort_mute` holds that
# room at zero. A second cohort over a second reading rather than a widening of the first, so a
# mute refusal over a `git` in Caravan still walks free and is still counted.
guard assert_evidence
path tools/a/assert_evidence_witness.rish
tier lap
seated 20260917.022530

# PAGE EVICT -- a cold reading that charges no peer a page. This fleet measures cold starts, and
# the only eviction anybody reached for was `echo 3 > /proc/sys/vm/drop_caches`, which empties the
# cache for every process on a pier carrying eight ships. So the measurement went untaken and one
# paper's central question stood open with its falsifier written down beside it.
#
# WHAT THE INSTRUMENT DOES. `tools/rye/page_evict.rye` drops one path's cached pages through
# `posix_fadvise(2)` with `POSIX_FADV_DONTNEED` -- per inode, no privilege -- and reads residency
# back through `mmap` plus `mincore(2)`, since the kernel answers success whether or not a page
# left. A path is checked by asking `/proc/self/fd/<n>` where the OPEN descriptor landed rather
# than by reading the spelling, which matters in a tree whose `rye/lib` is a link into `vendor/`.
#
# WHY IT IS ROSTERED. Every ship shares one pier, so the refusal is the load-bearing half: a tool
# evicting the wrong inode charges seven peers for one ship's number. Eleven cases are planted on
# real paths -- `/nix/store`, `/etc`, the peer tree next door, and a sibling whose name merely
# begins the way this root does -- and every welcome is asserted as hard as every refusal.
#
# `tier cadence`: ~30s, most of it the Rye build the witness takes from source so the binary it
# proves is the file a reader opens. The reading moves only when the instrument moves.
guard page_evict
path tools/p/page_evict_witness.rish
tier cadence
seated 20260917.030908

# PAGE RESIDENCY -- whether a path STAYS in the cache, which is the premise under a packing
# recommendation this fleet nearly built. `active-designing/date/20260917/20260917-031330_the-price-of-an-open.md`
# measured a cold library read at 304 us per open, found 87 percent of it is the opens, and advised
# merging 527 files into fewer. That advice is worth its build only where the fleet walks the cold
# path more than once, and nothing had read that.
#
# WHAT THE INSTRUMENT DOES. `tools/fixtures/p/page_residency_sample.sh` reads one path's residency
# repeatedly through `page-evict census` and reports the MINIMUM beside the free and cached
# kilobytes that explain it. A single census taken at a quiet moment reports the best case and
# reads as the case; a page cache under eight ships with no swap is a moving quantity.
#
# WHY IT IS ROSTERED. The control shows BOTH sides on real pages rather than on a stub: a pen file
# is warmed, sampled, dropped through `POSIX_FADV_DONTNEED`, and sampled again. One kernel property
# is load-bearing and was learned by losing a leg -- a DIRTY page is not droppable, so the pen is
# `sync`ed before any eviction is asked for, and the first run read 256 pages still resident after
# a successful evict call.
#
# IT REPORTS THE TREE'S OWN RESIDENCY AND GATES NONE OF IT. A cache is the host's to manage, and a
# guard that reds when a peer's build evicts a page is a guard somebody turns off. What it gates is
# the control: 19 legs, four mutations bitten, every refusal planted and then lifted.
#
# `tier cadence`: ~30s, most of it the Rye build the witness takes from source, the same build its
# `page_evict` sibling takes. The reading moves only when the instrument moves.
guard page_residency
path tools/p/page_residency_witness.rish
tier cadence
seated 20260917.040500

# WHAT WENT WRONG WITHOUT IT. `active-designing/date/20260917/20260917-023717_the-cold-read-is-a-file-count.md`
# inferred a mechanism from one corpus -- readahead cannot cross an `open`, so each file pays one
# device round trip -- handed another ship a recommendation resting on it, and named its own
# falsifier without running it. A single corpus cannot tell a per-FILE cost from a per-BYTE one,
# because either explains one number, so the inference could have been wrong in a way no repeat of
# that measurement would ever show.
#
# WHAT THE INSTRUMENT DOES. `tools/rye/read_latency.rye` times single 4 KiB `pread` calls one at a
# time against the monotonic clock, with the pages dropped per inode and `POSIX_FADV_RANDOM` set
# so no window is fetched around the first read -- the device's own queue-depth-1 latency. It then
# holds 67,108,864 bytes still and splits them across 64, 128, 256, 512 and 1,024 files, which is
# what separates the two costs: a per-byte world holds the total flat, a per-file world holds the
# per-file number flat, and neither happens. A fourth reading runs one pen with the kernel's
# readahead on and then off, per descriptor rather than by writing a host setting eight ships
# share.
#
# WHY IT IS ROSTERED. The numbers are load-bearing in advice already handed to a peer lane, and a
# number behind a recommendation that nobody re-runs is a number somebody will quote after it has
# stopped being true. Nothing here is gated to a VALUE -- a latency gate would red on the first
# pier with different storage. What is gated are RELATIONS one run produces: cold above warm by
# ten times, one open above many opens, the per-file floor above a warm read, and the same bytes
# costing more as more files carry them. Each holds wherever the mechanism holds and fails where
# it does not.
#
# ITS OWN HONEST LIMIT, RECORDED RATHER THAN PATCHED. Planting `POSIX_FADV_NORMAL` where the probe
# advises `RANDOM` -- a mutation making its second arm a copy of its first -- is caught by no case,
# because the effect sits under this device's run-to-run noise. That escape is the probe's result:
# it supports an upper bound rather than a measurement, and the control's header says so.
#
# `tier cadence`: ~90s, most of it the four cold arms and the five-row sweep, which read about 400
# MB from the device with its cache dropped. The reading moves when the storage does.
guard read_latency
path tools/r/read_latency_witness.rish
tier cadence
seated 20260917.033500

# THE FUND FAMILY -- seven witnesses, six seated here, and the seventh held by name.
#
# WHY THIS BLOCK EXISTS. `tools/gen/fund/` holds seven `gen_*_fund_prep.rish` witnesses, each
# asserting the seat, dedication, executive and refusal roster of one constellation fund seat
# against `tools/fixtures/g/gen_*_fund_prep.sh` and the facts in `context/LEXICON.md`. Until
# `20260917.052450` not one of them carried a guard record here: `grep -c fund_prep` read 0, so
# every refusal any of them could make reached no roster pass. That is REDS %219's shape wearing a
# family name, and the family paid for it -- `gen_trya_fund_prep` has refused on committed bytes
# since the Lexicon renamed Trya (fund) to Acme (fund) on `20260729.225900`, fifty days before a
# hand running the seven for an unrelated sweep happened to hear it (REDS %810).
#
# `tier lap`: measured on this pier `20260917.052400`, the six seated below run 113, 201, 156, 141,
# 126 and 134 ms, about 870 ms together -- cheap enough that the family is heard every lap rather
# than on the slower clock that let a rename stand for fifty days.
#
# `gen_linn_fund_prep` is seated as itself rather than skipped as a duplicate. Its own first line
# names it a historical shim forward to Bozo (Linn retired `20260730.150702`), and a shim that has
# stopped shimming is exactly the fault this block exists to hear.
#
# THE SEVENTH IS HELD OUT, AND HELD OUT BY NAME. `tools/gen/fund/gen_trya_fund_prep.rish` refuses
# in 41 ms at `grep -q 'trya.fund' context/LEXICON.md`. Seating it would red every roster pass on
# every ship for a fault no lap may repair, and a gate that reds on what nobody can fix is a gate
# somebody turns off. Its two doors -- retire the generator, or rename it forward to
# `gen_acme_fund_prep` checking Acme (fund), acme.fund and %acme -- are a POLE fund-seat naming
# ruling and wait on Keaton's word (REDS %810). WHAT SEATS IT: whichever door lands, add
#
#   guard gen_trya_fund_prep      (or its renamed successor)
#   path tools/gen/fund/gen_trya_fund_prep.rish
#   tier lap
#   seated <one-clock stamp of the ruling>
#
# and close %810 on the pass that hears it GREEN. Until then the held seat is written here rather
# than remembered, so the ruling has one line to land on.

guard gen_bozo_fund_prep
path tools/gen/fund/gen_bozo_fund_prep.rish
tier lap
seated 20260917.052450

guard gen_gren_fund_prep
path tools/gen/fund/gen_gren_fund_prep.rish
tier lap
seated 20260917.052450

guard gen_keeh_fund_prep
path tools/gen/fund/gen_keeh_fund_prep.rish
tier lap
seated 20260917.052450

guard gen_linn_fund_prep
path tools/gen/fund/gen_linn_fund_prep.rish
tier lap
seated 20260917.052450

guard gen_murr_fund_prep
path tools/gen/fund/gen_murr_fund_prep.rish
tier lap
seated 20260917.052450

guard gen_shyu_fund_prep
path tools/gen/fund/gen_shyu_fund_prep.rish
tier lap
seated 20260917.052450

guard checkable_binding
path tools/c/checkable_binding_witness.rish
tier lap
seated 20260917.060042
# THE DOORWAY ASKS WHETHER THE DOOR IS LABELLED; THIS ASKS WHETHER THE LABEL IS EARNED.
#
# `context/TWO_ROOMS.md` seats one crossing: "A claim crosses from the vision room to the checkable
# room by exactly one door -- it earns a witness." `two_rooms_doorway` above reads whether a page
# NAMES a room and stops there, by design, so a settled page declaring the checkable room while
# naming no evidence at all reads exactly like a page bound by six witnesses. Nothing read the
# second half.
#
# GATED as a ratchet with no slack: `settled_unbound` -- a door naming the checkable room beside a
# settled lifecycle word, over a body naming no instrument, no tracked module source, and no tracked
# construction pin. Read 8 on the seating lap over the doorway's own roster of 1,351 pages, 435 of
# which declare checkable and 202 settled. The raw finding was 61 and three successive fairness
# widenings dissolved it to 8, which is the strongest thing that can be said for what is left.
#
# REPORTED, never gated: `settled_delegated` 28 and `settled_hop_dry` 7. A page saying its binding
# is named elsewhere -- `foundations/20260905-154954_the-clock-and-the-mark.md` writes exactly that
# -- is honest, so only a DRY hop is a promise broken. The hop is followed one step, since two needs
# a cycle guard and measures something no reader does.
#
# 19s on the seating lap, which is why `tier lap` rather than cadence.
# cpu_unit -- WHICH UNIT can this pier measure a saving in? Every timing claim this fleet ships is
# stated in wall milliseconds on a pier eight ships share, and row 12's erratum on the twelve-
# moonshot page measured what that costs: a deterministic workload measuring a change of exactly
# zero read a wall spread of 16.7 to 54.8 percent of its median. The remedy named there was more
# trials; this instrument tests the other axis, the unit. It reads one MINSTD spin loop in two
# units inside ONE call -- wall from `read < /proc/uptime`, CPU from the `times` builtin -- so
# neither clock forks a child whose own CPU would land in the counter being read.
# THE GUARD GATES THE ARITHMETIC AND DECLINES THE HOST READING. Injected sample lists have one
# right answer each, so medians, spreads, the observed step, the contention share and the verdict
# are all asserted; the measured spread moves with whatever the other seven ships are doing, and a
# gate over the weather is a gate somebody turns off. 1.9s, 41 control legs, four mutations bitten.
# Reading: active-designing/date/20260917/20260917-054941_the-unit-that-survives-the-pier.md
guard cpu_unit
path tools/c/cpu_unit_witness.rish
tier lap
seated 20260917.054941

# LOOM SITTING -- can a key in the journal PROVE a movement, and how small a one? The trend reader
# beside it ends on `direction=rising|falling|level`, which reports that the last value differs from
# the first and is read as evidence the WORK changed -- two facts in one word, on a pier eight ships
# share. This groups a key's values by the day shelf each log was born on and prints the
# within-sitting spread (the resolution floor) beside the across-sitting one.
#
# THE GUARD GATES THE ARITHMETIC AND THE REFUSALS, AND DECLINES THE JOURNAL. Planted corpora have
# one right answer each, so every median, spread, parts-per-thousand figure, scale and verdict is
# asserted; what `wall_s` reads tomorrow is eight ships' habits, and a ceiling over it would red on
# a day somebody measured something new. The singleton sitting is proven from both sides -- its zero
# spread means UNMEASURED, so it is excluded from the floor, counted apart, and a key with no
# multi-value sitting refuses rather than printing a confident number.
# 2s, 56 control legs, six mutations bitten.
# Reading: active-designing/date/20260917/20260917-074214_the-numbers-nobody-can-compare.md
guard loom_sitting
path tools/l/loom_sitting_witness.rish
tier lap
seated 20260917.074214

# record_version -- a record header names its version chronologically.
# `context/specs/rye-versioning-style.md` settled this in June: versions here are chronological,
# `YYYYMMDD.HHMMSS`, refusing semantic versioning by name and citing Hickey's *Spec-ulation* --
# names endure, software grows by accretion. REDS `%765` found that ruling walked past, moved
# `mantra-weave-v3` to `mantra-weave-20260916.101910`, and left the REST as a molt wanting
# Keaton's word, since every counted header opens a store this tree already wrote. So the row
# stands OPEN on a population nothing read, and the population GROWS: measured by this classifier
# in a detached worktree at each commit, 16 families on `20260801`, 30 on `20260901`, 30 on
# `20260916`, 32 on `20260917` -- and the two that arrived in that last day, `rye-key-library`
# and `rye-key-cache`, were written the day AFTER the row was found, repaired and audited.
# THE DISCRIMINATOR IS THE INSTRUMENT: a record version is a string reaching stored bytes as a
# format or header field -- `declared` as a `*_format` constant, `written` through `append_kv`
# with a format key or appended as the header line, or `compared` against a parsed `format` or
# `header`. A grep for `-v<N>` cannot tell one from a look-alike, and seven stand here: two
# witness plaintexts, two test file CONTENTS, two fixture signature values, one generator seed
# name. Each is printed by name rather than silently dropped. ONE RATCHET that only falls,
# `counted_families`, because the standing 32 are stored bytes accrete-never-break keeps forever
# and a wall would red eight ships for a molt nobody has called; what it catches is the NEXT
# counted family, on the lap it is written. `chronological_families` is reported beside it so a
# molt reads as a movement rather than only as a lowered ceiling, and a `domain` tag -- mycelium's
# two -- is reported apart as its own question. `tier lap`: ~6s.
guard record_version
path tools/r/record_version_witness.rish
tier lap
seated 20260917.045550

# loom_name -- does a loom key's NAME say which population it measures? loom_sitting names a key
# MIXED from its spread, after the fact, and stops there; the two repairs that condition admits are
# opposite. A key merging several families parts at a name -- `roster_cold_s` beside `roster_hot_s`.
# A key varying inside one family reaches no name at all. This reads the half declared at the door:
# the family each loom line names in its FIRST token, in either the pair form or the bare form, with
# 427 of 6,574 lines declaring none. Over the 60 keys the journal writes, `name_shared` reads 60 and
# the other two verdicts never -- which is why the pen carries the weight here, drawing all three on
# planted journals. Gates nothing: which family words eight ships write is the pier's.
# 3s, 45 control legs, five mutations bitten.
# Reading: active-designing/date/20260917/20260917-081605_the-name-that-scopes-a-number.md
guard loom_name
path tools/l/loom_name_witness.rish
tier lap
seated 20260917.083040

# alloc_bound_reach reads TAME root rule 1 where a hand meets it. bound_kind_census reads the 670
# bound DECLARATIONS and sorts them by dimension; nothing read the 1,370 allocation CALL SITES, in
# the reflex the core states first. One wall at zero -- a count naming a max_ that is declared
# nowhere. Every other class is REPORTED, since boundedness is dataflow and a text scan cannot
# follow it; fn_neither rides no ceiling because a ratchet over a growing population is a wall
# with a delay.
guard alloc_bound_reach
path tools/a/alloc_bound_reach_witness.rish
tier lap   # 21s; 1,370 sites and 1,046 allocating functions read, 44 control legs, four mutations bitten.
seated 20260917.090000

# falsifier_reach reads the PROSE discipline that separates research from enthusiasm. Gauge at
# its Field setting asks every projection for a falsifier; three corpus readers watch the code
# rules and none watched this one. 580 stated falsifiers across 493 living pages, classified by
# whether they could ever fire -- runnable, quantified, narrative. One wall at zero: a page
# declaring Field, carrying a projection, and naming no falsifier, outside the tracked roster
# tools/fixtures/f/falsifier_field_baseline.txt. The roster's own 21 are ratcheted.
guard falsifier_reach
path tools/f/falsifier_reach_witness.rish
tier lap   # 19s; 493 pages and 580 regions read, 44 control legs, four mutations bitten.
seated 20260917.093812

# falsifier_form_outcome crosses the two readings that never met. falsifier_reach reads a
# falsifier's FORM and closes on one handoff -- grow the runnable count; rank_outcome reads
# what an erratum found wrong with it. Across BOTH ranked pages, 21 speculative rows, the
# runnable count is ZERO, so the crossing has an empty cell on exactly the class the handoff
# names and reads verdict=underdetermined rather than a correlation it cannot support. One
# wall at zero: a ranked row carrying no falsifier region, which no later erratum can grade.
guard falsifier_form_outcome
path tools/f/falsifier_form_outcome_witness.rish
tier lap   # 44s; 2 ranked pages and 21 rows crossed, 36 control legs, five mutations bitten.
seated 20260917.133000

# verdict-home -- where a verdict about an elder claim lands. Walls `unanswered` at zero: a
# child page declaring in its door that it graded row N of an elder page, where the elder
# carries no matching `Row N erratum` line. A wall rather than a ratchet, since all 14
# standing declarations are answered. The key census reports and gates nothing -- the seated
# `Runs the falsifier of:` spelling reached 0 of 16 opportunities in eight days and is
# withdrawn by the paper this guard ships with.
guard verdict_home
path tools/f/falsifier_verdict_home_witness.rish
tier lap   # 8s; 494 pages, 14 declarations and 12 errata read, 40 control legs, four mutations bitten.
seated 20260917.101905

# control_in_population -- a guard whose enumerated population holds its own control.
# REDS %785 found a ceiling 27 percent composed of the guard's own proof: a control proves a
# refusal by CONTAINING the thing refused, a scan proves a tree clean by READING a population, and
# where the population holds the control those two correctnesses pull against each other while each
# file does exactly its job. The witness landed `20260916` and stood on no roster row, so nothing
# ran it -- `witness_reach` read it `unheard`, named by no runner at all, which is REDS %810's
# silence one room over. `tier cadence`: 54s measured on this pier `20260917`, nine times the
# lap-tier rows beside it, and the property it reads moves when a guard or a control is written
# rather than hourly.
guard control_in_population
path tools/c/control_in_population_witness.rish
tier cadence
seated 20260917.095028

# control_perturbation -- does a guard's ANSWER move when its own control goes away?
# The sibling above counts MEMBERSHIP and says in its own header that membership is no proof the
# scan READS the file. This one runs the candidate twice in a detached worktree, once whole and
# once with its control absent from the index and the working tree, so the reading is behavioral
# rather than structural. It is also the probe that found REDS %788 -- 2 of its 9 candidates
# returned nothing either time, because 164 tracked scripts locate the tree root by `rishi/bin`,
# a directory only a build creates. Landed `20260916`, unrostered and `unheard` until this stamp.
# `tier cadence`: 166s measured on this pier `20260917` -- it builds worktrees, so it is the most
# expensive row in this pair by three times.
guard control_perturbation
path tools/c/control_perturbation_witness.rish
tier cadence
seated 20260917.095028

# link_touch -- the staged-page link reading, proven on real git repositories.
# A link names a door the reader expects to open, and this witness plants each break beside its
# repair and asserts both outcomes across 26 cases. It landed `20260909` and stood `unheard` for
# eight days; it is rostered here by the same lap that rostered the two above, because the growth
# reading named all three at once and running it costs 4.8s. Its lane is the link family rather
# than this seat's, so the row is a pickup rather than a claim on that work.
guard link_touch
path tools/l/link_touch_witness.rish
tier lap
seated 20260917.095028

# commit_parent_claim -- REDS %801 closed by measurement rather than by a sentence.
# The row booked one firing and handed the fleet question up; the reading finds TEN in
# the trailing 4,000 commits, one of them landed twelve hours AFTER the row was booked.
# Forward-only by construction: the anchor is the HEAD it was seated on, so testimony
# is counted and never graded, and the wall reds on the lap the eleventh arrives.
guard commit_parent_claim
path tools/c/commit_parent_claim_witness.rish
tier lap
seated 20260917.162810

# mantra_head_door_cost -- both sides of one falsifier, counted in one unit. The design page at
# active-designing/date/20260916/20260916-105110_no-anchor-names-the-head.md recommends Door C for REDS %807, a
# document-start sentinel, and writes its own falsifier in plain words: a count of the places that
# would have to learn about the sentinel, against the 12 place constructions Door B touches.
# Nobody ran it, and the page said so -- confidence moderate, only one of the two costs carrying a
# number. THE ANSWER IS TWO ANSWERS. Read literally the falsifier FIRES: 28 places must learn
# against a stated 12, so the page's own test refutes its own recommendation. Counted in ONE UNIT
# -- sites that must change, read the same way on both sides -- Door C is CHEAPER, 28 against 32.
# The page compares sentinel-learners against place constructions, which are two different units,
# and a falsifier whose sides are counted differently returns whichever answer the reader's unit
# chooses. The page's own 12 also does not reproduce: the module holds SIX place constructions,
# four place() accessors and two sentinel initializers -- so the first answer stands whichever
# number a reader trusts. The threshold is READ OFF THE PAGE rather than spelled, proven by
# handing the scan a page reading 40 and watching the falsifier stop firing. Door C's sites are
# CLASSIFIED rather than totalled, free tested first, and the order is load-bearing: the one site
# casting the length with the intCast builtin matches the free rule and the alloc rule both and is
# an insertion index, so the control swaps the two rules and watches it cross the gate.
# Twenty-seven control legs, every class planted, two rules priced. Takes no ruling: which door
# the weave walks through stays Keaton's word. Text only, no build, `tier lap` at about a second.
guard mantra_head_door_cost
path tools/m/mantra_head_door_cost_witness.rish
tier lap
seated 20260917.184937

# ROW 6 OF THE BOUNDED TORUS MOONSHOTS ASSUMED A HOST THAT CAN READ ITS OWN JOULES, AND NOBODY HAD
# ASKED THIS HOST. tools/fixtures/e/energy_readout_scan.sh probes seven facilities -- powercap
# RAPL, raw MSR, perf's energy-pkg event, hwmon, nvidia-smi, and (as of `20260918`) two DVFS/heat
# proxies, cpufreq and thermal -- rather than RAPL alone, so a "no" names the whole approach on
# this host rather than one path a kernel build happened to omit. The proxies are held on their
# own flag and never promote to the joule-bearing verdict, and the hypervisor is now named by
# `systemd-detect-virt` rather than only flagged. This guard proves the probe recognizes a real
# facility, an absent one, a gated one, and a proxy correctly isolated, in a throwaway pen, before
# trusting the wall of "no" this pier's own real paths return. 1.13s measured `20260918`, text and
# sysfs reads only, no build.
guard energy_readout
path tools/e/energy_readout_witness.rish
tier lap
seated 20260917.213500

# ROUND TWO'S SECOND PROPOSAL ASKED WHETHER A TWO-FIELD KEY BUYS TWO PROPERTIES FROM TWO FIELDS,
# EACH KEPT WHOLE, RATHER THAN TRADING ONE AWAY TO KEEP THE OTHER TWO. locality_key priced one key
# read two ways and found locality, evenness, and confidentiality were one quantity read three
# directions. This guard prices a composite key -- a placement field (the prefix) alongside a
# content field (the digest), concatenated rather than folded -- against both single-field keys.
#
# WHAT IT FOUND, read over the tree's own tracked bytes at a 64-cell grid. Storage placement put to
# the content field reads chi-squared 41.91, IDENTICAL to the digest key's own -- the composite buys
# evenness in full. Same-room distance put to the placement field reads 0.339 cells, IDENTICAL to
# the prefix key's own -- the composite buys locality in full. Room recovery put to the placement
# field reads 0.5451 over 34 rooms, IDENTICAL to the prefix key's own confidentiality loss -- NOT
# closer to the digest's lower 0.4745. Proposal 2's own stated falsifier fired exactly as named.
#
# THE FINDING. Locality and evenness compose without interference: a real store could use the
# content field for even storage placement while a separate declared field serves locality queries,
# buying both where a single field before had to choose. Confidentiality does not compose this way,
# because it was never a property of how a field's bytes are arranged -- it is a property of WHICH
# fields an observer holds. A field concatenated beside a leaky one adds information for an observer
# holding the whole key; it never conceals what the leaky field already gives away.
#
# WHAT IS GATED. The control, and the live reading's three equalities plus the recovery range
# identity. The buy/sell verdicts and every chi-squared and mean are REPORTED rather than gated, for
# the reason the sibling guard gives: the statistic refuses at its own stated error rate, and the
# means move with the tree's contents.
#
# Paper: `active-designing/20260918-013435_the-two-fields-that-composed-and-the-one-that-did-not.md`
guard composite_key
path tools/c/composite_key_witness.rish
tier lap   # ~1s; 16 behaviors in a pen, three exact equalities, two mutations bitten
seated 20260918.013000

# WHY THIS ROW. active-designing/20260918-000154_three-numbers-and-a-name.md measured a decision
# parked in four places at once -- a forward-looking sentence naming Keaton's word, a sentence
# already recording a word granted, a YOURS: line on a living pin, and an Open doors for Keaton
# section -- and found nothing read them together, then proposed a scan rather than an eighty-first
# place to park one, reported and gated on nothing. This row is that proposal, built.
guard pending_decision
path tools/p/pending_decision_witness.rish
tier lap   # ~1.2s cold reading over living tracked Markdown; 8 behaviors in a pen, all reported
seated 20260918.025301

# fascia_metric_v0 -- one of the Generator Chapter witnesses that had lived outside the standing
# roster. Its direct metric reading stays GREEN while its named shred verb refuses, so the chapter
# witness now has a seat where both directions can answer on every lap.
guard fascia_metric_v0
path tools/gen/chapter/fascia_metric_v0.rish
tier lap
seated 20260922.225159

# grad_seal -- the Generator Chapter seal proves the living pins and its planted negative;
# its composed module half stays RED while CION reports bare ordinals in lattice and fascia.
guard grad_seal
path tools/gen/chapter/grad_seal_witness.rish
tier lap
seated 20260924.012305

# almanac_breach -- the Generator Chapter breach witness proves preserved bytes, living links,
# and dated testimony on both sides of the existing archive fixture.
guard almanac_breach
path tools/gen/chapter/almanac_breach_witness.rish
tier lap
seated 20260924.072938

# relay_resin_census -- the Generator Chapter resin limb keeps twelve named beads bounded,
# requires the manifest bead beyond that limb, and checks the resin and bead lexicon seam.
guard relay_resin_census
path tools/gen/chapter/relay_resin_census_witness.rish
tier lap
seated 20260924.090630

# bounds_typed -- the Generator Chapter witness keeps declared literal bounds typed,
# with the zero-population scan and its refusal visible on each lap.
guard bounds_typed
path tools/gen/chapter/bounds_typed_witness.rish
tier lap
seated 20260924.194608

# bounds_home_census -- ten declared pairs agree with the living bound table,
# and a one-value drift refuses before the metal build is accepted.
guard bounds_home_census
path tools/gen/chapter/bounds_home_census_witness.rish
tier lap
seated 20260925.061518

# brush_parse -- the existing Brush Chapter witness proves the parser's accepted and refused
# paths, then carries the same frame through lowering, redraw, and the headless Skate grid.
guard brush_parse
path tools/gen/chapter/brush_parse_witness.rish
tier lap
seated 20260923.040437

# pond_gui_spine -- the constructive Pond GUI fold reaches the connector, Brush fixture,
# and frame legs, while the witness keeps the live display and build artifacts as honest skips.
guard pond_gui_spine
path tools/gen/chapter/pond_gui_spine_witness.rish
tier lap
seated 20260923.210912
