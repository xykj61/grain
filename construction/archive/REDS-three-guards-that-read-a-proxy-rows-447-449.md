# REDS %447, %448, %449 -- three guards that read a proxy for their subject

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.010000`, in the round that
booked `%451`, to bring the pin back under the byte bound its own header declares. Two CLOSED and
one BOOKED, kept together because each names the same substitution in a different material. `%447`
is the instrument built to find invisible failure carrying an invisible failure of its own. `%448`
is a control leg asserting where a ship's checkout sits rather than what the loop does with it.
`%449` is four rostered guards reading a Rye module with `grep`, so not one of them noticed it had
stopped compiling when the toolchain moved. A guard that reads a stand-in reports on the stand-in,
and reports it in the voice of the thing itself. Link depth moved with the text.*


**REDS %447 (`20260905.233110`) -- the instrument built to find invisible failure had an invisible failure, and a standing guard read it before the commit did.** *What went wrong:* `tools/fixtures/b/bound_kind_census.sh`, written this lap to measure which dimension each of the tree's named bounds constrains, closed both of its extraction pipelines with `|| true`. That discards the failure of an output-producing pass, so a broken `git ls-files` and a genuinely clean tree report the same number -- which is the exact class `%413` and `%416` booked, in the exact instrument whose whole subject is a check that cannot be seen. *What caught it:* `instrument_refusal` on the **hot** roster pass, reading `swallowed_instrument_passes=2` against a ceiling of **0** and naming both line numbers in its evidence file. The cold pass an hour earlier had missed it for a reason worth recording: the census did not exist when that pass started, and it closed `tree_moved=yes` because I edited files while it ran, against the card's own *hold still while it runs*. So the guard that caught this had to wait for the one pass taken over a still tree. *What it taught:* **dropping the swallow is only half a repair, because a pipeline ending in `sed` exits zero over empty input however badly the upstream failed.** The `|| true` was the visible half; the invisible half is that removing it changes nothing, since `sed` is the pipeline's last command and POSIX `sh` has no `pipefail`. An instrument refuses honestly only by **checking its own result** -- so each extraction now names its own emptiness: `no_bounds_found`, `no_extent_control`, `no_assert_corpus`. The two `|| true` that remain are grep-led, which the law's own header names as correct rather than a swallow, and each is now backed by one of those checks. *Repaired (`20260905.233110`):* all three refusals proven from the **refusing side** on real git repositories in a throwaway pen -- a tree with no Rye, a tree with a bound and no extent family, and a tree with both and no asserts -- exit status 2 agreeing with each verdict, because a gate that has never refused anything is a gate in name only. `sh tools/fixtures/i/instrument_refusal_scan.sh` reads `swallowed_instrument_passes=0`, `under_ceiling=yes`, `verdict=ok` **on metal**, and the census reports the same twelve readings after the repair as before it, so the fix moved no number. **CLOSED.**

**REDS %448 (`20260906.002530`) -- a control leg asserted where a ship's checkout is rather than what the loop does, so it was green on seven trees and red on the one it names.** *What went wrong:* `tools/fixtures/f/fleet_roster_control.sh` ended on a leg that ran the real fleet -- `FLEET_DRY=1 sh tools/l/fleet-loop.sh bakery` -- and asserted the answer contained *belongs in grain-bakery*. That sentence is true only where bakery's checkout is somewhere else. Run inside `grain-bakery` the loop correctly does **not** refuse: it prints `root=/home/keeper/grain-bakery` and the lap command, exactly as `%291` requires, and the leg read that right answer as `berthed_refuses_by_tree=no` -- `control_failures=1`, `control_verdict=broken`, and a rostered guard red at `tier lap`. *What caught it:* the opening cold pass on this tree, `114 guards`, which read **three** reds where the card's live front names two: `commit_message_guard` and `agent_jail` are the two every ship sees, and `fleet_roster` was the third and stood on this tree alone. *What it taught:* **this is `%422`'s own lesson standing one line below its own correction.** The comment directly above that leg says it, in this file, in these words: *"These legs named `bakery` as their berthed example and asserted against the real roster, so they measured the tree's current data instead of the scan's behaviour -- and they broke the hour bakery was lawfully promoted to `live`."* That repair moved **three** of the four berthed legs into a pen on `20260905` and left the fourth reading the fleet, so the round that named the hazard shipped one more instance of it. A repair that fixes a class by hand leaves whichever member the hand did not reach, and the member left behind is invisible from the tree that ran the repair. *Repaired (`20260906.002530`):* the fourth leg is **gone rather than moved into the pen**, because it was a duplicate of `wrong_tree_refused` written the wrong way round -- that leg already derives an away seat from the roster and proves the refusal on every tree. The one reading it uniquely carried, that the message names **which** tree, now stands beside its sibling as `refusal_names_the_wanted_tree`, with the wanted tree read from the roster rather than spelled. Proven from both sides on metal: with the loop's message intact the leg reads `yes`; with `belongs in $want_tree` planted down to a bare `refusing`, `wrong_tree_refused` **stays yes** while the new leg goes `no` -- so the two readings are distinct rather than one wearing two names, and the loop was restored byte-identical. `control_checks` holds at 38, one leg out and one in. **CLOSED** on `tools/f/fleet_roster_witness.rish` GREEN.

**REDS %449 (`20260906.001558`) -- the module four rostered guards read had not compiled since the toolchain moved, because all four read it with grep.** *What went wrong:* `mantra/src/weave.rye` does not build against this tree's pinned Zig 0.16.0: `Weave.empty()` returned `.{ .lines = .{} }`, the elder empty-literal form 0.16 replaced with `.empty`. Proven by building the HEAD blob in a pen: `error: missing struct field: items`. Four guards stand over that file at `tier lap` -- `mantra_gen_floor_a1_gate` and `mantra_glow_tend_limb1` through `limb3` -- and every one reads it with `grep -q`, for field names and the string `gen >= 1`. All four were GREEN in this lap's cold pass, over a file the compiler refuses. *What caught it:* writing the first program that ever imported it. `grep -rn 'rye build' tools/` names `main.rye` at three sites and `weave.rye` at **zero**; `main.rye` and `rye/tests/mantra_weave_test.rye` each **inline their own copy**. *What it taught:* **a test that inlines its subject migrates with the toolchain while the subject rots, and every guard stays green** -- the test's copy reads `.empty` because the test is compiled, so the two disagree about the language they are written in. Plainly: **grep proves a file's shape; only a compiler proves a file.** *Repaired here:* both sites move to `.empty`, and `mantra_weave_merge_witness` **builds** the module rather than grepping it. *Remainder, a booked lap:* a census of authored `.rye` no tool anywhere compiles. **BOOKED.** *Erratum:* the commit that landed this row names it **%446**; four `git fetch xy` calls each found the previous number published upstream, so the view moved five times and the stamp did not (`.claude/rules/derived-spine.md`).
