# REDS -- the roster ran on one tree and the commit shipped another (row %174)

*Folded from [`../REDS.md`](../REDS.md) on `20260823.204456`, **CLOSED**, as row %175 carried the living pin past its 24,576-byte bound.*

**REDS %174 (`20260823.174500`) -- the roster ran on one tree and the commit shipped another.** *What went wrong:* a build cache reached `main`. `tools/glow/.cache/` holds three files, two of them symlinks at **pre-fold** targets (`tools/caravan`, `tools/tally/gardens.rye`) that dangle in any fresh clone. The lap's order was: stage three named files, run the roster, read **31 of 31 green**, then `git add -A`, then commit. `tools/glow/` stood untracked during the run and tracked at the commit, so the green was measured on a tree that no longer existed by the time it shipped. *What caught it:* the very next roster run, one lap later -- `tracked_link` naming both dangling symlinks -- which is a day late and in the shared history. *What it taught:* **a guard measures the tree it was run against, so the roster belongs after the staging and never before it.** REDS %166 reached the neighbouring conclusion from the near-miss side -- *read the staged list* -- and caught its file before the commit; this is the same gap from the other side, where nothing read the list and the commit went out. The two together give the rule its final shape: **stage first, then measure, then read what you staged.** *Repaired:* `tools/glow/` added to `.gitignore` beside the `/glow/.cache/` rule it should have matched from birth -- the fold gave the witnesses a second cache room and the elder anchored rule never reached it, which is `%166`'s anchored-pattern lesson arriving a third time. `git rm -r --cached tools/glow` drops it from the index; `tracked_link` reads `verdict=ok`. CLOSED.
