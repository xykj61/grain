# REDS -- the instrument counted as field (rows %157-%158)

**Language:** EN
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Folded:** `20260823.034321` -- carried here from [`../REDS.md`](../REDS.md) when the living pin
passed its 24,576-byte bound as rows %160-%162 were booked. Both rows teach one lesson: a name a
control plants to match nothing is instrument wherever it is written, and every meter that walks
the tree meets one.

---

**REDS %157 (`20260823.010937`) -- the record of an instrument became field, and the meter rose because the round explained itself well.** *What went wrong:* the rostered `dated_path` guard stood red at HEAD with `refs_lost=193` against a ceiling of `192` -- a ceiling that has no slack and only ever falls. Nothing had broken. `tools/fixtures/tracked_link_control.sh` plants the filename `20260101-000000_a-dated-note.md` to prove dated testimony passes free, and the shared exclusion list keeps that control out of the corpus by name. The **session log** narrating why then quoted the planted name in an `obs` field, and the census -- which reads every file in the tree -- counted the quotation as a real reference resolving nowhere. *What caught it:* running the roster and finding four reds, then stashing the round to learn that this one predated it. *What it taught:* **an exemption written against a file cannot follow the fact out of that file.** The exclusion list's own test is *is this the field, or the instrument?*, and a planted name is instrument wherever it is written -- in the control, in the log that explains the control, in the ledger row that books the lesson. The exemption belongs to the **name**, not to its address.

**REDS %157 CLOSED (`20260823.010937`) -- the exemption follows the name.** *The repair, on metal:* [`../tools/fixtures/dated_path_exclusions.sh`](../../tools/fixtures/dated_path_exclusions.sh) gains `DP_FIXTURE_BASENAMES` and a `dp_fixture_basenames` helper, and [`../tools/fixtures/dated_path_scan.sh`](../../tools/fixtures/dated_path_scan.sh) subtracts each listed basename wherever it is quoted -- the same shape `REDS %139` already chose for a path asserted absent, one step further out. The subtraction is safe precisely because such a name is built to match nothing: a stamp of all zeros names no lap this tree ever ran. The census returned to its exact `192` baseline and the ceiling stayed where it was, which is the discipline -- a ceiling that rises to meet a number has stopped being a ceiling. `tools/d/dated_path_witness.rish` GREEN on metal, the one-list rung included, so the census and the repointer still read one list and neither has drifted back to a private copy.

**REDS %158 (`20260823.010937`) -- the new guard's own control planted what the guard refuses, and the guard reddened on its proof.** *What went wrong:* the first roster run after seating `phantom_path` returned it red, naming one phantom: `tools/fixtures/phantom_path_control.sh -> construction/ITINERARY.md`. That literal is planted on purpose, written into a throwaway repository to prove the refusal bites. *What caught it:* the roster, on the very run that seated the guard. *What it taught:* the lesson `REDS %157` booked minutes earlier, arriving again inside the same lap and against a different list -- **a control is instrument, and every meter that walks the tree meets one.** A lantern that fires twice becomes a loom, so the exemption is written into the scan's own named rules rather than remembered: a `*_control.sh` fixture is skipped, and the refusal stays proven because the pen names its planted tool `stale.sh`, so the exemption reaches the control and never the case it builds. GREEN on metal at `phantom_paths=0` with all eight control behaviors still agreeing.
