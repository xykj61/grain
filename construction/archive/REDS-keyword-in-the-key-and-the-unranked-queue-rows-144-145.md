# REDS -- the keyword in the key and the unranked queue, rows %144-%145

**Language:** EN
**Stamp:** folded `20260822.231949`
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Living pin:** [`../REDS.md`](../REDS.md)

Two rows about a fold meter reading its own field wrong: the visibility keyword
hashed into the body key, so one rule written `pub fn` in some rungs and `fn` in
others read as two families; and a fold queue whose head was a shortlist a hand
kept rather than a ranking of the whole field. Both were booked and closed on
`20260822`, and both moved here as row %149 was booked and the living pin stood
2,050 bytes over its 24,576-byte bound.

**REDS %144 (`20260822.160421`) -- both fold meters hashed the visibility keyword into the body, so one rule written `pub fn` in some rungs and `fn` in others read as two families, and the fold queue was ordered on a partial count.** *What went wrong:* [`../tools/fixtures/caravan_ladder_carry_scan.sh`](../../tools/fixtures/caravan_ladder_carry_scan.sh) and [`../tools/fixtures/ladder_reach_visibility_scan.sh`](../../tools/fixtures/ladder_reach_visibility_scan.sh) both key a body by its own exact text, signature included, so the leading `pub ` sat inside the hash. `elder_waits` is written byte for byte in fifty-one rungs of the Caravan ladder, forty-three publishing it and eight keeping it, and the meters read forty-three and eight rather than fifty-one. Fifteen families across the ladder stood split the same way, hiding **112 carried lines** from a ceiling whose whole job is to count them, and the reach scan's sink window was worse: it read `^pub fn` alone, so private duplicated bodies were invisible to it entirely and the fold line's foreclosure read **235 lines against a corpus of 61,873** where the truth is **1,140 against 69,379**. A rung the harness imports forecloses nearly five times what the arc believed. *What caught it:* reading the `elder_waits` family by hand before lifting it, at the head of the lap the prior log had named it for -- the same way REDS %140 was found one meter-blindness earlier. *What it taught:* **the lantern had already fired, and was answered with a note rather than a check.** The carry scan's own doc block carried a paragraph naming this exact split -- that `fill_table` printed as thirty-seven rungs while forty-three actually held it, the six left out differing by the word `pub` and nothing else -- and closed by asking the reader to read past the queue's own answer. A caution a reader must remember is a blindness with a note attached. A lantern that fires twice becomes a loom, and this one had been left as a lantern wearing a label.

**REDS %144 CLOSED (`20260822.160421`) -- the keyword left the key in all three windows, and the correction found real cost rather than a larger fold.** *The repair, on metal:* both scans now strip a leading `pub ` from each line before the body becomes a hash key, and the reach scan's sink window reads `^(pub )?fn` where it read `^pub fn`, so a private body is counted beside a public one in every window either scan opens. Visibility stays a separate fact about who may call a body rather than part of what the body says, which is the whole claim: a body duplicated is a rule a reader must change in every copy, whoever may call it. Two generated controls prove the merge, one per scan -- two rungs writing one three-line body, one publishing it and one keeping it, which must read `identical=2` from the reach scan and `CARRY_BODIES 2 distinct=1 copies=1 carried_lines=3` from the carry scan. **The carry ceiling rose 63,852 -> 63,964 to meet the corrected reading**, which is what an honest correction looks like against a ratchet that otherwise only falls, and it is named here so no later reader mistakes it for carry coming back. **The correction found cost rather than freedom, and that is the evidence it is a correction.** `elder_waits` reads fifty-one identical bodies where it read forty-three, and the five extra rungs standing above the fold line each keep their own `Table` private, so folding them would publish a record type five rungs chose to keep -- the honest lift still leaves those five at home, and the row now prices `widens_const=5` beside its forty-eight function openings. Every other family the arc has pinned -- `may_pass`, `choose_pass`, `sibling_waits`, `door_admits`, `finding_path`, `load_one`, `read_own_line` -- stands exactly where it stood. Caravan choir GREEN at 108, era suite GREEN at 12.

**REDS %145 (`20260822.194931`) -- the fold queue's head was a shortlist a hand kept, not a ranking, and three families that returned more than any row on it had never once been asked.** *What went wrong:* [`../tools/ca/caravan_ladder_reach_visibility_witness.rish`](../../tools/ca/caravan_ladder_reach_visibility_witness.rish) asserted three or four named families every lap and its GREEN line said *"the fall queue leads with"* whichever of them returned most. A claim about a leader is a claim about a whole field, and this one was measured over the handful of rows already written into the witness. Worse, that handful had been seeded from the carry meter's own top rows, and the two meters do not order the same way: **carry** is `(rungs - 1) * lines` while a **fall** is `(folding - 1) * (lines - stub)`, so carry over-weights breadth and a narrow family holding a long body scores low there and high here. `mind_the_impasse` (eight rungs of fifty-three lines), `hold_the_matter` (seven of fifty-nine), and `bound_the_relay` (seven of fifty-five) each return more than the shortlist's own head and appear nowhere in its carry-ranked window. *What caught it:* measuring `suits_path` by hand while rotating the pins after the `appraisal_note` lift -- a family sitting in the carry meter's printed tail returned 350 where the asserted head returned 330. *What it taught:* **an ordering is a measurement, and a leaderboard drawn from the rows you already know about is an impression wearing a measurement's clothes.** This is REDS %130's own lesson one level up: that row booked a *cost* described rather than measured, this one books an *ordering*. A lantern that fires twice becomes a loom.

**REDS %145 CLOSED (`20260822.194931`) -- the whole field is ranked now, exactly rather than sampled, and the correction demoted the standing head three places.** *The repair, on metal:* [`../tools/fixtures/ladder_reach_visibility_scan.sh`](../../tools/fixtures/ladder_reach_visibility_scan.sh) gained a **`queue` mode** that builds every family of two or more identical bodies, ranks them by carry, and measures fall down that order. The search is **exact rather than sampled** because carry bounds fall from above -- `folding <= rungs` and `lines - stub <= lines`, so `fall <= carry` always -- and it stops when the next carry falls to the **depth-th best fall** measured, since a top-of-K list is complete only when nothing unexamined could displace its Kth row. Bounding on the *best* fall instead would name the head correctly and truncate the tail wrongly: `bound_the_relay` at 312 went unseen behind a head of 350 while `term_written` at 270 printed above it. **Forty-eight of seven hundred families answer for all seven hundred.** Two generated controls prove it on metal: a ladder where no two rungs write one body must refuse `verdict=no_foldable` rather than print an empty list and answer ok (REDS %97), and a control whose two orderings **disagree** -- `broad` at twenty rungs of four lines carrying 76 and returning 19, against `deep` at three rungs of twenty-one lines carrying 42 and returning 36 -- must name `deep`, so a meter that silently reverted to carry would refuse. The living queue now reads `head=mind_the_impasse fall=350`, with `answered_by` demoted from first to fourth, and the ordering stands **asserted** rather than remarked on. Caravan choir GREEN.
