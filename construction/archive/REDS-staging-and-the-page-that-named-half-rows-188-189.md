# REDS -- staging is not shipping, and a page that named half its directory, rows %188-%189

**Language:** EN
**Stamp:** folded `20260824.095920`
**Style:** Gauge, Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Living pin:** [`../REDS.md`](../REDS.md)

Two rows about work that was real and unheard. The first is a round that ended at
`git add` rather than at the commit, so three guards that read the tree went red on
the next lap's cold open with the repair machinery already armed and simply not
reached. The second is the largest page in the tree naming 112 of the 227 modules
standing beside it. Both were folded here on `20260824.095920` when row %191 carried
the living pin past its 24,576-byte bound.

---

**REDS %188 (`20260824.082144`) -- the lap staged its work and stopped, so three guards that read the tree went red on the next lap's cold open.** *What went wrong:* the round that seated the 42nd standing guard ended with eleven paths staged and nothing committed. Three consequences followed, and each is a guard doing exactly its job one lap later than it could have. `readme_metrics` read the front door's generated block at **1669** witnesses while a fresh render read **1670**, because the round had added one. `geode_libraries` read one witness short in five rooms at once -- constel 33 against 34, crypto 114 against 115, glow 104 against 105, image 246 against 247, lotus 239 against 240 -- for the same reason. And `tracked_link` refused `construction/ITINERARY.md`, which cites `../active-designing/20260824-080208_the-roster-that-decides-what-gets-measured.md`: the essay was written, cited from a living card, and never added, so the promise resolved on this pier and would break in every clone. *What caught it:* the cold roster run that opens a lap, reading **42 guards, 39 green, 3 red**, against the previous lap's own cold reading of 41 of 41 green. *What it taught:* **staging is not shipping, and this tree's repair machinery is keyed to the commit rather than to the edit.** Two of the three reds are precisely what [`../tools/hooks/pre-commit`](../../tools/hooks/pre-commit) exists to prevent -- it regenerates both generated pages and stages them into the same commit whenever a witness is added, seated for REDS %151 and %152, which are this same class caught twice before. It is armed here: mode `100755`, with `core.hooksPath` naming `tools/hooks`, and [`../tools/g/generated_page_freshness_witness.rish`](../../tools/g/generated_page_freshness_witness.rish) proves it on throwaway repositories. It fires at `git commit`. A lap that ends at `git add` stops one step before the step that makes its own tree true, and the standing directive that would have named the gap anyway -- *roster cold, then hot* (REDS %174) -- was not run hot. *Repaired:* `git add -A` for the essay; both generated pages regenerated with their own writers (`readme_metrics.rish write`, `geode_libraries.rish write`); all three guards GREEN on metal, and the whole staged round shipped as one signed commit whose pre-commit hook found nothing left to refresh. *Prevention:* already whole, which is the honest finding here rather than a gap. The generated-page half has its hook and its witness; the citation half has `tracked_link`, doing its founding job from a new direction -- a file never added rather than a symlink gone stale. What was missing was reaching them, so the operator card's standing block now says a lap ends at the commit rather than at `git add`. **No guard can enforce that**, because such a guard would have to run after the lap ends, and naming the limit is better than pretending a check covers it. Measured beside it, since the reason a directive gets skipped is usually its price: the three census guards together run in **67 seconds**. CLOSED.

**REDS %189 (`20260824.084007`) -- the largest page in the tree named 112 of the 227 modules standing beside it, and two of the 112 lived somewhere else.** *What went wrong:* `image/README.md` stood at **400,042 bytes** over 288 lines -- the largest document anywhere in this tree, and the page a reader enters the Open Image module by. It named **112** distinct `.rye` files while **227** stand in `image/`, so **117 modules had landed with nobody adding a line**: the whole Season G shape-analysis, selection, and scene-structure arc, from `threshold.rye` and `label.rye` through `watershed.rye`, `hu_moments.rye`, the nine `click_*` gestures, the four `lasso_*` gestures, the fourteen `cluster_*` modules and the thirteen `line_*` modules. Two of the 112 were worse than absent: `dexter_line.rye` and `scooter_compose.rye` were cited by bare name beside local ones, and both live in `pond/apps/`, so the page read as if they stood in this directory. The page had also never joined the DOOR roster, so no meter had read a line of it. *What caught it:* `wc -c` over every tracked `README.md` on the previous lap (`%187`), then `comm` between `ls image/*.rye` and the names the page cites -- 112 against 227 in one line. *What it taught:* nothing new, and that is the finding. **This is REDS %184 at three and a half times the scale, in the directory the previous lap's own ratchet pointed at.** A roster a program runs gets checked; a roster a reader uses gets checked by nobody. **248** `*_witness.rish` files across the tool rooms name a path under `image/` -- 247 before this round's own guard joined them -- so not one of the 117 modules was unproven, only unlisted. The new half is a corollary worth writing down: **a page that names some of a directory is read as naming all of it, and the larger the page the more completely it is believed.** At 400,042 bytes nobody was going to count. *Repaired:* the page split into a **Door front page of 6,928 bytes** and four pages beside it in the same directory, so every relative link inside them stays true -- [`../image/PHOTOS.md`](../../image/PHOTOS.md) (the gesture arc), [`../image/MARKETPLACE.md`](../../image/MARKETPLACE.md) (the catalog and its query algebra), [`../image/PRIMITIVES.md`](../../image/PRIMITIVES.md) (the Season G color and type floor), and [`../image/MODULES.md`](../../image/MODULES.md), which carries **227 rows in twelve families**, each sentence written from that module's own `//!` head comment. The three `pond/apps/` citations are repointed to `../pond/apps/` so a bare name never again reads as local. *Prevention* seated as the **43rd** standing guard: [`../tools/i/image_module_roster_witness.rish`](../../tools/i/image_module_roster_witness.rish) over [`../tools/fixtures/image_module_roster_scan.sh`](../../tools/fixtures/module_roster_scan.sh), gating `unrostered`, `phantom`, `duplicate_rows` and `mismatched_rows` at zero, matching the **Module column** rather than any `.rye` link, and refusing an empty corpus so a reading over nothing can never report clean. Proven RED on metal against the real page by deleting the `watershed.rye` row (`unrostered=1`, `verdict=roster_incomplete`) and GREEN after, and both ways by [`../tools/fixtures/image_module_roster_control.sh`](../../tools/fixtures/module_roster_control.sh) -- **fifteen behaviors on real directories** in a throwaway pen, nine refusals bitten and six honest readings left free. Three carry the load here rather than in Caravan: a name carrying digits binds its row (forty-eight `font5x7_*` modules do), a **seam symlink counts as a module** and is refused when unrostered (`parse_int.rye` and `tally_copy.rye` point into `../tally/`), and a page naming half its directory counts every absentee. *And the meter bit the repair on the lap it was written:* the new Door page joined `tools/fixtures/prose_register_scan.sh`'s DOOR roster and read **27% negative sentences against the 20% ceiling**, swept to **13%** with every claim held exactly -- `%185`'s rule working the first time it was given the chance. The over-bound ratchet falls **7 to 6**, and its control now plants six free and seven bitten. CLOSED.
