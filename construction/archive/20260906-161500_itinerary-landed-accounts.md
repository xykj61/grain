# Landed accounts -- folded from the live front `20260906.161500`

*Held word for word from [`../ITINERARY.md`](../ITINERARY.md), so the card carries what is OPEN and
what waits on Keaton's word. GRASS's landed half, folded in the round that closed the two-rooms
doorway guard. The block's one relative link is re-anchored a level deeper here (`%474`), which is
the correction a fold owes every link it moves.*

---

**GRASS -- three findings, all CLOSED**, whole on the
[`133344` shelf](20260906-133344_itinerary-landed-accounts.md). **A guard's precondition
belongs in its roster row** (`%493`): `sow_allow_reach` needs a `seed/` no clone carries, so at
`tier lap` with no `capability` it red every tree and each ship paid a full cold pass a lap. **A law
is not kept by the room that writes it:** 63 of 104 rule pages held 859 non-ASCII characters, every
one on the rule's own table; `ascii_document` enforces both rooms at zero and ratchets **347** pages
at **3,956**. **A link quoted in backticks cites nothing** -- 30 shapes over 12 pages, all
teaching the fold rule.

---

*The carried find travels forward rather than folding: **890** depth-lost links in the two-level
rooms, a refusal nothing tracked can read. It stands on the live front beside the round that
follows this one, because it is open work rather than a landed account.*
