# REDS %197 and %198 -- a bound with two meters and no wall, and a byte that is not a character

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.140523`, both **CLOSED** --
the pin the unattended loop reads first standing at 1.92x the bound it declares, with two meters
reading the law and neither refusing; and five tracked specs carrying a lone orphan UTF-8 lead
byte, two of them living. Moved here as row %199 carried the living pin past its 24,576-byte
bound. Link depth moved with the text.*

---

**REDS %197 (`20260824.130807`) -- the pin the unattended loop reads first had a bound, two meters, and no wall, so it grew past it twice.** *What went wrong:* `construction/ITINERARY.md` stood at **47,213 bytes** against `living_pin_max_bytes` (**24,576**) -- 1.92x. It had been condensed to **41,449** on `20260824.060012` and grown back in five laps, and the lap that condensed it wrote *"the condensing wants to become a habit with a measurement rather than an occasional sweep"* and then left. Two meters read the law and neither refused this page: `tools/gen/season/equinox_e123_living_pin_guard_witness.rish` gates the bound over a **hand-written roster of seven paths**, on which this card was marked **`advisory`** and `session-logs/README.md` did not appear at all; and `tools/l/living_docs_lint.rish` duty 6, which discovers sixty documents and by its own header **always exits 0**. *What caught it:* the roster of `**Bound:**` declarations, grepped once. **Thirteen living pages declare `**Bound:** under `living_pin_max_bytes`` in their own header, and exactly one of them -- `construction/REDS.md` -- sat on the enforcing roster.** *What it taught:* **a hand-written roster reaches the pages somebody remembered, and a page nobody remembered is a page nobody measures.** This is REDS %187's shape a fourth time (a roster reaching 6 of 34 doors, then %190, then %192), arriving now on the two documents the unattended loop reads first every lap. And the sharper half, which REDS %195 named one lap earlier in a different room: the *declaration* is the thing to discover. A page that writes down its own limit has already told the tree what to measure it against, so the roster does not have to be remembered at all. *Repaired:* the card condensed **47,213 -> 21,859 bytes**, every released block held word for word at [`archive/20260824-130807_itinerary-settled-decisions.md`](20260824-130807_itinerary-settled-decisions.md) and the walk-back at checkpoint `d6c02f92cc`; a duplicate equinox table and a stale second copy of the seed's four-gate reading both fused, since a card carrying two copies of one fact is the single-strandedness this card exists to keep. `construction/ITINERARY.md` moved **`advisory` -> `enforce`** on the pin roster, and both walls were proven to bite: 4,000 bytes appended, `declared_ceiling` refusing with `over_declared_bound` naming the page and both numbers, `equinox_e123` exiting 1, the card restored byte-identical. *Prevention:* [`../tools/d/declared_ceiling_witness.rish`](../../tools/d/declared_ceiling_witness.rish), the guard seated one lap earlier for the `**Ceiling:** <=N lines` form, now reads the `**Bound:**` form beside it -- **one roof for a declared limit whatever its unit**, rather than a third roof over one law. It discovers **14** declaring pages, gates a spelled number disagreeing with the seated law at zero, holds the one page over its bound (`session-logs/README.md`, **280,142 bytes**, whose rows fold only when its logs fold and whose log fold runs on Keaton's word) under a ceiling that only falls, and counts the two honest prose bounds (`keep thin`, `listings stay sentence-cheap`) rather than calling them faults. The number is **read from the seated law** at `context/specs/20260724-132812_pin-and-ledger-living-pin-max-bytes.md` rather than copied a fourth time. Proven by [`../tools/fixtures/declared_ceiling_control.sh`](../../tools/fixtures/declared_ceiling_control.sh) across **twenty-two behaviors and thirty-seven assertions** on real pages in a throwaway pen, both bounds shown from both sides. *Named, not fixed:* three scripts still carry their own `24576` (`living_docs_lint_scan.sh`, `equinox_e123_living_pin_guard_scan.sh`, `index_fold_scan.sh`); repointing them at the law is its own lap. CLOSED.

**REDS %198 (`20260824.130807`) -- four tracked specs carried a byte that is not a character, and one of them is the law the room folds rest on.** *What went wrong:* `context/specs/20260724-132812_pin-and-ledger-living-pin-max-bytes.md`, `context/specs/append-only-growth-law.md`, `context/specs/oldness-cycle.md`, `context/specs/20260725-185041_grain-umbrella-supersedes-glow-os.md`, and `context/specs/20260801-140607_resin-manifest-v1.md` carried a lone **0xC2** -- the lead byte of a middot whose continuation byte an earlier ASCII sweep replaced with `-` (and once with the word `section`), leaving an orphan that no encoding can decode. Five files, four of them found by the sweep and the fifth by the same reading. Two are living specs; one is `append-only-growth-law.md`, the three-level growth law every room fold in this tree rests on, and another is the spec that seats `living_pin_max_bytes` -- which REDS %197's new guard had just started reading its number from. *What caught it:* a plain `grep -n "Lint"` over the bound spec returned nothing while the words stood plainly in `cat`. *What it taught, after a second look corrected the first:* the first reading was that every text meter in the tree was blind to these files, and that reading was **wrong**. Measured both ways: **GNU grep 3.12**, which every script here runs, reads an orphan lead byte correctly and no guard was blinded; **ugrep 7.8.4**, which this bench's interactive shell resolves `grep` to, classifies the file binary and returns silence with exit 1 -- which is byte for byte what a clean file with no match looks like. So the cost was borne by a hand searching the tree rather than by the guards, and **the honest gate is on the corruption rather than on either tool's verdict about it**, since a gate written against a tool moves when the tool moves. *Repaired:* all five orphan bytes, byte-exact, touching nothing else -- a corruption repair rather than a style rewrite, which is what `.claude/rules/ascii-first.md` already licenses wherever mojibake is found, dated testimony included. *Prevention:* [`../tools/l/living_card_ascii_witness.rish`](../../tools/l/living_card_ascii_witness.rish) -- REDS %83's own guard, whose subject has always been byte encoding -- gains a third reading over **13,509 discovered tracked text files**, gating **invalid UTF-8 at zero**. Its probe proves itself from both sides on planted files in a throwaway pen before it reads the tree, and the RED path was proven on the real tree by appending an orphan byte to a tracked roster and watching the guard name that path. The guard's own `grep` calls gained `-a` so its answer is the same whichever grep is installed. CLOSED.
