# REDS -- the unpriced queue and eight drifting guards, rows %146-%147

**Language:** EN
**Stamp:** folded `20260822.231949`
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Living pin:** [`../REDS.md`](../REDS.md)

Two rows about meters that had stopped describing what they watch: a fold queue
ranking families by what each returns and never by what it costs, so its head
named a row the discipline had stepped past by hand two laps running; and eight
standing witnesses each holding a count or a literal its source had moved past,
none of them run for as long as three weeks. Both were booked and closed on
`20260822`, and both moved here as row %149 was booked and the living pin was
carried back under its 24,576-byte bound.

**REDS %146 (`20260822.213621`) -- the fold queue ranked by what a family returns and never by what it costs, so its head named a row the discipline had stepped past by hand two laps running.** *What went wrong:* [`../tools/fixtures/ladder_reach_visibility_scan.sh`](../../tools/fixtures/ladder_reach_visibility_scan.sh) gained a `queue` mode one lap earlier that ranks every foldable family by fall, closing REDS %145. It ranks by fall alone. `answered_by` stood at its head with a fall of 330 and a price of **68 openings** -- 34 private functions and 34 private constants that 34 rungs each chose to keep -- while `recant_note` beneath it returned 324 for nothing. Both laps the bench read the price in the row, declined the head, and took the free row below it. *What caught it:* reading the queue's own columns before taking its head. The price was printed the whole time; the ordering simply never used it, so the meter's recommendation and the arc's practice had quietly disagreed since the mode was written. Every lift this arc has taken widened imports and nothing else -- `door_admits` at 86, `read_own_line` at 126, `elder_waits` at 43, all import bindings -- and it has never once published a function or a record type a rung kept private. *What it taught:* a ranking answers the question it was built to answer, and "what returns the most" is a different question from "what should I take next." When a hand corrects a meter's answer twice, the correction belongs in the meter.

**REDS %146 CLOSED (`20260822.213621`) -- the queue names a free head beside its best one, and the arc's first price-chosen lift landed on it.** *The repair, on metal:* `QUEUE_OK` now prints `free_head` and `free_fall` beside `head` and `fall` -- the best fall among families whose `widens_fn` and `widens_const` are both zero -- and every `QUEUE_ROW` carries `widens_fn` and `widens_const` beside its summed `widens`, so a reader prices a row without a second run. Import widening stays free by measurement rather than by assumption: it is the only kind this arc has ever paid. **Both numbers print and neither is chosen here** -- a lap that judges a price worth paying still reads `head`, and the meter stops pretending the two are one answer. A generated control proves the RED path: three rungs of twenty-one lines whose reached `helper` each keeps private return 36 at a price of three function openings, three rungs of eighteen lines return 30 for nothing, and the mode must name `head=costly fall=36 free_head=frugal free_fall=30` rather than costly alone. **The correction chose this lap's own lift:** `recant_note` folded on the free head -- 37 rungs, a twelve-line body to a three-line stub, carry **59,474 -> 59,150**, a fall of **324** predicted to the digit for the eleventh lap running. The priced head `answered_by` still stands first by return and still waits, honestly named as costing 68 openings. Caravan choir GREEN at 108.

**REDS %147 (`20260822.223123`) -- eight standing witnesses had drifted behind the code they guard, each holding a count or a literal its source had moved past, and none of them had been run for as long as three weeks.** *What went wrong:* the `|+` **barlus** rune head entered `glow/tokens.rye` `match_rune2` on `20260802` with the `sumto` folding gate, and the digraph count **27** stood written in six other places that were never brought along -- `tools/fixtures/glow_digraph_table.txt`, `tools/fixtures/glow_digraph_table_wrong_order.txt`, the `rows='...'` cord of `glow/gen/sample-digraph-table.glow`, `assert(count == 27)` in `glow/digraph_twin_check.rye`, four asserts in `tools/g/glow_digraph_twin_witness.rish`, and two in `tools/g/glow_rune_alphabet_worker.sh`. Beside it, three more of the same shape: `brushstroke/brush_parse.rye` grew `ParseError` from ten members to thirteen and `BrushSurface` from four fields to five while `src/sur/shape-brush-parse-error-count.glow`, `src/sur/shape-brush-surface-field-count.glow`, and `tools/gen/season/src_first_resident_witness.rish` held the old numbers; `tools/gen/season/sunn8_root_readme_witness.rish` grepped `README.md` for three literals a Radiant pass had reworded, and demanded the front door name `Q-vane`, a word retired to **Lumen** on `20260816`; and `tools/gen/season/sunn13_root_survey_witness.rish` demanded all three licenses name a **retired prior name**, so a hand repairing a license to satisfy the guard would have written that name back into the tree the identity sweep had cleared it from. *What caught it:* running the choirs at a cold start, which is the standing-equipment habit doing exactly its job -- `tools/gen/season/glow_hygiene_pin.rish` and `tools/gen/season/sunn14_witness_choir.rish` both reded on the first ask. *What it taught:* **a number written in seven places is a number that can disagree with itself, and a guard nobody runs is a guard that drifts silently into demanding the wrong thing.** The Caravan ladder arc has been proving the first half all month in one module; this red is the same finding pointed at the whole tree. The second half is sharper and newer: a stale guard is worse than an absent one, because its RED points a repairing hand at the *source* rather than at the *pin*, and the sunn13 case shows where that road ends.

**REDS %147 CLOSED (`20260822.223123`) -- every pin now reads what its source actually says, the missing rune has its name, and all eight witnesses run GREEN on metal.** *The repair, on metal:* `|+` is seated as **barlus**, the 28th G1 digraph row, in [`../active-designing/20260822-221639_glow-barlus-g1-row.md`](../../active-designing/20260822-221639_glow-barlus-g1-row.md) -- the two-syllable rule every bar row keeps, `|` bar and `+` lus -- and all seven `27` sites read **28**, including the pronunciation table row and the count claim the alphabet worker greps for. `ParseError` reads **13** and names its three new members by name in the desk prose; `BrushSurface` reads **5** and names `style_count`. The three README asserts bind to the sentences the front door actually carries, and the Q-vane assert is retired with its reason written in place rather than deleted. The three license asserts bind to `Copyright ... Keaton Dunsford and contributors`, the name the tree holds today. GREEN on metal: `tools/g/glow_digraph_twin_witness.rish`, `tools/g/glow_rune_alphabet_witness.rish`, `tools/gen/season/glow_hygiene_pin.rish`, `tools/gen/season/src_first_resident_witness.rish`, `tools/gen/season/sunn8_root_readme_witness.rish`, `tools/gen/season/sunn13_root_survey_witness.rish`, `tools/gen/season/sunn14_witness_choir.rish`, and the 108-rung `tools/ca/caravan_suite_witness.rish`.
