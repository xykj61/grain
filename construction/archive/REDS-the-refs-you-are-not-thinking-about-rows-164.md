# REDS -- the refs you are not thinking about (row %164)

**Language:** EN
**Style:** Gauge -- Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- CLOSED `20260823.072824`, every byte kept
**Folded:** carried here from [`../REDS.md`](../REDS.md) on `20260823.134057` as row %165 was booked and the living pin passed its 24,576-byte bound.

**REDS %164 (`20260823.072824`) -- a history rewrite that touches one branch leaves the material on every other.** *What went wrong:* the deep debride removed the personal material from all 3,314 commits of `main` and force-pushed both remotes, and the job was **still unfinished**. `xykj61` carried **159** stale `cursor/*` branches whose remote copies held the old history; a targeted fetch of one into a throwaway repository proved it, reading **three of the personal files present in its tree right now** rather than merely in its log. `origin` carried `image-checker-paint`, and that branch was worse: **38,355 commits**, because it roots in the urbit lineage that the `20260817` debride dropped from `main` and from `main` alone. So the elder lineage drop was incomplete in exactly the same way, and had been for six days. *What caught it:* enumerating every ref before force-pushing, rather than pushing `main` and calling the debride done. *What it taught:* **a history rewrite is scoped to the refs you name, and a remote holds refs you are not thinking about.** The rewrite tooling is honest about this -- `filter-repo` rewrote every local ref correctly -- yet a rewritten local branch changes nothing on a remote until it is pushed or deleted, and 160 branches nobody had touched in three weeks were quietly holding what the whole operation existed to remove.

**REDS %164 CLOSED (`20260823.072824`) -- both remotes hold one branch each.** *The repair, on metal:* the 159 `cursor/*` branches were deleted from `xykj61` on Keaton's word -- spent working branches from a superseded workflow, dated `20260730` to `20260801`, two of 159 merged, and the Cursor era they belong to has given way to Claude Code on this pier. `image-checker-paint` was deleted from `origin` and locally after proving it redundant rather than assuming it: `image/checker_paint.rye`, `tools/ch/checker_paint_witness.rish`, `image/README.md`, and its session log all stand on `main` already, the log folded to `session-logs/date/20260817/`. Its only unique content was 35,486 commits of dropped lineage. Verified after: **zero** local refs hold any of the four paths, **3,315 of 3,315** commits on `main` verify `G`, and both `origin` and `xykj61` list `refs/heads/main` and nothing else. The four paths stand on disk for their author, invisible to `git status`, and the pre-debride state survives in a verified 559 MB bundle named by checkpoint `20260823.072824`.
