# REDS -- the roster that made itself red (row %172)

*Folded from [`../REDS.md`](../REDS.md) on `20260823.174500`, **CLOSED**, as row %174 carried the living pin past its 24,576-byte bound.*

**REDS %172 (`20260823.161500`) -- running the roster is what made the roster red.** *What went wrong:* `phantom_path` answered red on a sound tree, naming `linengrow/lib_session_root_nest_cue_jam.rye` read by two witnesses. That path is a **symlink into `glow/.cache/` that those two witnesses create while they run**, and `.gitignore` line 196 names it. So the guard was honest about what it saw and wrong about what it meant: the roster's own run manufactured the artifact, and the next run reported it. *What caught it:* the roster, on the cold run this lap opened with -- 30 green, one red, and the red pointing at a file `git status` refuses to show. *What it taught:* **a path git is told to ignore is a declared absence, so a reader who clones is promised exactly what they get.** The scan already carried two hand-named versions of that idea -- `seed/`, and any path with a dot-segment -- each written as a guess about which rooms are generated. The general rule asks git instead: `git check-ignore -q`, consulting `.gitignore`, the excludes file, and every nested rule at once, on the handful of literals that resolve here and stand untracked. The two elder rules stay written, for the reader and for the pass that never has to shell out. *Proven both ways on metal:* the control pen builds the exact shape -- a lowered `.rye` in `glow/.cache/`, a symlink into `linengrow/`, a `.gitignore` line naming it, a tool reading it -- and checks it passes free; then builds the same shape with the ignore rule **removed** and checks it is refused. Ten cases now, three refusals bitten and seven honest readings left free. CLOSED.
