# REDS %203 and %204 -- a fault reproduced by the round explaining it, and a bound folding could not reach

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.190000`, both **CLOSED** -- four
rounds each leaving a reference that resolved nowhere, every one of them a fabricated path quoted to
illustrate a fabricated path; and a pin that folded correctly and still stood 5,421 bytes over the
number it declares. Moved here as row %205's closure carried the living pin past its 24,576-byte
bound. Link depth moved with the text.*

---

**REDS %203 (`20260824.174500`) -- four rounds of writing each left a reference resolving nowhere, and the guard that counts them was already above its ceiling when the fold asked to ship.** *What went wrong:* the mark law is explicit that **no fold ships without `tools/d/dated_path_witness.rish` GREEN**, and on the word to fold `session-logs` the witness was **RED**: `refs_lost=191` against a ceiling of **187**. Four references had arrived since the ceiling was set, and every one came from a round explaining itself well. Two reproduced the fabricated path of REDS %202 -- once in the ledger row describing that fault, once in the session log narrating it. One was a name planted by `commit_message_guard_scan.sh` to prove the new commit-msg wall bites, quoted where it had to be. One was a session log naming a sibling log that was never written. *What caught it:* running the witness before the fold rather than after, because the law says to. *What it taught:* **a round that documents a broken citation reproduces it, and the census cannot tell the illustration from the fault.** The mark law had already booked this shape -- *illustrate with placeholders, cite only what exists* -- and named three fabrications on the day it was written, in the law itself, in a witness, and in a research note. It fired again here **inside the guard written to prevent it**, which is the sharpest form it has taken. The transferable rule is narrower than *check your paths*: **when prose must name a path that does not exist, name the stamp or the shape rather than reproducing the whole path**, because a path-shaped string is a citation to every reader and every tool regardless of the sentence around it. *Repaired:* the ledger row now names the stamp -- `20260824.165520` against the log's real `20260824.164559` -- rather than reproducing the path; the planted control name joined `DP_FIXTURE_BASENAMES` in [`../tools/fixtures/dated_path_exclusions.sh`](../../tools/fixtures/dated_path_exclusions.sh), which is the seated mechanism for a name **built to match nothing** and now holds six; and three genuinely wrong citations in living pages were repointed -- two in [`../gratitude/toyota-production-system.md`](../../gratitude/toyota-production-system.md) naming `20260729-222500` where the tree carries `20260729-224828`, **the same recalled-stamp fault standing in the tree before this round ever made its own**, and one in [`../context/TAME_GUIDANCE.md`](../../context/TAME_GUIDANCE.md) that the Sala -> Seva rename had swept while the dated filename it names correctly never followed. *Not repaired, and honestly so:* the two session logs keep every word they wrote. Dated testimony is never rewritten to lower a meter. **The ceiling falls 187 -> 186** on the repairs rather than rising on the arrivals, and the fold shipped with the witness GREEN. *Named, not fixed:* `DP_FIXTURE_BASENAMES` is a hand-written roster, which is REDS %187's shape -- it reaches the planted names somebody remembered. Whether a planted name can be **discovered** rather than listed is its own round, and the remainder is uncounted here rather than guessed at, since a first pass at counting it returned real documents that had merely moved. CLOSED.

**REDS %204 (`20260824.174500`) -- the law promises the byte bound follows from folding, and one heavy day exceeds it anyway.** *What went wrong:* [`../.claude/rules/stamp-and-name.md`](../../.claude/rules/stamp-and-name.md) states plainly that *the byte bound follows without being asked for -- fold a room to today and its index holds one day of rows.* The `session-logs` fold ran on Keaton's word and did exactly that: 169 logs moved beneath `date/`, 169 rows moved onto their shelves, and the pin fell **291,781 -> 29,997 bytes**, a 90% reduction. It stands **5,421 bytes above** the 24,576 it declares. *What caught it:* measuring the pin after the fold rather than trusting the sentence. *What it taught:* **the bound and the row size are two seated numbers that were never checked against each other.** The prose header takes 2,033 bytes, leaving 22,543 for rows; rows average **798 bytes**, so the pin holds about **28** of them, and `20260824` produced **35**. The rule's own text names roughly a hundred and eight logs a day as the rate this room can reach, which is nearly four times what its index can carry. So the claim is true for an ordinary day and false for a working one, and it was written from the shape of the fix rather than from a measurement of the load. *Repaired:* nothing yet, and that is deliberate. Three answers exist -- shorter rows, an intra-day fold, or a bound that is wrong for this room -- and choosing among them is a decision rather than a measurement. **Surfaced as an open door on the card for Keaton's word.** *What already holds:* `declared_ceiling_witness.rish` reports the page honestly and rides it on a ratchet rather than gating it, which is why the tree stayed green through a page 11x its bound and stays green now at 1.2x. The ratchet counts **pages over**, so it reads 1 both before and after a 90% fall -- a count of pages cannot see a page improve, and that is worth knowing about the meter as well as about the page. CLOSED as recorded; the choice is open.
