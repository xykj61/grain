# REDS -- the fold line, the doorway, and the census, rows %137-%139

**Language:** EN
**Stamp:** folded `20260822.131047`
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Living pin:** [`../REDS.md`](../REDS.md)

*Folded from the living pin when it crossed its 24,576-byte bound as row %141 was booked. All three are closed, and all three are about a claim that stood unmeasured while a guard was available to measure it. %137 found a GREEN line calling the ladder's fold line expensive without ever pricing it, and the price turned out to be small and enormously uneven. %138 found nine forward-facing pages naming a state where the two-room discipline asks for a room, and the doorway guard that says so red in the dark because no standing suite ran it. %139 found the dated-path census reading a path asserted absent as a reference gone missing, so a proof of a clean shed read as breakage. The three sit together because the repair each one earned was the same: put the guard where a lap already listens.*

---

**REDS %137 (`20260822.111500`) -- a witness's own GREEN line priced a cost that had never been measured, and it is REDS %130's lantern firing a second time.** *What went wrong:* lifting `read_own_line` opened a real choice -- reach `queue_store` and `serve` through the rung, or import `queue.rye` and `serve.rye` into the harness and drop the comptime parameter as `load_one` had. The rung parameter was kept, correctly, since it costs nothing. The *reason* written down was that importing either would sink it below the fold line where it could never fold a body again, "a cost the widening column does not price, paid by every family those two rungs might yet join" -- shipped into `caravan_ladder_carry_witness.rish`'s GREEN line and the operator card, with no number behind it. *What caught it:* writing the design essay the discovery seemed to deserve, and reaching for the measurement to open it. The two rungs hold **three body lines each** in foldable families; the entire fold line forecloses **247** lines against a foldable corpus of **65,995**. The claim was true and roughly a thousand times smaller than its sentence implied. *What it taught:* **this arc has now booked the same crossing twice -- REDS %130 was a widening cost described by a hand rather than read off the tree, and this is a fold-line cost described the same way -- so it stops being a lantern and becomes a loom.** The deeper finding the meter gave is the one prose could never have: the holdings spread three orders of magnitude, from three lines to 3,901, so no general rule about the door is safe. Both "always keep the rung parameter" and "always import" are wrong, and the only honest policy is to price the sink before reaching for it.

**REDS %137 CLOSED (`20260822.111500`) -- the fold line is a meter now, and the rule it retires is a rule of thumb.** *The repair, on metal:* [`../tools/fixtures/ladder_reach_visibility_scan.sh`](../tools/fixtures/ladder_reach_visibility_scan.sh) grew a `sink` mode that reads every top-level public body in the ladder, groups them by name and body into families of two or more, and prints what each rung holds -- `SINK_BELOW` for the seventeen already under the line, `SINK_ABOVE` for every rung still over it, largest first, and `SINK_OK` with the totals. [`../tools/ca/caravan_ladder_reach_visibility_witness.rish`](../tools/ca/caravan_ladder_reach_visibility_witness.rish) asserts five of its numbers, the spread's both ends among them, so the claim is a measurement every lap rather than a sentence from one. Two RED legs prove it on generated controls beside the two already standing: a directory holding no public function is **refused by name** rather than answered with a zero (REDS %97's own lesson), and a hand-built pair of rungs sharing one three-line body reads exactly six while the body neither repeats reads nothing. The overstated sentences in the carry witness and the operator card now carry the measured numbers. Choir GREEN at 108, era suite GREEN at eleven.

**REDS %138 (`20260822.112058`) -- nine forward-facing pages carried a Status line naming no room, and the doorway guard had been red long enough that nobody heard it.** *What went wrong:* `context/TWO_ROOMS.md` requires every one-clock-stamped page written after the `20260705.203144` seating to name its register at the door -- `checkable`, `vision`, `mixed`, or `research for understanding` -- so a reader knows which room they are standing in before they weigh a sentence. Nine pages stamped between `20260821.022912` and `20260822.014628` wrote a Status that named a *state* instead: "Living (verification record)", "Living design essay", "Research + recommendation", "Dated read". Each is an honest description and none of them answers the question the door asks. Two of the nine argue design and cite measured counts in the same breath, which is exactly the mixing the two-room discipline exists to make visible. *What caught it:* running `tools/t/two_rooms_doorway.rish` while checking a newly written essay against it. The guard was already RED; removing the new essay and running again returned the same RED, so the failure predated this lap. It sits in `tools/p/parity_ch01.rish` and `tools/r/rishi_list_bound_witness.rish` rather than in the era suite or the Caravan choir, which is why a day of green rounds ran beside it. *What it taught:* **a guard nobody's standing suite runs is a guard that reds in the dark.** The tree already learned this for shared output paths (REDS %133) and for reds living only in chat windows (this ledger's own founding); the same sentence holds for a witness outside every roster. And the narrower lesson is about the Status line itself: a status that says *how alive* a document is answers a different question from one that says *which room it speaks from*, and a page can honestly carry both only if it writes both.

**REDS %138 CLOSED (`20260822.112058`) -- all nine name their room, and each token was chosen by reading the page rather than by pattern.** *The repair, on metal:* each Status line keeps every word it had and gains the room token in front of it -- `Checkable` for the two measured-on-metal reads and the license verification record, `Mixed` for the four that argue a shape while citing measurements they ran, `Vision` for the design proposal with nothing seated, and `Research for understanding` for the dated model read that orients without seating a fact. `rishi/bin/rishi run tools/t/two_rooms_doorway.rish` reads TWO-ROOMS DOORWAY GREEN, every forward page naming its door. The roster half closed in the same lap rather than waiting: the doorway guard now rides the era suite as rung 12 of 12, beside the dated-path census and the repointer that REDS %139 found in the same condition. Three guards that ran from no standing suite now run where every lap already listens.

**REDS %139 (`20260822.113100`) -- the dated-path census counted a path asserted absent as a reference that had gone missing, so a proof of a clean shed read as breakage.** *What went wrong:* the census reads every dated path in the tree and asks whether it resolves. Two season witnesses prove a fossil was shed by writing `test ! -f foundations/<stamp>_<sprig>.md` -- five such paths across `wafer_thin_witness.rish` and `horizon_stack_thin_witness.rish` -- and every one of the five was counted as a lost reference. The path is doing exactly its job by resolving nowhere. `tools/fixtures/dated_path_exclusions.sh` had already written the governing test in its own header, *"is this the field, or the instrument?"*, along with the consequence: counting the instrument "makes a meter rise as its proof gets stronger." The census applied that test to filenames and never to a path's grammar. Two genuine citations had drifted beside them: `tools/h/hunk_photos_convolve_witness.rish` named the 1,024-round itinerary at `20260813-171050` where it stands at `20260812-171050` under `active-designing/date/20260812/`, and `tools/ca/caravan_ladder_copy_witness.rish` cited the widening design read as `_what-a-widening-costs.md` where the file is `_what-a-widening-costs-a-rungs-door.md`. Seven together carried the census from its ceiling of 192 to **199**. *What caught it:* running `tools/d/dated_path_witness.rish` while checking this lap's own work, and finding it already RED with every one of this lap's changes stashed. *What it taught:* **this is the third guard in one lap that had been red before the lap began, and none of the three runs from a standing suite.** REDS %138 named that pattern for the doorway guard; the dated-path guard has the same shape and the same cause. The narrower lesson is that an exclusion rule written as a list of *names* cannot express a rule about *grammar*: a path's meaning depends on the sentence around it, and `! -f` is a different sentence from a citation.

**REDS %139 CLOSED (`20260822.113100`) -- the negation is read, and both drifted citations point where their files stand.** *The repair, on metal:* [`../tools/fixtures/dated_path_scan.sh`](../tools/fixtures/dated_path_scan.sh) takes a second grep pass anchored on the negation itself -- a dated path standing immediately after `! -f` or `! -e`, with or without a leading `test` -- and subtracts each such pair from the corpus for the citing file that wrote it rather than everywhere the path appears, so an ordinary citation of the same name in another file is still counted. The two drifted citations were repointed by reading where their files actually stand. The census reads `refs_lost=192` against `lost_ceiling=192`, `under_ceiling=yes`, and the ceiling stays exactly where it was -- a meter correction that removes a miscount earns no slack, and seven references had genuinely drifted since it was set. `tools/d/dated_path_witness.rish` and `tools/d/dated_path_repoint_witness.rish` both GREEN, the one-list guard among them. And the roster lesson the three shared is closed with them: [`../tools/g/glow_tend_era_suite.rish`](../tools/g/glow_tend_era_suite.rish) grew a twelfth rung carrying the doorway, the census, and the repointer, so all three now run where every lap already listens. The suite reads GREEN at twelve, and the Caravan choir GREEN at 108.
