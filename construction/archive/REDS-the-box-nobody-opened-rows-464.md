# REDS %464 -- the box nobody opened

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.080922`, the row CLOSED and every
other row on the pin still OPEN.*

**What the row taught, in one line:** a channel with a hole in it costs one duplicated lap per hole,
and the obvious probe for whether a record is safe -- `git log --all` -- reaches `refs/stash` and so
reports the dead-letter box itself as history.

The round-open filled a stash at every open and nothing ever read it back. Three of this ship's own
laps had complete session logs parked there, one of them the lap whose code shipped the nib writer,
so the work landed and its reasoning did not. The instrument the row asked for is
[`../../tools/fixtures/f/stash_record_scan.sh`](../../tools/fixtures/f/stash_record_scan.sh), gated
at `unlanded=0` by
[`../../tools/f/stash_record_witness.rish`](../../tools/f/stash_record_witness.rish) and printed at
every open by [`../../tools/f/fleet_round_open.sh`](../../tools/f/fleet_round_open.sh). It reads the
**record** rather than the box, so it greens by putting a log back in the channel and never by
dropping a stash.


**REDS %464 (`20260906.043823`) -- the round-open parks a dead lap's work, and nothing ever reads the stash back, so three laps' records fell out of the one channel this organization has.** *What went wrong:* `tools/f/fleet_round_open.sh` stashes an unsent working tree at the open -- correct, and named on the card as `%321`. The read back is missing. **This row and `%462`'s retraction are two halves of one thing rather than a disagreement:** the stash IS `%321` working and IS the only reason those bytes survived; what is absent is any step that reads them back, so the bytes live and the record does not. Measured `20260906.043823`: **three stashes, 32 files, 455 insertions**, and **each held a complete session log `git log --all` could not find**. One records a lap whose **work landed** (`docs-geode/study/reading-a-name.md` stands), so the code shipped and its reasoning did not; another held a **booked row and its repair**. *What caught it:* `standing_equipment_run.sh`'s own header, `stashed_entries=3` -- not anything a lap reads at its open, where `git status` answers **clean** with three stashes standing. *What it taught:* **a clean status is not an empty tree, and a channel with a hole in it costs one duplicated lap per hole.** Paid three times the same morning: the `04:00:55` lap fixed `reds_ledger_scan.sh`, its round-open stashed that at `04:03:14`, a peer landed the same repair as `%459`, and this seat wrote a third between -- same fix, same design. **Two of three discarded.** *Repaired in part:* the three logs and their shelf rows are restored **byte-for-byte**, SHA-256 verified against the stashed blobs, `index_row_bound` and `log_has_a_row` GREEN; `%450` and `%461` folded to a shelf so this pin has room again. **The mechanism is untouched and is the larger half**; 29 stashed files stay unreviewed, and the card's `%387` asks the neighbour. A cheap first instrument would report, at the open, any stash holding a `session-logs/` path absent from the tree. *Erratum:* booked `%460`, renumbered `%462`, `%463`, then `%464` as peers published each in turn -- **four collisions for one row**, which is the shape it exists to describe, since `--next` reads the anointed remote and a stashed allocation is invisible to it. The stamp is the key. *Repaired whole (`20260906.074112`):* the instrument the row named is built and the box is read at every open. [`tools/fixtures/f/stash_record_scan.sh`](../../tools/fixtures/f/stash_record_scan.sh) walks each `fleet-round-open` stash for session-log paths -- the dated shape `[0-9]{8}-[0-9]{6}[_.]`, sprig optional per `%175` -- and asks whether the worktree, a local branch, or a remote-tracking branch carries each one; `tools/f/fleet_round_open.sh` step 5 prints the count and the command that names them, guarded by the scan's own presence so the open can still never fail. **Gated at `unlanded=0`** by [`tools/f/stash_record_witness.rish`](../../tools/f/stash_record_witness.rish), rostered `tier lap`, **22 pen behaviors** and 3 more on the round-open's own control, every refusal shown from both sides. **The reading is about the RECORD rather than the box**, so a stash reads clean once its log has landed and stays standing: the only way to turn this guard green is to put a record back in the channel, never to drop evidence. *What building it found:* **`git log --all` is the wrong probe in both directions**, because `--all` reaches `refs/stash`. A record STAGED when the lap died rides in `stash@{N}^2` and `--all` reports the stash's own index commit as history -- measured here, `git log --all -- '*20260906-055857*'` answered `98b56e594`, which `git rev-parse 'stash@{0}^2'` returns exactly. A record merely UNTRACKED rides in `^3`, which the default walk prunes, so `--all` answers nothing. Same probe, opposite answers, neither about landing; both reproduced in the pen, legs 6-8. No tool in this tree uses that probe -- `git grep 'log --all' -- 'tools/*'` is empty -- so the finding is preventive and lives in the scan's header where the next builder reads it. *The three records read back:* `20260906.055857`, `20260906.045131`, `20260906.011125`, restored byte-for-byte with SHA-256 verified against the stashed blobs, their own rows re-seated on the `20260906` shelf in descending order, `index_row_bound` and `log_has_a_row` GREEN. **All three are this ship's own laps**, one of them the very lap that landed the nib writer -- so the code shipped and its reasoning stood in the box for two hours. *The row's other half, answered:* the 29 unreviewed files dedupe to **26 distinct**, of which **23 stand in the worktree** and **3 are superseded rather than lost** -- a REDS fold shelf a peer published differently (`%446` and `%459` each stand on their own shelf), and two draft fixtures (`agent_jail_control.sh`, `mount_namespace_probe.sh`) that the landed `%446` close named out of existence, referenced by nothing the tree carries. **CLOSED** on `tools/f/stash_record_witness.rish` GREEN, the gate proven to bite on this field by pulling one recovered log aside and putting it back byte-for-byte.
