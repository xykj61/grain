# REDS %445 -- the sentence a right machine produced

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.001800`, straight off the lap that
closed it, because the pin stood at 24,943 bytes against the 24,576 its own header declares once
this row landed. One row, kept whole because its argument is the useful part: three standing guards
refused correctly for a missing instrument, and every one of them named a file that carried exactly
what it was said to lack -- the machine was right, and the sentence it produced was wrong. Read it
beside `%442` and `%443` one shelf over, where two guards printed GREEN with the same instrument
gone, and beside `%439` on the pin, where a doc comment held the universe ten percent larger than
the constant thirty lines above it while the code refused the right number the whole time.*

*This row wore two other numbers before this one, and never for longer than an hour. It was booked
`%440`; the anointed spine spent that number on `20260905.233435`, later than this row's
`20260905.224445`. It was rewritten `%442`; the spine spent that one too, on `20260905.223102`,
in the same window. A published number never moves, so the view moved twice and the stamp, which is
the key, did not ([`../../.claude/rules/derived-spine.md`](../../.claude/rules/derived-spine.md)).
Dated logs and commit bodies keep the numbers they wrote. Link depth moved with the text.*

**REDS %445 (`20260905.224445`) -- three standing guards refused correctly for a missing tool, and every one of them accused an innocent file.** *What went wrong:* `rg` is a **borrowed** tool -- carried by some benches and not by others -- and the standing roster reaches three guards that call it in command position with no probe and no fallback. Run with the single PATH entry holding `rg` removed and nothing else changed, all three refused, which is right, and not one named the cause. `tools/fixtures/e/equinox_e122_roots_bench_kinds_scan.sh` answered `control_gate=failed / verdict=misread` about a control that had printed `verdict=ok` one line above it. `tools/t/tally_glow_tend_limb1_witness.rish` answered *"tally glow limb missing"* about `src/gate/shape-tally-max-gardens.glow`, which is present and matches. `tools/t/tally_glow_tend_limb4_witness.rish` answered *"parse_int.rye missing LeadingZero/InvalidCharacter"* about a file carrying both names twice over. Each accusation was checked with plain `grep` and each is false. The one true line, `rg: command not found`, went to **stderr**, which a witness reading `run` output does not keep -- so a hand arriving on a bench without ripgrep spends the hour repairing a file that was already correct. *What caught it:* an Aether rota lap, whose own threshold page carries the strand *a guard that cannot red guards nothing*, taking the sentence `construction/ITINERARY.md` had hoisted out of `active-designing/yonder/20260905-064341_the-tools-a-guard-may-assume.md` -- *a guard that cannot run its instrument refuses, and says which instrument* -- and asking whether the tree already obeyed it. Traced the roster transitively to 1,503 reachable scripts, found the three that reach `rg`, and ran each with the instrument taken away. *What it taught:* **refusing is half the reflex, and naming the instrument is the half that saves the hour.** The sharper half is that this class is invisible to every meter here, because a guard that refuses looks exactly like a guard doing its job: `shell_dialect` counts GNU-only idioms and finds nothing, since `rg -q` is not a dialect of anything -- it is a different program. And it is REDS %240's fault stood on its head. That one was a wrong **zero** that read as health; this is a correct **refusal** that reads as a defect somewhere else. A tree taught to distrust silence had not yet been taught to distrust noise. *Repaired (`20260905.224445`):* `have_tool` and `require_tool` in `tools/fixtures/s/shell_portable.sh` -- the first silent, for a caller that has a fallback to branch to; the second printing `instrument=<name>`, an optional `instrument_for=<purpose>`, and `verdict=instrument_absent` on **stdout** where a witness reads, returning **127**, the shell's own status for a command it could not find, so a caller tells a bench missing something from a tree measuring badly without parsing prose. Both Tally witnesses take exact `grep` and `find` spellings and are **GREEN with and without `rg`**; e122 asks twenty-three questions through `rg`, so it takes one `require_tool` at its door rather than twenty-three fallbacks written by hand, refusing at 127 with the instrument named while its `prove-red` leg still runs. Fifteen legs in `tools/fixtures/s/shell_portable_control.sh` carry `pass=32 -> 47`, each proven from both sides, the last running the real e122 scan with the `rg` entry removed; planting the elder shape back reds two of them. `tools/s/shell_dialect_witness.rish` GREEN. One more lesson rode inside the repair: the first draft of that last leg matched the PATH entry by directory name, `/ripgrep-*/bin`, and **skipped** on this pier, where the Nix store prefixes that name with a hash -- and a skip is what a vacuous pass looks like from the outside, which is the precise fault this control family was booked for. It reads the entries that actually hold an executable `rg` now. **CLOSED.**
