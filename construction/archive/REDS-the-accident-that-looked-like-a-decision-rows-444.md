# REDS %444 -- the accident that looked like a decision

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.235828`, in the round that
closed it, to bring the pin back under the byte bound its own header declares while three peer rows
landed in the same hour. One row, kept whole because the useful part is the reversal: `%431` had
weighed three ways to live with a file that could not travel, and every one of them rested on a
property the tree did not have. `.gitignore` never named `publish-seed.sh`; the root wildcard caught
it, as it catches every unlisted root path, because the repository sits inside a sandboxed home and
denies by default. Measuring that turned a workaround into a one-line allow-back and a manifest
verdict. Read it beside [`REDS-the-repair-that-could-not-travel-rows-431.md`](REDS-the-repair-that-could-not-travel-rows-431.md),
which it closed. Link depth moved with the text.*


**REDS %444 (`20260905.224117`) -- seven of eight ships pay a full cold pass every lap for a file nobody decided to withhold.** *What went wrong:* `publish-seed.sh` is absent from **seven of the eight trees on this pier** -- measured, only Incense holds it -- so `commit_message_guard` reds on each, which sets `roster_receipt_write=withheld_guard_red`, which makes `--scoped` refuse for want of a full green receipt. Every one of those seven therefore runs a **full cold roster pass every lap** at roughly ten minutes, for one grep against one missing file, while their other ten readings are green. `%431` priced this at two of three trees; the fleet launched to eight tonight and it is now seven of eight, compounding per lap rather than once. *What caught it:* reading each tree for the file after seven ships launched, rather than carrying `%431`'s two-of-three figure forward into a fleet that had tripled. *What it taught, and it is the reversal:* **`%431` reasoned from a property the tree does not have.** Its central inference is *the tell is that the repair is untracked*, and `.claude/rules/git-signing.md` stated the reason as *untracked at the root by design (`.gitignore` names it)*. `.gitignore` does not name it. `git check-ignore -v publish-seed.sh` answers **`.gitignore:8:/*`** -- the root wildcard this tree points at every unlisted root path, because the repository sits inside a sandboxed home holding the editor, credentials, and personal files, and denies by default. The file is untracked **incidentally, by a blanket deny nobody aimed at it.** And it holds nothing that wants withholding: `$ROOT` is resolved at runtime, the only literals are the two public GitHub remotes and the anonymous noreply address, and a grep for a name, a home directory, key material, or a personal address finds nothing. The privacy clause that IS load-bearing sits one line above and is untouched -- the seed's own config must never carry the host path, which is why the push arms `GIT_SSH_COMMAND` as an environment variable. **A rule that states its reason can be checked; this one had stated a reason for eleven days and no reader had asked the filesystem.** *What this changes:* `%431` weighed three shapes -- reconstruct per tree, read a tracked proxy, add a capability field -- and every one of them is a way to live with an accident. The fourth was invisible while the accident looked like a decision: **one allow-back line, `!/publish-seed.sh`, and the file travels to all eight trees on the next pull.** The manifest's own fail-safe then applies: every tracked root path carries exactly one verdict, and **`personal`** means *never copied; lives only in the private field* -- so the public seed carries it no more than it does today, while all eight private ships stop paying. *Named rather than built, and it is Keaton's:* the change edits `.gitignore` and `template-manifest.bron`, which together ARE the seed's privacy boundary, and a boundary is not something to widen on an inference however well it measures -- which is exactly the caution this ledger booked twice today. *Repaired (`20260905.224117`):* the false clause is corrected in both `git-signing` twins, since a rule stating a wrong reason is wrong whatever is decided about the file. *Repaired (`20260905.230357`), on Keaton's word:* `.gitignore` gains the allow-back `!/publish-seed.sh` with the reason written beside it, and `template-manifest.bron` gains the verdict **`personal publish-seed.sh`** -- *the field's own projector; the seed is its OUTPUT, never its carrier*. The file is tracked at **`100755`**, exec bit intact. Three readings taken before the byte entered history: the file carries no name, home path, key material, or personal address; `xy` and `gp405` are the **private field** while `grain-os/grain` is the public projection, so tracking publishes nothing; and the `personal` verdict withholds it from the seed by decision rather than by accident. `commit_message_guard` is **GREEN**, and `sow_witness` is **GREEN** with the new verdict, so the privacy boundary is proven intact rather than assumed. The seven ships collect it on their next pull and stop paying a cold pass a lap. *What this row does not reach:* whether the other twelve root paths the `/*` deny catches are each there on purpose. This one was checked because a guard was red seven times over; the rest have never been asked, and the same accident could be sitting in any of them. **CLOSED.**
