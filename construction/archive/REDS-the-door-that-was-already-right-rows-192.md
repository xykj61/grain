# REDS %192 -- the door that was already right

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.112806`, **CLOSED** -- the last
unsplit module front door, correct in every count and still past reading, and the 47th standing
guard it seated as the tree's first prevention-only roster guard. Moved here as row %194 carried the
living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %192 (`20260824.104946`) -- the last unsplit front door had every count right and was still past reading, so its guard is the first roster guard in the tree seated as prevention rather than repair.** *What went wrong:* `constel/README.md` stood at **69,979 bytes** over **109 lines**, and it was the last unsplit module front door in the tree. Counted against the directory beside it, the page read **clean in every direction the three earlier splits were booked for**: 31 of 31 `.rye` modules named, 31 of 31 `tools/f/fora_*_witness.rish` paths named and correct, and the highest rung it cites -- FORA30 -- matching the highest on disk. So there was no miscount here, and the fault was length alone. Two blocks carried nearly all of it: **one 9,848-byte sentence** under *The road on*, accreted `and now...` rung by rung across thirty rungs and still opening with its FORA10-vintage summary, and a **4,558-byte `Kin:` line** chaining twenty-three design notes. A third block, the `Status:` line, recited all thirty-one rungs inline. *What caught it:* the previous lap's `recommend` named this page as the next round and asked for the named-against-standing count first; `comm` between `ls constel/*.rye` and the names the page cites returned **31 against 31**, which is the answer that made this round different. *What it taught:* **a page can be entirely accurate and still past anyone's willingness to read it, and the guard for that is seated before the drift rather than after it.** The three earlier roster guards were each booked by a page already wrong -- 73 of 110 (%184), 112 of 227 (%189), 83 of 240 (%190) -- and the fourth by a guard that was right and off the roster (%191). This is the first seated over a directory that is correct today, which is the only time a roster guard costs nothing to seat and buys the whole of what it is for: a roster listing what a *reader* should find is checked by nobody, because a missing entry breaks only understanding, and understanding fails quietly. *Repaired:* the page split three ways in the same directory, so every relative link inside it stays true -- a **4,079-byte Door front page**, [`../../constel/LADDER.md`](../../constel/LADDER.md) carrying the thirty-one rung sections, the thirty-one prove commands, the road-on narrative, the rung recital and the kin chain moved **word for word** (verified by `diff` against the departing slices), and [`../../constel/MODULES.md`](../../constel/MODULES.md) with **31 rows in eleven families**, each sentence read from that module's own `//!` head and normalized to plain ASCII. *Prevention* seated as the **47th** standing guard: [`../../tools/co/constel_module_roster_witness.rish`](../../tools/co/constel_module_roster_witness.rish), gating `unrostered`, `phantom`, `duplicate_rows` and `mismatched_rows` at zero. It is **the fourth consumer of one scan**, [`../../tools/fixtures/module_roster_scan.sh`](../../tools/fixtures/module_roster_scan.sh), and adds no scan of its own -- a witness file and nothing more, exactly as the itinerary predicted when it named this page. Proven RED on the real page in **both directions**: the `durable.rye` row removed reads `unrostered=1, verdict=roster_incomplete`, a planted `socket.rye` row reads `phantom=1`, and the page restored reads `bijection=yes`. Proven both ways in a pen by [`../../tools/fixtures/module_roster_control.sh`](../../tools/fixtures/module_roster_control.sh), sixteen behaviors on real directories. *And the meter bit the repair, hardest yet:* the new Door page joined the DOOR roster in `tools/fixtures/prose_register_scan.sh` and read **47% against the 20% ceiling** -- the highest opening reading of the five, because this module's whole subject is a refusal, that a name can never be a real ship. It swept to **5%** with every claim held exactly: *none of them a real point* became *every one of them local to this bench*, *`y` never appears in the table* became *the table leaves `y` out entirely*, and *no network, no keys, no funds, no real address* became *the network, keys, funds, and real addresses all sitting outside what it touches*. **The missing vowel keeps its name**, in the one sentence the meter still counts and in the benediction, because it is what the property is called. **One more red inside this one, owned rather than smoothed over:** booking `%192` carried the living pin to 25,348 bytes, so row `%190` folded to `construction/archive/` -- and the fold moved the row's text without its depth. Its five tree links still read `](../../lotus/LADDER.md)` from one directory further down, where `](../../lotus/LADDER.md)` is the truth. `tools/r/readme_reach_witness.rish` went RED on the staged tree and named all five in its own output, `broken_in_living=5, living_files_affected=1`, and the repair was one regex that leaves `../REDS.md` alone. This is the **second firing** of the shape booked as `%183`, *a fold moves depth* -- that one was a tool, repaired by fixing the tool; this one is a hand fold, and the REDS ledger has no fold tool. A lantern that fires twice becomes a loom, and here the loom already stood: `readme_reach` catches a depth slip on the lap it is made, which is why this cost one command rather than a season. **With this the six-module front-door arc closes**: Caravan, Mycelium, Open Image, Lotus, Crypto, and Constel each stand at a Door page under the living bound with a standing guard holding the page to its own directory. CLOSED.
