# REDS %442, %443 -- the instrument that could not hear, and the one that could not see itself

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.233318`, in the round that
closed them, to bring the pin back under the byte bound its own header declares. Two rows, kept
whole because they are one argument in two movements. `%442` found guards that print their GREEN
line byte for byte with the tool they measure with gone from the host: `2>/dev/null` takes the
shell's complaint, `|| true` takes exit 127, and an emptiness test reads "nothing matched" out of
"nothing ran". `%443` is what happened when the guard built to catch that was itself committed --
its control has to plant the very shape the meter detects, the plant lives in a heredoc in the
control's own source, and the meter enumerates with `git ls-files`, so the guard read clean while
untracked and refused the moment it was staged. Read them beside `%438`, one shelf over, whose
sentence they both extend: the record of an instrument is still instrument. The repair is one
distinction applied twice -- a heredoc body is text a script writes rather than code it runs -- and
it is proven from both sides, freed inside a heredoc and bitten outside one. Link depth moved with
the text.*


**REDS %442 (`20260905.223102`) -- two guards printed their GREEN line byte for byte with the tool they measure with absent from the host.** *What went wrong:* `tools/fixtures/i/inner_i1_twah_residual.sh` and `tools/fixtures/i/inner_i2_djin_prose.sh` each prove a room clean by capturing ripgrep's output and refusing when it is non-empty -- `hits="$(rg PATTERN paths 2>/dev/null || true)"` and then `if [ -n "$hits" ]`. Three constructions in those two lines each discard the evidence: `2>/dev/null` hides the shell's own `rg: command not found`, `|| true` discards exit 127, and the emptiness test reads *nothing matched* out of *nothing ran*. Measured on this pier with ripgrep genuinely off PATH, both printed their GREEN line byte for byte and exited 0, and `rishi/bin/rishi run tools/i/inner_i1_twah_residual.rish` -- which asserts `sweep.ok` and `contains "GREEN: inner-i1"` -- read GREEN with no ripgrep on the host at all. **117 tracked scans name `rg` in command position**; the other 115 refused for reasons of their own, loudly and by accident, and not one of them named the instrument. *What caught it:* the live front's own sentence, taken literally -- *a guard that cannot run its instrument refuses, and says which instrument* -- turned into an experiment rather than a plan: shim the instrument away and see which guards stay quiet. **Two independent readings agreed on the same two files**, a behavioral one that runs every rg-naming scan with and without the tool, and a static one that looks for a borrowed capture whose emptiness is the pass branch. `tools/fixtures/i/instrument_refusal_scan.sh` (`%416`) reads 686 scans and finds **zero**, honestly: it gates an output pass redirected into a FILE, and this pass goes into a VARIABLE, so the elder meter is blind to it by construction -- an *edge* rather than a defect, in `active-designing/20260823-232125_the-edge-of-an-instruments-model.md`'s own terms, and the dimension it never modelled is that **the instrument may not exist**. *What it taught:* **emptiness is not evidence.** This is the vacuous-guard family one layer down: `%59` proved a guard must be *able* to red, and this is a guard that is able to red and cannot *hear*, because the sense it reads with was removed. The general form is worth the ink -- **the failure of an instrument is silent exactly when its healthy answer is an absence**, so every zero-is-good reading in this tree is one missing binary away from its best number. Twice the measurement lied to me before the row was written: a shim answering exit 127 is *not* absence, since `command -v` still finds it, so the first repair read as no repair at all; and the second reading of `inner_i2` came back exit 1 only because my own already-repaired `inner_i1`, which `inner_i2` calls, refused underneath it -- **a measurement contaminated by the fix being measured**, which is `%422`'s shape in a third material. *Repaired (`20260905.223102`):* `require_instrument` seated in `tools/fixtures/s/shell_portable.sh` beside its seven dialect answers, refusing by name at exit 2 so a caller can tell a wrong tree from a wrong bench; both sites ask for `rg` by name and now refuse without it while staying GREEN with it; and `tools/i/instrument_absence_witness.rish` over `tools/fixtures/i/instrument_absence_scan.sh` gates the construction at **zero from birth**, on the roster at `tier lap`. **24 behaviors** proven in a throwaway pen, every refusal shown from both sides, including a pen holding symlinks to every binary on PATH *except* ripgrep -- and a leg proving that pen is complete but for ripgrep, since otherwise a refusal there proves nothing about ripgrep. The meter's own awk is **POSIX** awk: the first draft used `gawk`'s three-argument `match`, and a guard about borrowed instruments that reaches for one has refuted itself. **This makes no claim about tiers** -- the tool-grant roster in `active-designing/yonder/20260905-064341_the-tools-a-guard-may-assume.md` is right that 992 unprobed `rg` sites are a ratchet needing a roster first, and that page keeps its order. *The guard's own first reading is `%443`: it counted this control's planted fixture as field and refused the moment it was tracked.* **CLOSED.**

**REDS %443 (`20260905.231715`) -- the new guard counted its own control's planted fixture as field, so it read GREEN while untracked and refused the moment it was staged.** *What went wrong:* `tools/fixtures/i/instrument_absence_scan.sh` enumerates its subject with `git -C "$ROOT" ls-files 'tools/*.sh' 'tools/**/*.sh'`, and its awk counts any logical line shaped `VAR="$(<borrowed> ... || true)"` that is later read by `[ -n "$VAR" ]`. `tools/fixtures/i/instrument_absence_control.sh` has to plant exactly that shape to prove the meter bites, and it plants it the only way a control can -- `cat > "$pen/tools/fixtures/z/planted_scan.sh" <<'PLANT'`, whose body is text sitting in the control's own source. So the meter read its own proof at `instrument_absence_control.sh:45` and counted it against a ceiling of zero. **Measured both ways on metal:** with the control unstaged, `scans_read=687`, `blind_captures_open=0`, `verdict=ok`; with it staged, `scans_read=688`, `blind_captures_open=1`, `verdict=over_ceiling`. *What caught it:* running `rishi/bin/rishi run tools/i/instrument_absence_witness.rish` against a restored stash, where `tools/f/fleet_round_open.sh` had preserved the index a dead lap left -- so the control was tracked for the first time. The lap that authored the guard measured it honestly, and measured a tree its own commit would not ship: `git ls-files` cannot see a file until it is added, and this guard's subject *is* `git ls-files`. *What it taught:* **a meter that reads tracked files cannot be measured before it is tracked, and a guard whose control must plant the guard's own shape will read its proof as a wound until it is told the difference.** That is `%438`'s sentence -- *the record of an instrument is still instrument* -- in a fourth material and the tightest one yet, because here the instrument and the field are one file. It is `%174`'s and `%223`'s shape as well: the card's rule that *a lap ends at the commit, never at `git add`* exists because the tree a roster measures must be the tree the commit ships, and this is the first case where the guard **being added** is what moves its own reading. A second, quieter fault rode along -- `require_instrument` at `instrument_absence_control.sh:64` sits inside the `ARMED` heredoc, and the meter's file-level `armed` flag is set by any line matching that name, so the control's four later plants were exempted by fixture text rather than by reasoning. An exemption held by luck is one nobody can check. *Repaired (`20260905.231715`):* the awk tracks heredoc bodies -- the `<<` opener, `<<-` tab-stripping, and a terminator compared after leading whitespace -- and a capture opened inside one is classified `fixture=yes`, while `require_instrument` inside a body no longer arms the file. The gate reads a file's own code alone; the fixtures are **printed by path** on `fixture_capture:` lines under a new `blind_captures_in_fixture` key, because a cap nobody can see is a cap nobody can check. The three counts now partition the total: `blind_captures_armed=3`, `blind_captures_in_fixture=9`, `blind_captures_open=0` against a ceiling of 0, `verdict=ok` -- and `blind_captures_armed` fell 7 to 3 as the accidental arming left. **The distinction is proven from both sides in fourteen new control legs, 38 where 24 stood:** the same two lines are freed inside a heredoc and bitten outside one, and `require_instrument` is refused inside a body and honored in a file's own code, because an exemption proven only in the freeing direction cannot be told from a hole. The tree had already drawn this line once, for its ASCII comment meters, which skip a shell heredoc body because it is what a program feeds onward rather than what it runs. **CLOSED.**
