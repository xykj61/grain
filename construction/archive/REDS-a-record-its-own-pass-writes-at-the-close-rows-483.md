# REDS %483 -- a record its own pass writes at the close

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.134234` to bring it back under the
byte bound its own header declares, as the repair landed on it. A guard whose evidence is a file its
own pass writes at the close is always one pass behind, and the direction of that lag is the
expensive one: it reports a fault that is already fixed, on the lap that fixed it. The repair does
not stop the guard from reading its peers -- it makes the record describe now, and moves the reader
to where the record is complete. Link depth moved with the text.*


**REDS %483 (`20260906.113000`) -- the roster's own guard reads a run card that is written once, at the close of the pass, so a repair landed in this pass is invisible to it and the lap that fixes the last red still closes `guard_red`.** *What went wrong:* `tools/fixtures/s/standing_equipment_scan.sh` counts `runs_red` off the run card, and the runner writes that card **wholesale at the end of a pass** -- proven by the card's own stamps, `oldest_run=20260906.111808` equal to `newest_run=20260906.111808` across all 140 rows. So `standing_equipment`, at position 151 of 172 output lines, reads the **previous** pass's card no matter how late in the roster it sits. Measured in one lap: the cold pass at `20260906.105803` read `index_row_bound red` honestly; the shelf was repaired; the hot pass at `20260906.111808` read **`index_row_bound green` at line 60 and `standing_equipment red` at line 151**, and the same scan run by hand two minutes later read `runs_red=0 verdict=ok`. Nothing changed between those two readings but the card being finished. *What caught it:* a lap that repaired the only real red and still closed `guards_red=1`, and asked which guard rather than accepting the number. *What it taught:* **a guard whose evidence is a file its own pass writes at close is always one pass behind, and the direction of that lag is the expensive one** -- it reports a fault that is already fixed, on the lap that fixed it. `%475` closed the half where the guard counted its OWN verdict (`runs_red_self`, reported never counted); this is the neighbour half, where a peer guard's stale verdict is counted, and `runs_red_self` cannot see it. *The cost is already priced:* a red guard sets `roster_receipt_write=withheld_guard_red`, `--scoped` refuses without a full green receipt, so **every lap that closes a red pays the next lap a full cold pass** -- 900s and 687s measured here. *Not taken, and why:* the runner and the roster are another seat's, and two shapes fit -- write the card incrementally, or move that guard's roster row so it runs after the write. Naming which is that seat's ruling. *Booked `%480` from a `--next` read that was fresh, and a peer published `%480` first; renumbered on the rebase, the stamp holding, which is exactly what `.claude/rules/derived-spine.md` is for.* *Repaired (`20260906.134234`) -- and the ruling is BOTH shapes, because neither is whole alone.* The card the guard read was the WORKING-TREE one, which the runner writes once at the close, so the second shape names a position that does not exist: **nothing runs after the write.** What `tools/fixtures/s/standing_equipment_run.sh` hands each guard now is a **pen-local live view** -- `$pen/card.live`, rewritten after every guard answers and exported as `STANDING_CARD`, which `rishi run` passes on to the scan (proven on metal before a line was written). It stays in the pen for the reason the evidence room does: a file written into the tree DURING the pass moves the tree under this runner's own `tree_moved` reading, in every control pen and in any clone where the card is not yet gitignored. **That half alone left a measured residue of seven** -- the lap-tier guards standing after `standing_equipment` at roster position **188 of 196**, whose rows could still only be last pass's -- so the second half is the elder second shape done where it *is* possible: the runner defers **its own guard by name** to the end of the todo list, which survives a hand adding a guard alphabetically after it where a roster position would not. `standing_equipment_control.sh` proves **8 behaviors** on a real repository in a throwaway pen -- a card planted with `alpha` RED, `alpha` green this pass, and `beta` refusing unless the card it was handed says so; the self guard listed FIRST and proven to print LAST; the working-tree card still written once at the close and now carrying this pass's verdicts. **The pen is proven innocent:** a runner with the hand-off line stripped reads the stale card and refuses, and the removal itself is asserted, since a grep matching nothing would prove a repair by running the repaired runner twice. *And the cost, priced again from this pier:* the last full pass spent **798 seconds** of guard time across 144 guards, and six ships share eight cores here -- so a phantom red does not bill a fixed number, it bills a full pass under whatever contention the pier is carrying. **CLOSED.**
