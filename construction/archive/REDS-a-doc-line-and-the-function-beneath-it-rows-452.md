# REDS %452 -- a doc line promised a distance the function never measured

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.020024` to bring it back under
the byte bound its own header declares, as `%456` landed on a pin standing two bytes below it. A
peer's row, kept whole: a documentation line promised a distance between points while the function
beneath it measured between outfits, which is the seam where prose and code drift apart in the one
direction no guard reads -- both sides are internally correct and only their meeting is wrong.
Link depth moved with the text.*


**REDS %452 (`20260906.002658`) -- a doc line promised a distance between points, and the function beneath it measures between outfits.** *What went wrong:* the doc comment on `route_hops` in `comlink/topology.rye` read *"Zero exactly when `a` and `b` are one point."* `route_hops` reads `tier` through `depth()`, and the inclusive breach made one point wear up to three outfits, so `Address.of_galaxy(8)` and `Address.of_planet(8, 0, 0)` both `encode` to **8** -- one point -- and read **two** hops apart. The sentence is a biconditional, and only one half of it survived the breach. *What caught it:* planting the doc's own sentence as a selftest assertion and building it. `PlantNotOnePoint` did not fire, which proved the two addresses are one point; `DocClaimRefused` did, which proved the function disagrees with the line above it. *What it taught:* **a breach that changes what a word MEANS is harder to sweep than one that changes a number.** REDS %439 was this same file, thirty lines up, on the same day: `universe_points` moved 792 to 720 and the prose kept the old figure, which is a number a reader can hold against a constant. This is the word *point*, which inclusion redefined -- an address and a point stopped being one-to-one -- and no grep reaches a definition. **The five named routing legs could not see it by construction:** every one compares two addresses at the same tier, so the degree of freedom inclusion opened was never exercised. *Repaired (`20260906.002658`):* the line says what the function keeps -- zero exactly when `a` and `b` are the same **address**, indices and outfit alike -- with a say-why naming both halves, the shape %439's repair used above it. Three legs pin the outfit reading (galaxy to its own star 1, to its own planet 2, star to its own planet 1, all three encoding to one number), and `prove_route_is_never_blind` walks **every ordered pair of both seated skies** -- 518,400 for the compass sky and 164,025 for the council -- proving the half that holds: two distinct points are never zero hops apart, the one property routing cannot survive losing, which `tools/fixtures/t/topology_metric_census.sh` had measured true with no guard to keep it. Proven able to red three ways on metal: a metric collapsed inside galaxy 11 -- a corner no named leg visits -- answers `RouteBlindBetweenPoints` while all eight named legs pass; a wholly blind metric answers `PlanetToStarNotOne`; a sky of 1,200 points answers `SkyTooLargeToProvePairwise` against the named ceiling of 1,024. Two witness asserts carry both halves onto the standing roster. **CLOSED** on `tools/co/comlink_topology_witness.rish` GREEN.
