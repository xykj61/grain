# REDS %495 -- a rename anchored to a bare number

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.140817`, in the round that booked it,
because the pin stood **9 bytes** under the 40,960 it declares and this row could not otherwise be
written at all. That is worth saying plainly: a ship found a red it had caused, and the ledger had
room for nine characters. The row is CLOSED and folds by the pin's own law; an OPEN one in the same
position would have had nowhere to go.*

*The lesson is one file away from this round's own work. The guard landed the same hour refuses to
read a path mentioned in a roster comment as a seat, because a seat is a `path` row at column zero
and anything looser lets a guard count as rostered for being written about. Two hours later the same
hand swept `rows-492.md` -> `rows-494.md` through the operator card with no anchor at all, and
the first match was a peer's.*


**REDS %495 (`20260906.140714`) -- renumbering my own row swept a peer's identical number, because the sweep was anchored to a bare number rather than to my own sentence.** *What went wrong:* my row moved `%487` -> `%491` -> `%492` -> `%494` across three rebases in one send, and each hop ran `s.replace('rows-<old>.md','rows-<new>.md', 1)` over `construction/ITINERARY.md`. On the last hop the FIRST match in the file was a peer's line 52, `[%492](REDS-two-algorithms-agreeing-rows-492.md)`, so their link was repointed at my shelf while my own block was left saying `%492`. Both published in `b441f97b2`. *What caught it:* a consistency read of my own landed row, one command after the push -- and only because the card's number and its link disagreed. *What it taught:* **a rename anchored to a bare number matches any row that shares it**, and on a spine eight ships write into, sharing a number is the ordinary case rather than the edge. It is the same fault this round's own guard was written to prevent one file over, where a seat is a `path` row at column zero rather than any line mentioning a path -- **I wrote the anchored rule and then swept unanchored in the same hour.** *Repaired (`20260906.140714`):* both links, each replace anchored to its own surrounding sentence rather than to the number alone. **CLOSED.**
