# REDS -- the fold round, rows 113-114 (folded)

**Language:** EN
**Folded:** `20260821.185411` from [`../REDS.md`](../REDS.md) -- a closed-range archive fold to keep the living pin under `living_pin_max_bytes` (24576). Every byte kept, nothing removed; the living ledger keeps rows 115 onward and points here.
**Style:** Radiant - **Voice:** Kyri
**Span:** the session-log fold of `20260821` -- a tool whose cost scaled with the thing it was fixing, and an index nobody had ever measured.

*A red owned in conversation is a memory. A red recorded here is a proof. These rows moved out of the living pin only to keep it readable; they are as binding as the day each was caught.*

---

**REDS %113 (`20260821.163528`) -- the fold tool's index rewrite was quadratic, and the room grew until that mattered.** *What went wrong:* `tools/session_logs_archive.rye` repointed the index by calling a whole-string replace once per moved file -- O(files x README) in time, and on an arena that reclaims nothing, in memory too. It was invisible while the room was small. Folding 1,443 logs out of a 2.7 MB index ran **over four minutes and 856 MB and had not finished**, leaving the tree with every file moved and the index still pointing at the old paths. *What caught it:* watching the resident-set climb during the fold rather than waiting for the exit code. *What it taught:* **a cost that scales with the thing being fixed will be paid exactly when the fix is needed most.** The tool was correct at every scale it had been run at and unusable at the scale it existed for. Repaired by a single pass that walks the index once and inserts `date/<day>/` before any bare dated link -- **0.174 seconds** for the same work, idempotent by construction, and now run on every real fold rather than only when something moved, so the index can never sit drifted from the tree. The pass is told which day stays flat rather than left to guess; a probe that defeated that guard made the selftest fail by name (`SelftestTodayLinkMoved`) before the guard was trusted.

**REDS %114 (`20260821.163528`) -- six index rows pointed at logs that were never there, and nothing had ever checked.** *What went wrong:* `session-logs/README.md` carried rows citing three logs whose files spell their sprig `colour` while the index spelled it `color`, one whose stamp read `013130` where the file and its own `stamp` field both read `013158`, and two naming logs that were never written at all. Every one had been wrong for days inside the tree's most-read index. *What caught it:* the dated-path census, on the lap the room folded -- the rows were already broken, and folding made them countable. *What it taught:* **an index nobody measures is an index that drifts, and drift in an index is invisible precisely because everyone trusts it.** Repaired where the answer was unambiguous: the links now name the files as they actually stand, and the stamp typo follows the file's own field. The `colour` spelling runs through those three logs' bodies as well -- 22, 13, and 10 lines -- so normalizing it is its own round and its own word, not a repair to smuggle inside a fold. The census now counts these as **lost** references under a ceiling with no slack, which fell 199 -> 193 as the six were fixed.

**REDS %114 CLOSED (`20260821.185411`) -- the spelling decision applied at last, and the record kept coherent.** *The repair, on metal:* **799 occurrences across 123 files** normalized `colour -> color`, and the three dated session logs whose filenames disagreed with the index renamed by `git mv`, index repointed. **All 57 witnesses over the touched modules pass**, the census holds at `lost=193`, and the tree-wide guards stay GREEN. *What the round had to read before it could sweep:* the spelling is not only prose. `craft_colour` is a file-local helper repeated in six modules -- safe, since each file defines and calls its own. `colour:red` is a facet key in sixteen files, and it is a NEGATIVE test: the parser knows `material:` and `max:`, so `colour` is bad because the key is unknown -- and `color` is equally unknown, so the behavior is identical. That had to be **read** to be known; a blind sweep would have been a gamble that happened to pay. *The trap this round nearly walked into:* four files had to be **held out**, because sweeping them would have destroyed the thing that asks for the sweep -- `construction/ITINERARY.md` carries the directive *"Use `color`, not `colour`"*, this ledger carries %114's own description of the drift, `external-research/20260816-221015_...` is the seat itself, and `session-logs/README.md` plus three logs from this arc record the drift in their own words. **A normalization that cannot describe what it normalized has erased its own reason.** *What it taught:* **a spelling sweep is a code change wearing prose clothes.** Identifiers, data keys, and the documents that state the rule each need a different decision, and the only way to tell them apart is to look. *Provenance, since it was asked:* the decision was seated `20260816.221015` in the Standfast correction wave as **the color decision** -- normalize on touch, a ratchet rather than a molt or a breach. This round is the first time it was applied deliberately across the tree rather than file by file as rounds happened to touch things.
