# SHRED_PREP shelf -- the rune-rename cut of `20260826`

**Language:** EN - **Status:** Shelf, immutable - **Folded:** `20260906.114734` from [`../SHRED_PREP.md`](../SHRED_PREP.md), whose living parts needed the room.

A finished cut record. Its numbers stand as measured on the closing tree; nothing here is living.

---

### Class M -- CUT `20260826`, and the prep had undercounted

*Keaton circled the rune renames on `20260826` and scoped them to the `glow/` core and its runners.
Measured on the closing tree, `20260826.201500`, by `git diff` over the round's own staged and
unstaged paths: **19 files renamed**, **46 files touched** (the 19 renamed, each also repointed, plus
27 repointed only), **538 name substitutions** in content, and **273 occurrences of the syllable left
standing** -- which is the scope line rather than a leftover. The syllable stays as the rune's spoken
name; only the module path takes the plain one.*

**The emitted code did not move, and the right instrument says so by reading rather than counting.**
Across the four emitter modules the whole content diff is two lines each -- the self-naming header
comment and the `@import` path. The emitted identifiers inside them stand at the same counts before
and after: `bartis_gate` 45, `bartis_pair` 18 in `lower_shop_gate.rye`, and `barket_gate` 28 in
`lower_shop_nest.rye`. All twenty-one witnesses the rename touched run GREEN on metal -- the twelve
rune witnesses, the two nest-type, the two tally, and the five STOA emit witnesses.

**A tree-wide string count is the wrong instrument here**, and it read as a fault before the diff
corrected it: `bartis_pair` appears inside the *filenames* `glow_bartis_pair_*_witness.rish`, so
counting the string across the tree mixes an emitted identifier with a path and reports movement
where the code stood still. Named because the next hand checking a rename will reach for the same
count first.

**The row below claimed those two modules were "the whole cut surface". That was wrong**, and the
measurement is worth keeping beside the claim: **80 living files** carry the syllable in their names
-- 7 in `glow/`, **59 authored fixtures in `glow/gen/`**, 12 tools witnesses, 2 lexicon tools -- plus
667 reference lines. The claim was true of the `lower_*.rye` family it was measured on and false of
the tree it was written about, which is the difference between a measurement and a generalisation.
**The 59 `glow/gen/g/gate-barket-*.glow` fixtures and the 2 lexicon tools await their own word.**

**And the baseline found six red witnesses before the rename touched anything** (REDS %283): a head
count pinned in a prose sentence while the table grew 27 -> 28, taking four with it, and a brief that
moved to `yonder/` while its citer's path stayed. Both repaired first, because a rename promising
byte-identical output needs a before-reading that is actually green.

