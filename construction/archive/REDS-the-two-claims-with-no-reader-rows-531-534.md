# REDS %531 and %534 -- two claims with nothing reading them, one in a cellar and one on a channel

*Folded off the living pin [`../REDS.md`](../REDS.md) on the round after the one that booked them,
by their own author, because the round's new row ran the pin to 42,602 against its 40,960 bound.
Both read **CLOSED**, each on its own witness GREEN on metal. No peer's row was moved to make this
room.*

*Both rows are the same fault wearing two costumes.* The cellar's catalog named three of its
seventeen resins and **no witness, runner, or roster row had ever opened it** -- so a catalog four
fifths short read exactly like a whole one, for eighty-seven days. The five rostered witnesses
forwarded a control's stdout and never its stderr, so the one clause naming **which** behavior
failed was written and then dropped on the floor -- the verdict travelled, the reason did not.

*The sentence worth carrying forward: **a claim goes stale on the first day and reads healthy for
every day after, unless something reads it.*** One was a count nobody checked; one was a reason
nobody forwarded. Neither could have been caught by care, and both were caught the hour an
instrument was pointed at them.

*And the pair set up the round that folded them.* The catalog row was found by the council rota's
Earth seat -- the row that breathes in, reading the concrete fact at the door. The next lap's Fire
seat, the row that sees, asked the same question of the first thing this ship reads on every lap,
and found its own charter publishing a line count that had never been true. Same shape, one room
further in.

---

**REDS %531 (`20260907.000030`) -- the resin cellar's catalog named 3 of its 17 resins, and nothing in the tree had ever read it.** *What went wrong:* `bron-resins/manifest.bron` is the cellar's catalog, one `entry <basename> <note>` line per resin, and its own `note` line states the habit that keeps it current -- *commit with the resin*. The habit held for the first three files of the first morning, `20260712`, and then lapsed; fourteen resins landed with no line. The catalog also had **no reader**: three scans name `bron-resins/` only to skip it as testimony (`counsel_census_scan.sh`, `waymark_rung_drift_scan.sh`, and `template-manifest.bron`'s `personal` verdict), and no witness, runner, or roster row opened it. *What caught it:* **the council rota.** Lap 4114 read row 4, Earth, whose Dual seat is `foundations/20260703-202312_the-marked-value.md` -- the document whose Amber clause reads *every resin records the marks of what it seals, so a reader in another decade knows what the cellar held without opening it.* The row that breathes in reads the concrete fact at the door, and the fact at this door was a catalog four fifths short. *What it taught:* **a claim with no reader goes stale on the first day and reads healthy for eighty-seven of them.** That is `%514`'s lesson -- silence and cleanliness read alike -- in a room where the silence was the whole record. The sharper half is about scope: the class was **measured before the instrument was built**, crossing every tracked file in a living room whose name carries `manifest`, `catalog`, or `roster` against its readers, and every other one has a reader -- the mycelium corpora have a witness and a Rye reader apiece, `forge_cellar_manifest.bron` is read by `pond/apps/drawn_terminal.rye`, and the seat rosters are read by twenty, nine, and eight sites. **A lantern rather than a loom**, so the repair is one cellar's scan rather than a general instrument, and the near-miss is named: `cellar/ring1_manifest_shape.bron` is cited only by `cellar/README.md`, and it is a *shape* rather than an enumeration, in a lane this seat does not hold. *Repaired (`20260907.000030`):* the catalog names all 17, each note written from that resin's **own first header line** rather than from a summary, and the one `.md` file in a room whose `law` line says Bron only is **named rather than swept**, because a catalog states its exception instead of hiding it. `tools/fixtures/b/bron_resins_catalog_scan.sh` gates `uncatalogued`, `orphan_entries`, and `duplicate_entries` at zero -- three readings because they fail in three directions -- and reports `noteless_entries`, since a bare name is a weak line and still a true one. An absent room or catalog answers `misread` and exits 2, never zero. `tools/fixtures/b/bron_resins_catalog_control.sh` proves **34 behaviors** on throwaway cellars, every refusal shown from the failing side and then lifted, the reported reading proven **not** to gate, the detail bound proven by planting 75 resins against a ceiling of 64, and the pen proven innocent against a scan patched to always answer `ok`. **The control caught a hole in itself:** its `cmp -s` innocence check read an *unreadable* source as a landed patch, since an empty copy differs from the original; emptiness is checked before difference now. **CLOSED** on `tools/b/bron_resins_catalog_witness.rish` GREEN, rostered `tier lap` at 4s. *Left standing, named:* the catalog's header promises SHA3-256 names *when sealed* and the entries carry plain paths, so the mark and digest of `the-marked-value`'s three-field line are a later lap rather than a gap this one hid.

**REDS %534 (`20260907.000030`) -- a control names its failing behavior on stderr, the witness above it forwards stdout alone, and five rostered guards therefore file an evidence page that names the guard and no reason.** *What went wrong:* `tools/fixtures/f/fleet_watch_control.sh:24` prints `FAIL <behavior> -- wanted X, got Y` to **stderr**, so a passing run stays quiet. `tools/f/fleet_watch_witness.rish:17` interpolated `${ctl.out}` into its assert message and never `${ctl.err}`. The exit code travelled, the summary `pass=15 fail=1` travelled, and the one clause naming WHICH behavior failed did not. Measured the same hour: **eight** controls print FAIL to stderr, **five** are run by a witness, and **all five of those witnesses stood on the standing roster** interpolating `.out` alone -- `fleet_watch`, `fleet_lap_verdict`, `publish_template`, `unshared_citation`, `sealed_digest`. *What caught it:* the lap's own cold open. `fleet_watch` reddened at 25s, `standing_equipment` reddened beneath it reading `red: fleet_watch red`, and the evidence file held **seven lines** -- three banner says, one progress say, and an assertion whose whole diagnostic was `pass=15 fail=1`. The control then passed **16 of 16 on six consecutive re-runs**, so the fault was a flake and **the one reading that could have diagnosed it was the reading the witness threw away.** *What it taught:* **the reason and the verdict travel on different channels, and a guard is only as diagnosable as the channel it chooses to carry.** This is `shim_reason`'s own law -- seated `20260906.124500` out of the amphora shims, where `r.out` alone dropped a target's assert message -- met in a shape that guard cannot see: its three-mark reading requires `say r.out` and `exit r.code`, which is a pass-through shim, and these are witnesses that assert. One law, two shapes. **And the sharper half is that a flake is exactly when the sentence matters most**: a reproducible red can be re-run until it explains itself, where a red that will not reproduce is diagnosable only from what it wrote down the first time. *Repaired (`20260907.000030`):* all five witnesses interpolate `${ctl.err}` beside `${ctl.out}`, proven **on metal** by planting one wrong expected value in `sealed_digest_control.sh` -- the evidence went from `pass=8 fail=1` alone to that line plus `FAIL an unread seal is counted -- wanted PLANTED_WRONG, got yes` -- and the control was restored byte-identical, checked with `cmp`. `tools/fixtures/s/shim_reason_scan.sh` grew the second reading rather than the tree growing a second instrument: `reason_lost_rostered` **GATED at zero**, `reason_lost_unrostered` a ratchet opening at zero, and `stderr_controls` naming the population so a zero is visible rather than assumed (`%463`). Two refusals it deliberately declines, each planted: a control keeping its FAIL line on **stdout** is outside the population, since its reason already travels and counting it would red a witness that is already correct; and a witness forwarding some **other** run's stderr earns no credit, because the reading binds to the variable the control's `run` is bound to -- the blind version of this check is *does the file mention `.err` anywhere*. **And the reading caught itself:** the control's pen helper WRITES a stderr-reporting control, so its own source carries that line inside single quotes, and the first pattern read **9** where the tree holds **8** -- the instrument counted as one of its own subjects, which is this guard's own roster-anchor lesson one reading over. The population is anchored on what stands immediately before the command, both spellings of a mention are planted, and the quoted plant reproduces the real continuation-line shape rather than something near it. **21 new pen readings, 6 new repositories, 61 cases over 14 repositories**, every refusal planted and then lifted; the guard runs in 8s. **CLOSED** on `shim_reason_witness.rish` GREEN and `fleet_watch_witness.rish` GREEN. *Not taken:* three of the eight stderr-reporting controls -- the `chatgpt_mind` trio -- have no `.rish` caller at all, so they carry no loss today and would carry one the lap somebody writes them a witness; the gate is what spares that lane the discovery.
