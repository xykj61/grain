# REDS %470-%471 -- the cheaper question an instrument asked

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.125522`, because both read
**BOOKED** -- their instances stand repaired and what remains is a ratchet or a seat -- while every
other row on the pin reads **OPEN**, and the pin stood 614 bytes over the ceiling its own header
declares. A BOOKED row may fold to a shelf; an OPEN one stays on the pin.*

*They share one clause worth carrying forward on its own. In `%470`, five guards read
`mantra/src/diff.rye` on every lap -- width-check, width_check_th3, tame_check_scan,
tame_check_mantra, opening_lines_scan -- and every one read it with `grep`, so five green readings
stood over bytes no compiler had accepted for weeks. In `%471`, the fleet loop classified a spent
session window by elapsed time alone, could not tell "the agent was unreachable" from "the window is
not open yet", and stopped six of eight ships nine minutes before their reset. Different rooms, one
shape: **an instrument that asks a cheaper question than its name claims, and a green reading that
is only ever evidence about the question actually asked.***

---


**REDS %470 (`20260906.061556`) -- `mantra/src/diff.rye` had not compiled since the Zig 0.16 move, and all five guards over it read it with `grep`.** *What went wrong:* three sites carried `std.ArrayListUnmanaged(T){}`, 0.15's form -- `%449`'s fault one file over, the tree's last three. All five read **by pattern**, so five green readings stood over bytes no compiler had accepted for weeks. *Second fault:* the head promised applying the Diff reproduces `new_text`, **false for any insert not at the end** -- `Diff.inserts` carries no anchor, so `apply` appends: `a c` against `a b c` reads back **`a c b`**. *What caught it:* the probe `%463`'s census asked for. **The cheap probe is confidently wrong** -- an importing probe builds at **exit 0** over a planted type error, Zig analysing lazily; a four-line **comptime declaration walker** reds at `diff.zig:82:46`, greens on the repair. *What it taught:* **five guards are no safer than one when all ask the same KIND of question.** *Repaired:* `.empty`, an honest head, `diff_witness.rye` proving **seven claims by doing**, a control breaking it seven ways whose **sharpest phase is a pass**: the plant rides free once the walker goes. `tier lap` **9s**; `asserted` **1 -> 0**, ceiling with it. *Seam:* an anchor per insert widens `Diff`, pinned against a Glow shape. *Erratum:* booked `%466`, renumbered to `%467` on a peer's publish, and renumbered again to `%470` on `20260906.081500` -- the captain pushed this commit out of the incense seat without running `reds_spine_derive` first, which froze a double-booking into the anointed spine: the compass-rose row (`20260906.053014`) already held `%467` there. The earlier stamp keeps the number, per the law's own worked repair, and because the fold shelf and the fold recital both spell `rows-467-468` in a closed record. Booked whole as `%471`. Whole in [`20260906-062055`](../../session-logs/date/20260906/20260906-062055_the-file-five-guards-read-and-nothing-compiled.kyri). **BOOKED.**

**REDS %471 (`20260906.082415`) -- six of the eight ships stopped themselves nine minutes before the window they were waiting on reset, and the printer that re-arms a stopped ship handed a blank paste to five of eight.** *What went wrong:* `tools/l/fleet-loop.sh` breaks after three laps that each died inside ten seconds, on the reasoning that *the fault is upstream of the agent and no number of retries will reach past it* -- exactly right for the ai-jail flag refusal it was written for (`%414`), and exactly wrong for a spent session limit, which is upstream of the agent AND clears on a clock. Measured off the panes: diffuser stopped **07:21:52**, pheromone **07:21:44**, petrichor **07:21:37**, copal **07:26:00**, grass **07:28:47**; the window reset at **07:30**. Only bakery lived, because its counter had not reached three when the clock turned. *Second fault, in the one instrument that answers a dark fleet:* `tools/f/fleet_rearm.sh` reads the roster for its REPORT and spelled six seat names by hand for its PASTE, so bakery, diffuser, grass, copal, and patchouli each printed the word `RELAUNCH:` followed by nothing -- five of eight live ships, the seventh copy of the seat table `%409` was written to end. *What caught it:* a hand reading eight tmux panes and asking why seven were at a shell prompt. *What it taught:* **an instrument that classifies by elapsed time alone cannot tell "unreachable" from "not yet", and the two answers are opposite** -- stop and fetch a person, against hold and try again. *Repaired:* `tools/fixtures/f/fleet_lap_verdict.sh` classifies a finished lap `ok|limit|quickfail|fault` and asks the limit question FIRST, since a limit refusal returns instantly; the loop holds `LOOP_LIMIT_WAIT` 300s up to `LOOP_LIMIT_WAIT_MAX` 72 holds without counting a lap, and `%414`'s own wound still stops the loop at three. Control **12 of 12**, both refusals bitten by hand -- the elder order reads `quickfail` on the limit transcript, an unwired loop fails case 12. The paste is derived from the roster now, gated at `empty_pastes=0` by `tools/fixtures/f/fleet_rearm_paste_scan.sh`; the elder form reads **1** in the pen and **5** against the real fleet home. *Erratum, owned:* the captain pushed pheromone's finished commit out of the incense seat without running `reds_spine_derive` first, freezing a double-booked `%467` into the anointed spine; repaired by moving the later stamp to `%470`. **BOOKED.**
