# REDS -- rows %166 and %167, the anchored pattern and the borrowed list

**Folded here** `20260823.144100` from [`../REDS.md`](../REDS.md), every byte kept, on the lap they
were booked, as rows %168 through %170 carried the living pin past its 24,576-byte bound. Both are
CLOSED. Their shared root is the one row %169 states in general: a fold changes location, so every
rule keyed on location is a rule the fold silently retires.

---

**REDS %166 (`20260823.144100`) -- an ignore rule anchored to a path is a promise about a location, and a fold moves locations.** *What went wrong:* `.gitignore` withheld six personal files by anchored path -- a leading slash, the room name, and the basename, for `key-card.conf`, `enclosure.conf`, `secrets.env`, `launch-cursor.sh`, `launch-zed.sh`, and `gh-token.secret`. The tools fold moved every flat entry one directory deeper, `enclosure.conf` came to rest at `tools/e/enclosure.conf`, the anchored pattern stopped matching, and the very next `git add -A tools/` **staged one person's host configuration for commit**. *What caught it:* reading the staged list rather than trusting the add -- `git status --porcelain tools/ | grep '^A'` printed `tools/e/enclosure.conf` at the head of the twenty-six additions. *What did not happen:* nothing was committed and nothing left the pier; the file was unstaged with `git rm --cached` in the same minute. *What it taught:* **a gitignore pattern with a leading slash is a promise about a location, and any fold is a change of location.** The six names now match at any depth, with no slash at all, so a future fold, a hand that drops the file flat again, and a room not yet invented all stay covered -- each name checked unique in the tracked collection first, because privacy comes before completeness. The deeper reading is the one to carry: a fold moves files, and every rule keyed on WHERE a file sits is a rule the fold silently retires.

**REDS %167 (`20260823.144100`) -- an exclusion list borrowed wholesale asserts that two tools ask the same question about the same references.** *What went wrong:* the new tools repointer sourced `tools/fixtures/dated_path_exclusions.sh`, on the honest reasoning that both tools ask *what is the field and what is the instrument*. They do. The answer differs, because the references differ: that list protects deliberately stale **dated** citations, and `dated_path_witness.rish` cites a flat `counsel/...` path to prove the resolver recovers it. Excluding the file wholesale also protected its live invocation of the dated resolver -- an ordinary living reference the fold had moved -- so the witness ran a tool at a path that no longer stood. *What caught it:* the standing-equipment roster, on the run immediately after the move: `dated_path red`, three siblings red beside it. *What it taught:* **an exclusion list is scoped to a meter, never to a file.** Only `tool_path_*` cites a `tools/` path that must stay stale, and the repointer names that one and nothing else. REDS %121 said keep one list; it did not say borrow another meter's.
