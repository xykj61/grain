# REDS %220 -- a lap ended at `git add` for the second time in two days, and the close it skipped had grown to twenty-five minutes

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.132121`, **CLOSED** -- the staging
close firing a second time, and the roster tier that made the close affordable. Moved here as row %221
carried the living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %220 (`20260825.110922`) -- a lap ended at `git add` for the second time in two days, and the close it skipped had grown to twenty-five minutes.** *What went wrong:* the round that seated the witness-reach meter staged **37 paths** and stopped before `git commit`. Three guards read the tree it left. `readme_metrics` and `geode_libraries` compare a generated page against a fresh render, and [`../../tools/hooks/pre-commit`](../../tools/hooks/pre-commit) regenerates both **at commit time**, so a lap that never commits leaves both stale. `readme_reach` found three Markdown link targets in `construction/archive/REDS-a-citation-in-a-comment-rows-218.md`, a file that same lap wrote, spelled `](../../tools/...)` from a directory two levels down -- the link text beside each one already read `../../tools/...`, so the display was right and the target was wrong in all three. *What caught it:* the cold roster that opens a lap, reading **60 guards, 57 green, 3 red**, against the previous lap's own cold reading of 58 of 58 green. *What it taught:* **a close that costs twenty-five minutes is a close that gets skipped, and the fix is the price rather than the reminder.** This is [REDS %188](REDS-staging-and-the-page-that-named-half-rows-188-189.md) firing a second time -- `20260824.082144`, then `20260825.092953` -- and a lantern that fires twice becomes a loom. %188 concluded that no guard can enforce this, since such a guard would have to run after the lap ends. That holds for **enforcement** and was wider than it needed to be about **reading**: `tools/fixtures/standing_equipment_run.sh` now prints `staged_uncommitted` before it runs a single guard, so this lap would have opened on `staged_uncommitted=37` at line one rather than meeting the consequence eleven guards later. On a hot run the same number is the round's own work and means nothing; on the cold open it is this row's signature. *Repaired:* the three link targets repointed, both generated pages rewritten with their own writers (`readme_metrics.rish write`, `geode_libraries.rish write`), all three guards GREEN on metal, and the whole staged round shipped as one signed commit. *Prevention, the structural half:* the roster grew a **tier**, so the every-lap set stays affordable while a choir is heard on the cadence lap -- `lap` and `cadence`, a record naming neither meaning `lap`, and any third word refused at zero by [`../../tools/fixtures/standing_equipment_scan.sh`](../../tools/fixtures/standing_equipment_scan.sh). The first cadence row is `tools/cr/crypto_suite_witness.rish`, nine minutes for 74 rungs, which stood **unheard** entirely until this lap. GREEN on metal. CLOSED.
