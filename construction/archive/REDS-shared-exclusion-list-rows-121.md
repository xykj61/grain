# REDS -- row 121, folded from the living ledger

*Folded `20260821.210517` under the living-pin bound (24,576 bytes). Every byte kept; nothing edited. The living ledger at [`../REDS.md`](../REDS.md) carries the pointer.*

**The range:** the shared-exclusion-list round -- a repointer that disarmed the fixture proving it was needed, closed by putting one exclusion list in one file that both dated-path tools read.

**REDS %121 (`20260821.201333`) -- the repointer quietly disarmed the test that proves it is needed.** *What went wrong:* `tools/d/dated_path_witness.rish` holds a **deliberately stale** reference -- the flat path a session log answered to before its room folded -- because proving the resolver recovers such a path requires having one. The repointer's apply run rewrote it to the folded path (commit `0d31946c59`), which left the assert checking that an **already-correct** path resolves. True, and worthless: the rung that proves the fold rule works had been proving nothing. *What caught it:* running the guards. Not one of the rounds that touched this tool caught it, because the witness still exited GREEN for a while and a green line is exactly what stops a person looking. It only reddened once the fixture's neighbours drifted too, and `git log -S` on the changed line named the commit in one query. *What it taught:* **a tool that edits its own test fixtures disarms them silently.** The census had already learned this and excluded `dated_path_*` by name; the repointer, written the same day for the same corpus, never got the same exclusion -- so the lesson was recorded in one instrument and not carried to its sibling. Two tools with one concern need one exclusion list, or they will disagree exactly where it costs most. *The repair:* the fixture is restored to the stale path it exists to prove, and the repointer now skips `dated_path_*`, `room_bound_control.sh`, and `session_logs_archive.rye` -- the same names the census already skips. *Three smaller things the same guard run found, all illustrative rather than broken:* a `slice` example in the new Rishi field guide used a real-looking dated filename as a literal string, now placeholders per the mark law's own clause; two index rows shortened a real filename with an ellipsis, which reads as a broken path, now written in full; and a control's planted page joined the census exclusions. `lost` fell 193 -> **192** and the ceiling ratcheted down with it. *One thing worth owning plainly:* repairing the elided index rows, I typed a filename from memory -- `the-qoi-cleanroom-study` -- for a file actually named `sel4-microkit-caravan-cleanroom-study`. Caught by listing the directory a second later. A path written from memory is a path invented, every time.

**REDS %121 CLOSED (`20260821.202424`) -- one list, in one file, that both tools read.** *The repair, on metal:* `tools/fixtures/dated_path_exclusions.sh` holds the exclusions once, and the census and the repointer both source it. Neither carries a flag of its own; `tools/d/dated_path_witness.rish` proves that by refusing a planted copy that grows one back, and by accepting the real two, which is the half that keeps the rule from becoming a nuisance. *What the unification found in the act of doing it, which is the argument for doing it at all:* the two lists **had already diverged again** within the hour of booking this row -- the census had gained `shipped_binary_claim_control.sh` and the repointer had not. Twice is carelessness; three times inside a day is structure. *And what the shared list needed that neither private copy had:* a **path** dimension. `docs-geode/demos/README.md` demonstrates the resolver recovering a stale reference, which requires quoting one, and it prints the answer it gets -- so the repointer wanted to rewrite it, which would have made the page's own quoted output wrong. Its basename is `README.md`, which no name-match could exempt without exempting every README in the tree. The list now carries exact paths beside names, and the same one-sentence test governs both: **is this the field, or the instrument?** *One regression made and caught in the same pass:* sourcing the shared file by a relative path broke the census's own control corpus, which runs it from a throwaway tree. It resolves from the script's own directory now -- the sha3 shim taught this exact lesson hours earlier, and it had to be taught again, which is its own small argument for reading the tree before adding to it.
