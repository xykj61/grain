# REDS -- the room folded and its index did not, so the index grew alone (row %182)

*Folded from [`../REDS.md`](../REDS.md) on `20260824.075409`, **CLOSED**, as row %187 carried the living pin past its 24,576-byte bound.*

**REDS %182 (`20260824.052950`) -- the room folded and its index did not, so the index grew alone.** *What went wrong:* `session-logs/` folds its logs by day into `date/YYYYMMDD/`, and `tools/rye/session_logs_archive.rye` repoints each moved link in `README.md` as it goes. It moves no rows. So every fold since `20260821.161758` shrank the room and left the index whole, and the index reached **2,895,849 bytes** -- **117.8x** the **24,576** its own header declares -- while the room beside it sat at **181** flat files against a bound of 256, green the whole time. Three faults rode along, each what an unbounded file looks like from a reader's side: the title `# Session logs` stood at **line 1,881** beneath 1,880 rows, because *prepend a newest-first row* means line 1 and the header had nowhere to go; **not one** of the 1,658 pipe rows sat under a delimiter row, so by the GitHub-Flavored Markdown spec the file held **no table at all**, 1,658 lines of literal pipes; and two row shapes were interleaved, 1,658 table rows against 714 list items. *What caught it:* `wc -c` over every tracked README while looking for the next crux -- and then, correcting a first reading of my own: I took the file for unrostered, and it is **on** `tools/fixtures/living_docs_lint_roster.sh`. Duty 6 of `living_docs_lint` has printed `session-logs/README.md: 2895849 > living_pin_max_bytes=24576` **on every run**, beside three more, and the witness is advisory by design and never fails. *What it taught:* **a measurement that only ever prints is a number rather than a bound.** The lantern fired every lap for a month over four documents; nothing turned it into a loom. And the deeper one: **two surfaces describing one set need one rule, not two numbers that happen to agree** -- the room bound and the pin bound were separate promises, so the room could pass while the index it indexes ran 118x over. *Repaired:* `fold_index` in `tools/rye/session_logs_archive.rye`, reached by `index-preview` and `index-fold`, moves each closed day's rows onto `session-logs/date/README-index-YYYYMMDD.md`, rewriting `](date/YYYYMMDD/name)` to `](YYYYMMDD/name)` -- **2,193 rows onto 26 shelves**, pin **2,895,849 -> 266,790**, and a table header written for a day whose rows are table rows. A day folds only once its logs have, so a shelf's links are computable once and correct forever. The pin's head is rewritten with the title first and the delimiter it never had; `SEASONS.md` gains 26 rows and its three elder `archive/` display texts are repaired. **Checkpoint `4b166d337e`.** *Prevention* seated as the **39th** standing guard: [`../tools/i/index_fold_witness.rish`](../../tools/i/index_fold_witness.rish) over [`../tools/fixtures/index_fold_scan.sh`](../../tools/fixtures/index_fold_scan.sh), which gates **`session-logs` at zero** stale rows and ratchets `active-designing` 86, `counsel` 112, `expanding-prompts` 78 and `waymarks` 41 -- **317** together -- under a ceiling that only falls. The gate is the **row rule** rather than a byte ceiling, because a byte ceiling refuses ordinary work and a guard that reds on the ordinary is a guard someone turns off; a row goes stale only when its logs fold, and one tool folds both. Proven both ways by [`../tools/fixtures/index_fold_control.sh`](../../tools/fixtures/index_fold_control.sh) -- fifteen behaviors on real directory trees in a throwaway pen, the ceiling shown from both sides by planting 317 rows and then 318, so no override exists and none is wanted. *Open, and named rather than closed:* the pin stands at 266,790 bytes, over its declared bound, because 180 logs are still flat in the room and folding the room is Keaton's word (`.claude/rules/session-logs.md`). Say **fold the logs** and the pin holds one day. CLOSED as to the fault; the byte remainder is a gate, not a red.
