# REDS %233-%236 -- the leaves that sing: a contradiction, a sweep's downstream, a pinned census, and three stale readers

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260826.001630`, all four **CLOSED** --
the two season rungs that demanded opposite things for twenty-five days, the mechanical sweep that
repaired one file and broke the file it reached, the three guards that pinned an exact count of a
population built to grow, and the three that cited content a legitimate move had left behind. Moved
here as rows %240, %241, and %242 carried the living pin past its 24,576-byte bound. These rows
cite paths in backticks rather than links, so nothing needed deepening.*

---


**REDS %233 (`20260825.235254`) -- two guards in one family demanded opposite things, and neither could ever be green.** *What went wrong:* `tools/fixtures/equinox_e121_roots_bench_amend_scan.sh` required the Lexicon's **roots** row to name `the Framework itself` and `a counsel container in the cloud`. `tools/fixtures/equinox_e122_roots_bench_kinds_scan.sh` **refuses** on exactly those two phrases. e121 had blurred *root* and *Bench*; e122 was seated the same evening (`20260731.221131`) to sharpen them and removed both entries from the row. From that commit forward the two rungs were mutually unsatisfiable -- e121 red whenever e122 was green, and the reverse -- for twenty-five days. *What caught it:* this lap's leaf sing, running e121 for the first time since it was written. *What it taught:* **a guard nobody runs cannot be told from a guard that agrees with its siblings.** A contradiction between two checks is the cheapest fault a suite can detect and the one it will never detect while neither check has a clock. Consistency between guards is not a property of the guards; it is a property of running them. *Repaired:* e121 now reads what its own successor seated -- it refuses the two phrases e122 removed and requires the row to distinguish the Bench -- so the pair states one law twice rather than two laws once. Both sing in the new leaf choir.

**REDS %234 (`20260825.235254`) -- a mechanical sweep taught living code two things that were never true.** *What went wrong:* two faults with one shape. `tools/gen/season/equinox_bundle_manifest.sh` computed `SHA3_ROOT` as `dirname "$0"/../..`, which from `tools/gen/season/` lands on `tools/`, then asked for `$SHA3_ROOT/tools/fixtures/sha3.sh` -- `tools/tools/fixtures/sha3.sh`, a path that has never existed. And the name-return sweep of `20260823.101837` rewrote `Keaton Livermore` to `Keaton Dunsford` across 35 references in 20 living files while correctly leaving 28 files of dated testimony untouched; `equinox_e136_mantrapod_supersede_scan.sh` is living so it was swept, and the pitch it reads is testimony so it was not, which left a guard demanding a name its subject can never say. *What caught it:* the leaf sing -- e129 exiting **127** with no reason printed, and e136 printing `detail=want_keaton`. *What it taught:* **a sweep repairs the file it edits and can break the file that file reaches.** A relative hop and a cited string are both reach, both invisible to a link scanner, and both silent until something runs the code. **And a fallback branch behind a `set -e` command substitution is unreachable:** `X=$(cmd)` takes the assignment's own status, so `RC=$?` on the next line is never read and the carefully-written `bundle_sha3 unavailable` branch died with the shell instead of printing. Proven both ways on metal. *Repaired:* three directories up rather than two; `|| true` on the substitution so the branch it guards is live; and e136 accepts either name, because a guard citing dated testimony inherits accrete-never-break along with the citation.

**REDS %235 (`20260825.235254`) -- three guards pinned an exact count of a population built to grow.** *What went wrong:* `tools/fixtures/date_dialect_scan.sh` and `equinox_e111_date_dialect_scan.sh` each required exactly **17** files under `context/*.md` to carry a date label; the tree measured **25**. `equinox_e141_commence_amphora_scan.sh` and `equinox_e142_q2_cli_doc_scan.sh` each required exactly **10** tracked files and **9** `.rye` in `amphora/`; the tree measured **11** and **10**. Every one of the five extra files is a file the tree was right to add, correctly carrying the label or the extension the guard was counting. *What caught it:* the leaf sing; four of the twelve refusals resolved to the `17` pin alone, through two chains. *What it taught:* **an exact count of a growing population is a refusal with a date on it, and the date is whichever lap adds the next one.** The proof these rungs were written for survives the change -- zero hyphenated labels, one dialect, no new `.rye` on that rung's own lap -- and only the census was ever fragile. `amphora/src/main.rye`, added by e143's own seating, broke e141 and e142 on the commit that landed it. *Repaired:* each pin became a floor that only rises, with the measured number printed beside it and the invariant check left exact where exactness is the point.


**REDS %236 (`20260825.235254`) -- three guards cited content that a legitimate move left behind.** *What went wrong:* `equinox_e140_align_amphora_scan.sh` grepped `construction/TASKS.md` for the live lap edge; TASKS fused into `construction/ITINERARY.md` on `20260823.103804` and is a 1,220-byte pointer now. `equinox_e50_geode_expedition_witness.rish` asserted the Lexicon's umbrella senses separated by U+00B7 middots; the ascii-first sweep converted every one to a hyphen, which also left a non-ASCII literal sitting in a tracked source under the law that forbids it. `equinox_season_e0_witness.rish` asserted that `glow/bin/mod-clock` exists; `git check-ignore` names `.gitignore` line 211, so `glow/bin/` is build output and no fresh clone can ever satisfy it. *What caught it:* the leaf sing. *What it taught:* **a guard is a reader, and a reader has to be moved when the thing it reads moves.** The fusion, the sweep, and the ignore rule were each correct; what went unrepaired was the code reading downstream of them. **A guard asserting a build artifact should build it** -- the sibling rungs already do, and the toolchain is vendored in this tree. *Repaired:* e140 reads the pointer or the card the pointer names; e50 cites the separator the ascii-first law leaves and is ASCII itself; e0 builds `glow/mod_clock.rye` with `vendor/zig-toolchain/zig` and then runs it, which is the reading it always wanted.
