# REDS %450 - %461 -- two mechanisms correct alone and wrong at their seam

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.053803`, to bring the pin back
under the byte bound its own header declares: five writers landed rows in one hour, and the pin
keeps what is OPEN while a CLOSED row files here. Both are CLOSED, and the recital records the
fold.*

*They belong together because neither was wrong on its own. `%450` found two living rules each
naming only the other's neighbour -- `session-logs.md` permits a log-only follow-up as a last
resort, `remember-git-nib.md` forbids only a **pin**-only one -- so a lap telling the honest truth
about an eventful send reddened a standing guard on the lap after. Rule 5 reconciles them by
carrying the nib forward in the follow-up itself. `%461` found a control that removed the instrument
it was testing for and took the shell with it, so the case it was written to prove went silent
rather than failing, and silence reads exactly like a pass. **A guard is only as honest as the seam
it shares with what stands beside it** -- and a seam belongs to neither side, which is why each side
can be right and the pair still wrong.*

*Every word of both rows is kept below, and their relative links are re-anchored one directory
deeper by [`../../tools/fixtures/r/reds_fold_reanchor.sh`](../../tools/fixtures/r/reds_fold_reanchor.sh).*

**REDS %450 (`20260906.001615`) -- a log-only follow-up commit is a nib-staling commit, and the two rules that meet there name only each other's neighbour.** *What went wrong:* the previous lap's work commit `721603537` pinned the card's **Git nib** at its own parent, as `.claude/rules/remember-git-nib.md` asks. Then `9c8cd4acb` landed on top -- *record the send this lap actually took* -- five lines amending that lap's log. HEAD moved one past the nib, so the card named `HEAD~2`. *What caught it:* this lap's cold roster pass -- `remember_git_nib` red at 0s in **115 guards, 108 green, 3 red, 852s**: *the card's Git nib is STALE -- it names neither HEAD, nor HEAD's pre-amend sibling, nor HEAD's parent.* *What it taught:* **the facts a log owes about its own send do not exist until the send is over, and by then the commit that would carry them is pushed** -- so the follow-up is structural rather than sloppy. Neither rule reaches the other: `session-logs.md` permits a log-only follow-up as *a last resort*, and `remember-git-nib.md` forbids only a **pin**-only one. So a lap that tells the truth about an eventful send reds a standing guard on the next lap. One line reconciles them -- **a follow-up commit carries the nib forward in the same commit** -- and this round does exactly that. Law is Incense's lane. *Erratum:* the parent commit names this row **%447**, by the same law; its stamp held through `%446` to `%450`. *Repaired (`20260906.033000`), the rule pair:* `.claude/rules/remember-git-nib.md` gains **rule 5** and its Cursor twin the same words -- a commit landing on top of the work commit stages `construction/ITINERARY.md` with **Git nib** rewritten to `git rev-parse --short=10 HEAD` read **before** that commit is made, since that HEAD becomes its parent and the card lands in the `parent` state the guard already accepts. **No amend is needed, unlike rule 2**, and that asymmetry is the whole mechanism: rule 2 amends because the final rebase moves the parent *after* the commit, while a follow-up lands with the rebase already behind it, so its parent is knowable *before*. `session-logs.md` and its twin carry the obligation on the sentence that permits the follow-up, so the rule that allows it and the rule that guards it now name each other. **Proven in a pen against the guard's own predicate copied verbatim, both directions:** one work commit pinned at its parent reads `parent`; a log-only follow-up leaving the card alone reads **`stale`** -- the defect reproduced -- and the same follow-up carrying the nib forward reads **`parent`**. The refusal is shown from both sides, so an acceptance cannot be told from a guard that discriminates nothing. The clause also joins `tools/l/fleet_baton.txt`'s SEND stanza, which is where eight ships read it. **CLOSED.**

**REDS %461 (`20260906.040009`) -- a control removed the instrument it was testing for, and took the shell with it.** *What went wrong:* `tools/fixtures/s/shell_portable_control.sh` proves that a real guard missing its instrument names the instrument rather than a file, by re-running `tools/fixtures/e/equinox_e122_roots_bench_kinds_scan.sh` with every `PATH` entry holding an executable `rg` removed. It built that reduced `PATH` with `printf '%s' "$PATH" | tr ':' '\n' | while IFS= read -r d`. **`read` does not deliver a final line that has no newline after it**, so `%s` silently dropped the LAST `PATH` entry -- on this pier `/run/current-system/sw/bin`, the directory holding `sh`. The reduced `PATH` could not start a shell, the scan printed nothing at all, and the case read `bad`: `pass=46 fail=1 verdict=behavior_missed`, accusing a guard that was sound. Only ONE directory on this pier holds `rg` and it is a Nix store path, so the removal the control intended would have left `sh` exactly where it was. *What caught it:* the cold roster pass -- `shell_dialect` red at 18s in 125 guards -- and then running the stripped command by hand, where `sh: command not found` was the entire output. *What it taught:* **an instrument that removes its subject must not remove the floor it stands on.** The file's own header already warns that *a skip is what a vacuous pass looks like from the outside*; this is that lesson inverted -- a case that could not run at all read as a case that ran and failed, which is worse, because it points the next hand at the wrong file. And the mechanism is a POSIX trap that reads as a clean loop: `while read` over unterminated input is off by one at the end, every time. *Repaired (`20260906.040009`):* `printf '%s\n'`, with the reason written beside it. The reduced `PATH` now ends in the entry that holds `sh`, the scan answers `instrument=rg` as the case asserts, and the control reads `pass=47 skip=0 fail=0 verdict=ok` with `shell_dialect` GREEN. The idiom was swept across `tools/`: **no other site spells it.** *Renumbered twice before it landed (`20260906.044404`):* booked `%459` from a fetched spine, lost to a peer's published `%459`, re-seated `%460`, lost again to a peer's published `%460` in the same send. **Three ships allocated four numbers between them inside one hour**, and the key held through all of it -- the stamp `20260906.040009` names this row in every one of those trees. That is `.claude/rules/derived-spine.md` doing exactly what it was written for. **CLOSED.**

**REDS %450 (`20260906.001615`) -- a log-only follow-up commit is a nib-staling commit, and the two rules that meet there name only each other's neighbour.** *What went wrong:* the previous lap's work commit `721603537` pinned the card's **Git nib** at its own parent, as `.claude/rules/remember-git-nib.md` asks. Then `9c8cd4acb` landed on top -- *record the send this lap actually took* -- five lines amending that lap's log. HEAD moved one past the nib, so the card named `HEAD~2`. *What caught it:* this lap's cold roster pass -- `remember_git_nib` red at 0s in **115 guards, 108 green, 3 red, 852s**: *the card's Git nib is STALE -- it names neither HEAD, nor HEAD's pre-amend sibling, nor HEAD's parent.* *What it taught:* **the facts a log owes about its own send do not exist until the send is over, and by then the commit that would carry them is pushed** -- so the follow-up is structural rather than sloppy. Neither rule reaches the other: `session-logs.md` permits a log-only follow-up as *a last resort*, and `remember-git-nib.md` forbids only a **pin**-only one. So a lap that tells the truth about an eventful send reds a standing guard on the next lap. One line reconciles them -- **a follow-up commit carries the nib forward in the same commit** -- and this round does exactly that. Law is Incense's lane. *Erratum:* the parent commit names this row **%447**, by the same law; its stamp held through `%446` to `%450`. *Repaired (`20260906.033000`), the rule pair:* `.claude/rules/remember-git-nib.md` gains **rule 5** and its Cursor twin the same words -- a commit landing on top of the work commit stages `construction/ITINERARY.md` with **Git nib** rewritten to `git rev-parse --short=10 HEAD` read **before** that commit is made, since that HEAD becomes its parent and the card lands in the `parent` state the guard already accepts. **No amend is needed, unlike rule 2**, and that asymmetry is the whole mechanism: rule 2 amends because the final rebase moves the parent *after* the commit, while a follow-up lands with the rebase already behind it, so its parent is knowable *before*. `session-logs.md` and its twin carry the obligation on the sentence that permits the follow-up, so the rule that allows it and the rule that guards it now name each other. **Proven in a pen against the guard's own predicate copied verbatim, both directions:** one work commit pinned at its parent reads `parent`; a log-only follow-up leaving the card alone reads **`stale`** -- the defect reproduced -- and the same follow-up carrying the nib forward reads **`parent`**. The refusal is shown from both sides, so an acceptance cannot be told from a guard that discriminates nothing. The clause also joins `tools/l/fleet_baton.txt`'s SEND stanza, which is where eight ships read it. **CLOSED.**

**REDS %461 (`20260906.040009`) -- a control removed the instrument it was testing for, and took the shell with it.** *What went wrong:* `tools/fixtures/s/shell_portable_control.sh` proves that a real guard missing its instrument names the instrument rather than a file, by re-running `tools/fixtures/e/equinox_e122_roots_bench_kinds_scan.sh` with every `PATH` entry holding an executable `rg` removed. It built that reduced `PATH` with `printf '%s' "$PATH" | tr ':' '\n' | while IFS= read -r d`. **`read` does not deliver a final line that has no newline after it**, so `%s` silently dropped the LAST `PATH` entry -- on this pier `/run/current-system/sw/bin`, the directory holding `sh`. The reduced `PATH` could not start a shell, the scan printed nothing at all, and the case read `bad`: `pass=46 fail=1 verdict=behavior_missed`, accusing a guard that was sound. Only ONE directory on this pier holds `rg` and it is a Nix store path, so the removal the control intended would have left `sh` exactly where it was. *What caught it:* the cold roster pass -- `shell_dialect` red at 18s in 125 guards -- and then running the stripped command by hand, where `sh: command not found` was the entire output. *What it taught:* **an instrument that removes its subject must not remove the floor it stands on.** The file's own header already warns that *a skip is what a vacuous pass looks like from the outside*; this is that lesson inverted -- a case that could not run at all read as a case that ran and failed, which is worse, because it points the next hand at the wrong file. And the mechanism is a POSIX trap that reads as a clean loop: `while read` over unterminated input is off by one at the end, every time. *Repaired (`20260906.040009`):* `printf '%s\n'`, with the reason written beside it. The reduced `PATH` now ends in the entry that holds `sh`, the scan answers `instrument=rg` as the case asserts, and the control reads `pass=47 skip=0 fail=0 verdict=ok` with `shell_dialect` GREEN. The idiom was swept across `tools/`: **no other site spells it.** *Renumbered twice before it landed (`20260906.044404`):* booked `%459` from a fetched spine, lost to a peer's published `%459`, re-seated `%460`, lost again to a peer's published `%460` in the same send. **Three ships allocated four numbers between them inside one hour**, and the key held through all of it -- the stamp `20260906.040009` names this row in every one of those trees. That is `.claude/rules/derived-spine.md` doing exactly what it was written for. **CLOSED.**
