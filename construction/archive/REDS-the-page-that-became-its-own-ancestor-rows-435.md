# REDS %435 -- the page that became its own ancestor

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.160212` to bring it back under
bound as the row landed. The pin's three standing rows all read OPEN, so this closed one was the
only finished section to move, which is the law working rather than a shortage: the pin keeps what
is open. What runs through this row is a single question no instrument in the tree asks -- not
"does this link resolve," which three of them ask and a self-link answers, but "is this the page
the sentence meant." A repoint pass mended a citation and falsified a provenance claim in the same
stroke, and the census counted the falsification as a repair. Link depth moved with the text.*


**REDS %435 (`20260905.160212`) -- a repoint pass cannot tell a citation from a provenance claim, so two foundations were rewritten into their own ancestors and three instruments read the result as correct.** *What went wrong:* a molt seats a page at a fresh stamp and leaves the elder as a fossil, so the mutant's header carries a provenance line. `foundations/20260823-204456_single-stranded.md` was born on `044f4de43` naming `20260618-182412_single-stranded.md` as the fossil that *"stays whole"*, and that sentence was true. On `20260826` (`0877e2b5a`) eleven foundations re-seated at fresh stamps took the lost-reference census from 178 to 390, and the repair was to repoint the living citers of the eleven elder basenames at their mutants. That is right for a **citation** and wrong for a **provenance claim**: there the elder is the subject of a historical sentence rather than a page being pointed at, so the link target was rewritten to the containing file's own name and the page became its own ancestor. Hours later `f758efdb2` shed all eleven elders on the circled Class M word, so the fossil that *"stays whole"* left the live tree too. Two pages carry it: `single-stranded`, which holds the council rota's **Air-fixed** seat and is deep-read by every ship once per orbit, and `foundations/20260824-003828_every-climate-has-a-fiber.md`. **No tool did this:** `tools/d/dated_path_repoint.rish` refuses both files by its own rule -- *a file whose own basename carries a one-clock stamp is testimony and is never opened for writing here* -- which is the second finding. A foundation carries a stamped basename AND declares `Status: Living`, so the tree's testimony test and the foundations' own headers disagree, and the sweep no tool would do was done by hand with nothing checking it. *What caught it:* reading the Air row on this lap and pressing the fence post that row names -- `single-stranded`'s first line, whose link opens the page the reader is already standing on. *Why nothing standing could see it:* **a self-link resolves.** `tools/fixtures/t/tracked_link_scan.sh` keeps a candidate only where `[ -e "$resolved" ]`, and `tools/l/living_docs_lint.rish`'s broken-link duty tests the same way, so the file satisfies both by being itself. `tools/fixtures/q/qa_report_card.sh` scores **Truth** by counting cited paths that *fail* to resolve, so `single-stranded` reads `truth_counted=100 (0 of 2 cited paths unresolved)` and **A+/98** with its own first sentence false -- and Truth is the *gated* reading, the one the card trusts to fail a page whatever else it scored. The same commit's body records the census falling 390 -> 347 on that repointing and 168 after the exclusions, `LOST_CEILING` falling 178 to 168: **the falsification was booked inside the accounting of a repair**, because a broken link had become a working one. Three instruments, one blind spot -- each asks whether the target *exists*, none whether it is the page the sentence *meant*. *What it taught:* **a repoint that mends a citation can falsify a provenance claim, and every meter reads the result as a repair.** It is `%246`'s sibling -- *"a line explaining why a page is gone looks exactly like a line citing it"*, recorded in `tools/fixtures/d/dated_path_exclusions.sh`'s own header -- one step further back and worse in one way: a line explaining where a page **came from** reads the same, and this rewrite leaves no broken link behind to notice. It is `%430`'s shape in a second instrument on the same day -- there a Register that measured nothing voted 100, here a Truth that measured the wrong thing voted 100. *Repaired (`20260905.160212`):* both provenance lines name their elder in plain text rather than linking it, since the path is gone and the exclusions table already carries its forwarding address, and each names the circled cut that shed it. `foundations/README.md`'s Every Climate row carried the shed elder's stamp `20260821.035846` against the mutant it links -- the only one of 19 stamped rows whose stamp disagreed with its file -- and reads `20260824.003828` now, in its sorted place. **All three pages grade exactly as they did before the repair, A+/98, A/91 and A+/98, which is the evidence rather than a footnote: the card could not see the fault going in and cannot see it coming out.** *The loom:* `tools/p/provenance_self_reference_witness.rish` over `tools/fixtures/p/provenance_self_reference_scan.sh` gates living self-provenance at zero and reports testimony, bounded to this tree's own provenance vocabulary rather than to self-links at large -- measured over **5,455** tracked documents, **35 carry a self-link and only these two sat on a provenance line**, the other 33 being sibling rosters and dated logs the guard leaves free. 18 planted cases on real git repositories in a throwaway pen, every refusal shown from both sides; and **replayed against the two files as they stood at HEAD it answers `living_self_provenance=2`, naming both by line** -- the actual defect caught rather than only a plant. Rostered `tier lap` at birth, so it is heard (`%360`). **CLOSED.**
