# Landed accounts -- folded from the live front `20260906.133344`

*Held word for word from [`../ITINERARY.md`](../ITINERARY.md), so the card carries what is OPEN and
what waits on Keaton's word. GRASS's landed half, folded in the round that closed `%493` and seated
the document half of the ASCII-first law. The block's one relative link is re-anchored a level
deeper here (`%474`), which is the correction a fold owes every link it moves.*

---

**GRASS -- `%474`'s LOOM LANDED** (`%491` CLOSED): 345 depth-lost links over 276 shelves, repointed
by program, proven to move no prose. **Four link guards miss it, three for ONE reason** -- a fold
shelf's basename carries a stamp, stamped means testimony, and testimony is where `tracked_link`
reads past and `readme_reach` stops gating: **a fold's own output wears the property that makes
every instrument let it go.** **Yours, with a number:** the two-level rooms hold **890** more.
**Yours, sized not taken:** a guard's refusal is written where nothing tracked can read it, so a lap
that dies takes its own red with it. Mand's twelve untraced refusal sites stay named
([audit](../../active-designing/20260906-003146_the-refusal-that-leaves-no-trace.md), Caravan parked Dream's).

*The row itself rests on [`REDS-a-folds-output-hides-the-link-it-breaks-rows-491.md`](REDS-a-folds-output-hides-the-link-it-breaks-rows-491.md)
-- a peer's shelf, published while this round rebased, after I had written my own for the same row.
Mine gave way, exactly as my `%492` number did an hour earlier and my `%481` shelf did yesterday.*

---

**GRASS -- A GUARD SEATED AT 12:26 TODAY RED EVERY TREE IN THE FLEET.** `%492` CLOSED.
`sow_allow_reach` reads the SHIPPED side, so its scan opens `seed/` -- **gitignored, built by
`sow.rish`, absent from every fresh clone** -- and refuses rather than reporting clean, which is
right (`%170`). Rostered `tier lap` with no `capability` it was the one red in **119 guards, 778s**,
and a red withholds the receipt, so **eight ships pay a full cold pass a lap**. Closed by
`capability seed_projection`, probed with the same `test -d` the guard's own scan performs.
**A guard's precondition belongs in its roster row** -- the reading this card gives an empty `vendor/`.
**AND ASCII-FIRST NAMES THREE SUBJECTS; ONE HAD A METER READING 8 FILES OF 5,529.** Comments: two
meters, whole subject. Commit bodies: **0 of the last 400**, so I built no wall. Documents: **63 of
the 104 rule pages an editor reads as law -- carrying the ASCII law itself -- held 859 characters,
every one on the rule's own table.** `ascii_document` ENFORCES both rule rooms at zero and ratchets
**347** living pages at **3,956**; the 63-file sweep **proved no prose moved by re-deriving all 104
from their committed bytes**. **Its own first draft read a clean zero for all 5,529** -- awk refused
a comment between a trailing `||` and its newline and the scan swallowed it.
**`%491` folds** to its [shelf](20260906-133344_itinerary-landed-accounts.md), carrying
**890** more depth-lost links and a refusal nothing tracked can read.

*`%492` rests on the living pin; the ASCII document meter is rostered as `ascii_document`.*

---

**AND THE REPORT CARD ASKED FOR A ROUND, SO I RAN IT.** `qa_report_card.sh` counted a link quoted
inside a backtick span as a citation and, finding no such file, took **20 points of Truth** for it.
Its own note named the gap and declined to guess: *"a link inside a fenced block or a backtick span
reaches 20+ documents and wants its own round with its own measurement."* **The measurement:** 30
wholly code-spanned link shapes across 12 tracked pages, and every one of them sits in a rule page,
a design essay, or a REDS shelf **teaching the fold rule** -- because `stamp-and-name` asks that an
example path be built from placeholders, `](X)` and `](../../Y)`, and the reading punished exactly
the pages that obeyed. The comments branch had masked code spans since it was written; prose had
not. It does now, fences included, which is what a renderer does. **The mask runs one way** -- it
can only remove candidate targets, so `unresolved` can only fall and a grade can only rise or hold;
nothing that resolves today stops resolving. Seven control legs, both halves planted: a quoted
shape and a fenced link cost nothing, a genuinely broken link in plain prose still costs and is
still named, and a line carrying both keeps the real one and cites once. This shelf's own sibling
read **59 -> 82** on the repair. **Not a red -- a booked lap landed**, since the instrument had
already written down what it could not yet measure.


---

**AND THE NUMBER WAS ALREADY TAKEN, WHICH IS THIS DAY'S FIFTH DUPLICATION.** I booked the guard's
defect as `%492` from a `--next` read that was fresh when I took it and forty minutes stale by the
time I wrote the row. A peer booked the same defect as `%493` at `20260906.133724` -- **OPEN, and
naming the very `capability` mechanism I used**, declining it because the guard's lane neighbours
custody gate `%1` and its instrument was an hour old in another writer's hands. Theirs was
published and mine was not, so **mine gave the number back and became their row's closure**: one
defect, one row, exactly the rule [`derived-spine`](../../.claude/rules/derived-spine.md) seats and
exactly what my own last lap did when a peer folded `%481` while I was folding it. The card's open
question, asked a fifth time in one day: **should an OPEN row carry a claim -- a seat and a stamp --
at start rather than at landing?** Here it would have cost one fetch and saved two hands the same
hour. *Noted in passing, and repaired by the same replacement:* the card's GRASS block had come to
cite `%494` for the loom that is `%491`; the block that carried the drift is the block this round
replaced.
