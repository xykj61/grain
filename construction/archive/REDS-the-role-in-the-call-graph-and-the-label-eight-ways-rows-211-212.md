# REDS %211 and %212 -- the role is in the call graph, and a label written eight ways

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.234500`, both **CLOSED** -- a bin
keyed on function names that no vocabulary could ever have reached, replaced by reachability with two
guards learned by getting them wrong; and a coverage pattern that read one spelling of a label this
tree writes eight ways, calling 206 stated reasons an absence. Moved here as row %213 carried the
living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %211 (`20260824.211500`) -- the fourth correction in four rounds, and the first one that is structural rather than a word.** *What went wrong:* `image/photos.rye` was named as the densest sweep target left anywhere, at **185 uncovered contract asserts**. Its real contract gap is **zero**. The file is a genuine module -- fifteen exported verbs, `crop` through `edges`, ending at line 781 -- and everything past line 1,100 is private functions named `run_hue_turn`, `run_bilinear`, `run_saturate`, called only by its selftest. **200 of its 244 asserts** live there, and all 49 of its real contract asserts were already covered. *What caught it:* opening the file to sweep it and reading the `pub` boundary -- the fourth time in four rounds that a hand opening a file found what the meter could not. *What it taught:* **the role a function plays is a property of the call graph, never of its name**, and no vocabulary would ever have reached `run_hue_turn`. Three rounds tried to fix a naming bin by adding the word it had just missed, which is the shape of a roster (REDS %187) wearing a different coat. *Repaired:* the bin reads **reachability** now -- a private function called only from a proof entry point, directly or through other such functions, is proof whatever it is called -- and the name vocabulary survives only as the seed that marks the entry points. **Two guards make it safe, and each was learned by getting it wrong.** A `pub` function is **never** reached into, whatever calls it: a selftest exercising the real API is what a selftest is *for*, and the first draft swallowed the API it was written to exercise, reading 22,017 contract asserts as **10,285**. And a private helper that a `pub` function **also** calls withdraws to contract, since shared code is contract code. *The reading, corrected four times and now standing:* **92% coverage, gap 1,385** -- against the **13,235** this tree carried into three rounds and two decisions. The arc: 13,235 -> 4,242 (three promises, one denominator, plus 211 symlinks) -> 3,978 (a third role word) -> **1,385** (reachability). *Proven, because a rule that moves a number this far is a rule to prove rather than trust:* [`../tools/fixtures/invariant_coverage_control.sh`](../../tools/fixtures/invariant_coverage_control.sh) runs **fifteen behaviors** on planted modules in a throwaway git pen, both drafts' failure modes among them. *What the board actually holds:* most rooms sit between 92% and 100%. Four hold the gap -- **crypto 271** (57.5%), **linengrow 239** (41.6%), **glow 198** (3.4%), **mand 61** (0.0%) -- and the densest single files are `caravan/regions.rye` at 48 and `glow/tokens.rye` at 36. *Owned plainly:* `image/photos.rye` needed no sweep, exactly as `lattice` needed none, and both were named to Keaton as targets on numbers this meter produced. CLOSED.

**REDS %212 (`20260824.222000`) -- the meter read one spelling of a label the tree writes eight ways, and called 206 stated reasons an absence.** *What went wrong:* `crypto` was named as the next sweep, at **271 uncovered contract asserts and 57.5% coverage**. Its real gap is **65**, and it stood at **89.8%**. The coverage walk matched `// invariant:` and nothing else, while this tree also writes `// invariant (bound):` (237), `// invariant (postcondition):` (93), `// invariant (precondition):` (89), `// invariant (construction):` (23), and the bare `// postcondition:` (26) and `// precondition:` (14) -- **483 lines carrying a qualified spelling the meter could not read.** *What caught it:* opening `crypto/mldsa_encode.rye` to sweep it and finding every FIPS 204 primitive already carrying its reason under `// invariant (precondition):`. Seventh time in seven rounds that a hand opening a file found what the meter could not. *What it taught:* **the qualified spellings are more faithful to the law than the one the meter knew.** TAME asks for a comment naming the reason and names three moments an assert belongs at -- construction, mutation, postcondition -- so a line reading `// invariant (postcondition):` is the seated form with its own category stated, and the meter was rewarding the terser, less informative one. A pattern written from the law's *example* rather than from its *sentence* will do that. *Owned in the same breath, and it is the sharper half:* Keaton was told the check for this had already been run. Asked whether crypto was a placement story or an absence, I measured trailing comments and reported **"absence, not placement -- zero trailing, 280 bare"** -- using the same narrow pattern, so the check was wrong in exactly the way it was meant to rule out. **A check that shares the bug it is checking for confirms it**, which is why the reading was taken again from the file rather than from the meter. *Repaired:* the coverage walk reads `//[ \t]*(invariant|precondition|postcondition)[^:]{0,30}:`, which admits every spelling on disk including the single `// invariant on the callers:`. Two control legs prove it from both sides -- four spellings read as covered, and prose that merely names the word without a colon left uncovered -- bringing [`../tools/fixtures/invariant_coverage_control.sh`](../../tools/fixtures/invariant_coverage_control.sh) to **17 behaviors**. *Landed with it:* `crypto` swept **89.8% -> 100%**, its real 65 closed by **61 blocks** across 27 files -- loop bounds against the arrays they index, buffer lengths against the offsets a standard pins, canonical residues before an unreduced modular add, and the rejection-sampler guards that make an XOF loop end on adversarial output. **Both crypto guards ran GREEN after**: the count bijection over 82 files, and the vendored-parity suite proving our authored Rye byte-for-byte against unmodified Monocypher and PQClean. **The tree reads 95%**, gap **853**, against the 13,235 it carried six rounds ago. CLOSED.
