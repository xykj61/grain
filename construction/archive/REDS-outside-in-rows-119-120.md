# REDS -- the outside-in rounds, rows 119-120 (folded)

**Language:** EN
**Folded:** `20260821.202424` from [`../REDS.md`](../REDS.md) -- a closed-range archive fold to keep the living pin under `living_pin_max_bytes` (24576). Every byte kept, nothing removed; the living ledger keeps row 121 onward and points here.
**Style:** Radiant - **Voice:** Kyri
**Span:** the two rounds that could only be seen from outside -- a command typed on camera that no viewer could run, and a room announced in four places that no reader ever received.

*A red owned in conversation is a memory. A red recorded here is a proof. These rows moved out of the living pin only to keep it readable; they are as binding as the day each was caught.*

---

**REDS %119 (`20260821.192038`) -- a command typed on camera that no viewer can run.** *What went wrong:* reading the manual's other twenty-six pages found the bare-invocation fault twice more, and in the worst possible place. `manual/video-scripts/first-module-walkthrough.md` and `intro-what-is-grain.md` both put `rye build scribe/reader.rye -femit-bin=scribe/bin/reader` **on screen**, under an `[ON-SCREEN]` marker, as a command that types itself and completes. A bare `rye` cannot run: the compiler lives at `rye/bin/rye` inside the clone and never on a PATH, and every build in this tree points at the pinned toolchain by variable. A wrong command in a guide costs a reader a minute; the same command in a video costs everyone who ever watches it, and cannot be corrected in their copy. *What caught it:* extracting every command from every fenced block and checking the tools they name -- thirty-four named, thirty-two real, and the two missing were reader-creates rather than defects. The bare-invocation check was a separate pass, because a path that exists says nothing about whether the command around it can run. *What it taught:* **the further a page is from a terminal, the less it is tested.** A video script is never executed, only performed, so nothing catches it -- and it reaches the most people. *The loom, widened rather than added:* `tools/fixtures/shipped_binary_claim_scan.sh` now carries a second check beside its first, since both are the same concern -- a page telling a reader something untrue about the tools a clone carries. The line between defect and legitimate use is mechanical, which is the only reason it is checkable: a runnable block is fenced ```bash or ```sh, while usage tables and captured version output sit in unlabeled fences, so only labeled ones are read. Archive rooms are exempt for the same reason dated artifacts are. *What this round also found, and it is the happier half:* every specific claim in `manual/reference/rishi-language.md` held -- including `.len` on lists and strings, and `flag args "--name"`, **both of which the new field guide had missed**. Two of the five earlier guides needed nothing, and this room's conformance reference corrected mine rather than the other way round.

**REDS %120 (`20260821.193339`) -- a room announced in four places that no reader ever received.** *What went wrong:* `active-development/` was opened as arc two's deliverable and named in `ORGANIZING.md`, `CLAUDE.md`, `context/LEXICON.md`, a seated rule, and a commit message. **It was never in git.** This tree's `.gitignore` is an allow-list -- line 8 is `/*`, ignoring everything at the root, with each room let back in by name, and its own comment says *"When we add a new top-level project directory, we list it here too."* I did not, so `git add -A` added nothing from it while succeeding loudly on the other files in the same commit. *What caught it:* **the reader's walk** -- cloning this repository to a clean directory and following the first-hour page as written. Nothing else could have. The commit was green, every link resolved, the witnesses passed, and the room existed perfectly on the one machine that never needed it. A defect visible only from outside is invisible to every check taken from inside. *What it taught:* **an allow-list fails silently by construction.** A deny-list forgets and lets something through, which shows up; an allow-list forgets and keeps something out, which shows up **nowhere**, because absence has no error message. Any tree with `/*` at the top of its ignore file needs a guard that asks the reader's question, since no author-side check will ever ask it. *The happier half, and it is the real vindication of a habit:* the walk also ran `tools/room_bound_witness.rish` in the clone, and it went **RED on `room=active-development flat=absent verdict=missing roster=enforce`.** That check was written two rounds earlier on pure principle -- *a room that vanishes from a meter is not a room that passed it* -- with no defect in view and nothing to catch. It caught this one the first minute a clean clone existed. *The repairs:* the room is allow-listed and committed; `tools/documented_room_witness.rish` now asserts that every room `ORGANIZING.md` names is carried by a clone, exempting a symlink to a tracked room and a room the guide names as departed; and the room-bound assertion message, which had sent a reader looking for a room to fold when the room was merely absent, now names both causes. *Two smaller things the same walk found:* the first-hour page promised the reader's own five-line program would print **78 rooms** when a fresh clone prints **71** -- a count measured in one tree and promised to every other, now explained rather than promised -- and `ORGANIZING.md` still described `old/` and `vere/` as live rooms nine days after the lineage drop removed them, now named as departed so the absence is legible.
