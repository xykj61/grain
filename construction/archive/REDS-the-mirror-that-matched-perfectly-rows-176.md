# REDS -- the mirror that matched perfectly (row %176)

*Folded from [`../REDS.md`](../REDS.md) on `20260824.025704`, **CLOSED**, as row %177 carried the living pin past its 24,576-byte bound.*

**REDS %176 (`20260823.212601`) -- a byte-identical mirror is only correct when its links are too.** *What went wrong:* `context/document-mirrors.brix` declares a document's several homes and a guard proves each holds the same bytes. Three commits later the canonical gained six relative links to new foundations, and **eight links broke in the mirror at once** -- `../foundations/x.md` resolves from `context/` at depth 1 and lands on `ember/foundations/x.md` from `ember/voices/` at depth 2. Bare sibling links (`GAUGE_STYLE.md`) break the same way at any depth. **The mirror guard stayed GREEN throughout, correctly: the bytes matched perfectly.** *What caught it:* checking the mirror's links by hand while linking the new foundations -- the guard could not, because it was built to compare bytes and a link's correctness is a property of bytes plus location. *What it taught:* **a mirror is byte-identical and a relative link is depth-dependent, so the two fight unless every link is written to resolve from every home.** The technique is `../<room>/<file>`, which is correct from any room at any depth, and never a bare sibling. *Repaired:* the mirror moved to `ember-voices/` at depth 1 -- Keaton's own second suggestion, which also dissolves the containment inversion the descriptor had already recorded, since **Ember is one of the four voices Kyri carries**. Both sibling links rewritten room-qualified. The scan gained a third reading, `links_unresolved_from_a_home`, gated at zero, with two control cases proving it from both sides: a link resolving from the canonical and failing from a deeper home is refused, and the same link at a same-depth home passes free. CLOSED.
