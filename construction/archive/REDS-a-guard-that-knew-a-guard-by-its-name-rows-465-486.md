# REDS %465, %486 -- a guard that knew a guard by its name

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.120517` at the moment they were
written, because both are **CLOSED** on a witness and every other row standing on the pin is OPEN. A
pair of this length would have carried the pin past the 40,960 its own header declares, so the shelf
is where they belong rather than a concession.*

*The story in one line: `tools/fixtures/u/unheard_guard_scan.sh` finds the guards nothing runs, and
it drew its population by BASENAME -- so a runner wearing neither `witness` nor `suite` was never
counted unheard, it was simply **absent**, and absence looks exactly like health. The limit stood
disclosed in the guard's own header for 36 days with no size beside it. It has a size now: 451
asserting runners wear neither word, 408 of them stand unreached, and 12 of those sing three or more
guards apiece. The largest is `tools/p/parity_ch01.rish`, which calls itself a witness suite on its
own first line, builds and runs 116 Rye programs, sings 281 guards, and stands on no roster -- the
biggest silent choir in the tree, invisible to the census built to find silent choirs.*

*The second row is what happened when the reading was pointed at itself. The closure credits a path
named on any non-comment line, which is deliberate generosity that makes the count a floor. One
assert in the witness naming `tools/p/parity_ch01.rish` -- written to prove the largest silent choir
is said out loud -- was read as a run, and marked that choir heard along with the 281 guards it
sings. A guard whose output is a list of names must never read that output as evidence.*

*What these rows do not reach is written into them and repeated here, because a boundary stated once
is a boundary a later reader takes for a claim: **this census reads REACH and says nothing about
EFFECT.** Which of the 408 may be put on a clock is a question about what a program touches, and the
triage was measured before it was declined -- 326 of the 408 print a GREEN line, `birth_a_clone.rish`
among them, so no name-or-verdict reading separates a guard from an actor. That instrument is still
to be built, and until it is, `unnamed_runners` is reported and never gated.*


**REDS %465 (`20260906.055523`) -- the census that finds guards nobody runs knows a guard by its filename, so a runner wearing neither word is not unheard but absent.** *What went wrong:* `tools/fixtures/u/unheard_guard_scan.sh` draws its population with a `(witness|suite)` basename match. Measured `20260906.055523`, method written down because it took three readings: `git ls-files 'tools/*' | grep -E '\.(rish|rye)$' | grep -v /fixtures/ | grep -vE '(witness|suite)[A-Za-z0-9_.-]*\.(rish|rye)$'` gives **523** candidates, of which those matching `^[ \t]*assert ` number **386**; counting `assert` anywhere reads 455. **77** are unreachable from the roster by the census's own closure, so the true population is at least **1,171** where the published `unheard=1094` runs against a ceiling of **1,108**. *What caught it:* rostering Amphora's ninth guard and finding the census could not see the row just added. *What it taught:* **a disclosed limit whose size is unknown cannot be weighed** -- the header names this shape and cites `%297`; nobody measured it in 36 days. *Priced, declined:* widening reds by 63 on eight ships at once, and **25 of the 77 are actors** -- `birth_a_clone.rish` copies `.ssh`, `make_key_card.rish` reaches gpg, seven `launch-*-chapter.rish` start loops -- so **rostering wholesale reaches custody gate 3 through a meter.** Widen, triage, lower once. BAKERY's seat. *Repaired (`20260906.120134`):* the census carries a second reading beside the first, so the limit has a size. `unnamed_population` **451** -- tracked `tools/*.{rish,rye}` outside the pens, wearing neither word, carrying an `assert` in command position -- `unnamed_heard` **43**, `unnamed_runners` **408** reported, `unnamed_choirs` **12** gated at exactly what was measured, and `elder_reach_gap` **2** giving the narrow rule's own blind spot a number. The elder reading stands within its own ceilings and untouched in rule: 1,833 / 744 / 1,089 / 37 after the rebase, 1,830 / 737 / 1,093 / 37 as measured at `2a3313c06b`, its two ceilings untouched, because it is a fleet-wide ratchet and its narrow rule is complete for its own population. *The triage this row asked for was measured and then declined by name.* This tree's success signature `say "GREEN` looked like the classifier and separates nothing: **326 of the 408** print it, `birth_a_clone.rish` and `make_key_card.rish` among them, so a classifier built on it would have credited the clone-birther as rosterable standing equipment. `unnamed_runners` is therefore **reported and never gated**, from measurement rather than preference: **10 of 10** arrivals into that set over the fourteen days to `20260906`, the `tools/` letter-room fold excluded, were ACTORS -- seven `launch-*-chapter.rish`, `birth_a_clone.rish` which copies a tree's `.ssh`, `chatgpt-mind.rish`, `lap_vocabulary_sweep.rish` -- so a ceiling there would red on the tree's ordinary growth and the only lawful answer to each red would be *never roster this*. *And this row's own numbers were one language short:* `^[ \t]*assert ` is Rishi's spelling alone, matching **0 of the 82** unnamed `.rye` files where `^[ \t]*assert[ (]` matches **63** -- this row's own fault one layer down, living in the pattern that measured it. *The sharpest thing the widening found:* `tools/p/parity_ch01.rish` calls itself a witness suite on its own first line, builds and runs 116 Rye programs, sings **281** guards, and stands on no roster -- **the largest silent choir in the tree, invisible to the census built to find silent choirs.** Control **52** behaviours (was 27), witness GREEN, and the scan reads **0.88s against HEAD's 0.60s** while carrying a second closure, because both closures now read each source once. Its own repair booked `%486`. **CLOSED.**

**REDS %486 (`20260906.120134`) -- the guard that names what nothing runs read its own findings, and the naming made them untrue.** *What went wrong:* `unheard_guard`'s closure credits a path named on **any** non-comment line -- deliberate generosity, since a mention treated as a run makes the count a floor under the real one. `tools/u/unheard_guard_witness.rish` then gained one assert naming `tools/p/parity_ch01.rish`, written to prove the largest silent choir is named out loud. The closure read that assert string as a run and marked the choir **heard**, and the **281** guards it sings with it: `unnamed_heard` **42 -> 194**, `unnamed_runners` **408 -> 256**, `elder_reach_gap` **2 -> 102**. One line moved 152 files' status, and the reading the line exists to publish is the reading it silenced. *What caught it:* running the witness in the same minute the assert was written, and reading numbers that disagreed with the scan run four minutes earlier. *What it taught:* **an instrument whose output is a list of names must never read its own output as evidence.** REDS %463's census disarmed its own gate by being committed, classifying itself as a compiling runner and crediting every path its comments mention; `%475` found a guard counting its own verdict in the record it reads. Three shapes, one root -- **a guard's own words are its product, never its evidence** -- and the third arrived inside the lap that closed the first two's family. *Repaired (`20260906.120134`):* the scan writes its own three paths to `$work/self.txt` and filters them out of both closures' source lists in one pass. Proven from the failing side rather than only the passing one: two identical pens differing **only in the accuser's path** -- at `tools/u/unheard_guard_witness.rish` the silent choir stays silent and nothing it names is credited, and at `tools/u/other_witness.rish` the same pen reads the choir heard, so the exclusion is what does the work rather than the pen being trivially green. *Renumbered once:* booked `%485` from a `--next` read at `20260906.120134`, and the rebase before the send found a peer had spent that number on `20260906.113014` and folded it straight to a shelf, so it never appeared in the living pin this seat had fetched. The earlier stamp keeps the number and this row took `%486` -- the derived spine taking its ordinary course, **the stamp holding throughout**, and the seventh firing of the card's own standing question about whether an OPEN row should carry a claim, a seat and a stamp at START. **CLOSED.**
