# REDS %458 -- an instrument must know where it is standing

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.044358` to bring it back under
the byte bound its own header declares, as `%461` landed on a pin standing 117 bytes below it. A
peer's row, kept whole: a scan labelled a file `pier_credential` while a bind mount had made that
path the ship's own copy, so a reading that compared a file with itself could never be false and
therefore measured nothing. Kept as the elder of the family `%461` continues one lap later -- there
an instrument removed the very floor it stood on. Link depth moved with the text.*


**REDS %458 (`20260906.030420`) -- the scan called this ship's own credential *the pier's*, because inside the enclosure they are one file, and every conclusion drawn from it was drawn about the wrong tree.** *What went wrong:* `agent-jail.sh` binds a ship's `loops/claude` over `$HOME/.claude`, so from inside the jail those two paths are **one file** -- proven by `stat`, same device and inode `64771:3313373`. `tools/fixtures/f/fleet_login_scan.sh` read `$HOME/.claude/.credentials.json` and labelled it `pier_credential`; run from inside a jail, which is where an agent runs it, that is the ship's own copy. So `trees_sharing_token=1` came from **comparing a file with itself** -- a reading that can never be false and therefore measures nothing -- and every sentence this seat wrote about *the pier's credential* was a sentence about incense's. *What caught it:* **Keaton, in four words: *aren't you in an incense jail?*** Nothing in the tree asked. The scan had been shipped, run, cited in a ledger row and in a card entry, and its central label was wrong the whole time. *What it taught:* **an instrument must know where it is standing.** This is `%414`-`%416`'s family -- an instrument answering a cheaper question than the one asked -- with a sharper edge, because the cheap answer *looked exactly like* the expensive one: a real file, at the expected path, with plausible contents and a plausible timestamp. A bind-mount does not announce itself, and `$HOME` is the last path anyone thinks to verify. *What it retracts, named rather than quietly amended:* three published claims fall. **(1)** *the pier holds exactly two credential files* -- it was one file found at two paths. **(2)** *the pier's credential is untouched since 01:53:39* -- that is incense's copy; the pier's is **invisible from here** and may well have been rewritten at 02:47 exactly as reported. **(3)** *wherever that login went, it is in neither place a ship reads* -- **unfounded**, since one of the two places was never examined. *What survives, and why:* the peer trees are bound in **read-only** and their `loops/` rooms are real, so `copal no_copy` with a `loops/` untouched since `20260905 18:24`, and petrichor holding no copy, stand as measured. The shared-credential mechanism stands too -- it was read from `agent-jail.sh`'s source rather than inferred from the mislabelled file. *Repaired (`20260906.030420`):* the scan **detects the seam rather than assuming it**, asking the filesystem whether the two paths are one file (`-ef`), and when they are it prints `pier_visible=no`, marks both pier readings `unreadable`, names this ship `own_copy_self` rather than comparing it to itself, and closes at `verdict=pier_unreadable_from_jail` -- the card's own reflex, *a guard that cannot run its instrument refuses, and says which instrument*. The tree half still reports, because that half is honest from either side. **CLOSED.**
