# REDS %231-%232 -- the room nobody heard, and the fast-forward that carried three unrun guards

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.235254`, both **CLOSED** -- the
folded ledger that nineteen guards kept asking the page it left, and the six-commit fast-forward
that arrived carrying three refusals nothing on the sending clone had run. Moved here as rows %233
through %239 carried the living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %231 (`20260825.213128`) -- the ledger folded and nineteen guards kept asking the page it left.** *What went wrong:* `construction/REDS.md` folds -- the living pin holds the rows still flat and every elder row moves onto a shelf under `construction/archive/`. Nineteen call sites across seventeen `tools/fixtures/equinox_*_scan.sh` files asked the PIN for a numbered elder row, `rg -q '^| 42 |' "$REDS"`, and each began refusing on the lap that row folded. Row 42 has lived at [`archive/REDS-voice-season-rows-25-57.md`](REDS-voice-season-rows-25-57.md) ever since. Nothing announced it, because nothing in the tree runs those scans: `tools/gen/season/` holds **298 tracked witnesses and exactly 3 are reached by a roster clock**, so **295 stand unheard -- 24.6% of the tree's whole 1,201-strong unreached population, in one directory**. *What caught it:* the stopwatch this card asked for before proposing a roster row for the family, run `20260825` on this pier. Of 33 leaf witnesses -- the ones that call scans without chaining an elder -- **17 refused**. A sequential sing of 47 of the 144 `equinox_*` witnesses cost **1,117 seconds**, per witness ranging **178 ms to 204,250 ms**, because **111 of the 144 chain an elder witness** and each rung re-runs the whole chain beneath it. *What it taught:* **a fold repoints documents and leaves guards standing, because a guard's citation is code rather than a link** -- and an unheard guard's green is a memory rather than a reading. The stamp-and-name law already says a stale reference is resolved rather than rewritten; what it could not say is that the resolving has to reach the *code* that cites, and `tracked_link_scan.sh` reads links inside files, never a `rg` pattern inside a shell script. *Repaired:* [`../tools/fixtures/reds_row_present.sh`](../../tools/fixtures/reds_row_present.sh) answers the question once, over the pin AND every fold shelf, in the same two row shapes the sibling `reds_ledger_monotone_scan.sh` reads; all nineteen sites call it now. [`../tools/r/reds_row_present_witness.rish`](../../tools/r/reds_row_present_witness.rish) proves the two readings agree by asking for **every one of the 230 rows the monotone scan counts** and for the row past the end, rather than asking a later hand to keep them together; [`../tools/fixtures/reds_row_present_control.sh`](../../tools/fixtures/reds_row_present_control.sh) builds a two-file ledger in a pen and proves 4 welcomes beside 4 refusals, with a misuse exiting 2 rather than 1 so a typo never reads as an absent row. Seated on the roster the same lap. *Surfaced, not crossed:* the family's remaining refusals are their own reds and their own laps -- the deepest of them wants `fascia_metric_v0` at **51** against a season-era floor of **92**.

**REDS %232 (`20260825.213128`) -- a second clone shipped three unrun guards, and the fast-forward carried all of them here.** *What went wrong:* a six-commit fast-forward from `xykj61` arrived carrying three refusals nothing on that clone had run. **Two living pins over bound:** `construction/ITINERARY.md` at **25,554 bytes** and `construction/SHRED_PREP.md` at **24,620**, against the `living_pin_max_bytes` bound of **24,576** each declares. **And the loop's own launch line would not start:** `tools/l/launch-claude-season.rish` prints the unattended recipe as a single-quoted prompt a hand pastes, and the new sync-rota clause added three lone apostrophes -- `the round's own`, `upstream's`, `a module's witness map` -- on both printed lines. A lone `'` inside a single-quoted shell string flips every character after it out of quoting, so both lines died on the next parenthesis with `syntax error near unexpected token ('`. That is REDS %211's fault exactly, in the file that guard was written for, twenty-four hours later. The guard that reads it, `tools/gen/season/equinox_e123_living_pin_guard_witness.rish`, holds a roster row and would have refused on the shipping tree. *What caught it:* this lap's leaf census, which ran the guard as a leaf and watched three season witnesses fall together -- e123 directly, then e125 and e126, whose scans call e123 as their elder under `set -eu` and so died before printing a reason. This pier's own cold roster had opened **63 of 63 green** twenty minutes earlier, against the tree before the fast-forward. *What it taught:* **a fast-forward is a tree change, and the green that measured the tree before it measured a different tree.** REDS %174 fixed the staged-versus-committed half of this by running the roster hot after `git add`; a merge is the same fault arriving through a different door, and the hot pass is what catches it -- so a lap that fetches mid-round runs hot over the merged tree rather than trusting its own open. This is also the third send from the other clone to reach this pier carrying guards it never ran (%226, %227-%229, now these three) -- and the loop half is the sharpest, since a printed string is code no compiler reads, so the loop simply would not have started. *Repaired:* the six apostrophes doubled, and `loop_prompt_parse_scan.sh` reads **6 runnable lines, 0 that do not parse**; both pins under bound in the same round, and the hot roster run over the merged, staged tree. The card's landed work folded to its shelves -- the answered readings and all but one *Prior laps* row -- and `SHRED_PREP.md`'s own fold recital moved onto the shelf it points at, since a pointer left on the pin grows it by exactly what folding was meant to shrink. `equinox_e123_living_pin_guard_scan.sh` reads both at zero.
