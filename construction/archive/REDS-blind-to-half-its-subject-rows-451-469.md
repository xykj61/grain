# REDS %451, %469 -- an instrument blind to half its own subject

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.065500`, in the round that
closed them, to bring the pin back under the byte bound its own header declares after a rebase
brought three peers' rows. The two are one finding a room apart, which is why they share a shelf.
`%451`: the prose register meter dropped every line beginning with an asterisk under the comment
`bullets`, and this tree writes none -- so it graded its front doors on the fraction of each page
that happened to open with a plain word. `%469`: thirteen guards' rows in the `--scoped` map named
less than their guards gate, so a scoped pass could skip the guard whose own control had just
changed. **A guard is green when it agrees with what it read; the question is always how much of
its subject that was.** `%451` stands **BOOKED** -- the classifier is repaired and the door tier
holds at zero, and the remainder is a named five-document ratchet belonging to other lanes. Link
depth moved with the text.*


**REDS %451 (`20260906.000505`) -- the register meter skips bullets, this tree writes none, and so it has been grading its front doors on half a page.** *What went wrong:* `measure()` in `tools/fixtures/p/prose_register_scan.sh` drops every line matching `/^[ \t]*[-*>#]/` under the comment *bullets, headings, quotes*. A CommonMark bullet is `-`, `*`, or `+` followed by whitespace; `**What would enter:**` is a bold span, and this tree writes its bullets with `- `. **Not one asterisk bullet exists anywhere in `docs-geode/`**, so that branch drops every paragraph opening with a bold lead -- which Gauge style produces constantly. Measured `20260906.000505`: of docs-geode's **537** non-blank, non-table, non-fence lines, **298 are read and 239 dropped by the `*` branch, none a bullet**; **108** are body paragraphs of twelve words or more. Across the **twelve** documents the door roster gates, **85** body paragraphs go unread. `tools/fixtures/q/qa_report_card.sh` cites the same `measure()`, so every Register grade in this room rests on the sample that branch left -- **and so does Reach**, which divides links by the same filtered word count. `blog/README.md` reads `reach=50` at *2 links over 35 words* while holding 6 over 380, so **two of the card's four readings ride one classifier**. *What caught it:* grading all 21 docs-geode pages and finding four below B whose registers rested on one to three lines each. `docs-geode/blog/README.md` was read at **1 sentence** of a 289-word page and freed by the 8-sentence floor; with real bullets alone skipped it reads **10 negative of 14, 71%**. *What it taught:* **a guard was green because it could not see enough of the page to disagree.** That is `%442`'s family one turn in -- there the instrument was absent, here present and reading the wrong half. **Three sub-shapes, which is why a repair is a lap rather than a character.** (1) The bullet test wants `[-*+]` plus whitespace, and `---` then needs its own skip. (2) Front matter is dropped today **by accident**, so a repair must drop it **on purpose** -- and a rule keyed on `**Key:**` over-drops Gauge's own bold lead-ins, which my first patch did, so every count above is a floor rather than a ceiling. (3) The shared **`Where this sits:`** navigation block is counted as prose on **111 pages**, its twelve words carrying *nothing*, so one boilerplate line books a negative sentence on 111 front doors -- `caravan`, `constel`, and `crypto` among the gated twelve. *Priced before proposing:* a patched `measure()` over both rosters turns the guard from green to refusing on **2 of 12** door documents (`mycelium` 20 -> 33%, `constel` 20 -> 22%) and **7** teaching documents against a ceiling of zero -- **three of the seven always over, saved only by the floor** (`docs-geode/press` 100% of 1 -> 37% of 8). The repair therefore carries a prose sweep across four lanes, and both door documents it reds sit at exactly 20 today, which is the tell. **The classifier is `tools/fixtures/p/`, core infrastructure and BAKERY's seat**, named here with its blast radius priced rather than repaired across a lane (`%360`'s precedent). *Landed:* `blog/README.md` **71% -> 9%**. *Repaired `20260906.063431`:* all three sub-shapes at once, in `measure()`. The bullet test is now the marker THEN whitespace (`[-*+][ \t]`), which also catches the `+` bullet the elder `[-*>#]` class never held at all; a rule line takes its own branch; and front matter is dropped **on purpose**, by position AND shape together -- the first block after the title whose opening line carries a short bold key holding no `*` and no inner `:`, its wrapped value riding to the blank line, which is what finally drops `**Where this sits:**` on 111 pages. **Both halves are load-bearing, and both were measured rather than argued:** shape alone ate real prose on **77 of 703** living head blocks when it was tried, and without the front-matter branch `docs/README.md` reads **50% on two sentences of pure metadata** and refuses the door gate on a page whose body is a link list, while `constel` reads 22% on metadata rather than on prose; with it, 0% and 18%. **The row's own price was pessimistic:** it forecast 2 door and 7 teaching documents, and the honest reading with all three repaired reds **1 and 5** -- the front-matter branch cancels three of the nine. Door tier holds at **zero**: `mycelium/README.md` was the one page it reds, 20% of 15 sentences -> 33% of 24, and three of its eight negatives carried an incidental form (*do not trust one another*, *without any thread being in charge*, *could not otherwise read by hand*), each rewritten with its claim exact -> 20% of 24. **The five that remain are the page's subject** -- two Meter claims, the *no real key, no funds, no network, no custody* disclaimer, and a two-line benediction [`radiant-wishes-ending`](../../.claude/rules/radiant-wishes-ending.md) asks for -- so a Door page obeying two seated laws carries a floor of four negatives against a twenty-percent ceiling, which is a question of its own and is named on the card rather than answered here. Teaching ratchet **re-seated at 5** with the five named beside the ceiling and both readings and both sentence counts kept; none got worse, each has read over Field the whole time on the half the meter could not see. **Six new control legs, each refusing under the elder reading and passing under this one** -- a case green both ways proves nothing. The report card's own door ratchet holds at `card_below_b=0`. **BOOKED** -- the five teaching sweeps are their lanes'.

**REDS %469 (`20260906.063431`) -- thirteen guards' own controls could not reach them, so a `--scoped` pass could skip the guard whose refusal-proof had just changed.** *What went wrong:* `standing_equipment_run.sh --scoped` keeps a mapped guard only when a changed path matches one of its watch words in `tools/fixtures/s/standing_equipment_scope_map.sh`; a guard the map does **not** know always runs, and a guard it knows *partially* is skipped on the paths its row forgot. Measured `20260906` with the matcher's own glob-and-room semantics rather than by eye -- a first pass reading only exact paths answered 16, and three of those were already covered by a room word: of **56** mapped guards, **26** carry a `<name>_control.sh` on disk, and **13** of those had no watch word reaching it -- `caravan_ladder_roster`, `custody_gate_instruction`, `index_fold`, `index_row_bound`, `log_has_a_row`, `loop_prompt_parse`, `mycelium_map_roster`, `pond_enclosure_state`, `prose_register`, `reds_ledger_headline`, `reds_row_present`, `rule_twin`, `skate_macos_choice`. A control is the one file whose breakage most needs its guard re-run, since it is the whole proof the guard can refuse at all. `prose_register` was blinder still: it hard-gates a **twelve-document DOOR roster** and watched **none of it**, so repairing a door README could not reprove the guard that refuses on that README. *What caught it:* editing `mycelium/README.md` for `%451` and asking which guards a scoped close would actually reprove. *What it taught:* **the file had already named its own red.** Its header reads *"The one unsound direction is a static row that names too little; when a guard grows a new subject, its row grows in the same commit"* -- written as a caution, standing in thirteen rows, and never measured. A hazard a document names is not a hazard a tree has counted. The map's other rule settled the repair rather than judgment: a row follows the guard's **gated** readings, and every one of the thirteen witnesses **asserts** on its control, between 2 and 42 times. *Repaired:* each missing control and the whole door roster added to their rows, and all thirteen proven to reach by running the matcher's own semantics over them. The change is **monotone toward safety** -- a watch word can only make a guard run more often, never less -- which is why it lands as one commit rather than thirteen. **CLOSED.**
