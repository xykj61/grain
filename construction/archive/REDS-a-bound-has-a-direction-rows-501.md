# REDS %501 -- a bound has a direction, and one of them decides nothing

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.144500`, in the round that
booked and closed it. The pin stood at 40,960 bytes of its 40,960 bound before this row and 44,714
after, so the row that put the page over is the row that folds -- no peer's account was touched to
seat it.*

*Its lesson stands beside [`%492`](REDS-two-algorithms-agreeing-rows-492.md), one room over. There,
two algorithms agreed and were each right about their own input, so the agreement proved the
readers rather than the subject. Here there is one reader and one inequality, answered honestly, in
the direction where the design has no freedom at all. **Slack in a variable nobody can spend reads
exactly like slack that matters** -- and reads better, because the number is enormous. The check
that separates them is not a second algorithm; it is asking which variable the design is actually
choosing, and solving for that one.*

*The instrument this row seats is bound to four graphs that ATTAIN the Moore bound rather than to
graphs that merely satisfy it. A bracketing case is satisfied by a formula that is wrong by a
little; an attainer sits exactly on the ceiling, so the same formula fails it. That distinction is
the whole reason the `>=` to `>` plant bites all four at once where three other plants bite one or
three.*


**REDS %501 (`20260906.142051`) -- the Moore bound was asked how many nodes could fit, when the sky's point count was the one thing already fixed, so a true answer decided nothing and a published paper concluded twice that the comparison it was measuring did not matter.** *What went wrong:* `external-research/20260906-010402_the-ring-and-the-ladder.md` reached for the Moore bound and evaluated it at the **achieved** degree and the **achieved** diameter, asking how many nodes such a graph could hold: **9,155,273,437** at degree 6 and diameter 14, **10,579,427** at degree 26 and diameter 5, against the **720** either shape holds. From that distance the body concluded *"neither is anywhere near its own limit, so a diameter argument between them compares two comfortable designs rather than a frontier"* -- and the first erratum re-derived the same reading at a corrected degree and **re-affirmed it**. Every figure was right. *What caught it:* asking which variable the design is actually free to choose. A sky fixes its point count by definition -- 720 in the compass sky, 405 in the council -- and a shape fixes its degree, so the **diameter** is the free variable and the bound should be solved for it. *The reading that replaces it:* the smallest diameter any graph of that many points and that maximum degree could hold. Compass sky: the ladder reaches **5** against a floor of **3** (**1.67x**), the torus **14** against a floor of **4** (**3.50x**); the council reads **2.50x** and **3.00x**. **The diameter argument decides after all, and it decides against the ring** -- strengthening the paper's own position rather than reversing it, since it already priced the torus's case as degree and resilience. *The sharper reading needs no comparison at all:* the ladder's maximum degree of 26 is thirteen times its own mean of 2.15, so it is measured against a floor it buys at twelve hub nodes, while the torus is 6-regular and stands alone -- **at the degree it actually uses, the seated torus sits three and a half times above the smallest diameter any shape of that degree could hold.** *And the family cannot close it:* the best three-ring torus over 720 points is 8 x 9 x 10 at diameter 13 against the seated 12 x 5 x 12 at 14, one hop of the ten; widening to four rings at degree 8 reaches 10 and five at degree 10 reaches 8, holding the ratio between 2.50x and 3.25x, and six rings do not exist here at all since six rings of three seats already need 729 points. *What it taught:* **a bound has a direction, and the direction that decides is the one whose free variable is the thing being chosen.** This is `%492`'s family one room over -- there, two algorithms agreed and were each right about their own input; here, one inequality answered honestly in the direction where the design has no freedom, and slack in a variable nobody can spend reads exactly like slack that matters. *Repaired (`20260906.142051`):* `tools/fixtures/t/topology_graph_census.sh` reports `moore_floor` and `over_floor` per connected leg, and **refuses** both sponsor legs by name -- their graphs are split 133 and 121 ways, so a ratio against the widest walk of one component while half the ordered pairs never connect would be a number about nothing. The arithmetic is bound in the agreement leg to **four graphs that attain the bound** -- the complete graph on ten nodes, Petersen, Hoffman-Singleton, and a fifteen-cycle for the degree-two branch -- because an attainer sits **on** the ceiling and catches the off-by-one a bracketing case reads past, proven by the plant that turns `>=` into `>` and bites all four at once. `tools/fixtures/t/topology_floor_control.sh` proves **23 behaviors**, five plants each from the failing side, the pen proven innocent, and the legs proven to earn their keep by a copy broken with the checking legs removed -- **which reads green while the torus's floor sits at 5 and its ratio at 2.80, a fifth of the finding, in the flattering direction, silently.** `tools/t/topology_floor_witness.rish` rostered `tier cadence` at **151s**, cadence rather than lap because the control runs the census seven times over 682,425 ordered pairs and its answer moves only when the census or the module moves. *What it does not reach:* how much of the gap a real construction recovers, which is the degree-diameter problem and is open -- the floor bounds the room from above rather than promising a shape at it. **CLOSED.**
