# REDS -- closed rows 96-100, the one-clock head, the ledger scan, and the ladder-fold pair

**Language:** EN
**Status:** Archive -- closed range, every byte kept
**Folded:** `20260820.232126` from [`../REDS.md`](../REDS.md), the living pin, to bring it back under `living_pin_max_bytes` (24576) after row 101 was booked.
**Voice:** Kyri

*A fold moves nothing out of the record -- it moves it out of the pin. Every row below stood in `construction/REDS.md` and reads here exactly as it read there. Rows 96-100 all closed on metal: the one-clock law's fifth duty, the reds-ledger scan that could not see its own rows, the harness lift that rewrote fifteen sentences a person reads, and the wall that then refused two sentences written by hand.*

---

**REDS %96 CLOSED (`20260820.030003`) -- two session logs were stamped ahead of the clock, and the one-clock witness ran GREEN over both.** *What went wrong:* the living head stamp read `20260820.031500` while the host clock read `20260820.025215` -- a log dated **22 minutes into the future**, and the lap before it dated ten minutes into its own. Measured against the commits that carried them, the drift grew lap over lap: `20260820.021400` landed two minutes *behind* its commit, honestly; `20260820.024612` stood ten minutes ahead of a commit made at `023622`; `20260820.031500` stood twenty-eight minutes ahead of a commit made at `024638`. A drift that grows by the lap is the signature of a stamp **estimated from the lap before it** rather than read from the clock -- the exact class REDS %63 named and `[[live-clock-stamps]]` exists to prevent. *What caught it:* reading the clock at the top of this lap to stamp it, and finding the answer *earlier* than the newest log in the index. Measurement, one turn before the work. *What it taught:* **the guard had four duties and none of them could see this.** Shape reads filenames; monotonicity can never catch a forward jump, since every false future is monotonic with itself; the zone duty weighs the host rather than any stamp; and duty 4, the one built for exactly this, takes its base from `origin/main` -- so it weighs a dated artifact for precisely as long as that artifact stays unmerged, and every lap that lands and merges retires its own stamps from the diff forever. The one number the whole law rests on, the **living head**, was the number nobody checked. Closed with **duty 5**: [`tools/fixtures/one_clock_head_scan.sh`](../../tools/fixtures/one_clock_head_scan.sh) asks whether the newest living stamp stands at or behind the live clock, within a tight 120s grace, and it reads the head from the monotonicity scan rather than growing a second walk that could drift from it. Three paths proven on metal: a stamp read from the live clock passes at a 0s delta; a stamp four hours ahead is refused by name; and a false future **planted as a real living log** makes the duty name that exact stamp and refuse, rather than only a fixture doing so. The two stamps already written are pinned in [`tools/fixtures/one_clock_head_erratum.txt`](../../tools/fixtures/one_clock_head_erratum.txt) with their cause -- accrete-never-break, so the logs keep their names, the erratum records the fault, and the guard catches the next one on the lap it enters. `one_clock_witness` GREEN on all five duties. Released.

**REDS %97 CLOSED (`20260820.030440`) -- the reds ledger scan read a 96-row ledger as empty, and nothing ever ran it.** *What went wrong:* `tools/fixtures/reds_ledger_scan.sh` counts a row as a table line beginning with a digit cell, and the living ledger stopped writing that shape rounds ago -- its rows are prose now, a bold `**REDS %96 ...**` opening followed by the three fields in italics. So the moment %96's fold moved the last elder table row to the archive, the scan answered `rows=0` and `verdict=no_rows` over a ledger holding ninety-six of them. Its default path also still named `work-in-progress/REDS.md`, resolving only through the compatibility symlink the `construction/` rename left behind. *What caught it:* running the scan while the ledger was already open for %96's fold, and reading `rows=0` against a file whose rows had just been counted at ninety-six by hand. The previous run had reported `past_bound` instead, so the bound failure had been masking the counting failure the whole time. *What it taught:* **a guard nobody runs drifts from the thing it guards, and the drift shows up as a confident number.** This is the `%81`/`%87`/`%91` family -- the guard existed and nothing pulled it -- fused with %93's lesson, that a count which cannot see what it measures is a guess wearing a measurement's clothes. Closed three ways: the scan learned the prose shape and holds each kind to its own promise, so a **full row** must name what caught it and what it taught while a **closure note**, which speaks about a row written elsewhere, must instead name its proof -- GREEN, on metal, or Released -- because a fix closes on a witness rather than on a claim; its default path now names `construction/REDS.md` directly; and it stopped being a scan nobody runs, since [`tools/gen/season/equinox_e123_living_pin_guard_witness.rish`](../../tools/gen/season/equinox_e123_living_pin_guard_witness.rish) now consumes it beside the bound it already weighed, so the ledger and the pin that holds it are guarded by one hand. Both RED paths proven on metal by a `prove-red` mode that generates its control rather than tracking one, so the control can never drift from the shapes the scan enforces: a fieldless row and a proofless closure are each named and refused. Living ledger `verdict=ok` at 5 rows, e123 GREEN. Released.

**REDS %99 CLOSED (`20260820.193120`) -- a mechanical lift rewrote fifteen sentences a person reads, and the module compiled and ran GREEN over every one of them.** *What went wrong:* lifting a check into `caravan/ladder_checks.rye` means prefixing each symbol of the rung it was handed with `rung.`, so one body can run against whichever rung hands itself in. The rewrite that did it walked the whole function by identifier, and an identifier regex cannot tell code from prose. It reached inside nine printed lines and six comments, turning `standing` into `rung.standing`, `read` into `rung.read`, and `carry` into `rung.carry` -- so the operator line that had read *"a record survives a provisioning, and leaves the reader's own word standing where they wrote it"* now read *"...the reader's own word rung.standing where they wrote it."* Zig saw nothing to object to, since a string literal and a comment are opaque to it; the rung built, the self-test ran, and the module printed GREEN with its own sentences quietly turned to nonsense. *What caught it:* `tools/ca/caravan_abate_witness.rish` refusing on metal, because it asserts on the **exact sentence** rather than on the rung's GREEN. The one witness in the arc that quoted a full printed line was the only thing in the tree that could see this, and it named the fault in one read. *What it taught:* **a compiler weighs the code and never the prose beside it, so a mechanical rewrite over a whole function is unguarded exactly where a person reads.** This is REDS %83's family -- a transformation corrupting text nobody re-read -- met one lap after it happened rather than months later, and only because a witness happened to quote what it was checking. A GREEN self-test is not evidence its own output is legible. Closed two ways: every corrupted literal and comment repaired against the original wording, and the rule made a wall -- [`tools/fixtures/caravan_ladder_harness_prose_scan.sh`](../../tools/fixtures/caravan_ladder_harness_prose_scan.sh) refuses any `rung.` standing inside a string literal or a comment in the harness, and [`tools/ca/caravan_ladder_copy_witness.rish`](../../tools/ca/caravan_ladder_copy_witness.rish) pulls it every lap beside the folds and the reach rule, so it is never a scan nobody runs (REDS %97's lesson). Three paths proven on metal: the living harness passes, the **elder** harness with eighty-odd bodies already lifted passes -- which is what proves the guard welcomes valid input rather than teaching the bench to route around it -- and a control planted from the living harness in exactly this lap's shape is refused by line number, in both the literal and the comment form. Meter GREEN at seven RED paths, `caravan_abate_witness` GREEN again, `caravan_conclude_witness` GREEN. Released.

**REDS %100 CLOSED (`20260820.212419`) -- the wall built one lap ago to protect prose refused two sentences that were written by hand and perfectly correct.** *What went wrong:* REDS %99 taught that a mechanical lift can rewrite the sentences a person reads, and the wall it left refuses the token `rung.` inside any comment or string literal in `caravan/ladder_checks.rye`. Its own header states the rule precisely -- a `rung.` that *reaches a symbol* belongs in code and never in prose -- yet the pattern it wrote was broader than the rule it stated: a bare `*rung.*`, which also matches the ordinary English noun closing a sentence. So fold D's hand-written harness doc, ending a line on *"the same way from every rung."*, was named a corruption and the meter went RED over prose nobody had mangled. *What caught it:* `tools/ca/caravan_ladder_copy_witness.rish` refusing during fold D's own send, naming both lines and their line numbers, and a read of the scan's header showing the pattern and the stated rule had drifted apart. *What it taught:* **a guard whose pattern is wider than the rule it states will refuse honest work, and a gate that reds on valid input teaches the bench to route around it** -- the Radiant playbook's own standing warning, met here on the second lap of the guard's life rather than after it had trained a habit of disabling it. This is REDS %97's family inverted: there a guard could not see what it measured, here a guard saw more than it meant to. Closed by narrowing the pattern to the reach it names -- `rung.` followed by an identifier character -- so a lift, which can only ever write `rung.` with a name behind it, still cannot slip through, while English keeps its own word. Proven rather than reasoned: both planted RED controls (`rung.standing` in a literal, `rung.reaching` in a comment) still refuse by line, the elder harness still reads clean, and a **new PASS control** hands the scan a comment and a printed line that each end on the plain noun and watches them welcomed. Meter GREEN at seven RED paths and three welcomes; living harness clean over 504 comments and 1,035 literals; 96 Caravan witnesses GREEN cold. Released.
