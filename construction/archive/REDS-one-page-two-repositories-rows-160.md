# REDS -- one page, two repositories (row %160)

**Language:** EN
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Folded:** `20260823.041442` -- carried here from [`../REDS.md`](../REDS.md) when the living pin
passed its 24,576-byte bound as `%161` closed. Its open remainder -- 848 links in other living
shipped documents, under a ceiling that only falls -- is carried forward by the living ledger and
by `tools/s/seed_link_witness.rish`, which reports the count on every run.

---

**REDS %160 (`20260823.034321`) -- one page serves two repositories, and 867 of its links land in only one of them.** *What went wrong:* this tree publishes two ways from one set of files -- the maintainer's field carries every room, and the public seed is an **allowlist** projection named path by path in `template-manifest.bron`. The same README serves both. A census this lap found **867 links in living seed-shipped documents pointing at rooms the seed does not carry** (198 more in dated testimony, which keeps every reference it ever wrote and is right to). The **front door alone carried nineteen**: eleven into `gratitude/`, three into `context/` files outside the allowlist, and the manifest itself. `tools/t/tracked_link_witness.rish` was green throughout, correctly -- it asks *is this in the repository*, which is the right question for the field and the wrong one for the seed. *What caught it:* Keaton, reading the front door and noticing that the gratitude links could not survive the scrub. *What it taught:* **a guard proves a property of the repository it runs in, and this tree ships two.** Twenty-four rostered guards all read the field; not one read the artifact a stranger actually meets. The seed's own tooling exists -- `tools/s/sow_witness.rish` proves `IDENT_CLEAN` -- yet it is **off the roster** and runs only by hand at publish time, so the face of the project was unmeasured between publishes.

**REDS %160 CLOSED in part (`20260823.034321`) -- the front door reads whole in both repositories; the rest falls on touch.** *The repair, on metal:* the root README is rewritten so that **every one of its 68 relative links lands on a path the seed ships**, and a withheld room is **named in prose rather than linked** -- the session logs, the operator card, the reading library, and the personal study room are each described in plain sentences with nothing to click, which is legible in the field and honest in the seed rather than correct in one and broken in the other. The loom is [`../tools/s/seed_link_witness.rish`](../../tools/s/seed_link_witness.rish) over [`../tools/fixtures/seed_link_scan.sh`](../../tools/fixtures/seed_link_scan.sh), which reads the manifest's allow and `sub_exclude` rows, walks every seed-shipped living document, and gates at zero every front-door link landing outside the projection. The front door is **named rather than discovered** -- four documents -- so a new root file cannot join the enforced set by accident and red on work it never agreed to cover. Dated testimony, unshipped documents, and external or anchor links pass free by named rule. [`../tools/fixtures/seed_link_control.sh`](../../tools/fixtures/seed_link_control.sh) builds real git repositories in a temporary pen, each with its own small manifest, and proves **sixteen** behaviors -- three refusals bitten, thirteen honest readings left free -- with the ceiling proven from **both** sides by planting 849 dead links against 848, so no override exists. GREEN on metal: `front_door_links_outside_seed=0` over 1,888 shipped documents and 6,889 relative links. Seated as the roster's **twenty-fifth** guard. **What stays open, named rather than carried:** **848** links in other living shipped documents, under a ceiling that only falls -- `MAP.md` 16, `bibliography.md` 19, `SOURCE.md` 4, `ORGANIZING.md` 6, `docs-geode/` 16, `manual/` 15, and `context/LEXICON.md` 131. Each wants a rewrite rather than a repoint, so they fall on touch rather than in one sweep that would bury the enforced zero in noise.
