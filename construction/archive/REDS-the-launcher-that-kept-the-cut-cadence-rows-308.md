# REDS -- the launcher that kept the cut cadence (row %308)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260828.050134` from [`../REDS.md`](../REDS.md), **CLOSED**.

One row, and it is the whole shape of a repair aimed at a class and applied to a member. `%307`
found the loop's prompt template telling an unattended lap to publish the public seed every fifth
round, and a hand repaired that file. The same sentence stood on in the launcher a hand actually
pastes to start a loop, which is the file that reaches a machine. The repair reached the template
and the fault reached the terminal. What closed it was writing `%307`'s missing guard rather than
trusting `%307`'s repair: a meter over the whole loop-prompt surface, discovered from
`git ls-files` rather than named, which found the file nobody had looked at on its first run.

---

**REDS %308 (`20260828.033212`) -- the red was repaired in the template and left standing in the launcher a hand actually pastes.** *What went wrong:* `%307` found `recursion-prompts/seed/autonomous-loop.seed.md` telling an unattended lap to run `bash ~/grain/publish-seed.sh` every fifth round, and repaired that file. `tools/l/launch-claude-chapter.rish` printed the same sentence at its lines 66 and 86, inside both seat prompts a hand pastes to START a Claude loop -- *"every fifth round, the lap the rota closes its cycle, also project and force-push the public seed with bash ~/grain/publish-seed.sh"*. A template is what a future prompt is written FROM; a launcher is what the running machine is handed, and only the second reaches a machine. The three launchers written since the role swap -- dream, hush, silence -- each say *never run publish-seed.sh*; the elder one, which the template's own header names as its elder, kept the cut cadence for two days after `20260826` cut it. *What caught it:* writing `%307`'s missing guard rather than trusting `%307`'s repair. The scan's first run over its own discovered roster named two lines no hand had looked at. *What it taught:* **a repair aimed at a class is applied by the meter that defines the class, never by the hand that found the first member.** The hand fixed the file it was reading; the meter reads twenty-seven and found the file it was not. *Repaired (`20260828.033212`):* both launcher lines now read *the public seed stays a manual gate -- never run publish-seed.sh from a loop, since custody gate %1 holds every refresh at Keatons own hand*. `tools/fixtures/custody_gate_instruction_scan.sh` reads the whole loop-prompt surface -- `recursion-prompts/` and `tools/l/launch-*`, discovered from `git ls-files` rather than named -- and gates at zero any of three gated command tokens (`publish-seed.sh`, `git push --force`, `git filter-repo`) standing in a clause carrying no refusal word beside it. Twenty-seven files, seven mentions, seven refusal-framed, `bare_instructions=0`. `tools/fixtures/custody_gate_instruction_control.sh` proves eleven cases on planted files: the real pre-repair sentence bitten and its token named, the repair freed, a far-off `never` refused the power to launder a bare instruction two clauses away, both other tokens bitten, prose about force-pushing left free, and an absent file told apart from a passing one. Seated at tier `lap`, since the scan costs 0.15s on this bench. **What it does not reach, named rather than left to be found:** four of the seven gates name an ACT rather than a command -- provisioning, moving funds, a real Kumara seed, a collaborator's domain -- and no grep finds those; and clause proximity is a reading rather than a comprehension, so word presence is the check and a reader is the standard. **CLOSED**, and `%307`'s open clause closes with it: a guard reads instruction files against the gate list now.
