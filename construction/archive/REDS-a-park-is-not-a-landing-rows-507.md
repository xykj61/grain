# REDS %507 -- a park is not a landing

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.153148`, the row CLOSED in the
round that found it.*

**What the row taught, in one line:** the round open fills two dead-letter boxes, and the guard
built for the first one accepted the second as proof that the first was empty.

**Kin.** [`%464`](REDS-the-box-nobody-opened-rows-464.md) built the reader for the
stash. [`%499`](../REDS.md) measured the park: 11 `pier/diverged-*` branches on `xy`, 33 distinct
subjects since `20260828`, **ten that no hand ever brought home**. Each row is correct about its own
drawer. Neither reached the seam between them, which is where this one sat.

---

**REDS %507 (`20260906.153148`) -- the guard that reads the round open's dead-letter box counted a `pier/` park branch as a landing, so a complete unlanded feature read `verdict=ok`.** *What went wrong:* `tools/fixtures/f/stash_record_scan.sh` asks, for every session log inside a round-open stash, whether any ref carries it -- and it built that ref list with `git for-each-ref refs/heads refs/remotes`, whole. The round open writes park branches into exactly that space: step 4 of `tools/f/fleet_round_open.sh` parks a diverged local line on `refs/heads/pier/diverged-<stamp>`, and its own header calls `refs/heads/pier/` *the rota's own deferral shelf*. So a record carried only by a park answered `landed:refs/heads/pier/diverged-...`, and the guard whose whole purpose is finding a lap's reasoning where nobody reads it reported the box clean. *What caught it:* opening a lap by reading the three stashes on this tree by hand, finding `stash@{0}` held six files that stand nowhere in the tree -- `Weave.annotate`, its 377-line Rye witness, a head scan, a control, a Rishi runner, a roster line, 1,107 insertions of work already proven GREEN -- and then asking whether the standing guard had seen it. It had not. **The concrete fact at the door outranked the plausible story**, which is the Earth row's own discipline and the lap that was reading it. *The measurement, on this tree, both sides, with the record moved off disk so the refs alone answer:* the elder scan reads `landed:refs/heads/pier/diverged-20260906-131810`, `unlanded=0`, `verdict=ok`; the repaired scan reads `unlanded:parked:refs/heads/pier/diverged-20260906-131810`, `unlanded=1`, `parked=1`, `verdict=records_unlanded`. The record stands on **no reader ref at all** -- not `main`, not `xy/main` -- and **23** park refs stand on this tree, local and remote. *What it taught:* **a guard is only as honest as its definition of the thing it is looking for**, and `landed` was defined as *any ref* when it meant *a ref a reader will reach*. Two dead-letter boxes in one workflow, and the cheaper reading of one certified the other -- `%467`'s family, where a cheap question returns a word the expensive one would not. *Repaired:* the ref list splits in two. `reader_refs` excludes `^refs/heads/pier/` and `^refs/remotes/[^/]+/pier/`; `park_refs` is exactly that complement, and `parked_on` names WHY a record is unlanded rather than deciding whether it is. **The exclusion follows the namespace rather than the `diverged-` prefix**, because the round open writes `pier/rebase-<stamp>` there too and a park named something else tomorrow is still a park. Remote parks are excluded on the same ground: pushing a park to the anointed remote moves no record into the channel, while a remote `main` still counts, which is what legs 20-21 already prove. *One gate, one diagnosis:* `unlanded` stays the single gate and keeps its meaning -- a record no reader ref carries -- and `parked` is reported as the subset a park does carry, since two readings that always fire together are one reading wearing two names (`.claude/rules/derived-spine.md`). That also leaves `tools/f/fleet_round_open.sh` untouched: it greps `^unlanded=` and begins printing correctly with no edit across a seat boundary (%291). *Proven:* the control grew **22 legs to 34**, and **eight of the new ones were run against the elder scan and bitten** -- including both negatives -- so the widening is proven able to refuse rather than merely observed passing. *A rider found the same way:* leg 21's `nk "and is not called unlanded" "050505<TAB>unlanded"` could never match anything, since a line reads `<stash>	<path>	<state>` and the stamp sits inside the path -- a negative assertion passing for the wrong reason since the day it was written. Anchored on the basename now, and it bites. *Not reached:* whether a park branch should exist at all after `%499`'s discriminator lands. This row makes the park visible to the one guard that was looking away from it, and stops there. **CLOSED.**
