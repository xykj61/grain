# REDS -- the ladder table that named 73 of 110 rungs (row %184)

*Folded from [`../REDS.md`](../REDS.md) on `20260824.082144`, **CLOSED**, as row %188 carried the living pin past its 24,576-byte bound.*

**REDS %184 (`20260824.062207`) -- the ladder table named 73 rungs while 110 modules stood beside it.** *What went wrong:* `caravan/README.md` carried a table headed **The Ladder**, and a reader used it as the roster of what Caravan is. It held **73 rows**. The directory held **110** `.rye` modules -- **106 rungs and 4 helpers**, with 109 witnesses in `tools/ca/` standing over them. So **33 proven rungs** had landed over the days nobody added the row, from `relent.rye` through `farewell.rye` and the whole seL4 arc (`refusals`, `objects`, `derivation`, `ipc_buffer`, `untyped`, `edge`, `mapping`, `reply`), and the page a newcomer enters the tree's largest module by stood a third short while reading exactly like a full account. Thirteen of the 33 even had their own `##` section further down the same file; the table alone had stopped growing. *What caught it:* counting the table's rows while splitting the 497,531-byte page into a Door front door and its reference pages -- `grep -c '^| '` over the table read 74 where 111 was expected, and `comm` against `ls caravan/*.rye` named all 37 absentees in one line. Nothing on the roster of 39 standing guards could have said so. *What it taught:* **a roster that lists what a program runs gets checked, because a missing entry breaks a run; a roster that lists what a reader should find gets checked by nobody, because a missing entry breaks only understanding -- and understanding fails quietly.** `tools/ca/caravan_suite_witness.rish` already holds a **bijection** between the witnesses on disk and its own roster, so not one of the 33 rungs could go unheard by the machine. The same module carried two rosters, one bound and one free, and the free one was the one a person reads. The transferable form: **wherever a document lists what is on disk, that list is a roster and wants the same bijection the program's roster already has.** *Repaired:* the page split three ways in the same round -- `caravan/README.md` is a **7,221-byte Door page** answering *what is this for*, `caravan/LADDER.md` holds the table and the rung-by-rung record, and `caravan/HARNESS.md` holds the shared-check carry; both new pages sit in the same directory, so every relative link inside them stays true. The table is completed to **110 of 110**, the 33 rows written from each module's own `//!` doc comment, and the four helpers rostered in a second table naming them as proven through the rungs that compose them. *Prevention* seated as the **40th** standing guard: [`../tools/ca/caravan_ladder_roster_witness.rish`](../../tools/ca/caravan_ladder_roster_witness.rish) over [`../tools/fixtures/caravan_ladder_roster_scan.sh`](../../tools/fixtures/caravan_ladder_roster_scan.sh), gating four readings at zero -- `unrostered`, `phantom`, `duplicate_rows`, `mismatched_rows` -- with the corpus size published beside the verdict so a reading over an empty directory can never report clean. The scan matches the **File column** rather than any `.rye` link, which is the load-bearing choice: the rung sections cite their own modules in prose constantly, and a reading that counted those would have called the short table complete. Proven RED on metal against the departing table (`unrostered=37`) and by removing one row from the repaired page (`unrostered=1`, exit 1), GREEN after. [`../tools/fixtures/caravan_ladder_roster_control.sh`](../../tools/fixtures/caravan_ladder_roster_control.sh) builds real directories in a throwaway pen and proves **twelve behaviors** -- six refusals bitten and six honest readings left free, including the prose-citation case that would have hidden the whole fault. **Checkpoint `e5d4097ef1`.** CLOSED.
