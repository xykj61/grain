# REDS %193 and %194 -- the projection read mid-write, and the rule written twice

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.121445`, both **CLOSED** -- a
public projection rebuilt in place while a gate was reading it, and forty standing rules each
written twice with thirty-six of the pairs saying different things. Moved here as rows %195 and
%196 carried the living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %193 (`20260824.104946`) -- the seed projection rebuilds its directory in place, so anything reading it mid-run reads a half-answer, and one of those readers is the gate that clears it for publication.** *What went wrong:* the seed-cadence lap ran `tools/fixtures/sow_project.sh` while the standing roster was still climbing, and the roster's own `sow` guard **re-projects**. The projection reported `SOW_OK copied=1644 scrubbed=354 withheld=4` where the same tree alone gives **`copied=6961 scrubbed=1066 withheld=122`** -- a **76% shortfall** against the 6,948 the card recorded a lap earlier. *What caught it:* the expected number. `constel/README.md`'s round had just spent four laps on rosters that stopped matching after a fold, so 1,644 read exactly like an allowlist gone stale, and the first move was to count `git ls-files` per `allow` path -- which summed past 6,000 and said the manifest was fine. Only then did the concurrent `sow` guard explain it. **A number nobody can tell apart from a real fault is a false red waiting to be booked**, and this one took a card entry from the previous lap to refute. *What it taught, and the misread is the cheap half:* `seed/` is the **public face**. `sow_leak_scan.sh`, `tools/s/sow_witness.rish`, and `publish-seed.sh` all gate on what stands in that directory, and a gate reading a tree still being written has examined a set nobody chose. **`IDENT_CLEAN` over a partial projection is a true statement about the wrong corpus** -- the one direction the seed's whole discipline exists to make impossible. This is REDS %172's shape returned (*running the roster manufactured the artifact the next run reported*), moved from a scan's output to the public projection itself, which is where it costs the most. *Repaired:* `sow_project.sh` takes a **lock before it clears anything**. It **refuses rather than queues** -- a second projection exits **3** and names the holding pid, because a queued run would still hand its reader a number from a tree that changed underneath them. The lock is a **directory**, since `mkdir` is atomic on every POSIX filesystem where a two-step test-then-create is not; it is released on `EXIT`, `INT`, and `TERM`; and a lock left by a killed run names its dead pid so the next run **clears it rather than wedging forever**. The path is overridable by `SEED_LOCK_DIR` so a control never touches the real one, and `git check-ignore -v` was asked rather than guessed: `.gitignore:8:/*` already covers it. *Prevention* seated as the **48th** standing guard: [`../tools/s/sow_lock_witness.rish`](../../tools/s/sow_lock_witness.rish) over [`../tools/fixtures/sow_lock_control.sh`](../../tools/fixtures/sow_lock_control.sh) -- **six behaviors on real directories**, four refusals bitten and two honest readings free, the refusal shown from every side that matters: it happens, it names the holder, it exits 3 rather than 1, and it leaves the holder's lock standing rather than stealing it. **The standing pass runs four of the six and says so.** The full control takes **171 seconds**, because release-on-exit is only true if the script reaches its exit and reaching it means one real projection; the four refusals take about a second. Release-on-exit is gated all the same, by a different guard: `sow_witness.rish` runs the projection every roster pass, so a leaked lock makes that projection refuse with exit 3 and turns `sow` red. Two guards, one property, and the expensive half named in the open rather than dropped in silence. *And the four seed gates were re-read clean afterward, alone:* `SOW_OK copied=6961 scrubbed=1066 withheld=122`, `IDENT_CLEAN`, `NO_PERSONAL`, and `seed_link` GREEN. **The force-push to `grain-os/grain` stays custody gate %1 and waits for Keaton's word.** CLOSED.

**REDS %194 (`20260824.112806`) -- every standing rule is written twice, both copies are read as law, and thirty-six of the forty pairs say different things.** *What went wrong:* this tree writes each rule as `.claude/rules/<name>.md` for Zed and Claude Code and `.cursor/rules/<name>.mdc` for Cursor. Whichever editor opens the session reads its own copy **as law**. Measured this lap: **40 pairs stand, 4 agree, 36 drift**, and one Cursor rule -- `fuse-resin-cleanup.mdc` -- has no Claude canonical at all. So for thirty-six rules the tree holds two laws under one name, and which one governs depends on who opened the editor. *What caught it:* a two-line edit. Lowering the exec-bit ratchet ceiling from 58 to 57 meant updating `.claude/rules/exec-bit.md`, and `docs-implementation-sync` says the doc moves in the same commit -- so I ran `sh tools/fixtures/document_mirror_scan.sh write` as the standing instruction directs, and the Cursor twin **still said 58**. `context/document-mirrors.brix` had never named it. Counting the pairs then gave 40 against **one** registered. *What it taught, and this is the finding rather than the count:* **the mirror mechanism cannot hold these pairs, and registering them would have been the wrong repair.** That descriptor proves homes **byte-identical**; a `.mdc` twin is a **transform** -- Cursor frontmatter on top, local links rewritten `.md` -> `.mdc`, and a closing cross-pointer each file aims at the other **on purpose**. Register all forty and every one reds forever, which is a guard nobody keeps. This is REDS %187's shape (*a roster reaching 6 of 34*) with a sharper edge: the roster was not merely short, it was **the wrong kind of roster**, and a short roster invites the reflex of adding rows to it. *And the drift runs in **both directions**, which is why this ratchets rather than gates:* `.cursor/rules/reds-first.mdc` names `work-in-progress/REDS.md`, a path that moved to `construction/`, so that twin is **behind**; `.cursor/rules/send-word.mdc` carries an **`ls-remote` pre-push guard** and a two-remote push discipline that `.claude/rules/send-word.md` has **never held**, so that twin is **ahead**. A bulk merge in either direction silently deletes a live safety rule, and `send-word` is the proof that the deleted one could be a guard against pushing to a remote that refuses. *Repaired, in the half that is mine to repair:* `.cursor/rules/exec-bit.mdc` now agrees with its canonical under the transform, so the pair I touched leaves this lap in agreement rather than one worse. *Prevention* seated as the **49th** standing guard: [`../tools/r/rule_twin_witness.rish`](../../tools/r/rule_twin_witness.rish) over [`../tools/fixtures/rule_twin_scan.sh`](../../tools/fixtures/rule_twin_scan.sh), which **declares the transform in four steps rather than assuming it** and holds drift under a ceiling that only ever falls -- each pair reconciled lowers it by one. The ceiling itself is gated hard, so the tree can only move toward agreement, and an empty corpus refuses, since a reading over no pairs finds no drift and would report clean while measuring nothing. Proven both ways by [`../tools/fixtures/rule_twin_control.sh`](../../tools/fixtures/rule_twin_control.sh): **twelve behaviors on real directories** in a throwaway pen, five refusals bitten and seven honest readings free, the ceiling shown from both sides at exactly 2 and 3, and the transform proven **tight** as well as generous -- one changed word bites, a paragraph on one side only bites, and a stale path bites, because a transform too forgiving to see those would report agreement over two files saying different things, which is worse than no meter at all. **GATE, uncrossed:** reconciling the 36 drifted pairs is a reading of both sides, rule by rule, and it waits for Keaton's word. `sh tools/fixtures/rule_twin_scan.sh diff <name>` prints what any one pair disagrees about. **Root closed and measured; the reconciliation surfaced as a gate rather than attempted.**
