# REDS %537 -- one ratchet held two debts that cost different things, so the number could stand still while a new fault landed on it

*Folded off the living pin [`../REDS.md`](../REDS.md) on the round that booked it, by its own
author, because the row ran roughly 4,300 bytes and landing it put the pin 2,281 over its 40,960
bound. It reads **CLOSED** -- the split is proven on metal and the control shows both ceilings from
both sides -- and a closed row folds. No peer's row was moved to make this room.*

*Booked `%536` and renumbered here to `%537` on the send's rebase, a peer having published `%536`
inside the hour. Its stamp never moved, and every living surface spelled the stamp, so the
renumber cost the file name and three lines -- which is the derived spine's fourth rule earning
its keep rather than a fault.*

*The sentence worth carrying forward: **a ratchet is a promise about one repair, so a number
covering two repairs is two ratchets wearing one name.** The 129 uncovered Glow desks were 83 that
cost one three-line block apiece and 46 the desk witness cannot name at all in its present shape,
since it passes zero arguments and the run worker refuses an argument for any stem absent from its
hand-written list. Both halves were honest and the total was right every time it was printed; the
reading still could not answer the one question a ratchet exists to answer -- did this get better?*

*The way it hid is the part to remember: the split is exact. Of the 46 desks the worker permits a
sample for in this room, **all 46 are uncovered and not one is covered.** A boundary that clean is
never an oversight -- it is the shape of the instrument, drawn in full, waiting for somebody to
cross the two enumerations and see it.*

*[`single-stranded`](../../foundations/20260823-204456_single-stranded.md) met in a counter rather
than in a predicate.*

---


**REDS %537 (`20260907.021050`) -- one ratchet held two debts that cost different things, so the number could stand still while a new fault landed on it.** *What went wrong:* `%532` landed `uncovered=129` as a single ratchet over `glow/gen/`. That number was the sum of two populations a reader cannot tell apart from it. `tools/g/glow_run_desk_witness.rish` runs every desk it names with **zero arguments** -- 218 run lines, every one ending `.glow"]`, no exceptions -- and `tools/g/glow_run_worker.sh` **refuses** an argument for any stem absent from its hand-written `case`, permitting one for **94** stems of which **46** live in this room. **All 46 are uncovered, and not one is covered.** That separation is exact, and it is the boundary of the instrument's shape rather than anyone's oversight: a desk the worker will only run WITH a sample cannot be named by a witness that passes none. So 83 of the 129 cost one three-line block apiece, and 46 cost a judgment -- *what argument proves this gate?* -- and then a fourth hand-written enumeration to hold the answers, which is the very shape `%532` booked. **And the sum hid a live fault:** cover one bare desk on the same lap a sample-taking desk lands, and 129 stands unchanged while the room has genuinely grown a hole. *What caught it:* reading the third enumeration. `%532` named three hand-written statements about this corpus and its repair measured **two** -- name marker against head marker, and coverage against the room. The arity `case` was named in that row's own prose as part of the braid and then left unread for a lap. Crossing it with the coverage set is what produced the 46/83 split and the zero overlap. *What it taught:* **a ratchet is a promise about one repair, so a number covering two repairs is two ratchets wearing one name.** The sharper half is how it hides: both halves of this one were honest, the total was correct every time it was printed, and the reading still could not answer the question a ratchet exists to answer -- *did this get better?* This is [`single-stranded`](../../foundations/20260823-204456_single-stranded.md) met in a counter rather than in a predicate. *Repaired (`20260907.021050`):* `tools/fixtures/g/glow_desk_reach_scan.sh` reads the permission list from the worker's `case` **pattern lines** rather than by grepping for stem-shaped words, so a stem named in a comment is never mistaken for a permission -- two independent extractions were taken and agreed exactly at 94, which is how the anchor was chosen. Four readings now: `sample_phantom` **GATED at zero** (a permitted stem naming no `.glow` anywhere is a dead branch, the `phantom` fault one enumeration over), `uncovered_bare` ratcheted at **83**, `uncovered_sampled` ratcheted at **46**, and `uncovered_ceiling` **derived** from the two rather than spelled, so the sum can never disagree with its parts. `tools/fixtures/g/glow_desk_reach_control.sh` proves **43 behaviors** (was 23) on miniature desk rooms in a pen -- every refusal planted then lifted, both ceilings shown from both sides, and **the two gates proven independent**: a sample-taking desk refuses at its own ceiling while the bare ceiling stands at zero, which is exactly the reading the elder sum could not express. A permission whose desk lives in another room reads `ok` rather than phantom, since 48 of the 94 name desks under `src/gate` and `src/shape` and counting them would red an honest tree. *An error of my own, found by the first new case:* the control's `runscan` helper shifted once and then handed the remaining words -- **the output path included** -- to `env`, so `env` read the output file as its first assignment and the scan never ran. It was dead code, called by nothing, and the first case to call it read every field empty. **A helper nobody calls is a helper nobody has checked.** *Also repaired, same file:* the scan's own door recited **213 covered** and **134 uncovered** -- the numbers a mis-collated `comm` produced before `LC_ALL=C` was exported, left standing in the prose when the fix landed in the code. A door reciting a number its own body disproves is this meter's subject, one room in. **CLOSED** on `glow_desk_reach_witness.rish` GREEN, 43 control behaviors, both ceiling refusals proven on metal.
