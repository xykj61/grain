# REDS %480 -- a claim whose only source is a previous statement of itself

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.092312`, in the round that
closed it, to bring the pin back under the byte bound its own header declares. Three crushed
indexes each stated a count or a completeness that nothing read -- a tally typed beside its own
table, a stamp cell left behind by the molt that moved its link, and a `GREEN` signature naming a
witness that stood exactly once in the tree, inside that sentence. **Every reading was true when
written and had no way to stay true**, because its only source was a hand rather than the thing it
describes. The repair is to read the subject, and to hold any claim that cannot be read out where a
reader can see it was held.*

*This shelf was written holding a second row, for the same shape one floor down: the roster guard
counting its own last recorded verdict among the verdicts it gates on. That row is **withdrawn
rather than published** -- a peer found, repaired and closed the identical fault as `%475` while
this round was running, and their commit landed first, so the ledger keeps one row for one finding.
What survived the rebase is the smaller half that was genuinely additive: two paragraphs in the
scan's header naming the exemption and the one-pass peer lag, and `standing_equipment_witness`
saying the scan's lines before it asserts on them, since its refusal tells a reader to read lines it
never printed. **The third duplication of the day**, and standing evidence for the open question the
card carries: nothing in the ledger shows a red is being worked.*

---

**REDS %480 (`20260906.091330`) -- three crushed indexes on one shelf each miscounted the room they stand in front of, and every link on all three resolved, so no link check could see any of them.** *What went wrong:* `docs-geode/README.md` said *ten rooms filled* while its own table, six inches below, listed **twelve** -- and it said so from the very commit that added the twelfth (`af7adb74d`, subject *fill the ten crush-empty rooms*), so the number was wrong the day it was typed and stood **sixteen days** on the page a newcomer opens first. `docs-geode/press/README.md` said *Three announcements stand* and argued the point at length in a section named *The scope, stated plainly*, over a room holding **four**. `docs-geode/wiki/README.md` promised *every shipped page* and had no row for `docs-geode/tutorials/SHOPPING.md`, which its own *what is missing* section left unnamed too -- a page four other pages already link to. *Two sharper halves beneath them.* The press index's Siya row carried a Stamp cell of `20260727.135351` beside a link to `press/20260827-165527_...` -- a month apart, because a molt moved the file and the repoint followed the **link**: a stamp in a table is a claim rather than a link, and no sweep reaches it. Measured across all **526** living non-testimony Markdown pages, that was the tree's **only** such disagreement. And the shelf's front door signed itself `witness:docs-geode-seat GREEN` from an instrument that stood **exactly once in this tree, inside that sentence** -- of seven living signed pages, six are backed by a fixture that reads their claims and aborts on the first that fails. *What caught it:* grading the shelf's own front door, which read **C+/76**, and then measuring each number on it. *What it taught:* **presence is the cheaper question wearing the expensive one's answer** (`%467`), met in a second disguise one shelf over -- a page can be perfectly connected and still miscount the room it stands in front of. The one index on this shelf whose count was right is the one rendered off disk each run (`geode_libraries`, 38 rooms); every typed one drifted. *Repaired:* `tools/fixtures/c/crushed_index_scan.sh` reads a page's OWN header declaration -- `**Kind:** crushed index of` followed by a link to the room -- so a new index arrives guarded the day it declares itself and no list here needs remembering, and gates `index_unlisted`, `index_rooms_missing`, `index_doors_missing`, `row_stamp_disagrees`, and `signature_unbacked` at zero. Control **23 behaviors**, every refusal shown from both sides, the pen proven innocent, and the control proven to bite -- a scan blinded on three counts fails it four ways. A bare mention in prose is refused as a row, since crediting one would be the fault itself wearing a friendlier face. Rostered `tier lap`, deliberately **off** the scope map: two of its readings are whole-tree censuses, and a row naming less than a guard gates is `%451`'s own shape. The tally moved out of the prose and into the table on all three pages; the front door reads **B+/87**. **CLOSED.**
