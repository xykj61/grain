# REDS -- the sample nobody drew (row %171)

*Folded from [`../REDS.md`](../REDS.md) on `20260823.171000`, **CLOSED**, as row %173 carried the living pin past its 24,576-byte bound. Both rungs it left open are closed: `pool_isolation_witness` was genuine drift and is repaired, and `pond_ring_lantern_witness` was a headless-machine gate rather than drift, which `%173` corrects and guards.*

**REDS %171 (`20260823.152322`) -- a sample sweep of 86 witnesses found two that cannot build, and neither is new.** *What went wrong:* the tools fold needed a gate wider than the 31-guard roster, so three witnesses were drawn at random from each of the 29 letter rooms and run. **Twelve of 86 answered red.** Read honestly, the twelve are four different things: **five are device or OS gated and correct to refuse here** -- `agent_jail` (nested bubblewrap is refused inside a jail), three macOS-only `cursor_jail_macos*` rungs, and `tube05_apk_pack`, which wants a phone; **three want a binary this pier has never built**, `amphora/bin/amphora`; **two were not reds at all** -- `caravan_conclude` passed on a second run, and `standing_equipment` was reading a run card holding the very reds this lap had just repaired; and **two were read as genuine**. `pool_isolation_witness` calls `table.add_child`, a member `linengrow/capabilities.rye` no longer has -- that module last changed **2026-07-15**, so the rung had been unbuildable for over five weeks, and that one was genuine. `pond_ring_lantern_witness` was read as the same and **was not**; `%173` corrects it -- its terminal wants a graphical link seam this headless pier has never carried, which is a machine fact rather than drift. **Both closed `20260823.171000`.** *What caught it:* the sweep, run as the fold's own gate, because 31 rostered guards over roughly 1,800 witnesses is a gate with a wide door beside it. *What it taught:* **a roster is a sample, and a sample nobody has drawn is a coverage claim nobody has checked.** REDS %156 left exactly this question open -- which witnesses belong on the roster -- and the answer arriving here is that the roster is not the only instrument: a periodic random draw across every room finds what a curated list was never going to look at. *Open, and named rather than closed:* the two genuine reds want the next lap, since both are module drift in `linengrow/` and `pond/` rather than anything this fold touched.
