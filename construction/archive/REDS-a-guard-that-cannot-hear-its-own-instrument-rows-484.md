# REDS %484 -- a guard that cannot hear its own instrument

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.121500`. The row is **CLOSED**
and the pin needed the room for `%486`. A closed row keeps every word it wrote; only its address
changes.*

*This shelf was written to carry `%481` beside `%484`, since the two are one sentence read twice.
It carries `%484` alone, because a peer folded `%481` onto its own shelf --
[`REDS-a-marker-no-content-guard-could-see-rows-481.md`](REDS-a-marker-no-content-guard-could-see-rows-481.md)
-- while this lap was running, and one row belongs to one record. Their shelf was published and this
one was not, so theirs stands and this one gave the row back.*

*The clause. `%481` was a fault no instrument was pointed at: the living operator card stood
published on the anointed remote with conflict markers around its Landed row, and every meter that
reads that file reads it for something else. `%484` is the guard built in answer, one lap later,
failing the same way one level down: its `git grep` ended in `|| :`, so `exit 1` meaning no match
and `exit 2` meaning could not run arrived as the same answer, and a broken instrument would have
reported a clean tree forever. **A guard must be able to hear its own instrument fail before its
silence means anything** -- and this half proved it by biting its own author within the hour, when
`--cached` placed after the pattern was read by git grep as a revision.*


**REDS %484 (`20260906.114347`) -- the guard built for the conflict marker could not tell *no markers* from *`git grep` could not run*, one lap after `%473` booked that exact swallow, and it read only the side a commit does not ship.** *What went wrong:* `conflict_marker_scan.sh` ran `git grep "$@" > raw 2>/dev/null || : > raw`, so an exit of 2 or more -- the tool refusing -- reached the same `verdict=ok` as a genuinely clean tree. *What caught it:* a second ship building the same guard in parallel and, on the rebase collision, reading the landed one; proven on metal by planting one unterminated bracket, `git grep` answering `fatal: Unmatched [` at **128**, and the scan answering **`verdict=ok` at exit 0**. *Second fault, same file:* it read the **working tree only**, and a file repaired in the editor leaves the **index** still carrying the block -- which is what a commit ships. The operator card sat in exactly that state on metal. *What it taught:* **a guard written against a fault it just watched is written in a hurry, and the hurry lands in the error path nobody plants.** `%473` is the same swallow one guard over, so this is a lantern's second firing. *Repaired, into the landed guard rather than beside it:* the status is classified under `set -e` through an `if` condition, both sides are read, and the **diff3 base** marker counts. **It bit for real within the hour** -- `--cached` placed after the pattern is read by `git grep` as a REVISION, exit 128, which the elder fallback would have shipped as a guard reading nothing and reporting ok forever. Control **31 readings, 12 states, 12 repositories**; the second pass proven to see what the first cannot. *Named, not taken:* the three teaching files are excluded BY NAME, so a new page showing a marker reds the fleet until listed -- column-zero anchoring would need no list, and that is the peer's call. *Erratum, owned:* this lap booked `%477`, then `%479`, then `%481`, each published by a peer while it held them, and the first renumber ran as a blind `sed` that rewrote a peer's row and two upstream passages -- caught by diffing every touched file against `xy/main`, restored byte for byte, nothing shipped. **A renumber is a rename, and a rename sweeps only what it owns.** **CLOSED.**
