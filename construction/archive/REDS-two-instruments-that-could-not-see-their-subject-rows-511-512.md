# REDS %511 and %512 -- two instruments that could not see their subject

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.194921`. Both rows were written
by laps that died before their own commits, and both were read back out of the dead-letter box and
landed by a third -- which is why each publishes five numbers above where it was booked. The pair
renumbered three times in one round: `%506` was spent while the first lap sat in the box, `%508`
while the second did, and `%510` by a peer mid-send.*

**What the two rows taught, in one line:** a guard that narrows its own subject reports a smaller
clean tree, and nothing about a clean reading says how much of the subject it covered.

**The same shape twice, in different characters.** A shell `*` stops at `/` where a git pathspec
crosses it, so a folded room silently left the doorway roster. A free function's first `if` block
closes at the same four spaces a struct method's body does, so the long-function ledger ended every
count there. Neither instrument reported an error; both simply answered about less.

**Kin.** [`%505`](../REDS.md) named the class one
day earlier from the reach side -- *an unprinted reach is a reach claimed by implication* -- and both
repairs here print their own reach because of it. [`%381`](../REDS.md) and `log_has_a_row` are the
first two firings of the narrowing half; this is the third and fourth.

---

**REDS %511 (`20260906.161500`) -- a broken path reds; a narrowed glob goes green, and 607 pages left a guard's reach on the day their room was filed.** *What went wrong:* `tools/fixtures/t/two_rooms_doorway_roster.sh` named its subject with a shell glob -- `for f in external-research/*.md active-designing/*.md docs/*.md`. Then `active-designing/` folded to `date/YYYYMMDD/` under the mark law, and **a shell `*` stops at `/`**. The roster kept returning files, so nothing read as wrong; it returned **216** post-seating pages where the three rooms hold **1,085**. Three of the 607 it dropped name no room at all. **The same instrument had two more faults, each hiding the next.** It stood on **no roster**, so nothing ran it -- and run by hand it was **RED**, at 45 pages. And `assert result.ok` stood ahead of `say result.out`, so the one run that mattered printed *scan failed* and swallowed all 45 names behind it. *What caught it:* the council rota's **Earth** row, whose whole instruction is to read the concrete fact at the door -- the room token off a Status line. Its own threshold page names no room, which sent this seat to the guard that is supposed to say so. *What it taught:* **the fold law is built entirely for the failure that announces itself.** `dated_path_repoint` moves living references, `dated_path_resolve` recovers stale ones, and a witness grepping an absent path reds loudly. A glob reads no absent path; it returns a smaller set and passes. **This is the third firing:** `index_row_bound` read the pin alone and passed four days over 87 unheld rows (`%381`), `log_has_a_row` watched the flat room and read `flat_logs=0` for nine days (repaired `20260905`), and now this. A lantern that fires three times is a loom. *Repaired (`20260906.161500`), on metal:* the roster reads `git ls-files`, whose pathspec `*` **crosses `/`** -- the difference is exact and measurable, `git ls-files 'counsel/*.md'` answering 941 where `ls active-designing/*.md` answers 110 of 1,055. Reach **319 -> 972** files, **216 -> 880** post-seating pages, **640** of them on folded shelves. `yonder/` and `archive/` stay out, because the shell glob never reached them and pulling them in would widen the subject rather than restore it. The scan **floors its own reach on a folded page**, so the same narrowing refuses rather than reporting a smaller clean tree -- this class watching for its own return. The witness prints the scan's output **before** any assert. Rostered `tier cadence` (28s over 972 pages; eight ships read the roster twice a lap on four cores). Fourteen behaviors in a pen, and the control is proven load-bearing rather than merely green: **ten of the fourteen flip to `no`** when the elder roster is put back. One of its own legs was a test that cannot fail -- `grep -qv PATTERN` answers yes whenever any line fails to match -- caught before it shipped and replaced by a counted reach (`%503`'s class, one lap later, in the fixture written to prove the fix). **BOOKED** *(landed `20260906.194921` by a third lap: this one died before its commit, the next round open stashed its work, and the lap that read the record back out died before ITS commit too -- so `%506`, then `%508`, then `%510` were spent upstream and the row publishes at `%511` under the derived spine, having renumbered three times. The ceiling publishes at **46** rather than the 45 measured, because one external-research page landed between the measurement and the send, which is `%341`'s borrowed-number class and the honest reading is the one taken at the send. Two of the 46 read `**Status:** Proposed -- external research`, and TWO_ROOMS.md's gloss table names four tokens with `proposed` among none of them while its own prose says* "what is proven as proven and what is proposed as proposed" *-- that disagreement is the canon's to settle, not this guard's. The remainder is a ratchet under a ceiling that only falls, three of them dated testimony that accrete-never-break keeps as written; and `yonder/`'s 262 post-seating pages, 11 naming no room, are a seat rather than a lap -- widening a subject is Keaton's word, restoring one is not).*

**REDS %512 (`20260906.173013`) -- the ledger behind TAME's own 70-line ratchet ended every function at its first `if` block, and read 15 where 321 stand.** *What went wrong:* `tools/fixtures/t/tame_style_long_fn_one.sh` is the instrument behind the tidy rule *functions past 70 lines -> split at natural seams*. Its awk ended a function at the first line matching `^}$` **or** `^    }$`. The second alternative was written for a struct method, whose own body closes at four spaces -- applied to a free function it matches the closing brace of that function's **first `if` block**, which sits at exactly four spaces too. Its start pattern `^( *)?(pub )?fn ` matched any **indented** `fn ` as well, so an inline comparator or a nested struct method restarted the count inside its own parent. Both holes fail **open**: the ledger reads clean, which is the one answer indistinguishable from a healthy tree. *What caught it:* a fire-row lap taking the card's own unclaimed sibling find and measuring rather than agreeing -- a brace-depth counter run beside the elder over the roster's **611** authored files. The elder reported **15** functions past 70 lines; **321** stand, across **173** files. Its longest read **180** where `caravan/farewell.rye:check_suffice_runs` runs **809** lines, 10768 to 11576, counted by hand, and **ten functions past 600 lines** were invisible to it. Every function it did find is still found -- ten at exactly the same count, five longer, `pond/apps/scooter_paint.rye:run_selftest` at **324** where it read 127 -- which is the reading that earned the replacement trust, since a counter disagreeing everywhere is likelier wrong than the one it replaces. *What it taught:* **a closing-brace pattern cannot end a function, because the brace that closes a function and the brace that closes its first block are the same character at the same column.** Only depth tells them apart, and only a depth read from code alone: five roster files carry a brace inside a char literal, twelve carry 119 multiline-string lines holding one, and a `{d}` format string stands on nearly every page. *Repaired (`20260906.173013`), landed `20260906.194921`, on metal:* the awk tracks brace depth through a `strip` function that scans each line character by character, dropping comments, string literals and char literals, with a Zig multiline-string line dropped whole and a named `MAX_FN_LINES` bound of 4096 -- five times the longest measured, rounded to a power of two -- so an unbalanced file names its bound rather than counting to its end. `tame_style_long_fn_scan.sh` walks the roster once and prints `functions_over_70` and `longest` ahead of the top ten, since a ledger showing ten rows and no total lets a reader take the ten for the whole set (`%505`, one room over). The control plants **20 behaviors** in a pen and the elder awk, run on the same fixtures, is **bitten on three**; four more legs exist only to hold the new mechanism, because counting braces introduces a hazard the elder never had. The witness gates the **instrument** -- the control, both elder patterns' absence from the code, and a **floor of one** on the tree's own reading -- and never the count, since 321 standing functions belong to six lanes and TAME's own tidy rules seat this family as a ratchet that prints. **And the blind reading was published on the front door.** `tools/fixtures/f/fascia_metric_v0.sh`
calls `tame_style_long_fn_one.sh` per roster file and spends the count directly as clutter, so the
root `README.md` carried **fascia 68** where the tree stood at **58** -- ten points the metric could
not see, on the most Lindy-exposed page the project owns. The repaired ledger drops it to 58 in this
same commit, and nothing about the tree changed: only what the instrument could count. **The reach
this leaves, named rather than implied:** that penalty is capped at 25, so the grade cannot tell 26
long functions from 321, and the ratchet's own progress stays invisible to the front door until the
count falls under 25. A saturated signal reports health it has not measured, which is this pair's
lesson arriving a third time in the metric that reads them. **CLOSED** *(the count itself is that
ratchet, migrate-on-touch; the card's ratchet line read 14 until this lap, and
`context/TAME_GUIDANCE.md`'s claim that the ledger "cleared" at parity 196 is corrected in the same
commit).*
