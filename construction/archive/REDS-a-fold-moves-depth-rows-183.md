# REDS -- a fold moves depth, and the first shelf writer only moved text (row %183)

*Folded from [`../REDS.md`](../REDS.md) on `20260824.075409`, **CLOSED**, as row %187 carried the living pin past its 24,576-byte bound.*

**REDS %183 (`20260824.054621`) -- a fold moves depth, and the first shelf writer only moved text.** *What went wrong:* `shelf_row` in `tools/rye/session_logs_archive.rye` rewrote one link shape when a row moved onto a shelf -- `](date/YYYYMMDD/name)` to `](YYYYMMDD/name)` -- and left every other target exactly as written. A shelf sits **one directory deeper** than the pin, so a relative target that meant `<room>/T` has to become `../T`, and **62 targets** in this room reach out past the room with `../`: `../docs/CRYPTO.md`, `../crypto/README.md`, `../foundations/...`. All 62 broke. The same fault rode the REDS fold in the same round: rows 177-178 moved into `construction/archive/` carrying `../tools/...` links that needed `../../tools/...`. *What caught it:* `readme_reach`, on the roster run after `git add`, naming each broken living link and its citing file. **The loom for this class already existed and fired**, which is the system working rather than a gap. *What it taught, twice over.* First the standing one, restated because it recurred: **a repointer moves text while a fold moves depth** (REDS %166-%170). The general repair is a rule over **every** relative target rather than over the shapes anybody remembered -- prefix `../`, with the one prettier equivalent for `date/<day>/` and no change to a URL, an anchor, or a root-absolute path. Second, the new one, and the reason this row exists: **when a tool's output is wrong, fix the tool and re-run from a clean base rather than patching the output.** I first repaired the 26 already-written shelves with a script, and it over-applied -- deepening each shelf **header's** own `../SEASONS.md` link, which the tool writes correctly for that depth, and reaching two **elder** shelves from `20260725.040520` that had been right for a month. A patch over generated output has to re-derive what the generator knew, and it will get some of it wrong; the generator already knows. *Repaired:* `shelf_row` rewritten to apply one rule to every relative target; the pin restored from `HEAD`, all 26 shelves deleted, the fold re-run from clean, and the two elder shelves restored byte-for-byte with `git checkout`. The REDS archive shelf's six links deepened by hand, since it holds no generator. `readme_reach` GREEN after. The selftest gains a `../` case so the rule is proven rather than believed. CLOSED.
