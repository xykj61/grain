# REDS %467-%468 -- presence where the claim was content, and a guard nobody ran

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.064500` at the moment they were
written, because every other row standing there is **OPEN** and the pin was already within 1,534
bytes of the ceiling its own header declares. Both are **CLOSED** on witnesses, so the shelf is
where they belong rather than a concession.*

*They share one clause. `%467`: the compass rose named `construction/TASKS.md` and
`construction/ROADMAP.md` as its last two stations for fourteen days after those files became
pointer stubs, and `tools/co/compass_rose.rish` -- the guard named for the rose -- asked `test -f`
of both and printed GREEN, including the words *pointer hygiene*. `%468`: the standing roster's
scan held its capability list as a hand-written literal while the runner grew a second probe, so the
runner skipped a guard by name while the scan refused the whole roster, and
`tools/s/standing_equipment_witness.rish` was red for hours with nothing listening -- because the
roster's own guard sat on no roster row. **Presence is the cheaper question, and it wears the
expensive one's answer right up until the day the content moves.** A link that WORKS and points at
nothing is invisible to every guard built to find links that break; a list restated is a list that
will drift, and the copy that goes stale is the one whose reader is quietest. Both repairs read the
claim from its own source now -- the stations out of the rose, the capabilities off the runner --
so the check moves when the thing does. Link depth moved with the text.*


**REDS %467 (`20260906.053014`) -- the compass rose sent fourteen days of readers to two pointer stubs to learn the now, and the guard named for the rose printed GREEN the whole time.** *What went wrong:* on `20260823.103804` commit `98a8ee481` fused three cards into `construction/ITINERARY.md`. `construction/TASKS.md` and `construction/ROADMAP.md` stayed on disk as **pointer stubs**, each declaring `**Status:** Pointer` on its own face, deliberately -- roughly thirty `equinox_e*` fixtures read them, and a guard reading a file that is gone reports green while measuring nothing. So the *paths* stayed alive and the *stations* left, and `foundations/20260826-024943_follow-our-compass.md` -- the tree's own return habit, the aether row's cardinal seat, deep-read every fifth lap by eight ships -- went on naming them as rose stations five and six, together with a precedence law (*"If TASKS and ROADMAP disagree with a seated brief..."*) about two files that can no longer disagree with anything. **Six living surfaces repeated it:** the rose itself, `.claude/rules/align.md` (the rule that walks it, in two places), `foundations/README.md` (twice), `docs/COMPASS.md` step 9 -- the *cold-start* compressor, where a reader has the least context to survive a redirect -- `recursion-prompts/seed/autonomous-loop.seed.md`, which calls both stubs *crux to-do ledgers* to every unattended lap on every ship, and `ORGANIZING.md`, which tells a reader to **write** a note into one. *What caught it:* the aether rota's own instruction to listen for **the silence where a claim used to be**, on a lap that read the rose as its cardinal seat and then opened the two files it names. **No standing instrument could have:** every link check in this tree asks whether a link *resolves*, and a pointer stub resolves -- `qa_report_card.sh` scored this document's Truth at **100**, *0 of 13 cited paths unresolved*, on the morning it was pointing at two empty rooms. And `tools/co/compass_rose.rish` asked `test -f construction/TASKS.md && test -f construction/ROADMAP.md`, then printed `GREEN: ... now, order; plain stories; pointer hygiene`. *What it taught:* **a path outlives its content, so presence is the cheaper question wearing the expensive one's answer** -- `%458`'s family exactly, one room over. A link that *works* and points at nothing is invisible to every guard built to find links that break. **CLOSED (`20260906.055500`):** all six surfaces repointed; `tools/fixtures/c/compass_station_scan.sh` reads the stations **out of the rose document itself**, so the check moves when the walk does, and refuses a station that is missing or self-declares as a pointer; `tools/fixtures/c/compass_station_control.sh` proves **18 behaviors** in a pen, refusals and welcomes alike, each plant removed afterward so the same pen walks free. Replayed against `fd62ad0a1`, the tree as it stood one commit ago, it answers `stations_pointer=2` and names both. The witness itself was planted with the historical fault and **red by name**, then green on restore. `compass_rose` was on **no roster at all** -- see `%468` -- and is `tier lap` now, 1s.

**REDS %468 (`20260906.055500`) -- the standing roster's own guard was red on metal and had been for hours, and nothing was listening, because the roster's guard stands on no roster.** *What went wrong:* `tools/fixtures/s/standing_equipment_scan.sh` held `known_capabilities="ipv6"` **written by hand**, while `capability_state()` in `tools/fixtures/s/standing_equipment_run.sh` is where a capability probe actually lives. On `20260906` a peer taught the runner a second probe, `jail_nesting`, so `agent_jail_enclosure` could name what a nested jail cannot do. The runner honored the word and skipped that guard by name -- this lap's own cold pass read `run_verdict=ok skipped_capability=1`, 126 guards, **zero red** -- while the scan, holding the elder one-word list, refused the entire roster as `roster_broken`, and `tools/s/standing_equipment_witness.rish` asserted *"the roster names something the tree cannot honor"* and stayed red. **The runner's own comment names the trap it then fell into:** *"Two copies of one list is the drift this tree keeps paying for."* It covered one direction -- a word the runner does not know reads `unknown` and therefore runs -- and this was the other, a word the runner **does** know and the scan does not. *What caught it:* running the scan by hand while seating an unrelated row in the roster, on a lap whose whole subject was instruments that measure a cheaper thing than they claim. **Not a meter:** the roster's header names this witness as *how it is kept honest*, and no row runs it, so the red was audible to nobody -- and the roster had already written the lesson under `comlink_nesting`: **a rule that names a check and a roster that never runs it are one sentence disagreeing with itself.** That sentence was standing at the roster's own front door. *What it taught:* **a list restated is a list that will drift, and the copy that goes stale is the one whose reader is quietest.** **CLOSED (`20260906.060000`):** the scan **derives** `known_capabilities` from the runner's own `capability_state()` case arms rather than restating them -- reading `ipv6 jail_nesting` today -- refuses when the derivation finds nothing, since a list read as empty accepts everything, and resolves the runner **beside itself** rather than from the working directory. That last is my own regression, planted and caught inside a minute: the first draft read a cwd-relative path, and the control `cd`s into a pen holding no `tools/` at all, so all 30-odd of its cases died at once. An unknown capability still refuses, proven by planting `warp_drive`. And `standing_equipment` is on the roster now, `tier lap`, 16s, GREEN on metal before its row was written -- because rostering a red guard reds every cold pass in the fleet.
