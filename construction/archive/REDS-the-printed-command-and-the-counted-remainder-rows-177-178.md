# REDS -- the printed command and the counted remainder, rows 177-178 (folded)

**Language:** EN
**Folded:** `20260824.052950` from [`../REDS.md`](../REDS.md) -- a closed-range archive fold to keep the living pin under `living_pin_max_bytes`.
**Style:** Gauge, Meter - **Voice:** Kyri
**Span:** two rounds about text nothing had ever parsed -- a loop recipe a hand was told to paste that bash could not run, and a remainder a sentence counted where a script should have.

*A red owned in conversation is a memory. A red recorded here is a proof. These rows moved out of the living pin only to keep it readable; every word stands as written.*

---

**REDS %177 (`20260824.025704`) -- the tree printed a command that bash cannot run.** *What went wrong:* `tools/l/launch-claude-season.rish` prints the unattended-loop recipe -- long single-quoted shell prompts a hand copies into an outer terminal. Inside a single-quoted shell string a literal apostrophe has to be written `''`, and **one of them was written as a lone `'`**, in the words *the linker's own search path*, added by the lap that seated the ask-the-system discipline. A lone apostrophe closes the string, so every character after it falls out of quoting, and the first later parenthesis -- `(round-set, quest, journey, equinox, season)` -- reached bash as syntax. Both printed launch lines died with ``syntax error near unexpected token `('``. **The season loop could not start at all**, and the paste looked at fault when the tree was. *What caught it:* Keaton pasted the line and read the error, then asked whether it was fixed -- and the root was found by reproducing the parse rather than reasoning about the text: writing the pasted command to a file, `bash -n` on it, then collapsing one `''` to `'` and watching the identical error appear. *What it taught:* **a string a tool prints for a hand to run is code, and until something parses it, it is untested code.** Every guard in the roster read files the tree executes; nothing read the one thing a person was told to execute. The class is wider than quoting: any generated command line -- install lines on the operator card, a recipe, a copyable one-liner -- carries it. *Repaired:* both `say` lines escaped to `linker''s`, and all six printed runnable lines now parse. Prevention seated as the 36th standing guard: [`tools/l/loop_prompt_parse_witness.rish`](../../tools/l/loop_prompt_parse_witness.rish) over [`tools/fixtures/loop_prompt_parse_scan.sh`](../../tools/fixtures/loop_prompt_parse_scan.sh), which runs the recipe tool and `bash -n`s every printed line the reader is told to run, gated at zero, with narration left free. Proven both ways by [`tools/fixtures/loop_prompt_parse_control.sh`](../../tools/fixtures/loop_prompt_parse_control.sh) -- eight cases in a temporary pen: the lone apostrophe refused and its line named, a correctly doubled one passed, prose apostrophes left alone, the outer loop's own line measured beside the jail elder, and an empty or absent printout told apart from a failing one. CLOSED.

**REDS %178 (`20260824.035828`) -- the open remainder was itself counted from memory.** *What went wrong:* REDS %175 found the tree's dated/living test requiring an underscore after the stamp where the session-logs law makes the sprig optional, repaired the canonical seam and the repointer's write set, and named **eight** read-only scans still carrying the narrow pattern. A grep for the three spellings this tree writes stamps in found **nineteen** sites across **fifteen** files -- the eight named, and eleven nobody had counted: `tools/ad/ad_walk_sample.sh`, its witness, `tools/d/dated_amendment_audit.sh`, `tools/fixtures/dated_guard_scan.sh`, three more sites inside `dated_path_repoint_scan.sh` beside the one already repaired, `oldness_census_scan.sh`, `radiant_lint_scan.sh`, and two in `shipped_binary_claim_scan.sh`. **The row that recorded a fault about working from memory named its own remainder from memory.** Two of the eleven carry a consequence past a miscount: `dated_guard_scan.sh` is the guard that refuses edits to dated artifacts, so the 237 sprigless logs stood **outside the protection it exists to give**; and the three repointer sites mean its map, its candidate filter, and its line matcher could none of them see a sprigless reference, so those logs were safe from being wrongly written and invisible to being rightly repointed. *What caught it:* writing the loom. Building a guard for the class required enumerating the class, and the enumeration disagreed with the row. *What it taught:* **a named remainder is a measurement and belongs to a script, not to a sentence.** Widening the right side then surfaced a looseness on the LEFT that had stood the whole time -- the census reference pattern had no boundary, so a stamp could match inside a longer filename, and the retired countdown-prefix names (`99991_20260619-090912.md`) are exactly that shape. 24 such substrings read as lost references. *Repaired:* all nineteen sites widened to `[_.]`, `(_|\.)`, or `(_<sprig>)?\.`; a PCRE lookbehind seats the boundary at the census's three reference greps; `tools/d/dated_path_resolve.rish` reads the stamp before the extension as well as before the sprig, so the fold rule now fires for a sprigless log, and gains a **by-stamp reading** bounded to sprigless references, so an elder name a collision split reports every file it produced rather than `missing`. Measured after: the census sees **239 genuine references it was blind to**, and `refs_lost` stands at **187**, exactly where it stood -- a wider and tighter meter reporting the same number. *Prevention* seated as the **37th** standing guard: [`../tools/d/dated_spelling_witness.rish`](../../tools/d/dated_spelling_witness.rish) over [`../tools/fixtures/dated_spelling_scan.sh`](../../tools/fixtures/dated_spelling_scan.sh), gating 4,705 living tracked sources at zero, proven both ways by [`../tools/fixtures/dated_spelling_control.sh`](../../tools/fixtures/dated_spelling_control.sh) -- twelve behaviors on real git repositories in a throwaway pen, three narrow spellings bitten and nine honest readings left free, including a **sprigless** dated basename read as testimony, since a guard recognising testimony only when it wore a sprig would carry the very fault it exists to catch. `dated_path_witness` gains a seventh verdict case and an eighth refusal. CLOSED.
