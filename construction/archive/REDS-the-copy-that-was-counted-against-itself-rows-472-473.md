# REDS %472 and %473 -- the copy that was counted against itself, and the meter that swallowed its own

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.070240` at the moment it was
written, for the reason the `%467`-`%468` shelf gives one hour earlier: every other row standing on
that pin is **OPEN**, and the pin sat 1 byte under the ceiling its own header declares once this row
was in it. This row is **CLOSED** on a witness, so the shelf is where it belongs rather than a
concession.*

*The clause is a counting one. An audit measured 113 regular Rye files standing under a basename
this tree also symlinks, found 108 byte-identical and 5 differing, and published that as
reassurance. The 108 are the **originals** -- each compared with itself, at distance zero by
construction. Set every basename's own canon aside and the population of real second copies is
**5**, of which **not one agrees**. Two are behind the file their siblings link and three are
separate modules sharing a word. The number was never wrong; the denominator held the thing being
measured against, and a ratio shaped that way flatters in the one direction nobody re-checks.*

*What closes it is an instrument rather than a correction: [`../../tools/co/copy_lag_witness.rish`](../../tools/co/copy_lag_witness.rish)
over [`../../tools/fixtures/c/copy_lag_scan.sh`](../../tools/fixtures/c/copy_lag_scan.sh), carrying
to all 108 both-way basenames the rule `copy_sameness` already kept for one.*


**REDS %472 (`20260906.070240`) -- a census that counts an original as a copy of itself reports 96 percent agreement over a set with none.** *What went wrong:* the audit at `active-designing/20260906-003146` published, and this card carried, *113 copies standing under a basename the tree also symlinks, 108 byte-identical and 5 differing*. Every count is true and the headline is not: **108 of those 113 ARE the canon**, each compared with itself, at distance zero by construction. Set each basename's own canon aside and the copy population is **5**, of which **none agree** -- this tree holds no healthy duplicate, where the reading said 108. *What caught it:* building the instrument that same audit named and left unbuilt. Its first run read **four split canons** where the tree has none, since `granary/parse_int.rye` reaches `tally/parse_int.rye` *through* `linengrow/` and one hop called each waypoint a canon -- `%463`'s lesson one room over. *What it taught:* **a ratio whose denominator holds the thing being measured against is a ratio of the population to itself.** *Repaired:* `tools/co/copy_lag_witness.rish` over `tools/fixtures/c/copy_lag_scan.sh` gates `behind` under a ceiling of **2** that only falls and `split_canon` at zero, reports `sibling`, and resolves chains bounded at 16 hops. **27 pen behaviors**, the ceiling **asked for, never spelled**, and the pen proven able to sound by three planted breaks -- dead classifier, one-hop resolver, canon read as a copy -- each bitten. `tier lap` **8s**. **CLOSED.**

**REDS %473 (`20260906.090312`) -- the reading written to name a silently short answer was itself giving one: its index listing was piped into `awk` under `|| true`, so a refused checkout and a tree with no Rye in it arrived at the same verdict, at exit 0.** *What went wrong:* `tools/fixtures/c/copy_lag_scan.sh` opened with `git ls-files -s -- '*.rye' 2>/dev/null | awk '{print $1, $4}' > "$pen/modes" || true`, and its own header three lines above declared the opposite -- *both refuse rather than truncate, since a silently short reading is the fault this file exists to name*. Two faults wore that one line. The **swallow**: a `git` refusal produced an empty listing rather than an error, which `instrument_refusal` gates at zero, since an `awk` producing output has no found-nothing exit and any non-zero from it is a failure. The **exit**: the empty branch answered `verdict=no_rye` and `exit 0`, so a root that could not be read was indistinguishable from an honest tree with no Rye in it, and the shell read a pass. *What caught it:* `instrument_refusal` refused, on the hour the scan was written -- `swallowed_instrument_passes=1` against `swallowed_ceiling=0`, `verdict=over_ceiling`. **Nobody read it for eighty minutes.** The lap that wrote the scan died mid-round, `fleet_round_open.sh` stashed its whole set under `%321`, and the guard's evidence was written to `construction/standing-equipment-reds/instrument_refusal.txt` -- **untracked by `.gitignore:384`**, so the tree's own record of its own refusal was invisible to git, to the card, and to every peer. It surfaced through a different instrument entirely: `standing_equipment` read `runs_unrostered: copy_lag` at the cold open, because the run ledger held a green receipt for a guard the stash had carried out of the roster. *What it taught:* **a meter's own instrument is the one thing its readings cannot check.** The scan proved twenty-seven behaviors of the tree and nothing at all about whether it could read the tree. *Repaired:* `git ls-files` and the `awk` pass each refuse by name -- `verdict=no_checkout` and `verdict=instrument_failed` -- and the empty corpus exits 2 as its sibling `rye_compile_reach_scan.sh` already did. Four control legs prove it, and the decomposition is the finding: **the exit-code leg alone would have missed the original fault**, since a swallowed `git` refusal fell through to a branch that also refused, so only the verdict leg tells a refused checkout from an empty tree. Planted whole as it stood at `20260906.072053`, all four legs go `no` and `instrument_refusal` reproduces `over_ceiling`; repaired, all four go `yes` and it reads `scans_read=717 swallowed_instrument_passes=0 verdict=ok`. 31 behaviors, `tier lap`. **CLOSED.**
