# REDS %454 -- a published distance promises a road, and a third of the sky had none

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.090503`, in the same lap that
closed it, to bring the pin back under the byte bound its own header declares -- it stood 325 bytes
free when this row's repair was written, and every other row on it reads OPEN. The row is kept
whole. What it teaches past its own repair: a metric that reads a ROLE rather than a POINT will
answer confidently about pairs it cannot walk, and neither end of that disagreement looks wrong on
its own -- the module's arithmetic is exact between two addresses, and the sponsor chain is exact
between two numbers, and only their meeting is false. The cure was not to correct the function but
to notice it had been asked two questions. Link depth moved with the text.*


**REDS %454 (`20260906.010402`) -- `route_hops` publishes a hop count, and for a third of the sky there is no walk of that length.** *What went wrong:* built from the metric's OWN unit distance, the `compass_sky` graph holds **642 edges over 720 points, 132 with no edge at all**, and a walk from every point cannot reach **172,524 of 518,400 ordered pairs (33.28%)** -- the isolated set being the star-index-zero planets. `Address.parent` says point 60's sponsor is point 0; `route_hops(decode(60), decode(0))` answers **2**. *What caught it:* `tools/fixtures/t/topology_graph_census.sh`. *What it taught:* **a metric that reads a role rather than a point is not a metric on points** -- yet where a walk exists it is exact, 345,876 pairs and zero disagreements, so one shape, small repair. Peer `%452` repaired the ZERO end; this is the other. Paper: [`a-third-of-the-sky`](../../active-designing/20260906-010402_a-third-of-the-sky-has-no-road.md). *Repaired (`20260906.090503`), by giving the two questions two functions rather than one:* `Sky.point_hops(a, b)` in `comlink/topology.rye` measures between point NUMBERS by walking the sponsor chain in number space -- `sponsor_of` composes `decode`, `Address.parent` and `encode`, so a star of index zero and the galaxy it sits on are ONE chain entry rather than two -- and the nearest shared ancestor is found by rising on `i + j` over the two three-entry chains, since a chain repeats once it reaches its galaxy and a meeting at a higher index names the same point by a longer road. `route_hops` is untouched, so `%452`'s just-seated address reading and its pinned outfit legs stand; the selftest pins both side by side, `point_hops(60, 0)` at **1** where `route_hops(decode(60).?, decode(0).?)` stays **2**. *Proven by doing:* `prove_the_sponsor_is_one_hop` reads the census's own failing predicate over every point of both skies, and `prove_every_pair_has_a_next_hop` walks all **682,425 ordered pairs** asserting some neighbour stands strictly closer, symmetry and the diameter riding the same sweep -- greedy progress from anywhere is what makes a published number a road. *Measured independently*, by an awk breadth-first walk sharing no code with the module: compass **132 isolated points -> 0**, **172,524 unreachable ordered pairs -> 0**, edges 642 -> 774, and **the walk diameter does not move, 5**; council 120 -> 0, 82,680 -> 0, diameter 5. **That prices the paper's torus:** it bought reachability at diameter 14 against the ladder's 5, and reading the same ladder by POINT buys it at 5 -- so the torus's remaining case is uniform degree, zero cut points, and the angle, which is a ruling rather than a repair. *Proven both ways:* `tools/fixtures/t/topology_point_metric_control.sh`, six phases, 18 assertions -- four plants refused by name (the search skipping zero, the dropped bridge, a chain that stops rising, a depth reading the outfit), a clean copy proving the pen innocent, and `legs_removed` **passing** with the same broken bridge once the legs are cut, while the binary still prints the claim. `comlink_topology` 2s -> 12s at `tier lap`, roster note carried. **CLOSED.**
