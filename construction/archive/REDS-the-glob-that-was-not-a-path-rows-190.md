# REDS %190 -- the glob that was not a path

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.104946`, **CLOSED** -- the
creative suite's front door naming 83 of 240 modules and a glob that matched nothing, and the 44th
standing guard it seated. Moved here as row %192 carried the living pin past its 24,576-byte bound.*

---

**REDS %190 (`20260824.091754`) -- the creative suite's front door named 83 of the 240 modules beside it, and told a reader to run a glob that matches nothing.** *What went wrong:* `lotus/README.md` stood at **297,878 bytes** over 144 lines -- one line of it 94,935 bytes -- and it is the page a reader enters Grain's creative suite by. It named **83** distinct `.rye` files while **240** stand in `lotus/`, so **157 modules had landed with nobody adding a line**: the whole reverb family, most of dynamics, and every one of the 91 `stereo_*` carries past the handful the page happened to mention. One of the 83 was worse than absent -- `channel.rye` is cited by bare name and lives in `constel/` and `pond/apps/`, never here. Two further claims had gone stale in the same page. Its *Prove the rungs* section told a reader to run `for w in tools/ales_*_witness.rish`, which matches **nothing**: `tools/` folded to letter rooms on `20260823.144100` and those 239 witnesses live in `tools/al/`. And it recited **213 witnesses, ALES0 through ALES212** while **239** stand on disk and the ladder reaches **ALES238**. *What caught it:* the previous lap's `recommend` line named this page as the next round and asked for the named-against-standing count first; `comm` between `ls lotus/*.rye` and the names the page cites returned 83 against 240 in one line. *What it taught:* **a repointer rewrites paths, and a glob is not a path.** `tools/t/tool_path_repoint.rish` builds its map from *"every folded file names the flat path it used to answer to"* and *"rewrites only a basename the map holds"* -- so it correctly repointed all twenty-one explicit `tools/al/ales_*` paths in the same code block, and passed over `tools/ales_*_witness.rish` and `tools/ales_<rung>_witness.rish` in silence, because neither is a basename it holds. A pattern is a promise a reader runs, and it wants repointing exactly as much as a path does. Beside that, this is the third firing in one day of the roster shape first booked as `%184` -- a ladder table naming 73 of 110, a page naming 112 of 227, and now 83 of 240 -- and `%189`'s corollary held again: at 297,878 bytes nobody was going to count. *Repaired:* the page split three ways in the same directory, so every relative link inside it stays true -- a **Door front page of 3,977 bytes**, [`../../lotus/LADDER.md`](../../lotus/LADDER.md) carrying the rung index, the rung sections, and the road-on narrative moved word for word, and [`../../lotus/MODULES.md`](../../lotus/MODULES.md) with **240 rows in twelve families**, each sentence written from that module's own `//!` head comment and normalized to plain ASCII. The glob and the rung-pattern text now name `tools/al/`, and the front door states 240 modules, 239 witnesses, and ALES0 through ALES238 -- each read from the tree. *Prevention* seated as the **44th** standing guard: [`../../tools/l/lotus_module_roster_witness.rish`](../../tools/l/lotus_module_roster_witness.rish), gating `unrostered`, `phantom`, `duplicate_rows` and `mismatched_rows` at zero. It drives **the same scan the image guard drives**, renamed [`../../tools/fixtures/module_roster_scan.sh`](../../tools/fixtures/module_roster_scan.sh) from `image_module_roster_scan.sh` in this round, since the scan already took the directory as an argument and a rule written twice is a rule two files may quietly come to disagree about -- checkpoint `7cc0b62b56`. Proven RED on metal against the real page by removing the `reverb_freeze.rye` row (`unrostered=1`, `verdict=roster_incomplete`) and GREEN after, and both ways by [`../../tools/fixtures/module_roster_control.sh`](../../tools/fixtures/module_roster_control.sh) -- **sixteen behaviors on real directories** in a throwaway pen, nine refusals bitten and seven honest readings left free. The sixteenth is new and specific to this page: ten rows write an absolute value as `y = \|x\|`, the correct Markdown for a literal pipe inside a table cell, and a reading that stopped at the first pipe would have called every one of them a mismatch. *And the Door meter read the new page at 10% against its 20% ceiling* on the lap it joined the roster -- the first front door in this arc that wanted no sweep after writing. CLOSED.
