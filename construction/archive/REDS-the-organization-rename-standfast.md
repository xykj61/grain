# REDS -- the organization rename, and the deep debride booked rather than run (standfast)

*Folded from [`../REDS.md`](../REDS.md) on `20260823.204456` as row %175 carried the living pin past its 24,576-byte bound. **The standfast still stands** -- folding moves the record, never the decision. The deep debride of the 11 commit-message occurrences of the elder organization name awaits Keaton's word.*

**STANDFAST (`20260823.184309`) -- the organization rename, and the deep debride booked rather than run.** Keaton renamed the GitHub organization **`autoproject96` to `groupproject405`** and granted the breach, the molt, and the deep debride. The **living** half is done on this lap: `origin` repointed to `git@github.com:groupproject405/grain.git`, **32 occurrences swept across 16 living files**, and both names seated on the seed's IDENT leak roster and in `sow_scrub.sed`, since dated testimony keeps every `autoproject96` it wrote. *What is booked and stopped:* the **deep debride** of git history. Measured before deciding -- **199 occurrences tree-wide, 83 dated testimony files, and 11 inside commit messages**. Eleven is the whole of what a history rewrite would reach, and the cost is every downstream re-cloning while ~2,900 commits are re-signed. Two deep debrides already ran on `20260823`. *Why it can wait, plainly:* **GitHub keeps a redirect from the elder organization name**, so nothing is broken today -- both URLs answered at the same commit when checked -- and the elder name is already scrubbed to `acme-owner` in every seed projection, so the public face never carried it. The field is private. **A standfast, awaiting Keaton's word**, rather than a red: nothing here is wrong, and the stopped line is a cost nobody has yet decided to pay.
