# REDS -- the definition rounds, rows 115-116 (folded)

**Language:** EN
**Folded:** `20260821.192038` from [`../REDS.md`](../REDS.md) -- a closed-range archive fold to keep the living pin under `living_pin_max_bytes` (24576). Every byte kept, nothing removed; the living ledger keeps rows 117 onward and points here.
**Style:** Radiant - **Voice:** Kyri
**Span:** the two rounds that turned on a definition -- three tools disagreeing about what a dated path is, and a substitution that would have swapped one hash algorithm for another in silence.

*A red owned in conversation is a memory. A red recorded here is a proof. These rows moved out of the living pin only to keep it readable; they are as binding as the day each was caught.*

---

**REDS %115 (`20260821.171105`) -- three tools disagreed about what a dated path is, and each disagreement cost a round.** *What went wrong:* the fold moves dated files, the resolver recovers references to them, and the census counts them -- and all three carried a different idea of the shape. The fold accepted a day-only name like `20260814-fill-ales28-x.md` and would have moved 33 files in `active-designing` the resolver can never find, since it needs the time to compute a home. The census had never learned the fold rule the resolver already used, so the moment four rooms folded it reported **82 references as ambiguous** that the resolver recovers by computation, and the lost-reference ceiling breached on a fold that had done nothing wrong. And the repointer's regex ended in a greedy character class rather than a known extension, so a reference written mid-sentence as `.../a-doc.md.` matched with the period attached, the key missed the map, and **49 real functional references went silently unrepointed** while the census correctly called them broken. *What caught it:* the ceiling, twice, and then a single reference traced by hand rather than assumed -- `tools/hunk_scroll_cursor_witness.rish` cited a file that plainly existed, so the disagreement had to be in the reader rather than the tree. *What it taught:* **when several tools act on the same kind of thing, the definition of that thing is one artifact, not three habits.** A fold looser than its resolver moves what cannot be found; a census stricter than its resolver reds a correct move; a repointer looser at the tail edits nothing while reporting success. The shape is now `YYYYMMDD-HHMMSS_slug.ext` with the extension anchored, checked the same way in all three, and each tool says in its own comment which of the others it must agree with.

**REDS %116 (`20260821.183454`) -- the fix that was asked for would have rewritten every content address in the tree.** *What went wrong:* Amphora lap 2 had been RED for want of `openssl`, and the repair proposed -- by me, in the round before -- was to teach its export the `sha256sum` already on the pier. That is the wrong fix and it would have been a quiet one. Every call in that path is `openssl dgst -**sha3**-256`, and **SHA3-256 is Keccak while sha256sum is SHA-2** -- different algorithms, not two spellings of one. The substitution would have compiled, run green, and silently changed every content address in every manifest this tree has ever written, and each new digest would have looked exactly as much like a digest as the old one. *What caught it:* reading what openssl was actually being asked to do before substituting for it, rather than pattern-matching on the word `sha256`. The tree's own hash-tier spec names **SHA3-256** for the Working tier in plain text, one grep away. *What it taught:* **a substitution is only safe once you know what the thing being substituted for actually computes.** A hash tool is the sharpest case -- wrong output is indistinguishable from right output by inspection, there is no crash to notice, and the damage lands in exactly the artifacts meant to be immutable. *The repair, on metal:* `crypto/sha3_file.rye` computes the digest over this tree's **own authored Keccak-f[1600]** (`crypto/sha3.rye`, clean-room from FIPS 202) -- the same algorithm, the same digests, no external dependency, and one more place the tree stands on what it built. Five fixtures moved from openssl to it. Amphora laps 1, 2, and 3 are **GREEN**, as are all seven Amphora and Cellar witnesses. *And the oracle question is answered rather than dropped:* the hash-tier spec called openssl the **independent host oracle**, and a digest checked only against the code that produced it proves nothing -- so `tools/sha3_file_witness.rish` checks ours against the **published FIPS 202 known-answer vectors**, which anyone can look up and nobody here can quietly change, and against openssl additionally wherever it is installed, reporting honestly when it is not. One more thing the repair found: the tool first printed a perfect digest through `std.debug.print`, which is stderr, so every `$(...)` around it read the empty string -- and an empty digest is precisely what wrote the short manifest line whose reader then died on an unbound field. The digest goes to stdout now, and the witness proves it is capturable.
