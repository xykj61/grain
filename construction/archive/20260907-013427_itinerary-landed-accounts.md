# Landed accounts -- `20260907.013427`, Copal

*Folded off [`../ITINERARY.md`](../ITINERARY.md)'s live front, which holds what is OPEN and what
waits on Keaton's word. Cited by stamp rather than by number, since the rows were unshared when this
was written and the derived spine's fourth rule makes the stamp the identity until the anointed
spine binds a view (`.claude/rules/derived-spine.md`).*

## The elder pair, closed and shelved

Both rows folded whole to the [`000030` shelf](20260907-000030_itinerary-landed-accounts.md):
**a flake is exactly when the sentence matters most** -- a control wrote its failing behavior to
stderr and five rostered witnesses forwarded stdout alone -- and the resin cellar's catalog named
**3 of its 17** with nothing in the tree reading it.

Two questions travelled with them and travel on. The `chatgpt_mind` control trio prints FAIL to
stderr and has **no `.rish` caller at all**, so it carries no loss today and would carry one the lap
somebody writes it a witness. And this card arrived at **40,959 of 40,960**, which is a fourteen-byte
tax on whichever ship writes next.

## What the elder pair set up

The catalog row was found by the council rota's **Earth** seat -- the row that breathes in, reading
the concrete fact at the door, which was a catalog four fifths short. The next lap's **Fire** seat,
the row that sees, asked the same question of the first thing this ship reads on every lap: its own
seat prompt. It found a line count that had never been true. Same shape, one room further in, and
the account of that lap is on the card above.

## The charter that said how big its lane was

A seat prompt is prepended to `tools/f/fleet_baton.txt` and handed to the agent, so it is the first
thing a ship reads on every lap it will ever run. **Eight live seats, eight prompts, and two carried
tree measurements** -- copal's `10 Rye modules and 3,861 lines` with `16 witnesses`, and patchouli's
`32 Rye modules and 9,175 lines` with `four rostered witnesses`. All six had parted from the tree.

**They had parted in the two different ways a figure can.** The witness pair was **exact** at its
seating: 16 tracked paths named both `amphora` and `witness` at commit `e831f252cb`, and not one
stood on the standing roster -- both true on `20260905.164639`, both false by `20260907`, when that
reading was **17 with 9 rostered**. The line figure was never true at all: `3,861` against the
**3,582** the room's ten `.rye` paths read at that same commit, and **4,058** today. The charter also
opened every lap assigning work that had closed two days earlier.

**The law was already seated one room over.** `ITINERARY.md`'s roster directive reads *Counts come
from the scan, never here*, and Gauge asks every figure for unit, date, and source. A charter carries
none of the three, which is why a figure in one cannot be corrected -- only replaced. That is `%409`
inverted: there, one rule written six times drifted six ways; here, one rule written once never
reached the room beside it.

`tools/fixtures/s/seat_prompt_figure_scan.sh` reads every live prompt, deriving the path exactly as
`fleet-loop.sh` does. **A rate is a law; a total is a measurement** -- `two asserts a function` is
counted under `rates` and excluded, and printed, since an exclusion nobody can see is one nobody can
check. The reading is a floor on purpose: only census nouns are read, because a charter says *read
its top three rows* as an instruction. **figures fell 6 -> 3**, the ratchet opens at 3, and the
ceiling was proven both ways on the **live** tree with the restore checked byte-identical.

**Two reds closed ahead of it.** The card advertised a ten-hex-digit hash resolving in exactly one
clone, so `nib_honesty` read FLOATING_CLAIM on all eight ships -- and a peer's fold removed that
whole block on the rebase, which withdrew the repair and closed the red anyway. And the anointed
spine bound **one number to two stamps**: a row published onto `%530` fifteen minutes after a peer's.
`reds_ledger_monotone` was GREEN throughout, because **a duplicate makes no gap** -- and that is the
guard trusted after each rebase of the send before this one.

**One cost worth naming:** `reds_fold.sh` writes row numbers into the shelf filename, so folding an
unshared row binds a number into a path. When the spine moved again mid-lap, the correction cost a
rename and a recital line where every other citation cost nothing, because it spelled the stamp.

## Folded here for room, whole and by another hand

*This section stood CLOSED on the live front while the card sat at 40,946 of 40,960. Folding a
finished section is what the card asks for when a lap needs room, and every byte is kept exactly as
its own author wrote it. It carried no standing question; the open rows beside it stayed on the card.*

**GRASS -- MY OWN REMAINDER WAS A GREP.** `20260906.222830` CLOSED. The *10 of 244* counted
spellings: **four false, two prose, four live, one that cannot be innocent** -- `radiant_negation`'s baseline lookup answered a failure with
*admit*, so a mode-000 baseline read **51 of 51 admitted, risen=0, verdict=ok, exit 0**: green
while comparing nothing. `instrument_refusal` reads that second spelling now -- a *fallback
value*, where `|| true` leaves emptiness -- and joins continuations, six of its first seven hits a ternary. **1 in 764 scans; both walled at zero, control 11->20.**
