# REDS -- the clock that spoke one dialect (row %250)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260826.075023` from [`../REDS.md`](../REDS.md), **CLOSED**.

One row, and it is the second half of the shell-dialect story. Row %249 found a guard enforcing a
zero it could not see past; this row found the tree's clock reading every stamp it holds as
unparsable, on the one pier where nobody was looking. The repair is three functions in
[`../../tools/fixtures/shell_portable.sh`](../../tools/fixtures/shell_portable.sh) --
`stamp_epoch`, `epoch_stamp`, and `stamp_ahead` -- and the proof is that
`tools/fixtures/one_clock_provenance_scan.sh` read `PROV_OK` twice on GNU and zero under a
BSD-shaped `date` before, and reads two on both after.

Two smaller rows came out of the repair and stay flat on the pin: %257, a tracked symlink that put
one file on the roster twice, and %258, the clock witness standing on no roster at all.

**REDS %250 (`20260826.015353`) -- two more guards read this host's dialect and refused: the clock's provenance fixture and the dated-path census.** *What went wrong:* on the macOS bench `tools/o/one_clock_witness.rish` fails its own PASS fixture (`PROV_FAIL count=1` at a 900 s tolerance) and `tools/d/dated_path_witness.rish` dies before its census completes -- both proven pre-existing at committed HEAD in a clean worktree, untouched by the panchanga molt. *What caught it:* the molt round's wider witness sample, the first full pass these guards ever took on this host. *What it taught:* %233's lesson is a family, not a file -- a guard suite grown on one host's GNU dialect meets BSD userland as a stranger, and the honest reading is a per-host census run once on arrival rather than a surprise mid-round. The repair lap for the family (exec-bit control, clock provenance, dated-path census) is one bench round on this Mac. *Two of the three roots are named, and one is repaired* `20260826.062128`. The **dated-path census** half was the same `xargs -a` family: `dated_classify_seam.sh` lines 35 and 154 and `dated_path_repoint_scan.sh` lines 104 and 113 all moved to `tools/fixtures/shell_portable.sh`, and the twelve-site family now stands at **zero** under a ceiling of zero (%249). The **clock provenance** half has a different root, found by widening the meter rather than by memory: `date -d` is a GNU extension that BSD `date` spells `-v+4H`, and `tools/fixtures/one_clock_provenance_scan.sh` line 27 and `tools/fixtures/one_clock_head_scan.sh` line 45 both parse a stamp with it, as do `tools/o/one_clock_witness.rish` lines 54 and 81. `shell_dialect_scan.sh` counted the family and read **advisory_date_d=9** across the tree. **CLOSED** `20260826.075023`, both halves, proven from both sides on metal. The nine read **seven distinct lines** -- one tracked symlink put `tools/o/one_clock_witness.rish` on the roster twice (%257). Those seven moved to three functions in `tools/fixtures/shell_portable.sh`: `stamp_epoch` (a stamp or a bare day becomes epoch seconds), `epoch_stamp` (its inverse), and `stamp_ahead` (the stamp N seconds from now), each trying GNU's spelling and falling back to BSD's, GNU first because `-r` means an epoch on BSD and a FILE whose mtime to report on GNU. `stamp_to_epoch` stood byte for byte in `one_clock_provenance_scan.sh` and `one_clock_head_scan.sh` and is one call in each now; `ad_walk_sample.sh` read **age=0d** for every artifact under a BSD `date` and reads the true age on both; the four-hours-ahead fixtures compute the shift as arithmetic on `date +%s`, which needs no extension at all. The family is **gated at zero** rather than advisory, because it earned its gate by costing a real reading. Proven on metal: `one_clock_provenance_scan.sh` read `PROV_OK` twice on GNU and **zero** under a BSD-shaped `date` before, and reads two on both after; `tools/o/one_clock_witness.rish` is GREEN under the same shim; and `shell_dialect_scan.sh prove-date` holds all four readings every run, the plant proving it works on GNU before its silence under the shim is allowed to mean anything. *Renumbered:* booked as `%234` on the macOS bench and shifted here, because this pier had already spent `%234` at an earlier stamp -- REDS %230's own rule, the earlier row keeps the number (%252).
