# REDS %209 and %210 -- a refusal that only declined to write, and a third role word

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.211500`, both **CLOSED** -- a row
bound that refused three times while two commits shipped anyway, since the writing step and the
commit were separate commands; and a bin that knew two role words where the tree writes three,
naming a 312-assert sweep target that held 57. Moved here as row %211 carried the living pin past
its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %209 (`20260824.201500`) -- a refusal that only declined to write let two commits ship a log nobody could reach from the front door.** *What went wrong:* the 192-byte index-row bound seated on `20260824.183000` refused three rows in four commits, correctly each time. **Twice the commit shipped anyway.** The row-writing step and the commit were separate commands rather than one chain, so the refusal stopped the write and nothing else, and `session-logs/README.md` -- the page whose own header promises it *holds exactly the logs still flat in this room* -- shipped without a row for a log sitting beside it. Both were repaired within the minute by a hand that happened to read the output. *What caught it:* reading the assertion text in a command's output, twice, and the second time asking why the same shape had arrived again. *What it taught:* **a guard that refuses at the wrong moment is a guard with a door beside it.** The bound was doing its job perfectly; what was missing is that a refusal has to **stop the lap** rather than decline one write and let the rest proceed. The transferable rule is narrower than *chain your commands*: **when a check runs before a write, the caller owns the refusal**, because the check has no way to reach the work that follows it. *Repaired:* [`../tools/l/log_has_a_row_witness.rish`](../../tools/l/log_has_a_row_witness.rish) over [`../tools/fixtures/log_has_a_row_scan.sh`](../../tools/fixtures/log_has_a_row_scan.sh) gates every `.kyri` and `.bron` log sitting flat in `session-logs/` at **zero** unnamed by the pin -- 41 of 41 at seating -- and joins the roster as the **54th** guard, so the fault is caught at the next cold open rather than by whoever happens to read a line of output. A folded log passes free by named rule, since its row moved to its day's immutable shelf when its file did. *Prevention proven both ways:* twelve behaviors on planted rooms in a throwaway pen, including the three a careless check would get wrong -- one missing among three found rather than carried by a majority, a **substring refused** so `_one.kyri` cannot answer for `_one-more.kyri`, and an absent pin refused rather than read as zero logs and called clean. *Owned plainly:* three arrivals in four commits, all mine, all in the round that seated the bound doing the refusing. The bound refused its author every time and the author kept the lap going every time -- which is exactly why the check belongs on the roster rather than in a habit. CLOSED.

**REDS %210 (`20260824.204500`) -- the third conflation in three rounds, and every one of them was found by opening a file rather than by the meter.** *What went wrong:* the sweep target named to Keaton twice as *the single densest thing on the board* -- `pond/apps/drawn_terminal.rye` at **312 uncovered contract asserts** -- holds **57**. **255 of the 312 sit in functions named `run_*_witness`**, which are proofs that run, the same role the bin already granted to `main` and to `selftest`. The bin knew two role words and the tree writes three. *What caught it:* opening the file to sweep it and reading the function names in the assert tally, which is the same move that caught REDS %208 one round earlier. *What it taught:* **a bin keyed on names needs its vocabulary enumerated from the population, rather than extended each time a sweep trips over a missing word.** REDS %207 merged three promises into one denominator; %208 kept a second conflation inside the correction for the first; this is the third, and the shape across all three is one sentence: **each was found by a hand opening a file, and never once by the meter that reported the number.** A meter cannot see a role nobody told it about, so the enumeration is the work and the patch is the symptom. *Repaired:* the selftest bin now reads a **role vocabulary** -- `main`, `selftest`, `witness` -- named as three words in the scan's own header rather than as a list that grows on contact, beside the file-header rule %208 seated. The tree reads **82%**, gap **3,921**. *Landed with it, and it is the round Keaton asked for:* `pond/apps/drawn_terminal.rye` swept **0% -> 100%**, its 57 contract asserts covered by **22 invariant blocks**, since a run of asserts under one block is covered by it. Every line names a reason rather than restating its assertion -- *the status line takes one row of the surface, so a glass of a single row would leave the session none to draw into* above `assert(body_rows > 0)`. Three sites already carried an unlabelled reason in `// why:` or plain prose; the author's meaning was kept and the seated label put on it rather than a second comment stacked above. **Lines wrapped at 96 characters to match the room that already does this well** -- `caravan` writes 8,361 invariant lines at a median of 73 and a maximum of 109, and the first draft of this sweep ran to 212. Proven by `zig ast-check` clean, `tame_style_check` green, and `width-check` green; the module's own build is **display-gated** on this headless pier, which is a machine fact rather than a tree red. **Pond moves 48.5% -> 50.4%**, gap 1,565 -> 1,508. *Named, not fixed:* `// why:` is TAME's own phrasing for the same duty and covers **8** asserts tree-wide against 18,096 for `// invariant:`. Whether the guidance should bless both spellings is a decision, and eight is small enough that it waits. CLOSED.
