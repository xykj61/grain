# REDS -- rows %148-%149, the banner and the unrun bound

*Folded from [`../REDS.md`](../REDS.md) on `20260822.234745`, when rows %150-%151 carried the living pin past its 24,576-byte bound. Every byte kept, nothing removed; the living pin names this file in its own fold line, and the monotone witness counts these rows beside the pin so the spine still reads 1..N with no gaps.*

**Room:** construction -- the ledger's archive
**Voice:** Kyri

---

**REDS %148 (`20260822.231949`) -- three living documents each carried a header banner naming a shelf the file had left, and three consecutive laps found one apiece by hand rather than by any meter.** *What went wrong:* the design-rooms test graduated six documents into `foundations/` on `20260821.211423`, moving the files and leaving their headers behind. `foundations/20260703-200712_what-needs-ordering.md`, `foundations/20260703-201612_the-sealed-crossing.md`, and `foundations/20260703-202312_the-marked-value.md` each opened with `**Register:** active-designing -- siloed; names only our own modules (foundations graduation on Kaeden's word)`, a line that told every reader the file lived in a room it had graduated out of, and that still named the graduation as pending after it had happened. Two more of the same shape -- `single-stranded.md` and `growing-a-language.md` -- were repaired on touch in the two laps before this one, each caught only because a hand happened to be reading that file for the council rota. *What caught it:* a reader, three times running, which is the definition of a lantern rather than a loom. Nothing was broken: the links resolved, the prose read true, and only the one line saying WHERE was wrong, so no existing guard had any reason to look. *What it taught:* a header field that names a shelf is a **claim about the filesystem**, checkable the same way a digest is, and the tree already had the two halves of the check on disk -- the field in the header and the directory in the path -- with nothing standing between them.

**REDS %148 CLOSED (`20260822.231949`) -- the banner and the path are compared by a meter now, and all twenty-nine shelf-naming banners agree.** *The repair, on metal:* [`../tools/fixtures/banner_room_scan.sh`](../../tools/fixtures/banner_room_scan.sh) reads the first thirty lines of every tracked `.md`, takes the first word of any `**Register:**`, `**Room:**`, or `**Shelf:**` line, and refuses when that word is a shelf this tree files on yet is absent from the file's own path. The three drifted lines now read `**Register:** foundations -- siloed; names only our own modules. Graduated from active-designing/ on 20260821.211423 under the design-rooms test, having awaited the word since 20260703`, so the shelf is current and the graduation is recorded rather than still pending. The check is deliberately narrow, and the narrowness is proven rather than asserted: `**Status:**` says what a piece **is** rather than where it lives, so `**Status:** Counsel -- recommendations only` on an archived file passes free, and a Two Rooms register -- `Checkable`, `Mixed`, `Vision`, `Radiant` -- names a voice rather than a room and never enters the count. [`../tools/fixtures/banner_room_control.sh`](../../tools/fixtures/banner_room_control.sh) builds a throwaway repository holding one agreeing banner, one moved banner, one voice, and one Status, and proves all four verdicts including both exemptions. Measured across the whole tree: **29** shelf-naming banners, **29** agreeing, **0** drifted, and no false positive anywhere, so the gate is zero with no slack. GREEN on metal: [`../tools/banner_room_witness.rish`](../../tools/banner_room_witness.rish).

**REDS %149 (`20260822.231949`) -- the living reds pin stood 2,050 bytes over its own bound with the guard that measures it unrun, and a fold that had landed four laps earlier had never been given its doorway line.** *What went wrong:* `tools/fixtures/equinox_e123_living_pin_guard_scan.sh` holds every living pin under **24,576** bytes, and `construction/REDS.md` had reached **26,626** -- over the bound before this lap opened a single byte of it. Rows %144 through %147 accreted across four laps of the ladder arc, each lap booking a row and none running the guard, so the ledger that records what standing equipment forgets had quietly become an instance of it. Beside it, a second miss of the same shape: rows %142-%143 were folded to `construction/archive/REDS-line-citation-and-spelled-number-rows-142-143.md` on `20260822.194931`, the archive file landed and was committed, and the **doorway line pointing at it was never written into the living pin** -- so a reader walking the fold pointers found sixteen ranges named and one range simply absent, with the bytes safe on disk and unreachable from the only page that knows where they went. *What caught it:* running the guard rather than trusting it, at the close of a round whose own subject was a meter for drift, and then reading the fold pointers by hand to place the new one. *What it taught:* **a bound is only a bound on the laps someone runs it**, and this is the second consecutive round to find standing equipment behind the code it guards -- REDS %147 found eight witnesses unrun for three weeks, and this found the bound guard unrun for four laps. The fold itself is also two acts rather than one: moving the bytes, and repointing the page that leads to them. Only the first is visible in a diff that looks finished.

**REDS %149 CLOSED (`20260822.231949`) -- the pin is back under bound, every folded range has a doorway line, and both guards run GREEN on metal.** *The repair, on metal:* rows %144 through %147 folded to [`archive/REDS-keyword-in-the-key-and-the-unranked-queue-rows-144-145.md`](archive/REDS-keyword-in-the-key-and-the-unranked-queue-rows-144-145.md) and [`archive/REDS-the-unpriced-queue-and-eight-drifting-guards-rows-146-147.md`](archive/REDS-the-unpriced-queue-and-eight-drifting-guards-rows-146-147.md), carrying `construction/REDS.md` to **20,331** bytes, which is **4,245** under the 24,576-byte bound with room for several rows before the next fold. The missing doorway line for rows %142-%143 is written into the living pin beside the fold lines that surround it, so all nineteen folded ranges now name where their bytes went. The headline recites **148** rows spanning 1 to 148 -- both numbers were one row behind when this round opened them, and `tools/gen/season/reds_ledger_monotone_witness.rish` refused until they read what the spine measures. GREEN on metal: `tools/gen/season/equinox_e123_living_pin_guard_witness.rish`, `tools/gen/season/reds_ledger_monotone_witness.rish`, and `tools/living_docs_lint.rish`.
