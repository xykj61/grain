# REDS %483 -- the guard that read the last pass

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.125308`, the row CLOSED.*

**What the row taught, in one line:** a guard whose evidence is a file its own pass writes at the
close reads the pass BEFORE it, and the lag runs both ways -- a peer this pass repaired still read
red, and a peer that reddened inside the pass read green.

`standing_equipment` is itself rostered, so it runs from inside the roster pass, and the runner
writes the run card wholesale at that pass's close. The expensive direction is priced: a red here
withholds the receipt `--scoped` needs, so every lap that closed the last red paid the next lap a
full cold pass. The runner hands its live record over now, in `STANDING_CARD_LIVE`, and a live row
outranks the card's row for the same guard -- handed by environment rather than written into the
tree, because a working-tree write between the runner's two digests is exactly what `tree_moved`
catches. Proven end to end in
[`../../tools/fixtures/s/standing_equipment_control.sh`](../../tools/fixtures/s/standing_equipment_control.sh),
whose readings went 161 to 182.

---


**REDS %483 (`20260906.113000`) -- the roster's own guard reads a run card that is written once, at the close of the pass, so a repair landed in this pass is invisible to it and the lap that fixes the last red still closes `guard_red`.** *What went wrong:* `tools/fixtures/s/standing_equipment_scan.sh` counts `runs_red` off the run card, and the runner writes that card **wholesale at the end of a pass** -- proven by the card's own stamps, `oldest_run=20260906.111808` equal to `newest_run=20260906.111808` across all 140 rows. So `standing_equipment`, at position 151 of 172 output lines, reads the **previous** pass's card no matter how late in the roster it sits. Measured in one lap: the cold pass at `20260906.105803` read `index_row_bound red` honestly; the shelf was repaired; the hot pass at `20260906.111808` read **`index_row_bound green` at line 60 and `standing_equipment red` at line 151**, and the same scan run by hand two minutes later read `runs_red=0 verdict=ok`. Nothing changed between those two readings but the card being finished. *What caught it:* a lap that repaired the only real red and still closed `guards_red=1`, and asked which guard rather than accepting the number. *What it taught:* **a guard whose evidence is a file its own pass writes at close is always one pass behind, and the direction of that lag is the expensive one** -- it reports a fault that is already fixed, on the lap that fixed it. `%475` closed the half where the guard counted its OWN verdict (`runs_red_self`, reported never counted); this is the neighbour half, where a peer guard's stale verdict is counted, and `runs_red_self` cannot see it. *The cost is already priced:* a red guard sets `roster_receipt_write=withheld_guard_red`, `--scoped` refuses without a full green receipt, so **every lap that closes a red pays the next lap a full cold pass** -- 900s and 687s measured here. *Not taken, and why:* the runner and the roster are another seat's, and two shapes fit -- write the card incrementally, or move that guard's roster row so it runs after the write. Naming which is that seat's ruling. *Booked `%480` from a `--next` read that was fresh, and a peer published `%480` first; renumbered on the rebase, the stamp holding, which is exactly what `.claude/rules/derived-spine.md` is for.* *Repaired `20260906.125308`, and the shape chosen out of three.* The runner exports `STANDING_CARD_LIVE` naming `$pen/fresh` -- the record it already keeps, seeded with the card rows of the guards a pass leaves alone and appended to as each guard answers -- and `standing_equipment_scan.sh` builds an effective record where a live row overrides the card's row for the same guard, in two `awk` passes rather than a `grep` per card line. A guard scheduled this pass and not yet run keeps its card row, which is genuinely its freshest reading; `runs_live` reports how many rows the pass in flight supplied, and reads 0 by hand. *The two shapes this row named, declined and why.* Writing the card incrementally puts a working-tree write between the runner's two digests, which is exactly what `tree_moved` catches -- invisible in this tree only because `.gitignore:379` names the card, so the repair would rest on a line no guard reads, and would refuse every red in a pen the control drives. Moving the roster row later is impossible without special-casing one guard by name: the card is written after the guard loop ENDS, so no roster position is after the write. *AND THE LAG RAN BOTH WAYS, which nothing had said.* Reproduced on metal in a two-guard pen: with the card carrying `alpha green` and alpha reddening at position 1, `standing_equipment` at position 2 read the stale green and **passed free** -- the one gate reading *no run-card line records a red* answering yes while a red stood three lines above it in the same output. The runner refuses over alpha's own red either way, so the pass lost nothing; the gate was simply blind on that side, and reads `guards_red=2` now. *Proven both directions, the elder reading asserted first in each pair* so the repair is known to have changed something rather than to have passed the same way twice: control **161 -> 182 readings**, 21 new, including the end-to-end case -- a two-guard pen whose second guard IS this scan, closing `run_verdict=ok` where the elder runner closes `guard_red` (measured `20260906.123532`). The control clears `STANDING_CARD_LIVE` on every planted scan, since it runs under the roster runner every lap and a case inheriting the real pass's record would measure the wrong tree in silence. `tier lap` 13s, was 16s. Witness header molted **C+/77 -> B/84** under a cairn, a frame already standing at HEAD. **CLOSED.**
