# REDS %492 -- two algorithms agreeing proves the algorithms, never the input

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.131115`, in the round that
booked and closed it. The pin stood at 39,475 bytes of its 40,960 bound before this row and 42,223
after, so the row that put the page over is the row that folds -- no peer's account was touched to
seat it.*

*Its lesson belongs beside `%451` and `%469`, whose sentence was that an instrument can be green
because it cannot see enough of its subject to disagree. This is the neighbouring shape: an
instrument can be green because its **two** readers see the same wrong subject and agree about it.
A self-check between two readers checks the readers. Only a bind to something outside them checks
the subject -- and the bind has to name the SHAPE, since three size readings held unchanged over a
graph missing an entire tier.*


*Booked `%490` from a `--next` read taken at `20260906.130223`; a peer had published its own `%490`
at `20260906.123531` and folded it the same hour. The earlier stamp keeps the number, so this row
moved to `%491` on that rebase, then to `%492` on the next, a second peer having published `%491` against the earlier stamp `20260906.120905` and the stamp below has not moved -- which is what
[`.claude/rules/derived-spine.md`](../../.claude/rules/derived-spine.md) is for, and the eleventh
firing of the card's own open question about whether a booked row should carry a claim at START.*

**REDS %492 (`20260906.130223`) -- the revocation census defended itself with two independent cut-point algorithms and three sibling-bound counts, and a graph missing an entire tier walked past all four.** *What went wrong:* `tools/fixtures/t/topology_revocation_census.sh` bound its agreement leg to three readings the sibling census publishes -- edges, isolated points, and the worst single cascade -- every one of them a **size**. Plant the point metric so that every planet is sponsored by its galaxy directly, deleting the star tier from the graph outright, and all three hold: edges stay **774**, isolated stays **0**, and max_stranded stays **59**, because the same 59 points are stranded however the tier beneath them is wired. *What caught it:* writing the plant for a new leg and refusing to adjust the expectation when it passed. The first draft of the control asserted that `max_stranded` would bite; the pen answered `verdict=ok`, and the assertion was rewritten to record the **miss** rather than the guess. *What it taught, and it is the sharper half:* **two independent algorithms agreeing proves the algorithms, never the input.** The census's own headline defense is *one algorithm measuring itself proves nothing; two disagreeing means this script is wrong* -- and under this plant the removal sweep and the depth-first lowlink pass both read **12** cut points, agreed with each other exactly, and printed `same_set=yes`. Both were correct about the graph they were handed. A self-check between two readers is a check on the readers; only a bind to something outside them checks the subject. *Repaired:* `degree_max` and the cut count are bound on both legs of the compass sky as well -- `compass_point_degree_max=26` and `compass_point_cut_points=60`, with the elder sponsor leg gaining `15` and `60` beside them -- and the plant now bites twice, degree_max **26 -> 70** and cut points **60 -> 12**. `topology_revocation_control.sh` proves **21 assertions across 6 plants** (was 12 across 4), and plant five asserts the three size binds staying **blind** and the agreement leg staying **quiet** as hard as it asserts the two shape binds biting -- because a shape bind proven only where a size bind also fires cannot be told from decoration. *Disclosed, measured rather than described:* this census builds its graph from the metric's **unit** distance alone, so an error moving no distance-one pair is invisible here -- spelling the cross-galaxy bridge with address depth instead of point depth leaves every number on every leg identical at `verdict=ok`, while the same plant in `topology_graph_census.sh` bites four ways with `metric_differs=158268`. Two censuses, two questions, and the limit now names its owner. **CLOSED.**
