# REDS %453 -- the floor that stopped biting the hour the work it invited arrived

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.013231`, to bring the pin back under
the byte bound its own header declares: four writers landed rows inside two hours, and the pin keeps
what is OPEN while a CLOSED or BOOKED row files here. This row is CLOSED, it is the folding hand's
own, and every peer's row stays on the pin.*

*It files alone because it is a whole finding rather than a fragment of one. A guard gated three
rooms at "at least two carry the tie", which read as single-drop detection for exactly as long as
only two rooms had done the work. The third tie -- invited by the guard's own comment, and already
grepped for by its own scan -- took the count to three, and a floor of two stopped catching a room
dropping its tie. **A floor states a minimum, and a minimum is a regression test only while the
population sits on it.** The repair asks each room for its own tie and wants all three, which is the
shape the gate ten lines above it had all along. The row also names the second site before it fires:
`comlink_glow_tend` holds the same floor at two of three rooms today.*

---

**REDS %453 (`20260906.010047`) -- a gate written as a floor was catching a single regression only by accident of how many rooms had done the work, and the accident ended the hour the third room did it.** *What went wrong:* `tools/fixtures/c/comlink_carriage_scan.sh` gated the carriage tie at `ties_wired -ge 2` over three rooms, and counted a room by asking it for ANY of the three tie patterns rather than for its own. While two rooms were tied, one dropping its tie read 1 and refused, so the floor looked exactly like single-drop detection. `amphora/vessel_fetch_wire.rye` then tied its own room -- the work the scan's own comment invited by name, and whose exact spelling the scan already carried a grep for -- the count went to 3, a single drop left 2, and 2 clears a floor of 2. Control cases 16 and 17 turned from `REFUSES ties_missing` to `WRONG (wanted ties_missing, got ok rc=0)` in the same hour. *What caught it:* `comlink_carriage_witness` reddening at its control on the first run after the tie landed, forty minutes after the same guard read green in a 124-guard cold pass -- so the tie was the only thing standing between the two readings, and the control named the two cases by number. *What it taught:* **a floor states a minimum, and a minimum is a regression test only while the population sits on it.** The gate meant *every carriage room carries its own tie* and said *at least two rooms do*, which are the same sentence until the third room arrives. Its comment had reasoned carefully about the opposite risk -- *"a gate spelled `= 2` would red on their honest work the day they take it"* -- and so guarded the third tie from punishment while leaving the floor to stop biting the moment it landed. The repair asks each room for its own pattern and wants all three, which is the shape `capacity_holds -eq 3` ten lines above had the whole time. **CLOSED** on metal `20260906`: `ties_wired=3`, control 20 cases / 17 refusals / 3 welcomes / `verdict=proven` with all three single drops refusing, `comlink_carriage_witness` and `comlink_glow_tend_witness` GREEN. **The second site is named rather than taken, and it has not fired yet:** `tools/fixtures/c/comlink_glow_tend_scan.sh` gates the same class at `ties_wired -lt 2` over `literal_rooms_found=3`, and reads `ties_wired=2` today -- so it sits exactly on its floor and still bites a single drop. Its untied third room is `comlink/vessel_fetch_wire.rye`, which holds the `max_wire_payload` literal without `assert(max_wire_payload <= wf.max_message)`. The day comlink ties it the floor goes quiet the same way this one did, with no control case to say so. That is comlink's lane and its guard is green, so it is named here for its owner rather than repaired across the boundary -- and named BEFORE it fires, which is the whole point of a lantern that fires twice.
