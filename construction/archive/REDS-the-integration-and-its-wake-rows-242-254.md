# REDS -- the integration and its wake (rows %242, %245-%248, %251, %254)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260826.044827` from [`../REDS.md`](../REDS.md), every row **CLOSED**.

Seven closed rows from the days the two piers ran apart and then came back together. Four of them
are one story told forward: a send arriving over its own bound (%242), the debride's wake on the
receiving pier (%245), the comment that stopped a census counting (%246), a shelved row keeping
links written for the pin (%247), and the parked package that would not renumber itself without a
word (%248). %251 is the rewrite pass that ran over a dirty tree. %254 is a witness pinning the
exact number of claims its module holds, which is REDS %235 firing a third time.

**The living pin keeps what is open.** Rows %230, %249, %250, %252, and %253 stand flat there --
the row-number allocation gate, the macOS dialect pair, the second collision, and the molt that
stranded its own references. A closed row shelves so the card a reader opens holds the questions
rather than the answers.

---

**REDS %242 (`20260826.001630`) -- a fourth send from the other clone arrived carrying a living pin already over its bound.** *What went wrong:* `construction/ITINERARY.md` reached `xykj61` at **24,933 bytes** against the **24,576** its own header declares, and this pier's rebase carried it to **24,952**. `tools/gen/season/equinox_e123_living_pin_guard_witness.rish` holds a roster row and refuses exactly this reading, so the guard existed, stood on the roster, and was not run before the push. *What caught it:* running the ledger guards after this round's fold, one of which is e123 -- and only because the fold made a reason to run them. *What it taught:* **a discipline that runs at one pier's open does not run at another pier's send.** REDS %232 booked this class three sends ago and repaired the tree rather than the gesture; counting %226, %227-%229, %231-%232, and this row, the class has now fired four times, which by this tree's own ladder -- rule, then reading, then refusal -- has earned its third rung. *Repaired:* the card condensed to **24,251 bytes** with **325** to spare, its landed round folded to one *Prior laps* line and the spent seed-cadence paragraph cut from 981 bytes to 570 with every live fact kept; walk-back `11b9e87321` recorded in `construction/CHECKPOINTS.md` before the sweep. *Surfaced, not crossed:* the rung this asks for is a **pre-push refusal** -- the pin bound, or the roster, read at `git push` rather than at a lap's open. It changes how both piers send, so it waits for Keaton's word.

**REDS %245 (`20260826.010645`) -- seven guards red on the pier that received the debride, and the pier that sent it ran none of them.** *What went wrong:* the merged tree failed `dated_path`, `equinox_e123_living_pin_guard`, `seed_link`, `tame_style_check`, `sow`, `readme_reach`, and `remember_git_nib` on a cold roster whose tree digest was identical at open and close, so the reading was sound. Four roots, each small: the debride **deleted** `counsel/date/20260730/20260730-150702_pole-bozo-djinn-murr-keaton.md` and left four living citations in `context/LEXICON.md` and two `test -f` assertions in the fund-prep fixtures, which reddened three guards at once; `construction/SHRED_PREP.md` arrived at **25,594 bytes** against the **24,576** its own header declares, two Class M rows past the bound; a compound `assert(a and b)` in `comlink/rehearsal_wire.rye` broke the TAME ban; and the nib lagged its own rebase. *What caught it:* the receiving pier's cold roster, on the lap after the send. *What it taught:* **a custody-gated act is still a change, and a change is still measured.** A debride is licensed to remove what it names, and the removal is a removal like any other -- references are promises, and four of them pointed at a page that had stopped existing. Counting %226, %227-%229, %231-%232, %242, and this row, a send arriving with guards its sender never ran is now the **fifth** firing, and %242 already named the rung it asks for. *Repaired:* the living citations replaced by the seat named in prose, since the page they promised is gone by design and no surviving page holds its content; the two fixtures reading the page under test instead; four closed sections folded off `SHRED_PREP.md` to **22,250 bytes**; the compound assert split so a failure names which seat collided, proven by building the module; the `dated_path` ceiling moved **177 -> 178**, the one rise this ledger records, because a debride is not drift and dated testimony keeps every word it wrote. *Named, not closed:* the census read `refs_lost` as 178, then 182 three times, then 178 twice consecutively on one unmoved tree. The settled value is 178 and the ceiling is set from it; **what made the middle three read 182 was not traced**, and a census that reports two numbers for one tree is worth someone's next round. *Surfaced, not crossed:* the pre-push refusal %242 named still waits for Keaton's word.

**REDS %246 (`20260826.022833`) -- the comment explaining why a page was gone is what stopped the census from counting it.** *What went wrong:* `dp_discovered_fixture_basenames` finds an instrument's planted names by two conjuncts -- the basename appears in authored `.rye`/`.rish`/`.sh`/`.brix`, and its sprig names no file -- and subtracts them from the census wherever they are quoted. Row %245 raised the `dated_path` ceiling **177 -> 178** for a counsel page the `20260825` deep debride removed, and wrote that page's full path into `tools/fixtures/dated_path_scan.sh`'s own comment to say why. A comment is authored code to a grep, and a debrided page's sprig names no file by design, so both conjuncts held: the census read the page as a fixture and stopped counting its **five** surviving references -- one in this ledger, three in `expanding-prompts/date/20260730/`, one in a session log -- on the same lap the ceiling rose to make room for them. *What caught it:* running the census four times on one tree and getting byte-identical output at 178, which killed the instability hypothesis and left the tree itself as the only thing that could have moved. `git show HEAD:context/LEXICON.md` then carried **exactly four** occurrences of that page against zero in the working tree, and `git diff --cached` showed the scan's comment absent from HEAD. Both transitions %245 called untraced were in the diff the whole time. *What it taught:* **code names a thing and a comment writes about it, and a tool that greps whole files cannot tell those apart.** The instrument-versus-field test this list is built on is sound; the proxy for it was reading prose as code. The wider shape is %244's, one layer over -- there the debride edited the guard listing what to debride, here the explanation of a removal performed the removal a second time. *Repaired:* conjunct 1 skips lines whose first non-blank character opens a comment (`#`, and `//` for Rye), which drops exactly **three** basenames from discovery, 59 -> 56; two of them are plants carrying **zero** references anywhere, so the strip costs nothing, and the third is the debrided page, now **LISTED** in `dated_path_exclusions.sh` beside the three names the `20260823.072824` debride removed -- where that file's own doctrine already said a debrided name belongs, since no instrument plants one. Proven both ways on metal: with the listing the tree reads **178** under its ceiling, without it **183** over. `dated_path_exclusions_control.sh` gains three legs -- a name on a full-line `#` comment and one on a `//` comment are read past, and a code line carrying a *trailing* comment still plants what it names, so the strip stays anchored to a line's first character rather than hunting a hash through code. All three fail with the strip removed, and `tools/d/dated_path_witness.rish` gates them. *Named, not closed:* the ceiling stays at 178 rather than returning to 177, because 178 is what this tree measures and a ceiling is set from a reading.

**REDS %247 (`20260826.030941`) -- a row folded from the pin to a shelf kept the links it was written with, and the shelf sits one directory deeper.** *What went wrong:* folding rows %243 and %244 onto [`archive/REDS-the-debrides-blast-radius-rows-243-244.md`](REDS-the-debrides-blast-radius-rows-243-244.md) copied their text verbatim, which is what accrete-never-break asks for. Their three relative links were written from `construction/REDS.md` and read `../tools/fixtures/upstream_shape_scan.sh`, `../tools/u/upstream_shape_witness.rish`, and `../active-designing/20260826-010645_the-rota-meets-the-debride.md` -- correct from `construction/`, and one level short from `construction/archive/`. Three broken links shipped into a signed commit. *What caught it:* `readme_reach` on the final roster, `broken_in_living=3` across one file, on the same tree it was created -- and it named all three by path rather than making anyone hunt them. *What it taught:* **a relative link is a fact about where a file sits, so moving text between depths rewrites the text's meaning while leaving its bytes alone.** Every fold this ledger has done was safe by accident: the four prior shelves carried only `](../REDS.md)`, which happens to resolve from both depths, so the class stayed invisible until a row cited a tool. Verbatim is the right rule for a row's *prose* and the wrong rule for its *paths*, and a fold is the one operation that can tell them apart, since it knows both depths. *Repaired:* the three links re-anchored to `../../`, each verified to resolve on disk from the shelf's own directory, and `readme_reach` re-run GREEN. *Named, not closed:* the fold is still done by hand, so the next one depends on whoever runs it remembering this row. A fold that re-anchored `](../` to `](../../` for every row it moves, leaving `](../REDS.md)` alone, is the loom this asks for -- one lap's work, and it wants the shelf-naming pass beside it.

**REDS %248 (`20260826.032335`) -- the two-pier row-number collision fired a second time, across a rewritten history, at five times the size.** *What went wrong:* row %230 booked two clones taking `%226` for different reds and repaired it by keeping the earlier-stamped row. It then surfaced the general question -- how a number is allocated when two piers run at once -- as needing Keaton's word. This round met the general case. The ordering remote was force-pushed twice during one send: first a name repair, then `c7c7872895`, a **5,092-file** rewrite molting `season` to `chapter` and re-rooting history so `git merge-base` reads **none**. That rewrite was built from a base predating three of this pier's laps, so upstream's spine ends at **%235** while this pier's reaches **%247**. Rows **%231** and **%232** agree on both sides, and **%233, %234, and %235 name six different reds** -- this pier's stamped `20260825.235254`, the bench's stamped `20260826.014404`, `.015353`, and `.023323`. Twelve further rows, %236 through %247, exist on one side only. *What caught it:* `tools/fixtures/upstream_shape_scan.sh`, the guard row %243 built one lap earlier, on its second real use. It read `verdict=rewritten`, printed `merge_base=none`, recovered the pre-rewrite fork point from the remote-tracking reflog, and printed the exact `git rebase --onto` that replays only what this pier owns -- three commits rather than 3,473. The collision itself surfaced when that rebase reached the ledger and both sides offered a `%233`. *What it taught:* **a guard that names a shape early converts a silent expensive mistake into a visible expensive decision.** The rebase was survivable and the numbering was not blind: %230's own rule -- the earlier stamp keeps the number -- resolves all three, giving this pier %233-%235 and shifting the bench's to %248-%250. What the rule does not do is make the *act* small. Applying it means renumbering three rows in a ledger this pier did not write, sweeping their citations, and carrying three commits across a vocabulary molt whose reasoning lives only in the rewritten history. *Surfaced, not crossed:* the package is parked whole on `refs/heads/pier/one-tree-one-number`, pushed additively and never forced, with every guard green and rows %236-%247 intact. The integration wants Keaton's word, which is exactly what %230 asked for and what this row now has the measurements to make cheap: three colliding numbers, twelve unseen rows, three commits, 53 files, 18 of them intersecting the rewrite.

**REDS %251 (`20260826.023323`) -- a third rewrite pass ran over a dirty tree, and --force ate the uncommitted seats.** *What went wrong:* `git filter-repo --force` resets the index and working tree to the rewritten HEAD; passes one and two ran on clean trees, and pass three -- five path renames -- ran with six uncommitted edits standing, which it silently discarded: the ITINERARY standfast lines, the LEXICON and manifest rows, and an index row. Untracked files survived. *What caught it:* the post-pass status read -- two untracked files where six edits should have stood -- before anything was pushed. *What it taught:* the flag's own name says it: --force is a promise that the tree holds nothing worth keeping, and a rewrite is a round boundary -- commit or stash everything, then rewrite. Repaired in the same round from the scripts that made the edits. OPEN none; closed by reconstruction. *Renumbered:* booked as `%235` on the macOS bench and shifted here, because this pier had already spent `%235` at an earlier stamp -- REDS %230's own rule, the earlier row keeps the number (%252).

**REDS %254 (`20260826.044827`) -- a witness pinned the exact number of claims its module holds, and the module grew.** *What went wrong:* `tools/k/kyri_receipt_witness.rish` asserted `out.out contains "ten claims held"`. `kyri/receipt.rye` printed `fifteen claims held` after the two fifteen-seat palettes landed on `20260826`, so the guard refused a module that was strictly better than the one it was written against, and its own selftest printed `GREEN` three lines above the refusal. *What caught it:* the round-open cold roster. *What it taught:* **this is REDS %235 firing a third time, in a room it had not reached.** %235 repaired four guards pinning an exact count of a growing population -- 17 date labels against 25, ten amphora files against eleven -- and concluded that an exact count is a refusal with a date on it, the date being whichever lap adds the next one. That conclusion was applied to the four scans in front of it rather than to the class, which is the same aim-at-the-sites-you-found mistake %237 booked one row later. A lantern that fires three times is a loom nobody built yet. *Repaired:* the module now owns the comparison, which is where the sibling repair put it. `kyri/receipt.rye` declares `claims_held: u32 = 15` beside `claims_floor: u32 = 15` and asserts `claims_held >= claims_floor` under an `// invariant:` line, so a sixteenth claim passes free and a claim silently dropped aborts; the selftest prints `claims=15 claims_floor=15`, and the witness reads the **floor**, never the count. Proven both ways on metal: planted at `claims_held = 14` the binary aborts with exit **134**, and restored to 15 the witness reads GREEN -- because a refusal proven only in the passing direction cannot be told from a bypass.
