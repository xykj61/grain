# REDS %222 -- a tier was chosen on a cost extrapolated from one sample, and the sample's conditions went unwritten

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.162410`, **CLOSED** -- the ales
choir's tier decided on one cold-cache reading multiplied by 239, six times the truth. Moved here as
row %224 carried the living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %222 (`20260825.132121`) -- a tier was chosen on a cost extrapolated from one sample, and the sample's conditions went unwritten.** *What went wrong:* the ales choir's tier was decided on `5.97s` for one witness times 239 rungs, read as roughly twenty-four minutes. Three clean full sings then measured **233s, 303s, and 233s** -- four to five minutes, six times less. The sample was the session's first Rye build and paid the Zig toolchain's cold start once; that cache lives at `~/.cache/zig`, outside the tree, and a single witness reads **1.27s** warm against **7s** on a scoped cold cache. A second reading in the same round was worse: it returned 28s and a per-rung RED at `ales_compress_witness`, because it overlapped a single-witness run and the choir clears `lotus/bin` and rebuilds it in place, so two sings delete each other's binaries. *What caught it:* running the thing. The first full sing printed `wall_s=233` beside a header claiming twenty-four minutes, and the second printed a RED naming a module that was sound. *What it taught:* **a measurement carries the conditions it was taken under, and a number that omits them cannot be re-checked.** Warm or cold, alone or beside another run, one sample or three -- each belongs in the sentence with the figure. *Repaired:* the figure corrected in five places, and the tier restated on its real reason rather than its wrong one. At four to five minutes this choir is **cheaper** than `caravan_suite` at 8m31s, which stands on the every-lap tier, so cost alone would allow `tier lap`; it stays on `cadence` because the every-lap roster already measures 20m20s to 22m19s read twice a lap, and Season C's Lotus rungs are stable while Caravan is the arc actually moving. The roster header says so now, and says to revisit a row's tier when its family reopens. *Prevention, the structural half:* [`../tools/fixtures/ales_sing_lock.sh`](../../tools/fixtures/ales_sing_lock.sh) -- `sow_project.sh`'s proven shape (REDS %193) in its own file rather than copied (REDS %215): an atomic `mkdir` lock, a refusal rather than a wait, and a lock left by a killed sing cleared by dead pid. Twelve behaviors proven both ways by [`../tools/fixtures/ales_sing_lock_control.sh`](../../tools/fixtures/ales_sing_lock_control.sh), the held case planted against a genuinely live process. *One more, caught by the choir itself:* that control's last check first read the living lock as **absent** rather than **unchanged**, so it went RED inside a perfectly healthy sing -- a guard refusing for a reason unrelated to what it guards, which is the very class this row is about. GREEN on metal. CLOSED.
