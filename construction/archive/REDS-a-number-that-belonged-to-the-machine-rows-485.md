# REDS %485 -- a number that belonged to the machine, written in a file that runs on any machine

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.113552` at the moment it was
written, because it is **CLOSED** on a witness and every other row standing on the pin is OPEN. A
row of this length would have carried the pin past the 40,960 its own header declares, so the shelf
is where it belongs rather than a concession.*

*The story in one line: `amphora/vessel_fetch_delivery.rye` answered a UDP port number compiled into
its own source, one line below two comments in that same file saying such a number is the host's and
never the file's -- and the asker's real address was arriving with every request and being read by
nobody. Two corrections came out of it, and both came from running rather than from reading. The
first repair refused, and its refusal named a second fault nobody had seen: a request that leaves by
a different socket than the answer arrives on asks to be answered somewhere its sender is not. The
second is the control's own: two askers at once, the obvious test, passes on the elder bytes too, so
it discriminates nothing -- what separates* answers the asker *from* answers 38494 *is crossing one
repaired end against one elder end, which nothing in this tree had ever done.*

*What it does not reach is written into the row and repeated here, because a boundary stated once is
a boundary a later reader takes for a claim: **halving a shared resource is not removing it.** The
source's own port is still one number a machine owns and eight trees reach for, so the port lock
stands, and its discovery wants a rendezvous the fixtures do not have yet.*

---

**REDS %485 (`20260906.113014`) -- `amphora/vessel_fetch_delivery.rye` answered a port number written in its own source, one line below two comments in that same file saying a number like this belongs to the host and never to the file.** *What went wrong:* the module held `fetcher_port: u16 = 38494`. Every source sent its answer there; every asker bound that same number. `recvfrom` handed the source the asker's own address on every request -- `var from: sockaddr_in = undefined;` filled by the kernel, read by nothing, and the frame ended. Two consequences, one measured and one structural. The measured one is `tools/fixtures/a/amphora_vessel_port_lock.sh`, a host-path lock taken around every step that speaks the pair, because eight trees on this pier reach for one machine's ports; the structural one is that a source could only ever answer an asker compiled to expect it. **The file states the rule twice and breaks it on the next line:** *THE HOST NUMBERS THESE, NEVER THIS FILE* over `SOL_SOCKET`, *THE KERNEL LAYS THIS OUT, NEVER THIS FILE* over `sockaddr_in`, both taken back from the source after `%282`, and the port sitting between them unread. *What caught it:* the prior lap's own recommend line -- *the next amphora lap is the fixed port pair, which wants a module change rather than a lock* -- written after `%478` bounded the stall the same pair had hidden. Reading two comments and the constant beneath them is what made it a red rather than a preference. *Second fault, found by the first repair refusing:* aiming the answer at the asker made the demo fail immediately with `Incomplete`. `send_wire` opened a **fresh socket per send**, so a request's return address named an ephemeral port the asker was not listening on and closed a moment later -- the source answered honestly and answered nobody. **A datagram carries a return address whether its sender meant it to or not, so which socket a request leaves by is part of the message.** *What it taught:* **a value that names the machine cannot be checked by a guard that only ever sees the two ends agreeing with each other.** The elder pair passed every witness it had, and its own concurrency test passed on both builds in half a second -- two short exchanges stagger by milliseconds and never overlap, so a test both bytes pass proves only that the test ran. What separates *answers the asker* from *answers 38494* is crossing one repaired end with one elder end, and nothing in the tree had ever done that. *Repaired:* `recv_on` hands back the address it was already given; `send_resin_response` answers it; `fetch_one` binds `ephemeral_port` -- zero, named as the ask rather than the port -- and the kernel returns a free one atomically, which no probe-then-bind can do; `bound_port` reads it back with `getsockname` and the asker prints it. `send_from` takes the caller's socket so an exchange speaks and listens through one fd; `fetcher_port`, `send_wire`, `send_to` and `recv_wire` are **gone rather than left uncalled**. Measured: two askers in one demo print two distinct kernel-chosen ports, neither 38494. Control **5 legs**, two plants built by transform rather than by git ref so they survive a history rewrite -- PORT (the written reply number) and SPLIT (the two-socket asker) -- each refusal bitten from the failing side, each planted pair answered so the pen is proven innocent. **The leg that corrected the round:** a repaired source DOES answer a PORT-planted asker, where the first draft expected refusal -- a fixed-port asker that speaks and listens through one socket is still answerable, so the repair is a superset of the elder rather than merely different from it. `amphora_asker_reply` seated `tier cadence`, 68s, because two crossed legs each spend the module's own three-attempts-of-ten-seconds bound and nothing takes exactly as long to prove as a bound says. *What this does not reach:* **the lock stands.** `source_port` is still one number the machine owns, so halving the shared resource is not removing it; the source's own port discovery wants a rendezvous the fixtures do not have yet, and is the booked lap after this one. *Renumbered once:* booked `%480` from a fetch at `20260906.112800`, and the rebase before the send found the anointed spine had spent that number on an unrelated row while this one sat in an unpushed commit. A published number never moves and an unshared one does, so this row took `%485` -- the derived spine's own law taking its ordinary course rather than a repair, and **the stamp held throughout**. **CLOSED** on `tools/am/amphora_asker_reply.rish` GREEN.
