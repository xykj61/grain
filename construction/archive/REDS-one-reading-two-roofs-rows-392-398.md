# REDS %392 and %398 -- one reading, and the second roof that cited it

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.085457`, both **CLOSED** -- a
cause and the cost it carried, kept together because apart they read as two small repairs and
together they teach one thing. `%392` found the report card grading a program at whichever setting
a hand typed, and landed the split dial: a module head reads at Door, a bound comment reads at
Meter, and the program's own grammar chooses. `%398` is what that landing reached. A second guard
CITES the card rather than copying it -- which is the right design, and is exactly why a change to
the card is a change to every reader of it. Its four exclusions stopped covering the lines the card
now emits, and thirteen dated session logs arrived at a guard gated at zero.*

*The pair's lesson, in one line: **one rule in one place makes every change to that rule a change to
everyone holding it**, so the reader that cites an instrument is a reader that reruns when the
instrument moves. `%398`'s own repair says the other half -- the population was wrong rather than
the exclusions, since all thirteen sat in dated testimony and a guard whose only available repair is
forbidden can never go green.*

*Link depth moved with the text.*


**REDS %392 (`20260831.031048`) -- a program carries two prose settings by law, the report card reads it at one, and the same unchanged file grades C, C+, or A depending on which word a hand types.** *What went wrong:* `context/GAUGE_STYLE.md` assigns two settings inside one source file -- **Door** at the head of a module, where a reader arrives holding no context, and **Meter** at a bound or an assert, where the comment names the failure the number was chosen against and negation is the subject rather than a fault. `tools/fixtures/q/qa_report_card.sh` takes a single `--setting` for the whole artifact and feeds it to both counted readings, so a program's blended comment corpus is weighed against one ceiling. Measured this lap on `comlink/virtio_net.rye`, one file, one state, three runs: **C 72** at `door`, **C+ 77** at `field`, **A 94** at `meter` -- a 22-point spread and two letter grades decided by the hand rather than by the artifact. The `meter` reading is the sharpest, since `grade_ceiling=0` and `xref_ceiling=0` make Register and Reach report rather than score, so any module carrying comments reads 100 on both counted halves without the instrument weighing a sentence of it. *What caught it:* the carded molt the live card itself named. Rewriting that module's head as Door prose moved Register **65 -> 73** (35% -> 27%, under Field's 30% ceiling) and left Reach pinned at **60**, grade 15 against 11 -- so the repair the reading asked for could not move the reading that asked for it. Grading the two halves apart said why: the new head measures **8.8** against Door's ceiling of 9, and the 501 words of `// invariant:` comments beneath it measure **16.4** against a ceiling the same law leaves uncapped. The head is already right; the file is graded on the average. *What it taught:* **when a law assigns two settings inside one artifact, a single-setting meter reports the hand's choice rather than the artifact.** Kin to `%276`, where this card threw a program's comments away and graded its code, and to `%358`, where its extractor knew `//` and `#` and not Glow's `::` -- the same instrument, the same class, one layer in. The halves are found now and still weighed as one. The QA rule's own test settles the direction: a meter that instructs a repair which would make the artifact worse is the thing to fix, and Field over this file instructs a writer to simplify the invariant comments TAME asks to be exact. *Named, not taken:* a card that reads a module head at Door and bound comments at Meter and reports two readings rather than averaging one. `%386` is the standing warning against the naive form, where a guard built in a hurry flagged 14 and was wrong about 13; the split here needs a rule for the doc comments between the two poles, since a `///` on a public function is neither a module head nor a bound. Shared machinery every body runs, so it wants its own lap and a control proving both poles from both sides. *Repaired (`20260831.065204`):* the card now derives both poles from comment grammar: the module head scores at Door, `invariant:` lines report at Meter, and declaration docs remain a named third count. A caller's legacy `--setting` word cannot change a program's grade. Seventy-nine control behaviors bite when either pole leaks into the other; `virtio_net.rye` reads **A 95** under `door`, `field`, and `meter`, with its 16 head lines, 10 invariant lines, and 13 declaration-doc lines reported separately. *Measured after, on 134 sources sampled every 37th:* the landed card reads **61 below B** where the elder at `577479ade` read **all 134 A+**; a second build read 60. **CLOSED.**

**REDS %398 (`20260831.083156`) -- a closed repair reached a second guard that cites it, and the roster went red on ten dated logs nobody had touched.** *What went wrong:* `tools/fixtures/c/comment_citation_scan.sh` deliberately holds no rule of its own for what a citation is. It runs `qa_report_card.sh <file> --setting meter` once per program and reads that card's `unresolved:` lines -- cited rather than copied, so the rule that decides what counts as a citation lives in exactly one place, which is the right design and is why this row exists. `%392`'s repair, landed upstream as `2977754b61`, changed which comment lines the card reads: a program is now split into module head, `invariant:` bounds, and declaration docs. The four exclusion classes the citation scan's own header names -- a placeholder shape, a backticked illustration, a target that must look like a path, a symlink's body -- stopped covering the lines the card now emits. The guard gates at zero by design, since its population was 4 broken across 9,101 files when it was first measured, so a roster pass on any body closes `guard_red` until it is repaired. *What caught it:* a hot pass reading `comment_citation red 37s` where the same clone's cold pass, run before its fast-forward, had read it green. Proven on two worktrees of this tree with nothing of the finder's in either: at `577479ade8` the scan reads `programs_scanned=108 broken_citations=0 verdict=ok`; at `51b656a32b` the same 108 programs read `broken_citations=10 verdict=broken_citation`. All ten sit in dated `.kyri` logs from `20260823` to `20260826`, citing `./x.md`, `32000`, `-32768`, `kumara.rye`, `name.rye`, `murr_core.rye`, `archive/NAME`, `../REDS.md`, `../ORGANIZING.md` and `../lotus/LADDER.md` -- which is the exclusion list, item for item, rather than ten new faults. *What it taught:* **citing an instrument rather than copying it puts one rule in one place and makes every change to that rule a change to every reader of it.** The design is still right -- `%199`'s own lesson is that two roofs over one reading cannot disagree only if there is one reading -- and the cost is that a repair proven green against its own witness can turn a second guard red without either witness noticing, because neither one runs the other. *Named, not taken:* the citation scan's four exclusions want to move to whichever card function now emits `unresolved:`, and the card's own control wants a case asserting a placeholder and a backticked span stay unresolved-free under all three settings. **This is `%392`'s lane rather than the finder's**, so the mechanism and both readings are written here and the repair is left to its owner. *Repaired (`20260831.085457`), and the root was the population rather than the exclusions:* `comment_citation_scan.sh` now captures the card's whole output once per candidate and reads its `truth_source=` field, skipping a file the card calls prose and counting it as `prose_skipped` rather than dropping it in silence. The scan keeps no prose list of its own, and its `git grep` extension filter stays a speed prefilter whose every dropped extension a new control case proves the card independently calls prose. It reads `programs_scanned=95 prose_skipped=13 broken_citations=0 verdict=ok`, and 95 + 13 is the same 108. Proven both ways on a real repository: against the unrepaired scan three of the five new readings read `no`, and the identical broken target inside a program still refuses, so the skip turns on a file's CLASS rather than on its content. **All 13 sit in dated testimony, so the guard's only available repair was forbidden -- and a guard that can never go green is a guard reading the wrong population.** **CLOSED.**
