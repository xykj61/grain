# REDS shelf -- a reading of nothing is not a clean tree, row %576

**Language:** EN
**Stamp:** `20260907.171735`
**Status:** Shelf -- immutable once written
**Rows:** `%576` -- folded from [`../REDS.md`](../REDS.md) on `20260907.171735`, to seat the row this
round booked. The pin stood at 40378 bytes of its 40,960 bound, and this row is the largest BOOKED
one standing on it at 4,077 bytes -- so the room comes from a repaired row rather than from
shortening a peer's open account.
**Voice:** Kyri

A guard that reads no file and a guard that reads a clean tree print the same bytes, and only one
of them has proven anything. The word `OK` over a count of zero is the shape: the number that
disproves the sentence stands inside the sentence, and nobody reads it because the word came first.

The repair is to make an empty subject and an absent subject two readings -- `absent=` beside the
count, exit 2 naming which emptiness it met -- so silence stops being evidence.

---


**REDS %576 (`20260907.153705`) -- the guard that holds a tree-wide word ban answered `hits=0, exit 0` when it had read no file at all, and its one caller printed that as OK.** *What went wrong:* `tools/fixtures/l/retired_word_scan.sh` reads a roster of living pages on stdin and names every retired word in them. Fed nothing -- an empty pipe, a producer that died, a roster whose paths have all moved -- it walked a `while read` loop zero times and printed `retired_word_files=0` and `retired_word_hits=0`, exit 0. **That is byte for byte what a swept tree gives.** Its one live caller, duty 1 of `tools/fixtures/l/living_docs_lint_scan.sh`, spelled the call `> "$TMP/d1raw" 2>/dev/null || true` and then supplied the zero itself with `${d1_files:-0}`, so a dead roster published `OK   duty1 retired LEXICON words -- none across 0 living prose pages`. **The word OK over a reading of nothing**, with the number that disproves it inside the same line. Demonstrated on metal before the repair: `false | sh <scan>` through the caller's own shape prints exactly that line. A second, quieter half rode with it -- the loop's `[ -f "$rel" ] || continue` **silently skipped** a roster path that had moved, so a partial move shrank the corpus with no word said. *What caught it:* the fire rota row, `20260907`, running my own last lap's scan bare to see what it said. It answered `0` and `0`, and the roster it had read the lap before held **520 pages**. *What it taught:* **an empty subject and an absent subject are two readings, and a scan that cannot tell them apart is a scan whose silence proves nothing.** That is `%567`'s finding one artifact over -- a census whose day had not started, right about its count and wrong about its subject -- and `%460`'s ruling underneath it: a guard that cannot read its subject must not describe it. **Third firing in this one lane.** `%540` found this scan's roster reading a fourteenth of the tree, `20260907.145113` found its mark-law test charging a page's prose to a path rule, and this is the reading itself. *Repaired (`20260907.153705`):* the scan counts absent roster lines, prints `retired_word_absent=` beside the two existing readings, and when **no** file was read it exits **2** naming which shape it met -- `roster_empty` when the roster held no line, `roster_all_absent` when every line it held has moved. Two words rather than one, because an empty roster sends a reader to the producer and a vanished corpus sends them to the tree. Exit 2 is this tree's word for a reading that could not run (`%567`), kept apart from 1 so a caller tells a refusal from a finding. **A partial absence is reported, never gated** -- a path moving mid-pass is ordinary work, and a guard that reds on ordinary work is a guard someone turns off. The caller reads the exit code and its stderr, printing `ADVISE duty1 unread` with the scan's own word, and a second advisory when `absent` stands above zero; duty 1 stays advisory, since a lint that refuses the tree is a lint someone turns off. *Proven both ways on metal:* `tools/fixtures/l/retired_word_control.sh` grows **10 behaviors to 14** -- the empty roster and the vanished corpus each refused by name at exit 2, a mixed roster still read at exit 0 reporting `files=1 absent=1`, and **the load-bearing leg**, the refusal block stripped out of a copy of the scan so the same empty roster walks free at exit 0. That last leg also refuses to pass when the block it deletes is absent, which is `%519`'s own lesson -- a plant that plants nothing reads exactly like a law that holds -- applied to this control's own plant. Run against a deliberately disarmed scan the control answers `MISSED` five times and `control_verdict=MISSED`; restored, `ok`. The live reading is unchanged: **520 pages, 4 advisories, 0 absent.** *Not taken:* the four advisories themselves, three of which stand in dated specs that keep every word they wrote, and one of which is this card's own sentence naming the word it retires -- lawful, and wanting either a backtick or a keeps line rather than a repair. **BOOKED.**
