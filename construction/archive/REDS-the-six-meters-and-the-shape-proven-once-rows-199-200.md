# REDS %199 and %200 -- five meters where a hand counted three, and a shape proven on one instance of itself

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260824.161948`, both **CLOSED** -- a
constant spelled six ways with the repair's own remainder line naming three of the five that
remained; and an index fold proven on the one room whose table is the last block in its file, which
made appending at the end and keeping in place the same write. Moved here as row %201 carried the
living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %199 (`20260824.140523`) -- the guard that reads the law was one of six meters, and the hand count of the other five said three.** *What went wrong:* `living_pin_max_bytes` is stated once, at `context/specs/20260724-132812_pin-and-ledger-living-pin-max-bytes.md`. Six meters measure against it, and **five of them spelled the number themselves** -- `tools/fixtures/equinox_e123_living_pin_guard_scan.sh`, `index_fold_scan.sh`, `living_docs_lint_scan.sh`, `living_docs_roster_scan.sh`, and `reds_ledger_scan.sh`, **six sites** across the five. REDS %197 seated `declared_ceiling` reading the number from the law rather than copying it, named the remainder in its own *Named, not fixed* line and in the scan's own comment as **"three scripts"**, and left. **The tree held five.** A constant written down six ways is a constant that can quietly disagree with itself: move the law and five meters keep measuring against yesterday, each of them green about the number it happens to hold. *What caught it:* `grep -rn "24576" tools/`, run once at the top of the next lap rather than trusting the previous lap's sentence. *What it taught:* **a count of what is on disk is a measurement, and a measurement written into prose stops being one the moment it is written.** This is REDS %187's shape a fifth time -- a hand-written reach naming what somebody remembered -- arriving now inside a row that had just booked the same lesson, which is the sharpest form it has taken: the lap that names a remainder is the lap best placed to measure it, and naming three where five stood cost nothing at the time and would have cost the next reader the difference. The transferable rule is narrower than "measure": **when a repair names what it is leaving behind, count the remainder with the same tool that found the fault.** *Repaired:* [`../tools/fixtures/living_pin_max_bytes.sh`](../../tools/fixtures/living_pin_max_bytes.sh) is the one reading -- it resolves the law from its own location rather than the caller's working directory, so a scan that later `cd`s into a pen still reads the real tree's law, and it refuses with a named reason rather than defaulting, since a meter whose bound silently defaults reports green over an unmeasured tree. All six sites call it; `declared_ceiling_scan.sh` gave up its inline copy of the reading idiom for the same call. `tools/l/living_docs_lint.rish` duty 6 stopped reciting the number and names the reading instead, and the header block that lap touched swept to plain ASCII (`-`, `<=`, `>=`) under `.claude/rules/ascii-first.md`. *Prevention:* [`../tools/d/declared_ceiling_witness.rish`](../../tools/d/declared_ceiling_witness.rish) gains a fourth reading over **2,046 tracked sources under `tools/`**, discovered with `git ls-files` rather than a glob. A meter that **assigns** the number or **compares** against it as a literal is deciding with a copy -- **gated at zero**. A file that writes it into a comment, an assert string, or planted pen data is reciting it, which is honest where the number is the subject -- **six lines across six files**, under a ceiling that only falls. **A witness asserts and a control plants; a scan decides**, so `*_witness.rish` and `*_control.sh` sit outside the gated reading **by file role rather than by name**, which is what keeps the exemption from becoming the very roster this row is about. Proven by [`../tools/fixtures/declared_ceiling_control.sh`](../../tools/fixtures/declared_ceiling_control.sh) across **twenty-seven behaviors and fifty assertions**, five of them new and built on **real git repositories in a throwaway pen** (`DECLARED_LAW_ROOT`), both refusals shown from both sides. *Owned in the same breath:* the new reading's first run refused the tree at **8 recitations against a ceiling of 6** -- both extra lines written by this very round, in the comment explaining the guard. They came out rather than the ceiling going up, because a guard whose ceiling you raise to pass it is a guard with a door beside it. *And a second, about method:* the opening cold roster was launched in the background and the lap edited the tree while it ran, so `declared_ceiling` was measured **after** its own repair and read the roster's one red. A cold roster taking 29 minutes is cold only if the tree holds still; the hot roster after `git add` is what proves the shipped tree. CLOSED.

**REDS %200 (`20260824.145109`) -- the index fold was proven on the one room whose table is the last thing in its file, so it appended every kept row to the end of the page.** *What went wrong:* `fold_index` in `tools/rye/session_logs_archive.rye` collected the rows it kept into a second buffer and appended them after walking all prose -- a deliberate repair for `session-logs/README.md`, whose title once stood at line 1,881 beneath 1,880 rows (REDS %182), and whose index table is the last block in the file. **Where the table is last, appending at the end and keeping in place are the same write**, so neither the selftest fixture (shaped the same way) nor the room itself could tell them apart. Every other room's README continues past its table. Folding `active-designing` put its two kept rows **below `## Redirect stubs`, below the width-law paragraph, and below the closing line** -- outside any table, which GitHub-Flavored Markdown renders as literal text with pipes in it. **That is REDS %182's own fault, reintroduced by the tool written to fix it.** *What caught it:* reading the front door the fold had just rewritten, before shipping it -- which is exactly why `index_fold_witness.rish` already said *four front doors written in a hurry is how a fix becomes its own red; each gets its own round*. The first round proved the sentence. *What it taught:* **a tool proven on one instance of a shape has been proven on that instance, not on the shape** -- and the tell is a fixture built to match the one subject rather than to differ from it. The fixture's table was last because the room's table was last. *Repaired:* a kept row is now written where it was read, so the table it belongs to keeps it; the fixture README grew a closing line beneath its table, and `SelftestKeptRowLeftItsTable` compares the kept row's offset against that line's. *And two more the repair itself found.* **First:** a fold removes the rows between a table and what follows, leaving a heading abutting the last surviving row, so the consumed blank line is put back -- and keying that off *whether the fold could date the row* inserted a blank **between two table rows**, since a table also holds header rows, delimiter rows, `living` rows carrying no stamp, and day-only rows, all of which this fold reads as prose, correctly, having no day to file them under. A blank inside a table ends the table in GFM: **a worse fault than the cosmetic one it was fixing**, caught by `SelftestBlankSplitTheTable` in the pen before it reached a room. The test is a **leading pipe**, which is what GFM itself reads. **Second:** the shelf header named `../SEASONS.md` unconditionally, and **exactly one room in this tree keeps that page**, so the first fold wrote **21 shelves each carrying one link to a file that was never there** -- and a shelf is immutable once written, so a header naming an absent page is a broken link no later pass may repair in place. The room is asked now (`path_exists`), and a room that grows a roster later gets it in the shelves written from then on. *Prevention:* four new selftest legs in `run_selftest`, **every one proven from both sides on metal** by putting the elder behavior back and watching the named error fire -- `SelftestKeptRowLeftItsTable`, `SelftestBlankSplitTheTable`, `SelftestHeadingAbutsTable`, and `SelftestShelfNamesAbsentRoster` beside `SelftestShelfLostItsPinLink`. *Landed with it:* `active-designing`'s index folded -- **86 rows onto 21 shelves**, the front door **30,748 -> 8,565 bytes**, every shelf link resolving, the room joining `session-logs` on the **ENFORCE** roster at zero stale rows, and the ratchet ceiling falling **317 -> 231** with the control's planted counts moving with it (231 and 232). *Two more the round's own guards caught before it shipped, both worth naming.* `readme_reach` refused this very row: it reproduced the shelf header's link in full, Markdown brackets and all, and **a quoted link is still a link**, resolving from `construction/` to a root-level page that has never existed -- demoted to plain code. And `dated_path` read **188 lost references against a ceiling of 187**, the one new arrival being the selftest's own planted row, which named a log that existed nowhere. **A selftest leaves its sandbox on the filesystem, and `dated_path_scan.sh` reads the filesystem rather than the index**, so a pen reference that resolves nowhere is a real entry in the tree's census. The planted log is written under `date/20260702/` now, so the shelf link it becomes resolves like any other. CLOSED.
