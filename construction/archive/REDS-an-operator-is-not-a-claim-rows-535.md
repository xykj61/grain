# REDS %535 -- an operator is not a claim

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.022810` to make room for the
instrument half of %536, which the same lap landed one row above. The row is BOOKED: its defect
stands repaired in one imported plant law and the remainder is a floor that only rises, 151 of 155
controls yet to adopt. It is folded by its own author, on the lap after it was written, because a
BOOKED row is exactly what door B seated the fold for -- the instances are repaired and what is
left is a ratchet. What it taught is worth carrying past its own family: `cmp -s` asks whether two
files differ, and a control uses that one question for at least three unrelated purposes, so a
meter reading the question and reporting the purpose has invented the purpose. Link depth moved
with the text.*


**REDS %535 (`20260907.004945`) -- the row that booked a plant which plants nothing measured its exposure with an operator, and the operator means three different things.** *What went wrong:* `%519` books a real defect and repairs its Mantra half -- a control's `sed` names a literal line of the module, the line moves, the sed matches nothing, and the phase builds the UNMUTATED module and reads exit 0, which is what a law that holds reads like. To size the remainder it counted the operator the repair happened to use: *102 of 155 tracked controls mutate a pen with `sed`, and 8 carry a `cmp -s`* -- and, reading all eight, *every one of them uses it to prove a plant landed*. Re-read at the commit that row names, `e567cbc128`, **seven** controls carried `cmp -s` and **three of them use it to prove the opposite** -- that a file did NOT change. `reds_ledger_headline_control.sh` proves every line but the headline stands byte-identical; `remember_git_nib_write_control.sh` proves refusal-before-mutation leaves the card intact; `shell_portable_control.sh` proves `sed_inplace` matching nothing leaves a file alone. Reading the word `planted` instead fails from the other side: two of those same three carry it -- once for pen rows the control authored itself, once in header prose. *What caught it:* writing the scan the row implied. The first design counted `cmp -s` presence, and checking its answer against the seven files by hand found `reds_ledger_headline` classified as a healthy control on the strength of an assertion that nothing moved. *What it taught:* **an operator is not a claim.** `cmp -s` asks whether two files differ, and a control uses that question for at least three unrelated purposes: proving a plant landed, proving a refusal mutated nothing, and proving a helper is idempotent. A meter that reads the question and reports the purpose has invented the purpose -- `%526`'s proxy fault one room over, and it would have graded three honest controls as covered while their real exposure stayed unmeasured. The exposure population is not decidable from control text at all: `sed` appears in 134 of 154 controls and only some are plants. *Repaired:* one implementation the fixtures import -- `tools/fixtures/p/plant.sh`, `plant_landed` / `plant_write` / `plant_apply`, each refusing by the word `plant_matched_nothing` and telling it apart from `plant_source_absent`, `plant_program_empty` and `plant_program_failed`. The last is a fault no local copy caught: a program `sed` itself rejects leaves a partial file that DIFFERS from the source, so a bare byte comparison calls that plant landed. `plant_apply` also writes through the original inode, keeping a tracked exec bit where the hand-rolled copies wrote a temporary. Proven on metal by `tools/fixtures/p/plant_control.sh`, **30 behaviors**, each function from both sides, the load-bearing leg stripping the byte comparison out of a copy and asserting the copy reads a no-op plant as LANDED. Adopted first in the three Mantra controls that met the fault, all three witnesses GREEN. The meter is **adoption, counted by what a control imports** -- exact where the text reading is a proxy -- as a **floor that only rises**, the mirror of this tree's usual ratchet, because the population counted is the good one. Rostered `tier lap`. *Standing:* 151 controls have not adopted; `sh tools/fixtures/p/plant_adoption_scan.sh --list` names them, and each falls on touch. **BOOKED.**
