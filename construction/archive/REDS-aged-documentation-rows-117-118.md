# REDS -- the aged-documentation rounds, rows 117-118 (folded)

**Language:** EN
**Folded:** `20260821.193339` from [`../REDS.md`](../REDS.md) -- a closed-range archive fold to keep the living pin under `living_pin_max_bytes` (24576). Every byte kept, nothing removed; the living ledger keeps rows 119 onward and points here.
**Style:** Radiant - **Voice:** Kyri
**Span:** the two rounds that read the manual against the tree and found the warmest pages aged worst -- a welcome telling a newcomer the tools were already in their clone, and the same false claim in three more guides.

*A red owned in conversation is a memory. A red recorded here is a proof. These rows moved out of the living pin only to keep it readable; they are as binding as the day each was caught.*

---

**REDS %117 (`20260821.190149`) -- the welcome told a newcomer the tools were already in their clone.** *What went wrong:* `manual/20260810-065116_your-first-hour-with-grain.md` -- the long, warm first-hour page, whose own Status line promised *"every command below runs today"* -- said at Step 2 that Grain's two tools *"already live inside your clone"* and *"came with the copy."* Neither is true. `git ls-files rye/bin rishi/bin` returns **zero**: a compiler and a shell are things this tree builds rather than ships, and the toolchain they build through is a 172 MB binary deliberately kept out of git. So the load-bearing step of the page most likely to be a stranger's first contact was false, on the one point a stranger cannot check for themselves. *What caught it:* filling the crush-empty docs-geode rooms, and finding a **rival first-hour page** already standing in `manual/` -- which is its own small finding, since I had written a second one the round before without looking for the first. Reading the elder to see whether it duplicated mine is what surfaced the false claim. *What it taught:* **a doc that predates a capability describes a tree that no longer exists, and the warmest pages age worst** -- they are written once, loved, and rarely re-run, while the terse ones get exercised. Its Status line was not lying when it was written; the tree moved and the page did not. *The repair:* Step 2 now names the three commands that put the toolchain, the compiler, and the shell in place -- which exist only because `sh tools/f/fetch-toolchain.sh` landed hours earlier. The two first-hour pages now **point at each other** rather than each pretending to be alone: the manual is the welcome, `docs-geode/tutorials/the-first-hour.md` is the path.

**REDS %118 (`20260821.191312`) -- the same false claim in three more guides, and the loom that ends it.** *What went wrong:* reading the five sibling guides written the same day as REDS %117's page found the fault repeated and widened. `manual/20260810-065116_running-grain-on-your-machines.md` said it **three times**, most plainly as *the shipped `rye` and `rishi` binaries are built for exactly this architecture*, and offered a clone-then-run-a-witness block with no build between them. `manual/20260810-065116_sandboxing-and-getting-set-up.md` invited a reader to *watch this hold for yourself* and then wrote a bare `rye build ...`, which no reader can run -- `rye` lives at `rye/bin/rye` in the clone, never on a PATH. And `manual/20260810-065116_the-developer-guide.md` illustrated the run-record with `vault/bin/vault`, **a binary nothing in this tree builds**. *What caught it:* running the corrected commands rather than trusting the correction -- which is how the developer guide's SECOND fault surfaced, and it was the more interesting one. With the path fixed to the real `vault/bin/shard`, the example still failed: it checks `ok.out` for the green line, and a Rye program prints through `std.debug.print`, so the answer arrives on `ok.err` and `out` is empty. The illustration teaching the run-record was reading the wrong field of the run-record. *What it taught:* **the warmest pages age worst.** They are written once, loved, and rarely re-run, while the terse ones get exercised daily. Two of the five guides were clean; the three with the most inviting prose carried every defect. *The loom, since a lantern that fires twice earns one:* `tools/s/shipped_binary_claim_witness.rish` measures the premise each run -- `git ls-files rye/bin rishi/bin` -- and refuses any living document asserting otherwise while that count is zero. It stands down if the tree ever does ship binaries, because a guard that cannot notice its own premise expiring will one day be wrong with confidence. Its own first run reddened on three pages **quoting** the false claim in order to explain it was false, so a line citing a `REDS %` row or wearing the emphasis-quote form is exempt: a guard that makes the record of a fault unwritable has erased its own reason.
