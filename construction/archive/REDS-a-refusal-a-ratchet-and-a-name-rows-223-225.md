# REDS %223-%225 -- a refusal, a ratchet, and a name the wall did not know

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.183336`, all three **CLOSED** --
the cold roster's refusal to open over an unclosed lap, the reach ratchet moving onto the property
it stands for, and the seed wall learning the fund's short name. Moved here as rows %226 through
%228 carried the living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %223 (`20260825.144025`) -- the reading that opened line one of the lap did not stop the lap after it from ending at `git add`.** *What went wrong:* the round of `20260825.132121` staged **19 paths** and stopped before `git commit`. The next lap opened the roster cold on `staged_uncommitted=19` and read **61 guards, 58 green, 3 red**. `readme_metrics` and `geode_libraries` compare a generated page against a fresh render and [`../tools/hooks/pre-commit`](../../tools/hooks/pre-commit) regenerates both at commit time, so both stood stale -- the front door still advertising 1,690 witnesses over a tree holding 1,692. `nib_honesty` read `git_nib=absent` and `verdict=NO_NIB_FIELD`, because the card carries the literal `NIB_HERE` until the round's own hash exists and only the commit makes one. *What caught it:* the cold roster, twice -- on line one with the staged count, and eleven guards later with the consequence. That line-one reading is the prevention [REDS %220](REDS-a-close-that-costs-twenty-five-minutes-rows-220.md) added for exactly this failure, **eleven hours earlier**. *What it taught:* **a reading persuades and a refusal decides**, so the ladder a recurring red climbs is rule, then reading, then **refusal**. [REDS %188](REDS-staging-and-the-page-that-named-half-rows-188-189.md) wrote the rule and concluded no guard could ENFORCE the close, which still holds -- such a guard would have to run after a lap ends. %220 answered with the reading. The class fired on the very next lap, which makes the reading a right diagnosis seated one rung too low: what a guard genuinely can do is refuse to **open** the next lap over the wreckage. *Repaired:* the 19 staged paths shipped as one signed commit `a31664d7b6`, `pre-commit` regenerating and staging both pages inside it; the Git nib filled by a single amend; all three guards GREEN on metal and pushed to both remotes before this round began. *Prevention, the structural half:* [`../tools/fixtures/standing_equipment_run.sh`](../../tools/fixtures/standing_equipment_run.sh) takes `--hot`, and a full-roster pass meeting a dirty index without it exits 1 under `run_verdict=lap_unclosed` -- ahead of the tree digest and ahead of the first guard, since nothing measured across that tree answers the question the lap has. `--hot` is how a round says the staged paths are its own, which is the after-`git add` pass %174 asks for; a guard asked for by name is no lap open and runs free. One flag and one structural distinction rather than a roster of exemptions, because a second exemption would be the hiding place this refusal exists to close. Proven from both sides by [`../tools/fixtures/standing_equipment_control.sh`](../../tools/fixtures/standing_equipment_control.sh) inside a real git repository it creates and throws away: a clean cold open runs its guard and passes, one staged path refuses the same pass, no guard line and no digest print above the refusal, `--hot` passes that same tree, a named guard runs free over it, and `--hot --all` still selects every tier. **Nine behaviors added, forty-three in all, ten refusals bitten.** GREEN on metal. CLOSED.

**REDS %224 (`20260825.162410`) -- the ratchet that stands for "a guard nobody runs guards nothing" can be lowered without running a guard.** *What went wrong:* [`../tools/fixtures/witness_reach_scan.sh`](../../tools/fixtures/witness_reach_scan.sh) gates `unheard`, the count of witnesses no file names in an invocation position. `sung`, its complement, means *named by any runner on disk, roster or choir* -- and whether anything runs that runner is a separate question the reading leaves open. So the ceiling falls whenever a choir is **written**, rather than whenever one is **rostered**: list a hundred witnesses in a new choir, leave it off `construction/standing-equipment.kyri`, and `unheard` drops by a hundred while precisely the same guards run as the day before. Measured on this pier `20260825.162410`: **265** witnesses stand named by a runner that no roster row reaches, so the gated number read **937** while **1,202** of 1,692 were carried by no clock. *What caught it:* comparing the meter's own two readings against each other -- the transitive closure it already walks from the roster, set beside the flat `sung` list it already prints. The 265 are the difference, and one of them reads plainly: `tools/b/buhr_mantra_browser_view_witness.rish` is named by two files, both of which the roster reaches by no path. *What it taught:* **a ratchet must gate the property it stands for, rather than a proxy that can improve while the property stands still.** REDS %219 had already written the sentence -- an unheard choir is a refusal nobody receives -- and the meter built to close it carried the same blind spot one level up. Its own control even plants the case and proves it, as `choir_only`: sung yes, standing no. The reading was there and the gate stood somewhere else, which is the whole of the lesson. *Repaired:* the scan prints five bands rather than four -- `reached` (490), `unclocked` (265), `unheard` (937), and the gated `unreached` (1202), which is `total` minus `standing` minus `cadence` -- and `WITNESS_REACH_CEILING` now holds **`unreached`** at 1202 with no slack. The new gate is strictly stronger: every unheard witness is also unreached, so nothing formerly caught now passes, and the number falls only when a roster row lands. `--families` follows the gate onto the unreached set, and `--unreached` and `--unclocked` list either band. *Prevention, the structural half:* [`../tools/fixtures/witness_reach_control.sh`](../../tools/fixtures/witness_reach_control.sh) proves the promotion on a planted git repository -- a witness sung only by an off-roster choir reads `unclocked`, then reads `standing` once that choir takes a roster row, with `unreached` falling by exactly the two files that moved -- plus the band partition as an arithmetic identity and the new ceiling from three positions. **Eight behaviors added, twenty-nine in all.** GREEN on metal. CLOSED.

**REDS %225 (`20260825.180329`) -- the identity wall spelled the fund's long name and the short name walked through beside it.** *What went wrong:* three sleeve memos placed at `20260825.171910`, `.171916`, and `.171922` were marked *withheld by construction* by the counsel that wrote them, reasoning that naming the fund would hold them out of the seed. The wall's IDENT regex carried the two-word and fused spellings and never the bare one, and the scrub rewrites the two-word form -- so the projector scrubbed the long name, read the file clean, and shipped all three, the bare name surviving six times in one body and in three basenames. The same gap stood already published: six earlier-shipped files carried the bare name, one in its own basename, across prior seed pushes. *What caught it:* Lap 8's own instruction -- confirm the withheld line rather than assuming it -- on the first macOS projection; the grep came back empty. *What it taught:* **an identity is every spelling it answers to, and the wall must carry the shortest one** -- a scrub that knows a name's long form and a wall that stops at it will pass the short form together. *Repaired:* a `*siya*` basename guard in [`../tools/fixtures/sow_project.sh`](../../tools/fixtures/sow_project.sh) withholds the four siya-named files into `.sow-withheld.log` exactly as the counsel predicted; the bare name joined IDENT in both the projector and [`../tools/fixtures/sow_leak_scan.sh`](../../tools/fixtures/sow_leak_scan.sh); [`../tools/fixtures/sow_scrub.sed`](../../tools/fixtures/sow_scrub.sed) gained two line-drops for references to withheld siya-named files and a closing capital-form rewrite, with the post-scrub check withholding what a rewrite leaves (two dated waymark logs took that door). Reprojected on metal: copied 7,024 -> 7,015, withheld 143 -> 152, the four basenames confirmed in the log, a tree-wide sweep of the seed for the bare name reading **zero files**, and IDENT_CLEAN - NO_PERSONAL - seed-link all GREEN under the stronger wall. The published copy heals on the next authorized seed push, which replaces the single projected commit whole. CLOSED.
