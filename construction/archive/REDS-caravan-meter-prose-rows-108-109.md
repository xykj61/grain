# REDS -- the Caravan meter-prose arc, rows 108-109

**Language:** EN
**Stamp:** folded `20260821.073606` from the living pin -- **Voice:** Kyri
**Status:** Closed-range fold -- every byte kept, nothing removed
**Parent:** [`../REDS.md`](../REDS.md)

*Rows 108 and 109 in full, exactly as the living ledger held them. Both were booked and closed on `20260821`, and both belong to one family: a meter's own prose drifting away from the measurement standing beside it, and the control built to catch that drift disarming itself by reaching for a moving target. Row 110, booked the same day, records the third turn of the same wheel -- the guard those two produced was bounded by a word rather than by a property -- and stands in the living pin above.*

---

**REDS %108 (`20260821.053811`) -- a meter's closing sentence recited a module count its own measurement contradicted, and drifted twice unheard.** *What went wrong:* `tools/ca/caravan_ladder_spine_witness.rish` closed every GREEN run saying its spine stood *"19 lines in the harness with 0 carried across 102 modules"* -- while the scan it had asserted three lines earlier printed **104**, and then **105** once this lap's rung landed. The assert bound the SCAN OUTPUT (`SPINE_MODULES 104 holding=1`) and left the closing sentence entirely unguarded, so a lap could move the pin, watch the witness go GREEN, and ship a summary line naming a number that had been wrong for at least two rungs. The sibling carry meter carried the same unguarded shape, updated only because the module count happens to sit inside its headline sentence. This is REDS %105 one shelf over: **a guard that measures a fact does not thereby guard the prose that repeats it** -- and the third time that family has fired (%77, %80, %105). *What caught it:* registering `caravan/ipc_buffer.rye` moved the ladder from 104 modules to 105, which reddened three meters at once; bumping their pins meant reading each closing line beside the freshly measured scan output, and the spine's line disagreed with both the old pin and the new one. Two numbers for one thing, in one witness, three lines apart. *What it taught:* **a meter's own subject is guarded and its context is not.** Carried lines, spine lines, printing lines each sit in an assert beside the prose that repeats them, so moving one forces the other on the same lap. The module count is nobody's subject; it is context, and context numbers are precisely the ones no lap updates. Closed at the root the same way %105 closed: **the fix is not a better number, it is no number.** Both meters now say *"anywhere on the ladder"* and *"standing on it"*, and the measurement reaches the reader through each meter's `living ladder -- ${living.out}` line, which cannot drift because it is the scan's own output. The lantern became a loom: [`tools/fixtures/caravan_ladder_prose_count_scan.sh`](../tools/fixtures/caravan_ladder_prose_count_scan.sh) refuses any `say` line in a `tools/caravan_ladder_*_witness.rish` carrying a `<number> modules` phrase, reports dated doc-comment measurements as an advisory count rather than rewriting testimony, and runs every lap from `caravan_ladder_copy_witness` beside the harness-prose rule. **The RED path is the line that actually shipped:** the guard is handed the spine witness exactly as it stood at HEAD this morning and names it by file and line number, rather than a shape invented to be caught.

**REDS %108 CLOSED (`20260821.053811`) -- the number is gone from the prose, and the guard names the line that shipped.** *The repair, on metal:* both ladder meters now close on *"anywhere on the ladder"* and *"standing on it"* rather than on a module count, and each still prints its scan's own output verbatim, so a reader gets the measurement without a sentence able to drift away from it. `tools/fixtures/caravan_ladder_prose_count_scan.sh` refuses any `say` line in a ladder meter carrying a `<number> modules` phrase, and runs every lap from `caravan_ladder_copy_witness` beside the harness-prose rule. *What the RED path is worth:* it is not a planted shape, it is the tree as it stood at HEAD this morning -- the guard is handed `git show HEAD:tools/ca/caravan_ladder_spine_witness.rish` and names the drifted sentence by file and line. *One thing the closure found on its way out:* the REDS spine witness carried the identical fault, closing GREEN on *"the spine accretes 1..107"* while its own assert had just moved to 108. Repaired the same way -- it now says `1..N` and points at the `rows=` line it already prints. Two witnesses, one habit, one fix: **say the number once, where it is measured.**

**REDS %109 (`20260821.060758`) -- a RED path pinned to `HEAD` disarmed itself the instant its own fix landed.** *What went wrong:* REDS %108's closure built the right loom and then armed it against a moving target. `tools/ca/caravan_ladder_copy_witness.rish` reached for its strongest control with `git show HEAD:tools/ca/caravan_ladder_spine_witness.rish` -- the drifted spine meter "exactly as it stood this morning" -- and asserted the guard refuses it. That reading was true while the round was still in the working tree, since `HEAD` then named the commit *before* the repair. The repair and the guard rode in the **same commit**, so the moment `40bd80623d` landed, `HEAD` named the **repaired** file, the guard welcomed it, and the assert flipped. The control proved itself on exactly one lap and refused every lap after. Nobody saw it, because a witness whose RED path has gone stale still exits non-zero -- it simply exits non-zero for the opposite reason, and the message it prints (*"the drifted spine prose must refuse"*) reads like a fresh regression rather than like a control that has lost its subject. *What caught it:* registering `caravan/untyped.rye` moved the ladder 105 modules to 106, reddening the meters exactly as the lap before -- and this time the copy meter reddened too, on a line that had nothing to do with the module count. Reading it showed the assert was fine and the *input* had moved. *What it taught:* **a control must be immutable, or it is not a control.** REDS %102 named the shape of a guard reading a hand-maintained set that stops existing; this is its twin -- a guard reading a set that quietly becomes *something else*. `HEAD` is the most moving target in the tree, and a RED path is the one place where the subject must never move, because the whole point is that it stays wrong. Closed at the root: the elder meter is pinned as a **tracked, frozen copy** at [`tools/fixtures/caravan_ladder_prose_count_elder/`](../tools/fixtures/caravan_ladder_prose_count_elder/) -- byte for byte as it shipped at `45e5303292`, line 106 carrying the sentence that genuinely drifted, with a README beside it forbidding its own repair. The copy cannot follow `HEAD`, and it survives a deep debride that would rewrite every hash in the tree, so the RED path stays armed through both. GREEN on metal: the guard names the frozen file by path and line number, and the copy meter runs GREEN with the control refusing as it must.

**REDS %109 CLOSED (`20260821.060758`) -- the control is frozen, and a control that can be repaired was never one.** *The repair, on metal:* `tools/fixtures/caravan_ladder_prose_count_elder/caravan_ladder_spine_witness.rish` holds the drifted meter exactly as it shipped, and `caravan_ladder_copy_witness` hands the scan that directory rather than a `git show`. The guard refuses it by path and line, the meter is GREEN, and the whole Caravan choir sings GREEN at 103 around it. *The habit this seats:* a RED path names a **frozen artifact under version control**, never a revision pointer -- not `HEAD`, not a branch, and not a commit hash either, since a sanctioned deep debride rewrites every hash the tree has. The tracked byte is the only subject that outlives both the fix and the history. *One thing worth keeping in view:* this is the second time in two laps that adding one module to the ladder has surfaced a fault nobody was looking for. A meter that reds when the tree grows is doing precisely what it exists to do -- the growth is the probe.
