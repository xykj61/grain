# REDS %474 -- a fold done by hand loses the depth the loom would have kept

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.090928` at the moment it was
written, for the reason the `%472`-`%473` shelf gives one lap earlier: every other row standing on
that pin is **OPEN**, and the pin holds 325 bytes of slack against a row of this size. This row is
**BOOKED** -- its sixteen instances stand repaired and the remainder is one booked lap -- so the
shelf is where it belongs.*

*The clause is about two folds that do the same thing and only one of which is a program. When
`tools/fixtures/r/reds_fold.sh` moves a REDS row onto a shelf it re-anchors the row's links for
the directory the row is landing in, and `reds_fold_witness.rish` proves it, printing
`shelf_links_one_level_short=0` on every run. The live front of `construction/ITINERARY.md` folds
the same way and by hand, onto the same `construction/archive/`, and nothing re-anchors anything.*

**REDS %474 (`20260906.090928`) -- text folded off the operator card kept the card's own link depth and landed one directory deeper, so sixteen links across five shelves pointed at nothing, and every standing guard read green.** *What went wrong:* `construction/ITINERARY.md` sits in `construction/`, so a link it writes as `archive/X` or `../Y` is correct from there. A hand folding that text onto `construction/archive/` moves it one directory down without touching the link, and both forms break -- `archive/X` needs the bare `X` and `../Y` needs `../../Y`. Measured `20260906.090312` over the eight `*_itinerary-landed-accounts.md` shelves standing at that stamp: **five carried the fault, sixteen links between them**, the worst holding ten. *What caught it:* the QA report card, run on a shelf this lap had just written -- `truth_counted=80 (1 of 2 cited paths unresolved)` -- and then run across the shelf's siblings because one instance of a mechanical fault is rarely one. *Why no guard saw it:* three read that room and none reads this. `tracked_link` gates a link that **resolves here and breaks in a fresh clone**, which is a link pointing at an *untracked* file -- these point at nothing at all, in either tree, so they are outside its subject. `reds_fold` re-anchors and proves `shelf_links_one_level_short=0`, and reads **only the shelves its own loom writes**. `readme_reach` walks front doors. A fold shelf written by hand stands in the gap between all three. *What it taught:* **a fold that a program performs keeps a property the identical fold performed by a hand loses, and the property is invisible from both ends** -- the card reads correct before the fold and the shelf reads plausible after it, since `archive/X` is exactly what the elder line said. *Repaired:* all sixteen, bounded to links that fail from the shelf's own directory **and resolve under exactly one** of the two corrections a card-to-shelf fold can need, so no target was guessed; five shelves now read `unresolved: 0`. *Booked, not taken:* the loom -- either `reds_fold.sh`'s re-anchoring generalized to the live-front fold, or a guard reading every `construction/archive/` shelf's links against its own directory. This lap repaired the instances and named the class rather than opening a second keystone. *And it fired on its own author, in the same lap.* Folding the `%467`-`%468` account off the card to make room for this very row carried `archive/REDS-presence-where-the-claim-was-content-rows-467-468.md` down one directory unchanged, and the report card caught it one command later -- **eighteen instances now, across three consecutive hand-folds, the last two written by the hand that had just described the mechanism.** Three folds in one lap, three losses: a property a hand cannot hold while actively thinking about it is a property that wants a program. **BOOKED.**
