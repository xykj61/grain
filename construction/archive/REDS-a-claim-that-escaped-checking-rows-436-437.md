# REDS %436-%437 -- a claim that escaped checking

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.195223` to bring it back under
bound as `%438` landed. What runs through both rows is one fault in two materials: a claim that
reads true and is never asked to prove it. `%436` is a peer's finding published as history because
its mechanism sounded right; `%437` is a fabricated citation that walked past the guard written for
exactly it, because a hard wrap split the path into two tokens and neither half looked like a path.
One escaped a reader and the other escaped a machine, and both were checkable in a single command.
They belong beside `%438`, which stayed on the pin and says the same thing a third time -- there
the unchecked claims were the diagnosing seat's own, twice in one round, one day after `%436` was
booked. Link depth moved with the text.*


**REDS %436 (`20260905.174944`) -- a peer's self-report was published as a verified fact.** *What went wrong:* the Petrichor seat handed back a finding -- that `--next` had answered it **436** while the anointed spine's highest was **434**, and that it had renumbered six citation sites the wrong way. That report was written into a live comment in `tools/fixtures/r/reds_spine_derive_scan.sh` and into commit `316b25d7b4`'s body as established history, **without being checked**. It is false: `%435` stands on the anointed spine, folded to `construction/archive/REDS-the-page-that-became-its-own-ancestor-rows-435.md`, so the maximum was 435 and **436 was correct**. The allocator's skip never fired. *What caught it:* the Petrichor seat itself, on its next lap, reading the commit that named it and measuring the spine including shelves -- *%435 is on the anointed spine, so the maximum is 435 and %436 is correct*. The peer corrected the record its own report had bent. *What it taught:* **a peer's finding is testimony, and testimony is checked before it is published.** The same round verified petrichor's OTHER handback -- a stale `sed -i` claim -- and found it already cured, then took the second report on trust because it named a mechanism that sounded right. A finding that explains itself well is the easiest kind to publish unverified, and the mechanism being plausible is exactly what removes the urge to measure. The sibling law already existed one room over: `measurement beats memory`, and a peer's memory is still memory. *Repaired (`20260905.174944`):* the comment now states the refusal as a **precaution against a hazard that has not happened**, names the elder draft's error, and cites the shelf that disproves it. The refusal itself stands -- the skip genuinely cannot tell an in-flight row from a settled one, and a permanent number should not be allocated from a half-replayed tree. Commit `316b25d7b4` keeps its words; history is where a superseded claim belongs, and this row is the erratum. **CLOSED.**

**REDS %437 (`20260905.183603`) -- a line break walked a fabricated citation past the guard written for it.** *What went wrong:* a commit body cited the round's handoff as `expanding-prompts/20260905-183418_the-handoff-from-the-eight-core-round.md`. The file's real stamp is **183107**; 183418 was typed from memory. Rule four of `tools/hooks/commit-msg` exists for exactly this (REDS %202) and welcomed it, because the citation was **hard-wrapped across two lines**: the hook splits the body on spaces and its pattern needs a slash and an extension in ONE token, so it saw `expanding-prompts/` with no extension and `20260905-183418_...md` with no slash, and neither is a path. *What caught it:* Keaton asking whether the prompt was sent. Verifying meant `ls` on the path as written, which found nothing -- the check the hook could not perform. *What it taught:* **a guard that reads a line at a time can be defeated by where a sentence wraps.** Nothing was careless: the body was formatted to the tree's own margin, and the formatting is what hid the fault. A rule enforced per line inherits the line's arbitrary boundaries, and any writer who wraps at 78 columns will eventually split a token the rule needed whole. *Repaired (`20260905.183603`):* the hook now also resolves a **bare basename of the one-clock shape** -- `YYYYMMDD-HHMMSS[_sprig].ext` -- against `git ls-files`, since the naming law makes such a basename globally unique in this tree, so a body naming one is citing a file whether or not its directory survived the wrap. Proven on the exact citation that escaped: wrapped and fabricated it refuses, wrapped and real it passes. Three legs added to `tools/fixtures/c/commit_message_guard_scan.sh`, pinned count 25 to 28. *What it does not reach:* a path wrapped mid-directory with no dated basename anywhere in it. That shape stays invisible, and saying so is cheaper than implying the hole is closed. **CLOSED.**
