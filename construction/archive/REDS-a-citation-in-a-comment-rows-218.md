# REDS %218 -- a fold repointed the paths it could see, and a citation in a comment was not one of them

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.092953`, **CLOSED** -- four
relative Markdown citations inside program doc comments, naming paths that have never existed.
Moved here as row %219 carried the living pin past its 24,576-byte bound. Link depth moved with the
text.*

---

**REDS %218 (`20260825.052000`) -- a fold repointed the paths it could see, and a citation in a comment was not one of them.** *What went wrong:* four relative Markdown citations inside program doc comments named paths that have never existed. `tools/rye/session_logs_archive.rye` moved one directory deeper in the `tools/` fold of `20260823.144100` and kept the depth its citations were written at, so `](../ORGANIZING.md)` and `](../.claude/rules/stamp-and-name.md)` pointed at `tools/ORGANIZING.md` and `tools/.claude/`. Two more broke when a design note folded into `active-designing/yonder/`, in `mantra/recall_tablecloth_query_wire.rye` and `mantra/recall_tablecloth_query_delivery.rye` -- the second of which is reached by a third door, since `comlink/recall_tablecloth_query_wire.rye` is a symlink onto it. *What caught it:* the report card's Truth reading, on the lap it was taught that **a program cites in its comments** -- a meter built to grade prose, looking at something else entirely. Nothing standing could have found it: `tools/d/dated_path_repoint.rish` and `tools/t/tool_path_repoint.rish` move path literals in living code, and `tools/fixtures/tracked_link_scan.sh` reads links inside `.md`. A Markdown link inside a `.rye` comment falls between all three, and stood two days. *What it taught:* **a citation is a promise wherever it is written, and a fold repoints only the paths it knows how to see.** Three tools ran the day of that fold and not one of them read a comment. *The near-miss, which is the larger half of the lesson:* the first measurement read **15 broken across 11 files**, and eleven of those were `pond/apps/**` entries at mode `120000` -- symlinks onto bodies at `pond/`, `granary/`, `linengrow/` and `mandi/` whose citations resolve **exactly right at the body**. Repairing them would have written through the links and broken six correct files at their real homes. Reading the data before acting on it, which is REDS %213's whole lesson, is what stopped it; the card now resolves a link and reads the citation from where it was written, naming that it did. *Repaired:* all four repointed. [`../../tools/fixtures/comment_citation_scan.sh`](../../tools/fixtures/comment_citation_scan.sh) over [`../../tools/co/comment_citation_witness.rish`](../../tools/co/comment_citation_witness.rish) is seated on the standing roster and **gated at zero** -- 89 programs scanned, 0 broken -- reading through the report card rather than spelling the rule again beside it. [`../../tools/fixtures/comment_citation_control.sh`](../../tools/fixtures/comment_citation_control.sh) proves fourteen behaviors on real git repositories in a throwaway pen: a broken citation bitten, named and counted, and each of five blessed shapes left free -- a placeholder, a backticked span, index-then-value arithmetic, link syntax in emitted code, and a symlinked door. GREEN on metal. CLOSED.
