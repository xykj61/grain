# REDS -- the map's rule reached 95 of 98 (row %185)

*Folded from [`../REDS.md`](../REDS.md) on `20260824.084007`, **CLOSED**, as row %189 carried the living pin past its 24,576-byte bound.*

**REDS %185 (`20260824.071500`) -- the map's rule reached 95 of 98, and a rule goes short more quietly than a list.** *What went wrong:* `mycelium/README.md` names 31 base modules across nine families and publishes a suffix table -- `_bron`, `_true`, `_knot`, `_fixture_gen`, `_bench` -- so a companion is reachable by rule rather than by row, which is why the page stays a map over 98 modules. Three stood outside both: the seam symlinks `parse_int.rye` and `tally_copy.rye` into `../tally/`, and `puddle_fleet_fixture_gen.rye`, whose name promises a base module nobody wrote. The same page read **46% negative sentences against a 20% Door ceiling** -- the exact reading that founded Gauge Style -- while standing off the DOOR roster, so no meter had ever read it, and it carried **87 non-ASCII characters** from being seated `20260813`, three days before ASCII-first. *What caught it:* writing the guard REDS %184 asked for one directory over, and adding the page to `tools/fixtures/prose_register_scan.sh` to see what it read. *What it taught:* **a rule-shaped roster goes short more quietly than a list-shaped one.** A short list looks short. A rule looks complete, because a reader sees the rule rather than the set it covers, and a name wearing no seated suffix is invisible to the map and the rule at once. And beside it: **a page joins the meter on the lap it is written, or it is measured by nobody** -- Caravan's README joined the DOOR roster one lap ago and reads 5%; this one had never been on it and read 46%. *Repaired:* the three modules named on the page; six habit-negations turned affirmative-first with every claim held exactly (**46% -> 13%**); the page swept to ASCII; and `mycelium/README.md` seated on the DOOR roster. *Prevention* seated as the **41st** standing guard: [`../tools/m/mycelium_map_roster_witness.rish`](../../tools/m/mycelium_map_roster_witness.rish) over [`../tools/fixtures/mycelium_map_roster_scan.sh`](../../tools/fixtures/mycelium_map_roster_scan.sh), gating unreachable, phantom, duplicate and mismatched at zero and **reading the suffix rule from the README itself** rather than reciting a copy that can drift from it. Proven both ways by [`../tools/fixtures/mycelium_map_roster_control.sh`](../../tools/fixtures/mycelium_map_roster_control.sh) -- fifteen behaviors on real directories in a throwaway pen, eight bitten and seven free, two of them load-bearing: a suffix dropped from the page leaves its companions unreachable, and a two-suffix name over an unnamed middle refuses to chain. It bit my own edit on the lap it entered, when a second `kumara.rye` link read as a duplicate entry. CLOSED.
