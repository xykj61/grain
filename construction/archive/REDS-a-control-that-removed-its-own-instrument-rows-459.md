# REDS %459 -- the control that removed the instrument it was testing for

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.053000`, to bring the pin back
under the byte bound its own header declares after four ships booked rows inside one hour. This row
is **CLOSED**; every OPEN row stays on the pin.*

*It files alone because it is whole. Read it beside `%460` and `%463`, which arrived the same night:
one guard could not run its instrument and made a confident claim about a binary anyway, one census
read an empty file and answered zero of everything, and this control removed the very tool it was
testing for and then measured what was left. **Three ways for an instrument to be certain about
nothing**, and the only defence any of them had was a pen that plants the failing case.*

---


**REDS %459 (`20260906.040301`) -- two guards refused a page for obeying a law raised four hours earlier, and the refusal was killing a third scan mid-run.** *What went wrong:* `tools/fixtures/l/living_pin_max_bytes.sh` answers the seated bound **per page**, and `living_pin_max_bytes[construction/REDS.md]` was raised to **40,960** on `20260906.022430`. Thirteen calls reach that fixture across eight files -- **six bare** because they print the GENERAL number and **seven passing the page they weigh** -- and `tools/fixtures/r/reds_ledger_scan.sh:125` was the one bare call **weighing a page**, so it measured a 33,200-byte ledger against **24,576** and answered `verdict=past_bound`. One argument closes it. Beside it, `tools/fixtures/l/living_pin_near_bound_control.sh` failed **3 of its 9** pen cases for a different reason wearing the same clothes: its plants were **spelled as absolutes**, so `construction/REDS.md` at 24,000 bytes was 97.6% of that page's bound the day the case was written and 58.6% of it overnight, and a case labelled *near* planted a page nowhere near. *What caught it:* this lap's opening cold roster -- **125 guards, 119 green, 3 red, 846s** -- naming `equinox_e123_living_pin_guard` and `living_pin_near_bound`, both `tier lap`, so both red on all eight ships and both withheld `roster_receipt_write`. *What it taught:* **a pen that plants an absolute is testing a snapshot of the law; a pen that plants a ratio is testing the law.** The pen's refusal was correct and its report was not -- three cases named symptoms (`seated_pin_named=no`, `over_bound_named=no`, `near_silent_when_hung_off_over=yes`) and none said the plants had gone stale. **And the class is invisible to the guard built for it:** `declared_ceiling_scan.sh`'s fourth reading gates a meter that *decides with a copy* of the bound and exempts `*_control.sh` by file role, because *a control plants* (`%199`). Every size that broke here -- 24,000, 25,616, 24,100, 52,000 -- **spells no bound at all**, so a literal search finds the three sites that never moved and misses the four that did. **A plant sized from the bound is a decision wearing planting's clothes.** *And the cost reached past its own guard:* `equinox_e113_fascia_health_scan.sh` calls the ledger scan at line 148 under `set -eu`, so the refusal killed it mid-run -- measured both ways on metal, exit **1** against the HEAD scan and **0** against the repaired one -- while its output had already printed a `verdict=ok` line from an earlier section, so a reader skimming the tail saw a pass over a scan that never finished. *Repaired:* the ledger scan passes `"$f"`, so a pen ledger at a temporary path still gets the general answer; all three tracked controls that sized a plant against the bound now read it through the one fixture (`living_pin_near_bound_control.sh`, `living_docs_roster_control.sh`, `declared_ceiling_control.sh`), and **zero non-comment lines in any tracked control spell a stated bound**, measured after. The near-bound pen gains **case 0**, `plants_agree_with_law`, which asserts the planted sizes still land in the bands their labels name and prints first -- proven to bite by spelling `REDS_NEAR=24000` back in, where it answers `no` and names the cause in one line the elder pen took three symptoms to imply. `declared_ceiling_control.sh` case 22 keeps reading the law directly on purpose: **a pen that plants reads through the fixture; a pen that verifies the fixture reads the law.** GREEN on metal: `equinox_e123_living_pin_guard`, `living_pin_near_bound` (10 cases), `living_docs_roster` (16), `declared_ceiling` (57), `reds_ledger`, `reds_ledger_negative`, `reds_ledger_monotone`, `reds_pin_capacity`, `index_fold`, `tame_style_check`, `width_check`. **CLOSED.**
