# Landed accounts -- folded from the live front `20260906.133500`

*Held word for word from [`../ITINERARY.md`](../ITINERARY.md), so the card carries what is OPEN and
what waits on Keaton's word. PETRICHOR's landed half, folded in the round that closed `%496`. The
block's links are re-anchored one level deeper here (`%474`): `archive/X` became `X`, since this
shelf already stands inside `construction/archive/`.*

---

**PETRICHOR -- THE CARD THAT GRADES THIS TREE'S PROSE SEES ONE LINK IN SEVEN.** `%496` CLOSED,
whole on its [shelf](REDS-one-link-in-seven-rows-496.md). Reach computes Flesch-Kincaid and
link density in ONE awk, which holds out the four line shapes carrying no sentence -- tables, lists,
headings, and lines opening `**Bold:**`, read as a bullet by its `[-*>#]` rule. Right for sentences;
**the link count rode along and went out with them**, on the one threshold in 740 lines carrying no
comment. Over **5,409** tracked Markdown files: **2,690** seen, **15,528** held out -- **14.8%** --
and **1,766** files carry links while printing `0 links`. The card's own worked example reads 5 of
28; `wiki/README.md`, a 14-row table of links, reads 2 of 26. **Caught by two readings of one file
disagreeing about a countable fact**, Truth's 2 against Reach's 0. *Landed:* `reach_links=<seen> of
<total>`, **reported never scored**, read off disk rather than `$prose_path` -- the filtered copy,
which would reproduce the blindness -- proven identical on five files, **no grade moved**. The
control plants the SAME TEN LINKS FOUR WAYS where it had only planted running prose, the one shape
the reading can see. **Held back on purpose:** the density, since widening the numerator re-grades
5,409 files in one step and that file refuses this shape by name. **Yours, and it is your own
standing question one room over:** a ceiling with no floor scores a page that leads nowhere at 100.
*Erratum:* the witness recited **117** behaviors against **128**; it counts the pen now.
**The three sangha pattern pages lead somewhere at last** -- witness-basis citations in backticks,
unclickable and unread by `tracked_link`, so a reader walked in and never out. Links now, and a
**Shelf** line home; the wiki's honest *nothing yet* became what each page leads to. **The shipping
shelf is plain ASCII**, 20 characters swept from three pages, guarded by nothing.
**Surfaced, not mine:** `sow_allow_reach` is `tier lap` and reds on any tree with no `seed/`
projection -- this tree's one red at the cold open. Its precondition is a gitignored artifact built
where the seed is published, so it reds on every ship that has never projected; projecting here
greens my tree and hides the shape on seven others. Its refusal leg already parameterizes the seed
path -- a pen projection is the door.

---

*Open, and on the card in its own words: whether Reach should carry a floor as well as a ceiling,
and the `sow_allow_reach` precondition that reds on every ship but the one that publishes.*
