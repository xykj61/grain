# REDS %482 -- a proof nobody runs

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.104500`, in the round that
booked it. `%449` found a module nobody **compiled** while four rostered guards read it with grep;
this is a proof nobody **runs** -- one substitution's second face, which is when a lantern is owed a
loom. `src/gate/README.md` ended its Equality Gates table with "the equality arc stands closed
(8/8)", and a grep across `tools/` and `construction/` for each of the eight basenames returned zero
callers for seven of them. **A sentence is not a runner.** Sharper, and it belongs to a live
question: the QA report card grades that page's Truth at **100 on twelve resolving paths**, because
Truth counts whether a cited path RESOLVES and all eight witnesses are present -- so a page whose
behavioral claim rests entirely on unrun proofs reads **B+/88 with a perfect Truth**. That is
`%467`'s sentence one room over: presence is the cheaper question wearing the expensive one's answer.*

*This row was booked as `%473`, and its number moved five times. Its lap's whole work was parked in a stash at a round open, and while it sat there the anointed spine spent `%472` and `%473`, then `%474`-`%476`, then `%477`-`%479`. **Its two companions were both landed independently by peers while it waited**: the roster guard counting its own verdict as evidence arrived as [`%475`](REDS-a-guard-that-reads-its-own-verdict-rows-475.md) at `20260906.093338`, and the dead-letter-box reading `%464` asked for arrived as [`%479`](REDS-the-instrument-that-was-in-the-box-rows-479.md) at `20260906.110500` -- **same diagnosis, same repair, same morning, from trees that had them while this seat's copies sat parked.** Both are withdrawn here rather than shelved twice; the peers' rows are the record. **Three findings, two of them written twice, and the instrument for that exact waste was one of the two.** The stamp below is the key and has not moved.*


**REDS %482 (`20260906.095100`) -- a page declared the equality arc closed on eight witnesses, and no runner in the tree named seven of them.** *What went wrong:* `src/gate/README.md` ended its Equality Gates table with *the equality arc stands closed (8/8)*. Measured `20260906`: a grep across `tools/` and `construction/` for each of the eight basenames returns **zero callers for seven**, the eighth's only hit being that sentence; **none of the eight** stands on `construction/standing-equipment.kyri`; and `tools/g/glow_tend_a1_suite.rish`, the suite the same page names one section down, is itself unrostered and mentions none of them. A sentence is not a runner. *What caught it:* reading back this ship's own dead-lap stash (`%464`'s shape), whose `recommend` line named the four Mantra witnesses as the next lane door. *What it taught:* `%449` was a proof nobody **compiled** and this is a proof nobody **runs** -- one substitution's second face, which is when a lantern is owed a loom. Sharper, and it belongs to a live question: **the QA report card grades this page's Truth at 100 on twelve resolving paths**, because Truth counts whether a cited path RESOLVES and all eight witnesses are present -- so a page whose behavioral claim rests entirely on unrun proofs reads **B+/88 with a perfect Truth**. That is `%467`'s own sentence one room over: presence is the cheaper question wearing the expensive one's answer. *Repaired:* all four Mantra witnesses build and print GREEN on metal (20s, 19s, 16s, 14s; 69s together), so they were **unheard rather than rotted** -- the state a proof is in just before it rots with nobody learning of it. `tools/m/mantra_a1_equality_witness.rish` builds each against the desk it reads and asserts its claim **and its just-over side**, which is the side a `gth` ceiling cannot answer and therefore the whole reason these gates say `eq`; `tools/fixtures/m/mantra_a1_equality_control.sh` breaks the arc six ways in a pen -- `clean_exit=0`, `eq_to_gth` `constant_moved` `desk_missing` `neighbor_broken` each **134**, `witness_uncompilable` **1**. Rostered `tier cadence`, **92s**, since eight ships reading it twice a lap would spend the saving it makes. The page now names which four are heard. And every module in `mantra/src/` was walked with the comptime declaration walker -- proven to bite by planting a fault -- so all five compile: the room is sound and the fault was hearing. *Remainder, a booked lap in their own lanes:* the Aurora three and the Caravan one. **BOOKED.**
