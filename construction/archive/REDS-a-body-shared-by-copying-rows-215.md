# REDS %215 -- two of twenty-two shared bodies were copies

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260825.030641`, **CLOSED** -- a body
shared by copying across two module roots, drifted apart by the very sweep whose purpose was making
its reasons better. Moved here as rows %216 and %217 carried the living pin past its 24,576-byte
bound. The row carries no relative links, so its text moved unchanged.*

---

**REDS %215 (`20260825.003210`) -- two of twenty-two shared bodies were copies, and this session drifted all three of them apart.** *What went wrong:* twenty-two modules reach Tally's `copy_disjoint` through a file named `tally_copy.rye`. **Twenty are symlinks to `tally/copy.rye`. Two -- `caravan/` and `mand/` -- were regular files at mode `100644`,** and both had drifted from the source. *Whose fault, named plainly:* **this session's own invariant sweep wrote the same three reasons three times, in three commits, in three wordings** -- `tally/copy.rye` in `0d7a18d378`, `caravan/tally_copy.rye` in `5b5ba9ab0e`, `mand/tally_copy.rye` in `e910719c54`. Where the source now reads *"a target shorter than the source would be written past its end,"* caravan's read *"lengths agree exactly"*; mand's carried the same sentence wrapped one word differently. **The drift landed entirely in comments, so every check that reads code called the three files identical.** *What caught it:* opening `caravan/tally_copy.rye` during a lap that cited it, because two living documents -- the `caravan/LADDER.md` row and `caravan/region.rye`'s own header -- both called it *the symlink*, and it was not one. A doc that describes behavior the tree no longer has is a stale claim wearing documentation's clothes, and here two docs held the truth while the file held the lie. *What it taught:* **a shared body that is shared by copying is shared until someone edits it, and the edit that breaks it will be the one improving it.** The sweep that widened this drift was the sweep whose whole purpose was making reasons better -- so the failure mode is not carelessness, it is *care applied N times*. And because it landed in comments, the tree's own uniformity guards could not see it: `caravan_ladder_carry_scan.sh` counts carried lines within `caravan/`, and nothing counts a body carried *across* module roots. *Repaired:* both are symlinks now -- `../tally/copy.rye`, mode `120000`, all twenty-two carrying one blob hash. Proven by the five importer witnesses on metal: `caravan_channels`, `caravan_regions`, `caravan_caps_a1_gate_bound`, `mand_ring2`, `mand_ring3`, every one GREEN. **The two documents needed no edit -- the fix made them true.** *Left open, measured rather than swept:* the same lap found **three `Region` bodies** across `caravan/region.rye`, `tally/seed.rye`, and `tally/gardens.rye`, differing by exactly one assert and one method, which a symlink cannot fold because it cannot share a portion of a file. CLOSED for the copy; the Region reading is named on the itinerary.
