# REDS -- a correction inherits the duty of the claim it corrects

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri -- **Room:** checkable
**Folded:** `20260908.234354`
**Row:** `%658` (`20260908.234354`) -- BOOKED, born onto this shelf. The spine answered
`next_free=654` after `git fetch xy` at this lap's open, and this lap's commit never reached `main`:
it landed on a diverged pier branch, a peer published `%654` for `20260908.225856` while it sat
there, and the row renumbered to `%655` on the recovery of `20260909.011200`. The key is the stamp;
the number is a view (`derived-spine`).

**Recovery:** The parked row is rebound to `%658` against the fetched shared spine. Its immutable stamp remains `20260908.234354`; the former shelf path routes here so dated logs keep their references.

The row is born folded rather than landed and folded, for the reason its neighbors record on the
shelves beside it: `construction/REDS.md` stood at 40,787 bytes against a bound of 40,960 when this
row was written -- **173 bytes free against a row of 3,396** -- with its remaining rows OPEN and so
unfoldable by law. A pin whose living parts fill it cannot accept a new one.

What the row is for, said once here so a reader arriving from a citation knows before they read.
Three files under `glow/gen/s/` are data tables rather than Glow desks, and the runner that runs
every desk in the room refuses all three. A first row recorded them as refusing one way. A second
row corrected that from metal to two ways, and wrote the correction into a living guard's own
comment. Running them again answers **three** ways -- and the second row's grouping put two files
on the same side of a line the compiler draws between them, because it grouped by a word rather
than by the exit code beside it. The repair keeps the exit code the runner was discarding, so the
count of what failed now says at which stage each one failed, and the guard asserts that split
rather than reciting a sentence about it.

**REDS %658 (`20260908.234354`) -- a correction that caught one refusal read as three made the same error at two, and grouped the three by a word the door says not to read.** *What went wrong:* `%532` recorded three unmarked data fixtures under `glow/gen/s/` as failing one way, `unsupported Glow head`. `%564` corrected that from metal to **two** ways -- *two at `TooManyLines` and one at `unsupported Glow head`* -- and that correction is written into a living, rostered witness's own invariant comment. Run on metal `20260908.234354`, the three refuse in **three** ways across **both** of glow_run's exit codes, and the elder grouping crosses the line rather than following it: `sample-digraph-table` exit **2** `unsupported Glow head`, `sample-demo-fixture-lits` exit **2** `too many Glow lines`, `sample-demo-fact-line-lits` exit **1** `multi lower failed (TooManyLines)`. `glow/glow_run.rye` draws that line in its contract -- **2 declines the file, 1 means a lowering was attempted and failed inside** -- so the two called one refusal sit on opposite sides of *declined* versus *broke*, and the one set apart shares a side with one of them. *What caught it:* the water rota's own instruction, run the actual thing up close: three `glow_run_worker.sh` invocations, reading exit codes rather than the sentence about them. *What it taught:* **a correction is a claim, and inherits every duty the claim it corrects had.** The sharper half is how the elder grouping was made -- by matching the word `TooManyLines` -- when `tools/fixtures/g/glow_desk_run_scan.sh`'s own door already says *nothing here reads prose out of the compiler, because a refusal's wording is the compiler's to change*. The door held the rule and the correction beside it broke it, because a hand reading two messages saw the same word in both; **an exit code is a contract where a message is prose**, and the scan was throwing the contract away one line later. `run_desk` collapsed four stages -- declined, lowering broke, build broke, binary exited nonzero -- into one `return 1`, so four faults wanting four repairs read identically and `failed=3` could never say which. *Repaired (`20260908.234354`):* `run_desk` keeps glow_run's exit status and returns a stage code; the scan reports `failed_declined`, `failed_lower`, `failed_build`, `failed_run`, `failed_unclassified` beside the gated `failed`, which is unchanged, so the guard refuses exactly what it refused before at higher resolution. `tools/fixtures/g/glow_desk_run_control.sh` proves **95** behaviors, up from 48: every stage planted through the `GLOW_DESK_RUN_ONE` stub and lifted back to zero, each stage shown to leave the other four at zero -- a split that always answered one would otherwise pass -- and the five parts proven to sum to the total on a pen carrying three faults at once. The witness asserts the split from the scan rather than reciting it, so a fourth fixture landing tomorrow moves the reading that names its own fault. *Not closed, and unchanged by this row:* what declares a desk's kind stays a language custody ruling for Keaton (`%532`). What this row adds to it is the ruling's **size**: two of the three are declined by a bound the language already states -- `max_lines = 6`, `max_bare_multi = 4` -- and one, `sample-demo-fact-line-lits`, glow_run accepts as a desk outright. Only that one is unspoken-for by anything. **BOOKED.**
