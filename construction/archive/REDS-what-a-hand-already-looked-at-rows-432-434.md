# REDS -- rows %432 to %434, folded from the living pin

**Folded:** `20260905.155141` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Three rows on one subject: a repair that reached every instance a hand had looked at, and stopped
there.* Three copies of a wrapper were fixed while the script that stamps them kept producing the
elder shape. A generator's friendly skip read as success, so a run that made nothing reported like
a run that made something. And a law citing four failed forecasts stood beside five more in a
living pin, unseen because a forecast written into a name reads exactly like a fact.

In each, the looking was careful and the looking was the limit. What closed all three was building
something that looks without being asked -- a factory repaired at its source, a report that says
what it did rather than what it attempted, and a meter that finds announcements nobody remembered
making.

The rows stand here exactly as they were written.


**REDS %432 (`20260905.153929`) -- three copies were repaired and the factory kept stamping the elder shape.** *What went wrong:* `tools/g/generate_jail_local_keys_linux.rish` writes each tree's `gpg.sh`, and it wrote `export GNUPGHOME=<this repo's absolute path>` -- the exact hardcoded form whose copy-propagation caused REDS %427. That morning the three copies on disk were fixed by hand; the script that produces them was left untouched, so the next run in any tree would have written the elder shape back. *What caught it:* reading the tool before recommending it. The ask was to generate fresh keys per ship, which meant opening the generator, and its own lines 167-169 were the answer sitting in plain sight. *What it taught:* **repairing every instance is not repairing the defect when something in the tree manufactures instances.** The `%427` round swept three trees, proved a guard over them, seated a law, and shipped -- and every one of those steps was about copies. A guard that reads the fleet's *state* passes cleanly while the *factory* keeps producing what the guard forbids, because the guard runs after the fact and the factory runs before it. *Repaired (`20260905.153929`):* the generator writes the `$0`-resolving wrapper, proven by building the exact string through the script's own `dq`/`nl` fixtures, copying the result to a foreign directory, and watching it report that directory as its home. **CLOSED.**

**REDS %433 (`20260905.153929`) -- a friendly skip reads exactly like success.** *What went wrong:* the same generator SKIPS when key material already stands -- the ssh half recomments an existing key (`ssh-keygen -c`), the gpg half prints *a signing key already exists ... leaving its key material alone* -- and both lines read as a completed run. A hand asking for fresh keys per ship would have run it three times, read three cheerful reports, and held the same keys. `grain-pheromone` would additionally have refused at the `GLOW_PROFILE.bron` assert before reaching either half, since that file is gitignored and was never copied when the tree was born. *What caught it:* checking what the command would actually do before handing it over, rather than after. *What it taught:* **the difference between *did nothing* and *did the thing* has to be visible in the words, not inferred from them.** This is the `%429` shape one room over -- there a status letter collapsed *cannot check* into *not signed*; here a report collapsed *skipped* into *done*. Both were read as the reading the reader expected. *Repaired (`20260905.153929`):* each skip now says **NO NEW KEY WAS MADE** and names the word that would change that, and the script takes `rotate` -- which moves elder material aside under a one-clock stamp rather than deleting it, since a signing key is the one thing a tree cannot re-derive. `grain-pheromone` holds its `GLOW_PROFILE.bron`. **Generation itself stays a hand's act at a plain host shell**, by the script's own standing instruction: a revocable key is a weaker promise if the agent that will use it also made it. **CLOSED.**

**REDS %434 (`20260905.155120`) -- the mark law cited four failed forecasts and five more stood in a living pin.** *What went wrong:* `.claude/rules/stamp-and-name.md` retires the counted rung for planned work, and names four ladders as its evidence -- `f0-f63` reached f3, `u0-u127` paused at u91, `i0-i15` paused at i6, a sixteen-round chapter filled two rooms of twelve. All four were found by a hand and typed into the rule. `context/LEXICON.md` carried **five more the whole time**: `SOON0-SOON63` at SOON0, `JARL0-JARL63` at JARL0, `BUHR0-BUHR63` at BUHR6, `TACT0-TACT63` at TACT0, `DISC0-DISC63` at DISC4. The Compass Chapter's four equinoxes announced **256 rungs between them and had reached six**. *What caught it:* being asked to confirm one ladder had been molted, and building a meter to answer rather than reading for it. The meter found five where the question named one. *What it taught:* **a law whose evidence is gathered by hand is a law that only sees what somebody already looked at.** Nobody was careless here -- a forecast written into a name is invisible once written, because it reads exactly like a fact, and the four in the rule were found only because their ladders visibly stalled. A lantern that has fired nine times is a loom. *Repaired (`20260905.155120`):* the five living announcements swept to the mark-law form, keeping every rung number already written (testimony stands). `tools/a/announced_length_witness.rish` over `tools/fixtures/a/announced_length_scan.sh` gates living forecasts at zero and **reports the six ladders that MET their announcement as loudly as the ones that fell short**, because a reading that only accuses is one nobody believes. Twelve control legs on real repositories, and the scan's own two false-positive paths are among them: an elder draft dropped the announced value from the reading, which made every FINISHED ladder read one rung short, and a second draft named range spellings one by one and missed a Unicode ellipsis in a dated page -- so one announcement voted a reach for another and SOON read as reaching SOON63. Both were caught by the control before the meter read a real tree twice. The concept is seated as a foundation, `foundations/20260905-154954_the-clock-and-the-mark.md`, on the council rota's Earth-Cardinal seat. **CLOSED.**
