# REDS %481 -- a marker no content guard could see

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.120137`, the row CLOSED and the
pin at 39,840 of the 40,960 its own header declares.*

**What the row taught, in one line:** every guard reads a file for one property, so a fault that is
none of those properties passes them all -- and the one meter aimed squarely at this file measured
LENGTH, which a conflict marker only increases.

The living operator card stood published on the anointed remote carrying `<<<<<<<` and `>>>>>>>`
around its Landed row, through three commits, while every ship in the fleet read that card whole at
the open of every lap. The commit hook reads a message; `tracked_link_scan.sh` reads links;
`standing_equipment_scan.sh` reads the roster; `living_pin_max_bytes.sh` reads length. The repair is
[`../../tools/fixtures/c/conflict_marker_scan.sh`](../../tools/fixtures/c/conflict_marker_scan.sh),
one `git grep` at 0.3s against 61s per-file, held at zero by
[`../../tools/c/conflict_marker_witness.rish`](../../tools/c/conflict_marker_witness.rish).

---

**REDS %481 (`20260906.105500`) -- the living operator card stood published on the anointed remote with two conflict markers around its Landed row, and every instrument that reads that file reads it for something else.** *What went wrong:* `construction/ITINERARY.md` carried `<<<<<<< HEAD` and `>>>>>>> 8b22606c4` through commit `8b22606c4` and the two commits after it, so the card's Landed table read as two rows plus three lines of git punctuation. **Every ship in the fleet reads that card WHOLE at the open of every lap**, and the Standing block inside it is what steers an unattended round. *What caught it:* a rebase in a sixth tree, which conflicted against markers already committed upstream. *What it taught:* **a marker is invisible to every content guard this tree owns, because each reads a file for a different property.** The commit hook reads the message rather than the diff; `tracked_link_scan.sh` reads links; `standing_equipment_scan.sh` reads the roster; and `living_pin_max_bytes.sh` reads LENGTH -- so **the one meter aimed squarely at that file read the damage as ordinary growth**, since a marker makes a pin longer. Same family as `%467` and `%482` one room over: the cheap question returns a word the expensive one would not. *Swept:* exactly **one** file in 15,681 tracked, and it was this one. *Repaired:* `tools/fixtures/c/conflict_marker_scan.sh` reads every tracked file for a line beginning `<<<<<<< ` or `>>>>>>> `, held at zero, under [`tools/c/conflict_marker_witness.rish`](../../tools/c/conflict_marker_witness.rish), rostered `tier lap` at **0.3s**. **The tier was bought by measurement:** a per-file `grep` walk over 15,681 tracked files measured **61s**, which would have put this on the cadence clock where a marker could stand published for four laps; one `git grep` with pathspec exclusions measures **0.3s**. *The one false positive it refuses to have:* a bare `=======` is also the Markdown setext heading underline, which this tree writes in prose, so only the two LABELLED markers gate and a divider is counted solely between a pair. *Two traps proven rather than assumed:* a marker inside a fenced code block is **still bitten** -- a file that must show the shape is excluded by NAME, countably, since an exclusion by shape is a door -- and an **unmerged path lists once per stage**, so a tree standing mid-merge reported the same file three times until `sort -u` was put on the reading, which is exactly the tree this guard exists for. `tools/fixtures/c/conflict_marker_control.sh` proves **23 readings over 10 planted states in 9 real git repositories**, every refusal lifted in its own pen. **CLOSED.**
