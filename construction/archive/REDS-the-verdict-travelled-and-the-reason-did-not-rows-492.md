# REDS %492 -- the verdict travelled and the reason did not

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.130755`, in the round that booked it,
because the pin stood 2,271 bytes above the 40,960 it declares and this row is the only closed one
in it that belongs to this seat. An accrete shim runs a target under `tools/gen/` and exits that
target's code, so a caller may name either path and get the same answer -- except in the one
direction nobody tested. Rishi's `run` captures the target's stderr into `r.err`, and a shim
saying only `r.out` writes zero bytes to its own, so the exit code arrives and the sentence stays
behind. **Forty-three of fifty-two shims, and zero of them on the standing roster** -- which makes
it a trap rather than a live fault, springing on the lap that rosters one, which is exactly the
lap [`%360`](REDS-what-no-meter-was-reading-rows-439-441.md) keeps asking for.*

*This row was booked as `%487` from the anointed spine after a fetch, and its number moved twice on
two rebases in one send. The first brought ten upstream commits carrying a published `%487` stamped
`20260906.120137`; the second brought three more, in which a peer repaired their own `%486` double
booking by taking `%491` -- the number this row had just moved to. The published number never moves
and the unshared one does, so this is `%492`. The stamp below is the key and has not moved through
either.*

*Kin, all from the same week: [`%484`](REDS-a-claim-whose-only-source-is-itself-rows-480.md)'s
family -- a guard that could not hear itself fail -- and `%473`, a swallowed exit status, whose
lesson this scan's own `git grep` obeys; and `%466`, a floor that carries a number, which is why
`exit_alias_sites` publishes this reading's blind spot rather than hiding it.*


**REDS %492 (`20260906.124217`) -- a shim forwards its target's verdict and drops its reason, so a guard on the clock can refuse without saying anything.** *What went wrong:* `tools/am/*.rish` and their kin are accrete shims -- each runs a target under `tools/gen/` and exits that target's code, so a caller may name either path and get the same answer. Rishi's `run` captures the target's stderr into `r.err`, and a shim that says only `r.out` never writes a byte to its own stderr. *Measured on a real file with no plant,* because one shim refuses honestly on this pier: `amphora_device_wire` drives a virtio lab and there is no qemu here. The target, run directly, hands a reader `rishi: assertion failed -- Amphora vessel fetch device wire lab failed` with its line number. The shim, run the same second, exits 1 with **zero bytes on stderr**, and all a reader receives is `amphora-device-wire: virtio fetch-by-digest lab...` -- a progress line printed BEFORE the failure, which reads like success. `standing_equipment_run.sh` captures a guard with `> out 2>&1`, so the merge was already there and caught nothing: the shim emitted nothing to merge. *What caught it:* the aether rota's own strand -- *a guard that cannot red guards nothing* -- read against this lane's roster comment, which had named five swallowing shims a ratchet on touch and left the question of how far the shape reached unasked. It reaches 43 of 52. *What it taught:* **a wrapper is only transparent in the direction it was tested.** The exit code was forwarded on purpose and the stderr was forgotten, and nothing distinguishes those two halves until something fails -- so the fault is invisible on every passing run, which is every run a lap sees. **It is a trap rather than a live fault, and that is what decides the instrument's shape:** zero swallowing shims stand on the standing roster today, so nothing is lost now; it springs on the lap that ROSTERS one, and `%360`'s standing pressure is to roster more. This lane walked into it on `20260906.063124` -- three shims onto a lap clock, the swallow found by planting a break and repaired in the same breath. *Repaired (`20260906.124217`):* the five remaining amphora shims take the two lines, 48 swallowers -> 43, and `tools/fixtures/s/shim_reason_scan.sh` reads every tracked `.rish` for the three marks together -- the `run ["rishi/bin/rishi" "run" ...]` call, the `say r.out` forward, the `exit r.code` tail -- since each alone is ordinary. **Gate:** `swallow_rostered` at zero, which the field cannot prove able to bite, so the pen supplies it -- one swallowing shim named by a `path` row refuses, the same shim with one line added passes. **Ratchet:** `swallow_unrostered` at 43, only falling, proven from both sides on one plant. A seat is a `path` row at column zero rather than a path named in a comment, or a guard could count as rostered because somebody wrote about it. `exit_alias_sites` publishes the reading's own blind spot -- two tracked files exit a captured run's code under another variable name (`%466`). The corpus arrives through one `git grep` whose status is classified rather than swallowed (`%473`, `%484`), which took the guard from 22 seconds to 5 with identical counts. `tools/s/shim_reason_witness.rish` on the roster at `tier lap`; 40 pen readings across 8 real repositories, every refusal planted and then lifted. **CLOSED.**
