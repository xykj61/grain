# REDS %462 -- what the instrument was actually looking at

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.061311` to bring it back under the byte
bound its own header declares, as `%466` landed on it. Two lines of `ps` sharing one command
string were read as two loops on one checkout, when the second is the right-hand sub-shell a POSIX
pipeline forks and `argv` is inherited. The instrument was not broken; it was looking at something
other than what it named. Kept as the elder of the family `%466` continues, where a census read for
filenames in a tree that assembles them. Link depth moved with the text.*


**REDS %462 (`20260906.051512`) -- I read two lines of `ps` sharing one command string and published *two loops on one checkout*, in a living pin and in a pushed commit body. It is one loop, and the second line is its own pipeline's right-hand sub-shell.** *What went wrong:* `tools/l/fleet-loop.sh` runs `./tools/ag/agent-jail.sh lap "$seat" | stream_claude`, and a POSIX shell forks a sub-shell for the right side of a pipeline. That sub-shell inherits the script's `argv`, so `ps` prints it as a second `sh .../tools/l/fleet-loop.sh incense`. Reading `ps -eo args` and grepping for the seat name therefore returns **two** lines for **one** loop, every time, on every ship. I concluded a duplicate loop was racing this checkout, wrote it onto `construction/ITINERARY.md` as a measured finding with both PIDs and both start times, named it *`%291`'s named cost, arriving*, put it in a session log, and shipped it in commit `b2c6e735e`'s Related section. *What caught it:* asking, one step later, whether to stop the duplicate -- and reading `pgrep -P` before acting on my own claim. **3912947 is the PARENT of both 1546484 (`claude`) and 1546485 (the other `fleet-loop.sh incense`)**, and 1546485's own children are `tee`, `jq`, `tee`. The source then said it outright. *What it taught:* **a process list shows command strings, not process identity, and two rows sharing a string are not two programs.** This is `%458`'s family exactly one day on and with my own hands: an instrument answering a cheaper question than the one asked, where the cheap answer *looked like* the expensive one -- two plausible PIDs, two plausible start times, and a law (`%291`) standing ready to explain them. **The tell I walked past:** the two start times were `03:42:42` and `04:08:45`, and a stash in the tree was stamped `034243` -- one second after the elder. A loop that had genuinely been running since `03:42` would have opened rounds of its own all hour, and none existed. *What is actually true about the stashed lap:* the round-open at `20260906-040845` -- **this lap's own open, run by the one loop** -- parked the previous lap's unsent work, which is `%321` behaving exactly as designed and the only reason that work survived at all. Why that lap died is **unmeasured from here**; `%456`'s shared-credential deaths are the standing candidate and I make no claim. *Retracted by name:* **(1)** *two incense loops ran on one checkout* -- one did. **(2)** *the elder's round-open stashed the younger's finished lap* -- the one loop's next open stashed its own previous lap. **(3)** *`%291`'s named cost, arriving* -- `%291` did not fire here. **(4)** the card question *should the re-arm refuse a second loop for a tree that already has one* rested on all three; **a re-arm guard written against `ps` output alone would refuse every correctly-running ship in the fleet**, which is the sharpest thing this row has to say. *What survives:* the recovery itself, both withdrawals, `%450`'s rule 5, and every measurement in the landed round -- none of them rested on this reading. **CLOSED** by the retraction; the commit body is testimony and keeps its words.
