format session-log-v1
stamp 20260905.181517
editor Claude Code
provider Anthropic
product Claude Code
role field captain, Earth trio
modality text
model claude-opus-5
model_status configured default, runtime unverified
voice Kyri
host NixOS pier, ai-jail enclosure, America/New_York
title the captain's view
prompt build the read-only captain's view
think Built it as a mount rather than a loosening. Every peer tree arrives through ai-jail's --map, which the launcher translates to bwrap --ro-bind, so the captain reads a peer and cannot become a second writer in it. REDS %291 is why that distinction matters.
think Three paths in every peer tree hold credentials rather than work -- .gnupg-rye is that ship's signing key, .ssh its deploy key, and loops/ its agent auth. Denied with --deny-path rather than masked, because an empty directory reads like a ship with no keys rather than one whose keys are none of the captain's business.
think Opt-in per launch through FLEET_CAPTAIN_VIEW, and refused from any tree but the captain's. An unattended lap never sets it, so the fleet's ordinary shape is unchanged.
obs Proved it by entering a real enclosure rather than reading the flags. A dry run prints an intention; the kernel gives the answer. Against petrichor: work readable, write refused with Read-only file system, signing key and deploy key and Claude token each Permission denied, and 11 uncommitted files visible -- which is the whole capability, since the anointed remote cannot show what a ship has not committed.
obs My first loops test was inconclusive and I nearly took it as a pass. grain-copal was newborn and had no loops directory, so the denial read as No such file or directory. Re-ran against petrichor, which has one, and got Permission denied.
obs The witness wanted tools/c and that room does not exist -- the letter fold split c into two-letter rooms long ago, so it belongs in tools/ca beside caravan.
loom captain_view legs=6 fail=0 peers=7 denials=21 wall_ms=102
file tools/ag/agent-jail.sh FLEET_CAPTAIN_VIEW mounts peers read-only and denies their credentials
file tools/fixtures/c/captain_view_control.sh six legs, each entering a real sandbox
recommend check-in Keaton -- relaunch the jail and read whether the onboarding text is gone; the captain's view waits on that same launch
