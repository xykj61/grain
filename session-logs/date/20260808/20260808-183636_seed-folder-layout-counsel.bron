format session-log-v1
stamp 20260808.183636
editor Claude Code
model claude-fable-5
voice Riyo
host NixOS 26.05 x86_64 cloud pier (Vultr SEA) — Eastern (EDT) time convention · this Claude Code session inside ai-jail
title seed folder layout — field + generated seed, not two hand-edited halves
prompt should the jailed ~/grain hold two folders (public/private, or grain-os/xykj61/autoproject96) as two distinct git repos; or rename outer grain to xykj61 with xykj61/grain-os/grain nesting; does it update the plans; recommendation
think separated the two ideas Keaton conflated — folder layout is a workflow convenience; the privacy guarantee is the projection witness, not a folder boundary
think a second hand-edited tree is a second place a personal file can slip in — folders do not make it safer, a green sow.rish witness does
think the persistence constraint decides much — CLAUDE.md says only the project directory ~/grain survives a jail reset; both repos must live INSIDE the pier, ruling out siblings of grain
obs recommend field + generated seed — keep ~/grain as single source of truth (already dual-remote autoproject96 + xykj61); add ~/grain/seed/ gitignored, its own .git, one remote grain-os/grain, regenerated by sow.rish, not hand-edited for shared code
obs name the folder by role (seed/) not by account — folder->account mapping is unobvious, accounts change, and the seed is the seed not the org; note this is opposite the org-name advice (grain-os over grain-os/seed) because locally discovery is not a concern
obs advised against renaming the pier to xykj61/ with xykj61/grain-os/grain — the OWNER/ORG/REPO mirror is a nice mental model yet renames ~/grain (wired into CLAUDE.md pier, launch scripts, remember.md, hundreds of refs), fights compact-root, buys no safety
obs advised against git-mv-ing the whole field into private/ — same mass-reference breach already declined for the module ring
obs plan update — pins M2 projection target to ~/grain/seed/ + a /seed/ .gitignore line; M4 becomes cd seed; git init; generic-author commit; remote grain-os/grain; push (Option B fresh history); manifest/nock/seven-room map unchanged
obs held everything gated — no folder created, no .gitignore edited; layout touches the pier shape, so it waits for Keaton's word
file session-logs/20260808-183636_seed-folder-layout-counsel.bron this counsel record
git_nib 07a71cfee1
recommend check-in (Claude) confirm the field + gitignored seed/ layout (and Option B) and I thread seed/ into the M2/M4 milestones and add the one-line .gitignore; still nothing published until the projection witness is GREEN
