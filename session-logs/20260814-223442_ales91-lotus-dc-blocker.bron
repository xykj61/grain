format session-log-v1
stamp 20260814.223442
editor Zed
model Fable 5
voice Kyri
host Ubuntu 26.04 LTS GNOME Wayland (cloud pier) — Eastern (EDT) time convention
title ALES91 — Lotus's one-pole IIR DC blocker, the carried-state sibling that follows a drifting offset a block mean cannot
prompt recur baton prin — take the next agent-doable Lindy-first crux-first lap, self-approve the design round, send each finished increment to origin and xykj61, stop at custody gates
think ALES90 landed the block-mean DC remover and NAMED its own successor in as many words: the one-pole IIR DC blocker, a drifting-offset remover with carried state, the ALES43 shape — a later, named rung. That is the crux-first next move: it is the hardest still-tractable Lotus step (the first rung to carry two feedback states through a recursive difference equation and prove them stable in pure integers), and Lindy-first it is a permanent staple of every audio chain — the streaming form a real console channel uses where the block-mean form fits a buffer. No custody gate: a bounded in-process i16 clip, siloed to lotus/.
think The crux is that a DC blocker CARRIES STATE (the previous input x[n−1] and previous output y[n−1]), so the ALES43 split-equals-whole property returns exactly: a span blocked in two pieces, the second continuing the first's ending state, equals the whole blocked once byte-for-byte, no re-transient at the seam. That is what makes ALES91 the SIBLING of ALES90 rather than a copy — dc_remove is memoryless (a second pass is a no-op), dc_block has memory (its second span continues the first's decay). The vivid distinction proven in the witness: a DC STEP (+4000 then −2000) drives each region to zero, where ALES90's block mean subtracts one average (1000) and leaves the regions riding ±3000 — the drift the IIR blocker follows and the block mean cannot.
think The step is y[n] = (x[n] − x[n−1]) + R·y[n−1], R = num/den. The difference is the zero AT direct current (a constant settles to zero out); R·y[n−1] = @divTrunc(num·y_prev, den) is the leaky integrator, and because it truncates toward zero the output reaches EXACTLY zero in finite steps once |y_prev| falls below den/num — the same finite-settling argument ALES40's low-pass uses. The feedback reads the SATURATED prior output so the recurrence stays bounded (|y_prev| ≤ sample_max), every intermediate in i64, the multiply bounded by a new max_pole_den (1<<16) so num·y_prev stays far inside i64.
think The coefficient law is ITS OWN, not the tone filter's. A DC-blocker pole must be a stable fraction in [0, 1): den > 0 and ≤ max_pole_den, num ≥ 0 (num = 0 is the LEGAL first-difference highpass, unlike ALES40 which demands num ≥ 1), and num < den STRICTLY (num = den is a marching integrator that never settles, num > den is unstable) — both refuse BadCoeff. Named the divergence plainly in the comments and the design doc so a composer reading the suite sees why two filters share BadCoeff by name yet differ in law.
obs Followed the ALES90/ALES43 pattern exactly: standalone module (dc_block.rye importing timeline) + self-approved design read + witness + README status-line and rung-bullet sync + session log, one signed round. Three faces of one implementation (dc_block_carry general, dc_block the state=0 case, DcBlocker the held value) mirror ALES43's low_pass_carry/low_pass/Lowpass so the carried and from-silence forms can never drift. Caught one wrong test on the first read-through: a sub-span of four constant samples at R=0.5 decays 4000→2000→1000→500, NOT to zero — fixed the span-discipline law to assert the exact decay rather than a settled tail. Ten laws proven; the witness imports ALES90's dc_remove and proves the drifting-offset contrast beside the map. Selftest GREEN, tame_style_check bans clean, dc_block.rye carries no ratchet debt (many asserts, snake_case, no @memcpy, i64 throughout, one saturate reused from ALES2).
loom witness=ales_dc_block_witness selftest=GREEN laws=10 module_lines=307 max_pole_den=65536
file lotus/dc_block.rye the one-pole IIR DC blocker, y = saturate(x[n] − x[n−1] + R·y[n−1]), carried two-state recurrence, split-equals-whole, settles to zero exactly, ten laws proven
file tools/ales_dc_block_witness.rish builds the module, runs the selftest, asserts the single GREEN ales-dc-block line
file active-designing/20260814-223442_fill-ales91-lotus-dc-blocker.md the self-approved design read naming the crux, the carried-state sibling relation, the own coefficient law, and the ten laws
file lotus/README.md front-door status line and rung bullet synced with ALES91, stamp refreshed
recommend keep-going both DC removers now stand together — the memoryless block mean for a bounded region, the carried-state IIR blocker for a stream — and the offset family the DRIVE shelf generates is fully answered; the loop names its own next Lotus crux (a hysteresis comparator giving ALES89's square a memory against chatter, a carried-state shelving IIR, or a fresh DSP family) as its own self-approved design round
