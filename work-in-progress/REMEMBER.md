# REMEMBER â living operator card

**Language:** EN
**Status:** Living pin â operator carry card
**Voice:** Kyri

*One-page carry card for outer terminal Â· phone Â· waymarks. Refreshed when Keaton says **remember**. Debrided to the Compass Season `20260809.024320` â the old season marks are gone; their greens live in the code and in the dated counsel.*

**Git nib:** `4691185f90` Â· Grain Â· cloud pier Â· **public seed `grain-os/grain` @ `26cae5b` (single-commit; force-push refresh)** Â· *living edge DREY5 â Season A's Mikrophone firmware journey (waymark DREY): DREY0 `mikrophone/session.rye` proves the memory that forgets (a capture held only while powered, kept only on a deliberate commit, the working buffer zeroed whole on power-down); DREY1 `mikrophone/wire.rye` carries only what was committed across the single wire â a self-describing frame (magic Â· version Â· length Â· Sha256 digest Â· payload) deframed verify-before-trust, `frame` reading `carry()` so a forgotten buffer never reaches the wire, six corruptions each refusing by name before a byte is yielded; DREY2 `mikrophone/recorder.rye` gives it the record button â a bounded control state machine (idle Â· recording Â· paused Â· stopped) whose single invariant is that `feed` reaches `session.capture` only while recording, pause holding the buffer without forgetting it, every illegal transition refusing by name; witnesses `tools/drey_{session,wire,recorder,firmware}_witness.rish` GREEN; DREY3 `mikrophone/firmware.rye` composes the three proven rungs into one loop â a recorded, committed run carries out to the verify-before-trust wire, an uncommitted tail never crosses, a forgotten run powered down carries NothingToCarry, a tampered frame refuses DigestMismatch; DREY4 `mikrophone/inbox.rye` gives the far side (the SETU Desk hand) its discipline â `accept` deframes verify-before-trust FIRST and keeps the payload ONLY on a clean deframe, every corruption refused by name leaving the inbox empty (a bad frame neither lands nor erases a prior keep), `clear` forgetting whole; real hardware stays custody gate #2. DREY5 `mikrophone/carry.rye` proves the two hands meeting as one — `carry_to_inbox` composes DREY3 `firmware.carry_frame` with DREY4 `inbox.accept` over their public APIs (no new transport, buffer, or error), adding one invariant of its own: a clean carry leaves the desk holding exactly the device committed bytes and nothing else; proven across five blocks (the carry meets byte-for-byte, only the committed crosses, a forgotten run refuses NothingToCarry leaving a prior keep untouched, a transit-tampered frame refuses DigestMismatch keeping nothing, both hands forget whole); witness `tools/drey_carry_witness.rish` GREEN, TAME + width clean. DREY5 `mikrophone/carry.rye` proves the two hands meeting as one — `carry_to_inbox` composes DREY3 `firmware.carry_frame` with DREY4 `inbox.accept` over their public APIs (no new transport, buffer, or error), adding one invariant of its own: a clean carry leaves the desk holding exactly the device's committed bytes and nothing else; proven across five blocks (the carry meets byte-for-byte, only the committed crosses, a forgotten run refuses NothingToCarry leaving a prior keep untouched, a transit-tampered frame refuses DigestMismatch keeping nothing, both hands forget whole); witness `tools/drey_carry_witness.rish` GREEN, TAME + width clean. DREY5 `mikrophone/carry.rye` proves the two hands meeting as one â `carry_to_inbox` composes DREY3 `firmware.carry_frame` with DREY4 `inbox.accept` over their public APIs (no new transport, buffer, or error), adding one invariant of its own: a clean carry leaves the desk holding exactly the device's committed bytes and nothing else; proven across five blocks (the carry meets byte-for-byte, only the committed crosses, a forgotten run refuses NothingToCarry leaving a prior keep untouched, a transit-tampered frame refuses DigestMismatch keeping nothing, both hands forget whole); witness `tools/drey_carry_witness.rish` GREEN, TAME + width clean; real hardware stays custody gate #2. Prior living edge HUNK93 (the whole faceted-and-sorted search now reads from one keeper-typed line â HUNK89 sorts cheapest-first, HUNK90 names the sort, HUNK91 reads it dearest-first, HUNK92 crowns either direction with a header that says which way it turned, HUNK93 lets a keeper type `sort:price-asc` / `sort:price-desc` in the query itself to pick the direction; no drift between the word and the order; parts-marketplace journey whole end to end; the readable faceted search reads query, count, page N of M, and the parts of that page top to bottom â filter, parse a typed query, narrow on several facets, draw the compound-narrowed page, tap a searched result, open the tapped part's detail page, a query header above the shelf; the boolean query algebra stands whole in the image layer â HUNK78 AND narrows, HUNK83 OR widens, HUNK85 joins them into the canonical faceted query (OR within a facet via `|`, AND across facets by space; a `|`-free line is exactly HUNK78, a strict superset) â and stands whole on glass too: HUNK79 draws the AND page, HUNK84 the OR union page, HUNK86 the faceted page (OR-within, AND-across; a `|`-free line is exactly HUNK79's AND page, a strict superset), what a keeper types is what a keeper sees); card body lags the log past HUNK55 and wants a refresh*
**Host:** Framework Â· EDT (`America/New_York`) Â· Vultr SEA VPS (HP AMD 2vCPU/4GB shared Â· never EWR) Â· this session in ai-jail

**Latest lap `20260813` â Season A opens (waymark HUNK).** The Mycelium consensus season (Season D's protocol half) stands whole and witnessed (98 modules Â· 80 witnesses Â· foundation + README) â a clean season close â so by Lindy-first crux-first the road turned to **Season A (Hardware & Right-to-Repair)** and its opening journey, the **open image module** beneath the parts marketplace and the Photos app. **HUNK0 LANDED:** `image/qoi.rye` â a whole open **QOI** codec (encode + decode), so `decode(encode(pm))` recovers a pixmap **byte-for-byte** across all six chunk kinds (rgbÂ·rgbaÂ·indexÂ·diffÂ·lumaÂ·run), real compression (37 < 256 raw), five corruptions each refusing by name. Waymark **HUNK** seated (derive script Â· both rule twins Â· Lexicon); QOI thanked clean-room (`gratitude/qoi.md`). Witness `tools/hunk_qoi_witness.rish` GREEN. **HUNK1 LANDED `20260813`:** `pond/apps/image_artifact.rye` â an image is a content-addressed **Tablecloth** artifact. `store_image` encodes a pixmap to QOI and beads it under a name (the name pins a SHA3 content address); `fetch_image` reassembles by name, proving every bead against its digest **first**, so a tampered bead refuses **DigestMismatch** and an unknown name refuses **UnknownArtifact** â each **before `qoi.decode` reads a single pixel**. Selftest rides a real 8Ã8 image THROUGH the store byte-for-byte (166 bytes stored), shows the dedup dividend (same image, two names, stored once â 2/2 beads deduped). Witness `tools/hunk_artifact_witness.rish` GREEN; TAME + width clean; HUNK0 still GREEN (`qoi.rye` symlinked into `pond/apps/`, code untouched). **HUNK2 LANDED `20260813`:** `brushstroke/image_skate.rye` â the decoded grid meets **Skate**. `down_map` partitions the image into colsÃrows blocks, averages each block's color, quantizes to the nearest of a seven-color anchor palette, and wears the matching slot over a full-block glyph (a full-block extended-glyph atlas set on Skate's `extended_glyphs` seam) â so `rasterize` paints every cell a solid color block. Proven on metal: an 8Ã8 two-tone image (red left, blue right) down-maps to 2Ã2 with red reaching slot 3 and blue slot 5 (**spatial**), the map is **deterministic** (same image, same cells), the rasterized 16Ã32 canvas carries the real down-mapped colors (left cell red, right cell blue, 256 lit pixels = one glyph block per cell), and an empty image / over-ceiling grid each refuse by name. Both grid axes bounded (`max_map_cols Â· max_map_rows`). Witness `tools/hunk_skate_witness.rish` GREEN; TAME + width clean. **HUNK3 LANDED `20260813` â the opening HUNK0âHUNK3 quest is COMPLETE:** `image/photos.rye` â the first Photos verb, a **crop**. `crop` is a pure, bounded function over the decoded RGBA grid: it reads a rectangle and returns a fresh smaller image, the source untouched. Proven on metal: cropping a position-tagged 6Ã6 image, **every cropped pixel is exactly its source pixel**; the source is unmutated (**pure**); the identity crop recovers the whole byte-for-byte; and a cropped image is still an **open image** â it re-encodes and round-trips through the codec byte-for-byte. Coordinates checked in u64 so a rectangle can never wrap; empty and both out-of-bounds rectangles refuse by name. Witness `tools/hunk_photos_witness.rish` GREEN; TAME + width clean. **The Open Image journey stands whole: HUNK0 codec Â· HUNK1 content-addressed Â· HUNK2 painted Â· HUNK3 cropped** â an open image module beneath the parts marketplace and the Photos app, no permission asked to read a pixel. **HUNK4 LANDED `20260813` â the sprite-marketplace quest opens:** `image/sprite.rye` fixes Season A's McMaster-Carr trick over the open module â a `SpriteIndex` binds each product name to a bounded rectangle (a **window**) into ONE decoded sheet, and `render` returns that window as a fresh open image by **reusing HUNK3's `photos.crop`**. Every window is bound-checked in u64 at **add time**, so a render can never fail on bounds; unknown name Â· out-of-bounds Â· empty Â· duplicate Â· overlong each refuse by name. Proven on metal: three products indexed into one 8Ã8 tagged sheet (256 bytes **decoded once**), each renders exactly its window, disjoint windows carry different pixels (no product leaks a neighbor's bytes), the sheet is shared and **unmutated** across every render, and a rendered product re-encodes and round-trips byte-for-byte. Witness `tools/hunk_sprite_witness.rish` GREEN (HUNK3 crop re-run GREEN beneath); TAME + width clean. Design `active-designing/20260813-201500_hunk-sprite-marketplace-index-exploration.md`. **HUNK5 LANDED `20260813` â the product page is seen:** `brushstroke/sprite_page.rye` `paint_product` joins HUNK4's window and HUNK2's down-map â it renders a product's window out of the one sheet (reusing HUNK3's crop) and down-maps it to a Skate grid, so a catalog entry is not only stored and windowed but **seen**. Proven on metal: two products indexed into one two-tone sheet (red left, blue right) paint distinct pages (red â slot 3, blue â slot 5, pages differ â a page is seen as its own product, never a neighbor's), the page **equals a direct down-map of the window cell for cell** (no drift between index and page), lit pixels reach a real canvas (256), and an unknown product refuses `UnknownProduct` through the whole composition before any paint. Pure composition over public APIs (sprite.rye + photos.rye symlinked into `brushstroke/`); no new storage, codec, or palette. Witness `tools/hunk_sprite_page_witness.rish` GREEN (HUNK4 index re-run GREEN beneath); TAME + width clean. **HUNK6 LANDED `20260813` â the sheet is content-addressed:** `pond/apps/sprite_store.rye` gives the marketplace sheet an identity â `store_sheet` beads the sprite content-addressed (HUNK1's `image_artifact.store_image`), `open_sheet` fetches it back proving every bead against its digest **before the codec reads a pixel**, so a tampered sheet refuses `DigestMismatch` and an unknown market refuses `UnknownArtifact` **each before a SpriteIndex is built or a product rendered**. Proven on metal: a two-tone sheet stored (54 bytes), a market opened over the **verified** sheet rendering two products from their own windows, then one stored byte flipped and the whole market refusing before any product renders. Pure composition over public APIs (sprite.rye + photos.rye symlinked into `pond/apps/`). Witness `tools/hunk_sprite_store_witness.rish` GREEN (HUNK1 verified fetch re-run GREEN beneath); TAME + width clean. **HUNK7 LANDED `20260813` â the marketplace catalog travels; the sprite quest CLOSES:** `image/sprite_catalog.rye` lets the index itself travel â `render_catalog` writes a `SpriteIndex` as a flat-Bron `format sprite-catalog-v1` record (one `product <name> <x> <y> <w> <h>` line per entry), `parse_catalog` reads it back into a fresh index **over a given sheet**, so `render(parse(render(x)))` is a fixed point and the rebuilt index renders every product from the same window byte-for-byte. Parse validates each window against the **real sheet** (through `SpriteIndex.add`), so a catalog can never name bytes the sheet lacks â proven by parsing over a smaller sheet and refusing `WindowOutOfBounds`; a bad header, a malformed line, and an extra field each refuse `BadCatalog`. Added two additive pub accessors to `sprite.rye` (`len`/`entry`) so a reader walks the catalog without private cell storage; HUNK4 re-runs GREEN. Witness `tools/hunk_sprite_catalog_witness.rish` GREEN; TAME + width clean. **The sprite-marketplace quest stands whole: HUNK4 index Â· HUNK5 seen page Â· HUNK6 content-addressed sheet Â· HUNK7 travels as text** â McMaster-Carr's one-sheet trick over the tree's own open module, one decode serving a whole catalog. **HUNK8 LANDED `20260813` â the Photos app turns and mirrors; a fresh quest opens:** `image/photos.rye` gains three pure bounded verbs added **additively** beside `crop` (untouched) â `flip_h` (mirror left-right), `flip_v` (mirror top-bottom), `rotate_quarter(turns)` (clockwise quarter turns; a 90/270 turn swaps width and height). Each returns a fresh `Pixmap`, leaves the source untouched, and refuses a zero-dimension image `EmptyImage`. The witness proves them by the **algebra** they must obey â rotate_quarter four times is identity byte-for-byte, `rotate_quarter(2)` equals `flip_h(flip_v)`, each mirror is its own inverse, `rotate_quarter(k)` equals k single turns â so an index bug is caught regardless of the arithmetic; a non-square 3Ã5 tagged image makes the width/height swap visible, a concrete corner pins the turn clockwise, a rotated image still re-encodes and round-trips as an open image. Added `/pond/apps/bin/` to `.gitignore` (compiled witness binaries were untracked). Witness `tools/hunk_photos_orient_witness.rish` GREEN; TAME bans + width clean; HUNK3 crop still GREEN beneath. Design `active-designing/20260813-153700_hunk-photos-orient-exploration.md`. **HUNK9 LANDED `20260813` â the Photos app resizes:** `image/photos.rye` gains `scale(new_w, new_h)` added **additively** beside crop + the orient verbs (all untouched) â nearest-neighbor, output pixel `(nx,ny)` samples src `(nxÂ·w/new_w, nyÂ·h/new_h)`, sampling in u64 so a product can never wrap; refuses a zero-dimension source/target `EmptyImage` and a target past the `2Â²â°` ceiling `OutOfBounds` before allocating. Witness proves identity resize byte-identical, a 2Ã nearest upscale sampling every pixel from `src(nx/2,ny/2)`, a downscale to 1Ã1 picking the top-left pixel, the scaled image still re-encoding + round-tripping as an open image. Nearest-neighbor is the honest first scaler; a bilinear/area-average resampler is a named horizon on the same signature. Witness `tools/hunk_photos_scale_witness.rish` GREEN; TAME + width clean; HUNK3 crop + HUNK8 orient still GREEN beneath. **HUNK10 LANDED `20260813` â the Photos app tunes light:** `image/photos.rye` gains `adjust(brightness, contrast_num, contrast_den)` added **additively** beside crop + orient + scale (all untouched) â each color channel takes the affine map `out = (v-128)Â·num/den + 128 + brightness`, clamped into `0..255`, so contrast pivots around the `128` midpoint and brightness shifts the whole channel; **alpha passes through untouched**. The map runs in `i64` so no product wraps, with contrast terms bounded by a named ceiling (`adjust_contrast_max = 1<<16`) and brightness bounded to a full range (`adjust_brightness_max = 255`) or refuses. Witness proves a neutral adjust (brightness 0, num==den) byte-identical to the source, a `+40`/`-40` brightness shift-and-clamp per channel, a `2/1` contrast pivoting around 128 (0âclamp 0, 100â72, 200âclamp 255), a `0/1` contrast flattening to the midpoint 128, the adjusted image still re-encoding + round-tripping as an open image, and a zero denominator (`BadContrast`), an over-range brightness (`BadBrightness`), and an empty image (`EmptyImage`) each refused by name. Nearest gestures beyond: hue/saturation and a non-destructive edit-list (record verbs as data, replay + content-address) are named horizons. Witness `tools/hunk_photos_adjust_witness.rish` GREEN; TAME + width clean; HUNK3 crop + HUNK8 orient + HUNK9 scale still GREEN beneath. Design `active-designing/20260813-154324_hunk-photos-scale-exploration.md`. **HUNK11 LANDED `20260813` â the Photos app edits non-destructively:** `image/photo_edits.rye` records the six Photos verbs **as data** â an `Edit` tagged union (one variant per verb), a bounded `EditList` (`max_edits = 64`), and one `apply` that **replays** the whole list over a source image (folding each verb's fresh result from a clone) to produce a fresh edited image, the source never mutated. Replay is **deterministic** (same list + same source â same bytes every time, the content-address guarantee an edited image never needs its pixels stored, only the pair source + edit-list), and the list **travels as a flat-Bron `format photo-edits-v1` record**, so `render(parse(render(x)))` is a fixed point like the sprite catalog's. Proven on metal: a 3-verb list replayed over a 6Ã6 image equals the verbs by hand byte-for-byte, deterministic, **non-destructive** (the source untouched, the empty list recovers it byte-for-byte), the edited image still re-encodes and round-trips as an open image, every verb kind travels to the same bytes, and a bad header Â· unknown verb Â· malformed field Â· extra field each refuse `BadEdits` while an out-of-bounds crop surfaces `OutOfBounds` **through the fold** (no new failure mode). Witness `tools/hunk_photo_edits_witness.rish` GREEN (58-byte fixed-point record); TAME bans clean, width exit 0; the Photos verbs untouched beneath. Design `active-designing/20260813-155719_hunk-photo-edits-non-destructive-exploration.md` names HUNK12â14 horizons (bead the edit-list content-addressed Â· the Skate view of a pending edit Â· revert/truncate). Next agent-doable crux: HUNK12 (bead an edit-list into Tablecloth so `(image, edit-list)` reproduces the edited image from cold), another Photos gesture (a smoother resampler, or hue/saturation), or another Season-A / double-seat journey. **HUNK12 LANDED `20260813` â the edit-list is content-addressed:** `pond/apps/edit_store.rye` banks HUNK11's determinism â `store_edits` renders an `EditList` to its `format photo-edits-v1` text and **beads it content-addressed** (HUNK1's Tablecloth store, bounded to a resin or refusing `EditsTooLarge`), and `reproduce` fetches it back proving every bead against its digest **before the text is parsed or a verb replayed**, then parses and applies it over a source image. So an edited image **reproduces from cold** out of the pair `(source, edit-list address)`: its pixels never need storing, only the recipe that makes them. Proven on metal: a 3-verb recipe banked (58 bytes), the cold reproduction equals a direct apply byte-for-byte, the same recipe under two names stored once (deduped 1/1 beads), a tampered recipe refusing `DigestMismatch` and an unknown name refusing `UnknownArtifact` each before a verb replays. `photo_edits.rye` symlinked into `pond/apps/`. Witness `tools/hunk_edit_store_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK13 LANDED `20260813` â a pending edit is seen before it is committed:** `brushstroke/edit_preview.rye` is the eye â `preview` replays an edit-list over a source image (`photo_edits.apply`, pure â the source untouched, **nothing stored**) and down-maps the fresh result to a **Skate grid** (`image_skate.down_map`) that lights the canvas, so a keeper **sees** the edit before committing it. Pure composition over two proven seams, adding **no new failure mode**. Proven on metal: a pending `flip_h` over an 8Ã8 two-tone previews to a 2Ã2 grid **equal to `down_map(apply(source,list))` cell-for-cell**; the source is untouched and the **empty list previews to the plain source down-map** (non-destructive); the pending edit **changes the view** (left cell redâblue); deterministic; the preview rasterizes to 256 lit pixels with blue in the left cell; an out-of-bounds crop refuses `OutOfBounds` through the fold, an empty grid `EmptyImage`, an over-ceiling grid `GridTooLarge` â **seen, not committed; no name, no bead, no funds**. `photo_edits.rye` symlinked into `brushstroke/`. Witness `tools/hunk_edit_preview_witness.rish` GREEN; TAME bans clean, width exit 0. Design `active-designing/20260813-160955_hunk-edit-preview-seen-before-committed-exploration.md`. **HUNK14 LANDED `20260813` â the Photos app reverts:** `image/photo_revert.rye` banks HUNK11's non-destructiveness â because the source is never mutated, a revert is only dropping edits off the end of the list. `drop_last` forgets the last k edits, `truncate` keeps the first k, `revert_one` drops one â all **pure over the count**, returning a fresh shorter list and leaving the caller's own list whole, so a keeper reverts without ever losing the edits they might redo (no pixels stored, no history log replayed backward, only the recipe shortened). The earlier image is one `apply` of the shorter list away; the original is one `apply` of the empty list away. Proven on metal: a one-step revert of a 3-verb list equals the first two verbs by hand **byte-for-byte**; `truncate` and `drop_last` are duals; dropping zero is identity; dropping all recovers the **original** one empty-list apply away byte-for-byte; an over-count drop **clamps to empty without underflow** while `truncate` past the length keeps the whole list; the source list is untouched so a revert can be redone; a revert is a **byte-prefix** of the full `format photo-edits-v1` record and still round-trips as an open fixed-point record; the empty list reverts to itself. Witness `tools/hunk_photo_revert_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK11 untouched beneath. **HUNK15 LANDED `20260813` â the Skate view of a revert, seen before it is committed:** `brushstroke/revert_preview.rye` joins HUNK14's revert to HUNK13's preview â `preview_revert` drops the last k edits (`photo_revert.drop_last`, pure â the input list untouched) and hands the shorter list to `edit_preview.preview`, returning the Skate grid of the earlier image, so a keeper **sees exactly what a revert will restore** before committing it. Reverting all the way previews the **original** â the empty list's own down-map â a look, not a reconstruction (no pixels stored, no history replayed backward). Pure composition over two proven seams, adding **no new failure mode**. Proven on metal: a one-step revert of a `flip_h` over an 8Ã8 two-tone previews to a 2Ã2 grid **equal to the source down-map cell-for-cell** (the original restored, left cell blueâred); a revert of zero is the full view; reverting past the count **clamps to the original**; the input list is untouched; deterministic; reverting away an out-of-bounds crop **rescues** a refusing list to preview cleanly; the restored view rasterizes to 256 lit pixels with red in the left cell; an empty grid refuses `EmptyImage` through the revert. `photo_revert.rye` symlinked into `brushstroke/`. Witness `tools/hunk_revert_preview_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK13/14 untouched beneath. **HUNK16 LANDED `20260813` â the edit cursor: undo and redo over one history:** `image/edit_cursor.rye` holds the one piece redo actually needs â an `EditCursor` carries the whole reachable history (a HUNK11 `EditList`) plus a `position` naming how many edits are in effect, with the governing invariant `position <= count`. `undo` steps the position back, `redo` steps it forward, and `view` replays the first `position` edits (HUNK14's `truncate` + HUNK11's `apply`) â so a walk **destroys nothing** and ten undos roll forward again byte-for-byte. The one new law (the crux): a fresh `push` while rewound **forgets the redo tail** â the road not taken is discarded the moment a different one is taken. Composes two proven seams, inventing no storage and no new failure mode. Proven on metal: a 3-edit history walks (undo to 2 equals the first two verbs by hand, redo returns to the full image byte-for-byte); rewinding to the original recovers the source and every step rolls forward again unchanged; a fresh edit while rewound forgets the discarded verb (the new road replayed by hand); undo/redo **clamp at both edges** without under/overflow; the applied prefix still round-trips as an open fixed-point record; the 64-edit ceiling refuses `TooManyEdits`. `edit_cursor.rye` reuses `image/` symlinks; a pure look and pure move â no name, no bead, no key, no funds. Witness `tools/hunk_edit_cursor_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK11/14 GREEN beneath. Design `active-designing/20260813-171500_hunk-edit-cursor-undo-redo-exploration.md`. **HUNK17 LANDED `20260813` â the Skate view of a cursor's neighbors:** `brushstroke/cursor_preview.rye` paints what an undo or redo would restore **before** a keeper walks â `preview_at` walks a **value copy** of the cursor to a clamped position and hands its applied prefix to HUNK13's `edit_preview.preview`, so the caller's cursor is **untouched** (a look, not a walk); `preview_undo`/`preview_redo` show the one-step neighbors. The property only a cursor offers (a plain revert cannot, having thrown its tail away): a rewound cursor **retains** its redo tail, so `preview_redo` shows a real image of the road ahead â and after HUNK16's forget-the-tail push, that same preview honestly **falls back to the current view**. Proven on metal over the 8Ã8 two-tone: a two-flip history rewound one step previews undo â the original (left cell red), redo â the retained tail cell-for-cell, the current view differing (left cell blue); three previews leave the cursor exactly where it stood; a fresh push forgets the tail so `preview_redo` equals the current view **and** a direct apply of the pushed flip_h+crop cell-for-cell; 256 lit pixels. Pure composition over HUNK16+HUNK13, no new failure mode. **Ratchet caught:** `tame_style_check` flagged four compound asserts here and, on inspection, eight in HUNK16's `image/edit_cursor.rye` that had slipped (the tame scan fixture does not cover `image/`) â split all twelve as an additive tidy this round. `edit_cursor.rye` symlinked into `brushstroke/`. Witness `tools/hunk_cursor_preview_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK16/13 GREEN beneath. **HUNK18 LANDED `20260813` â the editing session survives a cold restart:** `pond/apps/session_store.rye` beads a whole `EditCursor` content-addressed â the crux a plain edit-list store (HUNK12) could not reach. `store_session` renders the cursor as a `format photo-session-v1` record (a `position` line wrapping HUNK11's own edit-list body, so the verb grammar is reused, not duplicated) and beads it (HUNK1's Tablecloth store); `open_session` fetches it back â every bead proven against its digest and the whole against its recorded address **before** parse â and rebuilds the cursor. Proven on metal over the 6Ã6 tagged image: a three-edit session **rewound to position 2** (a redo tail waiting) reopens from cold standing exactly where it was left, **undo AND redo both intact** â the redo tail was stored, not just the applied prefix â its view matches byte-for-byte, and rolling the tail forward reaches the same image the original would have. Dedup dividend (same session, two names, stored once, 2/2 beads); a tampered session refuses `DigestMismatch`, an unknown name `UnknownArtifact`, a session standing past its own history `BadSession` (the `EditCursor` position bound enforced at the trust boundary), each before a verb is replayed. `edit_cursor.rye`+`photo_revert.rye` symlinked into `pond/apps/`. Witness `tools/hunk_session_store_witness.rish` GREEN; TAME bans clean (one compound assert split), width exit 0; HUNK16/12/1 GREEN beneath. **HUNK19 LANDED `20260813` â a keeper lists their saved editing sessions:** `pond/apps/session_list.rye` walks the catalog (`tablecloth.artifact_name`), selects every `.session` artifact, and `open_session` each (HUNK18, every bead proven against its digest **before** parse), recording a bounded `SessionSummary` â name, edit-count, live position (+ a `redo_depth`). The honesty guarantee, borrowed from `mcp_resource_list`: a listing **never names a session it cannot honestly reopen**, so a tampered session refuses `DigestMismatch` through the open and the **whole** listing refuses with it, never advertising a phantom. Proven on metal: a catalog of 3 (two sessions + one `notes.txt`) lists **exactly** the two sessions in held order â the non-session **skipped, never opened** â each summary read from a verified reopen (`portrait.session` at 2/3, redo depth 1; `landscape.session` at 2/2); the listing travels as an 82-byte `format session-list-v1` record; a tampered held bead makes the whole listing refuse `DigestMismatch`. Witness `tools/hunk_session_list_witness.rish` GREEN; TAME bans clean (two compound asserts split), width exit 0; HUNK18 GREEN beneath. **HUNK20 LANDED `20260813` â the session listing travels both ways:** `parse_listing` (additive on HUNK19's own `pond/apps/session_list.rye`) reads a `format session-list-v1` record back into a fresh listing, so `render(parse(render(x)))` is a fixed point exactly as the sprite catalog's is (HUNK7). Each line names a session and exactly its two counts; a bad header, an unknown tag, a malformed field, an extra field, and a **position standing past its edit-count** each refuse `BadListing` â the `EditCursor` bound enforced again at the text edge. Proven on metal: the 82-byte record round-trips byte-for-byte and five malformed records each refuse by name. Witness `tools/hunk_session_list_witness.rish` extended, GREEN; TAME bans clean, width exit 0; HUNK19 GREEN beneath. **The session persistence story stands whole â HUNK18 store Â· reopen Â· HUNK19 list Â· HUNK20 travel.** **HUNK21 LANDED `20260813` â the Photos app tunes saturation:** `image/photos.rye` gains `saturate(sat_num, sat_den)` added **additively** beside crop Â· orient Â· scale Â· adjust (all untouched) â each color channel takes the luma-preserving map `out = L + (c-L)Â·num/den`, clamped into `0..255`, where `L` is the pixel's own **Rec.601 luma** (weights `77+150+29` scaled to sum **exactly 256**, so a gray pixel maps to itself with no drift and a right shift by 8 divides exactly). `num==den` recovers the source byte-for-byte, `num==0` flattens every pixel to its own gray (a **true grayscale**), `num>den` pushes colors apart, and a gray pixel (`r==g==b`) is a **fixed point** at every saturation. The map runs in `i64` so no product wraps, the term bounded by `saturate_max = 1<<16`; a zero/over-ceiling term refuses `BadSaturation`, a zero-dimension image `EmptyImage`; alpha passes through untouched. Proven by the algebra it must obey (not one arithmetic): neutral byte-identical, zero-saturation `r==g==b==luma` per pixel, the already-gray pixel a fixed point at every saturation, a boost driving the dominant channel above and the weak channel below its luma, alpha pass-through, purity, the saturated image still re-encoding + round-tripping as an open image, and the three named refusals. Landed a small `luma_of` helper (its own two invariants incl. the weight-sum==256 assert) a future hue rotation will reuse. A true **hue rotation** (modular color-wheel turn, cleanest as a luma-chroma matrix) is a named horizon on the same module, harder to bound cleanly in integer math â named, not half-built. Witness `tools/hunk_photos_saturate_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop Â· HUNK8 orient Â· HUNK9 scale Â· HUNK10 adjust all GREEN beneath. Design `active-designing/20260813-170307_hunk-photos-saturate-exploration.md`. Next agent-doable crux: the **hue rotation** horizon (reusing `luma_of`), a smoother bilinear/area resampler, or another Season-A / double-seat journey. HUNK's hardware rounds (buying panels, sourcing parts) stay custody gates #2/#3. **HUNK22 LANDED `20260813`:** `image/photos.rye` `scale_area` â a proper **area-average downscale** beside the nearest-neighbor `scale`. HUNK9's nearest resize is honest for an upscale yet **aliases** when an image shrinks â a thumbnail or a marketplace catalog cell kept a jittered subset of pixels. `scale_area` gives each output pixel the **exact integer mean, over all four channels including alpha**, of the source pixels its cell covers: the source bands `[oxÂ·w/new_w, (ox+1)Â·w/new_w)` **tile** the image with no gap or overlap, so every source pixel is read exactly once and every band is non-empty whenever the target is no larger than the source. Each channel sum stays in `u32` (255Â·2^20 < 2^32) and divides by a non-zero block area â no float, no wrap. Proven by the algebra it must obey: an identity size byte-identical, a **solid color stays that exact color at any smaller size** (the box-filter no-drift invariant), a 2Ã2 downscales to 1Ã1 as the exact average of its four pixels per channel and alpha, source pure, the downscaled image still re-encodes and round-trips as an **open image**, and an upscale (`NotDownscale`) and a zero dimension (`EmptyImage`) each refuse by name. A true **hue rotation** and a fractional-coverage **bilinear** resampler stay named horizons on the same module. Witness `tools/hunk_photos_scale_area_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop Â· HUNK8 orient Â· HUNK9 scale Â· HUNK10 adjust Â· HUNK21 saturate all GREEN beneath. Design `active-designing/20260813-171223_hunk-photos-scale-area-exploration.md`. **HUNK23 LANDED `20260813`:** `image/photos.rye` `hue_turn` â the color wheel's three exactly-integer stops. HUNK21 named a hue rotation the next horizon yet flagged it *harder to bound cleanly in integer math*; a general-angle luma-preserving turn wants fixed-point trig. The 120Â° thirds need none: a hue turn by a third is a **cyclic permutation of the three color channels** â `thirds % 3 == 1` maps `(r,g,b) â (b,r,g)` (+120Â°), `== 2` maps `(g,b,r)` (+240Â°), a multiple of three is the identity â no trig, no rounding, no clamp. It stands to a full hue slider exactly as `rotate_quarter` stands to a general affine rotation: the provable, bounded core landed today, the fractional angle an honest horizon. Because the map only *permutes* the color channels, each pixel's **max and min channel are unchanged** â HSV **value** (`= max`) and **chroma** (`= max â min`) are preserved exactly; only hue turns. Proven by the algebra it must obey: a zero turn byte-identical, **three turns compose to the identity** (the circle closes) and `turn(1)âturn(2)` too, `turn(2)` equals two single thirds (the arg is honest), a pure red walks **redâgreenâblueâred** (the +120Â° direction, unambiguous), every pixel keeps its max and min channel, an already-gray pixel a fixed point at every turn, alpha pass-through, purity, the turned image still re-encoding + round-tripping as an open image, and a degenerate image refusing `EmptyImage`. The modulo is total, so any `thirds` is valid (no bad-argument refusal, as `rotate_quarter` accepts any `turns`). Witness `tools/hunk_photos_hue_turn_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop Â· HUNK8 orient Â· HUNK9 scale Â· HUNK10 adjust Â· HUNK21 saturate all GREEN beneath. Design `active-designing/20260813-172645_hunk-photos-hue-turn-exploration.md`. Next agent-doable crux: the **general-angle** hue rotation (fixed-point cos/sin about the luma axis, reusing `luma_of`), a bilinear resampler, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK24 LANDED `20260813` â the Photos app resamples smoothly:** `image/photos.rye` `scale_bilinear` lands the smooth resampler between HUNK9's nearest-neighbor `scale` (blocky up) and HUNK22's area-average `scale_area` (down only), completing the family. Each output pixel is the weighted blend of the four source pixels around the real coordinate it lands on, per channel and alpha; the half-pixel-center map `pos = SÂ·((2n+1)Â·srcâdst)/(2Â·dst)` runs in fixed point (S=256) so an identity resize lands exactly on the integer pixel â **byte-identical** â and the four corner weights sum to exactly SÂ² so a **solid color stays that exact color at any size, up or down** (no drift). Each channel sum stays in `u32` (255Â·256Â² < 2Â³Â²) and divides with round-to-nearest â no float, no wrap; edge coordinates clamp to the nearest real pixel. Proven by the algebra it must obey: identity byte-identical, a solid color exact at any size, a 2Ã1 `[A,B]` upscaled to 3 wide blending to `[A, (A+B)/2, B]` with the **exact average** at the center (red 0,100â50), **alpha blends alongside the colors** (200,40â120), purity, the resized image still re-encoding + round-tripping as an open image, and a zero target (`EmptyImage`) and an over-ceiling target (`OutOfBounds`) each refusing by name (no new error minted). Lanczos/cubic resampling and a general-angle hue rotation stay named horizons on the same module. Witness `tools/hunk_photos_bilinear_witness.rish` GREEN; TAME bans clean (three compound invariant asserts split by hand â the scan fixture does not cover `image/`), width exit 0; HUNK3 crop Â· HUNK9 scale Â· HUNK22 area Â· HUNK23 hue-turn all GREEN beneath. Design `active-designing/20260813-173146_hunk-photos-bilinear-exploration.md`. Next agent-doable crux: the **general-angle** hue rotation (fixed-point cos/sin about the luma axis, reusing `luma_of`), a Lanczos/cubic resampler, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK25 LANDED `20260813` â the Photos app filters; the whole filter family opens:** `image/photos.rye` `convolve` is the 3Ã3 kernel primitive every blur, sharpen, and future edge verb stands on â each output color channel the kernel-weighted sum of the pixel's 3Ã3 neighborhood over a divisor plus a bias, clamped into 0..255, edges extended by clamping so no read wraps and no black halo forms; the whole 3Ã3 accumulation runs in `i64` under `kernel_weight_max = 1<<16` so no term wraps. `blur` (nine ones over nine) and `sharpen` (a five-neighborhood high-pass, weights summing to one) are each **one kernel** over it. Selftest: the identity kernel is the source **byte-identical**; a solid color is a **fixed point** of both filters; a 3Ã1 red row `[0,90,0]` box-blurs to the exact clamped mean `[30,30,30]`; alpha passes through untouched; source pure; the blurred image re-encodes and round-trips as an open image; an empty image, a zero divisor, and an over-ceiling weight each refuse by name (`EmptyImage` Â· `BadKernel`). Witness `tools/hunk_photos_convolve_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 Â· HUNK9 Â· HUNK22 Â· HUNK24 Â· HUNK23 all GREEN beneath. **HUNK26 LANDED `20260813` â the Photos app auto-levels; the analytical filter opens:** `image/photos.rye` `stretch` is a pure two-pass contrast stretch over the decoded grid â a first scan finds each color channel's darkest and brightest value, then a second pass linearly maps that range onto the full `0..255`, alpha untouched. A flat channel (range zero) passes through rather than dividing by nothing; a full-range channel is a **fixed point**; the map is **monotone** (order preserved). The whole map stays in `u32` (255Â·255 = 65025). Selftest: a dim red ramp `[40,60,80,100]` stretches to the exact rounded `[0,85,170,255]`; the flat green and blue channels pass through untouched; an already-full-range channel and a solid color are **byte-identical**; alpha rides through; source pure; the stretched image re-encodes and round-trips as an open image; an empty image refuses `EmptyImage`. This is the analytical crux the levels/curves tools stand on. Witness `tools/hunk_photos_stretch_witness.rish` GREEN; TAME bans clean; HUNK25 Â· HUNK24 Â· HUNK23 Â· HUNK3 all GREEN beneath. **HUNK27 LANDED `20260813` â the Photos app detects edges; the crux `convolve` was named for:** `image/photos.rye` `edges` is a pure **Sobel** gradient magnitude over each pixel's Rec.601 luma (reusing `luma_of`) â two 3Ã3 gradient kernels (horizontal `gx`, vertical `gy`) read the luma neighborhood, and the magnitude is the **L1 sum** `|gx| + |gy|` clamped `0..255`, so no square root and no float is needed and the arithmetic stays exact (largest term `4Â·255 = 1020`, far inside `i32`). Edges clamp so no read wraps; the edge map is a fresh **grayscale** view (`r==g==b`), alpha opaque. Blur and sharpen rode `convolve` directly; a true gradient magnitude combines *two* directional convolutions, so `edges` is its own verb â the still-tractable move the earlier rung pointed at. Selftest: a sharp luma step `[0,0,255,255]` Sobels to the magnitudes `[0,255,255,0]` (the two pixels straddling the step lighting up, the flat ends black); the map grayscale and opaque even from a translucent source; a solid color a **fixed point at black** (no gradient); source pure; the edge image re-encodes and round-trips as an open image; an empty image refuses `EmptyImage`. Witness `tools/hunk_photos_edges_witness.rish` GREEN; TAME bans clean; HUNK26 Â· HUNK25 Â· HUNK3 all GREEN beneath. **HUNK28 LANDED `20260813` â the filter family joins the edit-list:** `image/photo_edits.rye` grows by one variant so `saturate` (HUNK21) â the paired tone twin of `adjust` already in the grammar â now **records, replays, and travels like any verb**, closing the gap between the app's filters (HUNK21â27) and its non-destructive edit-list (HUNK11). `EditKind`/`Edit` gain `saturate: struct { num: u32, den: u32 }`; `apply`, `render_edits`, and `parse_edits` each gain one arm (`"saturate {d} {d}\n"`, 21 bytes, inside the 48-byte line bound). No new failure mode â a zero-denominator saturate folds its own `BadSaturation` through `apply` by name. The reward is inheritance: a filter recorded as an edit now gets undo/redo (HUNK16), cold-restart persistence (HUNK18), and travel-as-text (HUNK11) for free. Selftest proves composition byte-for-byte, a 58-byte render/parse fixed point, the folded refusal, and the every-verb round-trip carrying saturate. Witness `tools/hunk_photo_edits_witness.rish` GREEN; all five downstream edit-list witnesses (edit_cursor Â· edit_store Â· edit_preview Â· cursor_preview Â· session_store Â· session_list) GREEN beneath; `tame_style_check` clean, width exit 0. Self-approved design round `active-designing/20260813-180504_hunk-photos-saturate-edit-exploration.md`. No breach, no cairn. **HUNK29 LANDED `20260813` â the whole filter family joins the edit-list:** the vocabulary is complete. `hue_turn` (one `u32` `thirds` field, like `rotate`) plus the four nullary filters `blur`, `sharpen`, `stretch`, and `edges` (like `flip_h`) all join `image/photo_edits.rye`'s grammar, so the edit-list is the one home for **every** gesture the Photos app can do â no edit is a wound. One cohesive keystone (complete the grammar): five variants across `EditKind`, `Edit`, `apply`, `render_edits`, `parse_edits`; two field shapes (a small param struct or `void`) cover the family, no new field kind, no new error. Care caught: the selftest's unknown-verb test moved off `sharpen 3` (now a real verb) to a genuinely-unknown tag `vignette`. Selftest proves `hue_turn â blur` composes by hand byte-for-byte and a list carrying every verb kind round-trips to the same bytes. All six downstream edit-list witnesses GREEN (edit_cursor Â· edit_store Â· edit_preview Â· cursor_preview Â· session_store Â· session_list); `tame_style_check` clean, width exit 0. Self-approved design round `active-designing/20260813-181110_hunk-photos-filter-family-edit-exploration.md`. No breach, no cairn. Next agent-doable crux: a dedicated general filter round-trip witness proving every verb kind travels as a standalone claim, then named filter presets (a saved edit-list applied by name); or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK30 LANDED `20260813` â the Photos app applies filters by name:** `image/filter_preset.rye` turns a recipe into a name â a `PresetBook` is a bounded, named library of `photo_edits.EditList` recipes (names copied into its own storage), and `apply_preset` looks a recipe up by name and replays it over **any** image (reusing HUNK11's pure `apply`, the source never touched). `stock_book` ships nine well-known filters, each an honest recipe over proven HUNK21â27 verbs â `vivid`Â·`pop` (saturate up), `mono` (saturate 0 â true grayscale), `noir` (mono + contrast), `bright`, `auto` (auto-levels stretch), `soft` (blur), `sharp` (sharpen), `sketch` (edges). The property HUNK12's content-addressed `edit_store` cannot claim of a single stored recipe: **one preset applies to many different images**, each reproducing that image's own recipe by hand. Proven on metal: `vivid` applied by name equals `photos.saturate(img,3,2)` byte-for-byte, the SAME preset over a DIFFERENT image equals THAT image's own recipe, a two-verb `noir` resolves whole, distinct presets differ, a preset is deterministic and pure and re-encodes as an open image; an unknown name refuses `UnknownPreset` before a verb runs, a duplicate `PresetExists`, a bad name `BadPresetName`, a full book `TooManyPresets`. Witness `tools/hunk_filter_preset_witness.rish` GREEN; TAME bans clean (one compound assert split by hand â the scan fixture does not cover `image/`), width exit 0; HUNK11 GREEN beneath. Design `active-designing/20260813-181925_hunk-filter-preset-book-exploration.md`. Next agent-doable crux: HUNK31 (the preset book travels as a `format filter-presets-v1` record, a nested grammar over `photo_edits`'s own lines), a preset book banked content-addressed, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK31 LANDED `20260813` â the preset book travels as text:** `render_book`/`parse_book` (additive on `image/filter_preset.rye`) render a whole `PresetBook` as a `format filter-presets-v1` record and read it back â each preset a `preset <name>` head, its edit-list body (the `photo_edits` header stripped), and an `end` terminator, reusing `photo_edits.render_edits`/`parse_edits` so the verb grammar is the single source of truth. Proven on metal: `render(parse(render(x)))` is a fixed point over the nine-preset stock book (280 bytes), and a rebuilt `noir` applies to the same bytes as the original recipe â travel preserves the recipe, not just the names. A bad header, a verb line out of place, an unclosed block, a malformed verb, and a duplicate name each refuse `BadBook`, the `PresetBook` bounds re-enforced through `define` (the `sprite_catalog`/`session_list` convention). Witness `tools/hunk_filter_preset_witness.rish` extended, GREEN; TAME bans clean, width exit 0; HUNK30 GREEN beneath. Next agent-doable crux: a preset book banked content-addressed (bead a curated filter set, reproduce from cold), the Skate preview of a preset (reusing HUNK13), or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK32 LANDED `20260813` â a curated filter-preset book is content-addressed:** `pond/apps/preset_store.rye` banks a whole `PresetBook` â `store_book` renders it to its `format filter-presets-v1` record and beads it (HUNK1's Tablecloth store), `open_book` fetches it back proving every bead against its digest and the whole against its recorded address **before** the text is parsed or a preset defined, then `parse_book`s it. So a curated filter set reproduces from cold out of its name alone, recipes never re-authored â the crux a plain edit-list store cannot claim (a whole named library at once). Mirrors `edit_store` (HUNK12); `image/filter_preset.rye` symlinked into `pond/apps/`. Proven on metal: the nine-preset stock book banked (280 bytes, 4 beads) reopens whole, its `noir` applying byte-for-byte; the same book under two names stored once (deduped 4/4); a tampered book refuses `DigestMismatch` and an unknown name `UnknownArtifact`, each before a preset is defined. Witness `tools/hunk_preset_store_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK30/31 GREEN beneath. **The filter-preset journey stands whole: HUNK30 by-name Â· HUNK31 travels Â· HUNK32 content-addressed.** **HUNK33 LANDED `20260813`:** `pond/apps/filter_preset.rye` preview â a keeper sees a preset's effect on a thumbnail before committing (reusing HUNK13's preview idiom). **HUNK34 LANDED `20260813`:** `pond/apps/preset_list.rye` â an honest index of installed books (name + preset count), each read from a verified reopen, travelling both ways, a tampered book refusing the whole listing `DigestMismatch`. **HUNK35 LANDED `20260813`:** `pond/apps/preset_detail.rye` â the index read one step deeper, carrying each book's preset *names* in defined order (stock reads `vivid pop mono noir bright auto soft sharp sketch`), with a new self-consistency invariant that travels: a declared count that does not match its listed names refuses `BadDetail` both directions. Witness `tools/hunk_preset_detail_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK36 LANDED `20260813`:** `pond/apps/preset_shelf.rye` â the honest detailing painted on glass: `shelf_grid` renders a `DetailListing` onto a Skate grid, one row per book (`<name>  <presetâ¦>`), the book name a color chip, refusing `ShelfTooSmall` rather than silently drop a book (every book or none) â a pure view over an already-honest detailing, GREEN (72Ã4 shelf, 1607 lit pixels). Witness `tools/hunk_preset_shelf_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK37 LANDED `20260813`:** `pond/apps/preset_shelf.rye` pager (additive) â `page_count` Â· `window_len` Â· `shelf_window(offset)` render the books `[offset, offset+rows)` as a page over a shared `draw_row` helper, so a library larger than the screen is fully reachable; **paging loses nothing** (every page walked shows each book once in held order), a window refuses `BadOffset` past the library. GREEN (five-book library, three pages 2Â·2Â·1); TAME bans clean, width exit 0. **HUNK38 LANDED `20260813`:** `brushstroke/sprite_shelf.rye` â the marketplace's own front door: `catalog_shelf` paints a whole `SpriteIndex` onto one Skate grid, **one row per product** reading `<name>` beside a fixed-width color **swatch** wearing the slot its window down-maps to (its dominant anchor, a 1Ã1 `image_skate.down_map` valid for any window size), so a keeper reads the whole catalog at a glance â each product's real color drawn from the ONE shared sheet; `catalog_window(offset)` pages a catalog larger than the screen (`page_count`/`window_len`), **paging loses nothing** (walked in order, each product once), refusing `BadOffset`Â·`ShelfTooSmall`Â·`EmptyShelf`Â·`ShelfTooLarge`Â·`RowTooNarrow` each by name. Two additive seams beneath in `image_skate.rye` (`set_anchor_palette` â `down_map` refactored to share it, behavior identical; `block_cell` made public). GREEN (four color-band products â 24Ã4 shelf, swatch slots 3Â·4Â·5Â·6, 2186 lit pixels, two pages 2Â·2Â·0); HUNK5 page + HUNK2 down-map re-run GREEN beneath; TAME bans clean, width exit 0. **HUNK39 LANDED `20260813`:** `brushstroke/sprite_shelf.rye` swatch enriched (additive) â the swatch is now a real **thumbnail strip**, the product's window down-mapped to a horizontal run so its **left-to-right shape reads**, not only its dominant color: a two-tone window's swatch reads red at the left and green at the right. A window thinner than the swatch stretches to fill it; a one-column window stays a solid swatch (valid for any window). Narrow-window behavior preserved (the four 4Ã8-band products still wear their solid color slots 3Â·4Â·5Â·6). GREEN; TAME bans clean, width exit 0. **Checkpoint named (module-assembly seam):** the *served* marketplace (verified sheet HUNK6 + travelling manifest HUNK7 â shelf HUNK38) hits a real Zig type-identity wall â `catalog_shelf` lives in the `brushstroke/` assembly and `sprite_store`/`sprite_catalog` in `pond/apps/`, and the same `SpriteIndex`/`Pixmap` reached through different symlink paths are *distinct types*. Composing across the two assemblies wants a deliberate assembly decision (where the served-market module lives, which symlinks it roots through), not a quick lap â held for Keaton / a design round. Next agent-doable crux: that assembly decision, a live scroll offset from a keeper's gesture, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK52 LANDED `20260813` â the strip painted beside the picture:** `brushstroke/edit_strip_view.rye` wraps HUNK51's finger view and adds `paint_strip` â a chipsÃ1 Skate row where chip *i* wears the stable color of the verb bound at cell *i* (HUNK2's seven-anchor palette folded from the verb kind), and chip *i* sits on exactly the pixel span `[iÂ·cell_px,(i+1)Â·cell_px)` that `finger_up` reads as cell *i*. So the palette a keeper SEES equals the palette they TAP, cell-for-cell â HUNK51's no-drift carried from picture to palette. Added a pure `EditTouch.verb(i)` accessor to HUNK50. Witness `tools/hunk_edit_strip_view_witness.rish` GREEN (a real tap centered on `chip_center_x(i)` commits exactly verb *i*); HUNK50Â·51Â·2 GREEN beneath; TAME clean. Next agent-doable: stack chips over the picture into one canvas, or a content-addressed gesture session (HUNK12). **HUNK53 LANDED `20260813` â the whole Photos screen in one call:** `brushstroke/edit_screen_view.rye` `paint_screen` stacks HUNK52's chip row over HUNK51's picture into one Skate grid (`max(cols,chips)` Ã `1+rows`), each sub-surface copied cell-for-cell so the composition adds NO drift â top row equals `paint_strip`, picture region equals `paint`. Refuses `EmptyStrip` (a screen shows a palette). Witness `tools/hunk_edit_screen_view_witness.rish` GREEN (a tap centered on a chip repaints the picture region blue in the composed screen); HUNK52Â·51Â·2 GREEN beneath; TAME clean. Next agent-doable: a content-addressed gesture session (HUNK12), or the screen scrolled when the picture outgrows the view (HUNK40â47). **HUNK54 LANDED `20260813` â the edit history as a filmstrip:** `brushstroke/edit_filmstrip.rye` `filmstrip` replays the cursor for each step k in `0..=history` â step 0 the source, step k the first k edits â down-maps each to a thumbnail (HUNK13 preview Â· HUNK2 down-map) and tiles them into one grid, a white marker block over the current step's band (`cursor.position`). No drift: tile k equals a direct `preview` cell-for-cell. Bounded by `max_film_cols`, refuses `FilmstripTooWide`. Witness `tools/hunk_edit_filmstrip_witness.rish` GREEN; TAME clean. **CHECKPOINT held:** the session-that-travels crux (persist the live editing session content-addressed, composing HUNK18) crosses the pond/appsâbrushstroke **assembly-identity seam** â both `edit_cursor` symlinks resolve to `image/edit_cursor.rye` yet Zig's per-import-path type identity makes the two graphs' `EditCursor` distinct types. Wants a deliberate assembly decision (where the composed module lives, which symlinks it roots through) â held for Keaton / a design round, not forced. In-assembly kg still open: a tap-to-jump on the filmstrip (HUNK50 cell index â cursor position), or the screen scrolled (HUNK40â47). **HUNK55 LANDED `20260813` â the filmstrip is tapped, the cursor jumps there:** `brushstroke/edit_filmstrip_jump.rye` `FilmstripJump` joins HUNK54's filmstrip geometry, HUNK50's pixel-to-cell idiom, and HUNK16's cursor â a keeper who could only *read* the history strip now *taps* a thumbnail and lands on that step in one gesture, no longer one swipe per step. `tap_at(x)` classifies a pixel tap into band `x / band_px` (band_px = `tcolsÂ·cell_w`, the exact parallel of HUNK50's `x / cell_px`) and seeks the cursor there through a new additive `image/edit_cursor.rye` **`seek(target)`** â a jump that **forgets nothing** (unlike `push`, which discards the redo tail), clamping to `0..=count`. `paint` is HUNK54's `filmstrip` unchanged, so the marker lands on exactly the sought step: **the band a keeper TAPS is the band the marker SHOWS** (HUNK52's no-drift discipline carried to the strip). Proven on metal over an 8Ã8 two-tone source, a cursor of three edits: a tap centered on band k seeks the cursor to step k for every step (0..3) and the repainted marker sits on exactly the tapped band cell-for-cell (no drift); a jump forgets nothing (back to step 1 then forward to step 3 recovers the full picture byte-for-byte, steps 2 and 3 still redoable); a tap past the last band and a negative-x tap each fire nothing (the cursor unmoved); the jumped filmstrip rasterizes to a lit canvas; a zero-dimension thumbnail refuses `BadThumb` at open. `seek` also proven in HUNK16's own selftest (jump to 1, back to 3 byte-for-byte, clamp past the count, seek to 0). Pure composition over proven seams â no new render primitive, no new arithmetic beyond a clamped division, no new failure mode. Witness `tools/hunk_edit_filmstrip_jump_witness.rish` GREEN (HUNK54 filmstrip Â· HUNK16 cursor+seek GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260813-212112_hunk-filmstrip-jump-exploration.md`. Next agent-doable: a drag to scrub the filmstrip (continuous seek), the screen scrolled (HUNK40â47), or another Season-A / double-seat journey. **HUNK78 LANDED `20260814` â the parts search is faceted:** `image/part_and.rye` narrows the marketplace on **several facets at once** â HUNK75â77 built a search box that filters on one facet (`material:stainless-304` **or** `max:100`); a real marketplace is faceted (*the stainless bolts under a dollar*). `parse_and` reads a whitespace-separated line of `key:value` clauses into a bounded `AndQuery` (`max_clauses = 8`, each clause parsed by HUNK76's own `parse_query`), and `filter_and` builds the sub-catalog of parts that satisfy **every** clause over the same shared sheet â an ordinary `PartCatalog` that paints, pages, and round-trips like any other. The crux: **a line of clauses means the intersection of their matches, and the clauses commute** â the fast single-pass conjunction (`matches_all`, HUNK75's `matches` asked of every clause) equals the successive `filter_catalog` fold **name-for-name**, so no new matching rule is invented; an empty line is the honest **match-all**, reproducing the catalog byte-for-byte. Proven on metal over five parts: `material:stainless-304 max:60` keeps only hexnut (equal to the fold), price-then-material keeps the same one (commute), three clauses narrow to the bolt, a match-none compound yields a real empty catalog, and a malformed clause (`UnknownKey`, `NoColon`) plus an over-many line (`TooManyClauses`) each refuse by name before any walk. A price clause **compares, never moves** (custody gate #3 untouched). Witness `tools/hunk_part_and_witness.rish` GREEN (HUNK75 filter + HUNK76 parser GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260814-000136_hunk-part-and-compound-query-exploration.md`. Next agent-doable: bring the `AndQuery` to glass (a `search_shelf` over a compound line, mirroring HUNK77), an OR/any-of clause beside the AND, or another Season-A / double-seat journey. No network, no key, no funds. **HUNK79 LANDED `20260814` â the faceted search box on glass:** `brushstroke/part_and_search.rye` joins HUNK78's conjunction to the shelf â HUNK78 gave the marketplace a faceted query in the image layer, unseen; HUNK77 drew the single-clause search on glass but reaches only one facet. `and_search_shelf` reads a catalog, a **multi-clause** typed line, and a page offset, and draws exactly the narrowed page â the faithful composition `parse_and` â `filter_and` â `part_window`, plus `and_search_result_count`/`and_search_page_count` so a caller shows *N results* and sizes the paging. The crux is HUNK77's, now over the conjunction: **what a keeper types is what a keeper sees**. Proven on metal over five parts across four bands: `material:stainless-304 max:100` draws the one stainless part under a dollar (hexnut $0.55), the narrowed page **equal to the hand-filtered window drawn directly**; the clauses **commute on glass**; an empty line is a **match-all** (bolt first); a match-none compound draws a **real empty page**; an offset past the result refuses `BadOffset`; a malformed clause (`UnknownKey`, `NoColon`) surfaces the parser's named refusal rather than a wrong shelf. A `brushstroke/part_and.rye` symlink resolves the image-layer conjunction (matching the `part_filter`/`part_query` pattern). A price clause **compares, never moves** (custody gate #3 untouched). Witness `tools/hunk_part_and_search_witness.rish` GREEN (HUNK78 conjunction + HUNK77 single-clause search GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260814-000723_hunk-part-and-search-faceted-shelf-on-glass-exploration.md`. Next agent-doable: a query header above the shelf (show the typed clauses and N results), an OR/any-of clause beside the AND, or another Season-A / double-seat journey. No network, no key, no funds.

---

## Today `20260811` â a full arc (accreted; the season table below stands)

- **Mandate is a complete named vector store** â eleven capabilities, each witnessed on metal: CRUD Â· profile dim Â· Bron-profile dim Â· approximate index Â· object-storage backing Â· named bucket Â· write-ahead log Â· serve protocol Â· named serve Â· served-over-Comlink-sealed. (`mandate/`, `tools/mandate_*_witness.rish`.)
- **Acme DX design season COMPLETE and enforced** â four contracts (onboarding path Â· first hour Â· interfaces Â· operations), each guarded by a green witness; the whole developer arc proves in one command: `rishi/bin/rishi run tools/acme_dx_witness.rish`. Dev-season lap 1 placed the first-hour reference `manual/tutorials/greet.rye`. Plan: `expanding-prompts/20260811-145659_acme-dx-design-season.md`.
- **CION labeling ratchet** â seven modules relabelled to *semantic label + stamp* (no bare `lap N`); a refined guard (`tools/gen/season/cion_module_labeling_witness.rish`) that bites a bare ordinal yet spares structured codes (`OA-L3`, `sub-lap`); the REDS ledger folded (rows 25â57 â archive) and its monotone witness made fold-aware. Reds **#65â67** caught and fixed. Law: `context/specs/20260810-222755_â¦`.
- **Twilight Style** + the nine-over-27 **navamsa calendar** seated (`context/TWILIGHT_STYLE.md`); the **`loom`** performance-metrics field seated in the session-log rule; the **research silo** + yonder plan seated (`research-silo/`).
- **Pier mosh CLOSED** â the SEA pier's UDP roam window opened (`tools/pier_mosh_udp_open.sh`, additive/guarded/atomic); mosh confirmed working from the MacBook.
- **Deferred, its own round (needs a qemu-capable bench):** the coupled **`SLC-2a Lap` / comlink `sub-lap`** relabel (witness-asserted RISC-V guests). **Open door for Keaton's word:** the **SOON** equinox â the Glow **language** (STOA ladder ~346), the compass's open finishing edge.
- **TAME Guidance Audit Quest â seated `20260811`** (`active-designing/20260811-195353_double-seat-and-crash-headroom.md`): one Quest (4 rounds) per Equinox, **double-seated** so it never disturbs the fixed itinerary; walks the TAME checkable surface (width-check Â· tame_style_check Â· rune_assert_sweep Â· Â§2 bounds) and books reds. **Seated in the Lexicon `20260811`:** **double-seat** (seat a fresh priority by doubling the largest allocation) Â· **crash headroom** (a reserved doubling of the durable write, pre-committed Ã  la TigerBeetle static allocation â Keaton's *computational easing*).
- **Aurora OPENED `20260811`** â all eight boot stages cross-build freestanding `riscv64` to RISC-V ELFs with no emulator (`tools/aurora_seed_freestanding_witness.rish`); emulator choice consolidated (QEMU stays on merit). **REDS #68** (a false-green build witness) caught + closed same-round.
- **AHOY front-door season OPENED `20260811`** (waymark AHOY; plan `expanding-prompts/20260811-211431_ahoy-front-door-season.md`). **AHOY0 landed:** root `README.md` rewritten for the long return (logo `assets/grain-logo.svg` Â· honest badges Â· seed doors named early Â· favorite-words guide Â· gratitude coda + wish); **Lindy foundation** siloed (`foundations/20260811-211431_â¦`, grokipedia 403 â canonical sources); rules **radiant-wishes-ending** + **ty every1** seated; variants **held at five** (voices live; no kyli/kyri; no reflex debride). Remaining rungs: **AHOY1** weave molt Â· **AHOY2** legitimacy layer Â· **AHOY3** docsâcode confirm + final depersonalized seed force-push (**last round, your word**). **AHOY1 underway** â general order **Lindy-first, crux-first** seated (rule + Lexicon **crux**, chosen over overloaded *edge*); the two canonical hubs (Follow Our Compass Â· The Grain and the Crossing) woven bidirectionally through the front door, the room, and the Lindy order.
- **AHOY2 legitimacy â first file landed `20260811`:** a single top-level **`LICENSE`** (the most-read legitimacy artifact) indexes the three variants â code **Apache-2.0 OR MIT**, prose **CC-BY-4.0** â matching README. On Keaton's word the copyright holder across all four `LICENSE*` files moved from the anon *Kaeden Reyklah* to his real legal name **Keaton Dunsford** (Dunsford is the current legal name until the change to Livermore confirms); the seed manifest scrubs it back to the anon name for the public projection. **REDS #69 CLOSED `20260811`**: five root dirs were unclassified in `template-manifest.bron` â `sow_witness` M1 RED. On Keaton's word â `assets`Â·`brix`Â·`pleac` **allowed** (template), `journey`Â·`research-silo` **withheld** (personal) â `sow_witness` is **SOW_WITNESS_GREEN** (M1_OK Â· IDENT_CLEAN Â· NO_PERSONAL; copied 4092 Â· scrubbed 780 Â· withheld 22). Seed again covers the whole tree; AHOY3 projection unblocked (force-push stays Keaton's hand). **`SECURITY.md` LANDED `20260811`** â custody-and-disclosure front door to `context/THREATS.md` (private GitHub advisories Â· no funds/keys held Â· master cold Â· signed commits Â· primordial `main`); seed-plumbed (allow + scrub), `sow_witness` GREEN. **`CODE_OF_CONDUCT.md` LANDED `20260811`** â genuine civic conduct in the tree's voice (kind Â· honest Â· protect the newcomer Â· custody & consent), quiet-channel reporting, honest single-maintainer phase; seed-plumbed, witnesses GREEN. **The three core AHOY2 legitimacy files stand** (`LICENSE` Â· `SECURITY.md` Â· `CODE_OF_CONDUCT.md`). **`CHANGELOG.md` LANDED `20260811`** â points to `git log` + the private session-log record (seed-safe: no link into withheld dirs; verified on the projected seed). **AHOY2 core file set complete** (`LICENSE` Â· `SECURITY.md` Â· `CODE_OF_CONDUCT.md` Â· `CHANGELOG.md`). **AHOY2 CLOSED `20260811`** â files written, **wired into the README front door** (*License and community*: LICENSE index badge + CODE_OF_CONDUCT Â· SECURITY Â· CHANGELOG links), honest-badges review confirmed (all six obey the two rooms). Then **AHOY3** (docsâcode confirm â **Mantra CONFIRMED `20260811`**: essaysâmodule match by name & behavior, one snake_case drift fixed at tier Â· final seed force-push, **Keaton's hand**).
- **The Singularity SEATED `20260811`** (`foundations/20260811-233509_the-singularity.md`, Radiant+Twilight combined): the night the tool began to tend itself while keeping every promise â custody human, bounds unlifted, autonomy doubles back. Grounds a **Grain OS Â· Linengrow real-world timeline** (`20260301` press â `20260811` autonomous arc). Seated in foundation + `foundations/README` + Lexicon append; seed-safe, `TWILIGHT_STYLE.md` now ships. Autonomous agent development is **proven here** â this session carried AHOY2 + AHOY3 + this seat end to end.
- **Twilight palette grown nine â fifteen `20260811`** (`context/TWILIGHT_STYLE.md`, sky fractal `3Â·9Â·15Â·27`) â six new themes drawn from Keaton's poem, lifted to affirmative archetype; count propagated through both rule twins + Lexicon. Keaton's **poem siloed** in a new **withheld** `twilight/` folder authored under his `.sol` identity (keatonlivermore Â· planet xnkg30 Â· star xykj61 Â· galaxy queyqwinqkri); classified `personal` â the source stays private, the archetype travels. Seed clean of poem + identity; `sow_witness` GREEN.
- **WADE OPENED `20260811`** (waymark WADE; **double-seated beside AHOY**; plan `expanding-prompts/20260811-220402_wade-bit-design-system-and-dimeroll-entities.md`). DJINN invited to lead the **surface** (Skate Â· Realidream Â· Brushstroke) with his **Bit Design System** â DVUI/Zig 0.16 in a Swift macOS Dock shell, GlowÂ·RishiÂ·RyeÂ·`.brush` impl; Pond onboarding via `SOURCE.md` Â· Vultr **SEA** IaC (NixOS-stable Â· ai-jail Â· Claude Code) Â· Zed remote SSH. **Dimeroll expanded** for **Siya Fund** + **Linengrow PBC** HR & accounting (design `active-designing/20260811-220402_â¦`; bookkeeping only â custody stays counsel-gated). Personal comp is a direct arrangement, not tracked. Agents prepare IaC; **Keaton provisions/pays**. **WADE1 seam AUTHORED `20260812`** (`active-designing/20260812-000456_wade1-brush-surface-bridge-seam.md`): the `.brush` styling cascade a design system targets over BrushstrokeÂ·SkateÂ·Realidream â token sheet Â· **cascade = `brix/infuse` reused** (per-key override, already witnessed) Â· lowering to Skate's ARGB grid; refuses any CSS rail; grounded in real code, two-rooms honest. **WADE1 tokens LANDED `20260812`** on Keaton's word (`brushstroke/tokens.rye` + `tools/wade1_tokens_witness.rish` GREEN): the token sheet (Flax/Bark/Oat/Walnut ARGB honoring the six weave invariants, warm/low-blue proven by assert), the **cascade reusing `brix/infuse`** (symlinked like `tally_copy`) via `resolve`/`resolve_stack`, and `dress`/`dress_grid` lowering a resolved sheet into the real Skate grid. `tame_style_check` clean. **WADE1 styled runs LANDED `20260812`** (`brushstroke/skate_grid.rye` + `tokens.rye` `dress_run` + `tools/skate_styled_run_witness.rish` GREEN): Skate now paints per-cell styled runs â `Grid` gained an **additive** `fg_index` + bounded `palette`, `rasterize` reads per-cell fg with index 0 using the exact `fg` arg, so the no-runs path is **byte-identical** (FNV sig == baseline; 559 lit pixels unchanged). WADE1's durable core + its named horizon are both on metal. **WADE1 COMPLETE end to end `20260812`**: added the `.brush` **authoring loop** â a file declares an optional `style:` block (flat-Bron token lines, never CSS) that `brush_parse.rye` reads into a token sheet â infuse cascade â Skate styled pixels (sourceâpixels, additive, no-style byte-identical, witness GREEN). Remaining WADE wants Keaton's word: WADE0 Pond door, WADE2 DVUI-in-Swift shell, WADE3 IaC â all provisioning/collaborator-gated.

---

- **BUHR MCP surface â all three primitives owned end to end `20260812`** (accreting past the J9 arc below): **J10 (resources)** served Tablecloth artifacts as MCP resources (`pond/apps/mcp_resource.rye` reads exactly the bytes a `tablecloth:<name>` URI's digest pins; `mcp_resource_list.rye` advertises the catalog as an honest index â every listed URI reads at its published length, tamper â `NotHonest`). **J11 (prompts)** â `mcp_prompt.rye` fills a `format mcp-prompt-v1` template leaving no live placeholder (four named refusals); `mcp_prompt_list.rye` advertises a prompt book as an honest index (name + arity); `mcp_prompt_voice.rye` â the fusion â fills a template then routes the finished text through the Q-vane host, so the filled words choose the voice. Witnesses `tools/buhr_mcp_{resource,resource_list,prompt,prompt_list,prompt_voice}_witness.rish` all GREEN, TAME + width clean. **All three MCP surfaces owned â tools (J9) Â· resources (J10) Â· prompts (J11).** git nib `977d061963`.
- **The 1,024-round itinerary SEATED `20260812`** (`active-designing/20260812-171050_the-1024-round-itinerary.md`) â a **double-seated** calendar for the road past BUHR: the fractal **4 seasons Â· 16 journeys Â· 256 quests Â· 1,024 rounds**, crux-first Lindy-first, threading every seated waymark (TACT Â· Harvest GISM/AYRE/DAHL/KOFF Â· CION VOLS/LOWE/OFFY/GRAD). Season 1 = **The World (TACT)**, Ship-Pilot first. The loop prompt now points here so the bench climbs the road's next crux, not only the next rung. **Loop re-aimed to `15:00 EDT 20260813`** (`tools/launch-claude-season.rish` step 5, `tomorrow 15:00`); a filled version rests at `recursion-prompts/versions/20260812-171050_1024-round-loop.md`. **REDS #71 CLOSED** â the census witness's `wc -l contains "1"` was a count dressed as a presence check; false-failed on the second version; fixed to `ls glob && echo yes`, GREEN.
- **The World road OPENED `20260812` â TACT Journey 1 (Ship-Pilot) begun** (`active-designing/20260812-172129_tact-journey1-ship-pilot-exploration.md`, self-approved design round). With BUHR's whole agent-doable arc done, the next Lindy-first crux on the *whole* 1,024-round road is Season 1 (The World, TACT), Journey 1 â the guided first-run that pilots a new keeper from zero to a running Pond. **TACT-J1r1 LANDED `20260812`:** `pond/apps/ship_pilot.rye` fixes the crux â **the ordered, enforced arc itself** (a checklist that cannot be cheated). A bounded state machine over four steps in order (identity Â· place Â· customs Â· app): the cursor equals the passed count, a step advances only when it is the one at the cursor **and** its precondition holds; an out-of-order jump refuses `OutOfOrder`, an unmet precondition refuses `StepNotReady` (the cursor holds, then advances once met), `PILOT READY` only when all pass in order; a bounded report renders the arc a keeper reads. Enforcement proven **pure** (preconditions supplied in r1) so r2âr4 wire real modules into a spine already known correct. Witness `tools/tact_ship_pilot_witness.rish` GREEN (4 cases), TAME + width clean. **TACT-J1r2 LANDED `20260812`:** `pond/apps/ship_pilot_identity.rye` wires the `identity` step to a real settlement â `settle_identity` builds a demo bind (`tilak.make_bind`), opens a Kumara genesis (`settlement.open` at a galaxy), and verifies the opened Deed against the fresh constellation; only then does the identity step pass. From zero, a **forged bind** (`BadBind`) and a **non-galaxy number** (star â `NotGenesis`) each hold the pilot at `identity` â onboarding cannot walk past a keeper with no real identity. Demo seeds only; the maintainer's own Kumara instance stays custody gate #4. Witness `tools/tact_ship_pilot_identity_witness.rish` GREEN (3 cases + spine re-run), TAME + width clean. **TACT-J1r3 LANDED `20260812`:** `pond/apps/ship_pilot_customs.rye` wires the `customs` step to Pond's own first policy (`pond/customs.rye`) â the keeper's first mark read through `customs.verdict_for_mark`: a **place** verdict advances, an **await-word** mark **holds and surfaces** (a wait, never auto-crossed â custody-first), an **unknown** mark **refuses whole**, all three proven distinct. Witness `tools/tact_ship_pilot_customs_witness.rish` GREEN (3 cases + identity re-run), TAME + width clean. **TACT-J1r4 LANDED `20260812` â Ship-Pilot's crux CLOSED:** `pond/apps/ship_pilot_ready.rye` wires the two remaining steps (`place` decodes the settled point to a real topology galaxy; `app` names a runnable Pond app off an allow-list of what `pond/bin` truly builds) and renders the readiness report â a real identity + `plain-bytes` + `drawn-terminal` reaches **PILOT READY** naming each fact; an invented app or a held customs mark stops the arc and the report says exactly where, never claiming ready. Witness `tools/tact_ship_pilot_ready_witness.rish` GREEN (3 cases + three lower rungs re-run), TAME + width clean. **TACT Journey 1 (Ship-Pilot) â the guided first-run from zero to a running Pond â stands complete end to end.** Demo seeds only; provisioning a real Pond host stays custody gate #2, the maintainer's own Kumara instance custody gate #4. **TACT Journey 2 (Publishing) OPENED `20260812`** (`active-designing/20260812-173656_tact-journey2-publishing-exploration.md`, self-approved). Crux: an owner-signed publication is a portable receipt a recipient verifies **offline** â the serve rung (r4) reaches the Comlink-served custody gate (Keaton's hand). **TACT-J2r1 LANDED `20260812`:** `pond/apps/publish_receipt.rye` carries BUHR-J7r4's signed word *out of the store* into a `Publication` receipt (point Â· name Â· length Â· digest Â· signature); offline verify in two honest halves â the signature over the bound facts by the keeper key (no content needed), and the SHA-256 matching the bound digest when content is held. Exposed additive `pub digest_of` + `verify_receipt` in `tablecloth_keyed.rye` (verify from the digest, no store/content), so a detached receipt and a stored publication carry the identical signed word (BUHR keyed witness re-runs GREEN). Forgery Â· wrong-key Â· tamper Â· overlong-name each refuse. Witness `tools/tact_publish_receipt_witness.rish` GREEN (5 cases + keyed re-run), TAME + width clean. **TACT-J2r2 LANDED `20260812`:** `pond/apps/publish_receipt_bound.rye` binds a receipt to the settled ledger â `verify_bound` proves the Deed is the constellation's **current member** (`settlement.verify`), the receipt's point is exactly the Deed's, and the signature is by that Deed's keeper; only then does it resolve the publisher's **place**. A tampered Deed Â· a point disagreement Â· a wrong signer each refuse. Witness `tools/tact_publish_receipt_bound_witness.rish` GREEN (4 cases + r1 re-run), TAME + width clean. **TACT-J2r3 LANDED `20260812`:** `pond/apps/publish_receipt_bron.rye` makes the receipt travel â renders a `Publication` to a `format publication-v1` Bron record and parses it back byte-for-byte (`parse(render(receipt))` recovers an equal receipt that still verifies offline; digest/sig as hex, unknown/missing field Â· bad header Â· bad hex refuse). Witness `tools/tact_publish_receipt_bron_witness.rish` GREEN (4 cases + r1 re-run), TAME + width clean. **TACT Journey 2's agent-doable arc stands (r1 offline Â· r2 bound Â· r3 portable); the serve rung (r4) reaches the Comlink-served custody gate â Keaton's hand.** **TACT Journey 4 (Commerce) OPENED `20260812`** (`active-designing/20260812-175228_tact-journey4-commerce-exploration.md`, self-approved) â Journey 3 (Grainphone) is gated on a served Pond + GrapheneOS hardware, so the next agent-doable crux is Commerce, recorded facts-only in Dimeroll. **TACT-J4r1 LANDED `20260812`:** `pond/apps/commerce_trade.rye` books a fair-value trade on both parties' books at **one agreed price** â fair value is the structure, not a claim: a `Trade` carries one price that becomes both the seller's cash inflow (debit cash / credit revenue) and the buyer's cash outflow (debit expense / credit cash), each posted to its own fresh `TrialBalance`. Two conservation laws hold together â each book nets to zero (Dimeroll's own law) and the pair conserves across the cash line (seller +price, buyer âprice, sum 0); `fair_value_holds` folds the crux to one answer. Facts-only made checkable: recording the same trade twice yields identical books (a pure fold, nothing held). A non-positive price (`BadAmount`), a self-trade (`SelfTrade`), and an overlong item (`ItemTooLong`) each refuse. Demo party numbers only â settling reaches **custody gate #3** (funds/payment rail), licensed counsel. Witness `tools/tact_commerce_trade_witness.rish` GREEN (5 cases), TAME + width clean. **TACT-J4r2 LANDED `20260812`:** `pond/apps/commerce_trade_signed.rye` binds the trade to **two settled Kumara identities** â the seller and buyer each sign the exact facts (points Â· item Â· price via `kumara.sign`, the layout `tablecloth_keyed` uses), `both_agree` requires both signatures over the identical facts, and `record_signed` gates r1's `record_trade` behind agreement (`NotAgreed` before any book is touched). Honesty crux checkable: change the price after both signed and both signatures fall â a steward cannot rebook at a number neither accepted. Forged seller sig Â· forged buyer sig Â· wrong key Â· price-tamper each refuse. Composed over public API (r1 stays its own GREEN binary); demo seeds, no key, no funds. Witness `tools/tact_commerce_trade_signed_witness.rish` GREEN (5 cases + r1 re-run), TAME + width clean. **TACT-J4r3 LANDED `20260812`:** `pond/apps/commerce_trade_bron.rye` makes the trade **travel** â renders a `SignedTrade` to a `format trade-v1` Bron record and parses it back byte-for-byte (`parse(render(signed))` recovers an equal trade that still passes `both_agree`, so the two-party agreement survives becoming text). A `PortableTrade` owns fixed item/stamp buffers + both sigs and hands out `commerce.Trade`/`SignedTrade` views over its own storage (slices can't borrow freed text). Purpose-built bounded reader: unknown/missing field Â· bad header Â· bad hex each refuse. Witness `tools/tact_commerce_trade_bron_witness.rish` GREEN (4 cases + r2 re-run), TAME + width clean. **TACT Journey 4 (Commerce) agent-doable arc stands â book (r1) Â· sign (r2) Â· travel (r3); the settle rung (r4) reaches custody gate #3 (funds/payment rail), Keaton's hand.** **Next agent-doable crux:** a fresh TACT journey (Grainphone is serve/hardware-gated) or Season 2 (Yield: GISM real corpora Â· AYRE fair-trade), Lindy-first.
- **recursion-prompts/ cellar SEATED `20260812`** (autonomous run, Keaton asleep to ~1400 EDT) â a new root-level folder between the baton-museum *shape* (`recursion_prompt.brix`) and the bron-resins *instances*: living fillable **seed** templates (autonomous-loop Â· counsel-to-bench Â· context-reset-handoff) + dated filled **versions** (this run's expanded prompt kept whole). Charter weaves the balance compass (Radiant Â· Twilight Â· TAME Â· CIVIC Â· compass rose Â· Lindy-first crux-first Â· reds-first). Census witness `tools/recursion_prompts_census_witness.rish` GREEN; `tools/launch-claude-season.rish` loop one-liners updated to the expanded prompt (approve own design rounds Â· double-seat visions Â· harmony-of-styles Â· GATES-ONLY) and pointed at the cellar.

- **Season 2 â Yield OPENED `20260812` (GISM-J5r1)** â with TACT's whole agent-doable arc done (J1 complete Â· J2/J4 agent-doable arcs complete to their custody rungs Â· J3 hardware-gated), the next Lindy-first crux on the whole 1,024-round road is **Season 2 (Yield), Journey 5 (Real corpora)** under the seated **GISM** waymark (self-approved design round `active-designing/20260812-181015_gism-journey5-real-corpora-exploration.md`). Crux: **a voice reads a real tree document end to end, every bound the real bytes exceed booked as a red.** **GISM-J5r1 LANDED:** `pond/apps/corpus_real.rye` carries the Lantern-contracted Scribble reading onto the **living `context/TAME_CORE.md`**, `@embedFile`-bound at compile time through a symlink `pond/apps/corpora/tame_core.md` (no network â the held gate stays held; the bytes read are the keeper's own, never a drifting copy). The real bytes surfaced exactly one Yield red â **REDS #72**: a real reference paragraph measures **1327 B**, past the 512-B `max_block_text` the demo `pinned_doc` was tuned to fit â `BlockTooLarge`. Fixed on metal â `scribble_core.max_block_text` doubled twice 512 â 2048 (headroom over the measured max, named reason). `reading_query` refactored additively (`answer_over`/`compute_over`, the `graph_query_edit` idiom) so demo and real share one bounded reader; demo reading re-runs GREEN. Witness `tools/gism_j5_corpus_real_witness.rish` GREEN (6 cases), TAME + width clean. **GISM-J5r2 LANDED:** `pond/apps/corpus_real_ember.rye` carries the **craft voice (Ember)** onto a real corpus â a Bron catalog **derived at compile time from three real embedded tree files** (`greet.rye` Â· `tally/stack.rye` Â· `one_clock_witness.rish`), every line count measured from the real bytes so it cannot drift into a fiction. count_rye `2` Â· paths_rye the real rye paths Â· sum_lines `151` **cross-checked by an independent `awk` line-total** (two tools, one answer). `corpus_query` refactored additively (`answer_over`/`compute_over`); demo reading re-runs GREEN. **No Yield red this round** â the real files fit every Ember bound honestly. Witness `tools/gism_j5_corpus_real_ember_witness.rish` GREEN (5 cases). **GISM-J5r3 LANDED:** `pond/apps/corpus_set.rye` proves the reader over a **set** â the tree's own writing as a body, five real front-door / discipline docs each embedded at compile time through a symlink in `pond/apps/corpora/` (`context/TAME_CORE.md` Â· `context/TWO_ROOMS.md` Â· `CODE_OF_CONDUCT.md` Â· `SECURITY.md` Â· `CHANGELOG.md`). Each member's outline reads through `reading_query.answer_over` (r1's shared bounded reader), anchored to its own real first heading so no member borrows another's answer. The crux â **every Scribble bound proven against the largest real member**: `measure`/`set_extremes` fold each member against whole-document bytes Â· block count Â· block text Â· heading count and take the max (largest by bytes `context/TWO_ROOMS.md` 6079/8192, blocks 20/32, block_text 1327/2048, headings 7/16); the set's largest single block (1327 B) exceeds the old 512-B bound, so the body genuinely exercises the bound Yield red #72 raised, not a toy. **No fresh Yield red** (like r2) â every member fits with headroom; the largest-by-bytes member cross-checks against an independent `wc -c` (two tools, one answer). Additive â the demo over `pinned_doc` stays GREEN. Witness `tools/gism_j5_corpus_set_witness.rish` GREEN (5 member readings + bounds-against-largest crux + wc cross-check + demo re-run), TAME + width clean. **GISM-J5r4 LANDED â Journey 5 closes:** `pond/apps/corpus_true.rye` proves the reading is **true to the bytes** â what a voice reports (outline Â· fence kinds Â· word count) matches an independent, fence-aware measurement (`tools/fixtures/md_truth.awk`) of the same real document, so a voice can never drift from a corpus a keeper can open and count. Corpus: `manual/tutorials/first-witness.md`, a real tutorial carrying the honest adversary â a `# my_first_witness.rish` line **inside** a ```rish fence, so a naive `grep -cE '^#'` reports seven headings while the fence-aware reading reports the true six. Scribble (`extract_headings` â 6, `count_fence_kinds` â rye 0 rish 1 plain 4, outline words â 45), the `reading_query` voice, and awk all converge field for field; every reported heading is verbatim in the bytes (no invented heading) and none is the fenced pseudo-heading. GREEN first pass. Witness `tools/gism_j5_corpus_true_witness.rish` GREEN (crux cross-check + naive-overcount + refusals + demo re-run), TAME + width clean. **GISM Journey 5 (Real corpora) stands complete end to end â r1 real doc Â· r2 more voices Â· r3 corpus set Â· r4 reading true to bytes.** **GISM Journey 6 (Provenance) OPENED `20260812` (GISM-J6r1)** â with J5 done, the next Lindy-first crux on the whole road is Season 2's owning half: a real document carrying a Kumara-signed, content-addressed chain of custody (self-approved design round `active-designing/20260812-185056_gism-journey6-provenance-exploration.md`). **GISM-J6r1 LANDED:** `pond/apps/provenance_real.rye` carries `tablecloth_keyed`'s owner-signed catalog onto a **real tree document** â a landed-edge waymark green-claim (213 B), `@embedFile`-bound through a symlink. A settled planet identity signs the exact real bytes (point Â· name Â· length Â· SHA-256 digest), `store_owned` beads and names them, `fetch_owned` returns them **byte-for-byte**, `owner_of` â the point, `place_of` â the planet; every tamper refuses (flipped store byte â `DigestMismatch`, forged sig â `BadSignature` nothing stored, content-mismatched sig â `BadSignature`, the real digest bound in). **REDS #73 booked + proven on metal** â a real front-door doc (`SECURITY.md`, 2689 B) refuses `ContentTooLarge` (does not fit one 512-B resin); unlike #72 there is **no one-line raise** (`max_resin_bytes` structurally capped by `max_beads Ã max_bead_bytes`), so the allocation is booked toward the **chunking-storage horizon** (large artifacts across ordered resins, a later rung), the honest cost named. Additive â composes `tablecloth_keyed` public API over demo keeper seeds only; demo keyed selftest re-runs GREEN; gate #4 untouched. Witness `tools/gism_j6_provenance_real_witness.rish` GREEN (5 cases + demo re-run), TAME + width clean. **GISM-J6r2 LANDED `20260812`:** `pond/apps/provenance_receipt.rye` carries the signed word *out of the store* as a portable receipt for the same real artifact â composing TACT-J2's `publish_receipt`/`publish_receipt_bron` (demo-proven) over the real embedded bytes: the receipt verifies **offline** (no store), renders to `format publication-v1` Bron, parses back **byte-for-byte**, still verifies, and the held real content matches the bound digest. Crux cross-check: the signed SHA-256 (`2010d160â¦0a6d`) equals an independent `sha256sum` of the real file â two tools, one answer. Tamper refuses (real byte flip Â· wrong keeper Â· unknown field). **No fresh Yield red** â the 213-B artifact fits with headroom; the size horizon stays #73's chunking. Additive â demo receipt selftests re-run GREEN; no custody crosses. Witness `tools/gism_j6_provenance_receipt_witness.rish` GREEN (5 cases + sha256sum cross-check + two demo re-runs), TAME + width clean. **GISM-J6r3 LANDED `20260812`:** `pond/apps/provenance_chain.rye` makes custody a verifiable **chain** â the same real artifact handed **planet â star â galaxy**, each transfer signed by the current holder over the constant artifact digest bound to (from Â· to Â· seq); the chain re-verifies **from cold** against the settled constellation and resolves the head holder. Design seam self-approved (the constellation holds Commitment digests, not keeper pubkeys, since the shared surface shrank): a link is self-describing (from/to Â· seq Â· from-keeper Â· sig), tamper-evident on its own, trust grounded by **re-presenting each holder's Deed** at verify (each a current member whose keeper matches the link â a never-settled forgery cannot pass). Tamper refuses (non-holder â `NotHolder` nothing appended Â· forged link Â· swapped digest Â· wrong presented holder â each `BadTransfer`). No fresh Yield red; composed over `tablecloth_keyed` primitives (re-run GREEN). Witness `tools/gism_j6_provenance_chain_witness.rish` GREEN (5 cases + keyed re-run), TAME + width clean. **GISM-J6r4 LANDED `20260812` â Journey 6 closes:** `pond/apps/provenance_true.rye` proves the signed facts are **true to the real file a keeper can open and hash** â the receipt's signed length (213) and SHA-256 (`2010d160â¦0a6d`) cross-check against an independent `wc -c` and `sha256sum` of the same file (two tools, one answer); in-app the binding is tight (a single-byte neighbor refuses the digest). GREEN first pass; composed over `publish_receipt` public API; no fresh Yield red; no custody crosses. Witness `tools/gism_j6_provenance_true_witness.rish` GREEN, TAME + width clean. **GISM Journey 6 (Provenance) stands complete end to end â r1 real artifact owner-signed (+ Yield red #73) Â· r2 receipt travels Â· r3 custody chain Â· r4 true to the bytes.**

- **REDS #73 CLOSED `20260812` â the spool (chunking storage) built** (reds-first crux, self-approved design round `active-designing/20260812-191206_gism-red73-spool-chunking-storage-exploration.md`). A booked red preempts the next durable journey, so before Season 2 Journey 7 (Fair-trade, AYRE) opens, the chunking horizon red #73 named is now a module. `mantra/spool.rye` winds a large artifact across **ordered resins** (each a valid bead-index over the shared `BeadStore`, fixed-size at 256 B for a legible store bound), the whole proving against a SHA3-256 content address unchanged by chunking, every tamper (flipped bead Â· corrupted resin index Â· truncated spool) refusing the whole reassembly. The very files red #73 refused now wind and unwind **byte-for-byte** â `SECURITY.md` (2689 B â 6 resins), `context/TWO_ROOMS.md` (6079 B â 12 resins) â and the honest **32 KB ceiling** is reachable in full (32768 B / 64 resins / 128 beads wound and unwound whole; over-ceiling refuses `ContentTooLarge`). No bound pretended: `max_resin_bytes` stayed structurally capped; the fix is a storage layer above it, with `beading.max_store_beads` raised 64 â 256 **additively** so the ceiling is real (beading re-runs GREEN). Witness `tools/gism_spool_witness.rish` GREEN (real docs Â· ceiling Â· stable name Â· dedup dividend Â· three tamper refusals Â· on-disk size cross-check), TAME + width clean. **Then the naming layer LANDED `20260812` (spool r2):** `pond/apps/spool_cloth.rye` â Tablecloth's sibling for large artifacts â binds a human name to a whole spool, so a real front-door document too large for one resin is stored under a name and fetched back **byte-for-byte** (`SECURITY.md` 2689 B, `context/TWO_ROOMS.md` 6079 B), the same content under two names sharing every bead (full dedup: security-copy deposited 0, deduped 11), a `format spool-cloth-v1` manifest round-tripping name Â· address Â· length Â· resin count, and every refusal holding (tampered bead â `DigestMismatch`, unknown/taken/empty name, bad manifest header). Composes the spool's public API only (spool re-runs GREEN); demo/real bytes only, no custody crosses. Witness `tools/gism_spool_cloth_witness.rish` GREEN, TAME + width clean. **Then the owner-signed large-artifact store LANDED `20260812` (spool r3) â the payoff red #73 was reaching for:** `pond/apps/spool_keyed.rye` â `tablecloth_keyed`'s sibling for large artifacts â makes a settled Kumara identity vouch for which bytes: the owner presents the point's Deed (verified against the constellation) and signs the exact publication (point Â· whole length Â· name Â· **SHA-256 of the whole content**), and only then is the document wound across resins and named. Two real front-door documents (`SECURITY.md` 2689 B â planet, `context/TWO_ROOMS.md` 6079 B â star), each far too large for one resin, are now **owner-signed, wound across resins, fetched byte-for-byte, and resolved to whose they are** (`owner_of`/`place_of`). Every refusal holds â a non-owner (ghost Deed â `NotOwner`, no resin wound), a forged signature (`BadSignature`), a content-mismatched signature (the whole digest is bound in, so a tampered large document cannot ride a real signature), and a tampered bead after an honest publish (`DigestMismatch`). Composes `tablecloth_keyed`'s public signing helpers (`sign_publication`Â·`digest_of`Â·`verify_receipt`) over `spool_cloth` (both re-run GREEN); `spool_cloth` grew only a `pub index_of`. Demo keeper seeds only â **no real key, no custody (gate #4 untouched)**. Witness `tools/gism_spool_keyed_witness.rish` GREEN, TAME + width clean. **Then the large-artifact portable receipt LANDED `20260812` (spool r4) â the arc's finishing rung:** `pond/apps/spool_receipt.rye` â the large-artifact sibling of TACT-J2's `publish_receipt`/`publish_receipt_bron` and GISM-J6r2's `provenance_receipt` (which travelled a receipt for a document small enough to fit one resin) â carries a large owner-signed document's signed word *out of the spool* as a portable receipt. The receipt is content-length-independent by design (point Â· name Â· whole length Â· SHA-256 Â· signature â the same signed word `spool_keyed` binds), so one `Publication` vouches for a document whether it lives in one resin or sixty; the crux over `provenance_receipt` is that the receipt is tied to bytes **reassembled from the resins**, not a whole-held copy. `SECURITY.md` (2689 B â 6 resins) winds under the owner's signature, the detached receipt carries the **identical signature** the spool stored (proven byte-for-byte), travels as a `format publication-v1` Bron record (263 B) and returns byte-for-byte, still verifies offline (no store, no spool, no network), and the bytes read back out of the resins match the bound digest â cross-checked against an independent `sha256sum` (`5d34cc8bâ¦571c`, two tools one answer). Every tamper refuses (flipped reassembled byte Â· wrong keeper key Â· unknown Bron field). No fresh Yield red; composes `spool_keyed` + `publish_receipt` + `publish_receipt_bron` public API (all three re-run GREEN); no custody crosses. Witness `tools/gism_spool_receipt_witness.rish` GREEN, TAME + width clean. **The spool arc stands complete: chunking (r1) Â· naming (r2) Â· owner-signed (r3) Â· receipt-travels (r4) â provenance for a document too large for one resin now stands on documents a keeper actually wrote and can hash by hand.**

- **Season 2 Journey 7 (Fair-trade, AYRE) OPENED `20260812` (AYRE-J7r1)** â with the spool arc complete, the next Lindy-first crux on the whole 1,024-round road is Yield's second promise beyond *stands on real data*: **trades fairly** (self-approved exploration `active-designing/20260812-193727_ayre-journey7-fair-trade-exploration.md`; four rounds â r1 satisfied Â· r2 signed Â· r3 travels Â· r4 tied-to-a-fair-trade â under the seated **AYRE** waymark). **AYRE-J7r1 LANDED:** `pond/apps/fairtrade_cert.rye` fixes a fair-trade certificate's honest meaning as the join it is â a `format fairtrade-cert-v1` certificate (one `require <criterion>` line per requirement) **evaluated against a cooperative `Record`** of held attestations, the same *every-required-name-is-held* join `tablecloth_brix.evaluate` proved for artifacts (BUHR-J7r3), carried from artifacts to fair-trade criteria. A record holding every required criterion yields a `Certified` naming exactly those criteria **in declared order**; a required criterion the record does not hold refuses the whole certification (`Unsatisfied`) rather than certify a gap â a satisfied `Certified` exists only when every criterion was held, so its existence is the proof. Malformed refusals real (wrong header Â· bad tag Â· empty Â· two names on one line â `BadCertificate`; overlong â `NameTooLong`; past-bound â `TooManyCriteria`). Pure declaration-satisfaction â no signing/books yet (r2 signs each attestation mirroring `commerce_trade_signed`, r4 books fairly in Dimeroll mirroring `commerce_trade`); facts only, custody gate #3 untouched, demo attestations only. GREEN first pass; witness `tools/ayre_j7_fairtrade_cert_witness.rish` GREEN, TAME + width clean. **AYRE-J7r2 LANDED `20260812`:** `pond/apps/fairtrade_cert_signed.rye` binds each attestation to a settled Kumara identity â the attesting party signs the exact fact (their point Â· the criterion name), and a satisfied certificate requires **every** required criterion's signature to verify against the attester's known keeper key, so a steward cannot certify a criterion no party vouched for. A `SignedRecord` carries name Â· attester point Â· signature per attestation; a `Keyring` maps point â keeper key (the caller's statement of whose word to trust, as `commerce_trade_signed` passes the seller/buyer keys); `verify_held` refuses `Unsatisfied` (unheld) Â· `UnknownAttester` (unenrolled) Â· `BadAttestation` (unverifying) by exact reason; `evaluate_signed` reuses r1's parse + `Certified`. The honesty crux mirrors commerce's price-tamper â a steward relabels a `price-floor` attestation to `living-wage` and reuses the signature, but verification is over `living-wage` which no one signed â `BadAttestation`. Additive (composes `fairtrade_cert` + `kumara` public API; r1 re-runs GREEN); demo keeper seeds only, custody gate #3 untouched. Witness `tools/ayre_j7_fairtrade_cert_signed_witness.rish` GREEN (6 cases + r1 re-run), TAME + width clean. **AYRE-J7r3 LANDED `20260812`:** `pond/apps/fairtrade_cert_bron.rye` makes the satisfied, signed certificate travel â the certified attestations render to a `format fairtrade-signed-v1` Bron record (one `attest <name> <point> <sig-hex>` line per criterion) and parse back byte-for-byte, **still satisfied**: a `PortableCert` owns its buffers, `still_satisfied` re-verifies each traveled attestation via r2's `verify_held`, and re-running `evaluate_signed` over the traveled bundle yields the identical `Certified`. A signature flipped after the crossing refuses `BadAttestation` **offline** (no store, no network); an unknown field (`UnknownField`, never dropped), a malformed line (`BadLine`), a wrong header (`BadFormat`), and bad hex (`BadHex`) each refuse; `render` refuses `NotCertified` rather than emit a lying line. Additive (composes `fairtrade_cert` + `fairtrade_cert_signed` + `kumara` public API; r2 re-runs GREEN); reads no network (serve transport = Comlink-served gate); demo keeper seeds only, custody gate #3 untouched. Witness `tools/ayre_j7_fairtrade_cert_bron_witness.rish` GREEN (3 cases + r2 re-run), TAME + width clean. **AYRE-J7r4 LANDED `20260812` â Journey 7 CLOSES:** `pond/apps/fairtrade_cert_trade.rye` ties the certificate to a fair-value trade â `record_certified_trade` gates `commerce.record_trade` behind two proofs, **before any book is touched**: the trade's seller must be one of the cooperative's own attesters (`SellerNotCertified`, so the certificate cannot float free of the trade), and the certificate must be satisfied against the signed record (r2's `evaluate_signed`, refusing `Unsatisfied`/`UnknownAttester`/`BadAttestation`). Only then does the trade book fairly on both books at one agreed price (each nets to zero, the pair conserves across cash; `fair_trade_holds` folds it to one answer). Even a certified cooperative cannot book an unfair trade â a self-trade (`SelfTrade`) and a zero price (`BadAmount`) refuse at commerce's own laws. Additive (composes `fairtrade_cert_signed` + `commerce_trade` public API, both re-run GREEN); facts only, holds nothing, settling reaches custody gate #3 (licensed counsel); demo seeds only. Witness `tools/ayre_j7_fairtrade_cert_trade_witness.rish` GREEN (5 cases + r2 + commerce re-runs), TAME + width clean. **AYRE Journey 7 (Fair-trade) stands complete end to end â r1 satisfied Â· r2 signed Â· r3 travels Â· r4 tied-to-a-fair-trade.**

- **Season 2 Journey 8 (Cash-first) OPENED `20260812` (GISM-AYRE-J8r1)** â with AYRE Journey 7 closed, the next Lindy-first crux on the whole road is Yield's last owning promise: **the accounting spine for Linengrow PBC / Siya Fund** (self-approved exploration `active-designing/20260812-195711_gism-ayre-journey8-cash-first-entity-books-exploration.md`; four rounds â r1 segregated transfer Â· r2 signed Â· r3 period travels Â· r4 read-true). The seated entity-books design names the strand: **entity separation, never braided.** **GISM-AYRE-J8r1 LANDED:** `pond/apps/entity_books.rye` fixes the crux â two named entities, each an `Entity` owning its name + bounded journal, folding its own trial balance (conserves) and balance sheet (balances, core-asserted) through `dimeroll_core`'s `fold_journal`/`fold_balance_sheet`. An inter-entity `transfer` books one fact on **both** journals (grantor debit-expense/credit-cash, grantee debit-cash/credit-revenue of one amount), so grantor-out == grantee-in by construction; each entity's books balance and the pair conserves across cash (`pair_conserves` isolates the transfer delta â Siya â1500, Linengrow +1500, sum 0). Segregation proven **byte-identical** (a fact on Linengrow leaves Siya's fold unchanged). A self-transfer (`SameEntity`), a non-positive amount (`BadAmount`, no journal touched), and a full journal (`JournalFull`) each refuse. Additive (composes `dimeroll_core` public API, no new value model; its selftest re-runs GREEN); facts only, holds nothing, disbursement custody gate #3; demo entities only. Witness `tools/gism_ayre_j8_entity_books_witness.rish` GREEN (5 cases + dimeroll re-run), TAME + width clean. **GISM-AYRE-J8r2 LANDED `20260812`:** `pond/apps/entity_books_signed.rye` binds the transfer to two settled Kumara identities â each entity's steward signs the exact fact (the two entity names length-prefixed Â· amount Â· memo), and `record_signed_transfer` books through r1's `transfer` only when both signatures verify, gating **before any journal is touched**: signed names must match the two entities' own (`NameMismatch`), then both must agree (`NotAgreed`). The honesty crux mirrors `commerce_trade_signed` â change the amount after both signed and both signatures fall (`NotAgreed`, journal untouched); a forged signature refuses too. Additive (composes `entity_books` + `kumara` public API; r1 re-runs GREEN); demo steward seeds only, custody gate #3 untouched. Witness `tools/gism_ayre_j8_entity_books_signed_witness.rish` GREEN (4 cases + r1 re-run), TAME + width clean. **GISM-AYRE-J8r3 LANDED `20260812`:** `pond/apps/entity_books_period.rye` folds an entity's period to a sealed statement (name Â· close stamp Â· four trial-balance nets), renders it to a `format entity-period-v1` Bron record, and parses back preserving every field, the recovered period **still conserving and balancing** â a `PortablePeriod` owns its buffers, refuses `Unbalanced` if its nets do not sum to zero (rather than travel as false books), else folds the balance sheet the core asserts balances (matching the sealed one field for field). An unknown field (`UnknownField`, never dropped), a missing net/header (`MissingField`), a wrong header (`BadFormat`), and a bad integer (`BadInt`) each refuse. Additive (composes r1's fold + `dimeroll_core` public API; r1 re-runs GREEN); reads no network (serve transport = Comlink-served gate); demo entities only, custody gate #3 untouched. Witness `tools/gism_ayre_j8_entity_books_period_witness.rish` GREEN (4 cases + r1 re-run), TAME + width clean. **GISM-AYRE-J8r4 LANDED `20260812` â Journey 8 CLOSES:** `pond/apps/entity_books_true.rye` carries the books onto a **real** Dimeroll journal fixture (`dimeroll/fixtures/journal.bron`, `@embedFile`-bound through a `pond/apps/corpora/` symlink) â `read_journal` parses it with `dimeroll_core.parse_journal`, loads each fact into a rung-one `Entity`, folds (conservation + balance asserted on real bytes), and reports 2 entries Â· amount total 1200 Â· cash net 800. The crux: `tools/fixtures/entity_journal_truth.awk` measures the same file independently and the witness asserts the app's fold equals the awk reading field for field â **two tools, one answer** â so an entity's balance can never drift from facts a keeper adds up by hand. A malformed real journal refuses `UnknownAccount`. Additive (composes r1 `Entity` + `dimeroll_core` public API; r1 re-runs GREEN); reads no network; demo entities only, custody gate #3 untouched. Witness `tools/gism_ayre_j8_entity_books_true_witness.rish` GREEN (2 cases + app==awk crux + r1 re-run), TAME + width clean. **Season 2 Journey 8 (Cash-first) stands complete end to end â r1 segregated transfer Â· r2 signed Â· r3 period travels Â· r4 read-true. Season 2 (Yield) agent-doable arc is complete across J5Â·J6Â·J7Â·J8.** **Next agent-doable crux (Lindy-first):** Season 3 (Commons, DAHL) â Journey 9 (Skate social layer, a consent-first surface over Pond identities), a new domain worth a self-approved design read first.

- **Season 3 â Commons OPENED `20260812` (DAHL-J9r1)** â with Seasons 1 (TACT) and 2 (Yield) complete across every agent-doable rung, the next Lindy-first crux on the whole 1,024-round road is **Season 3 (Commons), Journey 9 (Skate)** under the seated **DAHL** waymark: the social layer over Pond identities, *a shared surface entered by consent, never by default* (self-approved design round `active-designing/20260812-201611_dahl-journey9-skate-consent-first-exploration.md`). Crux: **a link between two members exists only when both have requested it.** **DAHL-J9r1 LANDED:** `pond/apps/skate_circle.rye` carries Pond's own customs trichotomy (place Â· hold Â· refuse) to the social border, where the resting verdict is the strictest â a `Circle` a keeper joins only by their own explicit act (no member by default), and a link that is **mutual-only**: one keeper's `request_link` is a standing offer that **holds**; the connection **places** (links) only when the other has also asked, so a one-sided request holds forever and a steward cannot manufacture a connection from one side. A link is not stored but **derived** as the presence of two opposing requests, so the mutual-only law cannot be bypassed by writing a link directly; `visible` follows `linked` exactly (invariant-asserted), so presence is never default â a joined keeper who linked no one sees no one and is seen by no one. Every refusal holds: a link touching a non-member (`NotMember`), a self-link (`SelfLink`), a taken seat (`AlreadyMember`), a repeated offer (`AlreadyRequested`), a full circle (`CircleFull`). Pure state machine â no signing yet (r2 binds each act to a settled Kumara identity mirroring `commerce_trade_signed`; r3 travels as Bron; r4 reads a real fixture true). Facts only, custody-first: no network, no key, no real identity (a circle served over the wire reaches the serve gate â Keaton's hand); demo keeper points only. Witness `tools/dahl_j9_skate_circle_witness.rish` GREEN (2 welcomes + 4 refusals), TAME bans + width clean. **DAHL-J9r2 LANDED `20260812`:** `pond/apps/skate_circle_signed.rye` binds each consent act to the keeper's settled Kumara identity â a join is signed by the joining point, a link-request by the requesting point, over the exact facts (a `tag` so a join can never be replayed as a request, the `circle_id` so consent cannot cross circles, the points). A `Keyring` maps point â keeper key (the caller's statement of whose word to trust, as `commerce_trade_signed` passes seller/buyer keys); `join_signed`/`request_signed` verify the acting keeper's **own** signature before touching r1's circle, refusing `UnknownKeeper` (unenrolled) or `BadConsent` (unverifying). The honesty crux mirrors commerce's price-tamper â relabel a request's target after signing and reuse the signature, it falls `BadConsent` (no offer recorded); a join signed by the wrong keeper (`BadConsent`, no seat taken), an unenrolled keeper (`UnknownKeeper`), and a signature presented to another circle (`BadConsent`) each refuse. Additive (composes `skate_circle` + `kumara` public API; r1 re-runs GREEN); demo keeper seeds only, no real key, no network. Witness `tools/dahl_j9_skate_circle_signed_witness.rish` GREEN (1 welcome + 4 refusals + r1 re-run), TAME bans + width clean. **DAHL-J9r3 LANDED `20260812`:** `pond/apps/skate_circle_bron.rye` makes the signed circle travel â a `SignedCircle` keeps, beside r1's live circle, the signature that authorized each act (a join sig per member, a request sig per link), renders to a `format skate-circle-v1` Bron record and parses back **byte-for-byte**, still consent-honest **offline**: `still_consented` re-verifies every carried signature against the recipient's keyring through r2's `verify_join`/`verify_request` (exposed additively â r2 re-runs GREEN), and `rebuild` replays the acts through r2's verified paths into an equal live circle whose `linked`/`visible` verdicts match. A signature flipped after the crossing refuses `BadConsent` offline (no store, no network, no live circle); an unknown field (`UnknownField`, never dropped), a wrong header (`BadFormat`), a missing circle id (`MissingCircle`), and malformed hex (`BadHex`) each refuse. Additive (composes r1 + r2 public API; both re-run GREEN); demo keeper seeds only, no network. Witness `tools/dahl_j9_skate_circle_bron_witness.rish` GREEN (1 welcome + 5 refusals + r1/r2 re-runs), TAME bans + width clean. **DAHL-J9r4 LANDED `20260812` â Journey 9 CLOSES:** `pond/apps/skate_circle_true.rye` carries the reader onto a **real** circle record â `skate/fixtures/circle.bron`, a genuine `format skate-circle-v1` record with real Ed25519 signatures (3 members Â· 1 mutual link Â· 1 one-sided hold), `@embedFile`-bound through a `pond/apps/corpora/` symlink. Two readings must agree: the app parses through r3 and reports member count Â· link count Â· **mutual-link count** (an unordered pair both directed requests appear â a one-sided hold not counted), and the witness cross-checks that triple against `tools/fixtures/skate_circle_truth.awk`'s reading of the same file â **two tools, one answer** (`3 3 1`). And the record is genuinely consent-honest â `still_consented` re-verifies every carried signature against the demo keyring, and `rebuild` recovers the mutual link (`linked(ivy,wren)`) and the one-sided hold (`verdict(flax,ivy)==hold`, not visible) exactly â so the fixture is a real signed circle, not a hollow shape. A tampered header refuses `BadFormat`. Additive (composes r1Â·r2Â·r3 public API, all re-run GREEN); reads no network; demo keeper seeds only. Witness `tools/dahl_j9_skate_circle_true_witness.rish` GREEN (2 welcomes + 1 refusal + app==awk crux + r1/r3 re-runs), TAME bans + width clean. **DAHL Journey 9 (Skate) stands complete end to end â r1 consent Â· r2 signed Â· r3 travels Â· r4 read-true.** The one remaining Skate horizon rung â **Comlink-served** (a circle shared over the wire) â reaches the serve custody gate (Keaton's hand). **Next agent-doable crux (Lindy-first):** Season 3 Journey 10 (Membership) â the one surface that needs quorum (names, groups) proven at more than two keepers, extending `settlement/names.rye`; a fresh self-approved design read first.

- **Season 3 Journey 10 (Membership) OPENED `20260812` (DAHL-J10r1)** â with Journey 9 closed, the next Lindy-first crux on the whole road is the one surface `settlement/names.rye` names in its own header as *the surface that genuinely needs consensus*, proven now at **more than two keepers** (self-approved design round `active-designing/20260812-204233_dahl-journey10-membership-quorum-exploration.md`). J9 proved *exactly-two* mutual consent; J10's durable new law is *threshold-of-n* quorum. Crux: **a candidate is seated only when a quorum of distinct members votes.** **DAHL-J10r1 LANDED:** `pond/apps/skate_group.rye` â a `Group` opens with a founder roster and a fixed quorum `t` (`2 â¤ t â¤ roster` â genuine consensus, never one hand, never a unanimity a lone hold-out could veto); a member votes for a candidate; the candidate is admitted only when `t` **distinct** members have voted. The honesty is distinctness with threshold â one keeper voting twice is counted once (`AlreadyVoted`), so a lone steward can never manufacture a quorum; admission is **derived** from the distinct-vote count reaching `t` (there is no direct add-member), and `is_member` follows the verdict exactly (invariant-asserted). Proven past two keepers: a quorum of three admits only when three distinct members vote (two hold). Every refusal holds â a non-member vote (`NotMember`), re-admitting a seated keeper (`AlreadyMember`), a lone quorum (`BadThreshold`), an unreachable quorum (`TooFewFounders`), a doubled founder (`DuplicateFounder`), a full roster refusing the quorum-reaching vote before it overflows (`GroupFull`), a full vote book (`VotesFull`). Pure state machine â no signing yet (r2 signs each vote with the member's settled Kumara identity mirroring `skate_circle_signed`; r3 travels as Bron; r4 reads a real fixture true). Facts only, custody-first: no network, no key, no real identity (a group served over the wire reaches the serve gate â Keaton's hand); demo keeper points only. Witness `tools/dahl_j10_skate_group_witness.rish` GREEN (2 welcomes + 4 refusals), TAME bans + width clean. **DAHL-J10r2 LANDED `20260812`:** `pond/apps/skate_group_signed.rye` binds each vote to the voting member's settled Kumara identity â a vote is signed by the very member it speaks for over the exact facts (a `tag`, the `group_id` so a vote cannot cross groups, the voter, the candidate); a `Keyring` maps point â keeper key (the caller's statement of whose word to trust, as `skate_circle_signed` passes keys), and `vote_signed` verifies the voter's **own** signature before touching r1's group, refusing `UnknownKeeper` (unenrolled â even for a real member) or `BadConsent` (unverifying). The honesty crux mirrors the circle's relabel â change the candidate after signing and reuse the signature, it falls `BadConsent` (no vote recorded); a forged vote by the wrong keeper (`BadConsent`, tally unmoved), a cross-group signature (`BadConsent`) each refuse. And the lone-steward guard carries through r1 unchanged â a member's valid signature lets them vote once, the repeat refusing `AlreadyVoted` with the tally held at one, so signing still cannot fake a quorum from one hand. Founders are the group's seeded bootstrap; every admission after takes signed distinct votes. Additive (composes `skate_group` + `kumara` public API; r1 re-runs GREEN); demo keeper seeds only, no real key, no network. Witness `tools/dahl_j10_skate_group_signed_witness.rish` GREEN (1 welcome + 5 refusals + r1 re-run), TAME bans + width clean. **DAHL-J10r3 LANDED `20260812`:** `pond/apps/skate_group_bron.rye` makes the signed group travel â a `SignedGroup` keeps, beside r1's live group, its founder roster Â· quorum Â· the signature that authorized each vote, renders to a `format skate-group-v1` Bron record (a `group <id> <quorum>` line Â· one `founder` line each Â· one `vote <voter> <candidate> <sig-hex>` line each) and parses back **byte-for-byte**, still quorum-honest **offline**: `still_quorum_honest` re-verifies every carried vote signature against the recipient's keyring through r2's `verify_vote`, and `rebuild` replays the founders and votes through r1's `open` and r2's `vote_signed` into an equal live group whose admission matches (Bram still seated, verdict `.place`). A vote signature flipped after the crossing refuses `BadConsent` offline (no store, no network, no live group); an unknown field (`UnknownField`, never dropped), a wrong header (`BadFormat`), a missing group line (`MissingGroup`), and malformed hex (`BadHex`) each refuse. Additive (composes r1 + r2 public API; both re-run GREEN); demo keeper seeds only, no network. Witness `tools/dahl_j10_skate_group_bron_witness.rish` GREEN (1 welcome + 5 refusals + r1/r2 re-runs), TAME bans + width clean. **DAHL-J10r4 LANDED `20260812` â Journey 10 CLOSES:** `pond/apps/skate_group_true.rye` carries the reader onto a **real** group record â `skate/fixtures/group.bron`, a genuine `format skate-group-v1` record with real Ed25519 vote signatures (3 founders at quorum 2 Â· 2 votes admit Bram Â· 1 one-sided vote for Cate holds), `@embedFile`-bound through a `pond/apps/corpora/` symlink, produced reproducibly by an in-tree generator (`pond/apps/skate_group_fixture_gen.rye`, demo seeds only). Two readings must agree: the app parses through r3 and reports founder count Â· vote count Â· **admitted-by-quorum count** (distinct candidates whose distinct-vote tally reaches the quorum â a candidate short of quorum not counted), and the witness cross-checks that triple against `tools/fixtures/skate_group_truth.awk`'s reading of the same file â **two tools, one answer** (`3 3 1`). And the record is genuinely quorum-honest â `still_quorum_honest` re-verifies every carried vote signature against the demo keyring, and `rebuild` recovers Bram's admission (`.place`) and Cate's hold (`.hold`, not seated) exactly, so the fixture is a real signed group, not a hollow shape. A tampered header refuses `BadFormat`. Additive (composes r1Â·r2Â·r3 public API, all re-run GREEN); reads no network; demo keeper seeds only. Witness `tools/dahl_j10_skate_group_true_witness.rish` GREEN (2 welcomes + 1 refusal + app==awk crux + r1/r3 re-runs), TAME bans + width clean. **DAHL Journey 10 (Membership) stands complete end to end â r1 quorum Â· r2 signed Â· r3 travels Â· r4 read-true.** The one remaining Membership horizon rung â **Comlink-served** (a group shared over the wire) â reaches the serve custody gate (Keaton's hand). **Next agent-doable crux (Lindy-first):** Season 3 Journey 11 (Council skies) â loadable topologies a community loads like a game (odd-quorum fractal `3Â·9Â·15Â·27`, modality roles), a new domain worth a fresh self-approved design read first.

- **Season 3 Journey 11 (Council skies) OPENED `20260812` (DAHL-J11r1)** â with Journey 10 closed, the next Lindy-first crux on the whole road is the loadable-topologies brief's load-bearing move: a community does not merely pick a threshold (J10), it **loads a whole shape** â a *sky* â the way a console loads a game, and the sky's own odd fractal *gives* the council its tie-free quorum and its roles (self-approved design round `active-designing/20260812-210603_dahl-journey11-council-skies-exploration.md`). Crux: **an odd sky gives a tie-free quorum and a complete triad.** **DAHL-J11r1 LANDED:** `pond/apps/council_sky.rye` composes `comlink/topology.rye`'s `Sky` â a `Council` loads a sky and seats a body at one of the sky's own fractal levels (`galaxies Â· stars Â· planets Â· prosperity`). The **council sky's** four counts are **3 Â· 9 Â· 15 Â· 27**, every one odd; the quorum is the strict majority of the odd body (`size/2+1`), and because the body is odd, `2Â·quorum > size` (invariant-asserted) â yes and no can never both reach the quorum, so a fully-voted proposal is **always decided, never a tie**. The civic **`compass_sky` is not a council sky**: its `prosperity` is 60 â even â so seating its d60 refuses `EvenCouncil` (its odd star level 5 still seats, quorum 3). Each seat wears a **modality role** â `initiator Â· sustainer Â· adapter` by `seat mod 3` â a pure function of position, so any triad is complete and can never drift from the seated body; the outcome is **derived** from the tally, never written. Refusals hold â a size the sky does not name (`NotAFractalLevel`), an even level (`EvenCouncil`), a non-seat vote (`NotSeated`), a doubled ballot (`AlreadyVoted`, tally unmoved). Pure state machine â no signing yet (r2 signs each vote with the member's Kumara identity mirroring `skate_group_signed`; r3 travels as Bron; r4 reads a real fixture true). Facts only, custody-first: no network, no key, no real identity (a council served over the wire reaches the serve gate â Keaton's hand); demo seats only. A compound `assert(a and b)` caught by `tame_style_check` and split. Witness `tools/dahl_j11_council_sky_witness.rish` GREEN (6 cases), TAME bans + width clean. **DAHL-J11r2 LANDED `20260812`:** `pond/apps/council_sky_signed.rye` binds each ballot to the voting seat's settled Kumara identity â a ballot is signed by the very seat it speaks for over the exact facts (a `tag`, the `council_id` so a ballot cannot cross councils, the council **size** so it cannot cross a differently-shaped body, the seat, the proposal, the yes/no); a `Keyring` maps a seat to its keeper key; `vote_signed` verifies the seat's own signature before touching r1's council, refusing `UnknownKeeper` (unenrolled) or `BadConsent` (unverifying). The honesty crux mirrors the group's relabel â flip a ballot's yes to no after signing and reuse the signature, it falls `BadConsent`; a forged ballot (wrong keeper), a cross-council signature each refuse. The odd-quorum lone-hand guard carries through r1 (`AlreadyVoted`, tally held), so signing still cannot fake a majority. Additive (composes r1 + `kumara`; r1 re-runs GREEN); demo keeper seeds only, no real key, no network. Witness `tools/dahl_j11_council_sky_signed_witness.rish` GREEN (2 welcomes + 4 refusals + r1 re-run), TAME bans + width clean. **DAHL-J11r3 LANDED `20260812`:** `pond/apps/council_sky_bron.rye` renders a signed council to a `format council-sky-v1` Bron record (a `council` line carrying the sky's galaxy/star/planet counts + id + seated size, one `ballot` line per signed vote) and parses back byte-for-byte. Still tie-free and quorum-honest offline â `still_quorum_honest` re-verifies each carried ballot via r2's `verify_ballot`; `rebuild` reconstructs the sky and re-seats through r1's `load`, **inheriting `EvenCouncil` so a record naming the compass sky's even d60 (12Â·5Â·12Â·60) refuses on rebuild** (the tie-free law travels with the record, distinct from `skate_group_bron`). Unknown/missing field Â· bad header Â· bad hex Â· non-0/1 yea each refuse. Additive (composes r1 + r2; both re-run GREEN); demo keeper seeds only, no network. Witness `tools/dahl_j11_council_sky_bron_witness.rish` GREEN (1 welcome + 6 refusals/re-runs), TAME bans + width clean. **DAHL-J11r4 LANDED `20260812` â Journey 11 CLOSES:** `pond/apps/council_sky_true.rye` `@embedFile`-binds `skate/fixtures/council.bron` (a genuine `format council-sky-v1` record with real Ed25519 ballot signatures â the council sky's triad, size 3, quorum 2; two yes-ballots decide proposal A, one yes-ballot holds proposal B open; produced reproducibly by `pond/apps/council_sky_fixture_gen.rye`, demo seeds only) through a `pond/apps/corpora/` symlink, parses through r3, and reports the seated **size**, **ballot count**, and **decided-proposal count**. The crux: `tools/fixtures/council_sky_truth.awk` reads the size and majority quorum, dedups ballots, and counts decided proposals independently, and the witness asserts the app's triple equals awk's â two tools, one answer (`3 3 1`). The record is genuinely tie-free and quorum-honest â `still_quorum_honest` re-verifies every ballot, `rebuild` recovers proposal A's passage and proposal B's open hold exactly, `2Â·quorum > size` holding on the real record. A tampered header refuses `BadFormat`. Additive (composes r1Â·r2Â·r3; all re-run GREEN); reads no network; demo keeper seeds only. Witness `tools/dahl_j11_council_sky_true_witness.rish` GREEN (2 welcomes + 1 refusal + app==awk crux + r1/r3 re-runs), TAME bans + width clean. **DAHL Journey 11 (Council skies) stands complete end to end â r1 odd quorum Â· r2 signed Â· r3 travels Â· r4 read-true.** The one remaining Council horizon rung â **Comlink-served** (a council shared over the wire) â reaches the serve custody gate (Keaton's hand). **Next agent-doable crux (Lindy-first):** Season 3 Journey 12 (Data-dignity) â succession, consent, the keeper's rights, custody-first; a new domain worth a fresh self-approved design read first.

- **Season 3 Journey 12 (Data-dignity) OPENED `20260812` (DAHL-J12r1)** â with Journey 11 closed, the next Lindy-first crux on the whole road is Commons' last and most tender: **what becomes of a keeper's data when the keeper is gone** (self-approved design round `active-designing/20260812-212607_dahl-journey12-data-dignity-exploration.md`). Journeys 9â11 built how *living* keepers meet; Journey 12 carries consent, and the keeper's own word, past a single lifetime. Crux: **succession honors the keeper's declared word, and rests what the keeper chose to rest.** **DAHL-J12r1 LANDED:** `pond/apps/data_dignity.rye` â an `Estate` opens naming a keeper and a **distinct** heir (`SelfHeir` refused); the keeper wills each holding **bequeathed** (passes to the heir) or **rested** (the keeper's dignity choice â extinguished at succession, inherited by no one, returned to no hand), **seals** the directive (the pre-declared word, fixed while present), and the heir **accepts**. Succession fires only on **two proofs, never one** â the sealed directive (`NotSealed`) *and* the heir's acceptance (`NotAccepted`) â so neither the departing keeper's word alone nor the receiving heir's alone moves custody (an heir is never conscripted; a steward cannot manufacture a will): the settlement ledger's escape law (both consent, the old sponsor keeps no veto) carried across the one boundary it had not crossed, **the keeper's absence**. Who holds an item is **derived**, never written â before succession every willed item is the keeper's; after, a bequeathed item is the heir's and a rested item is `rested` (no one's), an unwilled item `unknown`. The rested set's extinction is the promise made checkable â `heir_held_count == bequeathed_count` (asserted in `succeed`), and no rested item is ever the heir's â so the one preference a departed person can no longer defend is the one the rule guards hardest. Refusals hold â a self-heir (`SelfHeir`), a re-willed item (`AlreadyWilled`), a word after seal (`Sealed`, the keeper's word is one and fixed), a replayed succession (`AlreadySucceeded`, custody passes once). Pure state machine â no signing yet (r2 signs the keeper's seal and the heir's acceptance with settled Kumara identities, mirroring `skate_group_signed`; r3 travels as Bron; r4 reads a real fixture true). Facts only, custody-first: no network, no funds, no key, no real seed (the maintainer's own succession over a real Kumara instance is the maintainer's hand â gate #4-adjacent); demo keeper and heir points only. Witness `tools/dahl_j12_data_dignity_witness.rish` GREEN (5 selftests), TAME bans + width clean. **DAHL-J12r2 LANDED `20260812`:** `pond/apps/data_dignity_signed.rye` binds **each of the two proofs** to a settled Kumara identity â the keeper's seal to the keeper's key, the heir's acceptance to the heir's key. The keeper's directive signs a tag Â· estate id Â· keeper Â· heir Â· a **digest of the will's own marks** (SHA-256 over every willed item + disposition, in order); the honesty crux is that `seal_signed` recomputes the digest from the estate's **current** marks, so a bequeathedârested tamper after the keeper signed falls `BadConsent` â a steward cannot rewrite a departed keeper's will. The heir's acceptance signs tag Â· estate id Â· the two points, so it cannot cross estates. Refusals hold â tampered will Â· forged directive Â· forged acceptance Â· unenrolled party (`UnknownKeeper`) Â· cross-estate signature. Succession stays r1's unchanged two-proof gate. Additive â r1 grew only two pub readers (`item_at`, `disposition_at`); r1 re-runs GREEN. Two build reds caught on metal (a param shadowing a demo const; SHA-256 streaming method is `final`) fixed same-round. Demo seeds only; gate #4-adjacent untouched. Witness `tools/dahl_j12_data_dignity_signed_witness.rish` GREEN (5 + r1 re-run), TAME + width clean. **DAHL-J12r3 LANDED `20260812`:** `pond/apps/data_dignity_bron.rye` renders a signed estate to a `format data-dignity-v1` Bron record (`estate <id> <keeper> <heir>` Â· one `hold <item> <disposition>` per holding Â· `directive`/`acceptance` sig-hex) and parses back **byte-for-byte** into an owning `PortableEstate`. Still dignity-honest **offline** â `still_dignity_honest` re-verifies both proofs, `rebuild` replays through r2's signed gates and succeeds, the rested set still extinguished (`heir_held_count == bequeathed_count`). The crux: a `hold` mark flipped restedâbequeathed after the crossing shifts the will digest â the directive falls `BadConsent`, so the marks travel **under** the keeper's signature; a flipped directive refuses both check and rebuild. Unknown field Â· bad header Â· missing estate Â· missing proof Â· bad disposition Â· bad hex each refuse. Additive â r1/r2 re-run GREEN; GREEN first pass. Demo seeds only, no network. Witness `tools/dahl_j12_data_dignity_bron_witness.rish` GREEN (9 + r1/r2 re-runs), TAME + width clean. **DAHL-J12r4 LANDED `20260812` â Journey 12 CLOSES:** `pond/apps/data_dignity_true.rye` `@embedFile`-binds `skate/fixtures/estate.bron` (a genuine `format data-dignity-v1` record with real Ed25519 directive + acceptance signatures â 2 bequeathed, 1 rested; produced reproducibly by `pond/apps/data_dignity_fixture_gen.rye`) through a `pond/apps/corpora/` symlink, parses through r3, and reports **bequeathed Â· rested Â· heir-held**, the last computed by *actually running* the signed succession (`rebuild` â seal/accept/succeed) and resolving each item's holder. The crux: `tools/fixtures/data_dignity_truth.awk` reads the marks structurally and the witness asserts app==awk (`2 1 2`) â two genuinely different computations agreeing proves the succession yields exactly the bequeathed set, the rested holding resting. `still_dignity_honest` re-verifies both real proofs; a tampered header refuses `BadFormat`. One build red (mixed parse+rebuild error sets â merged return type) fixed same-round. Additive â r1/r3 re-run GREEN; demo seeds only, no network. Witness `tools/dahl_j12_data_dignity_true_witness.rish` GREEN (3 + app==awk + r1/r3 re-runs), TAME + width clean. **DAHL Journey 12 (Data-dignity) stands complete end to end â r1 two-proof rule Â· r2 signed Â· r3 travels Â· r4 read-true. Season 3 (Commons) agent-doable arc COMPLETE across J9Â·J10Â·J11Â·J12.** The Comlink-served rung is the serve custody gate. **Next agent-doable crux:** Season 4 (Seal, CION) â Journey 13 (Survey, VOLS), a fresh self-approved design read first, Lindy-first.

- **Season 4 â Seal (CION meta-season) OPENED `20260812` (VOLS-J13r1)** â with Season 3 (Commons) closed end to end, the next Lindy-first crux on the whole 1,024-round road is **Season 4 (Seal), Journey 13 (Survey, VOLS)**: name every living count-up ID, each site and each gap, before a single relabel (self-approved design `active-designing/20260812-215153_vols-journey13-survey-complete-census-exploration.md`). CION's opening lap swept seven modules *by hand* into `cion_module_labeling_witness`'s scan list â proving the pattern (a bare `lap N` is drift; `lap 4b`/`OA-L3`/`sub-lap` are permitted codes) yet not *completeness*: the guard scans only what a human remembered, exactly how a human â not a witness â caught red #65. **VOLS-J13r1 LANDED:** a census **complete by construction**. `tools/fixtures/vols_survey_scan.sh` **derives** its input set from the tree with `find` (never hand-lists), excludes dated testimony structurally (session-logs Â· counsel Â· archive Â· dated specs keep the ordinals history earned) and the `seed/` projection (a derived copy, named at its source), and **reports** every living surface naming a capability by a bare `lap N` â a survey reports, it does not gate (the GRAD seal, not VOLS, fails on drift). `tools/gen/season/vols_survey_witness.rish` GREEN proves it on a controlled fixture tree (a living README counted Â· a dated log excluded Â· `lap 4b`/`sub-lap 2` uncounted) and cross-checks the hit count against an independent grep -r traversal (`tools/fixtures/vols_survey_crosscount.sh`) â two tools, one answer. **Live source-tree census: 2690 surfaces Â· 223 sites Â· 1146 hits** â the real map LOWE will relabel against (223 sites vs 7 swept modules). Reads the tree, writes a census; touches no key, funds, network, or dated testimony; opens no relabel. **VOLS-J13r2 LANDED `20260812`:** each site classified by **kind** (prose relabel-target vs kept witness handle) and **guard** (watched vs gap). `tools/fixtures/vols_guardlist_extract.sh` derives the 29 guarded paths from the guard witness in force; `tools/fixtures/vols_classify_scan.sh` classifies each site against it. Crux invariant: the CION guard passing GREEN means every guarded file is clean, so **no site sits in a guarded file â `guarded_sites=0` by construction**, proven (and the classifier bites a planted drifted guarded file, `verdict=drift`). **Live: 223 sites = 144 kept witness handles + 79 prose gaps** (the real LOWE relabel worklist), `guarded_sites=0`. Witness `tools/gen/season/vols_classify_witness.rish` GREEN. **VOLS-J13r3 LANDED `20260812`:** the census travels as a durable `format cion-survey-v1` Bron ledger. `tools/fixtures/vols_survey_render.sh` emits the record (header Â· stamp Â· totals Â· one `site <path> <kind> <guard>` line each, sorted); `tools/fixtures/vols_survey_parse.sh` reads the totals back, counts the site lines so the record proves itself, and refuses a bad header (`verdict=badformat`). Round-trip proven three ways: parse recovers what render wrote, the render is **idempotent** (identical bytes twice), a tampered header refuses. Witness `tools/gen/season/vols_survey_bron_witness.rish` GREEN. **VOLS-J13r4 LANDED `20260812` â Journey 13 CLOSES:** the ledger **reads true** against an independent measure and is seated as a living pin. `tools/fixtures/vols_survey_truth.sh` computes the totals a second way (`grep -rl` traversal, basename classification â genuinely different from the classifier's find-loop); `tools/fixtures/vols_survey_readtrue.sh` emits `readtrue=agree` only when sites/handles/prose_gaps each match, no number hardcoded. Both agree on the live tree: **223 sites = 144 handles + 79 prose gaps**, `guarded_sites=0`. Seated living pin `work-in-progress/vols-survey-ledger.bron` (223 classified site lines) proves itself self-consistent. Witness `tools/gen/season/vols_survey_true_witness.rish` GREEN. **VOLS Journey 13 (Survey) stands complete â r1 derived census Â· r2 classified Â· r3 travels Â· r4 read-true + seated pin; the map LOWE relabels against is on metal.** **Next agent-doable crux:** Season 4 Journey 14 (Molt, LOWE) â grow the CION guard to cover the 79 prose gaps and relabel them to semantic label + stamp; the survey names them all, so the molt is a filled worklist.

- **Season 4 Journey 14 (Molt, LOWE) OPENED `20260812` (LOWE-J14r1)** â with VOLS Journey 13 closed and its survey ledger seated (79 prose gaps named, `guarded_sites=0`), the next Lindy-first crux is the molt itself: relabel each prose gap to semantic label + stamp and grow the CION guard to scan each relabeled surface (self-approved design `active-designing/20260812-222457_lowe-journey14-molt-relabel-exploration.md`). The crux the design fixes â the molt is **meaning-preserving, not a blind sed**: VOLS proved `lap N` rides in three roles and only *prose identity* is drift; a *witness handle* (`amphora_lap1.rish` and a README's shorthand for it) is kept, *dated testimony*'s facts stay. **LOWE-J14r1 LANDED:** the crux cluster is **amphora**, the smallest self-contained module carrying all three roles at once â relabeled `manifest_entry.rye` (`lap 1` â *manifest entry: one cargo lineâ¦ Â· 20260713*) and `vessel_fetch_delivery.rye`'s header + its GREEN print (`lap 3` â *vessel cargo fetch*), and reworded `README.md`'s `lap 1 Â· lap 2 Â· lap 3` shorthand to the kept `amphora_lap1/2/3` witness handles. The stamp `20260713` is the files' own git author date (never fabricated); witnesses assert on `.ok` + substrings `GREEN`/`fetch-by-digest` (not the literal `lap 3`), so the relabel is safe. Added amphora's three surfaces to `labeling_module_scan.sh`; the CION guard (`cion_module_labeling_witness`) stays GREEN over the widened set (mandate Â· scribe Â· vault Â· pond Â· granary Â· scribble Â· linengrow Â· **amphora**), zero residual bare ordinals, width-check + tame_style_check clean. `guarded_sites` climbs 0 â 3 of 79. The `amphora_lap3` witness's exit-1 is a **pre-existing** hosted-UDP-demo env limitation (git-stash A/B proves baseline exits 1 identically) â not this round. Reads/rewrites living prose only; touches no key, funds, network, or dated testimony; opens no cut. **Next agent-doable crux:** LOWE-J14r2 â the comlink guest cluster (fifteen `guest_*_tx/rx.rye` each carrying an `I2 â¦ lap 3 device` prose identity, plus `comlink/README.md`), relabel each to its device role + stamp, grow the guard, GREEN. **LOWE-J14r2 LANDED `20260812`:** the sixteen comlink device guests carrying a bare `lap N` beside a structured code (`OA-L3 lap 2`, `I2 snapshot lap 3`, `NS-L3 wire lap 2`, `Amphora lap 3`) relabeled â the redundant bare ordinal dropped, structured code + device role + stamp `20260713` (each guest's git author date) kept â plus `comlink/README.md`'s three port-table labels named by device. Worklist scoped to **exactly the sixteen** the VOLS ledger names (the ~44 other guests already use law-permitted codes: OA-L2/OA-L5/sub-lap/SLC-L2/MUR M2b/catch-up/two-way/Granary lap 4b/Tablecloth). A first-pass overreach (a `guest_*` glob hit all ~60 + a newline-eating regex) was caught in dry-run, reverted with `git checkout comlink/`, and redone on the explicit list with a newline-preserving regex (16 single-line diffs, `wc -l` unchanged). Guest binaries byte-unchanged in behavior (a `//!` header is never printed â the qemu/virtio device-wire witnesses are untouched). All 17 surfaces added to `labeling_module_scan.sh`; CION guard GREEN; `guarded_sites` 3 â 20 of 79; width-check + tame_style_check clean. **Next:** LOWE-J14r3 â the voice/market modules (`ember` Â· `lantern` Â· `lattice` Â· `mandi` Â· `dimeroll` Â· `glow/nock`). **LOWE-J14r3 LANDED `20260812`:** the three Q-vane voices (**lattice Â· ember Â· lantern**) READMEs and authored `.rye`/`.rish` prose relabeled â 38 sites, non-uniform: 9 hand-judged prose sites (version ordinals `(lap 39)`/`(lap 33)` â semantic name + git stamp; `/// Lap 1:` function-doc prefixes dropped; lattice README's parity note reworded off count-up) plus lantern/README.md's 29-row fixtures table stripped of its `Lap N` provenance prefix (each fixture kept by purpose). Comment-only + README (zero code tokens); proven safe on metal â `buhr_inference_query`, `buhr_reading_query`, `gism_j5_corpus_real_ember` all GREEN. 8 surfaces added to the guard; CION guard GREEN; `guarded_sites` 20 â 28 of 79; width + tame clean. **Next:** LOWE-J14r4 â mandi Â· dimeroll Â· glow/nock + the living `tools/*.rish` prose, then re-run the VOLS survey to prove `guarded_sites` covers every LOWE-touched surface, closing Journey 14. **LOWE-J14r4a LANDED `20260812`:** **mandi + dimeroll** (17 sites) relabeled â mandi's four `.rye` version headers named by capability (market listing Â· listing settle) + stamp `20260728` and its README section headings named by feature; dimeroll's lap-2/lap-4 feature comments + README status/design/link labels named by report + stamp `20260713` (the `dimeroll_sight_view_lap3.rish` tool handle and dated design-doc filenames kept). A **middot-byte trap** (a `.`-for-`Â·` regex orphaned a UTF-8 byte on a README line) was caught, reverted with `git checkout`, redone byte-exact, and every edited file verified valid UTF-8 by `iconv`. Comment+README only; proven safe on metal (`mandi_listing_settle`, `mandi_lap1`, `dimeroll_lap4`, `gism_ayre_j8` all GREEN). 8 surfaces added to the guard; CION guard GREEN; `guarded_sites` 28 â 36 of 79; width + tame clean. **Next:** LOWE-J14r4b â glow/nock (phased-plan prose, check the print-string witness first) + the living `tools/*.rish` prose, then re-run VOLS to close Journey 14. **LOWE-J14r4b LANDED `20260812`:** **glow/nock** (12 sites), the last module cluster â its phased-plan spine named by phase (`Lap 1 â the primitive core` â `The primitive core`, macro layer, edit primitive) across `nock_core.rye`, `nock_core_witness.rye`, `README.md`. The GREEN print string relabeled off `lap 1` only after confirming `nock_core_witness.rish` asserts on `.ok`/`GREEN`/`autocons`, never the ordinal (docs-implementation-sync); the `nock_lap2_witness.rye` handle kept in a reworded reference. Used the Edit tool (em-dashes made byte-regex fragile). Proven on metal â `nock_core_witness` GREEN; valid UTF-8, residual 0. 3 surfaces added; CION guard GREEN; `guarded_sites` 36 â 39 of 79 (**all six module clusters done**). **Next:** LOWE-J14r4c â the living `tools/*.rish` prose + mantra + the two README front doors + the `.glow` (~40 surfaces), then re-run VOLS so `guarded_sites` reaches 79, closing Journey 14. **LOWE-J14r4c LANDED `20260812`:** **mantra** (5 surfaces â the last module weave), snapshot/recall prose named by capability (`NS-L3 wire lap 1` â `NS-L3 wire`, `I2 snapshot export lap 3/2` â `I2 snapshot export`, `Lap 1 reuses`/`Lap 2 adds` â `Snapshot frames reuse`/`Horizon bundles add`) across README + four `.rye`, their hosted GREEN prints relabeled off the ordinal after confirming no witness asserts `contains` on them. Proven on metal â `mantra_snapshot_replay`/`horizon` GREEN (build `snapshot_export.rye`); the recall_sync device-wire lab's exit-1 is a pre-existing qemu/virtio env limit (git-stash A/B). Valid UTF-8, residual 0. 5 surfaces added; CION guard GREEN; `guarded_sites` 39 â 44 of 79. **Next:** LOWE-J14r4d â the ~35 remaining `tools/*.rish` witness-prose + 2 README front doors + `tools/gen/ember/README.md` + the `.glow` (~500 hits, mostly witnesses referencing the kept lap-handles â keep each handle, name each capability), then re-run VOLS so `guarded_sites` reaches 79, closing Journey 14. **LOWE-J14r4d-i LANDED `20260812`:** the **witness doc-comment header cluster** â thirteen `tools/*.rish` whose bare `lap N` sat only in a top `#` comment, the safest sub-cluster (zero behavior coupling: no `say`/`assert` string, no cross-witness `contains` dependency, so the relabel cannot break a driver): the four rishi stdlib witnesses (`filter_chain`/`fold`/`lines_bounded`/`quote_safe`), the three Acme DX conformance witnesses (`onboarding_path`/`interfaces`/`operations`, named by contract + stamp `20260811`), `granary_exchange`, the two mantra hosted labs (`snapshot_replay`/`snapshot_horizon`), `neth_sim_witness`, `nock_core_witness`, `wov_exit_honesty` â each relabeled to capability + its own git-author stamp, the filename kept as the stable handle. All 13 added to `labeling_module_scan.sh`; CION guard GREEN over the widened set (zero residual bare ordinals, valid UTF-8), sampled witnesses (`rishi_fold`/`nock_core`/`wov_exit_honesty`) re-run GREEN. `guarded_sites` 44 â 57 of 79. **Next:** LOWE-J14r4d-ii â the `say`/`assert`-coupled tools prose. **LOWE-J14r4d-ii LANDED `20260812`:** four coupled tools witnesses whose bare `lap N` rode both a `#` header **and** a `GREEN:` print string â `granary_resin_wire` (lap 4) Â· `granary_weave_settle` (lap 6) Â· `mantra_snapshot_wire` (I2 lap 3) Â· `mandi_listing_settle` (lap 3). The docs-implementation-sync crux checked first, tree-wide: **no driver anywhere asserts `contains` on any of their printer output** â every caller (`parity_ch01`, `granary_device_wire`) runs the witness and checks `.ok` only â so relabeling the GREEN string breaks nothing. Each named by capability + git stamp `20260713`, `GREEN` kept; re-run on metal â granary_resin_wire Â· granary_weave_settle Â· mandi_listing_settle all GREEN, and mantra_snapshot_wire's exit is a **pre-existing** qemu/virtio device-wire env limit (git-stash A/B: baseline fails identically at line 18, before the relabeled line 21). 4 surfaces added to `labeling_module_scan.sh`; CION guard GREEN; `guarded_sites` 57 â 61 of 79. **Deferred (its own coupled round):** the witness-asserted **`SLC-2a Lap 3`** scheme (`slc2a_ring3_metal`/`_session` printers â `pond_brushstroke_frame_witness`'s `contains "GREEN: SLC-2a Lap 3"` assert) â the design already flagged it as a coupled cross-cutting round. **LOWE-J14r4d-iii LANDED `20260812`:** the witness-asserted **SLC-2a Lap** coupled cluster relabeled **atomically** â the four ring witnesses (`slc2a_ring1_arbitrary` Â· `slc2a_ring2_redraw` Â· `slc2a_ring3_metal` Â· `slc2a_ring3_session`), `brushstroke/wayland_seed.rye`'s usage doc-comments naming the same scheme, and the coupled asserter `pond_brushstroke_frame_witness`. Each `SLC-2a Lap N` named by capability + stamp `20260713` (arbitrary-value viewer Â· redraw Â· Wayland metal smoke Â· session mirror); the ring3-metal printer's `GREEN:` string and the pond-frame `contains`-assert moved **together** to `GREEN: SLC-2a Wayland metal smoke`, so the printerâasserter coupling stays consistent by construction (the asserted substring is a prefix of the say). `SLC-2a` the scheme name is kept; only the bare ordinal moved. docs-implementation-sync checked tree-wide first: `parity_ch01` runs the ring witnesses but checks `.ok` only (its own SLC-2a-Lap prose is the separate parity cluster, a later round), and no other driver asserts `contains` on these printers. The ring/pond-frame witnesses are **Wayland-metal** â they need `WAYLAND_DISPLAY` (unset in this ai-jail bench), so metal GREEN stays the GNOME desk's to confirm; both proven to fail only at the pre-existing env gate (build libs / `WAYLAND_DISPLAY`), no parse error, the change being prose + coupled-string only. All 6 surfaces added to `labeling_module_scan.sh`; CION guard GREEN over the widened set (zero residual bare ordinals, valid UTF-8, negative still bites); `guarded_sites` 61 â 67 of 79; tame_style_check clean. **REDS #74 booked:** the automated `vols_guardlist_extract.sh` filters `\.(rye|md|brix)$` and silently drops every `.rish` guarded site (reports 44/45 where the hand-count reads 67) â the J14 read-true close cannot reach `guarded_sites 79` until the extractor learns `.rish`, booked to that close round. **Only comlink's `sub-lap` remains a deferred coupled round.** **LOWE-J14r4d-iv-a LANDED `20260812`:** the README + `.glow` prose cluster â four pure-prose surfaces with zero witness-print coupling (`active-designing/README.md` Â· `expanding-prompts/README.md` Â· `tools/gen/ember/README.md` Â· `src/app/shape-pool-agent-slot.glow`). These carry `lap N` as an **index reference to a dated design doc**, not a module capability: the two big READMEs are indexes of dated yonder `.md` files, so per the r4d handle-reference ruling each row's living title text is reworded to name the capability (hammock scope Â· sight view Â· reports Â· books view Â· TigerBeetle pin Â· exit honesty Â· SLC-L4 width/Sigma) with every **dated `.md` link target untouched** (the stamp column already dates each row). ember's corpus-catalog cell named by capability (the `ember_corpus_lap1.rish` handle kept); the `.glow`'s two invariant comments named "the first shape"/"honest first open". Structured codes (SLC-L4 Â· lap 2b Â· M2b Â· M2) and "M1 first lap" (no digit) correctly untouched. All 4 added to the guard; CION guard GREEN, valid UTF-8, residual 0; `guarded_sites` 67 â 71 of 79. **Next:** LOWE-J14r4d-iv-b â the remaining tools witnesses (`cellar_first_ring` Â· `compass_rose` Â· `fascia_metric_v0` Â· `vols_survey_witness` Â· `glow_riscv_target` Â· `pond_ring_drawn_terminal`), each checked for print-string coupling, then the two parity chapters (178/270 own-prose), then the read-true close (widen the extractor to `.rish` per REDS #74, re-run VOLS, prove `guarded_sites` 79) closing Journey 14. **LOWE-J14r4d-iv-b LANDED `20260812`:** the three **safe witness-prose** tools surfaces â `cellar_first_ring.rish` (a `#` doc-comment header) Â· `compass_rose.rish` (`say` + assert-else packet labels) Â· `pond_ring_drawn_terminal_witness.rish` (a `#` comment + one `say`). docs-implementation-sync checked first: `parity_ch01` runs cellar + compass and asserts `.ok` **only** (never `contains` on their printed `lap` strings), and pond_ring has no driver at all â so relabeling their own prose breaks nothing. cellar's header named `Cellar first ring: â¦ Â· 20260801`; compass's packet labels named by capability (`Dimeroll books-view packet` Â· `Dimeroll sight-view packet` Â· `inference stack packet`) with every dated `lap1/lap3/lap0`-no-space file path left untouched (the guard bites only `lap <space><digit>`); pond_ring's `Lap 3` session named `headless session`. Both cellar (line 11) and compass (line 66) carry **pre-existing** env/build failures upstream of the prose edits (git-stash A/B: baseline fails identically), and pond_ring is Wayland-metal (`WAYLAND_DISPLAY` unset in this jail) â the relabels are behavior-neutral `say`/comment prose. 3 surfaces added to `labeling_module_scan.sh`; CION guard GREEN over the widened set (drift 0, negative still bites); tame_style_check clean; `guarded_sites` 71 â 74 of 79. **Deferred coupled/special rounds:** `fascia_metric_v0.rish` (asserts `contains "GREEN: Amphora lap N"` on the amphora fold â a printerâasserter coupling, its own atomic round) Â· `glow_riscv_target_witness.rish` (large, qemu-gated, `Nock lap 2/3` in say strings) Â· `vols_survey_witness.rish` (bare `lap 3`/`lap 5` are **pedagogical examples** of the drift pattern in the survey's own doc-comments â a relabel would undo its teaching). **LOWE-J14r4d-iv-c LANDED `20260812`:** the `fascia_metric_v0.rish` coupling closed **without touching a kept handle** â a tree-wide audit proved it is the **only** asserter of `contains "GREEN: Amphora lap N"`, and the amphora witnesses it reads (`tools/gen/amphora/amphora_lap{1,2,3}.rish`) are **kept handles** (VOLS ledger `handle gap`) whose `lap N` prints the law legitimately keeps. So rather than churn the handles, fascia's own asserts were reworded to check each handle's **distinctive semantic phrase** (`manifest entry + vessel seated` Â· `Cellar season poured` Â· `vessel cargo fetched over Comlink`) instead of the bare ordinal â same verification (each specific witness greened), drift gone from fascia's scanned prose, handles untouched; the i5 header comment + three fold error messages named by capability. Behavior-neutral: A/B shows baseline and edited both fail at the same pre-existing line-29 `amphora2.ok` env check (amphora_lap2's Cellar-pour needs a built binary this jail lacks â the same condition as cellar's line 11), upstream of every reworded assert. Added to `labeling_module_scan.sh`; CION guard GREEN; tame_style_check clean; zero residual bare `lap N`; `guarded_sites` 74 â 75 of 79. **Next agent-doable crux:** LOWE-J14r4d-iv-d â `glow_riscv_target_witness.rish` (large, qemu-gated, `Nock lap 2/3` in say strings â check `contains` coupling) + `vols_survey_witness.rish`'s **pedagogical** `lap 3`/`lap 5` examples (reword to keep the teaching without a bare ordinal, or hold as a survey-of-itself exception) + the two parity chapters (178/270 own-prose), then the read-true close (widen `vols_guardlist_extract.sh` to `.rish` per REDS #74, re-run VOLS, prove `guarded_sites` 79) closing Journey 14.

- **BUHR MCP surface arc â all three primitives owned `20260812`** (accreting past the J9r2 detail in custody-gate item 1): **J10 (resources)** served Tablecloth artifacts as MCP resources â `pond/apps/mcp_resource.rye` `resources/read` returns exactly the bytes the `tablecloth:<name>` URI's digest pins (a tampered store â `resource_tampered`), and `mcp_resource_list.rye` `resources/list` advertises the catalog as an **honest index** (every listed URI reads at its published length; tamper â `NotHonest`). **J11 (prompts) â agent-doable arc COMPLETE** (self-approved design round `active-designing/20260812-153255_â¦`): **r1** `pond/apps/mcp_prompt.rye` `prompts/get` fills a `format mcp-prompt-v1` template into a body holding **no live placeholder** (four named refusals: `prompt_not_found` Â· `unknown_argument` Â· `missing_argument` Â· `unbound_placeholder`); **r2** `pond/apps/mcp_prompt_list.rye` `prompts/list` advertises a prompt book as an **honest index** (name + arity, `NotHonest` on disagreement); **r3** `pond/apps/mcp_prompt_voice.rye` â the fusion â fills a template then routes the finished text through the Q-vane host, so **the filled words choose the voice** (`matmul the pair`âLattice `19 22 43 50`; `lane_count the graph`âgraph `3`), every fill/voice refusal crossing back named, never routed half-filled. Witnesses `tools/buhr_mcp_prompt{,_list,_voice}_witness.rish` all GREEN, TAME + width clean. **All three MCP surfaces now owned end to end â tools (J9) Â· resources (J10) Â· prompts (J11).** Remaining BUHR door: **Comlink-served** (the serve gate, Keaton's hand).

- **The double-seat expansion SEATED `20260813.020035`** (`active-designing/20260813-020035_double-seat-expansion-six-seasons.md`) â a large vision dump loaded in whole as **six new to-do seasons** (Keaton's number: 4 or 6 â chose 6), double-seated **beside** the fixed 1,024-round itinerary (it doubles the allocation; the seated road never moves). Seasons: **A Hardware & Right-to-Repair** (Mikrophone = GrainphoneÃMantrapod buyable-now, firmware-first Â· colour e-ink Â· refurb-parts marketplace McMaster-style single-sprite render Â· Photos app) Â· **B Linengrow/Dimeroll** (finance rail Â· DimerollâDimeroll breach Â· press release) Â· **C Lotus** (creative suite/DAW â already a yonder doc, horizon; wire protocols XLR/USB-C/guitar basic, audio-interface design PAUSED yonder) Â· **D Kresfa & Mycelium** (.kres supersedes .myc + Sui Move Â· Mycelium consensus over TigerBeetle+Mysticeti Â· Puddle = Aurora fleet orchestration Â· constel test-nets) Â· **E Robotics & Mandate** (SiFive SKI Â· RISC-V veganic robotics Â· Mandate legislation all US states/territories Â· seawater civic ships Â· MMT budget) Â· **F Surface & Namespace** (Skate "interface" like outfit Â· council skyâconstel sky Â· .com > .sol/.sui Â· gratitude syntax.sh/Emory Jensen + ultrametric.ai Â· queyqwinqkri Twilight theme Shatabhisha/Pisces Â· batonÃChitra ASCII-hardware Â· root-README molt-tracking â depersonalized seed force-push, "initial public seed" wording). **Breach queue prepped, cairn planted `0a074d5059`** (`work-in-progress/CAIRNS.md`), **not cut this round**: DimerollâDimeroll Â· .mycâ.kres Â· council skyâconstel sky Â· deep debride + seed force-push (**gate #1, Keaton's hand**). Bought: `kresfa.com` Â· `constel.net` Â· `dimeroll.com`. Loop re-aimed (`tools/launch-claude-season.rish` steps 4â5 now read the expansion doc too; a breach records a cairn before it cuts). **VPS pier finding:** 3.9 GB RAM idle, **0 swap** â a 2â4 GB swapfile is the one crash-headroom win (provisioning = Keaton's hand). Twilight style approved for these prose+comments (discerning).

- **GRAD-J16r2 LANDED `20260813` â the whole CION conversion witnessed complete and sealed** (`active-designing/20260813-021123_grad-journey16-witness-whole-conversion-exploration.md`, self-approved). Season 4 (Seal) is the fixed road's last season; with LOWE Journey 14 (Molt) closed and OFFY (Debride, J15) a custody gate, the next agent-doable Lindy-first crux was Journey 16's other named charter half â **witness the whole conversion**. Two living artifacts still spoke as if the molt were unfinished: the seated survey ledger (rendered pre-LOWE at `20260812.221045`, `sites 223 Â· prose_gaps 79`) and `grad_seal`, which sealed guarded files **clean** yet never the conversion **complete** (a bare `lap N` born outside every guard would pass both halves). Measured on metal, not memory: `vols_classify_scan` over the live tree renders `sites 144 Â· handles 144 Â· prose_gaps 0` (144 self-consistent site lines) â every one of the 79 prose gaps LOWE relabeled is gone. **Move 1:** re-seated `work-in-progress/vols-survey-ledger.bron` from the live tree at `20260813.021357` (`prose_gaps 0`), the re-seat sanctioned by `vols_survey_true_witness`'s own comment; the pre-LOWE render rests in git + the dated LOWE logs (accrete-never-break â a living regenerable pin). **Move 2:** added a **third seal half** to `grad_seal_witness.rish` â positive asserts live `prose_gaps=0` (+ `guarded_sites=0`), negative classifies the `vols_classify_tree` fixture (still holding one unconverted gap) and asserts `prose_gaps=1`, so the completeness seal **bites** a molt left unfinished. The seal now guards the **whole tree** â a bare-ordinal prose identity born *anywhere* raises the count and bites, honest to handles (a filename-ordinal witness classifies as a handle, never a gap). GREEN on metal: `grad_seal` (three halves, completeness bites the fixture) Â· `vols_survey_true` (re-seated pin reads true + self-consistent 144==144) Â· `cion_module_labeling` guard. **The surveyâmoltâseal loop is closed: VOLS named 79, LOWE relabeled 79, GRAD seals that 0 remain.** No custody crossed â seats a guard and re-seats a living pin, cuts nothing. **Next agent-doable crux:** the Journey 16 GRAD/KOFF close (consolidate Â· read the 3/4 Â· benediction). **Gates that remain Keaton's hand:** OFFY debride cuts (gate #5) Â· deep-debride + seed force-push (gate #1).

- **Constel test-network naming law SEATED `20260813`** (`active-designing/20260813-022222_constel-test-network-naming-law.md`, self-approved â the double-seat expansion reserved this round). A **constel** is a fake, self-invented constellation for a sandbox/testnet/localhost Comlink p2p network run **inside the jailed pier** (as Urbit's fake-galaxy dev nets stood to Arvo). One load-bearing promise, structural: **every constel name carries a digit**, so it can never parse as a real `@p` (letters + hyphens only, no digit in the 256-syllable table) â the certainty placeholder-ship-names earns from segment length, the shape `xykj61`/`xnkg30` already wear. Seated the law doc, a first-draws fixture (`xnqvel34 Â· xzkerith58 Â· xwmur-dol12 Â· maqwintel06 Â· sunveknar90`), `tools/fixtures/constel_naming_scan.sh`, and `tools/constel_naming_witness.rish` GREEN (5 seated names safe; planted `@p`-shaped `sarlev` bites `verdict=unsafe`). Siloed, dev-only â never a seed artifact, never a real address, never one of the maintainer's real points; reaches no gate. Aesthetic revisable; the digit is the only guarded invariant. When the real Comlink dev-net harness is built it draws from this law.

- **Constel dev-net harness r1 LANDED `20260813`** (`active-designing/20260813-022908_constel-devnet-harness-exploration.md`, self-approved) â the harness the naming law named its own next rung. With the fixed 1,024-round road complete and no red open, the next Lindy-first crux-first move in Season D is the dev-net every future Comlink/settlement/Mycelium test runs against inside the jail. `pond/apps/constel_net.rye` fixes the crux: **a fake dev-net can only be born from a provably-non-`@p` name (a digit anywhere), and under that name it runs the real settlement protocol from genesis** â the name carries the quarantine, not a flag. `spin_up` refuses `UnsafeName` **before** any genesis opens (structural: no dev-net exists un-quarantined); `name_safe` mirrors `constel_naming_scan.sh`'s digit-check in Rye, so the harness draws from the seated law; then a real `settlement.open` galaxy opens from demo seeds and its Deed verifies (composed exactly as `ship_pilot_identity`). Selftest GREEN (5 cases): safe-name genesis Â· all 5 seated draws safe Â· `@p`-shaped `sarlev` refuses `UnsafeName` no genesis Â· non-galaxy number refuses `NotGenesis` behind a safe name Â· empty/overlong refuse before genesis. Witness `tools/constel_net_witness.rish` GREEN, re-runs the naming law at its door; TAME + width clean; no bare-ordinal prose. Demo seeds only â no key, no network, no custody (gate #4 untouched; a served constel reaches the serve gate). **Constel dev-net harness r2 LANDED `20260813`:** `pond/apps/constel_net_topology.rye` grows the fake net downward â a **star beneath the galaxy, a planet beneath the star**, each seated by `settlement.mint` under its rightful d12Â·d60 topology sponsor (galaxy 3 Â· star 27 Â· planet 267, the same branch `settlement/names.rye` seats on real ground), so the **whole fractal address space settles inside the quarantined net**, every point crowned by the one law-safe name. Sponsorship is enforced, not claimed: a planet minted directly under the galaxy refuses `WrongSponsor` (it must climb through its real star), and a forged spawn authority refuses `BadAuthority` â the real settlement protocol runs behind the name gate, so the fractal the tree settles on real ground settles identically on the dev bench. Composes r1's `spin_up` public API (r1 re-runs GREEN at r2's door); the galaxy keeper sows the star, the star keeper sows the planet (the same authority chain the ledger enforces). Selftest GREEN (3 cases: full branch settles + verifies Â· wrong-sponsor bites Â· forged-authority bites). Witness `tools/constel_net_topology_witness.rish` GREEN, TAME + width clean; demo seeds only, no key/network/custody. **Constel dev-net harness r3 LANDED `20260813`:** `pond/apps/constel_net_bron.rye` makes the net **travel as text** â `from_settled` builds a `PortableConstel` (owned name buffer + roster of point numbers) from r2's settled branch; `render` emits a `format constel-net-v1` record (a `net <name>` line + one `point <number>` line each, galaxy Â· star Â· planet in settle order); `parse` reads it back **byte-for-byte** (re-rendering the parsed record reproduces identical bytes â a fixed point). The distinctive r3 crux: **the quarantine travels with the bytes** â `parse` re-checks the naming law, so a record whose net name carries no digit refuses `UnsafeName` (a roster can never be read under a name that could parse as a real @p), exactly as `spin_up` refuses one at genesis. Purpose-built bounded flat-Bron reader: unknown field â `UnknownField` (never dropped) Â· wrong header â `BadFormat` Â· missing net line â `MissingNet` Â· non-numeric point â `BadInt` each refuse. Composes r1 (`name_safe`) + r2 (`SettledNet`) public API; the witness re-runs r2 GREEN at r3's door. Selftest GREEN (6 cases: round-trip recovers name+roster Â· unsafe-name-travels bites Â· unknown/header/missing-net/bad-int refuse). Witness `tools/constel_net_bron_witness.rish` GREEN, TAME + width clean; demo seeds only, no key/network/custody. **Constel dev-net harness r4 LANDED `20260813` â the harness's agent-doable arc CLOSES:** `pond/apps/constel_net_true.rye` carries the reader onto a **real on-disk constel record** â `tools/fixtures/constel_net.bron`, produced reproducibly by `pond/apps/constel_net_fixture_gen.rye` (the branch galaxy 3 Â· star 27 Â· planet 267 under `xnqvel34`), `@embedFile`-bound through a `pond/apps/corpora/` symlink. Two readings must agree: the app parses through r3 and reports **roster size Â· galaxy Â· planet**, and the witness cross-checks that triple against `tools/fixtures/constel_net_truth.awk` â **two tools, one answer (`3 3 267`)**. The record is a genuine, law-safe harness net: re-rendering the parsed roster reproduces the embedded bytes exactly (the fixture is what the harness emits, not a hand-mock), and the recovered name still carries a digit (the quarantine holds on the real bytes); a tampered header refuses `BadFormat`. Composes r3 public API (r3 re-runs GREEN at r4's door). Selftest GREEN (3 cases + app==awk crux), TAME + width clean; demo seeds only, no key/network/custody. **The Constel dev-net harness stands complete end to end â r1 named genesis Â· r2 the fractal settles Â· r3 the net travels Â· r4 reads true.** The one remaining harness horizon rung â **Comlink-served** (a constel shared over the wire) â reaches the serve custody gate (Keaton's hand). **Next agent-doable crux:** a fresh Season D journey worth a self-approved design read â the dev-net now stands as a bench, so the natural next crux is a real Comlink handshake or settlement test *running against a spun-up constel* (proving the harness earns its keep), or the Mycelium consensus design read (over TigerBeetle+Mysticeti). The three breach renames (DimerollâDimeroll Â· .mycâ.kres Â· council skyâconstel sky, each cairn-first) and the deep debride + seed force-push stay Keaton's hand (gates #1/#5).

- **Constel dev-net ledger r1 LANDED `20260813`** (`active-designing/20260813-030005_constel-devnet-ledger-exploration.md`, self-approved) â the harness's own named next crux, taken: a real settlement/economic test *running against a spun-up constel*, proving the bench earns its keep. `pond/apps/constel_net_ledger.rye` gives the tree's real Mycelium supply primitive (`mycelium/fold.rye` â supply as a pure fold over Kumara-signed facts, `Î£ issued â Î£ taxed`, non-negative, an overdraw refusing the whole fold) **a network to belong to**. The crux, fused: **an economic ledger runs from genesis inside the fake net â its money signed by the net's own settled identity, refused for anyone else â and it can never run under a name that could be real.** Two promises hold at once: the **quarantine wraps the ledger, not only the net** (`open_ledger` opens only through `spin_up`, which refuses `UnsafeName` before a single genesis â no code path to a ledger under an @p-shaped name), and **sovereignty is structural** (`admit` refuses any fact whose signer's key is not the genesis galaxy Deed's `keeper` â `NotSovereign` before the log; `issue`/`tax` mint sovereign-signed facts, the sovereign keypair derived from the same keeper seed that settled genesis, asserted equal to `galaxy.keeper` â settlement identity gates the Mycelium supply, neither module weakened). Selftest GREEN (5 cases): a law-safe net opens a ledger whose sovereign is the genesis keeper (fresh supply 0) Â· issue 100 / tax 30 / issue 20 folds to supply 90 over three sovereign facts Â· an @p-shaped name opens **no** ledger (`UnsafeName`) Â· a tax of 80 over supply 50 refuses `Overdraw` whole (supply held at 50, log untouched â Mycelium non-negativity holds in the fake net) Â· an alien signer's internally-honest fact refuses `NotSovereign` before the log (log stays empty). Witness `tools/constel_net_ledger_witness.rish` GREEN, re-runs the harness (`constel_net`) **and** the Mycelium fold at its door; TAME bans + width clean. Composes `constel_net.spin_up` + `mycelium/fold` public API only, invents no storage, weakens no bound. Demo keeper seeds only â the Mycelium fold is a supply *model*, not a payment rail (gate #3 untouched), no real key/funds/network/custody (gate #4 untouched; a served ledger reaches the serve gate). **Constel dev-net ledger r2 LANDED `20260813`:** `pond/apps/constel_net_ledger_stars.rye` carries the **third Mycelium fact kind â `star_reserve`** â into the sovereign ledger, mirroring the harness's own r2 (a star + planet settling beneath the galaxy): the ledger's *names* grow beside the topology's *points*, every reservation paid out of the net's own supply. `reserve_star` mints a sovereign-signed reservation and admits it; a reservation is **sovereign-only** (`NotSovereign` before the log), **unique** (a second reservation of a held name refuses `StarTaken` whole, supply held), and **drains supply exactly as tax does** (a reservation priced beyond supply refuses `Overdraw`, non-negativity holding). Additive â r1 grew only a pub reader `signer_is_sovereign_of` (the exact private gate `admit` uses, so one authority test never drifts into two); r1 re-runs GREEN at r2's door. Selftest GREEN (4 cases: issue 100 then reserve vega(30)+rigel(20) drains to 50 Â· double-vega refuses StarTaken Â· overpriced refuses Overdraw Â· alien reservation refuses NotSovereign). Witness `tools/constel_net_ledger_stars_witness.rish` GREEN, TAME + width clean; demo seeds only, no key/funds/network/custody. **Constel dev-net ledger r3 LANDED `20260813`:** `pond/apps/constel_net_ledger_bron.rye` makes the ledger **travel as text** â `from_ledger` builds a `PortableLedger` (owned name buffer Â· sovereign key Â· roster of signed facts) from a live ledger; `render` emits a `format constel-ledger-v1` record (a `net <name>` line Â· a `sovereign <pk-hex>` line Â· one `fact <kind> <amount> <star> <sig-hex>` line each); `parse` reads it back and `supply` re-folds the reconstructed facts through the real Mycelium law. Two promises ride in the bytes, distinct from the harness's number-only r3: the **quarantine travels** (a record whose net name carries no digit refuses `UnsafeName`, exactly as `open_ledger` refuses one at genesis) and the **sovereignty travels** (each fact binds the carried sovereign key as its signer, and the fold re-verifies every signature against it â a record whose `sovereign` line does not match its facts refuses `IdentityRefused`; the sovereign key is load-bearing in the bytes, not a note). Round-trip proven: recovers safe name + sovereign key + supply 50 over three facts, re-render byte-for-byte identical (a fixed point). Purpose-built bounded flat-Bron reader: unknown field Â· bad header Â· missing net Â· missing sovereign Â· bad hex each refuse. One build red caught same-round (the tokens buffer was sized [4] but a fact line carries five tokens â grew to [5]). Composes r1 + r2 public API (r2 re-runs GREEN at r3's door). Selftest GREEN (8 cases). Witness `tools/constel_net_ledger_bron_witness.rish` GREEN, TAME + width clean; demo seeds only, no key/funds/network/custody. **Constel dev-net ledger r4 LANDED `20260813` â the ledger's agent-doable arc CLOSES:** `pond/apps/constel_net_ledger_true.rye` carries the reader onto a **real on-disk ledger record** â `tools/fixtures/constel_net_ledger.bron`, produced reproducibly by `pond/apps/constel_net_ledger_fixture_gen.rye` (issue 100 Â· tax 30 Â· reserve vega 20 under `xnqvel34`, supply 50), `@embedFile`-bound through a `pond/apps/corpora/` symlink. The reader parses through r3, **re-folds** the record through the real Mycelium law (so the reported supply is the law's own fold, its signatures re-verified against the carried sovereign key), and reports **fact count Â· supply Â· reservations**; the witness asserts that triple equals `tools/fixtures/constel_net_ledger_truth.awk`'s reading (`3 50 1`, two tools one answer â a signature-verifying fold vs a bare arithmetic sum agreeing). Two genuineness proofs beyond the triple: the generator re-emits byte-identical bytes (not a hand-mock) and re-rendering the parsed ledger reproduces the embedded bytes exactly, the recovered name still law-safe; a tampered header refuses `BadFormat`. Composes r3 public API (re-runs GREEN at r4's door). Selftest GREEN (3 cases + measure). Witness `tools/constel_net_ledger_true_witness.rish` GREEN, TAME + width clean; demo seeds only, no key/funds/network/custody. **The Constel dev-net ledger stands complete end to end â r1 sovereign genesis Â· r2 star reservations Â· r3 travels as text Â· r4 reads true.** The one remaining ledger horizon rung â **Comlink-served** â reaches the serve custody gate (Keaton's hand). **Next agent-doable crux:** the Mycelium consensus design read (over TigerBeetle+Mysticeti) now that a fake net runs the real supply law end to end, or a fresh Season D journey worth a self-approved design read. Breach renames + deep debride stay Keaton's hand (gates #1/#5).

- **Mycelium consensus â the Cord OPENED `20260813`** (Season D; design read `active-designing/20260813-032851_mycelium-consensus-cord-exploration.md`, self-approved). With the constel dev-net ledger closed, REMEMBER's live edge named the next Lindy-first crux plainly â the Mycelium consensus design read over TigerBeetle + Mysticeti. The design read fixed the shape, the name, and the four crux-first rounds: from **TigerBeetle**, determinism + static allocation (mycelium/fold already keeps this â a pure fold over signed facts, static log 1024); from **Mysticeti** (Mysten Labs, Bullshark/Narwhal lineage, studied clean-room under Sui's permissive license), order read from an **uncertified DAG** by a deterministic commit rule, no leader, no certificate round. The synthesis earns its own clear name per `comlink-tendency` â **Weave** is already Mantra's text-history CRDT, so the consensus-ordering layer is **Cord** (a mycelial rhizomorph: bundled hyphae transporting as one; grepped zero collisions). Four rounds: **r1** deterministic commit Â· **r2** Byzantine refusal (equivocation, cycle, f<n/3) Â· **r3** travels as `format cord-dag-v1` Bron Â· **r4** read a real fixture true; the *improving-on-both* aim named as a **horizon**, not a witnessed benchmark (a later `loom` round). Also gitignored `/mycelium/bin/` (build artifact). **Cord r1 LANDED `20260813` â the crux CLOSED:** `mycelium/cord.rye` builds a bounded Dag of Kumara-signed Blocks (author key Â· round Â· parent hashes Â· one `mycelium/fold` Fact Â· author signature over canonical bytes); `add` verifies the block signature and admits a block only when **every parent hash it names is already held** (so the graph is acyclic by construction â a block may only point backward), refusing `BadSignature` Â· `DanglingParent` Â· `TooManyParents` Â· `DagFull` Â· `Duplicate` whole; `commit` is a **pure canonical topological walk** (round asc, then author key, then block hash) yielding one total order over which `fold_log` runs unchanged. The crux proven on metal: **four blocks added in three different arrival permutations commit to one byte-identical order and one identical fold state** (arrival-order independence â the Mysticeti insight under TigerBeetle bounds), the agreed order folding the real Mycelium law to supply 700 over one star; a tampered block signature, a dangling parent, and a duplicate each refuse before the Dag grows. Selftest GREEN first pass; witness `tools/mycelium_cord_witness.rish` GREEN (re-runs the Mycelium fold at its door), TAME bans clean, width-check exit 0. Composes public API only (mycelium/fold + tally/kumara), invents no storage, weakens no bound. Demo keeper seeds only â no key/funds/network/custody; a Cord gossiped over the wire reaches the serve gate. **Cord r2 LANDED `20260813` â Byzantine refusal:** the adversary a leaderless DAG must survive is **equivocation** â one author publishing two distinct blocks at the same round, hoping honest nodes admit different ones and split history. The Cord quarantines it by admission control (an author speaks at most once per round). Additive â r1's `add` stays a green binary; r2 is `mycelium/cord_byzantine.rye` composing r1 public API; `cord.rye` grew only two small pubs (`address` â a block's SHA-256, telling a same-hash `Duplicate` from a different-hash equivocation; `verify_block` â the signature check extracted so `add`/`add_bft` share one crypto check), r1 re-running GREEN. `add_bft` rejects a forgery (`BadSignature`) **before** classifying an equivocation (`Equivocation`), then delegates to r1's `add`. **The r2 crux on metal:** the honest four-block commit order + fold state are **byte-identical with a quarantined equivocator** (author A's second distinct genesis at round 0 refused, the Dag unchanged, commit re-run identical â supply 700, one star). Supporting: the same author at a **later** round is admitted (per-round, not per-author); a forward reference refuses `DanglingParent` (a block points only backward â no cycle); a forgery refuses `BadSignature`, never mislabeled. The `f<n/3` framing honored as a **mechanism** (safety by admission control), a simulated benchmark a named later `loom` round (Two Rooms). GREEN first pass; witness `tools/mycelium_cord_byzantine_witness.rish` GREEN (re-runs r1 at its door), TAME + width clean. **Cord r3 LANDED `20260813` â the Cord travels:** `mycelium/cord_bron.rye` renders a committed Dag to a `format cord-dag-v1` record (one signed `block` line each in committed order â author key Â· round Â· fact Â· fact signature Â· block signature â parents as following `parent <hash>` lines) and parses it back. The whole block rides (both signatures + every fact field; star/body hex-encoded with a `-` sentinel â Cord facts carry memos as bodies), so the recovered block is byte-identical. **The r3 crux â the agreed ORDER travels with the bytes:** the recovered Dag, re-admitted under r1's own `add` (every signature re-verified, every parent already held), commits to the **byte-identical order** and folds the identical supply (700, one star) â two nodes agree by exchanging blocks alone. Re-rendering reproduces the exact bytes (fixed point); a tampered block refuses `BadSignature` on rebuild â corruption cannot cross as consensus. Bounded flat-Bron reader refuses `BadFormat`Â·`UnknownField`Â·`BadLine`Â·`BadInt`Â·`BadHex`Â·`ParentBeforeBlock`Â·`TooManyParents`Â·`TooManyBlocks`. Added a `mycelium/parse_int.rye` symlink (matching the kumara/tally_copy pattern). GREEN first pass; witness `tools/mycelium_cord_bron_witness.rish` GREEN (re-runs r1 and r2), TAME + width clean; additive (r1/r2 unchanged). **Cord r4 LANDED `20260813` â the Cord reads true, the arc CLOSES:** `mycelium/cord_true.rye` `@embedFile`s a real `format cord-dag-v1` record (`tools/fixtures/cord_dag.bron`, produced reproducibly by `mycelium/cord_fixture_gen.rye`), parses it through r3, **rebuilds the Dag under r1's own `add`** (every signature re-verified, every parent already held), commits, and folds the supply through the real Mycelium law, reporting **blocks Â· supply Â· stars**. The witness cross-checks that triple against `tools/fixtures/cord_dag_truth.awk` â a signature-verifying rebuild+fold and a bare arithmetic scan both give **`4 700 1`** (two tools, one answer), so the agreed supply can never drift from a record a keeper adds up by hand. Genuineness: the fixture is rendered in agreed order, so re-committing reproduces the file's order (identity permutation â else `OrderNotCanonical`); re-rendering reproduces the embedded bytes exactly; the generator re-emits byte-identical bytes (`REPRODUCIBLE`); a tampered header refuses `BadFormat`. GREEN first pass; witness `tools/mycelium_cord_true_witness.rish` GREEN (re-runs r3, hence r1/r2), TAME + width clean; additive. **The Mycelium Cord stands complete end to end â r1 deterministic commit Â· r2 Byzantine refusal Â· r3 travels Â· r4 reads true.** The one remaining Cord rung â gossiped over the wire â reaches the serve gate. **Next agent-doable crux:** a fresh Season D journey (Kresfa/`.kres` language ecosystem, or Puddle fleet orchestration of the Aurora hypervisor) or another double-seat season's crux, each a self-approved design read first. Breach renames (cairn-first) + deep debride + seed force-push stay Keaton's hand (gates #1/#5).

- **Mycelium fact batching â COMPLETE `20260813` (r1Â·r2Â·r3Â·r4)** (Season D; design read `active-designing/20260813-043303_mycelium-fact-batching-exploration.md`, self-approved). With the Cord and gossip-convergence arcs closed, the next in-domain Lindy-first crux is the Cord's own named refinement: **one block seals a bounded batch of facts**, the TigerBeetle amortization (studied clean-room) where a block's fixed cost â one author signature, one node in the agreed order, one round of admission â buys many facts rather than one. Grown as a **sibling** (`mycelium/cord_batch.rye`), keeping the elder single-fact `cord.rye` and its whole witnessed arc a green binary. A `BatchBlock` seals up to `max_facts_per_block` (16) facts under one author signature; the canonical block bytes bind each fact's own signature, so a tampered fact anywhere breaks the block signature at `add_batch`; `commit_batch` reuses the elder's exact topological rule (round asc Â· author key Â· block hash); `committed_batch_log` flattens committed batches to the single fold stream. **The crux on metal:** the same four facts, folded one-per-block across the elder Cord and packed as two batched blocks (round-0 genesis pair Â· round-1 tax+reserve), yield **byte-identical** issued/taxed/supply/star_count (1000/300/700/1) â batching is a throughput transform, never a semantic one; the supply never notices how facts were packed. Refusals hold whole: `BatchFull` (mint 17), `EmptyBatch` (mint 0), a tampered fact (`BadSignature`), a dangling parent, a duplicate â the Dag never grows on a fault. Witness `tools/mycelium_cord_batch_witness.rish` GREEN (elder Cord + Mycelium fold both re-run GREEN at its door), TAME + width clean. Additive â composes `cord`+`fold` public API only; demo seeds, no key/funds/network/custody; a batched Cord gossiped over Comlink reaches the serve gate. **r2 LANDED `20260813`:** `mycelium/cord_batch_bron.rye` â the batched Dag travels as a `format cord-batch-v1` record (per block a `block` line, one `fact` line per batched fact, then `parent` lines); the recovered Dag re-admitted under `add_batch` commits to the **byte-identical order** and folds the identical supply (700, one star), re-rendering is a fixed point, a tampered fact refuses `BadSignature` on rebuild, and a wrong header Â· unknown field Â· fact-before-block Â· empty batch each refuse. Fixed a real red in-round â a stack overflow from returning the 1.77 MB `BatchDag` by value â `seat`/`rebuild` now write through an out-pointer (`max_record` shrank to 16384). Witness `tools/mycelium_cord_batch_bron_witness.rish` GREEN (r1 + elder cord-bron re-run GREEN), TAME + width clean. **r3 LANDED `20260813`:** `mycelium/cord_batch_bench.rye` â a fixed 32-fact corpus arranged one-per-block (32 blocks) and batched into full 16-fact batches (2 blocks) folds to the identical supply (320); Two Rooms honesty â asserts only the exact (identical supply Â· block counts), **reports** the amortization (`loom â¦ blocks_single=32 blocks_batched=2 block_ratio_x10=160`, a measured 16x fewer block signatures), never an asserted invariant; a wall-time benchmark stays a later loom round. Witness `tools/mycelium_cord_batch_bench_witness.rish` GREEN (r1 + r2 re-run GREEN), TAME + width clean. **r4 LANDED `20260813` â the arc CLOSES:** `mycelium/cord_batch_true.rye` `@embedFile`s a reproducibly-generated real `format cord-batch-v1` record (`tools/fixtures/cord_batch_dag.bron`, via `mycelium/cord_batch_fixture_gen.rye` + a `mycelium/corpora/` symlink), parses it through r2, rebuilds under `add_batch` (every signature re-verified), commits (identity permutation asserted), folds, and reports blocksÂ·supplyÂ·stars; the witness cross-checks that triple against `tools/fixtures/cord_batch_truth.awk` â **app == awk, `2 700 1`** (two tools, one answer). Re-render is a fixed point, the generator is `REPRODUCIBLE`, a tampered header refuses `BadFormat`. Witness `tools/mycelium_cord_batch_true_witness.rish` GREEN (r2 + r3 re-run GREEN), TAME + width clean. **The Mycelium fact-batching arc stands complete end to end â r1 folds identical Â· r2 travels Â· r3 throughput win Â· r4 reads true. Next agent-doable crux:** a fresh Season D journey (Kresfa/`.kres`, Puddle) or another double-seat season's crux, each a self-approved design read first. The **supersede-the-core** ruling (evolving `cord.Block` in place, a module-seam decision) and breach renames (cairn-first) + deep debride + seed force-push stay Keaton's hand (gates #1/#5).
- **Mycelium Knot â OPENED `20260813` (r1)** (Season D; self-approved design read `active-designing/20260813-050903_mycelium-cord-knot-checkpoint-exploration.md`). With the Cord, gossip-convergence, and fact-batching arcs all closed, the next Lindy-first crux on the Mycelium road is the one durable capability the Cord still lacked: its `Dag` is statically allocated (`cord_max_blocks` 256) â a TigerBeetle feature â yet a bound reached is a mesh that stops. Every long-lived deterministic state machine checkpoints: fold the committed past into a summary, prune it, continue from the summary as if from genesis; without it the bounded Dag is a demo, with it the same Dag runs forever. Named the **Knot** (`comlink-tendency`: a knot ties off a length of cord and begins the next; zero collisions grepped), grown as a **sibling** `mycelium/cord_knot.rye` keeping `cord.rye`+`fold.rye` green binaries. A `Knot` seals a committed Cord's `fold.FoldState` (issued Â· taxed Â· every reserved star) under a streaming SHA-256 digest â **no signature** (a Knot is a deterministic function of the committed Dag; the digest is integrity-in-travel, never authority; Kumara still signs each fact, the Cord still orders, the fold still decides supply). `seal_dag` commits+flattens+folds+seals; `continue_verified` folds a next-epoch log onto a verified Knot. **The crux on metal:** a prefix Dag sealed to a Knot, a fresh suffix epoch continued onto it, folds to the **byte-identical supply** (1000/300/700, one star `vega`) as the whole history from empty â a deep `states_match` over issuedÂ·taxedÂ·the whole star set. Both boundaries hold (the Knot at genesis Â· the full-prefix Knot). **Every fold invariant crosses the cut** (because `fold_fact` carries the law in the state it mutates): a corrupted Knot refuses `KnotCorrupt`, a suffix tax 800 past a checkpointed supply 700 refuses `Overdraw`, a suffix star already sealed refuses `StarTaken` â the cross-cut facts built raw (append_fact probe-folds from empty and would itself refuse them). One red caught: `append_fact` returns a `FoldState`, not void. Witness `tools/mycelium_cord_knot_witness.rish` GREEN (elder Cord + Mycelium fold both re-run GREEN at its door), TAME + width clean. Additive â composes `cord`+`fold` public API only; demo seeds, no key/funds/network/custody; a **quorum-agreed** checkpoint (a node pruning on a checkpoint it did not compute) reaches the Comlink-served serve gate, Keaton's hand. **r2 LANDED `20260813`:** `mycelium/cord_knot_bron.rye` â a Knot travels as a `format cord-knot-v1` record (issued Â· taxed Â· one hex `star` line per reserved star Â· digest hex), `render`/`parse` (through an out-pointer, the batch-bron stack-safety idiom). The crux: render â parse recovers a Knot that verifies and carries the **byte-identical** sealed supply (700, one star), continues to the identical supply as the original (a fresh 300 issue onto each â both issued 1300), and re-rendering is a **fixed point**. A tampered star breaks the digest â `continue_verified` refuses `KnotCorrupt` (a tampered checkpoint never crosses as trusted); bad header Â· unknown field Â· bad hex each refuse; over-full star set refuses `TooManyStars` (max 64). GREEN first pass; witness `tools/mycelium_cord_knot_bron_witness.rish` GREEN (r1 Knot re-runs GREEN at its door), TAME + width clean. **r3 LANDED `20260813`:** `mycelium/cord_knot_bench.rye` â checkpointing is a genuine bound win, measured. The ceiling is real (a fresh Dag filled to `cord_max_blocks` 256 refuses the next block `DagFull`); a checkpoint chain of 5 windows of 64 blocks folds **320 facts past the ceiling** (each epoch a bounded Dag sealed to a Knot then pruned, the next continuing onto it) to the **byte-identical** whole-history supply (320), the **live Dag never exceeding 64**. Two Rooms: asserts only the exact facts, reports the amortization as a `loom` line (`total_facts=320 dag_ceiling=256 max_live_blocks=64 windows=5 live_ratio_x10=50 would_overflow=yes` â 5.0x fewer live blocks, which the ceiling makes impossible not merely costly); wall-time stays a later round. GREEN first pass; witness `tools/mycelium_cord_knot_bench_witness.rish` GREEN (r1 + r2 re-run GREEN at its door), TAME + width clean. **r4 LANDED `20260813` â the arc CLOSES:** `mycelium/cord_knot_true.rye` `@embedFile`s a reproducibly-generated real `format cord-knot-v1` record (`tools/fixtures/cord_knot.bron`, via `mycelium/cord_knot_fixture_gen.rye` + a `mycelium/corpora/` symlink), parses it through r2, **verifies the recovered Knot against its own digest**, and reports supplyÂ·stars; the witness cross-checks that pair against `tools/fixtures/cord_knot_truth.awk` â **app == awk, `700 1`** (two tools, one answer). The record verifies, re-renders to the embedded bytes exactly, the recovered Knot continues to a lawful supply (fresh 300 issue â 1300), the generator is `REPRODUCIBLE`, a tampered header refuses `BadFormat`. Witness `tools/mycelium_cord_knot_true_witness.rish` GREEN (r2 + r3 re-run GREEN at its door), TAME + width clean. **The Mycelium checkpointing arc stands complete end to end â r1 fold identical Â· r2 travels Â· r3 bound win Â· r4 reads true** (the fourth closed 4-round Mycelium arc, after Cord Â· gossip convergence Â· fact batching). **Next agent-doable crux:** the supersede-the-core ruling (evolving `cord.Block` in place, a module-seam decision reserved for Keaton), or a fresh Season D journey (Kresfa/`.kres`, Puddle) / another double-seat season's crux, each a self-approved design read first. Breach renames (cairn-first) + deep debride + seed force-push stay Keaton's hand (gates #1/#5).
- **Mycelium contested-verdict arcs â the Tenure + the Till `20260813`** (Season D). **Tenure COMPLETE** (`mycelium/tenure*.rye`; design `20260813-060935_â¦`) â a contested **name** decided by the agreed order: the first reservation in the Cord's total order holds it, a later one is a **lawful no-op** loss, corruption refuses whole (r1 verdict Â· r2 travels Â· r3 across a Knot Â· r4 reads a real fixture true, app == awk). **Till OPENED â r1 LANDED** (`mycelium/till.rye` + `tools/mycelium_till_witness.rish` GREEN; self-approved design `active-designing/20260813-064129_â¦`) â the **double-spend** twin, the oldest reason a value ledger wants consensus (improving on TigerBeetle/Mysticeti). The fund model is a **global supply** (`fold.rye` folds `issued â taxed`), so the truest double-spend is a **supply-exhaustion race**: two keepers each draw 700 from a treasury of 1000 â each affordable alone, not both. `till.resolve` walks the committed order through `fold.fold_fact`; an accepted spend records a **winning Draw**, an `Overdraw` with amount **â¤ issued-so-far** (coins once existed â an earlier draw took them) records a lawful-no-op **spent-out loss**, while an `Overdraw` **> issued-so-far** (never-could-pay) Â· a broken signature Â· a `StarTaken` name-contest (the Tenure's verdict, not the Till's) each **refuse whole**. Deterministic across three arrival orders (drew 2 Â· short 1 Â· supply folds once to 200 Â· total lost 700). **r2 LANDED** (`mycelium/till_bron.rye` + `tools/mycelium_till_bron_witness.rish` GREEN) â the coin verdict travels as a `format till-v1` record (`supply` Â· `drew <pk> <amt> <pos> <round>` Â· `short <pk> <amt> <pos> <issued-at>`, keyed by signer), renderâparse recovers the identical draws/loss/supply, re-render is a fixed point; six refusals whole (bad header Â· unknown field Â· bad hex Â· broken supply law Â· missing supply Â· over-long). Composes `cord`+`fold`(+`till`) public API only; demo seeds â no key/funds/network/custody. **r3 LANDED** (`mycelium/till_knot.rye` + `tools/mycelium_till_knot_witness.rish` GREEN) â the verdict reads across a **Knot** (a `TillKnot` pairs the sealed `FoldState` with the prefix coin map; `continue_till` walks the suffix seeded by the sealed supply): a treasury of 1000 pays two prefix draws sealed at supply 200, a suffix 700-draw finds only 200 yet the sealed issued is 1000 â a **spent-out loss across the cut**; `verdicts_match` proves the full Cord and the Knot-continued resolution name the identical draws/loss/coins/supply/loser; corrupt Knot Â· tampered suffix fact Â· never-could-pay draw each refuse whole. **r4 LANDED â Till arc COMPLETE** (`mycelium/till_true.rye` + `till_fixture_gen.rye` + `tools/fixtures/till_dag.bron` + `till_dag_truth.awk` + `tools/mycelium_till_true_witness.rish` GREEN) â a real reproducible on-disk double-spend record read true: parse via `cord_bron`, rebuild under `cord.add`, resolve, report the spent-out signerÂ·drewÂ·short; **app == awk** (`a09aa5f4â¦ 2 1`, two tools one answer), tampered header refuses `BadFormat`. **The Mycelium Till arc stands complete end to end â r1 verdict Â· r2 travels Â· r3 across a Knot Â· r4 reads true** (the second closed 4-round contested-verdict arc, after the Tenure). **Purse OPENED â r1 LANDED `20260813`** (`mycelium/purse.rye` + `tools/mycelium_purse_witness.rish` GREEN; self-approved design `active-designing/20260813-070923_â¦`) â the Till's own named horizon, the **truest** double-spend: **one holder spends the same coins twice**, decided **per account** by the agreed order. A transfer rides the Cord with no new fact type â **sender** = the fact's `signer_pk`, **amount** = its amount, **recipient** = the fact's signed 256-byte `body` (bound to the signature); a genesis credit is `kind_issue` (body = account funded), a transfer `kind_tax` (body = recipient). One **additive** touch to an elder: `fold.verify_fact` (pub) exposes the signature gate `fold_fact` already runs (the shape `cord.verify_block` set) â fold re-runs GREEN. The Purse folds its own per-account law: the measure is **`received-so-far[holder]`** â overdraws current balance yet `â¤ received-so-far` = **lost a race** (lawful no-op spent-out loss); `> received-so-far` = **never could pay** (refuse whole). **The new invariant a per-account model earns â conservation:** no coin made or destroyed by a move, so `balances_sum` == `total_issued`, asserted at the resolve postcondition. Crux: genesis credits 100 to Alice; Alice signs two 100-transfers (to Bob, to Carol) same round â a real equivocation `cord.add` admits as two distinct-hash blocks; `commit` tie-breaks by hash, one moves the coins, the other faces an emptied purse. Winner 100 Â· loser's recipient 0 Â· Alice 0 (spent once), the loss lawful (100â¤received 100), conservation holds, `purses_equal` across both arrivals; `NeverPaid` (500>received) Â· `IdentityRefused` (tampered sig) Â· `StarReserve` (name reservation, honest to own resource) Â· `Malformed` (non-key recipient) each refuse whole. Additive â composes `cord`+`fold`+`kumara` public API only; demo seeds â no key/funds/network/custody. **Season D's third contested-verdict arc opens (Tenure names Â· Till supply Â· Purse accounts), the first with a conservation law. r2 LANDED `20260813`** (`mycelium/purse_bron.rye` + `tools/mycelium_purse_bron_witness.rish` GREEN) â the per-account verdict travels as a `format purse-v1` record (`issued <total>` Â· `holds <pk> <bal>` Â· `moved <from> <to> <amt> <pos> <round>` Â· `short <from> <amt> <pos> <recv-at>`), renderâparse recovers the identical balances/counts/totals, re-render is a fixed point; **conservation travels as the record's own law** (balances must sum to `issued` or `BadInt`), six malformed records refuse whole. **r3 LANDED `20260813`** (`mycelium/purse_knot.rye` + `tools/mycelium_purse_knot_witness.rish` GREEN; self-approved design `active-designing/20260813-072541_â¦`) â the per-account verdict reads **across a Knot**, mirroring `till_knot` carried from one shared drawer to many private purses. A `PurseKnot` pairs the integrity-sealed global supply (`cord_knot.seal_dag`, whose `issued` equals the prefix's `total_issued`) with the prefix's per-account map the FoldState omits; `continue_purse` verifies the Knot, seeds a Purse with the prefix balances, and walks the live suffix through the shared `purse.fold_transfer` step. The crux is truer across a cut than within one round â Alice is credited 100 and transfers it to Bob **inside the sealed prefix**, then signs a fresh 100 to Carol in the suffix; her purse emptied in the checkpointed past, so the suffix transfer comes up **short** (lawful no-op, Carol paid nothing, conservation whole). `purses_match` proves the full Cord and the Knot-continued verdict never drift (moves Â· loss Â· balances Â· loser); a corrupt Knot (`KnotCorrupt`), a tampered suffix fact (`IdentityRefused`), and a never-could-pay suffix transfer (`NeverPaid`, 500 past the 100 ever credited) each refuse whole. One **additive** touch to an elder: `purse.fold_transfer` extracted as the shared per-fact step `resolve` already ran (resolve re-runs byte-identical GREEN). One red caught in-round: the knotâverdict `issued` pairing is not digest-bound, so asserting it on untrusted input panicked the corruption test â replaced with `verify` as the runtime gate (a tampered Knot returns `KnotCorrupt`, never a panic). Witness GREEN, TAME bans clean, width exit 0. **r4 LANDED `20260813` â Purse arc COMPLETE** (`mycelium/purse_true.rye` + `mycelium/purse_fixture_gen.rye` + `tools/fixtures/purse_dag.bron` + `purse_dag_truth.awk` + `tools/mycelium_purse_true_witness.rish` GREEN; self-approved design `active-designing/20260813-073500_â¦`) â a real reproducible on-disk per-account double-spend record read true: a demo funder credits Alice 100, Alice â Bob 100 wins at round 1, Bob â Dan 40 moves uncontested at round 2, Alice â Carol 100 comes up short at round 2 (her purse emptied). The app parses via `cord_bron`, rebuilds under `cord.add` (every signature re-verified), resolves through `purse`, and reports the spent-out signer Â· moved Â· short; the witness cross-checks that triple against an awk doing **per-account balance accounting in file order** â **app == awk** (`bc7cbcb5â¦ 2 1`, two tools one answer), the losing transfer unambiguously the round-2 one and the two paying moves touching disjoint accounts so both name Alice. Conservation holds over the real bytes (balances 0/60/40/0 sum to the 100 issued); a tampered header refuses `BadFormat`. Composes `cord_bron`+`cord`+`purse` public API only (each re-runs GREEN at the door); demo seeds â no key/funds/network/custody. **The Mycelium Purse arc stands complete end to end â r1 verdict Â· r2 travels Â· r3 across a Knot Â· r4 reads true** (the third closed 4-round contested-verdict arc, after the Tenure and the Till â the first carrying a conservation law across all four rungs). **Next agent-doable crux:** a fresh Season D journey (Kresfa/`.kres` language ecosystem, or Puddle fleet orchestration of the Aurora hypervisor) or another double-seat season's crux, each a self-approved design read first. Per-account mint authority Â· a served Purse Â· breach renames (cairn-first) Â· deep debride Â· seed force-push stay Keaton's hand.

- **Mycelium two-phase transfer â the Pledge `20260813` (r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; design `active-designing/20260813-075049_â¦`). Season D's **fourth** ledger arc and its first **two-phase transfer** â coins **reserved** now (a pledge), then **honored** (a post â the recipient) or **released** (a void â the pledger), TigerBeetle's signature feature read as the Cord's agreed order. Conservation widens by one column: `balance + reserved` always sums to what genesis issued. The crux is **equivocation** â a holder signing both a post and a void for one pledge at the same round; the Cord's deterministic order settles it **once** (first resolution wins, the second a lawful no-op), identical across every arrival permutation. `mycelium/pledge*.rye`: r1 verdict Â· r2 travels (`format pledge-v1`) Â· r3 across a Knot (a pledge opened in the sealed prefix, honored or released in the live suffix) Â· r4 reads a real fixture true (app == awk, `55154f42â¦` Bob honored Â· posted 1 Â· open 1). Composes `cord`+`fold`+`kumara` public API only; demo seeds â no key/funds/network/custody.
- **Mycelium linked atomic chain â the Braid `20260813` (r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; design `active-designing/20260813-081709_â¦`, self-approved this session). Season D's **fifth** ledger arc, and the first whose honesty spans a **chain of many facts** â TigerBeetle's *other* signature (linked events, all-commit-or-all-reverse). A **Braid** (fiber family: Cord Â· Knot Â· Braid) is a chain of linked `kind_tax` facts sharing one id, each body `LÂ·idÂ·seqÂ·lastÂ·to`, links chained causally on the Cord; when the final link closes the chain `settle_braid` evaluates it as **one atom** over a bounded affordability walk â every link affords and the whole braid **commits**, or any cannot and the whole braid **reverses** (not one coin moves, not even the earlier links that afford alone). `mycelium/braid*.rye`: **r1** the crux (commit-whole-or-reverse-whole, decided once, arrival-independent, conservation on every path) Â· **r2** travels (`format braid-v1` â issued Â· holds Â· braid-state Â· links; conservation as the record's own law) Â· **r3** across a Knot (a chain **opened** before the cut, its routing tail **settled** in the live suffix â the honest seam that fold's global law reads `kind_tax` as a *burn* yet the Braid uses it as a *conserving* transfer, so a braid seals only its opening) Â· **r4** reads a real **reverting** fixture true (app == awk, `ed3234b2â¦` Dave terminal Â· committed 0 Â· rejected 1; the whole atom reversed with not one coin moved). Composes `cord`+`fold`+`kumara` (and `cord_knot`/`cord_bron`) public API only, editing none; demo seeds â no key/funds/network/custody.
- **Mycelium pending-transfer timeout â the Lapse `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; design `active-designing/20260813-085046_â¦`, self-approved this session). Season D's **sixth** ledger arc, and TigerBeetle's **third** signature primitive after the two-phase Pledge and the linked Braid: a reservation the order itself lets **expire on time**. A **Lapse** is a Pledge carrying a **deadline** measured in the agreed order's own **position** â never the wall, the one clock every honest node shares â and `fold_lapse` ticks the clock **before** it applies each fact (lapsing any open pledge whose `deadline <= pos`, reserved coins home to the pledger, state **lapsed**), so a post the order places at or past the deadline finds the pledge **already lapsed** and is a lawful no-op. The deadline rides **inside** the signed pledge body (`PÂ·idÂ·deadline(8 le)Â·to`) so it cannot move after the signature; a deadline not strictly after its opening refuses `BadDeadline`. Conservation is the Pledge's law unwidened (a lapse is a void by another cause). `mycelium/lapse*.rye`: **r1** the crux (a late honor loses to expiry, decided once, arrival-independent â a post & void as round-2 siblings both lapse identically; a never-reached deadline stays open) Â· **r2** travels (`format lapse-v1` â the deadline column added, `state_lapsed` admitted, a lapse crossing as a lapse) Â· **r3** across a Knot (the honest seam new to the Lapse: its deadline is an **absolute** order position, so a `LapseKnot` carries the **epoch base** and continues the clock at `base+i` â a pledge opened before the cut lapses when the suffix reaches its deadline, unbroken) Â· **r4** reads a real **lapsed** fixture true (app == awk, `55154f42â¦` Bob Â· lapsed 1 Â· open 1; the awk ticks the same file-order clock, parsing the deadline little-endian). Composes `cord`+`fold`+`kumara` (and `cord_knot`/`cord_bron`) public API only, editing none; demo seeds â no key/funds/network/custody. **The Mycelium season now stands: Cord Â· gossip Â· fact-batching Â· Knot Â· Tenure Â· Till Â· Purse Â· Pledge Â· Braid Â· Lapse.**
- **Mycelium account-statement voice â the Statement `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; design `active-designing/20260813-091851_â¦`, self-approved this session). The fresh Mycelium journey REMEMBER named next: the ledger's primitives each decide the order, yet a holder still meets a stream of facts, not an answer to the plainest question â **where do I stand?** A **Statement** is that answer, read as a pure, bounded projection of one account out of a resolved `lapse.Ledger`: its **balance** (spendable) Â· **reserved** (held in open pledges) Â· **received** (cumulative inflow), and a bounded list of pledge **lines** (made Â· received), each naming counterparty Â· amount Â· state (open Â· posted Â· voided Â· lapsed). **r1 the crux:** `mycelium/statement.rye` â **a statement reconciles** (an account's reported reserved equals exactly the sum of its own still-open outbound pledges' amounts, forbidden to drift by assertion) and **statements conserve** (Î£ balance + Î£ reserved across accounts == what genesis issued, unwidened from the Pledge's law). Proven across an open scene (Alice reserves 100 to Bob), a posted scene (reserve returns, Bob holds 100), a lapsed scene (Alice's coins come home, her line reads lapsed), a stranger (a true zero statement, no lines, present=false), and arrival-independence inherited from the ledger (two round-2 permutations, one identical statement). A bounded human render a keeper reads. Composes `cord` + `fold` + `lapse` + `kumara` public API only, editing none (each re-runs GREEN); demo seeds â no key/funds/network/custody. Witness `tools/mycelium_statement_witness.rish` GREEN, TAME + width clean. **r2 travels LANDED:** `mycelium/statement_bron.rye` renders a Statement to a `format statement-v1` record (`account <pk> <balance> <reserved> <received> <present>` Â· one `line <dir> <counterparty> <amount> <state>` per pledge) and parses it back byte-for-byte (fixed point), the crux being **reconciliation survives the wire** â the record carries reconciliation as its own law, a parsed present statement whose reserved â  its open-made sum refusing `BadReconcile` before it is trusted, so a holder checks r1's arithmetic with no ledger of their own. Alice's 200-across-two-open-made-lines carries whole; a stranger's true zero travels; bad header Â· unknown field Â· bad hex Â· missing account Â· unknown direction Â· unknown state Â· broken reconciliation each refuse. Witness `tools/mycelium_statement_bron_witness.rish` GREEN, TAME + width clean. **r3 across a Knot LANDED:** `mycelium/statement_knot.rye` reads one account's line over a `lapse_knot`-continued ledger and proves it **does not drift at the checkpoint** â the statement over the whole Cord equals the statement over the sealed Knot-plus-suffix (position Â· lines Â· reconciliation unbroken); the honest seam it surfaces is that **received accumulates across the epoch base**, not restarts at it (Bob, absent at the seal and credited in the suffix by a far-deadline pledge honored across the cut, reads his whole 100 inflow â the continuity the Lapse's epoch base keeps and the Statement reads); a Knot corrupted after sealing refuses whole, so no line is read from a tampered checkpoint. Witness `tools/mycelium_statement_knot_witness.rish` GREEN, TAME + width clean. **r4 reads true â the arc CLOSES:** `mycelium/statement_true.rye` carries the reader onto a real on-disk ledger record (**reusing the Lapse's own `lapse_dag.bron` fixture** â a statement is a projection of exactly the ledger the Lapse resolves), parses through `cord_bron`, rebuilds under `cord.add` (re-verifying every signature), resolves through `lapse`, projects the **genesis-funded** account's Statement, and reports `bc7cbcb5â¦ (Alice) Â· balance 100 Â· reserved 100 Â· received 200`; the independent `tools/fixtures/statement_dag_truth.awk` reads the same bytes with per-account **deadline-clock** accounting (the union of the Lapse's clock-tick and the Purse's per-account balances) and names the identical position (**app == awk**), both tools choosing the read account the same principled way â the genesis-funded one, so no constant picks whose line is read. The statement **reconciles** (reserved 100 == its single open made pledge to Carol, the lapsed pledge to Bob counting toward none) and **conserves** (Î£ balance + reserved == the 200 issued) over the real bytes; the fixture is reproducible and a tampered header refuses `BadFormat`. Witness `tools/mycelium_statement_true_witness.rish` GREEN, TAME + width clean. **The Mycelium Statement arc stands complete end to end â r1 reconciliation crux Â· r2 travels Â· r3 across a Knot Â· r4 reads true.** A **served** statement (a holder fetching their line over Comlink) reaches the Comlink-served gate.

- **Mycelium Voucher â a signed attestation of position â `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; self-approved design read `active-designing/20260813-095216_mycelium-voucher-attested-statement-exploration.md`). With the Statement arc complete, the next Lindy-first crux is the blind spot the whole ledger set names but never fills: the Statement reads a position and `statement_bron` travels it, yet a statement that travels is still only a **reading** â nothing binds those numbers to the real agreed order, so a holder could hand over any balance and the counterparty's only recourse would be to replay the whole ledger. A **Voucher** fixes it â **proof of position without replay**: a node reads an account's Statement out of a Dag and signs the position (`balance Â· reserved Â· received`) bound to an **order-head** (one SHA-256 digest of the committed block order the reading was taken against, folded over the ordered block addresses so it is stable exactly when the agreed order is). **r1 the crux:** `mycelium/voucher.rye` â `make_voucher` (from one Dag, so the numbers and the order they bind can't come from two worlds), `verify_voucher` (offline signature check against the record's own node key â trust the node, no ledger), and `check_against(dag)` (re-derive the order-head and re-read the Statement, confirm every signed field â a node cannot vouch a false balance to a verifier who holds the blocks, nor swap the order it read against, since the order-head is inside the signature). Proven across open Â· posted Â· lapsed scenes; **arrival-independence inherited** â a voucher made against one round-2 permutation checks against the other (the order-head folds to one digest, the Statement to one position); tamper refuses (flipped balance â `BadSignature`, wrong node key â `BadSignature`, an honest voucher for one ledger fails `check_against` a genuinely different â shorter â ledger). The order-head fold is the Voucher's own bounded derivation (â¤ `cord_max_blocks` ordered hashes); composes `cord` + `fold` + `lapse` + `statement` + `kumara` public API only, editing none (each re-runs GREEN); demo node/keeper seeds â no key/funds/network/custody. Witness `tools/mycelium_voucher_witness.rish` GREEN, TAME + width clean. **r2 travels LANDED `20260813`:** `mycelium/voucher_bron.rye` renders a Voucher to a `format voucher-v1` record (`node` Â· `head` Â· `account` Â· `position <balance> <reserved> <received>` Â· `sig`, each hex where fixed-width) and parses it back byte-for-byte (fixed point), the crux being **the signature survives the wire** â the record carries its attestation as its own law, a parsed voucher whose signature does not verify against its node key over the bound facts refusing `BadSignature` before it is trusted, so a proof of position cannot be forged or mangled in the crossing; a recipient who holds the blocks can still run r1's `check_against` on the recovered voucher. A tampered position (balance +1) breaks the signature; bad header Â· unknown field Â· bad hex Â· missing field each refuse. Witness `tools/mycelium_voucher_bron_witness.rish` GREEN, TAME + width clean. **r3 across a Knot LANDED `20260813`:** `mycelium/voucher_knot.rye` reads a voucher over a `lapse_knot`-continued ledger and proves **a voucher's position survives a checkpoint** â a node vouches a position against the whole Cord, and a counterparty who has already checkpointed (sealed the prefix into a Knot, discarded the base blocks, kept only the live suffix) reads the identical vouched position from the Knot-continued ledger (the added `check_position_across` verifies the signature offline then confirms the vouched triple equals the continued reading). Proven where a pledge lapses in the suffix and where a far deadline honors it (**received accumulates across the epoch base**, never restarts). The honest seam: the **order-head is the whole-history fingerprint**, base included, distinct from the prefix-only head, so a party that pruned the base checks the position, not the head (`check_against` needs the whole Dag); a flipped position and a wrong signer match no checkpoint, a corrupt Knot refuses whole. Witness `tools/mycelium_voucher_knot_witness.rish` GREEN, TAME + width clean. **r4 reads true â the arc CLOSES `20260813`:** `mycelium/voucher_true.rye` carries the attestation onto a real on-disk Cord record (**reusing the Lapse's own `lapse_dag.bron` fixture**), parses through `cord_bron`, rebuilds under `cord.add` (re-verifying every signature), makes a voucher over the **genesis-funded** account, and reports the vouched tuple `bc7cbcb5â¦ (Alice) Â· balance 100 Â· reserved 100 Â· received 200`; the independent `tools/fixtures/statement_dag_truth.awk` names the identical position (**app == awk**) â and the **order-head recomputed** from the rebuilt fixture Dag equals the head the node signed, so the signed order is the fixture's own agreed order. The voucher verifies offline and checks against the ledger it vouches; a tampered header refuses `BadFormat`. Witness `tools/mycelium_voucher_true_witness.rish` GREEN, TAME + width clean. **The Mycelium Voucher arc stands complete end to end â r1 proof-of-position crux Â· r2 travels Â· r3 across a Knot Â· r4 reads true.** A **served** voucher (a holder fetching a node's attestation over Comlink) reaches the Comlink-served gate.

- **Mycelium Chorus â a quorum of independent Vouchers â `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; self-approved design read `active-designing/20260813-102533_mycelium-chorus-quorum-attestation-exploration.md`). With the Voucher arc complete, the next Lindy-first crux is the blind spot the Voucher itself left: a Voucher makes a position provable under **one** node's signature, yet a verifier who does not hold the blocks must simply **trust** that single node read the ledger true â one node can sign a position it never honestly derived. A **Chorus** fixes it â **proof of position by quorum**: a bounded set of independent Vouchers for the **same** account position at the **same** order-head, believed only when a threshold of **distinct** nodes agree; the fault-tolerant heart the Mycelium-consensus vision named (improving on TigerBeetle/Mysticeti quorum discipline). **r1 the crux:** `mycelium/chorus.rye` â a Chorus carries one claim (order-head Â· account Â· position) and up to `chorus_max_members` (16) member Vouchers; `open` sets the claim to the true reading, `add` gathers one node's voice, and `verify_chorus(threshold)` folds four laws in order â every member verifies offline (else `BadSignature`), every member agrees on the one claim (else `Disagreement`), the signers are distinct pairwise (a repeated node â `DuplicateNode`, so one voice cannot manufacture a quorum), the distinct count meets the threshold (else `BelowThreshold`); `check_against(dag)` re-derives the head and re-reads the Statement once and confirms the claim is true. Proven across open Â· posted Â· lapsed scenes; **arrival-independence inherited** (a chorus gathered against one round-2 permutation verifies against the other). Composes `voucher` + `cord` + `fold` + `lapse` + `statement` + `kumara` public API only, editing none (each re-runs GREEN); demo node/keeper seeds â no key/funds/network/custody. Witness `tools/mycelium_chorus_witness.rish` GREEN, TAME + width clean. **r2 travels LANDED `20260813`:** `mycelium/chorus_bron.rye` renders a Chorus to a `format chorus-v1` record â the claim named once (`head` Â· `account` Â· `position` Â· `threshold`) then one `member <node-hex> <sig-hex>` line per voice â and parses it back byte-for-byte (fixed point), the crux being **the quorum law survives the wire** â a parsed Chorus is reconstructed member by member from the shared claim then re-run through `verify_chorus` at the record's own stated threshold, so a thin record (`BelowThreshold`), a padded record (`DuplicateNode`), or a forged member (`BadSignature`) refuses before it is trusted; a bad header Â· unknown field Â· missing field Â· bad hex key each refuse. Witness `tools/mycelium_chorus_bron_witness.rish` GREEN, TAME + width clean. **r3 across a Knot LANDED `20260813`:** `mycelium/chorus_knot.rye` reads a whole quorum over a `lapse_knot`-continued ledger and proves **a quorum's position survives a checkpoint** â `check_position_across` re-runs the quorum law at the threshold then confirms the one agreed triple equals the Knot-continued reading, so a mesh's word stays believable to a party who pruned the base (proven on a pledge lapsing across the cut and a far-deadline pledge honored in the suffix, received accumulating across the epoch base). The honest seam: the **order-head is the whole-history fingerprint**, base included, distinct in the claim **and in every member**, so no node can silently name a shorter order; a forged member Â· a thin threshold Â· a corrupt Knot each refuse. Witness `tools/mycelium_chorus_knot_witness.rish` GREEN, TAME + width clean. **r4 reads true â the arc CLOSES `20260813`:** `mycelium/chorus_true.rye` carries the quorum onto a real on-disk Cord record (**reusing the Lapse's own `lapse_dag.bron` fixture**), rebuilds under `cord.add`, gathers a three-node quorum over the **genesis-funded** account, and reports `bc7cbcb5â¦ Â· balance 100 Â· reserved 100 Â· received 200`; the independent `tools/fixtures/statement_dag_truth.awk` names the identical position (**app == awk**). The quorum is honest to the bytes: believed at threshold three yet refused `BelowThreshold` at four (the distinct count the threshold reads the members present), every member's recomputed order-head matching its signed one, checking against the ledger it vouches; a tampered header refuses `BadFormat`. Witness `tools/mycelium_chorus_true_witness.rish` GREEN, TAME + width clean. **The Mycelium Chorus arc stands complete end to end â r1 quorum crux Â· r2 travels Â· r3 across a Knot Â· r4 reads true. The Mycelium season now stands: Cord Â· gossip Â· fact-batching Â· Knot Â· Tenure Â· Till Â· Purse Â· Pledge Â· Braid Â· Lapse Â· Statement Â· Voucher Â· Chorus.** A **served** chorus (a holder fetching several nodes' attestations over Comlink) reaches the Comlink-served gate. **Next agent-doable crux:** a fresh Season D journey / double-seat season's crux, each a self-approved design read first. A served ledger Â· per-account mint authority Â· breach renames (cairn-first) Â· deep debride Â· seed force-push stay Keaton's hand.
- **Mycelium Muster â a quorum passes a known validator set â `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; self-approved design read `active-designing/20260813-110039_mycelium-muster-known-validator-set-exploration.md`). With the Chorus arc complete, the next Lindy-first crux is the blind spot the Chorus itself left: `verify_chorus` counts **any** distinct keys â three fresh keypairs pass `verify_chorus(3)` â and nothing named **which** nodes are legitimate validators (both Cord, admitting any signed block, and Chorus, counting any distinct signer, quietly assumed a validator set never written down). A **Muster** fixes it â **proof of quorum against a known set**: a bounded, named roll of validator keys with a **Byzantine threshold read from its size** (`nâ(nâ1)/3`, derived never stored so a tampered roll cannot lower its bar); the keystone every BFT protocol rests on (TigerBeetle's fixed replica set, Mysticeti's committee). **r1 the crux:** `mycelium/muster.rye` â `enroll` (a validator once; refuse `Duplicate`/`MusterFull`), `is_member`, `byzantine_threshold`, and `pass_muster(muster, chorus)` folding three laws â the Chorus is internally sound (`verify_chorus`), every distinct voice is enrolled (else `NotMember`, a padded quorum refused whole), the enrolled count meets the threshold (else `BelowQuorum`); the demonstration on metal â three fresh keys the Chorus alone believes (`verify_chorus(3)` passes) **refuse `pass_muster` NotMember** against a roll they were never enrolled in. Threshold arithmetic proven n=1Â·3Â·4Â·7Â·10 â t=1Â·3Â·3Â·5Â·7. Composes `chorus`+`voucher`+`kumara` public API only, editing none (each re-runs GREEN); roll bounded at `muster_max_members` (16); demo seeds â no key/funds/network/custody. Witness `tools/mycelium_muster_witness.rish` GREEN, TAME + width clean. **r2 travels LANDED `20260813`:** `mycelium/muster_bron.rye` renders a Muster to a `format muster-v1` record â one `validator <pk-hex>` line per enrolled key â and parses it back byte-for-byte (fixed point), the threshold **derived by the reader from the count, never written** (so a roll cannot smuggle a lowered bar across the wire); the crux is **the roll's verdict survives the wire** â a Chorus judged against a Muster parsed from bytes reaches the identical verdict as against the roll in memory (an enrolled quorum passes, three fresh keys refuse `NotMember`); a bad header Â· empty roll Â· unknown field Â· bad hex key Â· repeated validator each refuse. Witness `tools/mycelium_muster_bron_witness.rish` GREEN, TAME + width clean. **r3 across a Knot LANDED `20260813`:** `mycelium/muster_knot.rye` judges a quorum against a roll over a `lapse_knot`-continued ledger and proves **a roll's verdict survives a checkpoint** â `pass_muster_across` runs the membership-and-quorum law then confirms the agreed position equals the Knot-continued reading, so a party who kept only the Knot plus its suffix applies the same roll and believes the same enrolled quorum (proven on a pledge lapsing across the cut and a far-deadline pledge honored in the suffix, received accumulating across the epoch base); an outsider quorum refuses `NotMember` and a thin one `BelowQuorum` across the cut exactly as in memory, a corrupt Knot yields no continued ledger. Witness `tools/mycelium_muster_knot_witness.rish` GREEN, TAME + width clean. **r4 reads true â the arc CLOSES `20260813`:** `mycelium/muster_true.rye` carries the roll onto a real on-disk Cord record (**reusing the Lapse's own `lapse_dag.bron` fixture**), the mesh **being** the validator set â a real three-node quorum over the **genesis-funded** account passes the roll enrolling exactly that mesh, reporting `bc7cbcb5â¦ Â· balance 100 Â· reserved 100 Â· received 200`; the independent `tools/fixtures/statement_dag_truth.awk` names the identical position (**app == awk**). The roll's verdict is honest to the bytes: three validators Â· three voices Â· threshold three (the enrolled count exactly the members present), and a roll of three strangers refuses the same real quorum `NotMember`; a tampered header refuses `BadFormat`. Witness `tools/mycelium_muster_true_witness.rish` GREEN, TAME + width clean. **The Mycelium Muster arc stands complete end to end â r1 quorum-against-a-roll crux Â· r2 travels Â· r3 across a Knot Â· r4 reads true. The Mycelium season now stands: Cord Â· gossip Â· fact-batching Â· Knot Â· Tenure Â· Till Â· Purse Â· Pledge Â· Braid Â· Lapse Â· Statement Â· Voucher Â· Chorus Â· Muster.** A **served** muster (a holder fetching the live validator set over Comlink) reaches the Comlink-served gate.
- **Mycelium Warrant â a validator set rotates only when the current quorum certifies its successor â `20260813` (designÂ·r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; self-approved design read `active-designing/20260813-112420_mycelium-warrant-validator-set-rotation-exploration.md`). The blind spot the Muster left: the roll is **frozen** â nothing lets a validator set change, yet every long-lived BFT system must rotate its committee (TigerBeetle's view change, Mysticeti's committee reconfig), facing one trap â *who authorizes the new roll?* If a fresh set of keys could declare itself the validators, the Muster's guarantee evaporates. **Warrant** fills it: authority is **handed forward under signature, never seized** â a successor roll is believed only when a **Byzantine quorum of the *previous* roll** signed its content-address (SHA-256 over the canonical `format muster-v1` bytes). **r1 the crux:** `mycelium/warrant.rye` â `roll_digest`, `Warrant`, `open`/`endorse` (`DuplicateSigner`/`WarrantFull`), and `verify_warrant(warrant, prev, next)` folding five laws â the digests bind both real rolls (`DigestMismatch`), every endorser is enrolled in *prev* (`NotMember`), endorsers distinct (`DuplicateSigner`), every signature holds over the domain-separated tuple (`BadSignature`), the count meets *prev*'s Byzantine threshold (`BelowQuorum`); **the crux on metal** â the successor's own three fresh keys, signing their own coronation, refuse `NotMember`: **a set cannot crown itself**. **r2 travels:** `warrant_bron.rye` renders a handoff to `format warrant-v1` (epoch Â· prev/next digest hex Â· one `endorse <pk> <sig>` line) and parses back byte-for-byte (fixed point), the recovered certificate reaching the identical verdict offline. **r3 across a Knot:** `warrant_knot.rye` walks a **succession chain** `r0âr1âr2âr3`, each roll authorized by a quorum of the roll before it, and proves it **survives a checkpoint** â a party holding only a trusted anchor roll forward (base handoffs pruned) reaches the identical head as one who walked from genesis; a broken link refuses whole (`NotMember`/`BelowQuorum`/`DigestMismatch`/`StaleEpoch`). **r4 reads true â the arc CLOSES:** `warrant_true.rye` proves the signed facts are **true to the bytes** â the prev/next digests the certificate carries equal a system `sha256sum` of the same canonical roll bytes (two tools, one answer), the endorse count an independent `grep -c` of three; a warrant judged against a roll one validator different refuses `DigestMismatch`. Witnesses `tools/mycelium_warrant{,_bron,_knot,_true}_witness.rish` all GREEN, TAME + width clean; composes `warrant`+`muster`+`muster_bron`+`kumara` public API only, editing none; demo seeds â no key/funds/network/custody. **The Mycelium Warrant arc stands complete end to end â r1 the succession crux Â· r2 the wire Â· r3 the chain across a checkpoint Â· r4 true to the bytes. The Mycelium season now stands: Cord Â· gossip Â· fact-batching Â· Knot Â· Tenure Â· Till Â· Purse Â· Pledge Â· Braid Â· Lapse Â· Statement Â· Voucher Â· Chorus Â· Muster Â· Warrant.** A **served** warrant (a holder fetching the live succession chain over Comlink) reaches the Comlink-served gate. **Next agent-doable crux:** a fresh Season D journey / double-seat season's crux, each a self-approved design read first. A served ledger Â· per-account mint authority Â· breach renames (cairn-first) Â· deep debride Â· seed force-push stay Keaton's hand.

- **Puddle â a fleet places each world at exactly one host by a rule every node computes alike â OPENED `20260813` (r1, the berth crux)** (Season D; self-approved design read `active-designing/20260813-114651_puddle-fleet-placement-exploration.md`). With the Mycelium consensus season standing (Cordâ¦Warrant), the next Lindy-first crux on the whole road is the named-but-unbuilt Season D module **Puddle** â the fleet-orchestration layer that hosts many sovereign worlds (each a bounded Rye whole with its own Pond + Kumara identity; Aurora is its horizon substrate). The blind spot every arc so far left: they built how nodes *agree* (order Â· supply Â· quorum Â· rotation), never **which host runs which world.** Every fleet faces the trap the Warrant faced with authority â *who decides the placement?* A central scheduler is a single point of failure and of trust; the leaderless answer this tree keeps is that placement is **derived by everyone, never decided by anyone.** A world's assigned host is its **berth**. **r1 the crux:** `mycelium/puddle.rye` â a bounded `Fleet` of identity-bearing `Host`s (Ed25519 pk + capacity) and named `World`s; `berth(fleet)` derives the placement by **highest weave-score** (`SHA-256(domain Â· world-id Â· host-pk)`, the rendezvous/HRW concept studied clean-room), each world taking the highest-scoring host with room in canonical id order. **The crux on metal:** the same five worlds over three hosts, built in two *opposite* arrival orders (hosts and worlds both reversed), berth to the **byte-identical placement** â every world lands on the same host public key in both fleets, so two nodes reach one placement from the roster alone without ever speaking. Capacity is honored (each host â¤ its bound), no world orphaned (total berthed == world count); an **oversubscribed** fleet (worlds past total capacity) refuses `Oversubscribed` **whole** rather than strand one, a fleet of worlds with no hosts refuses `NoHosts`; the build refuses `DuplicateHost` Â· `DuplicateWorld` Â· `BadCapacity` Â· `BadWorldName`. The berth is **derived, never written** (no `set_berth`), and ties (never seen with SHA-256) break on the greater public key, never on insertion index â so determinism holds even in the impossible case. HRW chosen over hash-mod-count for its **minimal-disruption** property (only a changed host's worlds move) â which becomes r2's convergence. Witness `tools/mycelium_puddle_witness.rish` GREEN, TAME bans + width clean; composes `kumara` + `tally_copy` public API only, invents no storage; demo host/world seeds â no key/funds/network/custody (a real Aurora host + provisioning reach gates #2/#4; a served fleet reaches the Comlink-served gate). **r2 LANDED `20260813` â the fleet heals (convergence with minimal disruption):** `mycelium/puddle_convergence.rye` proves the property highest weave-score was *chosen* for over hash-mod-count â **a host departs and only its worlds re-berth.** Under generous capacity the berth is exactly pure HRW, so removing one host **moves only the worlds it carried** (onto their next-highest-score survivor) and **leaves every world on a surviving host byte-identical** (its top host still stands); the crux made checkable â `moved == the departed host's former load`, `moved>0 && stayed>0`, no survivor-tenant moved. The **symmetric join** is minimal too â a newcomer only *pulls* the worlds it now wins, never reshuffling the hosts already there; and the healed fleet stays arrival-order independent. So a failed node costs **one re-placement, never a migration**. Composes r1 public API only, editing r1 nothing (r1 re-runs GREEN at the door); witness `tools/mycelium_puddle_convergence_witness.rish` GREEN, TAME + width clean; demo seeds â no custody. **r3 LANDED `20260813` â the fleet travels, and berths the same offline:** `mycelium/puddle_bron.rye` renders a fleet to a `format puddle-fleet-v1` record (one `host <pk-hex> <capacity>` line per host in enrollment order, one `world <id>` line each) and parses back **byte-for-byte** (fixed point). The reader rebuilds through r1's own `add_host`/`add_world`, so the fleet's laws **travel with the bytes** (a record can never rebuild a fleet r1 would refuse). **The crux â the PLACEMENT survives the wire, not merely the bytes:** the recovered fleet berths to the **byte-identical placement** as the fleet in memory, so two nodes agree on where every world runs by exchanging the roster alone. Purpose-built bounded reader: a wrong header refuses `BadFormat`, an unknown field `UnknownField` (never dropped), a malformed line `BadLine`, a bad hex key `BadHex`, a bad capacity `BadInt`, and a duplicate host/world surfaces r1's own `DuplicateHost`/`DuplicateWorld` on rebuild. Composes `puddle` + `kumara` + `parse_int` + `tally_copy` public API only (r1 re-runs GREEN at the door); witness `tools/mycelium_puddle_bron_witness.rish` GREEN, TAME + width clean; demo seeds â no custody. **r4 LANDED `20260813` â reads true, the arc CLOSES:** `mycelium/puddle_true.rye` carries the reader onto a **real** on-disk `format puddle-fleet-v1` record (`tools/fixtures/puddle_fleet.bron`, produced reproducibly by `mycelium/puddle_fleet_fixture_gen.rye`, `@embedFile`-bound through the `mycelium/corpora/` symlink). **The crux â two tools, one answer:** the app parses the record through r3's reader and reports the roster shape **hosts Â· worlds Â· total capacity** (`4 6 16`); the independent `tools/fixtures/puddle_fleet_truth.awk` reads the identical triple from the same bytes (`app.out == awk.out` enforced). Genuineness beyond the triple: the fleet **berths over the real bytes** (every world on a real host within capacity, none orphaned), the record **re-renders to the embedded bytes exactly** (a fixed point â the fixture is what the Puddle emits, not a hand-mock), the generator re-emits byte-identical bytes (`REPRODUCIBLE`), and a tampered header refuses `BadFormat`. Composes `puddle` + `puddle_bron` public API only (r3 re-runs GREEN at the door); witness `tools/mycelium_puddle_true_witness.rish` GREEN, TAME + width clean; demo seeds â no custody. **The Puddle agent-doable arc stands complete end to end â r1 the berth crux Â· r2 the fleet heals Â· r3 the roster travels Â· r4 reads true.** Season D now carries a fleet-orchestration layer beside its consensus season (Cordâ¦Warrant) and its dev-net (Constel). A served fleet (a node fetching the live roster over Comlink) reaches the Comlink-served gate; a real Aurora host + provisioning reach gates #2/#4. **Next agent-doable crux:** a fresh Season D / double-seat season's journey (Kresfa/`.kres` language is a breach rename gated on Keaton's hand; Puddle could grow a served/Aurora rung at the serve gate) â each a self-approved design read first.
- **Freight â a world carries its whole signed ledger to its new berth â OPENED `20260813` (r1, the freight crux)** (Season D; self-approved design read `active-designing/20260813-121424_puddle-freight-world-state-migration-exploration.md`). With Puddle's fleet-placement arc standing, the next Lindy-first crux is the seam Puddle left: convergence (r2) proved *placement* moves minimally, yet a placement is only a pointer â a fleet that reshuffles pointers while dropping or altering the ledgers they name is a bank that reassigns your account and forgets your balance. **Freight** is what a world carries to its berth â its whole signed Mycelium ledger (a Cord Dag) â and the durability guarantee a fleet is worthless without: when a host departs and a world re-berths, it arrives reading the **byte-identical account position** it held before. Placement moves; state does not. **r1 the crux:** `mycelium/freight.rye` â a world's `Position` (balance Â· reserved Â· received Â· order-head digest) is read by `read_position(dag, account)` through `lapse.resolve` + `statement.statement_for` + `voucher.order_head_of`, a **pure function of the ledger bytes** that takes no host as input; so the same read on the sending and receiving host must agree. **The crux on metal:** three worlds each carry a distinct owner-signed ledger (distinct pledge ids â distinct order-heads); a host departs, the heaviest host's worlds re-berth (Puddle's own heal), and each **moved** world genuinely changes host public key **yet reads the byte-identical position** (order-head + triple, `Position.same`), while a **stayed** world keeps both berth and position. **The teeth â `receive` rebuilds a moving world's ledger and refuses `FreightTampered`** unless the recomputed order-head equals the freight's advertised head, so a carrier that flips one fact byte is caught (proven: a tampered pledge block refuses, the honest freight accepts and reads true) â a host cannot serve a silently-rewritten position. **Distinctness:** every world's order-head is its own, so a migration can never cross-wire two worlds' state. The position is **derived, never re-declared** (no `set_position`). Composes `puddle` + `cord` + `lapse` + `statement` + `fold` + `voucher` + `kumara` public API only, editing none (Puddle berth Â· convergence Â· Statement all re-run GREEN at the door); demo host/world/keeper seeds â no key/funds/network, no world provisioned or moved. Witness `tools/mycelium_freight_witness.rish` GREEN, TAME bans + width clean. **r2 LANDED `20260813` â the whole fleet's state is conserved across a healing:** `mycelium/freight_conservation.rye` raises the claim from one world to the whole fleet â a `FleetSum` (balance Â· reserved Â· received Â· worlds) read before a host departure equals the sum read after, where every world's after-position is obtained by **receiving its carried freight on its new berth** (r1's receive path), so conservation is proven *through* the migration; six worlds over three hosts (varied credit/pledge so balance and reserved are both non-trivial), the busiest host departs, and `sum_before.same(sum_after)` â **a host failure costs zero state**. Every order-head stays pairwise distinct (no cross-wiring), and the law has teeth â a fleet that dropped the moved worlds reads a strictly smaller sum, short by exactly their state. Witness `tools/mycelium_freight_conservation_witness.rish` GREEN. **r3 LANDED `20260813` â the freight travels as text:** `mycelium/freight_bron.rye` lets a world's freight cross as a `format freight-v1` manifest (world id Â· berth host key Â· ledger order-head Â· position triple + present); a manifest parsed from bytes reads the **byte-identical position** offline (fixed point), and the teeth tie the light record to r1's heavy proof â the recovered manifest's advertised head **accepts the world's real ledger blocks** through `freight.receive`, while a head altered in transit makes those same blocks refuse `FreightTampered`. Every one of seven fields is required (missing â `MissingField`); bad header Â· unknown field Â· malformed line Â· bad hex head Â· bad amount Â· bad present flag each refuse. Witness `tools/mycelium_freight_bron_witness.rish` GREEN. **r4 LANDED `20260813` â reads true, the arc CLOSES:** `mycelium/freight_true.rye` carries the reader onto a **real** on-disk `format freight-v1` record (`tools/fixtures/freight_manifest.bron`, produced reproducibly by `mycelium/freight_fixture_gen.rye`, `@embedFile`-bound through the `mycelium/corpora/` symlink). Two crux checks: **two tools, one answer** â the app's position triple (balance Â· reserved Â· received) equals `freight_manifest_truth.awk`'s reading (`60 40 100`, all three nonzero); and **the record is the freight of a real ledger** â the app rebuilds the world's ledger from the fixture's own seeds, confirms the parsed position and order-head match byte for byte, and the advertised head accepts the real blocks through `receive`. The record re-renders to the embedded bytes (a fixed point), the generator is `REPRODUCIBLE`, and a tampered header refuses `BadFormat`. Witness `tools/mycelium_freight_true_witness.rish` GREEN, TAME bans + width clean. **The Freight journey stands complete end to end â r1 the migration crux Â· r2 conservation Â· r3 travels Â· r4 reads true.** Season D now carries a world's *state* across a re-berth, closing the durability seam Puddle left. A served migration (a node fetching a moving world's freight over Comlink) reaches the Comlink-served gate; a real Aurora host + provisioning reach gates #2/#4. **Next agent-doable crux:** a fresh Season D / double-seat season's journey, each a self-approved design read first (breach renames are cairn-first; deep debride + seed force-push stay Keaton's hand).
- **Portage â value carried across two worlds, atomic on both ledgers or neither â OPENED `20260813` (r1, the cross-world crux)** (Season D; self-approved design read `active-designing/20260813-124159_mycelium-portage-cross-world-transfer-exploration.md`). Freight moved a world's whole state across a re-berth, yet every Mycelium coin so far still lived and died inside ONE Cord. The highest-value still-tractable crux on the whole road â the one the double-seat vision names as beating **TigerBeetle/Mysticeti** â is value crossing **between two worlds** (shards) with no shared total order, atomic on both ledgers or neither: cross-shard settlement. **Portage** is that crossing (the old word for goods carried between two waters that share no channel). It binds its two halves by a content **digest** â SHA-256 over `idÂ·amountÂ·fromÂ·to_worldÂ·to_account` â so a receiving world credits only a digest it can verify against the sending world's burn; the decision is a **pure function of both ledgers' bytes**, no correspondent trusted (the same insight Freight used for migration, now for transfer). **r1 the crux:** `mycelium/portage.rye` â its OWN two-world fold law `resolve_world(dag, verify)` (mirroring how Pledge wrote its own over the shared signed facts, since lapse/statement know only P/H/V): a signed portage-**out** (`OÂ·idÂ·worldÂ·to`) debits the signer on World A and records the world's burn digest; a signed portage-**in** (`IÂ·idÂ·digestÂ·to`) credits the recipient on World B **only when its embedded digest equals the verified burn's**. **The crux on metal:** Alice on A falls by exactly the amount, Bob on B rises by exactly the amount, **the pair conserves** (`left == reached == 40` â a credit for every debit, double-entry widened across two shards), and the two worlds' order-heads differ (a real crossing, not a no-op). **The teeth:** an in whose digest matches no verified burn refuses **`NoMatchingBurn`** (World B can never conjure value across the gap), a second in for one id refuses **`AlreadyLanded`** (the crossing lands once), a burn beyond the signer's balance refuses **`NeverPaid`** (a debit cannot be conjured either). The credit is **derived from a verified burn, never re-declared** (no `set_balance`). Composes `cord` + `fold` + `voucher` + `kumara` public API only, editing none (the Voucher order-head and the Pledge two-phase transfer both re-run GREEN at the door); demo world/keeper seeds â no key/funds/network, no value crossed. Witness `tools/mycelium_portage_witness.rish` GREEN, TAME bans + width clean. **r2 LANDED `20260813` â a crossing lands on both worlds or neither (atomic all-or-nothing):** `mycelium/portage_atomic.rye` makes the out **two-phase**, the Pledge's atomicity widened from two phases to two shards â Alice **stages** (balanceâreserved, coins held in transit), then **commits** once the in lands on B (reserve leaves circulation, Bob's now) or **reverts** if it never lands (reserve returns to Alice, whole). **The two-outcome law:** a committed source pairs with a landed in (Alice's spendable falls by the amount **and** Bob rises by it â both moved, `left == reached == 40`); a reverted source pairs with no in (Alice whole **and** Bob unpaid â neither moved, the two-world sum as if the crossing never began); never a loss (Alice short, Bob unpaid), never a double (Alice whole, Bob paid). **Equivocation decided once:** Alice signs both commit and revert at one round and both arrival permutations name the identical verdict (`sources_equal`), exactly one settled; a staged-but-undecided crossing holds its coins reserved in transit, never lost. Witness `tools/mycelium_portage_atomic_witness.rish` GREEN (Portage r1 Â· Pledge re-run at the door), TAME bans + width clean. **r3 LANDED `20260813` â the portage travels as text:** `mycelium/portage_bron.rye` renders a crossing to a `format portage-v1` record (id Â· from Â· to_world Â· to_account Â· amount Â· digest Â· out-head Â· in-head) and parses it back byte-for-byte (a fixed point); a purpose-built bounded reader accepts a crossing by its record alone â the digest self-certifies its bound fields, the out-head ties the record to World A's real ledger with a matching burn, the in-head to World B's real ledger crediting the recipient (`40 left A == 40 reached B`); a tampered field or head, and every malformed record, refuse. Composes portage + cord + voucher + kumara public API only. Witness `tools/mycelium_portage_bron_witness.rish` GREEN (r1 crux + r2 atomic re-run at the door), TAME bans + width clean. **r4 LANDED `20260813` â reads true (Portage arc closes):** `mycelium/portage_true.rye` reads a real on-disk fixture, its reported value and digest cross-checked against an independent `awk` of the same bytes (`app==awk`, `40 d2bafdd4` â two tools, one answer); the record is provably the receipt of a real crossing whose two heads accept both rebuilt ledgers, its fixture reproducible, its record a fixed point; a tampered header refuses. Witness `tools/mycelium_portage_true_witness.rish` GREEN. **The Portage agent-doable arc â r1 cross-world Â· r2 atomic Â· r3 travels Â· r4 reads true â stands complete end to end.** A **served** portage (World B fetching World A's out-fact, or A learning of B's in, over Comlink) reaches the Comlink-served gate; a real Aurora host for either world reaches gates #2/#4.

- **Constel â a named, reproducible dev-net constellation reaches quorum â OPENED `20260813` (r1, the named-quorum crux)** (Season D; self-approved design read `active-designing/20260813-132009_mycelium-constel-named-dev-net-exploration.md`). Every consensus witness before this conjured its nodes inline (`seeded(0x11)`â¦) â anonymous keypairs made up for one proof and forgotten; no round gave the dev-net a **name and a roster**. **Constel** is the named test constellation â the sovereign-lane echo of elder Urbit's fake-galaxy dev networks, run from inside the jailed pier. Its crux: a ship's identity is a **pure function of its name** (`seed = SHA-256(name)`), so the whole constellation â every key, the Muster it seats, the quorum it reaches â is determined by the list of names alone. **r1 the crux:** `mycelium/constel.rye` â `boot(names)` seats each named ship with `keypair_from_seed(SHA-256(name))`, `into_muster` enrolls every ship, and `reach_quorum` opens a Chorus, gathers every ship's voucher, and passes the constellation's own Muster. **On metal:** a five-ship roster boots to **byte-identical keys** twice (reproducible, distinct ships), seats as a Muster (Byzantine threshold **4**), and reaches quorum on the account's **true order-head** (`== voucher.order_head_of`, not a claim). **The teeth:** a stranger ship (a name not seated) refuses **`NotMember`** (an outsider never counts), a quorum short of the threshold refuses **`BelowQuorum`** (a thin quorum is not believed). Identity is derived from the name, never declared (no `set_key`). **Naming law seated this round** â the first Constel roster (`xnvethqua` Â· `kwelmirran` Â· `xzobrilth` Â· `murrqaveln` Â· `xwendakosh`), single tokens under the always-on placeholder-ship-names rule (no `~`, length â  3/6/12, never a valid @p), siloed dev fixtures. Composes `kumara`+`cord`+`fold`+`voucher`+`chorus`+`muster` public API only, editing none (Chorus + Muster re-run GREEN at the door); demo seeds â no key/funds/network, no real value. Witness `tools/mycelium_constel_witness.rish` GREEN, TAME bans + width clean. **r2 LANDED `20260813` â a constellation tolerates f down, the (f+1)th honestly fails:** `mycelium/constel_depart.rye` â the Muster stays the full roster (every ship enrolled whether up or not), only a subset is up and vouches; a five-ship constellation tolerates **1 down** (4 up still meets threshold 4) and **honestly fails `BelowQuorum` at 2 down**, keeping exactly the promise its arithmetic makes (`n â threshold == f`); a permanently smaller roster re-derives its own lower threshold and still agrees. Composes `constel`+`chorus`+`muster`+`voucher` public API only (r1 re-runs GREEN at the door; r1's `demo_genesis`/`seeded` made `pub`, additive). Witness `tools/mycelium_constel_depart_witness.rish` GREEN, TAME bans + width clean. **r3 LANDED `20260813` â a named constellation travels, self-certifying:** `mycelium/constel_bron.rye` renders the roster to a `format constel-v1` record (one `ship <name> <pk-hex>` line each) and parses it back byte-for-byte (a fixed point); because a ship's key is a pure function of its name, the record is **self-certifying** â `parse` re-derives each key from its name and refuses **`PkMismatch`** unless the carried key matches (a tampered key cannot ride a real name). **The crux:** a keeper boots the identical constellation from the record alone â the recovered roster reaches the **byte-identical quorum head** as the one in memory (the Dag's true order-head). A bad header Â· unknown field each refuse. Composes `constel`+`chorus`+`muster`+`voucher` public API only (r1 re-runs GREEN at the door). Witness `tools/mycelium_constel_bron_witness.rish` GREEN, TAME bans + width clean. **r4 LANDED `20260813` â reads true (Constel arc closes):** `mycelium/constel_true.rye` reads a real on-disk `format constel-v1` fixture (`tools/fixtures/constel_roster.bron`, produced reproducibly by `constel_fixture_gen.rye`, `@embedFile`-bound through `mycelium/corpora/`). **Two tools, one answer:** the app derives the roster shape (`<count> <firstkey8>`) **independently of the record text** â by booting the constellation from its ship names (each key a pure function of its name) â and reports `5 97f92882`, cross-checked against `constel_roster_truth.awk`'s reading of the same bytes. **The record is the receipt of the real constellation** â every carried key self-certifies against its name, the parsed roster matches the name-booted one name-for-name and key-for-key, and it reaches quorum on a demo Dag's true order-head; the record re-renders to the embedded bytes (a fixed point), the generator re-emits byte-identical (`REPRODUCIBLE`), a tampered header refuses `BadFormat`. Witness `tools/mycelium_constel_true_witness.rish` GREEN. **The Constel agent-doable arc â r1 named-quorum crux Â· r2 fault tolerance Â· r3 travels self-certifying Â· r4 reads true â stands complete end to end.** A **served** constellation (ships attesting over Comlink) reaches the Comlink-served gate; a real Aurora host for any ship reaches gates #2/#4.. A **served** constellation (ships attesting over Comlink) reaches the Comlink-served gate; a real Aurora host for any ship reaches gates #2/#4.
- **Testament â a named constellation's verdict travels as an offline certificate â `20260813` (r1Â·r2Â·r3Â·r4 COMPLETE)** (Season D; self-approved design read `active-designing/20260813-135126_mycelium-testament-named-quorum-certificate-exploration.md`). The Constel quorum lived and died **in one process** â nothing carried the constellation's belief out. `chorus_bron` already lets a bare quorum travel and re-verify at a **stated** threshold, yet it carries no roster, so it cannot tell an enrolled ship from a stranger with a valid signature. A **Testament** closes exactly that gap: a portable certificate that *this named constellation, by Byzantine quorum, certified this reading*, verifiable **offline** holding only the certificate and the roster **names**. **r1 the crux:** `mycelium/testament.rye` seals a reading into a certificate carrying the roster names + the sealed Chorus; made **with** the ledger, verified **without** it â `verify` boots the exact roster from the names alone (each key `SHA-256(name)`), seats the Muster, **derives** the Byzantine threshold from the roster's size, and passes the seal with no Dag. **The teeth (the named binding a bare Chorus cannot enforce):** a stranger ship's valid, agreeing voucher refuses **`NotMember`**, a thin quorum **`BelowQuorum`**, a one-byte name tamper re-boots a different key â **`NotMember`**. **r2 LANDED â fault tolerance inside the certificate:** `mycelium/testament_fault.rye` â a Testament sealed with `f` ships down still names the **whole** roster, so `verify` boots the full constellation, derives the full threshold, and believes the survivors from the names alone (five ships, threshold 4, f 1: one down still meets four); one more down refuses **`BelowQuorum`** at the honest edge; a permanently smaller roster re-derives its own lower threshold and is believed. **r3 LANDED â the certificate travels, self-proving:** `mycelium/testament_bron.rye` renders to a `format testament-v1` record (one `ship <name>` line each â the **name only**, key derived; the shared claim once; one `member <node> <sig>` line per voice) and parses back byte-for-byte (a fixed point). Unlike a `chorus-v1` record it states **no threshold** â `parse` boots the roster, derives the threshold, and re-runs `verify`, so the named binding travels: a stranger's voice refuses `NotMember`, a thin quorum `BelowQuorum`, a tampered ship name `NotMember`, now across the wire. **r4 LANDED â reads true, the arc CLOSES:** `mycelium/testament_true.rye` reads a **real** on-disk fixture (`tools/fixtures/testament_cert.bron`, produced reproducibly by `mycelium/testament_fixture_gen.rye`, `@embedFile`-bound through `mycelium/corpora/`). **Two tools, one answer:** the app derives the shape (`<ship_count> <head8>`) **independently of the record text** â ship count by booting the roster from names, head by rebuilding the demo Dag â reporting `5 c2d99dd2`, cross-checked against `testament_cert_truth.awk`; the certificate self-proves on parse, the parsed roster matches the booted one name-for-name, the sealed head is the demo Dag's true order-head, the record is a fixed point, the fixture `REPRODUCIBLE`, a tampered header refuses `BadFormat`. Witnesses `tools/mycelium_testament{,_fault,_bron,_true}_witness.rish` all GREEN, TAME + width clean; composes `testament`+`constel`+`chorus`+`muster`+`voucher` public API only, editing none; demo seeds â no key/funds/network/custody. **The Testament arc stands complete end to end â r1 named-quorum crux Â· r2 fault tolerance in the certificate Â· r3 travels self-proving Â· r4 reads true.** A named constellation's verdict is now a portable, self-proving certificate a keeper reads by hand â the Lindy artifact the Mycelium consensus season built toward. A **served** testament (ships attesting over Comlink rather than in one process) reaches the Comlink-served gate; a real Aurora host for any ship reaches gates #2/#4. **Next agent-doable crux:** a fresh Season D / double-seat season's journey, each a self-approved design read first (breach renames stay cairn-first; deep debride + seed force-push stay Keaton's hand).

## The Compass Season â OPEN `20260809.021829` (Keaton's word)

**256 rounds Â· four equinoxes = four compass directions.** Coords **Compass Season Â· SOON Â· j1 Â· q1 Â· r1**. Nesting: **Round Â· Quest 4 Â· Journey 16 Â· Equinox 64 Â· Season 256**.

| Equinox | Waymark | Holds |
|---|---|---|
| The Language | **SOON** â | **Rishi language spine complete** â runes `^- ?! ?& ?| ?:` Â· regex `matches`/`find` (`{n,m}` Â· `\b` Â· lookbehind) Â· `sort`/`unique`/`upper` Â· list `+` Â· `fold` Â· user functions `fn`. Seams folded native: `claim_preserve` Â· `census_control`. **Brix-infuse LANDED** `20260811` (`brix/infuse.rye`) Â· **PLEAC opened** `20260811` â strings (join/split) Â· lists (chunk/window/flatten) Â· numbers (clamp/parse/to_str), each witnessed. **Cookbook wired into the Rishi interpreter** `20260811` â `clamp` Â· `chunk` Â· `window` Â· `flatten` Â· `parse` Â· `str` builtins (suite 23/23; whole cookbook in the interpreter). **TAME core/shelf LANDED** `20260811` (`context/TAME_CORE.md` â compressed core matched into the claude+cursor tame rules; shelf = `TAME_GUIDANCE.md`). Open: Aurora. **TAME Audit Quest pass 1** `20260811`: **full surface GREEN** (width-check Â· rune_assert_sweep Â· tame_style_check). **REDS #64 CLOSED** â the stray `comlink_r1` .py ported to Rishi (`tools/comlink_r1_dual_bind_probe.rish`, Keaton's word: port over shred), `.py` removed, TAME bans clean. `one_clock` mono-erratum **FIXED** `20260811` (archive-tolerant integrity check + one dangling entry retired â MONO_OK). Its zone leg reds only on this sandbox (host zone UTC, no config); the real Framework/pier host (America/New_York) passes it â environmental, not a tree drift (`moldâshape` CLOSED â residue left). **Aurora OPENED** `20260811` â **all eight boot stages** (seed Â· relay Â· wire Â· sealed Â· posted Â· deciding Â· named Â· kumara) cross-build freestanding `riscv64` to RISC-V ELFs (`e_machine 243`) with **no emulator** (`tools/aurora_seed_freestanding_witness.rish` GREEN via `rye build`, rm-first so no stale artifact); the whole floor reaches metal in-sandbox. **REDS #68 CLOSED** â the first cut of that witness used raw `zig build-exe` (which can't read `.rye`), no-op'd behind `|| true`, and read a stale ELF (false green); the next lap's fresh build exposed it, fixed same-round with a witness on metal. **Emulator choice consolidated** (`active-designing/20260811-202757_the-emulator-choice-qemu-and-the-modern-rust-vms.md`): **QEMU stays on merit** â its pure-emulation `virt` + modern virtio-net + `-bios none` + SMP union is unmatched by the Rust VMs (Firecracker/Cloud Hypervisor are KVM kernel-booters; rvemu-class lack the virtio device model); rust-vmm virtio + a pure-Rust RISC-V core named as the future clean-room bridge. Waking the seed stays qemu-gated |
| Identity & Network | **JARL** | **Kumara** (5 tilaks) Â· **Comlink** turn-route + live handshake (loop closed) Â· **d12Â·d60 topology** Â· **settlement constellation** landed (`settlement/constellation.rye`: five transitions â genesis Â· mint Â· transfer Â· rotate Â· **escape** â a galaxy's d60 as owned records, every refusal held) Â· scarcity reconcile held Â· **generalized topologies + Pond** horizon |
| Surface & Intelligence | **BUHR** | Realidream DAG surface Â· Quin's four voices (Lattice Â· Scribble Â· Lantern Â· Ember) Â· MCP-in-Bron Â· Tablecloth |
| The World | **TACT** | Ship-Pilot pitch Â· publishing Â· Grainphone Â· commerce Â· CONTRIBUTING four-doors Â· Grain Energy PBC |

**Now at JARL â identity, network, address space, and the settled ledger.** Coords **Compass Season Â· JARL Â· j4**. Four seats stand, each witnessed GREEN: **(1) Identity** â `kumara/tilak.rye` seats five signed tilaks (point Â· bind Â· turn Â· capÃ3 Â· sponsor), every tamper refusing. **(2) Network** â `comlink/handshake_turn.rye` reads a peer's rotation end to end: a signed introduction admits the peer, `turn_route.rye` takes the freshest verified turn (a reset outranks any key rotation), and `handshake_wire` recovers that turn from the 112 descriptor bytes it rode in â the loop closed from wire to route key. **(3) Topology** â `comlink/topology.rye` seats the **d12Â·d60 fractal address space**: twelve galaxies, five stars each, twelve planets each, a galaxy leading a d60 of sixty; a number decodes to a place, a place's sponsor climbs planetâstarâgalaxy, two places share a route by number. **(4) Settlement** â `settlement/constellation.rye` is the ledger, chosen on **Sui's** ground: a galaxy and its d60 settle as owned records whose versions climb on every change. A galaxy opens the constellation from its own bind; a **sow** capability spawns a star beneath it and a planet beneath that, each under its rightful topology sponsor; a **hand** capability transfers ownership; a **tend** capability rotates the networking key while the keeper's key sits cold; a reset outranks a key bump. And **escape** completes the five: a child re-parents to another settled sponsor one tier up, by its own word (the sponsor tilak) and the new sponsor's adoption â the old sponsor keeps no veto, and the child's number never moves, only whom it answers to. Every refusal holds â orphan parent, wrong sponsor, forged cap, forged spawn signature, a re-mint, a stale rotation, a wrong-key turn, a galaxy that cannot escape, a forged request, a forged adoption. All five JARL witnesses run GREEN together. **Scarcity `20260810`:** the **d12Â·d60 fractal** â Azimuth ranks retired; `kumara/tilak.rye` reads its tier from `comlink/topology.rye`. **Shared-surface shrink `20260810`:** an owned **Deed** (136 B, keys on the fast path) + a shared **Commitment** (56 B â pointÂ·tierÂ·sponsorÂ·versionÂ·digest); `verify` proves a current deed, refuses stale/tampered/ghost. **Human-name custody `20260810`** (`settlement/names.rye`): a keeper claims + releases a globally-unique spoken name, resolvable both ways; the shared registry is the one surface that needs consensus. **JARL's settlement doors all stand** â the next-season breach (Pond Â· skies Â· Kyri) is unblocked, opening on Keaton's word. **Horizon** (Keaton's word, `20260809`): generalizing the constellation to **loadable topologies** â a *sky* (proposed) a community loads like a game: odd-quorum fractal (3Â·9Â·15Â·27) Â· 5 elements Â· modality roles (initiator/sustainer/adapter) Â· the three role-dimensions (contend/solve/calling). **Pond** becomes the full application module; **Pool retires into it**. **Decided:** breach opens **after JARL**; molt sweep **tight** (Pool paper + kin). Study: `external-research/20260809-232015â¦` + `â¦-233940_divisional-rolesâ¦`. Plan: `active-designing/20260809-234413_loadable-topologies-and-pond-silo-brief.md`. Molt-prep: `SHRED_PREP.md` pending note.

**Next-season breach â OPEN `20260810`** (charter: `expanding-prompts/20260810-000032_the-next-season-breach-charter.md`). **Move 1 (Pond) begun:** Pond seated as Grain's **application module**, **Pool retired into it** by molt (living `foundations/20260810-011514_pond-the-application-module.md`; Pool study bannered as a fossil). Remaining: (2) **Bron â Kyri** by **molt** (2,163 `.bron` kept whole, new logs `.kyri`, 378 refs forward â never a rewrite); (3) **Kyri the one voice** â *voice* becomes the whole concept, retiring "variant" (Quin + ReyaÂ·RiyoÂ·TreyÂ·TrizÂ·Trya rest named in archive); (4) `kyri.sol`/`kyri.sui` reference â **Keaton's hand alone**; and the loadable **skies** that finish move 1. **Decided:** Kyri names **both** voice and notation (logs are the voice's journal); **Kyri** on the favorites list. Baton: [`../expanding-prompts/20260809-021406_the-compass-season-baton-four-equinoxes.md`](../expanding-prompts/20260809-021406_the-compass-season-baton-four-equinoxes.md).

---

## Waymarks

Seated ladders: **HAWM Â· TUBE Â· ZETA Â· JABS Â· LULU Â· STOA Â· SETU Â· SUNN Â· POLE** (elder) Â· **SOON Â· JARL Â· BUHR Â· TACT** (Compass Season). Draw before you number: `.claude/rules/waymark-ladders.md` Â· `tools/waymark_derive.rish`. Claims: `waymarks/`.

---

## Pier & hands

- **Pier path** â `~/grain` (persists across jail resets) Â· agent `home-xy-grain` Â· Cloud on `xykj61/grain`.
- **Lane** â every **send** pushes both `origin` (autoproject96) and `xykj61`. ls-remote guard first; `origin` may 403 from the cloud (home pier closes the gap). Map: [`../PUBKEYS.md`](../PUBKEYS.md) Â· [`../context/REMOTE_ROSTER.md`](../context/REMOTE_ROSTER.md).
- **Jail authors; host installs** â agents write inside the project / enclosure; USB `adb` installs and key ops stay Keaton's hand.
- **Live state** (`20260808`) â guides 0â2 CLOSED Â· `gh` as `xykj61` Â· Claude Code on pier Â· **agent-jail GREEN** (`./tools/agent-jail.sh` for claude + cursor-agent) Â· editors + Aug cursor-cli witnessed Â· tmux `pier` standing.
- **Cursor launch** â `rishi/bin/rishi run tools/launch-cursor.rish --cursor ./Cursor-*.AppImage --gpu`.
- **Outer terminal / phone** â USB/`adb` and phone look stay on the operator desk; prefer git nib + `prin scope` for season state.

---

## Two grains

The private field is `~/grain`; the public template **grain-os/grain** is *projected* by `tools/sow.rish` along the boundary in `template-manifest.bron`, proven clean by `tools/sow_witness.rish` (no name or key crosses into the seed). **Seed COMPLETE + privacy-fixed `20260810`** â every new module classified. **Leak found + closed:** the scrub/scanner only knew the maintainer name + keys, so the family surname (**Dunsford**), prior name, and personal handles were leaking into the public seed; expanded the scrub + IDENT net to every family/handle/contact form, made the withhold case-insensitive (privacy over completeness â leaking files are withheld whole). `IDENT_CLEAN` confirmed; the vision batons + rosters + raw contacts held out. Raw PII **not persisted** in-tree â held for the **Vault** (custody-first, Keaton's hand). Publish push is Keaton's hand.

## Shred-prep

[`SHRED_PREP.md`](SHRED_PREP.md) â Class H fossils Â· Class O rooms (propose-never-seat) Â· **PythonâRishi molt seated** (`20260809`, prep only) Â· shred stays **RED** until circled. **debride** is the stronger word (removes dead history, deep on Keaton's word).

---

## Custody gates â an autonomous agent STOPS here and surfaces (never crosses)

For any self-paced or outer-jail loop: recur through all agent-doable work, yet **stop and surface â never cross â these custody/irreversible/provisioning acts.** They are Keaton's hand by design (custody-first):

1. ~~**The AHOY3 final seed force-push**~~ â **DONE `20260812`** on Keaton's word (*push the seed*): the clean projection published to `grain-os/grain` @ `6d184f7` (fast-forward, author *Grain OS*, unsigned by design). Caught + fixed a scrub name-doubling first (**REDS #70**) â LICENSE and family names had garbled to "the maintainer the maintainer"; a pre-push content scan caught what IDENT_CLEAN could not. **The AHOY front-door season is CLOSED.** **Seed style seated `20260812`** (Keaton): the seed is kept a **single force-push commit** â the weave (**Mantra**) is Grain's real version control, git one snapshot; each refresh replaces the one commit (now `26cae5b`, author *Grain OS*). Future seed refreshes stay Keaton's word.
- **BUHR OPENED (explored) `20260812`** (`active-designing/20260812-053742_buhr-explorationâ¦`): Compass Equinox 3 â Surface & Intelligence. **Crux = Quin's four voices** (LatticeÂ·ScribbleÂ·LanternÂ·Ember all witnessed GREEN; Quin host named-only, composes through Pond). Realidream = far-horizon composition on real substrate (no module yet, rightly). MCP-in-Bron = greenfield. **First door LANDED `20260812`** â **BUHRâ1r1**: `pond/apps/inference_query.rye` composes Lattice+Lantern as a bounded Qâvane host â a Lantern request is validated + modelâallowâlisted, an arithmetic op routes to **real** Lattice math (`[[1,2],[3,4]]Ã[[5,6],[7,8]]=[[19,22],[43,50]]`), and a Lanternâbounded response returns (token/stop metering); an unknown op is refused (never faked). Both cores byteâidentical (composed over public APIs only); witness `tools/buhr_inference_query_witness.rish` GREEN, TAME clean. **BUHRâ1r2 LANDED `20260812`** â `pond/apps/reading_query.rye` carries the same Qâvane host to the **reading voice**: a Lantern request routes through **Scribble** for a real reading of a pinned markdown document (outline â `Grain Voices Detail` Â· depthâtwo fold â `Voices` Â· fence kinds â `rye 1 rish 1 plain 1`), returned bounded by the same allowâlist + token/stop metering; unknown op refused, zero budget refused by the Lantern contract, length budget truncates with a length stop. Additive â rung one stays GREEN, TAME bans clean; witness `tools/buhr_reading_query_witness.rish` GREEN. **BUHRâ1r3 LANDED `20260812`** â `pond/apps/corpus_query.rye` carries the same host to the **craft voice**: a Lantern request routes through **Ember** for a real reading of a pinned corpus catalog (count_rye â `3` Â· sum_lines â `160` Â· paths_rye â `rye/forge.rye rye/tally.rye rye/mandi.rye`), returned bounded by the same allowâlist + token/stop metering; unknown op refused, zero budget refused, length budget truncates. Additive â rungs one & two stay GREEN, TAME bans clean; witness `tools/buhr_corpus_query_witness.rish` GREEN. **All three built voices (Lattice Â· Scribble Â· Ember) now compose through the Qâvane host, each over public APIs only.** **BUHRâ1r4 LANDED `20260812` â the Qâvane host, realized:** `pond/apps/quin_host.rye` gathers the three voices under one bounded entry (`dispatch`), routing a single Lantern request to whichever voice recognizes the prompt (matmulâLattice Â· outlineâScribble Â· sum_linesâEmber), returning the Lantern Response tagged with the serving `Voice`. Faults surface at once (disallowed model / zero budget â no fallâthrough); a prompt no voice knows is refused, not answered; the serving voice's metering (length stop) carries through unchanged. Additive â imports the three apps, calls only their public `answer`; rungs 1â3 stay their own GREEN binaries. Witness `tools/buhr_quin_host_witness.rish` GREEN, TAME bans clean. **BUHR Journey 1 (Quin's voices) â the crux â is COMPLETE.** **BUHR Journey 2, Rung 1 LANDED `20260812` â the first Realidream app:** `pond/apps/mantra_browser.rye` reads a Mantra `BoltCatalog` and renders its appendâonly log as a readable revision graph â under each peer/bolt, each path a lane, each lane's revisions chaining `r1 -> r2 -> r3` with a short content digest per node â composed over Mantra's public catalog API only (no new abstraction). Deterministic (referential transparency carried into the view), empty log bounded to `(empty log)`, undersized buffer refused with `RenderTooLong`. Witness `tools/buhr_mantra_browser_witness.rish` GREEN, TAME bans clean. **BUHR Journey 2, Rung 2 LANDED `20260812` â the graph, painted:** `pond/apps/mantra_browser_view.rye` carries rung one's revision graph onto WADE1's styled Skate surface â `paint_graph` calls `mantra_browser.render` for the exact bytes rung one witnesses and dresses each line onto a Skate grid row (Flax paper Â· Bark ink), so **editor and view share one graph** (the painted view cannot drift from the text). Glyphs rasterize to lit ink pixels; light and dark token sheets lower to distinct grounds (styling is real); a short viewport bounds the paint with no overflow. Witness `tools/buhr_mantra_browser_view_witness.rish` GREEN (rung one still GREEN â additive), TAME clean. **BUHR Journey 3 exploration SEATED `20260812`** (`active-designing/20260812-111800_buhr-journey3-mcp-in-bron-exploration.md`) â MCPâinâBron: the Model Context Protocol spoken in Bron, not JSON, grounded in real surfaces (Bron notation Â· `mandate/serve.rye` bounded request/response Â· `brix/infuse` cascade). Ordered Lindyâfirst cruxâfirst: the durable artifact is the descriptor **shape** (`format mcp-tool-v1` manifest Â· `mcp-call-v1`/`mcp-result-v1` envelope), the crux is bounded Â· valueâmodelâhonest Â· roundâtrippable with unknown fields **refused**; BronâJSON tiles named horizon. **BUHR Journey 3, Rung 1 LANDED `20260812` â the descriptor shape fixed:** `pond/apps/mcp_bron.rye` â a `format mcp-call-v1` envelope (id Â· tool Â· repeated `arg name value`) parses into a bounded request, routes to a real tool (`add` over integers, `join` over strings), and answers with a paired `format mcp-result-v1` that `parse_result` reads back deterministically (round-trip asserted byte-for-byte); a minimal `format mcp-tool-v1` manifest reader reads tool name + arg count. Refusals real: unknown tool â `error unknown_tool`, unknown field â `UnknownField` (never dropped), wrong header â `BadFormat`, missing tool â `MissingField`, missing arg â `error bad_args`. Reuses the flat-Bron idiom (`brix/infuse`) + bounded request/response (`mandate/serve`); no new transport. Witness `tools/buhr_mcp_bron_witness.rish` GREEN (8 cases), TAME clean. **BUHRâJ3r2 LANDED `20260812` â the BronâJSON tiles:** `pond/apps/mcp_json.rye` carries the owned shape to a JSONâspeaking MCP host and back. A bounded Bron `Call` renders to a real JSONâRPC `tools/call` request (`call_to_json`) and reads back into the same `Call` (`call_from_json`); a Bron `Result` renders to the JSONâRPC response shape (a `content` text item on success, a `code`/`message` object on error) and reads back into the same `Result`. The crux property carries across the tile â `parse(render(x))` recovers `x` both directions, asserted byteâforâbyte, and a quoted+backslashed value survives the crossing. A purposeâbuilt bounded reader (objects Â· strings Â· unsigned ids), never a general JSON parser: unknown field refused (not dropped), nonâ`tools/call` method â `BadMethod`, truncated envelope â `Unterminated`. Additive â imports rung one's shapes, calls only their public API; rung one stays its own GREEN binary. Witness `tools/buhr_mcp_json_witness.rish` GREEN (7 cases + rungâone reârun), TAME clean, widthâcheck clean. **BUHRâJ3r3 LANDED `20260812` â the manifest tile:** `pond/apps/mcp_manifest.rye` lets a JSON host **discover** a Grain tool (the discovery half to r2's call half). A Bron `format mcp-tool-v1` manifest (tool Â· summary Â· typed args Â· result) renders to the JSON tool descriptor a `tools/list` carries (`name` Â· `description` Â· `inputSchema` typed properties + `required` Â· `outputSchema`) and reads back into an equal manifest, asserted field by field. Value model crosses honestly â `int`/`str`/`bool` â `integer`/`string`/`boolean`, an unmappable type refused at the border, unknown descriptor field refused. Additive (exposed r2's JSON helpers `pub`, reuses them; r1/r2 reârun GREEN). Witness `tools/buhr_mcp_manifest_witness.rish` GREEN (4 cases + r1/r2 reâruns), TAME + width clean. **BUHRâJ3r4 LANDED `20260812` â the tools/list envelope:** `pond/apps/mcp_list.rye` carries r3's singleâtool discovery to its real shape â the whole `tools/list` response, a bounded array of descriptors, so a JSON MCP host learns **every** Grain tool a server offers in one exchange. A bounded Bron catalog renders to a JSONâRPC `tools/list` response (`{â¦,"result":{"tools":[<descriptor>,â¦]}}`) and reads back into an equal catalog, tool for tool and argument for argument, byteâforâbyte. It reuses r3's own writer and reader â each tool rendered by `manifest_to_json`, each read by a newlyâexposed `read_manifest` looped over one shared cursor (r3 refactored additively, reâruns GREEN), so no braceâscanner is duplicated. Refusals real: a list past the catalog bound â `TooManyTools` (never silently truncated), an unknown field beside the tools array â `UnknownField`. The list envelope owns its own bounded append (the descriptor tile's 512âbyte buffer is too small for the whole catalog); the reader inherits the tile's 512âbyte scratch bound. Witness `tools/buhr_mcp_list_witness.rish` GREEN (4 cases + r3 reârun), TAME + width clean. **BUHRâJ3r5 LANDED `20260812` â the JSON host bridge:** `pond/apps/mcp_host.rye` joins r1âr4 into one entry â a JSON MCP host's whole conversation served over the owned Bron shapes end to end. `serve` reads a JSONâRPC request's method and routes it: `tools/list` â the server's whole catalog (r4); `tools/call` â r2 `call_from_json` â r1 `dispatch` (real `add`/`join`) â r1 `parse_result` â r2 `result_to_json`. Pure composition â no new transport, no new parser but a small bounded `skip_value` so `read_method` passes over any field (including `params`) regardless of order, so a paramsâfirst request still routes. Reuses r4's bounded append (made `pub`) to place the call response into the larger buffer (no third append). Refusals real end to end: unknown tool â JSONâRPC `unknown_tool` (never faked), unrecognized method â `method_not_found`. Witness `tools/buhr_mcp_host_witness.rish` GREEN (5 cases + r4 reârun), TAME + width clean. **BUHR Journey 3 whole â shape Â· tiles Â· manifest Â· list Â· bridge. BUHRâJ2r3 LANDED `20260812` â the living edge, lit:** `pond/apps/mantra_browser_heads.rye` deepens the Realidream surface with **semantic perâcell color** â each lane's **head** revision (the newest node in a chain) is lit in the sheet's warm `edge` token via WADE1's `dress_run`, while older nodes keep the reading ink, so a reader sees at a glance where each lane's history currently stands. The head is found in the very bytes rung one renders (the rightmost `r<n>[digest]` on a lane line), so the paint can never point at a node the text does not show; the paint is strictly additive (glyphs byteâunchanged, rung two stays its own GREEN binary). Proven on metal â both token colors reach the canvas at once (1468 ink pixels Â· 826 head pixels). Added `tokens.token_color` (a public sheetâtokenâSkateâColor reader, reused by the witness). Witness `tools/buhr_mantra_browser_heads_witness.rish` GREEN (6 welcomes + rungâtwo reârun), TAME + width clean. **Then refined `20260812`:** the living edge now wears its own **semantic designâsystem token** â added `head` (Ochre `0xFFC89B5A`, `head_ochre`, proven warm/lowâblue and brighter than both ink and edge) to `base_sheet`, and repointed the app from `edge` to `head`, so a `.brush` theme recolors the head on its own key without touching borders or ink (asserted head â  ink â  edge). **BUHRâJ2r4 LANDED `20260812` â the living edge, themed:** `pond/apps/mantra_browser_theme.rye` closes the authoring loop the `head` token was named for â a real `.brush` file (`pond/apps/hot-edge.brush`) whose `style:` block overrides `head` (terracotta `0xFFB4502D`, warm/lowâblue) is read through the **actual authoring loop** (`brush_parse.parse_brush` validates Â· `collect_brush_style` reassembles flat Bron), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by rung three's unchanged `paint_heads`, so **every lane's living edge burns the authored color**. The recolor is proven twice: the themed head resolves to the terracotta, and the default ochre leaves **zero** pixels on the themed canvas (recolor, never addition). Warm/lowâblue holds *through* the theme (a theme cannot smuggle a cold head past the palette law); glyphs are byteâunchanged (editor and view stay one graph); a styleâless `.brush` refuses with `ThemeMissingHead`. Additive â rungs 1â3 stay their own GREEN binaries. Witness `tools/buhr_mantra_browser_theme_witness.rish` GREEN (5 welcomes + rungâthree reârun), TAME + width clean. **BUHRâJ2r5 LANDED `20260812` â two `.brush` layers compose:** `pond/apps/mantra_browser_theme_stack.rye` proves the deeper designâsystem property â an independent **mode** layer (`pond/apps/dark-mode.brush`, ground only) and **accent** layer (`pond/apps/warm-accent.brush`, living edge only) compose through the WADE1 cascade (`tokens.resolve_stack`), the top layer winning per token, the base sheet's untouched tokens inheriting beneath both. Proven by reading back **three sources at once**: the ground (Bark/Flax) from the mode layer, the head (honey `0xFFD9A441`) from the accent layer, the edge rule (`edge_warm`) from the base sheet neither layer named. Each layer read through the real authoring loop; a styleâless layer refuses `LayerEmpty`; the composed honey head reaches the canvas. Additive â rungs 1â4 stay GREEN. Witness `tools/buhr_mantra_browser_theme_stack_witness.rish` GREEN (4 welcomes + rungâfour reârun), TAME + width clean. **BUHRâJ2r6 LANDED `20260812` â the reading ground, themed:** `pond/apps/mantra_browser_view_theme.rye` carries the authoring loop to the other side of the sheet â rung four themed the living edge (the `head` accent); this rung themes the reading **ground** itself. A real `.brush` *mode* file (`pond/apps/dark-mode.brush`) whose `style:` block overrides `paper`+`ink` is read through the actual authoring loop (`parse_brush` Â· `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by rung two's unchanged `view.paint_graph`, so the whole graph lands on the authored ground â the reading surface flips lightâdark by editing one file, never Skate or Rye. The recolor is proven honestly around **roles**, not a false zero: the dark theme swaps Flax and Bark between the ground and ink roles, so the same graph painted twice shows the ground inverting from the light majority to the dark majority (Bark rises from a minority ink to the majority ground, Flax falls the other way). Warm/lowâblue holds through the theme; glyphs byteâunchanged; a styleâless `.brush` refuses `ThemeMissingGround`. Additive â rungs 1â5 stay GREEN. Witness `tools/buhr_mantra_browser_view_theme_witness.rish` GREEN (5 welcomes + rungâtwo reârun), TAME + width clean. **Now both sides of the sheet are `.brush`âauthorable â the accent (r4) and the ground (r6), each proven end to end. BUHRâJ2r7 LANDED `20260812` â one `.brush` file reâskins a whole component on both axes at once:** `pond/apps/mantra_browser_full_theme.rye` proves the property a design system actually promises â a single `.brush` file (`pond/apps/hot-edge.brush`, all three tokens) reâskins the heads view on **both** axes together. `resolve_full_theme` requires `paper`+`ink`+`head` all present (else `ThemeIncomplete`), infuses over `base_sheet`, and paints via rung three's unchanged `paint_heads`, so one edited file flips the reading surface dark **and** burns every lane's living edge terracotta in one reading. Each axis by its own honest measure â the ground by role inversion (Flax rules the base canvas, Bark the themed one, since the theme swaps the two colors between ground and ink roles), the accent by a clean zero (terracotta distinct from both grounds â zero pixels on the untouched base canvas). Warm/lowâblue holds across every axis; glyphs byteâunchanged; a groundâonly theme refuses `ThemeIncomplete`. Additive â rungs 1â6 stay GREEN. Witness `tools/buhr_mantra_browser_full_theme_witness.rish` GREEN (4 welcomes + rungâfour & rungâsix reâruns), TAME + width clean. **The `.brush` theming arc (BUHRâJ2 r1âr7) is complete end to end.** **BUHR Journey 4 OPENED `20260812` â the Realidream editing surface:** the exploration's three planned journeys (Quin voices Â· Mantra browser Â· MCPâinâBron) all ran green, so the planâexhausted blind spot opened a new journey â the **writing** half of Realidream's oneâgraph promise (J2 built only the reading half). Exploration `active-designing/20260812-123509_buhr-journey4-realidream-editing-surface-exploration.md` names the crux (**an edit is an append the graph already knows how to show**) and one door. **BUHRâJ4r1 LANDED `20260812`:** `pond/apps/mantra_editor.rye` `commit` reads a lane's head via a new `pub mantra_browser.head_revision` (added where headâfinding already lives â substrate `recall_lap1.rye` untouched), appends the next revision by Mantra's public `append_leaf` (appendâonly order Â· immutability Â· field bounds enforced), returns it, refuses an unknown lane (`LaneUnknown`). Witness renders before/after: the edited lane grew by exactly one `r<n>` node (chain arrow intact), every other lane byteâunchanged, a second edit chains `r1 -> r2 -> r3` (appendâonly). An edit is an append â the editor inherits accreteâneverâbreak as its data model. Additive â J2 reader reâruns GREEN. Witness `tools/buhr_mantra_editor_witness.rish` GREEN (3 welcomes + reader reârun), TAME + width clean. **BUHRâJ4r2 LANDED `20260812` â the edit moves the living edge forward, visibly:** `pond/apps/mantra_editor_view.rye` composes `editor.commit` with `heads.paint_heads`, proving at the gridâcell level (`heads.head_slot`) that after a commit the edited lane's headâtoken cells sit further right â the living edge advanced `r1 â r2`, the lit head covering the new node's span (`heads.head_span`) â every other lane byteâunchanged; a second edit advances `r2 â r3` on a wider grid (the paint's own `ViewTooNarrow` bound named honestly). Gridâcell level â the edge is a column number, its advance an inequality. Additive â editor (J4r1) + heads (J2r3) reârun GREEN. Witness `tools/buhr_mantra_editor_view_witness.rish` GREEN (3 welcomes + two reâruns), TAME + width clean. **BUHRâJ4r3 LANDED `20260812` â the editor forks a new lane as well as continuing one:** added `pub open_lane` to `mantra_editor.rye` beside `commit` (the editor's "new file" to commit's "edit file") â refuses a path that already carries a leaf (`LaneExists`), else appends revision 1. Both are appends to one appendâonly log, so the whole editor surface stays inside Mantra's order/immutability laws â no rewrite path to guard. Witnessed in `mantra_editor_fork.rye`: opening `c.txt` grows the graph by one lane line (`r1`, no arrow), lane_count +1, existing lanes byteâunchanged; the forked lane is a lane like any other (commit chains `r1 -> r2`); a duplicate open refuses `LaneExists`. Additive â editor (J4r1) reâruns GREEN. Witness `tools/buhr_mantra_editor_fork_witness.rish` GREEN (3 welcomes + editor reârun), TAME + width clean. **The editor's core append surface â open Â· commit Â· showâlit â is complete.** **BUHRâJ4r4 LANDED `20260812` â undo is reading an earlier revision, then appending it:** added `pub revert` to `mantra_editor.rye` beside `commit` â in an appendâonly log there is no node to delete, so undo recalls the bytes bound at an earlier revision (`catalog.recall`) and commits them as the lane's next revision. `pond/apps/mantra_editor_undo.rye` proves it by the bytes: on a lane at r2 ("betaâtwo"), reverting to r1 ("beta") returns the r1 bytes as a new head **r3**, while r2 still recalls "betaâtwo" untouched (undo added a node, never removed one) and the graph grows `r1 -> r2 -> r3`, every other lane byteâunchanged; a revert past the head refuses `RevisionUnknown`, a revert on a lane with no head refuses `LaneUnknown`. Undo inherits accreteâneverâbreak by construction â no rewrite path to guard, only a read and an append. Additive â rung one (editor) reâruns GREEN. Witness `tools/buhr_mantra_editor_undo_witness.rish` GREEN (3 welcomes + editor reârun), TAME + width clean. **The editor's whole core surface â open Â· commit Â· undo Â· showâlit â is complete.** **BUHRâJ4r5 LANDED `20260812` â undo, painted through to the styled surface:** `pond/apps/mantra_editor_undo_view.rye` carries r4's undo onto WADE1's Skate surface and shows the visible signature of appendâonly undo â after reverting r2 back to r1, the headâtoken **living edge advances** (to r3, since undo is a commit like any other), **yet the newest node wears the revertedâto node's content fingerprint**: r3's rendered 8âhex digest equals r1's (Mantra derives each digest from content, and undo brought r1's content back), while r2's digest differs. Proven at the cell level: the edge column moved right, the lit head covers the restored r3 node, every other lane paints byteâunchanged. So a reader sees history grow and the content return at once. Additive â rungs two (view) & four (undo) reârun GREEN. Witness `tools/buhr_mantra_editor_undo_view_witness.rish` GREEN (3 welcomes + two reâruns), TAME + width clean. **BUHR Journey 5 OPENED `20260812` â the webâread half beside the programâwrite half:** with J4's editor complete, the planâexhausted blind spot opened the last half of Realidream's oneâgraph promise. Exploration `active-designing/20260812-125750_buhr-journey5-web-read-half-exploration.md` (selfâapproved design round) names the crux â **a read source and a written program are two lanes on one appendâonly graph, and one fold shows both** (Realidream's *two sources feed one node*) â and holds a real network fetch as a **named seam behind a bounded reader** (its own later rung, surfacing for Keaton's word). **BUHRâJ5r1 LANDED `20260812`:** `pond/apps/realidream_reader.rye` `pin_read` appends a read source's content under a `read/` path (marking origin honestly; refuses a nonâread path `NotAReadPath`, a duplicate `LaneExists`), and the very same `mantra_browser.render` paints the read lane beside the program lanes, grouped by bolt. Proven: one render shows both origins (`local/web` read + `local/notes` program); a commit to a program lane grows it by one node while the read lane stands **byteâforâbyte unchanged** â a write on one origin never disturbs the other. **Reads no network** (content supplied inâprocess, as the demo catalog does today). Additive â J4 editor + J2 browser reârun GREEN. Witness `tools/buhr_realidream_reader_witness.rish` GREEN (3 welcomes + editor reârun), TAME + width clean. **BUHRâJ5r2 LANDED `20260812` â a reâread is an append, exactly as an edit is:** `pond/apps/realidream_reread.rye` `refresh_read` continues an existing `read/` lane â the newer content never overwrites the old, it chains after it (over the editor's own public `commit`), so the read half inherits accreteâneverâbreak by the same construction the editor did. Proven: pin a read lane at r1 and refresh it, and the read lane chains `r1 -> r2` while every program lane stands byteâforâbyte unchanged; the earlier read revision still recalls its original bytes after the refresh (r1 â "firstâread" Â· r2 â "secondâread" â the old read stands, never rewritten); a nonâread path refuses `NotAReadPath`, an unpinned read lane refuses `LaneUnknown` (a reâread continues a chain; `pin_read` opens). Reads no network (refreshed content supplied inâprocess). Additive â rung one (J5r1) + J4 editor reârun GREEN. Witness `tools/buhr_realidream_reread_witness.rish` GREEN (3 welcomes + rungâone reârun), TAME + width clean. **BUHRâJ5r3 LANDED `20260812` â a program node depends on a read node (the graph's true fused edge):** `pond/apps/realidream_derive.rye` `derive_from_read` reads a `read/` lane's head content and appends a derived program node whose content is a **pure function** of the read (`read-len=<n>`, standing in for any real consumer), so the read is **upstream** of the program, not beside it. Because the derived node is a pure function of the read it consumed, a reâread flows downstream honestly: refresh the read (rung 2's append), derive again, and the new program node reflects the new read (`read-len=10`) while the earlier program node **still recalls** the value it derived from the earlier read (`read-len=4`) â referential transparency across the fused edge, nothing mutating behind the reader; the change flows **only** on a fresh derive. First derive opens the program lane, each later one continues it. Refuses a nonâread source (`NotAReadPath`), a read destination (`NotAProgramPath`), an unpinned read (`LaneUnknown`). Reads no network. Additive â rungs one & two reârun GREEN. Witness `tools/buhr_realidream_derive_witness.rish` GREEN (3 welcomes + rungâtwo reârun), TAME + width clean. **BUHRâJ5r4 LANDED `20260812` â the fusion whole: two read sources feed one program node:** `pond/apps/realidream_fuse.rye` `derive_from_two_reads` reads the head content of **two distinct** `read/` lanes and appends a fused summary (`a=<la> b=<lb>`) whose content is a pure function of **both** current heads â the foundation's "two sources feed one node," drawn whole. Refreshing **either** source flows downstream independently on the next derive: reâread the first â `a=8 b=2`, reâread the second â `a=8 b=3`, while the first fused node still recalls `a=4 b=2` (referential transparency across a twoâinput edge, nothing mutating downstream). Refuses a nonâread source (`NotAReadPath`), a read destination (`NotAProgramPath`), the same lane named twice (`SameSource`), an unpinned read (`LaneUnknown`). Reads no network. Additive â rung three reâruns GREEN. Witness `tools/buhr_realidream_fuse_witness.rish` GREEN (3 welcomes + rungâthree reârun), TAME + width clean. **The Realidream fusedâedge arc (J5 r1âr4) stands: two lanes on one graph Â· a reâread is an append Â· one read feeds one program node Â· two reads fuse into one.** **BUHRâJ5r5 LANDED `20260812` â the read, program, and fused lanes painted together, origin made visible:** `pond/apps/realidream_view.rye` `paint_origins` renders the whole fused scene (two `read/` lanes Â· a `fused` program node Â· the demo program lanes) onto **one** WADE1 styled Skate surface â ten styled rows â and marks each **read origin** in the sheet's own new **`read` (Madder)** token, while every lane's living edge keeps Journey 2's **`head` (Ochre)** token, reused unchanged. So one canvas carries **three** readings at once: the body ink, the read origins, each lane's head â proven on metal (1758 ink Â· 391 readâorigin Â· 1604 livingâedge pixels share the canvas; read â  head asserted). The origin mark reads off the very bytes the browser renders (`read_label_span` finds the `read/` path label â exactly 2 read lanes tinted, 21 cells; program lanes clean), so the paint can never point at an origin the readable text does not name. Added `tokens.read_madder` + a `read` line in `base_sheet` (warm/lowâblue proven, distinct from ink Â· edge Â· head), additively â the tokens selftest, J5r4 fuse, and J2r3 heads all reârun GREEN. Reads no network. Witness `tools/buhr_realidream_view_witness.rish` GREEN (6 welcomes + rungâfour & heads reâruns), TAME + width clean. **BUHRâJ5r6 LANDED `20260812` â a `.brush` theme recolors the read origin:** `pond/apps/realidream_view_theme.rye` closes the authoring loop the `read` token was named for â a real `.brush` accent file (`pond/apps/read-source.brush`) overriding only the `read` key is read through the actual authoring loop (`parse_brush` Â· `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by J5r5's own `paint_origins` unchanged, so every read origin burns the authored amber while ink Â· edge Â· livingâedge head stand untouched (proven equal channel by channel). Recolor proven twice: the amber reaches the canvas (391 px), the default Madder leaves **zero** px (recolor, never addition); warm/lowâblue holds through the theme; glyphs byteâunchanged; a styleâless `.brush` refuses `ThemeMissingRead`. Made `realidream_view.fused_scene` pub (additive); J5r5 reâruns GREEN. Witness `tools/buhr_realidream_view_theme_witness.rish` GREEN (5 welcomes + rungâfive reârun), TAME + width clean. **The read origin and the living edge are now both `.brush`âthemeable, each proven end to end.** **BUHRâJ5r7 LANDED `20260812` â two `.brush` layers compose the Realidream surface:** `pond/apps/realidream_view_theme_stack.rye` proves the compose property (mirroring J2r5 for the read origin) â a **mode** layer (`dark-mode.brush`, ground) and a **readâsource accent** layer (`read-source.brush`, read token) compose through the WADE1 cascade (`tokens.resolve_stack`), the top layer winning per token, the base's untouched tokens (`head` Â· edge) inheriting beneath both. Three authors read back from one sheet â ground from the mode, read origin (amber) from the accent, livingâedge head (Ochre) from the base neither layer named; on the canvas the stacked amber read (391 px) and the inherited Ochre head (1604 px) both land through J5r5's `paint_origins`; a styleâless `.brush` refuses `LayerEmpty`. Additive â J5r6 reâruns GREEN. Witness `tools/buhr_realidream_view_theme_stack_witness.rish` GREEN (4 welcomes + rungâsix reârun), TAME + width clean. **The read origin's theming story is now symmetric with the living edge's â singleâfile theme and multiâlayer compose, each proven end to end.** **BUHRâJ5r8 LANDED `20260812` â the fused downstream edge, painted:** `pond/apps/realidream_view_derived.rye` `paint_flow` lights the other end of the edge rung five opened â a **derived** program node (content a pure function of the reads upstream of it) names itself under a `prog/` path, symmetric to a read origin's `read/`, and wears the sheet's own new **`flow` (Weld)** token, so one canvas carries the whole edge at once: read origin upstream in Madder, derived node downstream in Weld, every lane's living edge in Ochre. The mark reads off the very bytes `mantra_browser.render` writes (`derived_label_span` finds the `prog/` label), so it can never point at a computed node the text does not name; the scene derives `prog/summary` from `read/src` through rung three's own `derive_from_read` â a real derived node in the log (9 rows Â· 1 derived lane Â· 13 flow cells Â· 162 readâorigin + 267 derivedânode pixels on one canvas, read â  flow asserted). Grew `tokens.base_sheet` with the `flow` (Weld) token â a plantâleafâdye gold, warm/lowâblue proven, distinct in red from ink Â· edge Â· head Â· read â additively; `read_token` kept local so no file beneath changed. Reads no network. Witness `tools/buhr_realidream_view_derived_witness.rish` GREEN (tokens Â· J5r5 Â· J5r3 reâruns; J5r6/J5r7 themes over base_sheet stay GREEN), TAME + width clean. **Both ends of Realidream's fused edge are now lit on one canvas.** **BUHRâJ5r9 LANDED `20260812` â a `.brush` theme recolors the derived node:** `pond/apps/realidream_view_derived_theme.rye` closes the authoring loop the `flow` token was named for, symmetric to r6's readâorigin theme. A real `.brush` accent file (`pond/apps/flow-source.brush`) whose `style:` block overrides only the `flow` key is read through the actual authoring loop (`parse_brush` Â· `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by r8's own `paint_flow` unchanged â so every derived node burns the authored honey (`0xFFD2882A`) while ink Â· read origin Â· edge Â· livingâedge head stand untouched. Proven twice: the honey reaches the canvas (267 px), the default Weld leaves **zero** px (recolor, never addition); warm/lowâblue holds through the theme; glyphs byteâunchanged; a styleâless `.brush` refuses `ThemeMissingFlow`. Additive â J5r8 & J5r6 reârun GREEN. Witness `tools/buhr_realidream_view_derived_theme_witness.rish` GREEN (5 welcomes + two reâruns), TAME + width clean. **Both ends of Realidream's fused edge are now themeable â upstream read origin and downstream derived node, each on its own key.** **BUHRâJ5r10 LANDED `20260812` â two `.brush` accent layers compose both fusedâedge ends at once:** `pond/apps/realidream_view_derived_theme_stack.rye` joins r6's read accent and r9's flow accent â a **readâsource** layer (`read` only) and a **flowâsource** layer (`flow` only) compose through the WADE1 cascade (`tokens.resolve_stack`), each winning the one token it names, ground Â· head Â· edge inheriting from base â so a person themes both ends at once by stacking two small `.brush` files (amber read origin over honey derived node). Proven: three authors on one sheet (read amber Â· flow honey Â· inherited Ochre head), both accents distinct, both reach the canvas (162 read + 267 flow + head px through r8's `paint_flow`); a styleâless layer refuses `LayerEmpty`. A compoundâassert red was caught by `tame_style_check` before commit and split. Additive â J5r9 & J5r7 reârun GREEN. Witness `tools/buhr_realidream_view_derived_theme_stack_witness.rish` GREEN (4 welcomes + two reâruns), TAME + width clean. **The Realidream fusedâedge theming arc is symmetric and complete â each end themeable on its own key (r6 read Â· r9 flow), both composable together (r7 read+mode Â· r10 read+flow).** **Held seam (Keaton's word):** the **real network fetch** behind a bounded reader â the one genuine outward act, its own later rung. **BUHR Journey 6 OPENED `20260812` â the intelligence reads the surface:** the equinox is named *Surface & Intelligence*, and five journeys built each half alone (J1 the Q-vane voices over pinned corpora; J2âJ5 the Realidream graph); no rung had let the intelligence read the living surface â the itinerary's blind spot. A self-approved design round (`active-designing/20260812-140058_buhr-journey6-â¦-exploration.md`) double-seated J6 and named its crux: a voice whose corpus is the *living* graph. **BUHRâJ6r1 LANDED `20260812`:** `pond/apps/graph_query.rye` â a Lantern-shaped request routes by prompt to a real reading of the actual `BoltCatalog` the browser renders (`lane_count`â3 Â· `bolt_count`â2 Â· `head` of a.txtâr2 Â· `summary`â`bolts=2 lanes=3`), each bounded/metered by the same Lantern contract every other voice obeys (validate Â· allow-list Â· token/stop metering). Not a pinned answer â the head the voice reports (r2 on a.txt) is exactly the head the browser paints and the editor continues, so intelligence and surface share one graph as editor and view do. A two-token summary proves the length-budget truncation (length stop); a disallowed model, an unknown op (no fake answer), and a zero budget each refuse. Composed over public APIs only (`lantern_core` + `mantra_browser`), no new abstraction. Witness `tools/buhr_graph_query_witness.rish` GREEN (8 cases + browser re-run), TAME + width clean. **BUHRâJ6r2 LANDED `20260812` â the query tracks a live edit (the crux, taken next):** refactored `graph_query` additively (`answer_over` over an *explicit* catalog; `answer` delegates over the demo graph â rung one reâruns GREEN), then `pond/apps/graph_query_edit.rye` composes the query with J4's editor over one catalog: ask head (r2 on a.txt) â `mantra_editor.commit` â ask again â head advances to r3 (moved by the edit, not guessed); a fork (`open_lane`) grows the reported `lane_count` 3â4; and because an edit is an append the query is **readâonly** â an edited catalog reads r3 while a pristine one reads r2, each its own true head. Additive (imports `graph_query` + `mantra_editor`, public API only); witness `tools/buhr_graph_query_edit_witness.rish` GREEN (3 welcomes + rungâone reârun), TAME + width clean. **BUHRâJ6r3 LANDED `20260812` â the surface voice, gathered under the Qâvane host:** `pond/apps/quin_host_graph.rye` *wraps* J1's `quin_host.dispatch` unchanged (Lattice/Scribble/Ember tried first) and falls through to `graph_query.answer` only on UnknownOp, tagging a fourth **graph** voice â so arithmetic, reading, corpus, and the living graph all answer under one bounded dispatch (the equinox thesis at the host level). A disallowed model surfaces at once (no fallâthrough); an unknown prompt refuses. Additive (a `lift` maps the threeâvoice `Voice` into the wider set; `quin_host` stays its own green binary); witness `tools/buhr_quin_host_graph_witness.rish` GREEN (6 welcomes + 2 unwelcomes + host reârun), TAME + width clean. **BUHRâJ6r4 LANDED `20260812` â the reading voice folds the graph (Journey 6 complete):** added an *additive* markdown door on the browser â `mantra_browser.render_markdown` emits the same appendâonly grouping as `render` but as a `# mantra log` title Â· `## peer/bolt` bolt headings Â· `- path` lane lines (its own selftest; `render`/`head`/count paths untouched); then `pond/apps/graph_reading_query.rye` folds that markdown with **Scribble** â `outline`â title + every bolt (`mantra log local/notes local/site`), `bolts`â depthâtwo bolt headings only; a fork under a new bolt grows the fold (`local/wiki` appears), the reading tracking the living surface as the counting voice did. The one source is `render_markdown`, Scribble parses exactly those bytes, so the reading can never drift from the graph the reader sees. Length budget truncates (length stop); disallowed model Â· unknown op (no fake answer) Â· zero budget each refuse. Additive (browser + counting voice stay their own green binaries); witness `tools/buhr_graph_reading_query_witness.rish` GREEN (4 welcomes + 3 unwelcomes + browser reârun), TAME + width clean. **BUHR Journey 6 stands complete end to end â read Â· trackâaâliveâedit Â· gatheredâunderâhost Â· readingâfold; Surface and Intelligence meet over one graph.** **BUHR Journey 7 OPENED `20260812` â Tablecloth, the named artifact store:** with Journey 6 complete, the itinerary's blind spot is the equinox's **last unbuilt module** â **Tablecloth**, the Lexicon's *Brixâfacing application database and artifact store* (held apart from the alreadyâwitnessed namespace query). A selfâapproved design round (`active-designing/20260812-142132_buhr-journey7-tablecloth-artifact-store-exploration.md`) opened it Lindyâfirst cruxâfirst; the crux â **a human name bound to a content address**, so fetchâbyâname returns exactly the bytes whose digest the name pins, a tamper anywhere refusing. **BUHRâJ7r1 LANDED `20260812`:** `pond/apps/tablecloth.rye` is the **naming layer over `mantra/beading.rye`'s contentâaddressed `BeadStore`** â it invents no storage. `store_artifact` beads content into the shared store (dedup earned: identical content stored once, named twice â `notes.txt` + `notes-copy.txt` share every bead, 0 new deposits) and binds a name to the whole digest + beadâindex recipe; `fetch_artifact` reassembles by name, every bead proving itself by digest and the whole against its recorded address (a tampered bead refuses `DigestMismatch`, an unknown name `UnknownArtifact`, a taken name `NameTaken`). The Brixâfacing `format tablecloth-v1` manifest (`artifact <name> <digest> <len>`) roundâtrips nameÂ·addressÂ·length field for field; a wrong header refuses `BadManifest`. Migrated the one `parseInt` site to `tally/parse_int.rye` (ratchet 9â8). Additive â beading stays its own GREEN binary; witness `tools/buhr_tablecloth_witness.rish` GREEN, TAME bans + width clean. **BUHRâJ7r2 LANDED `20260812` â the manifest as the true index:** `pond/apps/tablecloth_index.rye` `verify_against_store` proves a parsed `format tablecloth-v1` manifest is an **honest index of a live store** â every published name is held, reassembles (each bead by digest, the whole against its recorded address via `fetch_artifact`), and matches its published **length** and **address** (`tablecloth.address_hex`, added additively with `address_hex_width`). A name the store lacks refuses `UnknownArtifact`, a wrong length `LengthMismatch`, a wrong address `AddressMismatch` â so a person handed a manifest can check the store satisfies it, artifact for artifact. Additive â rung one (the naming layer) stays its own GREEN binary under the reader. Witness `tools/buhr_tablecloth_index_witness.rish` GREEN, TAME bans + width clean. **BUHRâJ7r3 LANDED `20260812` â a Brix descriptor declares artifacts the store satisfies:** `pond/apps/tablecloth_brix.rye` `evaluate` makes the Brixâfacing half literal â a `format tablecloth-decl-v1` descriptor (one `require <name>` line per needed artifact) is **evaluated against a live store** into a `format tablecloth-v1` manifest of exactly those declared artifacts, in declared order. The join refuses honestly: a required name the store cannot satisfy refuses the whole evaluation (`Unsatisfied`), never emitting a manifest naming bytes the store lacks; a bad header or empty descriptor refuses `BadDescriptor`. The evaluated manifest then **verifies against the store by rung two**, closing the chain â a system declares what it needs, the store proves it serves it, the manifest is the audited receipt. Additive â rungs 1â2 stay their own GREEN binaries; `tablecloth.rye` grew only a pub `artifact_len`. Witness `tools/buhr_tablecloth_brix_witness.rish` GREEN, TAME bans + width clean. **BUHRâJ7r4 LANDED `20260812` â the ownerâsigned catalog:** `pond/apps/tablecloth_keyed.rye` `store_owned` makes a name bind bytes to a settled **Kumara identity**, not merely pin them â the owner presents the point's Deed (verified against the constellation, exactly as `mandate/keyed.rye` and the name registry gate) and signs the exact publication (point Â· name Â· length Â· a SHAâ256 of the content); only then does rung one's `store_artifact` bead + name it, and the owning point is recorded. A publication resolves to *whose* it is â `owner_of` returns the point, `place_of` resolves it to its galaxy/star/planet (symmetric with `mandate/keyed.rye`'s `place_of`). Every refusal holds: a nonâowner (ghost deed, **no byte stored**), a forged signature, a contentâmismatched signature (the digest is bound in â a tampered artifact cannot ride a real signature), and rung one's takenâname refusal. **Demo keeper seeds only â no real key, no network, no custody** (gate #4, Keaton's real Kumara instance, is untouched). Additive â `tablecloth.rye` grew only a pub `index_of`; rung one stays its own GREEN binary. Symlinked kumara/tilak/topology/constellation into `pond/apps` (mirrors `mandate/`). Witness `tools/buhr_tablecloth_keyed_witness.rish` GREEN, TAME + width clean. **BUHR Journey 7's agentâdoable arc now stands â store (r1) Â· honest index (r2) Â· Brix declaration satisfied (r3) Â· ownerâsigned (r4).** The one remaining Tablecloth horizon rung â **Comlinkâserved** â reaches the network/serve gate (Keaton's hand); the held seam stays the real network fetch. **BUHR Journey 8 OPENED `20260812` â the intelligence reads the artifact store:** BUHR's seven journeys built every named module, yet one pair never met â the intelligence had read the Realidream graph (J6) and pinned corpora (J1), but no voice had read a **stored artifact** by name from **Tablecloth**. A selfâapproved design round (`active-designing/20260812-144916_â¦`) named the crux â **a voice reads only verified bytes**. **BUHRâJ8r1 LANDED `20260812`:** `pond/apps/artifact_query.rye` composes the Lantern contract over Tablecloth's verified fetch â `bytes`/`words`/`line` over a named artifact, the content ops reading **only through `fetch_artifact`**, so a **tampered bead refuses** (`DigestMismatch`) and the voice answers nothing rather than reading corruption; contentâaddressed storage becomes the intelligence's integrity guarantee for free. Unknown name Â· unknown op Â· disallowed model Â· zero budget each refuse; a length budget truncates with a length stop. Composed over public APIs only (`lantern_core` + `tablecloth`); J7r1 store stays its own GREEN binary. Witness `tools/buhr_artifact_query_witness.rish` GREEN (9 cases), TAME + width clean. **BUHRâJ8r2 LANDED `20260812`** â `pond/apps/artifact_query_publish.rye` proves the *living* in a living store: it composes J8r1's `artifact_query.answer_over` with J7's `tablecloth.store_artifact` over one catalog+store â `words brief.txt` refuses (`UnknownArtifact`, no invented answer) before the publish, and reads its true word count after; a publish is an append so held names stand unchanged (`bytes notes.txt` reads the same length across a later publish) and the query itself writes nothing (a published store reads the new name, an independent pristine store still refuses it â the reading is readâonly). Additive â J8r1 stays its own GREEN binary; witness `tools/buhr_artifact_query_publish_witness.rish` GREEN (3 welcomes + J8r1 reârun), TAME + width clean. **BUHRâJ8r3 LANDED `20260812`** â `pond/apps/quin_host_artifact.rye` gathers the artifact voice under the Qâvane host: it wraps J6r3's `quin_host_graph.dispatch` unchanged (LatticeÂ·ScribbleÂ·EmberÂ·graph tried first) and falls through to `artifact_query.answer` only on `UnknownOp`, tagging a fifth **artifact** voice â so arithmetic, reading, corpus, the living graph, and a stored artifact all answer under one bounded dispatch, the artifact voice's contentâaddress integrity travelling with it. A disallowed model surfaces at once (no fallâthrough); an unknown artifact name (`UnknownArtifact`) and an unrecognized prompt (`UnknownOp`) each refuse. Additive â the fourâvoice host stays its own GREEN binary; witness `tools/buhr_quin_host_artifact_witness.rish` GREEN (5 welcomes + 3 unwelcomes + J6r3 reârun), TAME + width clean. **BUHR Journey 9 OPENED `20260812` â the intelligence served as MCP tools:** with J8 done, the blind spot was a pair the equinox names that never met â J3 spoke MCP in Bron, J8 gathered five voices, yet the MCP host still routed `tools/call` only to J3's toy `add`/`join`. A selfâapproved design round (`active-designing/20260812-150600_â¦`) opened J9. **BUHRâJ9r1 LANDED `20260812`:** `pond/apps/mcp_voice.rye` joins them â an MCP `ask` call crosses into a Lantern request, dispatches through `quin_host_artifact.dispatch` (routing by prompt to whichever voice knows it), and the response crosses back into a `format mcp-result-v1` envelope read by J3's own `parse_result` (`matmul`â`19 22 43 50` Â· `words notes.txt`â`16` Â· `lane_count`â`3`). Every voice refusal crosses back as a **named** MCP error (`unknown_tool`Â·`model_not_allowed`Â·`unknown_op`Â·`unknown_artifact`Â·`bad_args`); the bridge never invents a result, and the artifact voice's contentâaddress integrity travels through the MCP surface. Reads no network (real fetch + Comlinkâserved stay the held gates). Additive â `mcp_bron` (J3r1) + `quin_host_artifact` (J8r3) stay their own GREEN binaries; witness `tools/buhr_mcp_voice_witness.rish` GREEN (8 welcomes + both reâruns), TAME + width clean. **BUHRâJ9r2 LANDED `20260812`:** `pond/apps/mcp_voice_tools.rye` gives each voice its own name at the protocol surface â five typed tools (`lattice`Â·`scribble`Â·`ember`Â·`graph`Â·`artifact`) discovered through J3r4's `tools/list` envelope (`list_to_json`/`list_from_json` roundâtrip), and on invoke the tool name is a **typed assertion**: `serve_voice` maps the tool to a required voice, dispatches, and crossâchecks `served.voice == want` â a `words` prompt under the `lattice` tool routes to the artifact voice, so it refuses `wrong_voice` rather than claim Lattice answered; an unlisted tool refuses `unknown_tool`, a disallowed model `model_not_allowed`. Reused J9r1's `render_ok`/`render_error`/`error_name` (made `pub`, additive â J9r1 witness reâruns GREEN) + J3r4 `mcp_list`. Witness `tools/buhr_mcp_voice_tools_witness.rish` GREEN (6 welcomes + both reâruns), TAME + width clean. **BUHR's whole agentâdoable arc stands: the intelligence is reachable, discoverable, and addressable voice by voice over the owned MCP shape.** The remaining BUHR doors â **ownerâsigned corpus** + **Comlinkâserved** â reach the custody/serve gates that are Keaton's hand.
2. **Provisioning or paying** for any cloud/VPS/Pond/subscription (Vultr SEA IaC, WADE2/3) â agents author IaC; Keaton provisions and pays.
3. **Moving funds, holding keys, or opening any custody/wallet/payment rail** â Dimeroll records facts only; disbursement waits on licensed counsel.
4. **Generating Keaton's own Kumara instance** from his real seed/keeper â his hand alone.
5. **Deep debride / history rewrite + force-push** of the living tree â named target, Keaton's explicit word.
6. **Seating a new module in a collaborator's domain** (e.g. DJINN's surface lead) beyond authored implementation-floor code â the invitation and lead are the collaborator's to accept.

Everything else â design, code, witnesses, docs, weaves, seed *projection* (not push), reds â is agent-doable and does not wait.

## Open doors (awaiting Keaton's word)

| Door | Kind |
|------|------|
| **Next JARL step** â **escape** (a child re-parenting, from the sponsor tilak) Â· shrink the shared surface to a membership commitment Â· or the scarcity design call | live |
| **Breach OPEN `20260810`** â Pond = application module (Pool retired) Â· **skies lap 1** Â· **topology inclusive** (galaxy is star is planet, 720/universe, sponsor by mod, **outfit** roles; 6 witnesses GREEN) Â· **Kyri** the notation (was Bron) Â· **Skate** = the social network | breach Â· live |
| **HANDOFF BATON `20260810`** â full vision on disk for a context reset: [`expanding-prompts/20260810-025942_the-handoff-baton-vision-checkpoint.md`](../expanding-prompts/20260810-025942_the-handoff-baton-vision-checkpoint.md). Proposed modules: **Mandate** (turbopuffer, next build) Â· **Scribe** (Kyri home + settings) Â· **Starseeding** Â· **Unsplash** Â· **`.myc`/Mycelium** (Sui reimpl) Â· **Mala** revival. Roster: `context/inspirations.kyri` (tribute, consent-gated). **Vault LANDED** `20260810` â `vault/shard.rye` (keeper of secrets, molt-supersedes Jael): Shamir GF(256) sharding, n-of-t, signed shard tilaks in location-classes (homeÂ·kinÂ·coldÂ·relicÂ·brain), tamper-refused, custody-first (fake seed only), main key never the older word. **Mandate LANDED** `20260810` â `store.rye` (zero-copy vector store, cosine k-NN, tag filter) + `keyed.rye` (**Kumara-keyed**, owner-signed). **Scribe LANDED** `20260810` â `scribe/reader.rye` (zero-copy Kyri reader; dispatch by format; the seated 1-notation-many-formats). Seated: **baton** (favorite word; a `.kyri` `format baton-v1`) Â· **"thank you everyone"** (end-marker) Â· **expanded-prompts/** (cold) beside expanding-prompts (hot) Â· council-names roster. | checkpoint |
| **3x39 BATON `20260810`** â 2nd vision on disk: `expanding-prompts/20260810-044453_the-3x39-baton-passports-dividers-and-starseeding.md`. Concepts: 3x39 namespace reset Â· divider grammar (prefix/suffix/middle, `.sol`/`.sui`-provable) Â· **Kumara passports** Â· L1 synonym-outfits (MURR/MURâ¦). Proposed: **Loom** (ScribeÃTally monitor) Â· `bat/` archetypes. Multi-agent build = Workflow on **ultracode** opt-in. **begin starseeding** (own round). | checkpoint |
| **Keaton's own Kumara instance** â generate from his real seed + keeper, by his hand alone | JARL Â· when ready |
| **moldâshape debride** â **CLOSED `20260811`** (Keaton: *leave the residue*): primary rename landed `20260720`; the residue is testimony Â· retirement-explanation Â· deliberately-kept study twins. Optional horizon only: an atomic Glow-identifier refactor (`mold_slice` ~23 files) on the language bench. Survey: `active-designing/20260811-184002_mold-shape-debride-survey-and-plan.md` | resolved |
| **TAME core/shelf** â lossless-compressed living law over a preserved shelf | proposed |
| **Brix-infuse** â **LANDED `20260811`** (SOON j2): `brix/infuse.rye` â per-key override over flat Bron (override wins per key, base-only kept in place, override-only appended), zero-copy, bounded; witness `tools/brix_infuse_witness.rish` GREEN. Horizon: nested-descriptor deep merge when Brix grows nesting | resolved |
| **Identity Remake / Kumara** â the first identity template + Keaton's instance | JARL |
| **Geode / Genode-on-NixOS** â proven-seat parallel | GATED |
| **Grainphone Â· Realidream Â· Pond seven Â· data-dignity Â· succession Â· Mand ring-3 Â· O3 gen-home** | awaiting Keaton |

---

## Card habits

- **kg** â keep going, next mechanical lap. **check-in** â pause for Keaton's word / design. **send** â commit Â· push both remotes Â· merge. **remember** â reprint this card. **align** â walk the compass, reconcile plan with green witnesses. **molt** â prep a fossil for shed. **debride** â remove dead history (Keaton's word). **shred** stays RED until circled. remember â  send â  kg â  align.
- **Vocabulary** â the tree seats **shape**, not Hoon's *mold*. Prefer **git nib**. One clock: `TZ=America/New_York`.

---

*Carry lightly. Prefer git nib. `prin scope`. May the season stay clean and the fascia hold.*
