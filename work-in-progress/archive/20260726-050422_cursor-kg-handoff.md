# Cursor kg Handoff — From Align `050422`

**Language:** EN  
**Stamp:** `20260726.050422`  
**Voice:** Quin (you are Quin — `context/QUIN.md` · Radiant Style)  
**For:** a fresh Cursor agent on Framework metal · pier `~/grain` · agent root `home-xy-grain`  
**Ground:** align note [`20260726-050422_align-before-cursor-handoff.md`](20260726-050422_align-before-cursor-handoff.md) · git tip at handoff write `ef0c527dc6` (verify with `git log -1`)  
**Remotes:** `origin` + `codeberg` → `autoproject96/grain` only (`context/REMOTE_ROSTER.md`) — never fan into xykj61 / groupproject36 lanes  

---

## Align first (already done this sitting — verify, don’t re-argue)

| Source | Wins |
|---|---|
| Counsel `044729` · `223409` · REMEMBER | Campaign finishing edge |
| Green witnesses / COLD suite | Claims of “landed” / H unlock |
| Stale TASKS/ROADMAP one-clock “Now” | **Reconciled** at `050422` to F→H→S-ladder |

**Product horizon** (newcomer ≤30 min green) remains true and is **not** what you finish next.

---

## Who you are · how you move

- **Quin** · TAME · collaboration rhythm: **kg** = keep going on mechanical, policy-written work; **check in** with Claude on seams, naming, Rishi/value-model, unruled design  
- **Send-after-every-round** is seated — when a lap lands, session log + commit (GPG) + push origin+codeberg + REMEMBER nib pin; do not urge rest  
- **Report, never urge** when the ungated queue is empty  
- Stamps: `America/New_York` · `YYYYMMDD.HHMMSS` · one clock  

---

## HARD: do not disturb in-flight COLD F

```bash
tail -f /tmp/parity-f-rerun/parity.log
# process: rishi run tools/parity.rish → parity_ch01 (started ~2026-07-26T08:10Z)
```

- This run uses the **pre-S0** driver and host `~/.cache/zig` — correct; leave it  
- Do **not** kill it, re-point its env, or start a second full parity overlapping it  
- Untracked `*.zig` beside modules are often **rye/zig emit noise** from F — **do not commit** them  
- Prior full sitting ~106 min; this one was ~54 min into ch01 at handoff write  

---

## kg queue (priority order)

### While F still runs (sidecar only)

1. Optionally thin hygiene that cannot race F (prose pins, this handoff’s follow-ups) — prefer idle watch over cleverness  
2. **Do not** start S2 packs, archive witnesses, or S3 receipt code  

### When F exits

| Result | Action |
|---|---|
| **GREEN** (or PARTIAL only for named ABSENT under `223409`) | Session-log the result · then **H** Brix survey — *reading only*: Glow generator surface · elder `.brix` · TAME Brix supplement · Tally bounds → one doc naming smallest Glow descriptor + one witness + apply path + proposed ladder slug · **STOP** (ladder name is Keaton’s) |
| **RED** | Stop · name the witness · restore if needed · check in — do not “fix forward” past two reds on one witness |

### After H (or if H blocked)

3. **Instrumented COLD** — `rishi/bin/rishi run tools/parity.rish` with the **current** tree (S0 timers + `tools/.cache/zig/`) · tee a log · ensure `PARITY_COST_REPORT=1` path writes `work-in-progress/<stamp>_parity-cost-table.md`  
4. **Only after that table exists:** S2 pack drivers (additive flags; chapters unchanged) per `044729`  
5. **S3 impl / lane (b) archive:** Keaton word; each cut is its own lap with evidence  

---

## Read order (most relevant first)

1. [`REMEMBER.md`](../REMEMBER.md) — operator Now  
2. Align + this handoff (`050422`)  
3. [`counsel/20260726-044729_the-graph-is-not-a-vane.md`](../../counsel/20260726-044729_the-graph-is-not-a-vane.md) — speed law · no P-vane  
4. [`counsel/20260725-223409_the-tool-the-door-the-rung-and-the-lane.md`](../../counsel/20260725-223409_the-tool-the-door-the-rung-and-the-lane.md) — ABSENT · PARTIAL · H after GREEN  
5. [`20260726-050047_zig-cache-audit-and-parallel-packs.md`](20260726-050047_zig-cache-audit-and-parallel-packs.md) — cold 40404 ms / warm 212 ms · parallel vs serial families  
6. [`../active-designing/yonder/20260726-050047_mantra-core-witness-receipts-hammock.md`](../../active-designing/yonder/20260726-050047_mantra-core-witness-receipts-hammock.md) — S3 design only  
7. Compass habit: [`../foundations/20260706-185112_follow-our-compass.md`](../../foundations/20260706-185112_follow-our-compass.md)  
8. Identity/style: `context/QUIN.md` · `context/RADIANT_STYLE.md` · `context/TAME_GUIDANCE.md`  

Skip re-reading the whole Radiant campaign history unless a gate trips.

---

## Standing parks (do not “helpfully” open)

- **P-vane** naming (five checks on REMEMBER)  
- Breach two · Pond seven · xykj61 remotes · Acme · lap-kinds table · Brix ladder **name** · license badge · data-dignity · succession · Mand ring-3  
- Gates-for-content-laps (additive heading + claim_preserve tokens) — parked as one future lap  
- Radiant sweeps — **on-touch only**  

---

## Witness / tool crumbs

```bash
# seated zig shelf (next runs)
sh tools/parity_zig_cache_seat.sh

# single witness
rishi/bin/rishi run tools/receipt_verify_wasm.rish
rishi/bin/rishi run tools/mand_ring1_witness.rish

# full suite (AFTER F finishes — instrumented)
mkdir -p /tmp/parity-next && rishi/bin/rishi run tools/parity.rish 2>&1 | tee /tmp/parity-next/parity.log
```

S0 helpers: `tools/parity_time_one.sh` · `tools/parity_cost_table.sh` · chapters already wrapped.

---

## First move for the new agent

1. `git status` · `git log -3` · confirm tip · note untracked zig noise without adding it  
2. Confirm F still running or read its exit + log tail  
3. Follow the kg queue above — **finishing edge before new ideas**  
4. End every pass with one line: `Recommend: keep going to …` or `Recommend: check in …`  

---

*May you pick up the edge already in flight. May COLD speak before H. And may every kg step be one the compass already named.*
