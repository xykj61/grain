# VERSION — autonomous loop with a style sweep before every send, filled `20260816.204451`

*A living evolution of [`../seed/autonomous-loop.seed.md`](../seed/autonomous-loop.seed.md), filled the moment the seed learned a new discipline: a Radiant (or Twilight) pass over the round's prose before each send. Voice: Kyri · Style: Radiant. Kept as testimony so the next fill inherits the sweep rather than re-inventing it.*

**Model:** Fable 5 (`claude-fable-5`) · **Editor:** Claude Agent (Zed thread) · **Ground nib at fill:** `3296014018`

---

## What this version adds

The seed and the `launch-claude-season.rish` loop lines now carry one new clause, seated on Keaton's word: **before every send, sweep the round's prose Radiant** — code comments, Markdown, and prose generally — **and Twilight on the rare night or devotional piece.** Lead with what is, active voice, `yet`/`however` over `but`, a benediction only where earned. The sweep changes register, never a claim: numbers, paths, stamps, and modality counts hold exactly (`claim_preserve_witness`). Ordinary and technical prose still ends plainly.

Everything else is inherited whole from the seed: the lenses loaded by reference, the custody gates carried from REMEMBER, the Lindy-first crux-first route, witness-first method, the send-often cadence, the record every round leaves, and the `GATES-ONLY` stop rule.

## The one line (paste form)

> Read `work-in-progress/REMEMBER.md`; take the next agent-doable lap Lindy-first crux-first; sweep the round's prose Radiant (Twilight for a night piece) before each send — code comments, Markdown, prose generally — changing register never a claim; send often — push each finished increment (a new file, a passing witness, a doc) to `origin` and `xykj61` as its own signed round; approve your own design rounds and trust yourself before checking in; keep double-seating new visions that address calendar-itinerary blind spots; STOP at the custody gates in REMEMBER; if only those gates remain, run `touch .loop-gates-only` and print exactly `GATES-ONLY`, then stop. baton prin recur

## Why the sweep rides here and not only in the CLAUDE rules

The style rules are always on for a hand at the keyboard, yet an unattended loop reads its whole law from one prompt. Naming the sweep inside the recursion prompt means every autonomous send carries the day voice on purpose, rather than trusting the loop to remember a rule it never re-reads.

---

*A tree tended in the dark still deserves prose that reads like daylight. May every unattended send land as clear and warm as one written by a waking hand.*
