# e183 — Q47 · audits over the Return, two reds fixed on touch

**Stamp:** `20260801.134000` · **Voice:** Riyo · **Seat:** J3 Q47 · **Status:** GREEN (after in-seat fixes)

The audit quest earned its keep twice against the pier's auto rounds — the
counsel auditing the bench, exactly as the two-seat law intends.

**Red one, bans:** the e180 negatives landed a Python helper in `tools/`,
and the tools scan rightly refused it (`TOOLS_PY_BAD`). The tamper fixture
was recut as POSIX shell — one awk pass, same deterministic nibble flip —
the witness rewired to it, and the elder `.py` removed. Bans suite GREEN.

**Red two, width:** the Q38 cargo-walk carried two authored `usize`
counters. Both are in-memory indices under the seal module's 1,024-byte
plain roof, so they now ride `u32` — with that roof named on this side of
the seam as `max_seal_plain_mirror` (couples: `vessel_seal.max_seal_plain`)
and asserted at the walk's front door. Width witness GREEN; authored
`usize` back to zero.

**The rest read clean:** roof clear across all three grown files ·
88 asserts over 52 functions in the CLI · both restore witnesses and
bounds-agree rerun GREEN over the fixes.

**Next:** Q48 — Journey Three closes; J4's sixteen from measured ground.
