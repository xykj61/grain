# Surface Season Pause — History Tidy · REMEMBER Pin Squash

**Stamp:** `20260728.205029` · **Voice:** Quin · **Season:** Surface (paused for tidy) · **Scope:** sept pause  
**Ask:** waymark p19 complete / p20 next · seat squash of REMEMBER pin commits · change commit pattern · force push · return to p-season  
**Kind:** declared **git history tidy** · Keaton's word · not path-BREACH

## Verdict

**LANDED.** Separate `work-in-progress: pin REMEMBER git nib` follow-up commits in the Surface season range are squashed into their parent work commits. Commit law going forward: the REMEMBER **git nib** updates **in the same work commit** as the send — never a second pin commit for scroll noise.

## Not path-BREACH

[`context/BREACH.md`](../context/BREACH.md) relocates and re-points; it **never rewrites bytes**. This tidy rewrites **git history SHAs** only, by Keaton's explicit force-push word. Path-BREACH law is undisturbed. Call this a **history tidy** / scroll rebase so the two motions stay distinct.

## Scope (measured)

| Measure | Before | Target | After |
| --- | --- | --- | --- |
| Range | `d4916f565e^` .. old `e8ad191f8d` (Surface p0–p19 era) | same tree tip | rewritten tip `44ff71c7e8` |
| Pin follow-ups in range | **21** | **0** | **0** (witness GREEN) |
| Work commits kept | **22** | **22** (+ tidy seat) | **23** (p0–p19 work + tidy seat) |
| Backup | — | undo branch | `backup/pre-pin-squash-e8ad191f8d` |

Whole-tree historical pins outside this range stay as dated artifacts (not rewritten this stamp).

## New commit pattern (seated)

1. Stage the work **and** set `work-in-progress/REMEMBER.md` **Git nib** to the commit about to land (or amend once after `git commit` so the nib matches HEAD — still **one** commit).  
2. **Refuse** a follow-up commit whose only job is pinning REMEMBER.  
3. Session log ships in the same work commit when possible (unchanged).  
4. Round-close send still runs; it no longer implies a second pin commit.

Rules: `.cursor/rules/remember-git-nib.mdc` · `.claude/rules/remember-git-nib.md` · round-close twins updated.

## Instrument

`tools/gen/season/remember_pin_history_tidy_witness.rish` — asserts zero pin-follow-up subjects in the Surface tidy range · rule files present · backup branch name documented.

## Return

**p20 next** — Surface Season resumes on the rewritten tip. Live-five still on word.

---

*History tidy · stamp `20260728.205029` · Quin · scroll readable · path-BREACH untouched*
