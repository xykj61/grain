# e282 — fuse v71 · personal knock home · model seated

**Stamp:** `20260804.002140` · **Voice:** Riyo · **Word:** fuse kg · save · pull · connect · model switch
**Status:** GREEN — tip already carried STOA341 · e281 knock docs landed · addresses stay ignored

## Save · pull

Tip and both remotes already agreed at `958d787bd5` before the fuse — nothing to pull; progress was already on the waters.

## Fuse v71

Basis `9755b0ecc4` · two patches. Beads matched. Patch 0001 (STOA341 metal) skipped — already aboard, byte-identical max desk; select witness twice-measured GREEN (one red owned: parallel witness race on `glow_run` binary — re-ran alone). Patch 0002 landed: counsel e281 seat + README personal-knock line. Guide 0 placeholders and flake templates untouched. Patch greps clean of HostName/IPs.

## Personal knock (local only)

`.ssh/config` written under `~/grain/.ssh/` — `git check-ignore` confirms `/.ssh/` shields it; absent from `git status`. Addresses live nowhere in tracked files.

## Model seat

`GLOW_PROFILE.bron` (root-ignored personal) already named Cursor Grok 4.5 300k High Fast; `checked` refreshed `20260804.002103` with Auto→Grok note. REMEMBER drops "Cursor Auto Mode." Template unchanged.

## Proven

| Lap | Result |
|-----|--------|
| `glow_bartis_pair_select_witness` | **GREEN** (alone) |
| `glow_tally_pair_bound_witness` | **GREEN** |

---

*May the knock be one word. May addresses never travel. May the same key that signs also open the pier.*
