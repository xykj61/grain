# e273 — STOA338a seats · pair-faces parse · PairNotYetLowered

**Stamp:** `20260803.162233` · **Voice:** Riyo · **Word:** fuse kg · **Status:** GREEN — 338a first try

## The fuse

Pier tip at fuse was `5e080f252a` (e271 walls + pier compose chart already dual-pushed). Resin v65 basis `25de09863a`. Patch 0001 walls **byte-identical** → skipped. Patch 0002 STOA338 claim hand-landed (REMEMBER/ROADMAP unioned on the pier). Beads matched `BUNDLE.manifest`.

Counsel's measurement **supersedes** the pier's interim O1 `%-` unary lean as the Now stone: the simple-gate path compares a face to a literal only, so **STOA338 pair-faces** is the hardest solvable with the widest vane dependency. The grant stays scoped — runes only; walls · keys · custody untouched.

## What 338a seats

| Seam | Result |
|------|--------|
| Spec | `face2`/`face2_len` · `cond_eq_faces` · `cond_gth_faces` · `PairNotYetLowered` |
| Parse | `|=  [a=@u32 b=@u32]` · `?:  (eq\|gth a b)  <dec>  <dec>` |
| Lower | every emit path answers **`PairNotYetLowered`** |
| Fixtures | `glow/gen/gate-pair-eq-faces.glow` · `gate-pair-gth-faces.glow` |
| Witness | `tools/glow_bartis_pair_faces_witness.rish` — parse GREEN · stranger `BodyFaceMismatch` · lone bracket refuse · argv+a2 warm |

## Next under the grant

**338b** — emit sites · driver `--sample-argv` second sample · worker `"$3"` · both fixtures both sides.

---

*May two faces parse before they lower. May every refuse keep its name. May 338b find the door already open.*
