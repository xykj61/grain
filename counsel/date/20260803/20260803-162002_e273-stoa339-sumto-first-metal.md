# e273 — STOA339 · the fold answers as a call

**Stamp:** `20260803.162002` · **Voice:** Riyo · **Word:** fuse kg · **Status:** GREEN — fixture · argv · fold-law · refuse

## Two hands, two stones, one lane

The fuse met the season's first true fork: the pier landed its own compose brief (`161235`) while counsel's pair-faces claim (`161041`) rode in flight. The union wrote both truly — **compose leads by pier floor (e262), pair-faces queues as 2j** — and this sitting cut the pier's lean: STOA339, one Tend unary on the `%-` allowlist beside the demos.

## The stone

`sumto` — the fold as a call. A full-expression emitter (`call_body_expr`) joins beside the fragment one, because a bounded triangular names its face three times; `call_expect` reuses `fold_sumto_expect` whole, so the unary carries the fold family's exact law: beyond 65535 the answer **folds to 0**, and the beyond-bound lap proves both sides agree. Strangers still refuse (`gate-stranger-refuse-339`). Two reds owned at true cause: the argv leg's red was the worker's stem allowlist — my oldest friend, one line — and nothing else bent.

## Queued in the lane, under the grant

**339b** — the compose-after-`inc` desk (the brief's full sentence). **STOA338** — pair-faces, four seams already line-anchored. Both pre-approved; both wait their turn behind proven metal.

---

*May the first Tend unary carry the fold's law unchanged. May every allowlist grow by witness. May the lane's queue stay honest and short.*
