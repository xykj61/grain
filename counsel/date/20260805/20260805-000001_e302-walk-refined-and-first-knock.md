# e302 — walk refined · first knock · steward door open

**Stamp:** `20260805.000001` · **Voice:** Riyo · **Word:** fuse kg · continue · approve and proceed  
**Status:** GREEN — resin v90 landed · Identity overclaim owned · pier knock proven · guide 1 mid-walk

## Resin v90

Basis tip `88724e7288` · one patch · beads matched · clean `git am`. The confirmation seat now carries the bench's finer grammar on the record: **Identity Remake** (k6/k7 · `return_surface_p55`) stays **PAUSED** at Lexicon row 326 — outside the sweep's `"The X Season"` pattern — so counsel's "nothing anywhere is open" overclaimed by one lane. **Walk complete at the season-row grammar**; **6b word-only** forever (name · keys · history rewrite).

## Hands — self-evident next

Close-down map door 3. Local ignored knock realigned to the grain-seated vultr key and `known_hosts_jail` (host `~/.ssh` was empty). First knock:

| Proof | Result |
|-------|--------|
| `ssh pier` as root | **GREEN** — hostname `pier` · NixOS 26.05 · `/etc/nixos` flake present |
| Host key (ED25519) | `SHA256:DonzcJPH1NWiByj4Gjyz9gIyqAMj3jEjRjylRZYS9EU` — accepted into known_hosts_jail |

## Guide 1 — Movements 1–2 (lockout-safe)

Steward name taken from the guide's own example: **`keeper`**. Declared in living `/etc/nixos/configuration.nix` with both authorized keys (vultr jail · dc1). `security.sudo.wheelNeedsPassword = true`. `nixos-rebuild switch --flake /etc/nixos#pier` **GREEN**. Proven:

| Proof | Result |
|-------|--------|
| `ssh -l keeper pier` | **GREEN** — uid 1000 · wheel |
| `ssh pier` (root) | **still GREEN** — root door not closed yet |
| `sudo -n` as keeper | needs password — as designed |

**Held for your hand:** set `passwd` once as root for `keeper`, then `ssh -l keeper pier` → `sudo -v`. Only after that smile do we walk Movement 3 (close root's network door) and Movement 4 (prove the refusal).

## Untouched

6a · 6b · quarantine word · row six · next season name · geode · LICENSE HOLD · Mitra execute.

---

*May a truer quiet keep one PAUSED lane honest. May the first knock be one word. May root stay open until the steward's password proves the second door.*
