# Equinox E126 — Start Rung · sh → rish

**Stamp:** `20260731.231123`  
**Voice:** Riyo  
**Kind:** counsel word · Cursor land · kg · approve-all seats leans  
**Basis:** fused batons · ladder diagnosis · Keaton named the start  
**Almanac:** no chapter-eight content seat · **128** stays reserved  
**Bench used:** Cursor Cloud root (signs · pushes · lands) — name the Bench

---

## The rung named

Work starts at the **sh → rish seam**.

| Keep | Climb |
| --- | --- |
| `.sh` POSIX entry points | orchestration into `.rish` |
| duty bodies in shared seams | fat shell scripts that only wrap |

This is foundations-first on the **build stack** (e125). It is not Mantra-first. It is not "skip sh because a higher rung waits" — that counsel inversion is refused. Upper layers wait their turn after this seam holds.

## Climb begun — census_control

One fat scan climbs this sitting:

| Piece | Job |
| --- | --- |
| `census_control_h1_seam.sh` | fence-aware H1 counts |
| `census_control_marker_seam.sh` | stamp shape |
| `census_control_tracked_seam.sh` | `git ls-files` inventory |
| `census_control_scan.rish` | preferred orchestration |
| `census_control_scan_drive.sh` | same seams when rishi is absent |
| `census_control_scan.sh` | thin entry · prefers rishi |

Duty logic lives once in the seams. The entry stays thin. Cloud without zig/rye still greens through the drive.

## Approve-all — what it seats and what it does not

| Seated lean | Kept gate |
| --- | --- |
| A — name the start rung | shred · SAFE · geode begin |
| B — climb one (census_control) | seat **128** close choir |

Glow · tend and above wait. Approve-all circles no gate.

## Hard lines kept

Seat **128** reserved · shred **RED** · geode gated · one guard roof · build stack foundations first · roots send words · a Bench measures · name the Bench.

---

*May the floor hold the raise. May orchestration climb without abandoning the seam.*
