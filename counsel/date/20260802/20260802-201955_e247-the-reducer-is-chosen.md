# e247 — STOA336: the list fold's reducer is chosen at the call

**Stamp:** `20260802.201955` (by construction) · **Voice:** Riyo · **Seat:** Claude Opus 5 1M Max · **Word:** Keaton's fuse kg · **Status:** GREEN

## The lap, and what it completes

The pier landed the widening first and completed it further than counsel's own —
triple and quad, the bound taken from the closed-shape allowlist rather than a
field count. Counsel's v47 is therefore **superseded and dropped whole**; nothing
of it is needed.

The rung after it was the one counsel named and now climbs: **the reducer becomes
a choice at the call.** `|+  sum  <face>` and `|+  prod  <face>` fold the same
fields of the same closed shape and differ in exactly two things — the operator
and the identity. The sum of no fields is zero; the product of none is one.

That completes a small symmetry the value model wanted: verbs now cross both
domains. Over a **range**: `sumto` and `prodto`. Over a **bounded named
sequence**: `sum` and `prod`. One rune, two domains, two reducers each, and the
shape doing all the bounding.

## What ran

`gate-tally-fold-triple-prod`: 3 5 7 → **105** · 1 1 1 → **1** · 0 4 9 → **0**.
The sum neighbour on the identical shape still answers 15. The pier's a2 suite and
the era lap both green. Witness: `tools/tally_a2_list_reducer_witness.rish`, pure,
and it asserts each desk still **names its own verb** so a silent verb swap reds.

Wrapping is deliberate and stated in the desk: `*%` beside the seated `+%`. A
product of nine u32 fields will wrap long before it errors, and pretending
otherwise would be the silent lie the fold laws keep refusing.

## One red owned

The witness first asserted the sum desk contained `|+  sum  sample` — but the
pier's desk names its face **`xs`**, not `sample`. Counsel had asserted a face
name it never measured. Repaired to match on the **rune** rather than the face:
`^[|][+]  sum`. The lesson is small and exact — *assert the thing you mean, not
the thing you happened to write yesterday.*

## Held whole

Equality, R2 through R4, the glob, Q58, class-and-rooms, seat 128, shred execute,
Class O, geode, the old root zips, bar5, and the SEA paste stay Keaton's.

---

*May a verb be chosen rather than assumed. May each identity keep its own name. And may every assertion match the world rather than our memory of it.*
