# e231 — the sampler gets a guard, so luck stops being the test

**Stamp:** `20260802.172513` (by construction) · **Voice:** Riyo · **Seat:** Claude Opus 5 1M Max · **Word:** Keaton's fuse kg · **Status:** GREEN

## The pattern behind two rounds of fixes

The walkthrough sampler earned two corrections in two sittings, and both
arrived the same way: **a random draw happened to surface a flaw**. README
appeared at e228, `steep.md` at e229. Each fix was right, and neither was
guarded — the next flaw of the same family would again wait on the draw.

Luck is a poor test, so the sampler now has a witness of its own.
`tools/ad_walk_sample_witness.rish` asserts four things deterministically:
the sampler runs and speaks; **every drawn line is a dated artifact**;
**both facts a verdict needs are present** — the inbound count and the age;
and **the shelved class never surfaces**. A fourth leg proves the guard can
bite, feeding the bare name `steep` to the same dated pattern and asserting
it is refused — because a filter that accepts everything would pass the
first three legs in perfect silence.

This is the vacuum row's law turned on our own instrument: it is not enough
that the sampler ran green today; the properties must be checkable on any
day, whatever the draw happens to produce.

## Also re-cut

The **age** instrument was still in flight — the pier had landed the
dated-only filter but not the age column — so it is re-cut here, and the
witness now depends on it: a sampler that stopped printing age would red at
the second leg rather than quietly returning to one fact per file.

## This round's walkthrough

By the sampler, with both facts: `stoa134-triple-mold-gate` 6 · 13 d ·
`stoa180-at-cord-aura-thin` 8 · 12 d · `modules-aspects-and-mailable-money`
18 · 31 d · `stoa106-nest-type-surface-wording` 11 · 13 d.
**Four KEEPs, zero moves.**

## Held whole

**a2 waits on the word.** R2 through R4 want a word and a check-in. The
equality rune, the glob, Q58, class-and-rooms, seat 128, shred, geode, the
two old root zips, bar5, and the SEA paste stay Keaton's.

---

*May our instruments be judged by the same laws they apply to everything else. May a guard that cannot refuse be no guard at all. And may the flaws that luck once found be caught, from here, on purpose.*
