# e265 — the first Pond ring · one app under policy, green

**Stamp:** `20260802.230432` · **Voice:** Riyo · **Word:** fuse kg · **Status:** GREEN

## The continue's third opening

Quiet lane, measured lean, continue word — the e252 shape spoke for the third time, and the Now re-pointed as the brief leaned: **one Pond ring on the NixOS host, drawn terminal first**. The ring's gap was measured before anything was cut: the app carried headless witness modes and no manifest; customs carried the law and no caller.

## The ring

`tools/pond_ring_drawn_terminal_witness.rish` closes the loop end to end on this metal:

| Leg | Result |
|-----|--------|
| customs builds · app builds (seated fixture) | GREEN |
| good manifest crosses — **place** admits (exit 0) | GREEN |
| admitted app runs its headless Lap 3 session | GREEN (frame signatures spoken) |
| stranger-mark manifest crosses — **refuse** blocks whole (exit 2) | GREEN — the app stays unrun |

Two committed fixtures carry the app's true source sha (`f3e721b1…dd23`); customs reads marks at receipt, and Amphora's hash discipline stays where it lives.

## Reds owned at true cause

The stream law earned its keep twice: customs and the runtime speak through `std.debug.print` — stderr — while the app's witness line rides stdout; five asserts moved to their true streams. Then a finding worth pinning: **this rishi has no `.status` field** — the probe answered `NoSuchField`, so the TAME supplement's `{status, out, err}` spec outran the metal. The refuse leg now tests exit 2 through the seated sh-probe shape (`…; test $? -eq 2`), capture-before-pipes intact.

## Parked

The standing manifest file's home and extension (fixtures carry the ring today) · the ring widening to the other five apps · G1 revive · SixOS study · Nock rest · string/record rows · quarantine · R2–R4 and the hands.

---

*May the first ring hold a person's whole session under policy. May place and refuse both stay loud. May the next five apps find the gate already standing.*
