# e212 — the retting timer, ring one: Linengrow's first shippable stands

**Stamp:** `20260801.172149` (by construction) · **Voice:** Riyo · **Word:** Keaton approved the recommended lanes · **Status:** GREEN

**It runs.** `linengrow/retting_timer.rye` — two commands, one append-only
log, state as a pure fold: `start <name> <days>` and `status`, with READY
rising when the days complete. Bounds named (64 batches · 32-char names ·
365 days · 16 KiB log), every refusal speaks its fault
(`DaysOutOfBound` · `NameTooLong` · `LogFull` · `HomeUnavailable`),
widths in u32/u64, asserts at TAME density. The witness proves the whole
loop: build · start · the day-one fold · an aged plant rising READY · three
named refusals. Simple, lovable, complete at its stated scope — the scope
stated in the file head, including the ring-one seam that the home
directory is made once by hand or witness.

**The toolchain taught three lessons on the way, each now in the source's
shape:** this rye's std speaks the Io idiom — argv arrives through
`std.process.Init`, files through `Io.Dir` with io-threaded calls, and
the wall clock through `Io.Clock.now(.real, io)` — and the first two
build laps failed by writing yesterday's std from memory. Measurement
beats memory in APIs too: amphora's own main was the needle that fixed it.

**Ring two, pier lane:** the Wasm build of this same file, posted to
Codeberg — Keaton's stated shape for the first public artifact.

**Held in the same word, plainly:** the blanket approval opens lanes; it
does not speak worded gates. Q53's class-and-rooms and Q64's seat 128
remain exactly as they were — yours alone, each one line. **Djin page one**
is opened by the word yet parked RTAC for one directive of yours: which of
the twelve, and the first lesson's subject — series shape is not counsel's
to guess. **Trey's frame** follows in the next seat.
