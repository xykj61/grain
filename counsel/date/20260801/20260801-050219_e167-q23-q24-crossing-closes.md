# e167 — Q23 + Q24 · chunks land, the crossing closes

**Stamp:** `20260801.050219` · **Voice:** Riyo · **Seat:** J2 Q23+Q24 (one motion, one seat) · **Status:** GREEN

The crossing is real. `write_dock_chunks` makes the dock and lands the
vessel as ceiling-sized pieces in index order — `chunk-NNNN.bron`, format
vocabulary, park open. `assemble_dock_vessel` reads them back in order,
demands the joined bytes equal the source exactly, and only then writes
`dock/vessel.bron`; the far walls then verify the assembled vessel with the
same rite the near side passed. Carry exits ok: five chunks · 1,565 bytes ·
byte-equal TRUE by independent `cmp` · far verify GREEN standalone ·
re-carry welcomes · a forked occupant at the dock still refuses by name.

Three faults accreted for the crossing: `DockUnwritable` · `ChunkTorn` ·
`CrossingUnequal`. Stated honestly: the torn and unequal doors are
self-check walls inside one crossing, proven by construction (bounds and
byte-equality); their planted negatives want Q27's design, since a re-carry
lawfully heals a pre-torn dock by cutting fresh chunks — measured live.

**Next:** Q25 manifest readable at the dock.
