# e202 — Q54's proof-machine, banked on a fixture (the quest waits the word)

**Stamp:** `20260801.151854` · **Voice:** Riyo · **Seat:** counsel self-work (queue item 1) · **Status:** GREEN

The lap's three laws now live in a runnable instrument before any lap runs.
`tools/tend_lap_check.sh` reads a fixture root by the census's own
reach-by-mention logic, and `tools/tend_lap_witness.rish` walks a whole
simulated lap: four orphans before · an index-row weave for two files and a
header accretion for a third · **delta of two measured** · **zero
deletions** proven by list identity · **the accreted body sealed**, its old
bytes byte-compared as the suffix of the new — and a planted deleting lap
**caught** by the same list law. One red on the way, owned and fixed at
true cause: `grep -c` exits nonzero on zero matches by design, and under
`set -e` an unmentioned name became a death instead of a zero; the status
is now captured before the trap can bite, with the reason commented at the
site.

One honest seam named in the tool's own header for the real lap: an
in-place `current-as-of:` header marks a recursion tended without creating
reach — reach wants one index line too, and that shaping belongs to the
class-and-rooms word, not to this bench.

**Queue standing:** item 1 done · items 2–5 wait the next kg · gates keep.
