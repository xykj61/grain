# e153 — Q9 Kumara stamp inside pour

**Stamp:** `20260801.040828`  
**Voice:** Riyo  
**Seat:** Equinox Amphora CLI wave · J1 Q9  
**Status:** GREEN  
**Bench:** Claude counsel container (Fable 5 1M Max) · basis `1f517496ce` · relay lands by patch

## What landed

After the seal, pour stamps the vessel body: `stamp_sealed_vessel` resolves
`amphora/bin/vessel-core` (env `AMPHORA_VESSEL_CORE` · argv0 sibling · repo
default — the seal resolver's mirror), runs `vessel-core sign` as a
subprocess (path C — no `@import` of `vessel_*.rye`), and confirms a
`stamp_sig` field landed. The rite holds by construction: `stamp_sig` stays
outside the sealed head, so the seal opens after the stamp. Shoulder stays
clear (`amber-ring1-season`, e149 park untouched). Two faults accreted,
named for the fault: `StampToolAbsent` · `StampFailed`. Successful stamp
still `CommandNotGrown` (Q10 write-guard).

## Paths (exit codes accrete)

| argv | exit | note |
| --- | ---: | --- |
| version | 0 | stdout `amphora 20260801.040828` |
| pour \<fixture\> vessel | 2 | sealed · **stamped** · waits Q10 |
| pour · stamp tool absent | 2 | StampToolAbsent |
| vessel-core verify · tampered stamp_sig | 1 | VerifyFailed (module door, G10) |
| vessel-seal open-check · after stamp | 0 | rite holds — seal, then stamp |

## Fixture (fresh)

`tools/fixtures/cellar_ring1_tree` → season `files=4` · sealed
`cargo_bytes=378` · `stamp_sig` landed · `vessel-core verify` GREEN ·
`vessel-seal open-check` GREEN after stamp.

## Laws measured

`cmd_pour` 69 lines (roof 70) · authored `usize` 0 · bans 0 · file 651 lines ·
fn count grown by four (`resolve_core_bin` · `run_vessel_core_sign` ·
`verify_stamp_field` · `stamp_sealed_vessel`), each under the roof, each
asserting its preconditions.

## Owned red (standing, isolated as not-Q9's)

`tools/amphora_bounds_agree.rish` reads RED on this bench — and reads RED
identically on the untouched e152 basis (stash-probe), so the red predates
this seat. The failing arm is the witness's own planted-disagree assert
(line 24: the plant must FAIL, and did not). Q9 touched no coupled number.
Parked for the pier: rerun on Cursor bench; if RED there too, the checker's
disagree door regressed between e146 and now.

## Held

Class W versioned shoulder · Crossing · stdout family · return_deca_128 ·
P7 · seat 128 · Q10 write-guard word.

## Next

Q10 — write vessel · refuse a second pour that would fork identity.
