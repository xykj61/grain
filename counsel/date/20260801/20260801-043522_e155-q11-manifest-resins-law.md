# e155 — Q11 manifest entry under resins law · pour grown

**Stamp:** `20260801.043522`  
**Voice:** Riyo  
**Seat:** Equinox Amphora CLI wave · J1 Q11  
**Status:** GREEN  
**Bench:** Claude counsel container (Fable 5 1M Max) · basis `1f517496ce` + e153 `ef1ffe5971` + e154 `5b66da476f`

## What landed

Pour now writes its manifest and completes. `build_manifest_lines` lists
every enclosed thing — `manifest <mark> <digest> <name>`, one line per cargo
file, the mark now the named constant `mark_plain_bytes` shared with the
cargo builder — and the listing rides in the vessel head, readable without
opening. `vessel_core` accreted the field: manifest lines validate through
`manifest_entry.parse_manifest_line` (the resins-law contract, literally),
carry into canonical bytes, and so sit under the Kumara stamp. With the
manifest, pour is grown: the round path exits ok; success speaks stderr
only, because the stdout result family still awaits Keaton's word. And the
owed split landed: `cmd_pour` is a twelve-line sequencer over `pour_fill`
(parse · read · sort · cargo · manifest · parent · guard) and `pour_ship`
(write · seal · stamp · complete), every function under the roof — including
`parse_vessel`, split at its listed-line seam when the accretion nudged it
past 70.

## Coverage, measured rather than assumed

A flipped manifest digit fails `vessel-core verify` (the stamp catches it)
while `vessel-seal open-check` still opens — the seal guards the cargo, the
stamp guards the whole text, and restore's standing law checks both walls.
The header now states exactly that; an earlier draft claimed AEAD coverage
for the listing and was corrected to the measured truth.

## Paths (exit codes accrete)

| argv | exit | note |
| --- | ---: | --- |
| pour \<fixture\> vessel | **0** | manifest · seal · stamp — pour complete |
| pour same season, same vessel | 0 | same parent — re-pour welcome |
| pour forked season / garbage occupant | 2 | IdentityFork, occupant untouched |
| pour, season with a 201-char name | 2 | **ManifestOverflow** — entry contract bound |
| pour · stamp tool absent | 2 | StampToolAbsent |
| tampered manifest digit → verify | 1 | VerifyFailed — stamp wall |

## Elders stand

`vessel-core selftest` and `vessel-seal selftest` GREEN after the accretion —
elder vessels carry no manifest lines and their canonical bytes are
unchanged, so every standing `stamp_sig` still verifies. Accrete-never-break,
proven rather than promised.

## Owned red (carried)

`amphora_bounds_agree` still RED-here and at the e152 basis — pier rerun parked.

## Held

Class W versioned shoulder · Crossing (carry) · stdout result family ·
return_deca_128 · P7 · seat 128.

## Next

Q12 — pour witness GREEN (round path measured, in the tree's own `.rish`).
