# e192 — Q59 · the stamped manifest beside the bundle, kit fixed on touch

**Stamp:** `20260801.145705` · **Voice:** Riyo · **Seat:** J4 Q59 · **Status:** GREEN

The manifest stands beside the bundle carrying everything the plan promised
— pier, span `root..8da8113d14`, tip, bundle and bound bytes, 163 refs,
and the living-doc limb **re-measured at cut time** (REDS 57 · REMEMBER
55,958 · Lexicon 116,140 · index lines 802) — plus two truths the first
pass exposed and the kit now keeps:

**Kit fix one, the pipeline law inside the kit itself:** `commit_count`
printed blank because rev-list's failure hid behind a `tr` pipe — the very
masking the workshop banned. The count is now captured with its status
before any pipe, `root` joins `--all` as a full-ancestry alias, and the
manifest reads **commit_count 37065**.

**Kit fix two, the promised content address:** the plan's Q59 row named a
sha3 of the bundle bytes; the kit now writes it via the openssl POSIX seam
— `bundle_sha3 80e084e2…a580c5` — and an independent hash on this bench
matches it character for character.

**Next:** Q60 — a clone from the bundle proves whole on a clean worktree.
