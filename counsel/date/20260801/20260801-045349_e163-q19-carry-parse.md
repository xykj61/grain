# e163 — Q19 · carry argument parse

**Stamp:** `20260801.045349` · **Voice:** Riyo · **Seat:** J2 Q19 · **Status:** GREEN

Carry now owns its door. `CarryArgs` names the two operands, `parse_carry_args`
mirrors pour's parse with the same bounds and the same fault names, and the
dispatch routes `carry` to `cmd_carry` while restore keeps its inline waits.
Proven: missing operands, too many words, and the 5000-character path each
refuse by name; a valid pair parses and waits its quest. Walls come Q20.

**Next:** Q20 — verify both walls before motion.
