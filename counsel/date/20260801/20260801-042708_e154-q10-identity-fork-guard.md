# e154 — Q10 write-guard: refuse the pour that forks identity

**Stamp:** `20260801.042708`  
**Voice:** Riyo  
**Seat:** Equinox Amphora CLI wave · J1 Q10  
**Status:** GREEN  
**Bench:** Claude counsel container (Fable 5 1M Max) · basis `1f517496ce` + counsel e153 `ef1ffe5971` · crosses by patch pair

## What landed

Pour now guards its write. `parent_of_cargo` names the pour's identity —
SHA3-256 over the sorted cargo plain — and `guard_identity_fork` reads any
occupant of the vessel path before a byte is written: an occupant carrying
the **same parent** is the same pour said again, and re-pours welcome
(idempotent — seal and stamp refresh); any other occupant — different
parent, no parent line, unreadable — refuses through the new fault-named
door `IdentityFork`, and the occupant stays untouched. The header's oldest
promise, *"Identity refuses a second pour that would fork,"* is now enforced
rather than foretold. Successful pour still `CommandNotGrown` (Q11 manifest).

## Paths (exit codes accrete)

| argv | exit | note |
| --- | ---: | --- |
| pour \<fixture\> vessel (first) | 2 | guard passes empty · sealed · stamped · waits Q11 |
| pour same season, same vessel | 2 | same parent — re-pour welcome, re-sealed, re-stamped |
| pour forked season, same vessel | 2 | **IdentityFork** · occupant untouched (verify + open-check GREEN after) |
| pour onto garbage occupant | 2 | **IdentityFork** — continuity unconfirmed, nothing clobbered |
| pour · stamp tool absent | 2 | StampToolAbsent (Q9 door still holds) |

## Laws measured

`cmd_pour` **70/70** — at the roof, not past it; **Q11 owes the split** at its
own accretion (name the seam: pour front half parse-read-guard, back half
seal-stamp-manifest). `guard_identity_fork` 22 lines · `parent_of_cargo` 8 ·
authored `usize` 0 · bans 0 · file 689 lines · every new fn asserts its
preconditions · error-set switch at the readFile seam, never `== error.`.

## Owned red (carried from e153)

`tools/amphora_bounds_agree.rish` still RED-here and RED at the e152 basis
(stash-probe, e153 seat) — predates both seats. Pier rerun stays parked.

## Held

Class W versioned shoulder · Crossing · stdout family · return_deca_128 ·
P7 · seat 128 · Q11 manifest word.

## Next

Q11 — manifest entry under resins law · split cmd_pour at its named seam.
