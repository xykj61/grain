# e158 — Q14 seventy-line + assertion-density audit

**Stamp:** `20260801.044747` · **Voice:** Riyo · **Seat:** J1 Q14 · **Status:** GREEN

**Seventy-line ledger:** clear — no function over the roof anywhere in
`amphora/`, measured across all ten `.rye` files after the Q11 splits.

**Bans suite:** `tools/tame_style_check.rish` GREEN, repo-wide, on this bench.

**Assertion density (file level, measured):** `src/main.rye` 45 asserts
across 23 fns — the two-per-function average met where the CLI lives.
Module files vary: `manifest_entry` 8/4 and `tally_copy` 6/3 strong;
`vessel_core` 10/20 and `vessel_seal` 3/19 thin; `kumara` 0/6 — a thin seam
wrapper whose calls resolve upstream, named here for on-touch densifying
rather than swept today. A per-function census was attempted and set aside:
the textual instrument misreads multi-line signatures, so only the
file-level counts are stated as measured.

**Next:** Q15 width pass.
