# e206 — five vacuous guards in the enclosure witness, found and given teeth

**Stamp:** `20260801.160208` · **Voice:** Riyo · **Seat:** counsel self-work (queue refilled from the tree's own debt) · **Status:** GREEN — with a finding the pier must hear

**The finding, loud:** the e205 red generalized into an advisory, and the
advisory's first sweep found **thirty-seven backslash-quote shapes inside
rishi strings** — and five of them were the custody guards of
`pond_enclosure_sixbar.rish`, the ai-jail's own isolation witness. Rishi's
law is that backslash escapes nothing, so those `\"` shapes reached sh as
literal quote characters, and every one of the five guards passed
**vacuously**: the elder form claims `/etc/hostname` is absent on a bench
where it exists, and a key planted under a fake HOME sails straight
through it — both proven live, side by side, elder rc=0 against fixed
rc=1. The bars that were supposed to prove *host SSH key unreachable ·
cloud lane key absent · host Documents refused · Wayland lane present*
were proving nothing at all.

**The teeth:** all five guards recut quote-free — `test x$VAR != x` for the
env bar, unquoted `$HOME` paths for the four custody bars (no spaces on
these benches; the assumption and the vacuity proof stand commented at the
site). The planted-key demonstration shows the fixed form bites where the
elder form slept.

**For the pier at wake, marked urgent-ordinary:** rerun
`pond_enclosure_sixbar.rish` on the Framework after landing. With teeth,
bars five and six will test the jail for the first time in their lives —
and if the host key is actually reachable from inside, the witness will
finally say so. A green there is now worth something; a red there is a
gift.

**Queue refilled honestly:** **8. backslash-site triage** — the remaining
~thirty-two advisory sites, classified harmless-expansion or
guard-with-teeth-needed, each with a reason; the two stoa grep-pattern
sites want the most care.
