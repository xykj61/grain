# e145 — bans false positive fixed · Q4 named bounds · REDS 52

**Stamp:** `20260801.031219`  
**Seat:** e145 · Amphora CLI wave **Q4 of 64** · parent Equinox OPEN  
**Voice:** Riyo (Radiant · TAME) · Nested documentary: Trey  
**Bench:** Cursor Cloud Grok 4.5 · living nib at start `929315fba8`  
**Word:** Keaton — `kg` · fuse Claude Opus 5 (G5/G6 · P5–P7 · Q4 bounds)  
**Status:** GREEN — bans instrument fixed · planted pair · Q4 bounds named · REDS 52 · 128 RESERVED

*Written together by Keaton and Riyo from Claude's propose-never-seat reply.*

---

## Fuse

Counsel's container was still on elder `e983f5a869` (e143). Living `main` already carried e144 (Q3 on stdout · J1 sixteen · opening-lines). This sitting rules the bans false positive before anything else widens that scan, then lands Q4 named bounds.

## G5 — standing red was a false positive (verified)

| Fact | Standing |
| --- | --- |
| Line | `brushstroke/brush_parse.rye:384` — `///` teaching the seam, naming `) == error.` |
| Census | one hit on the widened roster · **all** on a comment line · non-comment hits **0** |
| Control | counsel: e140 worktree same exit 1 · `brush_parse` byte-unchanged · only roster line changed |
| Verdict | corpus clean · instrument was not · pre-dates Amphora |

## P5 + P6 — fix the instrument, planted pair

| Piece | Path |
| --- | --- |
| Helper | `tools/fixtures/tame_style_ban_noncomment_files.sh` — `grep -HnF` · skip `//` lines · always `-H` |
| Bans wire | `) == error.` / `) != error.` use the helper |
| Positive plant | `tools/fixtures/tame_style_ban_err_eq_code.rye` — must fire |
| Negative plant | `tools/fixtures/tame_style_ban_err_eq_comment.rye` — must not |
| Selftest | `tools/tame_style_ban_err_eq_comment_selftest.rish` — **GREEN** |
| Living bans | `tame_style_scan_bans.rish` — **GREEN** |

`brush_parse.rye:384` kept verbatim — the lesson stays.

## On-touch discovery (masked by the standing red)

Once comment-skip let bans finish, `tools/fixtures/dated_classify.py` (e116 one dated definition · REDS 40) failed the tools/*.py zero gate — seated under `tools/` without a named exempt. Exempt named with reason; not a deletion. Python-at-seam law already required this entry.

## P7 — gate clothes (named · not suite-rewritten)

`tame_style_scan.rish bans` is still outside the seated ten. This sitting **ran** it and greens it. Seating it into the instrument-suite lap remains available; until then: it is blocking **when invoked**, and this wave's round runs it. A gate that never runs wants a lap or an advisory label — never both unspoken.

## Q4 — named bounds (header accreted)

| Constant | Value | Why |
| --- | --- | --- |
| `max_words` | `u8 = 4` | program · command · two operands |
| `max_vessel_path_len` | `u16 = 4096` | season / vessel / dock path text |
| `max_cargo_bytes` | `u32 = 1024` | aligns `max_resin_bytes` / `max_seal_plain` |
| `max_chunk_datagram` | `u16 = 340` | aligns `max_wire_payload` |

Build + four paths still GREEN · `V=$(amphora version)` captures · bans still one comment hit under raw grep, zero under noncomment helper.

## Q3 already landed (e144)

Counsel re-delivered Q3 against e143 seed. Living e144 already held metal; this sitting does not rewrite it. Downloads path absent on Cloud.

## What this does *not* do

| Held | Why |
| --- | --- |
| Rewrite brush_parse teaching comment | corpus was right |
| Seat bans into the ten-witness suite | P7 word still open |
| Seat family stdout law / Crossing / 128 | Keaton alone |
| Q5 error set names | next kg |
| Touch Mantra | on-touch never campaign |

## REDS 52

Look for the thing inside the tool too. Fix the instrument, never a correct corpus. A blocking check no lap runs is documentation wearing a gate's clothes.

---

*May every teaching comment keep teaching. May every gate that claims to block also run.*
