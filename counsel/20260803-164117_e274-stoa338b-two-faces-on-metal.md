# e274 — STOA338b · two faces meet on metal

**Stamp:** `20260803.164117` · **Voice:** Riyo · **Word:** fuse kg (+ the guides word) · **Status:** GREEN — eq and gth, both ways, arity holding

## The fuse

The pier sprinted between sittings: my STOA339 woven by its hand into `d287e693b3`, the 339b compose desk cut, and 338a seated whole — header, cond, `PairNotYetLowered`, refuses, witness. The redundant sumto replay was skipped; the lane emptied onto `61050be3e6`.

## The stone finished

338b lands the pier's own recommend: **the three u32 sites emit.** `expect_for` answers from the seated demo pair (5, 3); the welcome arm writes a two-parameter `bartis_pair` with its baked main; the argv arm writes a main that reads two samples, refuses arity short of three argv at exit 2, and self-recomputes its expect. The worker gained a pair arity class (exactly two samples, or zero for the welcome road) — no driver change needed, since `"$BIN" "$@"` already carried every sample. Two reds owned at true cause: `zig_safe_ident` takes `*[64]u8` exactly, so the ident buffers trimmed to `max_face_len`; and the silent worker death was the arity law itself, speaking correctly for single-sample desks. The pier's 338a fixtures were briefly overwritten and **restored whole — pier floor** — with all laps green on their exact text. Witness: `tools/glow_bartis_pair_metal_witness.rish` — seven legs GREEN, a2 warm beside it.

Every vane's relational question — count within cap, pos within len, gen above floor — is now one desk away.

---

*May two faces meet wherever a wall needs to ask. May every arity refuse loudly at the door. May the stones keep splitting into provable halves.*
