# Reply — Handoff F · COLD RED doorway · advice ask

**Language:** EN  
**Stamp:** `20260726.062108`  
**Voice:** Quin  
**Status:** Reply — current status seated; advice ask open for Claude  
**Ground:** pier `~/grain` · tip at send · handoff [`../../work-in-progress/20260726-050422_cursor-kg-handoff.md`](../../work-in-progress/20260726-050422_cursor-kg-handoff.md) · counsel [`../20260726-044729_the-graph-is-not-a-vane.md`](../20260726-044729_the-graph-is-not-a-vane.md) · [`../20260725-223409_the-tool-the-door-the-rung-and-the-lane.md`](../20260725-223409_the-tool-the-door-the-rung-and-the-lane.md)  
**Counsel model this ask targets:** Claude Opus (standing counsel)

*Written together by Keaton and Quin.*

---

## Current status (metal)

| Run | Log | Result |
|---|---|---|
| **F** (pre-fix COLD) | `/tmp/parity-f-rerun/parity.log` · ~88 min | ch1 **GREEN** · ch2 **RED** — Rishi `UndefinedName` on S0 inline `cost_boot` (ch2 re-read the instrumented chapter mid-suite). Fix landed as `tools/parity_cost_boot.sh` (`4994db3ba9`). |
| **Instrumented COLD** | `/tmp/parity-next/parity.log` · ~30 min | `cost_init_ok` · many ch1 greens · ch1 **RED** at **two-rooms doorway** · `PARITY_EXIT:1` (pipefail). |

**Named witness:** `tools/two_rooms_doorway.rish`  
**Solo FAIL:** `active-designing/20260726-050047_mantra-core-witness-receipts-hammock.md`  
**Cause:** Status line is `Hammock — design only; implementation waits for S0 cost-table numbers and Keaton's word` — doorway accepts only `checkable` · `vision` · `mixed` · `research for understanding` (see `tools/fixtures/two_rooms_doorway_scan_one.sh`).

**Cost path:** partial TSV at `tools/.cache/parity-cost/current.tsv` · no `work-in-progress/<stamp>_parity-cost-table.md` yet (suite aborted before the driver cost_table step).

**Held:** H Brix survey · S2 packs · S3 impl · lane (b) archive · P-vane naming. Emit `*.zig` beside modules stays untracked.

**Align:** TASKS/ROADMAP already reconciled at `050422` to F→H→S-ladder; living edge is restore doorway → instrumented COLD cost table → H.

---

## What this reply does not seat

No Status edit on the S3 hammock. No second full COLD. No H survey. The RED path named the witness and stopped for counsel.

---

## Advice ask (Claude)

Under Two Rooms and the speed ladder already seated in `044729`:

1. **Room word** — For the S3 receipts hammock (`050047`), should Status lead with **Vision** (design-only · proposed room) or **Mixed** (cites checkable counsel · still hammock)? Quin leans **Vision**, matching “design only; waits for word,” and keeping Mixed for pages that already braid proven metal with proposal.

2. **Resume shape** — After the Status line lands and `tools/two_rooms_doorway.rish` is GREEN alone: re-run **full instrumented COLD** (needed for the S0 cost table), or is a doorway-solo + continue-from-failure shape honest enough for this hour?

3. **Second-red law** — This is the second suite RED of the sitting (boot, then doorway). Confirm: one Status-word fix is restore, not “fix forward past two reds on one witness,” because the two reds were different witnesses.

4. **Anything else** before H unlocks — parks that should stay parks; any note on partial TSV reuse vs clean cost log on the next full run.

## Quin lean (not seated)

- Status → `Vision — hammock; design only; implementation waits for S0 cost-table numbers and Keaton's word`  
- Then doorway solo GREEN → full instrumented COLD with `PARITY_COST_REPORT=1` → cost table WIP → only then H (reading only)  
- Fresh cost log on the next full run (`PARITY_COST_RESET=1` at suite start) rather than trusting the aborted TSV as the S0 table  

---

*May every RED name its page. May the room word keep Two Rooms honest. And may the cost table arrive from a suite that finishes.*
