# Surface Season p5 — Glow Connector Seam Sketch

**Stamp:** `20260728.195456` · **Voice:** Quin · **Season:** Surface · **Scope:** sept · **Round:** p5  
**Prior:** [p4 Wayland study ledger](20260728-195228_surface-season-p4-river-zig-wayland-study-ledger.md)  
**Ask:** waymark p4 complete / p5 next · kg Glow connector seam sketch  
**Kind:** design sketch · no new backend file · no invent stack · no metal claim

## Verdict

**p5 LANDED as seam sketch.** The Glow → Frame → Wayland hop is named in one page. Authorship stays Glow/Rye values; presentation stays Brushstroke `Frame`; host hop stays `libwayland-client` on Ubuntu GNOME Wayland. No JS/HTML/CSS. No new `backend_wayland.rye` this stamp — the living seed already proves the hop.

## The connector (five named hops)

```text
① Glow / src userland     values · gates · vegan-dogfood desks
        │
② Mantra · Weave · Tilak  facts · history · type-marks  (graph under surface)
        │
③ render(state) → Frame   pure fold · lines/cells · Tally-bounded
        │
④ Brushstroke present     Frame → Skate grid → SHM buffer / pixels
        │
⑤ Wayland host hop        wl_surface · xdg_toplevel · commit · frame callback
                          (libwayland-client · GNOME Mutter today)
```

| Hop | Living today | Sketch lean |
| --- | --- | --- |
| ① | `src/lib/gate-surface-double-u32.glow` · `glow/` · hygiene pin | Grow vegan-dogfood; O3 gated |
| ② | Mantra/Weave modules · Tilak design | DAG paper at p8 |
| ③ | `Frame` in `brushstroke/wayland_seed.rye` · stdout seed | Keep `render → Frame` pure |
| ④ | Skate grid · SHM fill in wayland_seed | One Pond-framed frame at p6–p7 |
| ⑤ | `-lwayland-client` · metalsmoke GREEN | Glow never imports Wayland C |

## Seam law (seated)

1. **Glow does not speak Wayland.** Desks produce values; they do not `@cImport` protocol headers.  
2. **Frame is the only glass contract.** Anything that wants pixels crosses `Frame` (or a thin named supersession later).  
3. **Backend is compile-time.** Elder lean (`backend.rye` / `Backend.draw(frame)`) stays the shape — hosted Wayland now; sovereign later; **no Wasm/Web backend** for Realidream / `.brush`.  
4. **Pond wraps the app, not the protocol.** Enclosure/customs admit the process; compositor stays host Mutter until a sovereign compositor earns its day.  
5. **zig-wayland** remains optional MIT study for Zig binding style — not required to name this seam.  
6. **River** remains GPLv3 design teacher for compositor↔WM split — not a hop in this connector.

## What “Glow connector” means (and does not)

| Means | Does not mean |
| --- | --- |
| A named path from Glow values to a hosted window | A new Glow rune that opens windows |
| Documented hops + witnesses per hop | Merging glow/ into brushstroke/ |
| Reuse `wayland_seed` + Pond metalsmoke | Inventing Vulkan/GPU in p5 |
| `.brush` later describes surfaces in Glow/Bron | HTML · CSS · JSON · `src/web` |

## Minimal next proofs (charted)

| Round | Proof |
| --- | --- |
| **p6–p7** | One Pond-framed Brushstroke frame from values (reuse seed / metalsmoke) |
| **p8** | Thin Mantra/Weave “one graph” paper tying hop ② to redraw |

## Thin witness

`tools/gen/season/glow_connector_seam_witness.rish` — asserts this sketch + Frame-bearing seed + vegan-dogfood desk exist (orientation GREEN).

## What this round does *not* do

No new Rye backend file · no metal window claim · no O3 · no River/zig-wayland vendor · no shred · no OS infusion · no `src/web`.

---

*p5 · stamp `20260728.195456` · Quin · seam named · hops held*
