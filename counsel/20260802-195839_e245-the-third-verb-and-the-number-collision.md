# e245 — the third verb, and a STOA number that collided

**Stamp:** `20260802.195839` (by construction) · **Voice:** Riyo · **Seat:** Claude Opus 5 1M Max · **Word:** Keaton's fuse kg · **Status:** GREEN

## Two benches reached the same rung by different roads

Counsel's lap two proposed making the fold **verb a name**; the pier, in the same
hours, generalized the dispatch and added `sum` — a fold over a **pair's fields**,
which is the list-fold rung counsel had named as the one after next. Both landed
on the same insight from opposite ends: *the reduction must be chosen, not built
in.* The pier's tree already reads the verb from `op` and branches, so counsel's
work reduces to one clean accretion rather than a redesign.

**A number collided.** Both benches wrote **STOA333** — the pier for the pair
fold, counsel for the second verb. The pier's landed first, so **counsel renumbers
to STOA334** without argument, and the record shows the collision rather than
hiding it. A STOA number is a name, and names are first-come in this house.

## What landed here

`prodto` joins `sumto` and `sum` as the third fold verb: 0 folds to the product
identity **1**, 3 to 6, 8 to 40320, and past the wall to 0. The bound is refused
above twelve **by name** — `FoldBoundTooLarge` — because 13! leaves u32; the plant
proves it bites rather than wrapping.

The two-verb lesson holds and is now visible in three: **each reduction carries its
own identity**, and a single hardcoded fold would never have surfaced it. The pier's
`sum` sharpens the same point from the list side — a fold over fields has no range
to be empty, so its identity question lives elsewhere.

Green beside it: the pier's own a2 suite, the era lap, and `sumto` unchanged.

## The re-application, and the discipline that made it cheap

Counsel's earlier prodto lane was cut against a tree that no longer exists. Rather
than merge conflict hunks in a compiler, the lane was **abandoned and re-applied
fresh onto the pier's version** — verb arm, expect, emitter, two block-form switch
arms copied from the file's own text. Four files touched, no conflict resolved,
nothing of the pier's overwritten. **A compiler is a bad place to merge and a fine
place to re-apply.**

## Held whole

The equality rune, R2 through R4, the glob, Q58, class-and-rooms, seat 128, shred
execute, Class O, geode, the old root zips, bar5, and the SEA paste stay Keaton's.

---

*May two roads to one rung be counted as agreement rather than collision. May the elder name keep its number. And may every reduction we add remember the identity it was born with.*
