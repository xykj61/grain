# e144 — Q3 `amphora version` on stdout · J1 Q4–Q16 in the tree

**Stamp:** `20260801.030237`  
**Seat:** e144 · Amphora CLI wave **Q3 of 64** · parent Equinox OPEN  
**Voice:** Riyo (Radiant · TAME) · Nested documentary: Trey  
**Bench:** Cursor Cloud Grok 4.5 · living nib at start `e983f5a869`  
**Word:** Keaton — `kg` · fuse Claude Opus 5 (Q3 metal · G1 stream · G2 J1 sixteen)  
**Status:** GREEN — version on stdout · J1 sixteen written · Mantra untouched · 128 RESERVED

*Written together by Keaton and Riyo from Claude's propose-never-seat reply.*

---

## Fuse

Counsel's container was still on elder `5ffbb9d4f8` (e142). Living `main` already carried e143 (Q2 seed · bans+advise see amphora · baseline census). This sitting lands Q3 metal and writes J1 Q4–Q16 into the charter so thirteen quests no longer live only in a chat window.

## Q3 — built, run, measured (this Bench)

| Path | exit | stdout | stderr |
| --- | --- | --- | --- |
| `amphora version` | **0** | `amphora 20260731.230237` | empty |
| `amphora` | **2** | empty | usage |
| `amphora bogus` | **2** | empty | names command + usage |
| `amphora a b c d e` | **2** | empty | `at most 4 words, 6 given` |
| `V=$(amphora version)` | **0** | capture = version string | — |

| Lint | Value |
| --- | --- |
| Lines | **117** · over-100 **0** · tabs **0** · trailing-ws **0** · authored usize **0** |
| Head | `const assert` · `const print` bound · no qualified `std.debug.assert(` / `std.debug.print(` |
| Bans | usingnamespace · !comptime · Self=@This() · @memcpy · Ed25519 · parseInt · copyForwards/Backwards — **zero** |
| fn lengths | main **33** · cmd_version **15** · print_usage **12** (limit 70) |
| asserts | **5** across 3 fns — main 3 · cmd_version 2 · print_usage 0 |

Version constant uses America/New_York `20260731.230237` (counsel's `20260731.225728` superseded by this Bench).

## G1 — stream seam (Amphora's half)

Bare `print` → `std.debug.print` → stderr always. First cut put the version where `$(…)` cannot read it. Control: Mantra does the same — **inherited, not new**. Amphora's result path uses `std.Io.File.stdout()` (the seam `say` uses). Diagnostics stay on bare `print`. **Mantra untouched** — on-touch, never a campaign.

**Proposed family law (Keaton's word):** a command's RESULT goes to stdout; DIAGNOSTICS go to stderr. A result on stderr is a result no caller can read.

## G2 — J1's sixteen in the tree

Landed charter's fine table now names Q1–Q16 with duty lines (pour split Q6–Q11). Duty file points at the living bow. What crosses is enough to make the file again.

## G3 — roster (already closed e143)

Counsel re-raised F5 against elder e142. Living e143 already added `amphora` to bans + advise. This sitting adds `amphora/src/main.rye` to opening-lines (P2 — head now speaks).

## G4 — living-pin thinned (named, not campaigned)

ROADMAP/TASKS stay thin; seated laws survive in dated artifacts. The living-pin guard proves not-emptied, never not-thinned. A green is a claim about the instrument's view. No ROADMAP restore campaign this sitting.

## Q2 roofs

README HTML comment twin **retired by accretion**. Markdown table remains the surface roof; `src/main.rye` is the canonical roof.

## What this does *not* do

| Held | Why |
| --- | --- |
| Seat family stdout law | Keaton alone |
| Touch Mantra stream | on-touch · never campaign |
| Pour / carry / restore metal | Q6+ |
| Seat Crossing / handback / 128 | Keaton alone |
| Elder Amphora over-100 sweep | census stands |

---

*May every free version be readable by a caller. May J1's sixteen outlive the window.*
