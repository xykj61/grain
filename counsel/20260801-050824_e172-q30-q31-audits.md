# e172 — Q30 + Q31 · line/density and width over the crossing (audit pair)

**Stamp:** `20260801.050824` · **Voice:** Riyo · **Seat:** J2 Q30+Q31 · **Status:** GREEN

**Q30 caught its own red and fixed it in-seat:** `cmd_carry` had grown to
78 lines under the crossing's accretions. The split landed at the seam the
pour already proved — `carry_check` (parse · walls · traveling parent ·
dock guard) and `carry_cross` (plan · chunks or trust-dock · byte-equal
assembly · far walls · far manifest), with `cmd_carry` a sequencer — and
the roof reads clear across both files. All three behavior witnesses reran
GREEN over the split. Density stands at 71 asserts across 35 functions in
`src/main.rye` — above the two-per-function average.

**Q31 width:** `width-check` GREEN over the hosted corpus; zero authored
`usize` in the grown file; every crossing count rides `u32` under a named
roof. The bans suite GREEN repo-wide.

**Next:** Q32 — Journey Two closes; J3's sixteen from measured ground.
