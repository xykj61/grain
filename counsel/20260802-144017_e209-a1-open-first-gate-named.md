# e209 — Ashvini a1 OPENS: the first gate named, and it names the compiler

**Stamp:** `20260802.144017` (by construction) · **Voice:** Riyo · **Word:** Keaton's kg after a0 closed and the pier's check-in · **Status:** GREEN (finding + work order · no surgery this sitting)

**The charter's letter is met:** a0 closed on the pier; the charter seats
a1 on Keaton's kg; the kg came. a1 is OPEN — and its first honest work
item turned out to live one tier deeper than expected. The garden-bound
gate stands written (`src/lib/gate-tally-garden-bound-u32.glow`: answer 1
within the wall of eight, 0 past it) and the worker's stem law now admits
it — but the sampled bartis grammar holds only identity and call bodies
today (STOA100–143 closed the molds), so the `?:` body refuses as
**MalformedBody by grammar, not by bug**. The conditional lowerer itself
exists (`lower_line_welcome` proves `?:` lowers); the seam is that
`rune_bartis.parse_source` never routes body lines to it.

**a1 work order one, scoped tight (STOA-next in the file's own style):**
teach the sampled bartis a third body form — `?:  (gth sample <lit>)
<lit>  <lit>` — parse in rune_bartis, emit in the argv path, witness both
sides at the lowerer (accept + malformed plants) and at the gate (3→1 ·
9→0). The desk above is the acceptance target, already carrying its
standing in its own header. Surgery waits a fresh sitting or the pier's
hand — compiler cuts deserve full energy, and TAME orders safety first.

**Bar5, ruled (custody — Keaton's hand alone):** the red is the signal
working. The jail keyring wrapper answers, and the sandbox secret
(`DBF8 5343 7A93 7B4E 36B9 3611 D949 807A C26B 2B99`) is not provisioned
in it. **Restore the sandbox signing secret into `.gnupg-rye` by your own
key ritual** — master stays cold and out, exactly one `sec:` remains —
then bar5 wants green and bar6 gets its first look. Counsel touches no
key material and writes no key commands.

**Also this fuse:** refused onto `edc40da6be`; the pier's in-place
ontology weave stands as the seated text (same recommendation, better
home — counsel's appended section withdrawn in its favor); the e208 seat
testimony restored beside it; e214 corrections stand in the pier's landed
migration.
