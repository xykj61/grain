# e280 — the compass set · STOA341 GREEN · the walls answer with what they hold

**Stamp:** `20260803.191415` · **Voice:** Riyo · **Word:** fuse kg — approve best path · align by compass · pursue through the vanes · **Status:** GREEN

## The word, honored and scoped

The rune grant renews, aimed through Tally · Caravan · Mantra · Weave · Mycelium · Murr · Realidream, best path pre-approved. Scoped as before and said again: **Mycelium stays at MUR M4, Murr's ladder stands, no chain object or custody motion flows from counsel** — the vanes are targets the runes grow toward.

## The measurement, and the stone

The census read thirty-five vane desks: eighteen conditionals, **every arm a memorized literal**. So the compass brief ([`active-designing/20260803-191415_the-vane-rune-compass.md`](../active-designing/20260803-191415_the-vane-rune-compass.md)) set the queue and the first stone cut whole: **STOA341 — face-valued arms.** `then_face`/`else_face` join the spec; the pair-arm parse accepts a face where a decimal sat (strangers → `BodyFaceMismatch`); `pair_arm_value` selects the demo answer and `pair_arm_expr` writes the emitted one, at both emit sites and the argv self-check. **Min and max are now one desk each** — `?: (gth a b) a b` — with seven laps green: both fixtures, argv three ways, the eq family unbroken, the stranger arm refused. One red owned at true cause: `expect_for` hands the spec by pointer, so the helper takes `*const`. a2 stays warm.

## Parked

V2 (real vane unaries) next under the grant · V3 word-gated · the walks · walls' EXPLICIT word · the standing queue.

---

*May the compass and the grant pull the same direction. May min and max be the first of many one-desk answers. May every vane wall trade its memorized number for a question it can finally ask.*
