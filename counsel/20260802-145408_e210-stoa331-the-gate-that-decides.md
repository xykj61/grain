# e210 — STOA331: the sampled bartis learns to decide, and the a1 gate goes green

**Stamp:** `20260802.145408` (by construction) · **Voice:** Riyo · **Word:** Keaton's fuse kg (the fresh sitting e209 promised) · **Status:** GREEN

**The surgery, exactly as ordered in e209:** the sampled bartis carries a
third body form — `?:  (gth <face> <dec>)  <dec>  <dec>` — parsed in
`rune_bartis` (new `.cond_gth` body · three literal fields on GateSpec ·
a house-law decimal reader refusing empty, leading zero, and overflow) and
emitted in both lower paths: welcome bakes sample five and decides at
lower time; **argv speaks its answer** (PLEAC 1.2's speaking main), so
EXIT:0 plus the spoken digit prove the gate. The compiler walked the
accretion home: nine shape-guard switches gained the `.cond_gth` refuse
arm under kind/xact/xfer/tuple samples — conditional bodies nest only
under plain `@u32` today.

**Both sides witnessed** (`tools/tally_glow_tend_a1_gate_bound_witness.rish`):
within the wall, 3 answers **1**; past it, 9 answers **0**; the
missing-arm plant refuses by name (**MalformedBody**, read from stderr per
the stream law); the pier's dec neighbor, PLEAC ch01, and
lower_bartis_witness all rerun green. Two reds owned on the way: the
plant first refused at the *stem* wall (vacuous-adjacent — renamed behind
an allowlisted stem so it reaches the grammar), and the refusal was
asserted on stdout when the runtime speaks on stderr — the tree's own
stream-discipline law, met again in the flesh.

**The a1 ladder now holds two gates that do work:** the pier's
`gate-tally-dec-u32` (call body, EXIT-proof) and this bench's
`gate-tally-garden-bound-u32` (cond body, both-sides-proof) — the twins'
first gates standing side by side, one grammar richer than yesterday.

**The Vultr key (Keaton's ask, ruled):** a dedicated ed25519 SSH key for
VPS auth is exactly right — and **its birthplace must be your metal, not
this container.** Two reasons, both hard: this bench resets between
sittings, so a key born here dies or must *travel as private material
through the outputs channel* — inverting PUBKEYS.md's whole trust shape
("the private half stays home"). The ritual for your hand on the pier:

```
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_vultr -C "xykj61-vultr-$(date +%Y%m%d)"
chmod 600 ~/.ssh/id_ed25519_vultr
ssh-keygen -lf ~/.ssh/id_ed25519_vultr.pub   # fingerprint for PUBKEYS.md
```

Then paste `~/.ssh/id_ed25519_vultr.pub` into Vultr's SSH-keys panel at
deploy (SEA region, your hands), add a `Host vultr-sea` block to
`~/.ssh/config` pointing `IdentityFile ~/.ssh/id_ed25519_vultr`, and
accrete a fourth key section to PUBKEYS.md with the fingerprint —
counsel drafts that section the moment the fingerprint exists. No key
material was generated or touched on this bench.
