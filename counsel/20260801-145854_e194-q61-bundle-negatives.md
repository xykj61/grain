# e194 — Q61 · bundle negatives, and a finding that upgrades the doctrine

**Stamp:** `20260801.145854` · **Voice:** Riyo · **Seat:** J4 Q61 · **Status:** GREEN (after one owned red)

**The finding first, because it matters beyond this quest:** a torn bundle
— one hundred megabytes cut off the tail — **passed `git bundle verify`**.
Verify reads the header and prerequisites, never the whole pack. So the
doctrine upgrades in writing: *verify is necessary; the manifest's content
address is the load-bearing wall.* The torn bytes fail against Q59's
`bundle_sha3` (proven live: digests differ), and a clone from them refuses
at `fatal: early EOF`, exit 128 — the pack itself is the second wall.

**All three plants now refuse in a standing witness**,
`tools/equinox_bundle_negative_witness.rish`: torn-vs-sha3 refused ·
clone-from-torn refused · span bundle in a repo missing its basis refused
(`Repository lacks these prerequisite commits`). One red on the way, owned:
the first cut of the witness used shell brace expansions inside rishi
strings — the interpolator owns that shape — recut clean, and a leftover
dead assignment from the fix was itself removed before sealing, since a
judge who ambers dead code does not get to seal his own.

**Next:** Q62 — audits over the growth, and the two second-notice ambers
come due.
