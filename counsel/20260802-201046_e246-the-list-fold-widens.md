# e246 — the list fold widens: pair through nona, under one rune

**Stamp:** `20260802.201046` (by construction) · **Voice:** Riyo · **Seat:** Claude Opus 5 1M Max · **Word:** Keaton — *fuse kg with completing of recommendations* · **Status:** GREEN

## The rung both benches named, now climbed

Counsel's standing recommendation and the pier's own lean agreed: the honest next
step was **not a fourth verb** but a fold over a **bounded list** wider than two.
Keaton's word completes it, and **STOA335** climbs it.

`|+  sum  <face>` no longer means *the pair's two fields*. It now folds **every
field of any closed tuple — pair through nona** — with the shape doing the
bounding. The rune did not change; its reach did. That is the value model arriving
as a running simple: **a value reduced from a bounded sequence of named parts**,
which is Mantra's Weave shape one rung wider.

`gate-mantra-fold-triple-fields` proves it: three fields fold to fifteen, an
all-zero triple folds to zero, and the shape name in the desk is what sets the
width.

## The pair path was delegated, not rewritten

The widened emitters carry one deliberate line at the top: **at two fields they
call the pier's own pair emitters.** The seated bytes stay the authority for the
case they already served, the pair witness keeps passing against the same code it
always did, and the widening adds reach without touching what worked. The witness
proves the delegation rather than assuming it — 3 5 still folds to 8 through the
elder path.

This is the pointer lesson from e241 applied inside a compiler: **widen by
delegating to what stands, rather than by replacing it.**

## What ran

Triple fold green on both cases · pair fold green through the elder emitter ·
`sumto` and `prodto` unchanged · the pier's a2 suite green · the era lap green.
Witness: `tools/mantra_a2_list_fold_witness.rish`, pure. Worker stem accreted
onto the living file, three field decimals, beside the pier's own triple stems.

## Where this leaves the hardest-solvable

The value model now has: a decision (a1), a reduction over a range (sumto,
prodto), and **a reduction over a bounded named sequence (sum, pair…nona)**.
What remains before the Nock seam is worth deliberating from a mature whole:
a fold whose **reducer is chosen at the call** rather than named in the rune, and
equality — which is Keaton's word, since the remaining pedestal numbers are exact
rather than bounded.

## Held whole

The equality rune, R2 through R4, the glob, Q58, class-and-rooms, seat 128, shred
execute, Class O, geode, the old root zips, bar5, and the SEA paste stay Keaton's.

---

*May a rune widen without changing its name. May the part that already worked keep doing the work. And may the value model arrive one honest rung at a time.*
