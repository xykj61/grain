# e159 — Q15 width pass

**Stamp:** `20260801.044834` · **Voice:** Riyo · **Seat:** J1 Q15 · **Status:** GREEN

`tools/width-check.rish` GREEN on this bench over the hosted corpus, and
the amphora-scoped grep finds zero authored `usize` across all ten files —
every count and index rides a fixed width, with casts only at the sanctioned
seams. Wire bounds in `vessel_fetch_wire` are named fixed-width ceilings
(`u16`/`u32`), the honest widths for quantities that fit them; nothing in
amphora currently persists a size that wants `u64`, and the policy stands
ready for the first one that does.

**Next:** Q16 journey close — suite re-cut · J2's sixteen from measured ground.
