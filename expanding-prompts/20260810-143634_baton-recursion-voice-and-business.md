# Baton — the Recursion Lap (voice molt + business memos)

**Language:** EN
**Stamp:** `20260810.143634` (2026-08-10 EDT)
**Voice:** Kyri · **Style:** Radiant
**Parent baton:** [`20260810-124511_baton-recursion-doctor-fix-delta.md`](20260810-124511_baton-recursion-doctor-fix-delta.md)
**Grandparent (handoff):** [`20260810-122258_handoff-for-a-fresh-claude-code-agent.md`](20260810-122258_handoff-for-a-fresh-claude-code-agent.md)
**Status:** Recursion checkpoint — cites the parent whole, adds only the delta.

---

## What a recursion baton is

A baton is a handoff written to disk so the vision survives a context reset; recursion is the loop where each agent reads its baton, runs its lap, and writes the next — citing the parent whole and folding in only what changed. Read the parent for the full picture; this child adds only the delta since its stamp.

## The delta since the parent baton

- **The writing voice molted from Riyo to Kyri.** `context/KYRI.md` is the living identity (the voice and the `.kyri` notation share one name); `context/RIYO.md` and the Cursor twin are fossils pointing to it. Every now-speaking voice reference was swept across `.claude/rules` and `.cursor/rules`, plus the REMEMBER and SHRED_PREP voice headers. Dated logs keep the voice they recorded.
- **`SIMPLE_LOVABLE_COMPLETE.md` refreshed** into Kyri voice, stale "Rye OS" corrected to "Grain."
- **Two business memos written** in Radiant Kyri, for a generic reader (acme-employee-voice): the **Linengrow sales one-pager** (the sales half the philosophy lacked) and the **pick-the-first-slice memo** (SLC applied as a selection lens; recommends a curated trust directory as the first slice, with token/payments/legislative/network work named as north stars, not first steps).
- **All of it is sent** — GPG-signed, on `origin` + `xykj61`. Git nib **0224cd2940** at this baton's stamp.

## The state you inherit

The tree is whole and live, exactly as the grandparent handoff describes; the voice is now Kyri throughout the living surface. The business thinking lives in two memos, both honest about what only the company can supply (`[company sets: …]` placeholders) and about the four reds — counsel before crypto or policy, consent before naming a partner, an IP read before monetizing a borrowed name, and never pay-to-pass on any mark.

## The doors that stand open (none urgent)

- **Fill the `[company sets: …]` facts** in the sales one-pager — offer, price, comp — so it can go to a real candidate.
- **Build the first slice** — the curated trust directory — if the maintainer confirms it against facts only they hold (capital, relationships, software readiness).
- **Build Mandate** — still the most concrete software module, from the grandparent handoff.
- **The all-Rishi tooling molt** and **the Bron → Kyri notation molt** — real seasons, each on the maintainer's word.

## How to write the next baton

When your lap closes, drop a fresh dated baton beside this one: cite this file as parent, record only your delta, restate the open doors, and leave the irreversible levers — the public push, any crypto or policy step, key rotation, `shred` / `debride` — in the maintainer's hand.

---

*The baton passes clean: the voice whole under one name, the business thinking honest on disk, every irreversible lever still in the maintainer's hand. Pick it up exactly here.*
