# Expanding Prompt — Door 9 Charter · Persistence Proof

**Stamp:** `20260730.084042` · **Voice:** Riyo · **Style:** Radiant · **Lens:** TAME  
**Kind:** door charter · Journey 4 (h4 Accrete-never-break) · quest Do  
**Basis:** tip `8f555a9f1e` · recursion paste lean **A** · queue seat 9 from `20260730-034932_build-door-6-charter-and-queue.md`  
**Foundation:** Accrete-never-break — state that sleeps must wake equal

*Written together by Keaton and Riyo — shapes travel here; the bench seats them.*

---

## One sentence

Door 9 proves the myc fold **across a sleep**: snapshot fold state (Amber cellar shape — bit-faithful bytes at home), restore, fold the remainder, and assert equality with a fresh fold of the same full log.

## Why this door (not inventing)

The Build queue named seat 9 as persistence proof after star-reservation mechanics and before tend two. Door 8 graduated the fact-fold shape; purity already compares fresh vs resumed *in memory*. Door 9 accretes the missing seam: **bytes that leave the process and return**. Amber names cellar preservation in place; this door borrows that duty for FoldState without inventing wallet, chain, or membership policy.

## Check shape

| Limb | Contract |
| --- | --- |
| **snapshot** | encode FoldState (or log prefix + state) to a bounded byte buffer · refuse whole when over |
| **restore** | decode refuses malformed / truncated / unknown version whole — never silent skip |
| **equality** | fresh fold(full log) == restore(snapshot) then fold(remainder) · byte-for-byte on supply · issued · taxed · star set |
| **purity** | same log + same snapshot bytes → same accept/refuse |
| **bounds** | snapshot max bytes named with why · persistence arithmetic `u64` · inherit `myc_log_max_facts` / fact ceilings from brix |
| **identity** | facts still verify through kumara before count — snapshot does not launder a bad seal |

## Path

`mycelium/fold_persist.rye` — one file · narrow-scope · witness in `main` · imports fold + tally_copy only (no Weave/Mantra/wallet).

## Bounds to seat (before or with the create)

Propose in `recursion_block.brix` (builds inherit; never invent at call sites):

| Bound | Lean | Why |
| --- | --- | --- |
| `myc_fold_snapshot_max_bytes` | **4096** | one FoldState freeze for a journey-scale log · power of two · fits beside discovery staleness 4096 |

## What this door never touches

- Membership · join · departure · star release/expiry policy  
- Amber deep-storage export paths / openssl host oracles (elder ring stays; this is myc-local)  
- Wallet · gas · keys · deploy · season name  
- Discovery round-trip (still queued separately)

## After GREEN

- Almanac ch1 earns the next open seat **from the metal** (seat 8 of 16 if still open — number follows the book, not this charter)  
- Tend two stays available while fascia < 80 (cadence law)  
- Design shape may accrete beside `fact_fold.brix` only at second outside consumer

## Cadence note

Fascia holds at **69**. Tend rounds remain real while under 80. This charter fills the empty Build door; the next tend may land before or after the create when the third-round cadence bites — measurement beats calendar theater.

---

*May every sleep leave the number whole. May restore refuse louder than it guesses. May Accrete-never-break keep the elder log readable across the nap.*
