# Expanding Prompt — Door 10 Charter · Refusal Storm

**Stamp:** `20260730.090221` · **Voice:** Riyo · **Style:** Radiant · **Lens:** TAME  
**Kind:** door charter · Journey 4 (h4 Accrete-never-break) · quest Do  
**Basis:** tip `e1e7e711bd` · fresh Cursor Grok handoff lean · queue seat 11 from `20260730-034932_build-door-6-charter-and-queue.md`  
**Foundation:** Accrete-never-break — every refuse that already bites stays loud; the storm gathers them

*Written for an Acme Corporation employee reading this charter — shapes travel here; the bench seats them.*

---

## One sentence

Door 10 proves a **refusal storm**: one witness walks adversarial fixtures across the five primitives and asserts each refuse is loud, named, and leaves honest state untouched.

## Why this door (not inventing)

The Build queue named seat 11 as refusal storm after tend two and before the performance pin. Doors 6–9 already prove welcome paths and local refuse limbs in place. This door accretes the missing joint: **negative space as one storm**, so a reader never hunts five selftests to trust the unwelcome path. Discovery round-trip stays queued separately (elder seat 6); rye still refuses imports outside a module path, so the storm lives where myc metal already composes (fold · fold_persist · ship_sol · kumara) and maps each limb onto a primitive without inventing a sixth.

## Five limbs → five primitives

| Primitive | Storm fixture (must bite) |
| --- | --- |
| **keypair** | tampered seal → `IdentityRefused` (or equal named refuse) · state unchanged |
| **signed event** | unknown kind / wrong-shape body → refuse whole · never skip silent |
| **append-only log** | refused append leaves `log.len` identical · no quiet trim |
| **pure fold** | tax overdraw and/or double star → refuse whole · supply invariant holds on the kept prefix |
| **capability / seal at seam** | over-bound or truncated snapshot (`fold_persist`) **or** `ship_sol` shape/tamper refuse — one named seam refuse is enough |

Each fixture fails the door if accept sneaks through or state mutates on refuse.

## Check shape

| Limb | Contract |
| --- | --- |
| **coverage** | at least `refusal_storm_min_cases` fixtures · one per primitive row above |
| **loudness** | every refuse is a named error · never a bare false or silent skip |
| **purity** | same adversarial bytes → same accept/refuse |
| **bounds** | inherit seated myc · ship_sol · snapshot ceilings from `recursion_block.brix` · invent none at call sites |
| **identity** | facts still verify through kumara before count — the storm does not launder a bad seal |
| **narrow** | one file · witness in `main` · imports only mycelium-local modules already GREEN |

## Path

`mycelium/refusal_storm.rye` — one file · narrow-scope · witness in `main`.

## Bounds to seat (before or with the create)

Propose in `tools/gen/season/recursion_block.brix` (builds inherit; never invent at call sites):

| Bound | Lean | Why |
| --- | --- | --- |
| `refusal_storm_min_cases` | **5** | one adversarial bite per primitive · storm incomplete under five |

## What this door never touches

- Membership · join · departure · star release/expiry policy  
- Discovery wire-lab both-sides (elder queue seat 6 — still open)  
- Wallet · gas · keys · deploy · season name  
- New refuse vocabulary that renames errors already GREEN on fold / ship_sol / fold_persist  
- Cross-module `@import` into `comlink/discovery/` (rye module path law)

Discovery introduce/gossip/table refuse fixtures remain GREEN on their own witnesses; this storm does not re-home them. The discovery round-trip door may later compose both sides across a wire.

## After GREEN

- Almanac ch1 earns the next open seat **from the metal** when the run itself writes the line  
- Performance pin and tend three stay queued · fascia still under 80 keeps tend cadence live  
- Design shape accretes only at a second outside consumer

## Cadence note

Fascia holds at **69**. Tend rounds remain real while under 80. This charter fills Build door 10; tend three may lift when the third-round cadence bites — measurement beats calendar theater.

---

*May every unwelcome path speak its name. May refuse leave the log as it found it. May the five primitives prove their negative space together.*
