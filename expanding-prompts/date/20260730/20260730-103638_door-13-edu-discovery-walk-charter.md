# Expanding Prompt — Door 13 Charter · Edu Discovery Walk

**Stamp:** `20260730.103638` · **Voice:** Riyo · **Style:** Radiant · **Lens:** TAME  
**Kind:** door charter · Journey 4 (h4 Accrete-never-break) · quest Do  
**Basis:** tip `2f8f19bd4a` · recursion paste lean **A** · queue seat 15 from `20260730-034932_build-door-6-charter-and-queue.md`  
**Foundation:** Accrete-never-break — learners run what is already GREEN; invent no wire lab in edu clothes

*Written for an Acme Corporation employee reading this charter — shapes travel here; the bench seats them.*

---

## One sentence

Door 13 lands an **edu demo of the round-trip shape**: a learner page under `edu/` that walks the discovery exchange and myc fold already GREEN, with runnable commands and a thin witness — `edu/` law holds (`git add -f`).

## Why this door (not inventing)

The Build queue named seat 15 as edu demo of the round-trip after the five-primitives reference and before create prep. The elder **wire both-sides** discovery round-trip (queue seat 6 · original door-6 charter) never landed under that door number — `ship_sol` took door 6. This door does **not** invent spawn/wait-for wire metal to fill the name. It accretes the honest teaching joint: **compose what already runs green** (descriptor · table · gossip · introduce · fold) so a newcomer can watch the round-trip *shape* on one bench. The wire lab stays queued as elder seat 6 / a future door.

## Check shape

| Limb | Contract |
| --- | --- |
| **learner page** | one `edu/` markdown page · Acme-plain · hearable aloud · names coined once |
| **run book** | ordered commands a learner copies · each points at an already-GREEN path |
| **composition** | at least discovery quartet + myc fold · refuse path named once (introduce stranger or storm) |
| **witness** | thin `tools/*.rish` (or equal) re-runs the walk and asserts GREEN lines · invents no sixth primitive |
| **edu law** | paths under `edu/` · commit with `git add -f` · update `edu/README.md` index |
| **honesty** | page states plainly that wire both-sides round-trip metal is not claimed here |
| **narrow** | one teaching page + one witness · zero new myc primitives · no wallet vocabulary |

## Path

| Artifact | Role |
| --- | --- |
| `edu/discovery/round-trip-walk.md` | learner page — the demo |
| `tools/edu_discovery_walk_witness.rish` | thin witness — re-runs the walk GREEN |
| `edu/README.md` | index line for the discovery walk lane |

## Commands the walk must include (already GREEN)

```
env RYE_ZIG=vendor/zig-toolchain/zig rye/bin/rye run comlink/discovery/descriptor_test.rye
env RYE_ZIG=vendor/zig-toolchain/zig rye/bin/rye run comlink/discovery/table.rye
env RYE_ZIG=vendor/zig-toolchain/zig rye/bin/rye run comlink/discovery/gossip.rye
env RYE_ZIG=vendor/zig-toolchain/zig rye/bin/rye run comlink/discovery/introduce.rye
env RYE_ZIG=vendor/zig-toolchain/zig rye/bin/rye run mycelium/fold.rye
```

Optional refuse bite (one is enough): introduce stranger fixture already inside introduce.rye, or point at `mycelium/refusal_storm.rye` for negative space without re-homing it.

## Bounds to seat

No new arithmetic bound. Peers-at-two and convergence ceilings remain the elder wire-lab charter's seats when that door opens. This edu walk inherits discovery bounds already in `recursion_block.brix` (512 · 256 · 8 · 4096 · 2).

## What this door never touches

- Inventing `comlink/discovery/*_round_trip.rye` wire spawn/wait-for as if seat 6 metal landed  
- Wallet · gas · keys · deploy · season name · shred · tuning  
- Star-reservation policy words · membership  
- Rewriting Sangha pages or myc GREEN modules  

## After GREEN

- Almanac earns a seat **from the walk witness** when it prints GREEN  
- Create prep (seat 16) and elder discovery wire round-trip stay queued  
- Fascia **90** holds

## Cadence note

Fascia holds at **90**. Tend three is DONE. This charter fills Build door 13; create teaches the composition the metal already blessed — edu grade, not a fake pier.

---

*May every learner command be one the machine already said GREEN. May the wire lab wait for its own door. May edu stay a floor you can walk with your hands.*
