# Baton — the Recursion Lap (round sent, debride gated)

**Language:** EN
**Stamp:** `20260810.152611` (2026-08-10 EDT)
**Voice:** Kyri (Qkri register) · **Style:** Radiant
**Parent baton:** [`20260810-145743_baton-recursion-brix-molt-journey-qkri-calendar.md`](20260810-145743_baton-recursion-brix-molt-journey-qkri-calendar.md)
**Grandparent (handoff):** [`20260810-122258_handoff-for-a-fresh-claude-code-agent.md`](20260810-122258_handoff-for-a-fresh-claude-code-agent.md)
**Bound:** one round in the **calendar** (`context/LEXICON.md` → calendar) — power-of-2, TAME-guided baton · prin · recur.

---

## What a recursion baton is

A baton is a handoff written to disk so the vision survives a context reset; recursion is the loop where each agent reads its baton, runs its lap, and writes the next — citing the parent whole and folding in only the delta.

## The delta since the parent baton

Small and honest: the parent round **sent clean** (both remotes, git nib `338d130dc8`), and `prin` reads green (sundial 100). No new substantive work landed this lap — the constructive queue is at gates that need the maintainer's word.

## The one thing waiting — the debride gate

The **debride of "old logs no longer needed"** is held, correctly, and is the single open action:

- Debride overrides accrete-never-break, so its rule requires a **specific named target** and a **cairn** (walk-back marker) first.
- "Old logs no longer needed" names no specific target, and the 2,000+ dated logs are protected by the one-clock law — a vague sweep is not safe to run.
- **Low-regret scope on offer:** debride only the archived Markdown logs that already have a superseding `.kyri`/`.bron`, with a cairn dropped first. This waits on the maintainer naming the scope.

`mitra` / shedding likewise stays RED — only molt-prep is done.

## The doors that stand open (all need the maintainer's word)

- **Name the debride scope** (which logs, exactly) — or leave the logs whole.
- **Configure the other two duals** beside Qkri.
- **Build the first slice** (curated trust directory), fill the sales one-pager `[company sets: …]` facts, or write a first Linengrow profile on a real subject's consent.
- **Build Mandate**, or open the all-Rishi / Bron→Kyri notation molts.

## How to write the next baton

Cite this file as parent, record only your delta, keep the bound a power of 2, print with `prin`, and leave every destructive lever — `shred`, `debride`, the public push, crypto/policy steps — in the maintainer's hand.

---

*The round is sent and green, the loop passes clean, and the one open action rests honestly at its gate until a real scope and a cairn are given. Pick it up exactly here.*
