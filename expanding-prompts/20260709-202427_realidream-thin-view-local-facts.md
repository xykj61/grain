# Expanding Prompt — Realidream Thin View (Local Facts)

**Stamp:** `20260709.202427`
**Voice:** Rio 3
**Status:** **Landed** `20260709.202427` — `thinviewtest` GREEN; parity **202**
**Ground:** TASKS thin-demo exception (`021112`) · counsel §3 · chain-read **removed** `152612` — re-scope to local MUR/WOV fixtures (was MALA) through SLC-2a drawn terminal  
Radiant pass `20260728.052149` — living rename-forward: fixture path `mala_m1_mint.bron` held until tool step 4 lean A (or module); memo `mala:mint` waits module wave · MUR Season u20 dry-run

---

## Re-scope (honest)

Counsel `021112` named "already-fetched chain-read data." Settlement chain-read is gone. The thin exception still holds if the demo only **renders local, already-on-disk fact fixtures** through the existing drawn-terminal / Skate surface — no new input, no Pond policy, no Dexter graduation.

## DoD

1. `pond/bin/drawn-terminal thinviewtest` GREEN — compose frame from `mala_m1_mint.bron` + WOV bundle header lines; content signature moves vs empty; golden lines hold.
2. `tools/realidream_thin_view.rish` GREEN; wired into parity (**202**).
3. Missing fixture refused (unwelcome).

## Out of scope

Wayland live window, keyboard, Pond enclosure, Dexter second consumer, network fetch.
