# Expanding Prompt — Door 11 Charter · Performance Pin

**Stamp:** `20260730.101101` · **Voice:** Riyo · **Style:** Radiant · **Lens:** TAME  
**Kind:** door charter · Journey 4 (h4 Accrete-never-break) · quest Do  
**Basis:** tip `c7dd064aaf` · recursion paste lean **A** · queue seat 12 from `20260730-034932_build-door-6-charter-and-queue.md`  
**Foundation:** Accrete-never-break — measure before movement; never tune in the dark

*Written for an Acme Corporation employee reading this charter — shapes travel here; the bench seats them.*

---

## One sentence

Door 11 records an **S0 performance pin**: wall-time baselines for the myc GREEN witness roster, printed and tracked, with **no tuning** in the same door.

## Why this door (not inventing)

The Build queue named seat 12 as performance pin after the refusal storm and before tend three. Doors 6–10 already prove welcome paths, sleep·wake, and negative space. This door accretes the missing joint from the S-ladder (`counsel/20260726-044729_the-graph-is-not-a-vane.md`): **measurement before movement**. Ojjo's yardstick begins as a dated cost table, not an optimization pass. Discovery round-trip stays queued separately (elder seat 6).

## Check shape

| Limb | Contract |
| --- | --- |
| **correctness first** | every rostered witness must exit GREEN (or equal named success) before its elapsed_ms is kept |
| **coverage** | at least `myc_perf_baseline_min_rows` timed rows · roster names the myc Build surface |
| **wall time** | host wall clock (`date +%s%N` or equal) · one sample per roster name · stamp each row |
| **report** | tracked TSV (or equal append-only table) written by the pin · not a chat claim |
| **no tuning** | this door never changes myc algorithms, Zig cache seating, or parallel packs to chase a number |
| **no budget red** | a slow row is a fact · the pin does not refuse on elapsed_ms · never Goodhart the meter |
| **narrow** | one driver file · reuses `tools/parity_time_one.sh` shape when fit · invents no sixth myc primitive |

## Roster (minimum)

Time these mycelium mains (add none that are not already GREEN):

1. `mycelium/fold.rye`
2. `mycelium/fold_persist.rye`
3. `mycelium/ship_sol.rye`
4. `mycelium/build_bounds.rye`
5. `mycelium/refusal_storm.rye`

Optional extras (`kumara.rye` · `tally_copy.rye`) may land in the same table when cheap; they do not replace the five.

## Path

`tools/myc_perf_pin.rish` — one driver · runs the roster under wall timing · writes the report · prints GREEN when rows ≥ bound and every exit is success.

**Report path:** `work-in-progress/myc-s0-baselines.tsv` — columns at lean: `name` · `start_stamp` · `end_stamp` · `elapsed_ms` · `exit` (TSV · one header · append or rewrite whole table each pin run · prefer rewrite of the living pin so the tip holds one clear baseline).

## Bounds to seat (before or with the create)

Propose in `tools/gen/season/recursion_block.brix` (builds inherit; never invent at call sites):

| Bound | Lean | Why |
| --- | --- | --- |
| `myc_perf_baseline_min_rows` | **5** | fold · fold_persist · ship_sol · build_bounds · refusal_storm — the Build myc surface after door 10 |

## What this door never touches

- Tuning · cache relocation · parallel witness packs · receipt FAST/COLD (S1–S3 stay later)  
- Discovery wire-lab both-sides (elder queue seat 6 — still open)  
- Wallet · gas · keys · deploy · season name · shred  
- Performance budgets that fail the door when a host is slow  
- Cross-module rye `@import` inventions — the pin is a tools driver over already-GREEN mains

## After GREEN

- Almanac ch1 earns the next open seat **from the metal** when the pin itself writes the line  
- Tend three stays queued (seat 13) · fascia **90** means tend cadence under 80 no longer forces the calendar  
- Discovery round-trip and reference page stay queued · no optimization lap opens from silence

## Cadence note

Fascia holds at **90** (exit bar MET). Tend three remains a queue shape, not a fascia emergency. This charter fills Build door 11; create records numbers the metal said — never a guess.

---

*May every baseline be a number the host wrote. May tuning wait for a table. May measurement keep Accrete-never-break honest about cost.*
