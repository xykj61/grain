# Rankings and the Season Itinerary — a Deterministic Plan

**Language:** EN
**Stamp:** `20260810.213133` (2026-08-10 EDT)
**Voice:** Kyri · **Style:** Radiant
**Status:** Plan — defines a deterministic itinerary; opens no build lap
**Kin:** `context/rankings.kyri` · `context/LEXICON.md` (calendar · rankings) · `tools/gen/season/recursion_block.brix` (the elder 16-slot cycle)

---

## What this plan gives

Two things the maintainer asked for, joined:

1. **Rankings** — a 27-slot emphasis rota over the living corpus (`context/rankings.kyri`), generalizing the elder priority cycle from *practices* to *documents*.
2. A **pregenerated, deterministic itinerary** — every round of a season computed from its number alone, with carry-over folded into the next round's responsibilities. No hand-written list of 256 entries; a formula that *is* the itinerary, so any round's plan is reproducible from the number.

## The calendar, restated

`context/LEXICON.md` → **calendar**: nesting by powers of two.

- **Round** — one lap.
- **Quest** — 4 rounds.
- **Journey** — 16 rounds (4 quests).
- **Equinox** — 64 rounds (4 journeys).
- **Season** — 256 rounds (4 equinoxes = four compass directions).

## The deterministic itinerary formula

Let `r` be the **round index within a season**, `r = 0 … 255`. Every field below is a pure function of `r` — nothing hidden, nothing random.

```
equinox(r)        = r / 64            # 0..3  → the season's four themed equinoxes
journey(r)        = r / 16            # 0..15 → four journeys per equinox
quest(r)          = r / 4             # 0..63 → four quests per journey
round_in_quest(r) = r % 4            # 0..3
ranking(r)        = rankings[r % 27]  # the emphasis target this round (context/rankings.kyri)
```

**A round's itinerary** is therefore, deterministically:

- **Coords** — `season · equinox(r) · journey(r) · quest(r) · round_in_quest(r)`.
- **Theme** — the equinox's waymark and holds (below).
- **Emphasis** — tend `ranking(r)`: freshen that living target, run its witness, apply its discipline, and record any drift.
- **Carry-over** — see the rule below.

Because `256 mod 27 ≠ 0`, the ranking emphasis **drifts** across the calendar (≈9.48 revolutions per season): a ranking never sticks to one journey or equinox, so the whole corpus is tended no matter where the calendar sits. That drift is the point, not a flaw.

## The carry-over rule

Each round's responsibilities are **its own emphasis plus whatever the round before did not finish**:

```
responsibilities(r) = { tend ranking(r); advance the equinox theme }
                      ∪ { any responsibility of round r-1 not marked done }
```

A responsibility is *done* only when its witness is GREEN or its edit is shipped; anything else rolls forward into `r`. So the itinerary is pregenerated **and** self-healing — an unfinished round is never dropped, it becomes the next round's named debt. This mirrors reds-first: a red found books the remainder of the journey before new work opens.

## Round-open wiring

The ranking is surfaced **at round-open** through `prin`, the orientation printer:

- **`prin ranking`** (alias `prin rota`) prints the 27-slot rota — the one living thing each round tends — so opening a round shows the emphasis without hunting for it.
- **`rishi/bin/rishi run tools/gen/season/itinerary.rish <r>`** gives a specific round's whole day (coords · theme · ranking · carry).

So the round-open ritual now carries the rankings: print `prin ranking` (or the round's `itinerary.rish`), tend the named slot, and let the carry-over fold the prior round's unfinished work in.

## Season 1 — the Compass Season (the four directions)

The four equinoxes are already seated as waymarks (`.claude/rules/waymark-ladders.md`):

| Equinox (r range) | Waymark | Theme — its holds |
|---|---|---|
| 0 (r 0–63) | **SOON** | The Language — Glow runes · stdlib/PLEAC · Brix-infuse · pipeline |
| 1 (r 64–127) | **JARL** | Identity & Network — Kumara · d12·d60 topology · Comlink · settlement |
| 2 (r 128–191) | **BUHR** | Surface & Intelligence — Realidream · Quin voices · MCP-in-Bron |
| 3 (r 192–255) | **TACT** | The World — Ship-Pilot · publishing · Grainphone · commerce |

Each equinox's four journeys and their quests are generated by the formula: journey `j` of an equinox runs rounds `64·e + 16·(j-4e)` onward; each journey is one 16-round arc opening with a charter and closing with a recursion baton (per the calendar). The **ranking(r)** emphasis runs underneath the theme, so (for example) round 0 opens SOON while tending `rankings[0]` = TAME_GUIDANCE, round 1 tends RADIANT_STYLE, and so on — the discipline rota woven through the thematic work.

## Season 2 — the Harvest Season (sketch)

Where the Compass Season *builds* the world, the Harvest Season *tends and gives* what was built — the ecological-civic turn the foundations point at. Four equinoxes, **waymarks drawn `20260810.222755`** (canonical, from the flw corpus; digest verified against the pin):

| Equinox | Waymark | Theme |
|---|---|---|
| 0 | **GISM** | **Yield** — prove the built modules on real data (Mandate + Unsplash, the trust directory's first entries) |
| 1 | **AYRE** | **Trade** — the honest commerce layer: fair-trade certification surface, cash-first, no pay-to-pass |
| 2 | **DAHL** | **Commons** — the social layer (Skate) and the community that champions clean brands |
| 3 | **KOFF** | **Rest** — consolidation, the saga close, the 3/4 reading of whether the year rose |

The same formula, rankings rota, and carry-over rule apply unchanged; only the four equinox themes differ.

## The CION meta-season — after the Compass Season

Before or beside the Harvest Season, the **CION meta-season** (waymark drawn `20260810.222755`) runs a **molt · debride · sweep** that converts the tree's count-up-from-zero identifiers (`eNN`, `rNN`, `journeyN`, `questN`) to the two meaningful schemes — **chronological one-clock stamps** and **semantic waymarks** — per `context/specs/20260810-222755_chronological-semantic-labeling-and-the-cion-meta-season.md`. The itinerary *formula* is untouched (it still computes `r/64` etc.); CION changes what a round is *called and cited by*. Dated testimony keeps its ordinals; only living labels convert.

## How this becomes runnable — **landed `20260810.213133`**

The deterministic generator is built: **`rishi/bin/rishi run tools/gen/season/itinerary.rish <r>`** prints any round's full itinerary from the formula above — coords, equinox theme, the ranking tended (`rankings[r%27]`, read from `context/rankings.kyri`), and the carry-over. Arithmetic lives in `tools/fixtures/itinerary_compute.sh` (Rishi has no integer division/modulo, so the sh fixture computes and the `.rish` prints and gates the bound). The witness `tools/gen/season/itinerary_witness.rish` is **GREEN**, proving: (1) all 256 rounds yield a valid coord tuple (`swept=256 bad=0`), (2) `ranking(r)` is a valid roster slot for every `r`, (3) a representative round reads correctly (137 → equinox 2 / BUHR, ranking slot 2), (4) the carry-over field is always present, (5) a round past 255 is refused.

---

*The itinerary is a number made legible: give a round its index and the whole of its day falls out — its place, its theme, the one living thing it tends, and the unfinished work it inherits — so a season can be walked deterministically and nothing is ever quietly dropped.*
