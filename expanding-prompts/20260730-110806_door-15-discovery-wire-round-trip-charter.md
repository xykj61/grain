# Expanding Prompt — Door 15 Charter · Discovery Wire Round-Trip

**Stamp:** `20260730.110806` · **Voice:** Riyo · **Style:** Radiant · **Lens:** TAME  
**Kind:** door charter · Journey 4 (h4 Accrete-never-break) · quest Do  
**Basis:** tip `aa054ccea0` · recursion paste lean **A** · elder Build seat 6 from `20260730-034932_build-door-6-charter-and-queue.md` · handback lean from door 14 create-prep  
**Foundation:** Accrete-never-break · Gall — the working whole grows from parts already GREEN; invent no sixth primitive

*Written for an Acme Corporation employee reading this charter — shapes travel here; the bench seats them.*

---

## One sentence

Door 15 lands the elder **discovery wire round-trip**: two lanes across a spawn/wait-for wire, both sides witnessing table convergence and a shared fold supply — composing the five proven primitives with zero new ones.

## Why this door (not inventing)

Original Build seat 6 chartered this wire lab; `ship_sol` took door 6 instead. Door 13 taught the round-trip *shape* on one bench and left wire both-sides named open. Door 14's create-prep lean said **extend** here. This door accretes the missing joint: **witness-both-sides taken literally across a wire**, reusing the tree's own wire-lab orchestration (`spawn` · `wait-for` · outer timeout), not a fake one-process theater.

Edu walk `edu/discovery/round-trip-walk.md` stays the teaching floor. This door is metal.

## Five primitives composed (zero new)

| Step | GREEN surface | Wire duty |
| --- | --- | --- |
| descriptor | `comlink/discovery/descriptor_test.rye` | each lane names itself inside the bound |
| table | `comlink/discovery/table.rye` | claim ↔ reach · both lanes hold rows |
| gossip | `comlink/discovery/gossip.rye` | descriptors travel under fanout / message ceiling |
| introduce | `comlink/discovery/introduce.rye` | lane A introduces to lane B through kumara · stranger refuses loud |
| myc fold | `mycelium/fold.rye` | one signed fact folds on both lanes · supplies byte-equal at the same prefix |

Gall governs: compose what already prints GREEN. No sixth primitive. No star-reservation policy words.

## Check shape

| Limb | Contract |
| --- | --- |
| **both-sides** | two lanes rise · each side independently asserts row-for-row table equality after converge |
| **fold parity** | one signed fact folds on both · supply bytes equal at the same prefix |
| **refusals** | stranger wrong-shape introduce → far table clean · tampered gossip → refuse whole · fixtures bite |
| **peers** | held at **two** — smallest true conversation |
| **convergence** | named round ceiling · loop that cannot state settle has not understood itself |
| **timeout** | outer timeout · resumable written verdict · long-loop law |
| **orchestration** | spawn + wait-for (tree wire-lab shape) · not silent background hope |
| **almanac** | when GREEN, the run itself appends the next ch1 seat line — metal writes, prose never |
| **narrow** | one create file · zero new myc/discovery primitives · no wallet vocabulary |

## Path

| Artifact | Role |
| --- | --- |
| `tools/discovery_round_trip_wire.rish` | the wire-lab witness — spawn/wait-for · composes GREEN rye · asserts both-sides |

Discovery-family home stays `comlink/discovery/`. The create lives under `tools/` because that is where the tree's spawn/wait-for wire labs already sit (`tools/comlink_*_wire_lab.rish`). Lane bodies remain the GREEN rye modules; the rish orchestrates and asserts.

If create proves a single rye under `comlink/discovery/round_trip_wire.rye` cleaner while keeping spawn of two lane children and the same check shape, that path may replace the rish — **still one file**, still zero new primitives. Charter prefers the rish; create may swap only for honesty of metal.

## Bounds to seat (before or with the create)

Propose in `tools/gen/season/recursion_block.brix` (builds inherit; never invent at call sites):

| Bound | Lean | Why |
| --- | --- | --- |
| `discovery_wire_peers` | **2** | smallest true conversation · elder seat 6 |
| `discovery_wire_convergence_rounds_max` | **8** | settle ceiling · power of two · matches gossip fanout scale |
| `discovery_wire_outer_timeout_seconds` | **64** | outer long-loop bound · power of two · resumable verdict on cut |

Message size inherits `discovery_descriptor_max_bytes` **512**. Introduce hops inherit `discovery_introduce_hops_max` **2**. Fold fact size inherits `myc_fact_max_bytes` **256**. Invent none at call sites.

## What this door never touches

- Claiming door 13 edu walk already proved wire both-sides  
- Star-reservation · membership · tax policy beyond seated fold  
- Wallet · gas · keys · deploy · shred · force-push · season name  
- A sixth discovery/myc primitive  
- Tuning performance (S0 baselines stay measure-only)  
- Opening Equinox B/C/D without a handback word  

## After GREEN

- Almanac ch1 advances **13 of 16** only when the wire run writes the seat line  
- Elder seat 6 debt cleared · Journey 4 may lean return (`return_equinox_e7`) or name the next door  
- Fascia **90** holds · tend cadence under 80 stays off  

## Cadence note

Fascia holds at **90**. Build queue is CLOSED; this door extends Journey 4 outside that closed queue — the honest remainder the create-prep named.

---

*May two lanes agree without one speaking for the other. May refuse stay loud across the wire. May the almanac line arrive from running code alone.*
