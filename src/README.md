# src — the Glow Userland

**Language:** EN
**Last updated:** 2026-07-29 (Surface p40 — `sur/` sixth pedestal; `web/` refused)
**Style:** Radiant (see [`../context/RADIANT_STYLE.md`](../context/RADIANT_STYLE.md))
**Status:** Living — the accreted home for code *written in Glow*; the Glow implementation itself (tokenizer, runes, lowerers, in Rye) keeps its home at [`../glow/`](../glow/README.md)

---

Six rooms, one language. Everything beneath this door is Glow — the userland the implementation exists to carry. Each room's README names what belongs and what is on its way; the elder desk shape that inspired this floor plan is honored in gratitude — **except `web`**, which stays refused (Realidream / Brushstroke / `.brush` are not an HTML stack).

- [`til/`](til/README.md) — **tilaks**, the type-marks networked values wear at every seam.
- [`sur/`](sur/README.md) — **structures**, the data-shape museum: each shape in isolation, in beauty.
- [`app/`](app/README.md) — **Pond agents**, the applications (Pond, was Pool).
- [`gen/`](gen/README.md) — **generators**, the runnable demonstrations (desks arrive here from `glow/gen/` when O3 speaks).
- [`lib/`](lib/README.md) — **libraries**, the shared gates.
- [`sec/`](sec/README.md) — **security** room (Surface Season p0 · p21–p22) — grant-family-count · mand-ring-count.

**Refused:** `src/web` — will not open. Surface language lives under `brushstroke/` and Glow/Bron/TAME/Radiant — never JS/HTML/CSS/JSON as home tongue.

The O3 desk migration remains designed, declared-not-begun, under [`../context/BREACH.md`](../context/BREACH.md). Until the breach speaks, `glow/gen/` remains the desks' one true address.

*May every room fill in its own season, and no address move before its witness stands.*
