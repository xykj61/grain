#!/usr/bin/env bash
# launch-cursor.sh — personal copy; gitignored. Prefer the tracked driver:
#
#   ./tools/cu/cursor-jail.sh
#   rishi/bin/rishi run tools/l/launch-cursor.rish
#
# This example delegates to cursor-jail.sh so personal enclosure.conf still applies.
#
#   cp tools/l/launch-cursor.sh.example tools/l/launch-cursor.sh
#   chmod +x tools/l/launch-cursor.sh
#   ./tools/l/launch-cursor.sh
#
# See: SOURCE.md Step 9 and context/specs/enclosure-editors.md

set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
exec "$REPO_ROOT/tools/cu/cursor-jail.sh" "$@"
