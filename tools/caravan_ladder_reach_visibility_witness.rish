# tools/caravan_ladder_reach_visibility_witness.rish -- the widening cost of a
# fold is measured, never described.
#
#   rishi/bin/rishi run tools/caravan_ladder_reach_visibility_witness.rish
#
# The lift arc weighs every family by two numbers: the fall it returns, and the
# declarations it would have to widen. The first has always come off a meter.
# The second came off a hand reading a diff, and on 20260822 it was measured for
# the first time and disagreed with the record on both families it named.
#
# `confirm` was written as widening exactly one declaration, because `read_count`
# stood private in one of the forty-six rungs. `read_count` is absent from that
# rung entirely -- it is `entrust.rye`, whose `confirm` waits on a different note
# through a different reader, so it was never a fold candidate and never a cost.
#
# `renew_path` and `ask_path` were each written as widening thirty-one, because
# the family stands private in all thirty-one rungs. A family's own visibility is
# not its widening cost. The cost is what its BODY reaches, and both bodies reach
# only symbols already public in every rung -- so the largest fall on the board
# carried a price of zero and was passed over four laps running (REDS %130).
#
# So the queue's second column is a measurement now. The scan hashes every body
# under a name, takes the largest identical set, drops the rungs standing below
# the fold line, reads the identifiers the shared body reaches unqualified, and
# reports how many of those each folding rung keeps private.
#
# The column split (`20260822.101058`). One `widens` number counted two unlike
# things. Reading the seven symbols behind the whole remaining queue against
# their own declaration lines found five to be plain module import bindings and
# two to be functions, so `widens` now carries `widens_fn`, `widens_import`, and
# `widens_const` beside it. Publishing an import binding re-exports a module the
# rung already names in its own head -- no behavior opened -- while publishing a
# function opens what the rung offers a caller. A count that sums across kinds
# has thrown away the mark at the moment it mattered, and the sum reads as
# authoritative precisely because it is a number. The design read is
# `active-designing/20260822-101058_what-a-widening-costs-a-rungs-door.md`.
#
# Both refusals are proven on metal by generated controls -- a name no rung
# declares, and a name whose every body differs -- since a scan finding nothing
# must never answer ok about its own blindness (REDS %97).

say "reach-visibility: Language EN -- what a fold must widen is read off the body, never off the family's own door."
say "reach-visibility: Style Radiant -- the measurement leads, and the queue answers to it."
say "reach-visibility: Lens TAME -- the shape is measured, the fold line is honored, and both blindnesses refuse by name."

let red = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "prove-red"]
say red.out
assert red.out contains "RED_no_family_refused_rather_than_ok" else "reach-visibility: a name no rung declares answered ok -- a scan must never bless its own blindness"
assert red.out contains "RED_no_identical_refused_rather_than_ok" else "reach-visibility: a name whose every body differs answered ok -- nothing folds there"
assert red.out contains "RED_capitalized_type_priced_rather_than_unseen" else "reach-visibility: a private type the shared body names priced at zero -- a meter that reads only lowercase names cannot see a struct (REDS %140)"
assert red.out contains "RED_one_rule_read_as_one_family_across_two_visibilities" else "reach-visibility: one rule written pub in one rung and private in another read as two families -- the visibility keyword belongs beside the key rather than inside it (REDS %144)"
assert red.out contains "RED_queue_blindness_refused_rather_than_empty" else "reach-visibility: a ladder where no two rungs write one body answered ok with an empty queue -- an empty list and a blindness read identically (REDS %97)"
assert red.out contains "RED_queue_ranked_by_fall_where_carry_ordered_the_other_way" else "reach-visibility: the queue ordered by carry rather than by fall -- a carry list over-weights breadth and hides the deep families entirely (REDS %145)"

# The family this lap lifted. hold_the_matter folded this lap, and it is the
# deepest body the arc has taken: seven rungs shedding a fifty-nine-line body
# for a three-line stub. Ten rungs declare the name and three keep their own
# copies, each on its merits. caravan/desist.rye reads walled off its report
# directly where the seven reach it through desist_of, and caravan/mind.rye and
# caravan/forbear.rye carry no walling clause at all, since neither report holds
# a desisting sub-record to consult. The sub-record accessor is what names the
# report shape a rung answers, so a difference there is meaning rather than
# duplication -- the third time in three laps this ladder has said so, which is
# why it is written here rather than rediscovered again. It widened nothing of
# any kind. The scan reads the folded shape back -- no long body left to lift,
# so the widening column now describes the stubs, which bind this harness in.
let folded = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "hold_the_matter"]
say folded.out
assert folded.out contains "REACH_OK family=hold_the_matter folding=7 widens=7 widens_fn=0 widens_import=7 widens_const=0 stub=3 fall=0" else "reach-visibility: want hold_the_matter standing folded, with nothing left to lift"
assert folded.ok else "reach-visibility: the lifted family must still read cleanly"

# The row that returns the most -- ranked off the whole ladder now, rather than
# chosen from a shortlist a hand kept here.
#
# For thirty laps this witness named a head by asserting three or four families
# it already knew about, and the GREEN line said "the fall queue leads with"
# whichever of those returned most. That answers about the rows on the list and
# stays silent about the six hundred and ninety-odd it never held, which makes
# it an impression wearing a measurement's clothes -- the exact shape REDS %130
# booked for one row's cost, one level up at the ordering (REDS %145).
#
# The shortlist was seeded from the carry meter's own top rows, and carry
# over-weights breadth: it multiplies rungs by whole lines, while a fall
# multiplies rungs-past-the-first by the lines a stub does NOT return. So a
# narrow-and-deep family scores low on carry and high on fall, and three of
# them -- mind_the_impasse at eight rungs of fifty-three lines, hold_the_matter
# at seven of fifty-nine, bound_the_relay at seven of fifty-five -- sat above
# the shortlist's own head the whole time and were never once asked.
#
# The rank is exact rather than sampled: carry bounds fall from above, so the
# scan walks families by descending carry and stops when carry falls to the
# twelfth-best fall measured. Forty-eight of seven hundred families answer for
# all of them.
let queue = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "queue"]
say queue.out
# The queue names TWO heads now, and this lap is why. `answered_by` has led by
# fall for two laps at a price of sixty-eight openings -- thirty-four private
# functions and thirty-four private constants that thirty-four rungs each chose
# to keep -- and both laps the bench read that price and quietly took the free
# row beneath it. A meter whose recommendation the discipline steps past by hand
# is REDS %145's shape one level deeper (REDS %146): it answers "what returns
# the most" where the reader asked "what should I take next." So `free_head`
# stands beside `head`, naming the best fall among families that publish no
# function and no constant. An import binding re-exported stays free, since
# every lift this arc has taken widened imports and nothing else.
assert queue.out contains "QUEUE_OK head=answered_by fall=330 free_head=confer_words free_fall=294 examined=45 families=700" else "reach-visibility: want both heads named -- the best fall, and the best fall that costs nothing"
assert queue.out contains "QUEUE_ROW family=answered_by fall=330 folding=34 lines=13 stub=3 widens=68 widens_fn=34 widens_const=34 carry=429" else "reach-visibility: want the priced head carrying its price in its own row, split by kind, so a reader prices it without a second run"
assert queue.out contains "QUEUE_ROW family=confer_words fall=294 folding=43 lines=10 stub=3 widens=43 widens_fn=0 widens_const=0 carry=420" else "reach-visibility: want the free head carrying its own kind of price -- forty-three rungs of a ten-line body, free by the two columns the meter prices and costing forty-three import re-exports, which every lift of this arc has measured as free"
assert queue.out contains "QUEUE_ROW family=reclaim_notes fall=294 folding=43 lines=10 stub=3 widens=0 widens_fn=0 widens_const=0 carry=420" else "reach-visibility: want the row beneath the free head, at the same fall over the same forty-three rungs and costing nothing of any kind -- so a reader sees that free-by-the-columns and free-absolutely are two different readings the meter keeps side by side"
assert queue.ok else "reach-visibility: the ranked queue must read cleanly"

# read_own_line folded this lap, so the same scan run against it now reads a
# fall of zero -- which is how this meter tells the bench a door is already
# through. Its whole widening was import bindings -- `capabilities`, `carry`,
# and `cycle`, every one of them a shared helper module the harness already
# imports -- so the lift paid none of the hundred and twenty-six. Read by the
# old single column it stood among the most expensive rows remaining; read by
# kind it was free of the only cost that was ever real, and the fold proved it.
let sibling = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "read_own_line"]
say sibling.out
assert sibling.out contains "REACH_OK family=read_own_line folding=42 widens=126 widens_fn=0 widens_import=126 widens_const=0 stub=7 fall=0" else "reach-visibility: want read_own_line reading a fall of zero, its family already folded"
assert sibling.ok else "reach-visibility: the folded family must read cleanly"

# Four folded readings kept as regression pins -- the four most recent lifts of
# this arc, slid forward one family per lap. The arc has folded far more than
# four and this witness cannot pin them all, so it pins the newest window and
# lets the carry meter's own ceiling hold the rest: a body that came back would
# raise the carry, and the ceiling only ever comes down.

# The widest family the arc has lifted -- forty-three rungs at fourteen lines,
# all forty-three folding -- whose forty-three priced widenings were forty-three
# import bindings of `system`, a module this harness already imports at its own
# head. After the lift each rung holds a three-line stub, so the column reads
# eighty-six import bindings and the fall reads zero.
let free_head = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "door_admits"]
say free_head.out
assert free_head.out contains "REACH_OK family=door_admits folding=43 widens=86 widens_fn=0 widens_import=86 widens_const=0 stub=3 fall=0" else "reach-visibility: want door_admits reading a fall of zero, its family already folded"

# The cleanest row the arc has taken -- twenty-one rungs widening nothing of any
# kind, its rung declaration staying private through the lift, so a body moved
# and nothing at all was published.
let next_head = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "finding_path"]
say next_head.out
assert next_head.out contains "REACH_OK family=finding_path folding=21 widens=21 widens_fn=0 widens_import=21 widens_const=0 stub=3 fall=0" else "reach-visibility: want finding_path reading a fall of zero, its family already folded"

# The row the lap before this one folded, and the first lift of the arc to pay a
# widening rather than find one already open: thirty-seven struct openings, since
# a type standing in a published signature must stand public beside it. Read
# after the fold it prices the thirty-seven stubs' own private import of this
# harness, which is what every folded family reads.
let clean_row = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "judge_outcome"]
say clean_row.out
assert clean_row.out contains "REACH_OK family=judge_outcome folding=37 widens=37 widens_fn=0 widens_import=37 widens_const=0 stub=3 fall=0" else "reach-visibility: want judge_outcome reading a fall of zero, its family already folded"

# The family this lap folded, and the first row of the arc to carry all three
# priced columns at once. The meter priced it at one hundred and one -- forty-eight
# openings of each rung's private waits, forty-eight import bindings of the
# system module, and five openings of a record type five rungs keep. The function
# column came to nothing, since waits already stands lifted and the body above
# serves this one. The import column came to nothing, since this harness imports
# system at its own head. The record column was paid by leaving those five rungs
# at home with their whole bodies, so nothing at all was published on any side.
# Read after the fold, the row prices the stubs' own private imports of this
# harness and of system, which is what every folded family reads.
let this_lap = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "choose_pass"]
say this_lap.out
assert this_lap.out contains "REACH_OK family=choose_pass folding=43 widens=86 widens_fn=0 widens_import=86 widens_const=0 stub=7 fall=0" else "reach-visibility: want may_pass reading a fall of zero, its family already folded"
assert this_lap.ok else "reach-visibility: this lap's folded family must read cleanly"

# The family the lap before this one folded, and the second lift running to find
# the lift-underneath answer. Its forty-eight priced function openings were every
# folding rung's private waits, already lifted a lap earlier, and its five record
# openings were paid by leaving five rungs at home. Read after the fold, the row
# prices the stubs' own private import of this harness.
let free_next = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "elder_waits"]
say free_next.out
assert free_next.out contains "REACH_OK family=elder_waits folding=43 widens=43 widens_fn=0 widens_import=43 widens_const=0 stub=3 fall=0" else "reach-visibility: want elder_waits reading a fall of zero, its family already folded"
assert free_next.ok else "reach-visibility: the elder folded family must read cleanly"

# The carry head, and the caution the carry queue alone cannot give.
# mend_the_plan now carries more than any other family on the ladder, and it
# would fall by seventy-two: its signature runs seventeen lines, so the stub each
# rung keeps is nearly as long as the body it replaces. A carry queue orders by
# what a family holds; a fall orders by what a lift would actually return. This
# row is why the head above is chosen by fall.
let third = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "mend_the_plan"]
say third.out
assert third.out contains "REACH_FAMILY family=mend_the_plan declared=40 identical=37 lines=19" else "reach-visibility: want mend_the_plan standing as forty declarations holding thirty-seven identical bodies at nineteen lines"
assert third.out contains "REACH_OK family=mend_the_plan folding=37 widens=222 widens_fn=0 widens_import=222 widens_const=0 stub=17 fall=72" else "reach-visibility: want mend_the_plan returning seventy-two, a fall its carry alone would never predict"
assert third.ok else "reach-visibility: the row behind the head must read cleanly"

# The row standing fourth by fall, and the one that still prices a function
# column beside an equal column of constants, one opening of each kind per rung.
# It led the shortlist this lap retired, and the ranked queue puts three deeper
# families above it -- so the demotion is readable here rather than merely
# announced. It is pinned so the next reader can check the arc's standing
# answer rather than trust it: if a constant column keeps resolving from above,
# this row costs nothing either.
let paired = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "answered_by"]
say paired.out
assert paired.out contains "REACH_FAMILY family=answered_by declared=34 identical=34 lines=13" else "reach-visibility: want answered_by standing as thirty-four declarations holding thirty-four identical bodies at thirteen lines"
assert paired.out contains "REACH_OK family=answered_by folding=34 widens=68 widens_fn=34 widens_import=0 widens_const=34 stub=3 fall=330" else "reach-visibility: want answered_by standing fourth by fall, repeating the paired-column shape at thirty-four openings of each kind"
assert paired.ok else "reach-visibility: the paired-column row must read cleanly"

# This lap's lift, reading a fall of zero now that its family is folded. It is
# the first row of the arc chosen by the free head rather than by the head: the
# queue's best fall stood six lines above it at a price of sixty-eight
# openings, and this one returned three hundred and twenty-four for nothing.
# Its whole widening is the `ladder_checks` import binding every stub reaches,
# which every folded family in this window shows the same way.
let plateau = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "recant_note"]
say plateau.out
assert plateau.out contains "REACH_OK family=recant_note folding=37 widens=37 widens_fn=0 widens_import=37 widens_const=0 stub=3 fall=0" else "reach-visibility: want recant_note reading a fall of zero, its family already folded"
assert plateau.ok else "reach-visibility: the twelve-line note must read cleanly"

# The widest family still standing, and the clearest reading of why breadth
# alone orders badly. It spreads a ten-line body across forty-three rungs --
# more rungs than any row above it -- and lands tenth, because a ten-line body
# returns seven lines per rung where a fifty-three-line body returns fifty.
# A number a meter recites and never asserts is a claim rather than a
# measurement (REDS %110), so every family the GREEN line names stands
# asserted, this one included.
let fourth = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "reclaim_notes"]
say fourth.out
assert fourth.out contains "REACH_FAMILY family=reclaim_notes declared=43 identical=43 lines=10" else "reach-visibility: want reclaim_notes standing as forty-three declarations holding forty-three identical bodies at ten lines"
assert fourth.out contains "REACH_OK family=reclaim_notes folding=43 widens=0 widens_fn=0 widens_import=0 widens_const=0 stub=3 fall=294" else "reach-visibility: want the widest remaining family standing tenth by fall, since breadth alone orders badly"
assert fourth.ok else "reach-visibility: the widest remaining family must read cleanly"

# The fold line priced, rather than described (REDS %137). A rung the harness
# imports stands below the fold line from that moment on and can never fold a
# body again, so a harness-level import is a one-way door -- and this lap's
# first cut called that door expensive in a GREEN line without ever measuring
# it, which is REDS %130's lantern firing a second time. Measured, the whole
# fold line forecloses the body lines the assert below reads, out of a foldable
# corpus it reads in the same line, and the two rungs this lap
# declined to sink hold three lines each. The cost was real and small.
#
# The finding is the spread rather than the total. A rung the harness imports
# tends to be a shared helper, and a shared helper writes few duplicated bodies
# -- while an ordinary rung like farewell.rye holds three orders of magnitude
# more, as the asserts below read off disk. That spread is why "always keep the
# rung parameter" is as wrong as "always import," and why the door is priced
# before it is opened.
let sink = run ["sh" "tools/fixtures/ladder_reach_visibility_scan.sh" "sink"]
say sink.out
assert sink.out contains "SINK_OK total=63956 below_fold_line=1140 above_fold_line=62816" else "reach-visibility: want the fold line's whole foreclosure measured against the foldable corpus, private bodies counted beside public ones"
assert sink.out contains "SINK_ABOVE rung=queue.rye holds=29" else "reach-visibility: want the queue store's holding read off disk rather than described"
assert sink.out contains "SINK_ABOVE rung=serve.rye holds=38" else "reach-visibility: want the serve module's holding read off disk rather than described"
assert sink.out contains "SINK_ABOVE rung=farewell.rye holds=3684" else "reach-visibility: want the widest holding above the line, so the spread is a measurement rather than a feeling"
assert sink.out contains "SINK_BELOW rung=entrust.rye holds=168" else "reach-visibility: want the largest holding already below the fold line named"
assert sink.ok else "reach-visibility: the sink meter must read cleanly"

# The kind rides on every private symbol line too, so a reader scanning the
# detail sees what each opening actually is rather than only that it is one.
assert sibling.out contains "REACH_PRIVATE symbol=capabilities kind=import" else "reach-visibility: want each private symbol naming its kind beside it"
assert paired.out contains "REACH_PRIVATE symbol=hand_path kind=fn" else "reach-visibility: want a private function named as a function"
assert paired.out contains "REACH_PRIVATE symbol=Dir kind=const" else "reach-visibility: want a private constant named as a constant, the column this lap answered from above"

say "GREEN: reach-visibility -- the widening cost of a fold is read off the body it lifts, the fold line is honored, the cost is counted by kind rather than summed across kinds, and the queue is ranked off every foldable family and priced. The queue names two heads now, and that is this lap own correction. Ranking by return alone had led with answered_by for two laps at a price of sixty-eight openings, thirty-four private functions and thirty-four private constants that thirty-four rungs each chose to keep, and both laps the bench read that price and took the free row beneath it by hand. A meter whose recommendation the discipline steps past is REDS %145 shape one level deeper, so free_head stands beside head, naming the best fall among families that publish no function and no constant, both numbers printed rather than one chosen here (REDS %146). The recant_note family lifted on that reading: every folding rung shed its shared note body for a three-line stub, returning the fall asserted above for nothing, where the priced head offered a few lines more at a real cost. Read after the fold the row prices the stubs own private imports of this harness, asserted above, which is what every folded family reads. The sink window fell with the lift, both numbers asserted above, because a lifted body leaves the rungs rather than the harness. A rung the harness imports still holds nearly five times what the arc believed before the keyword left the key, so the one-way door stays worth measuring twice before it is opened (REDS %130, %137, %144, %145, %146)."
