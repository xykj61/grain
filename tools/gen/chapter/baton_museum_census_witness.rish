# tools/gen/chapter/baton_museum_census_witness.rish -- thirteen halls - named elders.
#
#   rishi/bin/rishi run tools/gen/chapter/baton_museum_census_witness.rish
#
# Seated `20260730.030553` with the museum - hall 12 tend_round - hall 13 cell
# `20260730.072425`. Census-zero keeps the banked breach word unspent.
# Meter name: museum-hall census (not breach). Breach stays census_breach_count.

say "baton-census: Language EN — thirteen halls, counted once."
say "baton-census: Style Radiant — name absences; keep breach banked when zero."
say "baton-census: Lens TAME — museum path bounded; elders named up front."

let scan = run ["sh" "tools/fixtures/b/baton_museum_census_scan.sh"]
say scan.out
assert scan.out contains "verdict=ok" else "baton-census: a hall or named elder is absent"
assert scan.ok else "baton-census: the verdict reads ok while the status fails — they must agree"
assert scan.out contains "census_breach_count=0" else "baton-census: census found a breach; spend the banked word only on Keaton's yes"
assert scan.out contains "halls_expected=13" else "baton-census: expected thirteen halls"

let control = run ["sh" "tools/fixtures/b/baton_museum_census_control.sh"]
say control.out
assert control.ok else "baton-census: planted missing hall must be recognized"
assert control.out contains "control_verdict=ok" else "baton-census: planted missing hall must refuse with one breach"

say "GREEN: baton-census — thirteen halls present · named elders hold · one missing hall refused"
