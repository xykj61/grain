# Planted negative — labeling module guard

This fixture exists to prove `labeling_module_scan.sh` **bites**. The line below names a
capability by a bare count-up ordinal, exactly what the labeling law retired — the
guard must flag it.

- **Object-storage backing (lap 6, `20260811`).** A capability named by a bare ordinal.

If this file ever stops tripping the guard, the guard has gone blind — never "fix" it
by removing the planted line.
