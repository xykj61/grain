# Known-good fixture — labeling module guard

This fixture proves `labeling_module_scan.sh` does **not** false-positive on the
meaningful labeling schemes the law permits. Every line below must pass clean —
if the guard ever flags one of these, it has grown too greedy and would block
valid identifiers, so route the fix through the pattern, never by editing this file.

Structured protocol codes (a level and a revision — the `R2`/`R3` case, kept):

- `OA-L3` — Open-Asks Level 3, the escrow wire.
- `NS-L3` beading and the bead-index.
- `I2 snapshot` revision batch.
- wire sub-stages `lap 3w-3a`, `lap 3w-3b`, and hosted `lap 3w-4`.
- device sub-revision `lap 4b` fetcher and source.

Hyphen-joined compounds (a staging word, not a bare ordinal):

- `sub-lap 1` hosted seal/open, `sub-lap 2` freestanding link, `sub-lap 3` sealed datagram.

Ordinary prose that merely contains the letters:

- The two windows `overlap 3` times without a seam.
