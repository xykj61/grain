# Corpus control -- a reader-facing page that still says the retired word.

This file is never a living page. It exists so the guard's refusal is shown on metal
rather than described in prose.

The witness scans the corpus and reports every match. A second corpus is held beside it.
