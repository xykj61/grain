# rune_or_bad_type.rish -- a logical-or over a non-boolean must refuse.
# tools/r/rish_rune_connective_witness.rish runs this child and asserts it exits non-zero:
# only booleans fold under `?|`, so a string element refuses rather than coercing.
let x = ?| [false "yes"]
say x
