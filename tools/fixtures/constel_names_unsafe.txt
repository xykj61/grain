# tools/fixtures/constel_names_unsafe.txt — the planted negative for the Constel naming law.
#
# A name shaped like a real @p (lowercase letters + hyphen, NO digit) must be flagged unsafe
# by constel_naming_scan.sh — such a name could parse as a real routable point. This fixture
# exists only to prove the scan bites; it is never a seated draw.
xnqvel34
sarlev
xwmur-dol12
