# The elder carry meter, kept exactly as it shipped

**Stamp:** `20260821.073606` - **Voice:** Kyri - **Status:** Frozen control -- never edited, never repaired

`caravan_ladder_carry_witness.rish` in this directory is a **byte-for-byte copy** of that meter as it stood at commit `b356c54b8e`, the last commit before REDS %110 was closed. Its closing line recites *"137,176 lines across 10,198 copied bodies of the 18,039 standing on it"* -- three numbers the meter's own assert, four lines earlier, contradicted with 137,185, 10,199, and 18,055.

It is the same family REDS %108 named one file over, and it rode free of that fix because the guard REDS %108 built refuses one phrase, `"<number> modules"`, and the carry meter drifted on lines, bodies, and copies instead. A window narrow enough to miss its own subject is REDS %102's lesson at meter scale, and this file is what it looks like when the lesson arrives a second time.

It lives here so that [`../caravan_ladder_prose_count_scan.sh`](../caravan_ladder_prose_count_scan.sh) can be pointed at a real offence forever. The strongest available control is the line that genuinely shipped rather than a shape invented to be caught.

## Why a tracked file rather than a commit reference

REDS %109 records what happens to a control pinned to `HEAD`: it passes on the lap it is written and disarms itself the instant the fix lands, because the fix and the guard ride in the same commit. A copy under version control cannot drift with `HEAD`, and it survives a deep debride that would rewrite every commit hash in the tree.

## Discipline

Do not edit this file, do not sweep its prose, and do not repair its recited numbers. It is testimony, and its whole worth is that it is wrong in exactly the way the guard exists to catch.
