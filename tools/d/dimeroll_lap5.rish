# dimeroll_lap5.rish -- Dimeroll lap 4 memo prefix filter witness.
#
# Run from the repository root:
#   rishi/bin/rishi run tools/d/dimeroll_lap5.rish

let zig = "vendor/zig-toolchain/zig"
let bin = "dimeroll/bin/dimeroll"

let build = run ["sh" "-c" "mkdir -p dimeroll/bin && env RYE_ZIG=${zig} rye/bin/rye build dimeroll/dimeroll.rye -femit-bin=${bin}"]
assert build.ok else "Dimeroll lap 5 build failed"

let selftest = run ["sh" "-c" "${bin} selftest 2>&1"]
assert selftest.ok else "Dimeroll selftest exited non-zero"
assert selftest.out contains "GREEN" else "Dimeroll selftest not GREEN"
assert selftest.out contains "memo prefix" else "Dimeroll missing memo prefix welcome"

say "GREEN: Dimeroll lap 5 — filter_by_memo_prefix on journal entries."
