# The remaining laps of the vendor and seed program

**Language:** EN - **Style:** Gauge, Field - **Voice:** Kyri
**Stamp:** `20260906.125757` - **Status:** Booked. Laps A and B have landed; C through H are open.
**Granted by:** Keaton, `20260906` -- one sitting's grant covering eight distinct laps, closing
*I grant all of this and all your recommendations. Publish seed when you're all done.*
**Survey:** [`20260906-100247_the-three-vendor-names-survey.md`](20260906-100247_the-three-vendor-names-survey.md)

A grant this wide is a **program**, and a program wants writing down. This page holds it so a peer ship can take a lap under claim-as-override, and so a shape already
measured stays measured for whoever picks it up.

## Landed

**Lap A -- the vendor names prepped** (`13a334f86`). Tiers 1 and 2 of the survey, filed as Class M
in `construction/SHRED_PREP.md`: thirteen fossils bannered, three refused a banner for three
different reasons, two Grok phrases breached, and `personalize.template.brix` turned from this
pier's own bench into fillable stubs. Every fossil stays on disk and every reference still lands; the cut stays RED.

**Lap B -- gratitude ships** (`4e8c83fb6`). `allow gratitude` with a `scrub` verdict, a saved
third-party article withheld by copyright and all twenty gitlinks by licence, and REDS `%489` for
the second filter that made the allowlist a lie by omission -- now gated by `sow_allow_reach`.

## Open, in order

**Lap C -- the scrub-script template.** `publish-seed.sh` is `personal` in the manifest by law: the
field's own projector, never the seed's carrier. So a seed user arrives with everything except a publisher. This lap
writes `publish-seed.template.sh` -- the same five steps, with the field's five literals standing as fillable stubs -- and adds the fields that fill them to `GLOW_PROFILE.template.bron`, so a newcomer's own
agent can write their publisher from their own profile. The five literals, measured: the two remote
URLs, the anonymous identity name and email, and the root-commit subject, which is the one word
their own commit-msg wall must learn to exempt.

**Lap D -- the Glow profiles molt from Bron to Kyri.** `GLOW_PROFILE.template.bron` and
`GLOW_HOST.template.bron` become `.kyri`, in the field and in the seed, with every inbound reference
repointed in the same commit and the fascia proven back to the front door. The notation molted on
`20260810` and these two never followed. **References are promises**, so the sweep is whole-tree by
law, and the census of what points at them is the lap's first move.

**Lap E -- contributing, onboarding, and the PR grammar.** `CONTRIBUTING.md`, `SOURCE.md`, and
`docs-geode/tutorials/the-first-hour.md` gain what a depersonalized outside contribution needs: PR
title classifications in brackets, and New Gauge sentences a contributor can actually reuse for
*foundations harmony*, *rota adherence*, and *active-designing progression*. **The crux is already
known:** `SOURCE.md` carries 58 Cursor mentions across 45,037 bytes and Part One's stated end is *a
working, jailed Cursor*, so this lap and Tier 3 of the survey are **one rewrite of one document**.
Held together, one rewrite serves both; held apart, the same 45 KB is paid for twice.

**Lap F -- the gratitude fascia.** Every gratitude paragraph in `external-research/` links through
to the room it thanks, and lands in all four doors -- `xy`, `gp405`, and both seed faces. Lap B made
this checkable for the first time: a link into `gratitude/` used to resolve in the field alone, which is the
example `tools/fixtures/s/seed_link_scan.sh` uses in its own header. The room ships now, so those
links can be made true in every door rather than in one.

**Lap G -- the silo read.** `foundations/` and `active-designing/` follow silo technique: our own
module names throughout. `active-development/` is exempt by its own charter. This lap
measures whether any gratitude paragraph in those two rooms carries an **unsiloed technique
reference**, and repairs what it finds. `context/SILO_TECHNIQUE.md` already ships in the seed; if
the read shows the law reads thinner than the practice, the same lap molts it into New Gauge with
Civic and TAME beside it.

**Lap H -- the context room's own reading.** A general QA report card over `context/`, and a
**recommendation** on Keaton's granted-if-recommended question: whether every `context/` file should
also stand in `foundations/` as a Brix mutant under alias-sameness. **The recommendation waits on the reading**, and the reading is the lap.
`.claude/rules/alias-sameness.md` seats one referent, one implementation, and *peers* -- so a
hundred-odd duplicated pages would be a second copy wearing a synonym's clothes, which is the fault
that rule exists to prevent. The lap tests that reading against the actual room, and answers from
what it finds.

## Then, and only then

**The publish.** Custody gate `%1`. `publish-seed.sh` in its bare form projects, proves, and commits
and then **stops**, printing the push -- a script whose bare invocation stops at a custody gate is
the shape the card's own law asks for. The `--push` is the maintainer's word, typed once, at the
moment he means it. It waits for laps C through H because those laps rewrite the seed's own front door, its onboarding
guide, and its contributing grammar -- and a seed published between them would teach a newcomer the
world we are in the middle of leaving.
