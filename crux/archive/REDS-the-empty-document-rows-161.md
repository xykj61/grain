# REDS -- the empty document, and the two temperatures (row %161)

**Language:** EN
**Style:** Gauge -- Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Folded:** `20260823.045448` -- carried here from [`../REDS.md`](../REDS.md) when the living pin
passed its 24,576-byte bound as row %163 was booked.

---

**REDS %161 (`20260823.034321`) -- two tracked documents hold zero bytes, and every guard passed them.** *What went wrong:* an audit of the roster for blind classes found `session-logs/date/20260731/20260731-220432_e121-roots-bench-amend-green.bron` and `expanding-prompts/archive/20260731-220432_equinox-season-e122-recursion-prompt.md` at **0 bytes**. The session log is **indexed in `session-logs/README.md`**, so the index promises a reasoning record that does not exist; two later recursion prompts cite the empty prompt by name. *What caught it:* an explicit sweep for classes of tracked content no rostered guard reads, run because `REDS %156` asked which witnesses belong on the roster and `REDS %159` answered that the roster was blind to an entire *kind* of content. *What it taught:* **an empty file passes every guard by construction.** A link guard is satisfied -- the path exists. A prose guard reads nothing and finds nothing wrong. A banner guard finds no banner to disagree with. **Vacuous truth is the shape of this whole class:** a check phrased as *nothing in this file is wrong* is answered perfectly by a file with nothing in it. The content of that lap's log is not recoverable and the row records the loss honestly rather than pretending otherwise; the **class** is guardable, and a rung that gates tracked documents at a minimum honest size is named here as the next lap's work.

**REDS %161 CLOSED (`20260823.041442`) -- nothing was lost, and the class that hid it now reds.** *The root, found by reading git rather than assuming:* both files were **born empty** in commit `758d64e639` on `20260731` and stood so for twenty-three days. Neither was corrupted -- `git ls-tree -l` reads size 0 at their own seating commit -- and neither lap's record was lost, because both recorded themselves in the waymark room instead: `waymarks/date/20260731/20260731-220432_e121-roots-bench-amend-green.md` at 1,759 bytes, and `waymarks/date/20260731/20260731-221131_e122-roots-bench-kinds-green.md` beside its counsel. *The repair, on metal:* both files are **filled with an honest pointer** to where their record actually lives -- accretion rather than a rewrite, since there was no content to preserve, and removal would have broken the `session-logs/README.md` index row and the two later recursion prompts that cite the empty prompt by name. Every inbound reference keeps resolving, now to something true. The loom is [`../tools/empty_document_witness.rish`](../tools/empty_document_witness.rish) over [`../tools/fixtures/empty_document_scan.sh`](../tools/fixtures/empty_document_scan.sh), which gates at zero every tracked `.md`, `.bron`, `.kyri`, or `.txt` holding nothing or nothing but whitespace -- a file with one newline says exactly as much as a file with none. `tools/fixtures/**` and `context/fixtures/**` pass free as **instrument, never field**, written as a named rule rather than remembered, because that lesson has now fired three times (`%157`, `%158`, here) and a lantern that fires twice becomes a loom. [`../tools/fixtures/empty_document_control.sh`](../tools/fixtures/empty_document_control.sh) proves **fourteen** behaviors on throwaway repositories -- five refusals bitten, nine honest readings left free. The first shape spawned one `git cat-file` per document and took 33 seconds; one `--batch-check` reads all 8,486 in **0.16s** with every field identical, because a guard nobody waits for is a guard nobody runs. GREEN on metal at `empty_documents=0`. Seated as the roster's **twenty-sixth** guard.
