# REDS -- rows %127 and %128, folded from the living pin

**Folded:** `20260822.054053` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two rows about a number a hand typed: the ledger's own headline drifting from the count its named tool measured, and the delegating stub described as one line when disk said three. Both closed on a meter, and both meters run every lap -- era rungs 9 and 10.*

Each row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read and stays under its named bound; the
lesson travels forward in the guards these rounds built, and the row itself stays one
click away.

---

**REDS %127 (`20260821.235936`) -- the ledger's headline recited a count its own named tool measured differently, one lap after being corrected by hand.** *What went wrong:* the living pin's opening paragraph reads *Rows: 125 ... counted from the ledger and its archives by `tools/gen/season/reds_ledger_monotone_witness.rish`* -- and that witness measured **126**. All three numbers the headline recites about the spine had drifted together: the total read 125, the span read *1 to 125*, and the derived remainder read 105 where the arithmetic gives 106. The drift was one lap old. Row %126 landed and closed in the round before this one, the same round that corrected this very headline from a prior drift; booking the row and republishing the total are **two acts**, and only the first of them was ever guarded. *What caught it:* running the ledger's own guard at the top of a Standfast pass, then reading the number it printed against the number the page beside it published -- rather than reading the page and believing it. *What it taught:* **a number a human republishes is a number that will one day be republished wrong.** REDS %110's answer to a drifting tally was to *derive* it rather than recite it, and the derivation still has to be typed by a hand on the lap the row lands. A tally that names its own measuring tool is making a checkable claim, so the check belongs to a machine. This is the third time this one paragraph has drifted, which is a lantern that has fired twice over -- so it becomes a loom.

**REDS %127 CLOSED (`20260821.235936`) -- the headline now answers to the spine, and a drift refuses by name.** *The repair, on metal:* the three recited numbers are corrected to whatever the guard prints rather than to a number typed here, and a new contract seam stands beside the counting one. [`tools/fixtures/reds_ledger_headline_scan.sh`](../tools/fixtures/reds_ledger_headline_scan.sh) takes the ledger and the measured row count, reads the headline's total, span, and remainder back out of the prose, and refuses `verdict=headline_drift` naming exactly which number disagrees; a ledger publishing no total at all refuses `verdict=no_headline`, since a scan finding nothing to check must never answer ok about its own blindness (REDS %97's shape). `tools/gen/season/reds_ledger_monotone_witness.rish` now measures the spine, hands that measurement to the headline scan, and asserts agreement -- so the two halves are the mirrored pair the happy zone asks for at a seam: one side counts, the other publishes, and the contract check lives with the publisher. Proven both ways on metal: the scan refused all three drifted numbers on the living tree exactly as it stood this lap, and generated controls prove a drifting headline and an absent one both refuse by name. The witness prints a single GREEN line covering both halves, so no reader can stop at a GREEN that speaks for part of the subject (REDS %102). **And the guard refused its own author within the minute.** Booking this very row moved the spine from 126 to 127, so the headline corrected moments earlier went stale again on the same lap -- and the witness reddened by name rather than passing quietly, which is the whole argument for building it. **REDS %126's second half closes here too:** that row named a witness sitting in `tools/gen/season/` on no roster, with nothing to call it, and fixed only the equality. It is rung **9/9** of `tools/glow_tend_era_suite.rish` now -- the roster whose promise is to be provable on any host, which a guard that reads two files can keep. Released -- the class is guarded now rather than remembered.

**REDS %128 (`20260822.035612`) -- the record described a three-line stub as one line, and the arithmetic beside it stopped adding up.** *What went wrong:* the `lost_published` lift's own session log, its index row, and the living operator card each wrote that forty rungs now call the lifted body "through a one-line stub," and the log printed the fall as *39 x 20 became 39 x 1*. A delegating stub is three lines -- the signature, the return statement, and the closing brace -- which the eight folds before this one had all written correctly. The measured fall of 663 was right; every sentence explaining *why* it was 663 was wrong, and a reader redoing the arithmetic as published gets 741. The error rode a signed commit to both remotes. *What caught it:* opening the very next family. Weighing `press_path` at twenty-eight rungs by twenty-seven lines against the queue's printed 648 forced the subtraction by hand -- 27 x (27 - 3) = 648 rather than 27 x (27 - 1) = 702 -- and the stub length that made the printed number true was the one the prior sentence had just denied. Reading the stub on disk settled it in one command. *What it taught:* **a number that is measured survives a wrong sentence beside it; a number that is described does not.** The carry, the ceiling, and the delegate count all came from the scan and were all correct, because a meter printed them. The stub length was the one quantity in the whole round that a hand typed from a glance at a diff, and it is the one quantity that was wrong -- the same shape as REDS %110 and %127, now in a third dress. A quantity a sentence asserts about the diff belongs in the same sentence as the command that reads it back.

**REDS %128 CLOSED (`20260822.035612`) -- the living pin corrected, the dated log given an erratum beside its wrong line.** *The repair:* `crux/REMEMBER.md` and the `session-logs/README.md` index row now read *three-line stub* and *39 x 20 became 39 x 3*, since both are living surfaces. The session log keeps its wrong `obs` line exactly as written and carries an `obs ERRATUM` line beneath it naming the correct arithmetic -- a wrong belief stays visible as a wrong belief, and the correction sits beside it rather than over it. *What stands unchanged:* every measured number of the round -- carry 78,265, ceiling 78,271, fall 663, delegates 2,736, choir 107 -- because each came off a meter rather than off a memory, which is the row's own lesson demonstrating itself. *The proof, added `20260822.043742`:* this closure first rested on a prose edit alone, which the ledger's own completeness guard refused by name -- so the prevention the row booked is built and GREEN on metal. [`tools/ladder_stub_length_witness.rish`](../tools/ladder_stub_length_witness.rish) over [`tools/fixtures/ladder_stub_length_scan.sh`](../tools/fixtures/ladder_stub_length_scan.sh) spans the enclosing function of every `return ladder_checks.` call across the 79 caravan modules, reports the **2,141** pure delegating stubs and their measured minimum of **three lines**, and refuses `verdict=stub_claim_drift` naming the line whenever `crux/REMEMBER.md` writes a different number in words. A source tree holding no stub refuses `verdict=no_stubs` rather than blessing its own blindness, and both refusals fire on generated controls. It stands as rung 10/10 of `tools/glow_tend_era_suite.rish`, so the quantity is measured every lap rather than described once.

