# REDS -- row %123, folded from the living pin

**Folded:** `20260821.232733` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The positional-read round -- a guard that reads *the first thing shaped like X* will eventually grade the wrong thing, because a document's shape changes and a positional read does not.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guards
the round built, and the row itself stays one click away.

---

**REDS %123 (`20260821.210517`) -- a guard read the first thing shaped like a nib, and had been red for two days with nobody to notice.** *What went wrong:* `tools/nib_honesty_witness.rish` took *the first ten-hex run in the operator card* to be the Git nib. Condensing the card on `20260819` moved a **cairn** row above the nib line, so the guard began grading a **walk-back nib** against the nib's own ancestor-of-HEAD law -- a law a cairn cannot keep, since a cairn is off main **by construction** after a deep debride, which is the entire reason it was worth recording. It also read `work-in-progress/REMEMBER.md`, an untracked hardlink of the living `crux/REMEMBER.md` that no fresh clone carries. *What caught it:* running it by hand at a round close -- and only after mistyping its path from memory and having to go find where it actually lives. *What it taught:* **a witness on no roster and in no suite is a witness nobody runs.** It went red the lap the card was condensed, and no run ever asked it. And a guard that reads *the first thing that looks like X* will eventually grade the wrong thing, because a document's shape changes and a positional read does not.

**REDS %123 CLOSED (`20260821.210517`) -- the field is read by name, and the two voices answer to the laws they can keep.** *The repair, on metal:* the guard reads the **`Git nib:` field by name**, sweeps **every** advertised hash for the one law both voices share -- it resolves -- and **names** reachability per hash (on-main, on-remote, local-only) rather than grading a cairn against the nib's law. The nib's own HEAD/sibling/parent law is **cited**, not copied: `tools/remember_git_nib_witness.rish` owns it and reads GREEN. A planted floating-nib control proves the refusal on metal. It is **pier-side by design and deliberately outside the era suite**, whose promise is to be provable on any host -- a promise this guard cannot keep while four cairns live only here. **Open, and Keaton's word:** `3b8b0d4858`, `4a50722016`, `82b7ee3342`, and `d2368cb9db` are on **no remote** -- the cairn rule's promise that the departing card stays one `git show` away holds on this machine and nowhere else. Pushing the pre-debride branches would restore it and would also re-publish tissue a debride deliberately removed, so the guard **reports** it and crosses nothing.
