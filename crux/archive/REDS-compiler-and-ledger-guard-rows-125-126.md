# REDS -- rows 125 and 126, folded from the living ledger

*Folded `20260821.235936` under the living-pin bound (24,576 bytes). Every byte kept; nothing edited. The living ledger at [`../REDS.md`](../REDS.md) carries the pointer.*

**The range:** the compiler under everything, and the ledger's own guard -- a bridge that walked its import graph without a way to stop, and a growing count recited inside an equality.

**REDS %125 (`20260821.232733`) -- the compiler under everything walked its own import graph without a way to stop, and answered a fair question with a segfault.** *What went wrong:* `bridge_rye_tree` in `rye/src/main.rye` walks a root `.rye` file and every `.rye` it imports, staging each as a `.zig` the toolchain can read. Its visited set was appended **after** the walk into a file's own imports rather than before -- so while a cycle was being traversed **nothing was ever marked seen**, `bridge_already_staged` never fired, and two files importing each other recursed until the stack died. No named error, no diagnostic, no bound: a **segmentation fault** from the tool that builds Caravan's hundred and ten rungs, the crypto ladder, Aurora's freestanding seed, and Rye itself. Beneath it sat a second fault of the same family, visible only once the first was fixed -- the file the walk **starts** from bridged to `foo.rye.zig` while every dependency bridged to `foo.zig`, so a module that imported the root by name asked the toolchain for a file the bridge had never written. **One file had two names, and only a cycle ever asked.** And a third, plainer than either: **the Rye compiler carried no witness at all** -- 1,639 witnesses stand in this tree and not one of them was pointed at the tool underneath every other. *What caught it:* a carry fold on the Caravan ladder. Lifting the shared `main` wanted four base modules -- `cohort`, `carry`, `cycle`, `unhand` -- to import the harness that already imports them, so the question *does Rye allow a mutual import?* was asked **of the compiler** rather than of memory. It answered by crashing. Nothing in the tree holds a mutual `.rye` import today, so no scan of the tree could ever have found this; only a corpus written for the question reaches it. *What it taught:* **a visited set that records a node after its subtree finishes records nothing at all while a cycle is being traversed.** Post-order marking is correct for emitting a topological order and worthless for termination, and the two duties had been folded into one list. More broadly: **the deepest tool is the least-witnessed, precisely because everything above it is green.** Every witness that passes is evidence the compiler works on the graphs the tree happens to hold -- which is REDS %102's family at the very bottom of the stack, a GREEN over part of a subject answering in the voice of the whole.

**REDS %125 CLOSED (`20260821.232733`) -- the walk marks on entry, names its bound, and the root answers to one name.** *The repair, on metal:* the visited set is appended **before** the imports are walked rather than after, which is what a visited set must do to terminate on a cyclic graph -- and which also makes a mutual import **legal**, as Zig's own file-as-struct model already allows. Beside it a named bound, `max_bridge_depth = 1024`, refuses a pathological chain with `error.BridgeTooDeep` and a printed diagnostic rather than the stack's end (TAME root rule 1: no unbounded recursion). `bridge_zig_path` now names every file the same way, root included, so the walk can reach its own start by the name the rewriter writes. The guard stands where none stood: `tools/rye_bridge_cycle_witness.rish` over `tools/fixtures/rye_bridge_cycle_control.sh` builds a throwaway corpus whose answer is known by construction -- two files importing each other bridge, compile, link, and **run**, with a value crossing the cycle and returning unchanged; the root is reached **by name** from its own dependency; a chain of 1,026 refuses **by name** at the bound, read off the source rather than recited; and every staged `.zig` is swept, so a bridge run leaves its corpus as it found it. Both legs proven on metal: the **previous** binary segfaults (code 134) on the exact corpus the fixed one builds. Registered the same lap it was written, per REDS %81 -- it is rung **8/8** of `tools/glow_tend_era_suite.rish`, the roster whose promise is to be provable on any host, which is a promise a compiler guard can keep. No regression: all **107** Caravan witnesses GREEN under the fixed compiler, `tame_style_check` and `width-check` clean.

**REDS %126 (`20260821.232733`) -- the ledger's own guard reds on the ledger doing its job, and had been red for a day with nobody to run it.** *What went wrong:* `tools/gen/season/reds_ledger_monotone_witness.rish` asserted `rows=121` as a literal equality. The ledger only ever grows, so the guard went RED the moment row %122 landed -- not because a spine broke, but because a number that changes on every correct lap had been recited inside an assert. It stayed red through %122, %123, and %124 while every one of those rounds closed GREEN, because the witness sits in `tools/gen/season/` on no suite roster and nothing ever called it. *What caught it:* running it by hand while booking %125, since a round that touches the ledger should ask the ledger's own guard. *What it taught:* **a count that grows belongs in a floor, never in an equality.** The literal was guarding something real -- a spine that stops reaching every row is a genuine red, and this one once read 73 of 101 and said ok -- yet an equality guards the floor and forbids the growth in one stroke. And REDS %123's lesson lands a second time in two days: **a witness on no roster is a witness nobody runs**, so a lantern that fires twice becomes a loom.

**REDS %126 CLOSED (`20260821.232733`) -- the guard keeps its floor and welcomes the climb.** *The repair, on metal:* the assert reads `rows >= 121` rather than `rows = 121`, so a shrinking spine still refuses by name while a ledger that grows passes as it should. The exact count stays printed by the scan, measured off disk rather than recited (REDS %110). Both legs proven: GREEN at the true 125, and RED by name when the floor is lifted above what the spine reaches. The header's own tally moved to the measured 125 in the same pass.
