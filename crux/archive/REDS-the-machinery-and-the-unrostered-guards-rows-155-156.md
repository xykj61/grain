# REDS -- the machinery that read the elder room, and the guards off the roster (rows %155-%156)

**Language:** EN
**Style:** Radiant (see [`../../context/RADIANT_STYLE.md`](../../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- a closed range of the living ledger, every byte kept
**Folded:** `20260823.025333` -- carried here from [`../REDS.md`](../REDS.md) when the living pin
passed its 24,576-byte bound as row %159 was booked. Row %156's coverage question -- which
witnesses belong on the roster -- is carried forward by the living ledger rather than left here.

---

**REDS %155 (`20260823.010937`) -- the breach swept the prose and left the machinery reading the elder room.** *What went wrong:* one lap after `REDS %153` repaired the twenty-six document links resolving through the untracked `work-in-progress -> crux` symlink, a census of path literals inside **tracked tool sources** found **116 more across 58 files**, every one reading a room the repository does not carry. `tools/fixtures/reds_first_scan.sh` set `ledger="work-in-progress/REDS.md"`, so the reds-first guard read the ledger through the symlink. `tools/compass_rose.rish` printed `NOW_OK` from a bare `test -f work-in-progress/TASKS.md` -- the `[ -e ]` class again, in the compass itself. `tools/fixtures/radiant_lint_scan.sh` named three living cards there, so the Radiant lint's roster reached through the symlink for eight days. And `.brix`, the descriptor listing the bricks Mantra tracks, carried `file work-in-progress/ROADMAP.md` and `file work-in-progress/TASKS.md`. *What caught it:* a census written this lap in answer to the question `REDS %153` closed on -- which other guards read the filesystem where they mean to read the repository. *What it taught:* **a sweep scoped to prose leaves the machinery behind, and machinery is where a broken reference stops being visible.** A stale link in a document shows a reader a 404; a stale path in a guard shows a reader GREEN. The stamp-and-name law already says living code is repointed rather than resolved, and a repoint that reaches only documents has repointed half a tree.

**REDS %156 (`20260823.010937`) -- two guards stood red at HEAD for thirteen days, because neither is on the roster that gets run.** *What went wrong:* running the guards this lap touched found `tools/width_check_th4.rish` demanding `.brix` carry `version 20260629.004912` when the descriptor molted to `20260810.145743` on `20260810`, and `tools/compass_rose.rish` demanding the word `superseded` in a settlement-seat document that a Radiant pass had reworded to `replaced`. Both were red **before** this lap touched anything, proven by stashing the round and re-running against HEAD. *What caught it:* running them -- which happened only because the phantom-path repoint touched both files. *What it taught:* **a guard off the roster is a guard nobody runs, and a guard nobody runs is prose.** This is the third firing of one lantern: `REDS %147` found eight standing witnesses drifting behind their code, `REDS %150` seated a roster of twenty and its first run found three reds, and here two more sat red for thirteen days outside it. The roster holds **twenty-two** rows against a tree of **1,653** witnesses, so the loom is real and its coverage is the open question.

**REDS %155 CLOSED (`20260823.010937`) -- a tool reads a path the repository carries, or it reads nothing.** *The repair, on metal:* all 116 literals repointed `work-in-progress/ -> crux/` across the 58 tracked sources and `.brix`, applied only to **non-comment** lines so each guard keeps the prose explaining the breach that shaped it. The loom is [`../tools/phantom_path_witness.rish`](../tools/phantom_path_witness.rish) over [`../tools/fixtures/phantom_path_scan.sh`](../tools/fixtures/phantom_path_scan.sh), which reads every path literal on a non-comment line of a tracked `tools/**.rish`, `tools/**.sh`, or `.brix` and gates at zero every one that resolves on this filesystem while standing outside the tracked tree. Four readings pass free by named rule: `vendor/`, provisioned by submodule or `fetch_toolchain`; `seed/`, the depersonalized projection, gitignored by design; any path carrying a dot-segment, since a dot names a generated or machine-local room; and a literal resolving **nowhere**, whose duty belongs to `tools/dated_path_witness.rish` -- one duty, one guard, so two can never disagree. A literal travelling through a **tracked** symlink resolves for a reader who clones and passes free, composing with the promise `tracked_link` already proves. [`../tools/fixtures/phantom_path_control.sh`](../tools/fixtures/phantom_path_control.sh) builds real git repositories in a temporary pen and proves eight behaviors -- two refusals bitten and six honest readings left free, the comment case among them, since a scan that gated its own explanation would delete the record of why it exists. GREEN on metal: `sources_read=2670`, `path_literals=7706`, `phantom_paths=0`. The guard stands as the roster's twenty-third row.

**REDS %156 CLOSED in part (`20260823.010937`) -- both pins repaired per sense; the coverage gap stays open and named.** *The repair, on metal:* `tools/width_check_th4.rish` now pins the living `version 20260810.145743`, with the molt named in a comment beside it so the next reader knows the elder value was real rather than wrong. `tools/compass_rose.rish` now matches `supersed|replaced`, binding to the supersession **fact** the document states rather than to one spelling a Radiant pass is free to improve -- the same per-sense repair `REDS %151` chose over a second abstract word. Both GREEN on metal. **What stays open, stated rather than implied:** twenty-three rostered guards against 1,653 witnesses means the roster is a sample, and nothing yet chooses which witnesses belong on it. That is the next crux, and it is named here rather than quietly carried.
