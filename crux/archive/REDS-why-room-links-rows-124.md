# REDS -- row %124, folded from the living pin

**Folded:** `20260821.232733` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The why-room round -- a classification rule inherits the blind spot of the thing it classifies by, and a resolver's census answers an agent's question rather than a reader's click.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guards
the round built, and the row itself stays one click away.

---

**REDS %124 (`20260821.230319`) -- the room written for the long return was the one room every meter walked past.** *What went wrong:* on `20260821` five rooms folded to `date/YYYYMMDD/` and six essays graduated into `foundations/`, and **thirty-eight Markdown links broke across twenty-four living foundation files** -- including **step 2 of the compass rose itself**, the tree's own navigation hub, pointing at a path that no longer existed. Three tools read that room and each correctly saw something that was not its job. `tools/dated_path_repoint.rish` classifies by basename, so a file whose name carries a one-clock stamp is testimony and is never rewritten -- exactly right for a session log, and exactly wrong for a living foundation, whose whole room is stamp-named by convention and living by status. `tools/dated_path_witness.rish` grades the **resolver**, so a stale reference an agent can recover counts as RECOVERABLE and the census stayed honest at 9,697 -- while a reader clicking a link has no resolver. And `tools/living_docs_lint.rish` does own a broken-link duty, over a roster that never included `foundations/`. *What caught it:* the council rota. Row 0 is Jupiter and aether -- *why the work exists* -- and reading `follow-our-compass.md` end to end for the rota meant reading its own step 2, which named a file that had graduated out from under it that afternoon. A rota read as reading, rather than as a filename ticked off, found what four green meters could not. *What it taught:* **a classification rule inherits the blind spot of the thing it classifies by.** Basename-as-testimony is a good rule that silently exempts an entire living room, because that room's naming convention and its liveness are independent facts and only one of them is in the filename. And more plainly: **the resolver's census answers an agent's question, never a reader's click.** A reference an agent can recover is a reference a reader still cannot follow, so the two want two meters.

**REDS %124 CLOSED (`20260821.230319`) -- the why-room's links land, and the room is a check now rather than an assumption.** *The repair, on metal:* all fifteen distinct broken targets resolved unambiguously by measurement rather than guess, and every one of the thirty-eight living citations was repointed -- the graduated essays to their `foundations/` home, the folded ones to `date/YYYYMMDD/`, `../CIVIC_STYLE.md` to `../context/CIVIC_STYLE.md`, and one sibling in `foundations/yonder/` that had been citing itself through its own parent. Dated testimony elsewhere in the tree keeps every word it wrote and stays resolvable by `dated_path_resolve.rish`; only the living room moved. The guard stands: `tools/foundations_link_witness.rish` over `tools/fixtures/foundations_link_scan.sh` reads **73 files and 332 clickable links at zero broken**, trims an anchor before checking since `file.md#section` is a claim about `file.md`, and bounds the walk at 512 files and 8,192 links. Both legs are proven -- `tools/fixtures/foundations_link_control.sh` builds a throwaway room whose answer is known by construction and watches a moved target refused **and named** while a folded target, an anchored target, an http link, and a bare anchor all pass -- and the enforce leg was proven a second time on the **real** room, by planting a link in `foundations/README.md`, watching the scan name that file and refuse, and restoring it byte-identical. **A class is guarded on the lap it becomes a check, never on the lap it becomes a rule.**
