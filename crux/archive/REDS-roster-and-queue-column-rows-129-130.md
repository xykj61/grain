# REDS -- rows %129 and %130, folded from the living pin

**Folded:** `20260822.092442` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two rows from the Caravan ladder arc, one day apart, both about the same fault in different clothes: a roster that proved a number rather than a body, and a queue column a hand described where a meter should have counted. Each closed on a guard that runs every lap.*

Each row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read and stays under its named bound; the
lesson travels forward in the guards these rounds built, and the row itself stays one
click away.

---

**REDS %129 (`20260822.043742`) -- a roster named the whole ledger and proved only its numbering, so an empty closure rode four signed commits.** *What went wrong:* two guards watch `crux/REDS.md` and only one is on a roster. `tools/gen/season/reds_ledger_monotone_witness.rish` counts the spine and checks the headline, and it is rung 9 of the era suite. `tools/gen/season/reds_ledger_witness.rish` reads every row for its three fields -- what went wrong, what caught it, what it taught -- and holds a closure note to naming a proof rather than a claim, and it sits on no roster at all. Commit `ac68074042` closed REDS %128 with a closure note that named no witness, turning the completeness guard RED; three further lifts landed on top of it and all four rode signed to both remotes, while every lap's own Standfast reported the era suite GREEN at nine. The rung's line reads *the reds ledger itself*, so the roster promised the whole subject and delivered half of it. *What caught it:* running `tools/gen/season/reds_ledger_witness.rish` by hand at the top of this lap's Standfast rather than trusting the suite that claims to cover it, then bisecting the four commits with the scan against `git show <nib>:crux/REDS.md` to find the exact commit where ok became `incomplete_rows`. *What it taught:* **a rung's name is a promise about coverage, and a rung that names a subject broader than the guard it runs is a GREEN speaking for something nobody checked.** This is REDS %102's shape one level up: that row asked a single witness to cover its whole subject, and this one asks a *roster* to. The repair is arithmetic rather than judgment -- every guard watching a subject the roster names runs at that rung, and a guard on no roster is a guard nobody will run twice.

**REDS %129 CLOSED (`20260822.043742`) -- both halves run at the rung, and the suite counts ten.** *The repair, on metal:* era rung 9 runs the completeness witness beside the monotone one and asserts both, so the line that says *the reds ledger itself* now proves the spine, the headline, and every row's three fields together. Rung 10 joins it with `tools/ladder_stub_length_witness.rish`, the prevention REDS %128 booked. The suite renumbers 1/9 through 9/9 to 1/10 through 10/10 in the same pass, since a rung count carried in prose drifts the moment a rung is added -- the same shape as %127's headline. Proven by running the whole suite on metal after the ledger was made honest: ten rungs GREEN end to end, and the completeness witness refused the tree by name before the repair, which is the argument for putting it on the roster rather than merely writing it.

**REDS %130 (`20260822.054053`) -- the fold queue measured one column and described the other, and the described one sent four laps past the largest fall on the board.** *What went wrong:* every lift of the Caravan ladder arc weighs a family by two numbers -- the lines it returns, and the declarations it would have to widen, since a body moving into `caravan/ladder_checks.rye` reaches every helper through the rung handed in and a private helper is a compile error there. The first number has come off `tools/fixtures/caravan_ladder_carry_scan.sh` since the arc opened. The second was typed into the queue's prose by a hand, and it was computed from **the family's own visibility** rather than from what the family's **body reaches**. So `renew_path` and `ask_path` were each recorded as widening thirty-one declarations, because `fn renew_path` stands private in all thirty-one rungs -- while both bodies reach only `max_note_path_len`, `NoteError`, `addressable`, and `reader_dir`, every one of them already public in every rung. Their true widening cost is **zero**, and `renew_path` returns **720**, the largest realized fall standing. Four laps chose 612, 629, 646, and 531 over it on that reading. The same column also said `confirm` would widen exactly one declaration because `read_count` stood private in one of forty-six rungs; `read_count` is **absent** from that rung -- `entrust.rye`, whose `confirm` waits on a different note through a different reader, so it was never a fold candidate and never a cost. *What caught it:* building the meter rather than reading the sentence. Weighing `confirm` at the head of this lap meant checking the visibility of its three reached symbols across the forty-five identical rungs by hand, finding all three already public everywhere, and noticing that answer disagreed with the queue's own printed prose. Running the same reading across the whole queue found the two zero-cost heads. Confirmed a third way by hand, one `grep` per symbol per rung. *What it taught:* **a family's own door says nothing about what its body reaches, and the arc had already written that sentence down without ever turning it into a meter.** A prior lap's log records the lesson in exactly those words while the queue beside it kept computing the widening count from the family's own visibility anyway. That is REDS %128's shape a second time -- a number a meter prints survives a wrong sentence, a number a hand describes does not -- and the honest repair is the same one: the column becomes a measurement. A lantern that fires, gets written into a log, and changes no tool is a lantern that will fire again.

**REDS %130 CLOSED (`20260822.054053`) -- the widening column is a meter now, and it names the fold line as it counts.** *The repair, on metal:* [`tools/fixtures/ladder_reach_visibility_scan.sh`](../tools/fixtures/ladder_reach_visibility_scan.sh) takes a family name, hashes every body written under it across the ladder, takes the largest identical set, drops the rungs standing **below the fold line** (the ones `caravan/ladder_checks.rye` itself imports, since folding a rung the harness reaches for inverts the ladder), reads the identifiers the shared body reaches **unqualified**, and reports how many of those each folding rung keeps private -- printing `REACH_OK family=<name> folding=<n> widens=<n> stub=<n> fall=<n>`. Three readings the first cut got wrong were each caught by running it: a `join` under the host locale read its own sorted input as unsorted and answered with garbage, so one collation is set for the whole scan; the opening triad `std` and `assert` were counted as rung API, so they are excluded by name; and an invariant comment reading *a confirmation always waits on reach* put the word `waits` in the reached set, so comments and string literals come off before any identifier is read. The fall is derived rather than assumed too -- the stub is the measured signature length plus two, and a family splitting at the fold line loses a whole body's carry rather than shrinking it to a stub, which is why this lap's own realized fall was 531 where the flat formula gives 516. [`tools/caravan_ladder_reach_visibility_witness.rish`](../tools/caravan_ladder_reach_visibility_witness.rish) asserts the lifted family, both zero-cost heads, and both refusals -- a name no rung declares refuses `verdict=no_family`, a name whose every body differs refuses `verdict=no_identical`, and both fire on generated controls rather than on the living ladder. It stands on the Caravan roster, whose bijection guard proves every `caravan_*_witness.rish` on disk is registered, so the column is measured every lap rather than described once.
