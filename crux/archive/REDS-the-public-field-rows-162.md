# REDS -- the depersonalisation that protected a public field (row %162)

**Language:** EN
**Style:** Gauge -- Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Status:** Archive -- CLOSED by Keaton's word on `20260823.045448`, every byte kept
**Folded:** carried here from [`../REDS.md`](../REDS.md) as row %163 was booked.

**Resolution:** Keaton chose the first of the three named resolutions. `xykj61/grain` and
`autoproject96/grain` were set **PRIVATE** on `20260823.045448`, verified by query.
`grain-os/grain` stays public and will be republished from the private field when the standfast
of `%163` lifts. No history was rewritten and no signature changed, so the cost was the smallest
of the three.

---

**REDS %162 (`20260823.034321`) -- OPEN, GATE -- the depersonalisation protects a field that is already public.** *What went wrong:* `.claude/rules/git-signing.md` withholds the maintainer's GPG signature from the public seed on the explicit reasoning that signing *"would cryptographically link the anonymous seed back to the maintainer -- defeating the whole point of depersonalization."* Checked directly on `20260823.034321`, **all three remotes read `PUBLIC`**: `grain-os/grain`, `xykj61/grain`, and `autoproject96/grain`. The field the seed is depersonalised *from* is itself world-readable, and it tracks `PUBKEYS.md`, a resume draft, a personal ontology, a cover letter, and the `twilight/` poems -- material the manifest carefully `sub_exclude`s from the seed while the field publishes it anyway. Content matching links the two repositories in one search, so the unsigned commit buys nothing it was meant to buy. *What caught it:* a visibility check run before siloing a letter containing family financial details -- the letter was placed in an **untracked** room instead, since seed-withholding alone would have published it. *What it taught:* **a privacy control is worth exactly the weakest boundary it depends on**, and this one depends on an assumption never written down and never checked. *Why this row is a gate rather than a repair:* the resolutions -- make the field private, accept the linkage and restore signing, or debride the personal material -- are all Keaton's word, and two of them touch published history. Named plainly and left standing.
