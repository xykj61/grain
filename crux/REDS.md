# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Radiant (see [`../context/RADIANT_STYLE.md`](../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Season - opened `20260729`

*Rows 1-24 folded to [`archive/REDS-voice-season-rows-1-24.md`](archive/REDS-voice-season-rows-1-24.md) on `20260810.171343` -- closed-season fold under the living-pin bound; every byte kept, nothing removed.*

*Rows 25-57 folded to [`archive/REDS-voice-season-rows-25-57.md`](archive/REDS-voice-season-rows-25-57.md) on `20260811.143004` -- the same fold, the same discipline: the dense Voice-Season counsel arc (`20260730`-`20260801.033305`) moved to the archive, every byte kept, the living pin brought back under 24576.*

*Rows 58-73 folded to [`archive/REDS-self-work-yield-rows-58-73.md`](archive/REDS-self-work-yield-rows-58-73.md) on `20260815.210520` -- the same closed-range fold: the self-work-and-Yield arc (`20260801.145854`-`20260812.191206`) moved to the archive, every byte kept, the living pin brought back under 24576, and the monotone witness taught the new archive in the same act (REDS %67's own lesson).*

*Rows 74-79 folded to [`archive/REDS-image-crypto-rows-74-79.md`](archive/REDS-image-crypto-rows-74-79.md) on `20260817` -- the same closed-range fold: the image-and-crypto arc (`20260813`-`20260816`) moved to the archive, every byte kept, the living pin brought back under 24576 after the ledger crossed its bound. Living rows 80, 83, 84, 85 and every prose note remain below. **Living rows 80 onward below.***

*Rows 80-87 folded to [`archive/REDS-microkernel-arc-rows-80-87.md`](archive/REDS-microkernel-arc-rows-80-87.md) on `20260820.002705` -- the same closed-range fold: the image-crypto and Microkernel-arc range moved to the archive, every byte kept, the living pin brought back under 24576 before rows 88-90 were booked. **Living rows 88 onward below.***

*Rows 88-95 folded to [`archive/REDS-caravan-arc-rows-88-95.md`](archive/REDS-caravan-arc-rows-88-95.md) on `20260820.030003` -- the same closed-range fold: the Caravan-arc and Two-Rooms range moved to the archive, every byte kept, the living pin brought back under 24576 before row 96 was booked. **Living rows 96 onward below.***

*Rows 96-100 folded to [`archive/REDS-one-clock-and-ladder-fold-rows-96-100.md`](archive/REDS-one-clock-and-ladder-fold-rows-96-100.md) on `20260820.232126` -- the same closed-range fold: the one-clock-head and ladder-fold range moved to the archive, every byte kept, the living pin brought back under 24576 after row 101 was booked. **Living rows 101 onward below.***

**REDS %101 CLOSED (`20260820.232126`) -- a meter was born, proved itself GREEN, and stood outside the choir for two laps.** *What went wrong:* `tools/caravan_ladder_print_witness.rish` -- the third ladder meter, the one that made the printing carry a number two honest meters could not see -- landed on `20260820.221349` and was never added to the roster `tools/caravan_suite_witness.rish` sings from. Its two kin, the copy and spine meters, both stand registered; this one did not. So the choir's own bijection guard was RED from the moment the meter was born, and three sends went by -- the printing meter's own lap, fold E, and fold F -- each running the rungs it touched and none pulling the choir. *What caught it:* running the whole Caravan witness set from a cold tree during this lap's fold, where 98 witnesses answered GREEN and `caravan_suite_witness` alone refused, naming `caravan_ladder_print_witness` in one line. The guard was right, it was specific, and it had been waiting two laps for somebody to ask it. *What it taught:* **this is REDS %81 in a second module, which makes it a lantern that has fired twice.** There the nine crypto rungs stood unregistered in their suite and the count guard caught it the moment it was run; here the printing meter stood unregistered in its choir and the bijection caught it the moment it was run. The shared root is neither guard: it is that **a per-rung send runs the witness it wrote and the choir costs minutes**, so the cheap question rides on the expensive answer and gets asked only when somebody has the minutes. Closed at the root and as a loom. The root: the meter is registered, and the choir sings 98 GREEN in one voice. The loom: the bijection lifted out of the choir into [`tools/fixtures/caravan_roster_bijection_scan.sh`](../tools/fixtures/caravan_roster_bijection_scan.sh), which answers in two greps, and [`tools/caravan_ladder_copy_witness.rish`](../tools/caravan_ladder_copy_witness.rish) -- the meter every fold lap already pulls -- now asks it every lap, so the question is separated from the cost of the answer (REDS %97's shape: a guard nobody runs drifts from the thing it guards). Both consumers read the one scan, so the roster is one question answered in one place. Three RED paths proven on metal rather than reasoned, each generating its own control so no control can drift from the shape it guards: a witness on disk that no roster names is refused **by name**, a roster line with no file behind it is refused, and a directory holding no witnesses at all refuses rather than reporting a whole roster over an empty tree. `caravan_ladder_copy_witness` GREEN at ten RED paths, `caravan_suite_witness` GREEN at 98. Released.

**REDS %102 CLOSED (`20260820.232126`) -- the ledger's own spine guard proved 73 rows of 101 and called it whole.** *What went wrong:* `tools/gen/season/reds_ledger_monotone_witness.rish` proves REDS row indices accrete 1..N with no gaps, and it did so over a **hand-written list of fold archives**. Three folds landed without ever being added to that list, and the paths it did name still pointed at the retired `work-in-progress/` directory, resolving only through the compatibility symlink the `crux/` rename left behind. Beneath that, the scan it calls read only the **elder table shape** -- a line opening on a digit cell -- while every row from %81 forward is prose, exactly the blindness REDS %97 found and repaired in the sibling `reds_ledger_scan.sh` and never swept into this one. The two faults hid each other perfectly: the truncated list happened to name only archives whose rows were all table-shaped, so the scan's blindness never showed, and it printed `verdict=ok` over a spine reaching row 73 of the 101 that exist. *What caught it:* folding rows 96-100 to bring the living pin back under its bound, then repointing this witness at the archives that actually stand on disk -- at which point it refused at row 81, naming a row it had never been able to see. The fold is what made the guard read its whole subject for the first time. *What it taught:* **a guard that reads a set somebody maintains by hand will eventually guard a set that no longer exists** -- the `%81`/`%87`/`%97` family once more, and the deepest form of it yet, because here the stale set silently *narrowed the guard's subject* rather than disabling the guard. A GREEN over a subset is more dangerous than a RED, since it answers the question nobody asked in the voice of the question they did. Closed at both roots and hardened. The list is gone: the archives are **discovered on disk** by glob, so a fold joins the spine on the lap it lands rather than the lap somebody remembers, and the glob is expanded by the shell rather than interpolated from captured output (REDS %85's loom). The scan learned the **prose shape**, unioning both kinds, and it now counts the **distinct** spine while printing the mention count beside it -- because a closure note honestly keeps the number of the row it speaks about, and counting mentions would refuse a perfectly whole ledger, which REDS %100 named as its own kind of fault. The witness states what it reached (`rows=101`), so a spine that silently narrows again is a red rather than a quieter GREEN. Four paths proven on metal with generated controls: a gap refused **by the exact row it is missing**, a wholly-prose ledger counted rather than read as empty, a row and its own closure note welcomed as one row, and a ledger holding nothing refused by name. Spine GREEN at 1..102 across seven archives and the living pin. Released.

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**Rows: 106 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 81** -- counted from the ledger and its archives on `20260820.030032` rather than carried forward, since a tally repeated from memory drifts (REDS %93). Rows 1-24, 25-57, 58-73, 74-79, 80-87, and 88-95 folded to `archive/`, and the closed prose notes for 73, 74, 75, 78, 80 folded beside them on `20260819` ([`archive/REDS-closed-prose-notes-73-80.md`](archive/REDS-closed-prose-notes-73-80.md)). Every number from 1 to 102 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**Reds-first accounting for v11:** two reds found, both fixed in-round with witnesses on metal, ledger closed. The remaining journey allocation is therefore **released** rather than booked -- which is the law working, not the law skipped.

**Reds-first accounting for the two-grain projection (`20260808.183836`):** two reds (61, 62), both real key-material leaks the seed witness caught before any public push, both fixed in-round by hardening the projection guard to match the witness, GREEN on metal after. Allocation **released** -- the witness held the line exactly where it was built to.

**Reds-first accounting for AHOY2 (`20260811.225629`):** REDS %69 -- a pre-existing seed-boundary coverage gap (five root dirs unclassified since recent seasons) surfaced while adding the top-level `LICENSE`. Classified on Keaton's word -- `assets`-`brix`-`pleac` allowed (template, clean code + logo), `journey`-`research-silo` withheld (personal) -- and `sow_witness` returns **SOW_WITNESS_GREEN** (M1_OK - IDENT_CLEAN - NO_PERSONAL, copied 4092 - scrubbed 780 - withheld 22). Closed with a witness on metal, same arc. Allocation **released**.

**Reds-first accounting for GISM-J5r1 (`20260812.181015`):** REDS %72 -- the first real corpus the reading voice read tripped `BlockTooLarge`, exactly the Yield red Season 2 exists to surface. Found by the real read, fixed in-round by raising `max_block_text` to admit real reference-doc paragraphs with headroom (`tools/gism_j5_corpus_real_witness.rish` GREEN), the demo reading re-run GREEN. Allocation **released** -- the round's own real bytes caught the bound the demo never exercised.

**Reds-first accounting for GISM-J6r1 (`20260812.185056`):** REDS %73 -- the first real document carried through the owner-signed catalog refused `ContentTooLarge`, the Yield red Season 2 exists to surface. The refusal is proven on metal in-round (`tools/gism_j6_provenance_real_witness.rish` GREEN), and the chain of custody stands GREEN over a small real artifact (a waymark green-claim) the same round. Unlike #72, the fix is **not** a bound-bump -- `max_resin_bytes` is structurally capped -- so the allocation is **booked** toward the chunking-storage horizon (large artifacts across ordered resins, a later GISM rung), the honest cost named rather than a raise pretended.


**REDS %81 (`20260816.183311`) -- the loom fired, unheard: nine crypto rungs on disk, none registered.** *What went wrong:* the ML-KEM ladder (`mlkem_sample` - `mlkem_keygen` - `mlkem_encaps` - `mlkem_decaps`) and the ML-DSA ladder (`mldsa_ring` - `mldsa_encode` - `mldsa_sample` - `mldsa_keygen`) each landed as its own GREEN per-file witness, yet none was added to `crypto_suite_witness`'s registration list -- so the count guard's bijection had drifted eight modules wide before the signing rung even opened, `crypto/*.rye` files (70) exceeding suite registrations (61). *What caught it:* `tools/crypto_count_guard_witness.rish` -- the very loom REDS %80 built for this exact recurrence -- went RED naming the first unregistered file the moment it was run this round. The guard worked; what failed was running it. *What it taught:* a per-rung send must run the **count guard** (or the whole suite, which now embeds it), not only the new module's own witness -- a rung that proves itself but never joins the choir is invisible to the one check built to see it. Closed the same round: all nine (the eight prior plus `mldsa_sign`) registered in dependency order, guard re-GREEN at **71**. A loom only guards what actually pulls it.




**REDS %81 CLOSED (`20260817.183713`) -- the loom now hears every rung:** the nine ML-KEM and ML-DSA rungs that had landed as their own GREEN per-file witnesses without being added to `crypto_suite_witness`'s registration are all registered now, and the fix is proven not by re-reading but by the loom REDS %80 booked -- `crypto_count_guard_witness` runs **GREEN** over an exact **bijection**: 80 `crypto/*.rye` files, each registered in the suite and each registration backing a real file, so an unregistered rung would go RED naming itself. The registration gap #81 named is closed and now guarded against recurrence. Released -- the gated-reds sweep proved it on metal.


**REDS %83 prevention LANDED (`20260817`) -- the booked living-card ASCII guard stands on metal.** *What it closes:* REDS %83 fixed the mojibake and taught the ASCII-first rule, then **booked** a living-card non-ASCII witness so the class could never enter the operator card unseen again. That witness is now built: `tools/living_card_ascii_witness.rish` over `tools/fixtures/living_card_ascii_scan.sh` greps the ENFORCE roster (`crux/REMEMBER.md`, `crux/REDS.md`) for any byte above 0x7F and fails hard if one appears, while reporting the pins still holding legacy dated non-ASCII (ROADMAP, TASKS, SEAT_MAP, SHRED_PREP, THREADS, CAIRNS -- 244 lines total this lap) as an advisory ratchet to sweep down on touch rather than force-rewrite. A tracked mojibake control (`tools/fixtures/living_card_ascii_control/mojibake_control.md`, real high bytes) proves the RED path on metal. GREEN in-lap. The lantern that fired once (the operator-card corruption) is now a loom -- the line need never stop for it a third time (`foundations/20260816-214652_standfast-the-stopped-line.md`). Released.



**REDS %103 (`20260821.014514`) -- the card named the round before it, because the push came before the pin.** *What went wrong:* `.claude/rules/remember-git-nib.md` seats one order for a send -- stage `crux/REMEMBER.md` with the round's work, commit signed, then amend **at most once** so **Git nib** names `git rev-parse --short=10 HEAD`, and never push a pin-only follow-up. Fold L's send ran commit, then push, then noticed the pin, which puts the only sanctioned repair behind a force-push that `.claude/rules/send-word.md` forbids without Keaton's word. So the operator card spent the lap naming `c352a3b7ee`, the commit *before* the round it describes -- a whole commit of lag rather than the one amend the rule tolerates. *What caught it:* reading the rule's own sequence after the push rather than before it, and comparing the card's nib against `git rev-parse HEAD` on metal. Measurement, one command, after the fact. *What it taught:* **an ordered rule is only kept if the order is read before the first step, not after the last.** The amend is not a tidy-up at the end of a send; it is a step *inside* it, and a send that reaches `git push` has already spent its chance to take that step cheaply. Repaired the way the rule actually allows: the nib is corrected in the working tree and rides the **next** work commit, since a pin-only follow-up is exactly what the rule exists to refuse and a force-push over a shared remote is a word only Keaton speaks. Booked here rather than fixed quietly, because REDS %84 taught that a fix nobody records is a fix nobody can trust.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

**REDS %84 CLOSED (`20260819.145920`) -- the fix landed, the ledger never said so.** *What went wrong, past the row above:* the `crux/` allowlist repair shipped in `a196bad32b` (*gitignore: allowlist crux, the renamed priority folder*), and no closure note was ever written beside row 84 -- so the living ledger read as though the tree's highest-priority folder were still silently ignoring every new untracked file. A fix nobody records is a fix nobody can trust. *What caught it:* a Standfast pass reading the living rows and then **measuring** rather than believing either the row or the repair -- `.gitignore` line 70 carries `!/crux/`, and `git check-ignore` on a fresh file under `crux/` answers not-ignored on metal. *What it taught:* **the ledger is part of the fix.** A red closes on a witness *and* on the line that says so, since the next reader inherits the ledger rather than the commit that quietly repaired it. Closed here on measurement, with the check named so a future reader can re-run it in one command.

**REDS %98 CLOSED (`20260820.171127`) -- a rung reached past its neighbor, and 195 lines rode the ladder for it.** *What went wrong:* `caravan/desist.rye` was written as a copy of `caravan/mind.rye`, which is what makes the parity of every rung beneath it readable -- and it inherited `mind.rye`'s delegations **whole**, so all 39 of its folded checks named `forbearing_rung`, the rung **two** steps down, skipping `mind.rye` entirely. Every other rung on the ladder folds into the rung directly beneath it. Two rungs naming one rung below means two rungs holding the same five-line stub byte for byte, and the copy meter counted every one of them: 195 carried lines, plus four real bodies `desist` copied from `mind` and never lifted. The chain also stopped being readable -- a reader following `desist` down landed past the rung whose parity it was written to show. *What caught it:* the copy meter's own number. `desist` closed its lap at 622 lines a rung and named the lift as the next crux; reading **which** bodies those lines were, rather than trusting the total, showed 85 of the 89 duplicated bodies were identical five-line stubs -- and the only way two rungs write the same stub is if they name the same rung. Measurement beats memory, and reading the parts of a measured number beats reading its sum. *What it taught:* **a rung born as a copy of the rung beneath it inherits that rung's reach, not its own.** The fold's whole discipline is one step down, and nothing in the tree ever said so where a machine could check it. Closed three ways: `desist`'s 39 folds now name `minding_rung`, one step down, so the comment above each stub ("the rung below owns this check") is true where it was a small lie; the four copied bodies -- `check_mind`, `check_mind_refusals`, `check_forbear_measure`, `check_forbear_wire` -- lifted into `caravan/ladder_checks.rye`, the harness a rung hands itself to; and the rule became a guard, [`tools/fixtures/caravan_ladder_reach_scan.sh`](../tools/fixtures/caravan_ladder_reach_scan.sh), which refuses any rung folding into more than one rung and any rung folded into by two. Proven on metal three ways: a three-rung PASS corpus, a planted skip corpus refused as `reason=shared_target`, and the **living tree as it stood this morning**, where the guard names `forbearing_rung is folded into by more than one rung: desist mind`. The carry falls **2,574 to 2,202**, fold B climbs 484 to 492, fold A holds at 679, and the choir sings 89 rungs GREEN from a cold tree. `tools/caravan_ladder_copy_witness.rish` GREEN.

**REDS %103 CLOSED (`20260821.020803`) -- the pin rode inside the send, exactly where the rule puts it.** *The repair, done rather than promised:* fold M's send read `.claude/rules/remember-git-nib.md` **before** its first step rather than after its last, staged `crux/REMEMBER.md` with the round's work, committed signed, and amended once so **Git nib** names `git rev-parse --short=10 HEAD`. The lagging pin from fold L is corrected by that same amend, riding a work commit rather than the pin-only follow-up the rule exists to refuse -- so the card names the round it describes, and no force-push over a shared remote was needed to get there. *What proves it:* the card's nib compared against `git rev-parse HEAD` on metal after the send, one command, the same measurement that caught the slip. *What it leaves standing:* an ordered rule is read at its first step. This closure is the cheapest possible demonstration of that, since the only thing that changed between the red and its closure is **when the rule was read**.

**REDS %104 (`20260821.032140`) -- a seated ordering called "drift," because the assessment never read the seating.** *What went wrong:* asked whether the recursion loops were still fit to continue, the loop assessment checked every mechanical thing correctly -- 27 rota docs present, all named paths resolving, gates matching the card, the sentinel properly ignored -- and then declared the loops' **route** stale on the grounds that both prompts lead with the Microkernel Target rather than the DISC arc opened the same session. That conclusion contradicts a decision seated **on Keaton's word `20260817`**: the Microkernel Target double-seat "takes precedence over Seasons A-H," and Cryptography is Season G, squarely inside A-H. So the loops were carrying the seated ordering **faithfully**, and the assessment called that faithfulness drift. The claim shipped to `crux/REDS.md`'s sibling record -- a session log and its index row -- and was pushed to both remotes at `c909922389` before the seating was ever read. *What caught it:* Keaton, in one sentence -- *isn't the microkernel higher Lindy priority by definition?* -- which sent the reading to `active-designing/20260816-205859_double-seat-expansion-eight-seasons.md` line 101, where the precedence is stated outright. *What it taught:* **an ordering claim is a measurement, not an impression.** The assessment ran witnesses for every mechanical question and ran nothing for the ordering question, then spoke about both in the same confident register. Lindy-first is not a feeling about which subject seems more foundational; in this tree it is a *seated* ordering with a date and a word behind it, and the check is to read the seating. A second lesson rides with it: the same pass found the microkernel's own leading question -- *can Zig target seL4 against today's real toolchain versions* -- still open and explicitly agent-doable, which means the queue the loop points at was never empty. Had the ordering been read first, the honest report would have been "the loops are aimed correctly, and here is the open rung they are aimed at."

**REDS %105 (`20260821.034037`) -- the suite recited a count and cited its own auditor as agreeing, while the auditor said otherwise.** *What went wrong:* `tools/crypto_suite_witness.rish` closed every run by printing *"all seventy-four Season G crypto files (fifty primitives + twenty-four compositions) built fresh and proven ... the count guard confirms the file count."* The count guard measured **81**. The suite was reciting a number seven stale **and naming its auditor as confirmation of it** -- the most convincing possible form of a wrong claim, since a reader who trusts the guard is handed the guard's name attached to a figure the guard never produced. This is REDS %77 and %80's family for the **third** time, and the loom booked after the second firing did not catch it: that loom proves a **bijection** between files and registrations, which was GREEN throughout, while the drift lived in the suite's own **prose**. *What caught it:* a bench-measurement round ran the crypto suite as a load test rather than as a proof, read its closing line for the wall-clock number, and noticed 74 where the guard had just said 81 -- with `ls crypto/*.rye` showing 86, of which 5 are symlink shims the guard correctly excludes. Three numbers for one thing, in one afternoon. *What it taught:* **a guard that measures a fact does not thereby guard the prose that repeats it.** The fix is not a better number; it is no number. The suite now recites no count at all and points at the guard as the single source of truth, and the guard was widened to **refuse** any recited count in the suite -- spelled or numeric -- with the RED path proven on metal by planting *"eighty-one crypto files"* into the suite and watching the guard name the offending line, then restoring. A lantern that fires twice becomes a loom; one that fires a third time means the loom was aimed too narrowly.

**REDS %106 (`20260821.041423`) -- the same pin fault, an hour later: %103's closure was a habit, and a habit is not a check.** *What went wrong:* the license-read send ran `commit` then `push` then pin then `amend` -- pushing **before** the nib was pinned, so the commit that reached both remotes carried a card naming the *previous* round. That is REDS %103 exactly, and %103 had been booked and closed the same day. The amend that followed then diverged local HEAD from what was already published, and the push was rightly refused as non-fast-forward. *What caught it:* the remote, refusing the second push -- which is a poor place to learn it, since the wrong card was already public by then. *What it taught:* **%103 closed by doing the next send correctly, and doing a thing correctly once is a habit rather than a check.** The rule was written, read, and followed three times running, then missed on the fourth when the round's shape changed slightly. A lantern that fires twice becomes a loom, so the loom is [`tools/remember_git_nib_witness.rish`](../tools/remember_git_nib_witness.rish): the card's nib must name **HEAD or HEAD's parent**, the only two honest states -- the pin inside the send, or the single amend of lag the rule explicitly tolerates. It cannot force the pin to be right at commit time, since no commit can contain its own hash, so it guards the reachable invariant and catches the drift on the very next lap. RED path proven twice on metal: once in-run against a four-commit-old hash, and once by planting a genuinely stale nib in the card and watching it refuse, then restoring. *The repair, done rather than promised:* the divergent amend was **reset away rather than force-pushed** -- a history rewrite plus force-push of the living tree is custody gate %5 and stays Keaton's word, and no round's tidiness is worth crossing it. The nib rode the next work commit instead, exactly where the rule puts it.

**REDS %104 CLOSED (`20260821.043012`) -- closed by record, and honestly weaker for it.** *The repair:* the wrong ordering claim was corrected the moment Keaton named it, the dated log kept its text and took an **erratum** rather than a silent edit, and the index row that a reader actually meets carries the correction. *The honest note this closure owes:* reds-first says a fix is closed by a witness on metal, never by a claim -- and **this red has no possible witness.** "Read the seating before speaking about an ordering" is a discipline, not a predicate; no script can tell an ordering claim from an ordering measurement. So it closes by record, which is the weaker kind of closure, and saying so is better than dressing a recorded lesson up as a proof. What stands in place of a witness is the seating itself being written down and dated in `active-designing/20260816-205859` -- the check is to read it, and the cost of not reading it is now in the ledger twice over.

**REDS %105 CLOSED (`20260821.043012`) -- the number is gone, so it cannot drift again.** *The repair, on metal:* `tools/crypto_suite_witness.rish` recites **no** file count at all now and points at `tools/crypto_count_guard_witness.rish` as the single source of truth, and that guard was **widened** to refuse any recited count in the suite -- spelled or numeric -- with the RED path proven by planting *"eighty-one crypto files"* into the suite, watching the guard name the offending line, and restoring. The fix is not a better number but no number, which is the only repair that closes the class rather than the instance. Count guard GREEN at 81 with its bijection intact.

**REDS %106 CLOSED (`20260821.043012`) -- the loom stands, and it refused its own author first.** *The repair, on metal:* `tools/remember_git_nib_witness.rish` requires the card's nib to name HEAD, HEAD's pre-amend sibling, or HEAD's parent, and refuses anything older. Its RED path is proven twice -- in-run against a four-commit-old hash, and by planting a genuinely stale nib in the card and watching it refuse. *What the closure gained beyond the fix:* the loom's first real run **refused a correctly-run send**, which taught that the rule's pin-then-amend order makes the card carry HEAD's *pre-amend sibling* rather than HEAD -- so the honest states are three, not two. A flaw in the seated convention was surfaced by building the guard and is named in the witness rather than quietly fixed: the pre-amend hash is unreachable from a fresh clone, and changing a seated convention is Keaton's word.
