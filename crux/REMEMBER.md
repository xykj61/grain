# REMEMBER -- living operator card

**Language:** EN
**Status:** Living pin -- operator carry card
**Voice:** Kyri

## INNER LOOP -- live directives the running loop applies each lap (seated `20260816.214652`)

*The outer shell loop reads this card first every lap, so directives here take effect on the NEXT lap without a restart. The agent MAY edit this block as it goes -- it is the inner loop the outer loop points at. Keep it ASCII, short, and current; move settled items down into the body.*

- **ASCII-first.** Write every new document, comment, and commit message in plain ASCII. No em-dashes, middots, curly quotes, arrows, or non-ASCII math -- use `--`, `-`, `'`, `"`, `->`, `<=`, `gamma_2`. The one exception is an explicitly-named set of work rounds (e.g. a Unicode-handling module's own fixtures). Rule: `.claude/rules/ascii-first.md`. This card was corrupted to mojibake once (REDS %83); ASCII-first is how it never happens again.
- **Stamp and name, never an ascending mark.** Mark a lap by its one-clock stamp and a plain name -- `the standing movement (20260821-142939)` -- never `Fold AI`, `f0-f63`, or `X0/X1` for planned work. Count a total with `git log --grep ... | wc -l` rather than carrying it inside a name. Waymarks stay (they are names, not counts); `rung` stays where a real ladder exists in code; `lap` and `round` carry planned work. A room that outgrows a reader folds to `<room>/date/YYYYMMDD/` keeping the WHOLE stamp in the filename, and a stale reference is resolved rather than rewritten -- `rishi/bin/rishi run tools/dated_path_resolve.rish <reference> [<citing-file>]`. No fold ships without `tools/dated_path_witness.rish` GREEN; its broken-reference ceiling has no slack and only ever comes down. Seated `20260821.160050` -- rule `.claude/rules/stamp-and-name.md`.
- **Spelling: American.** Use `color`, not `colour`; normalize `colour -> color` on touch. This is a USA project.
- **Style sweep before every send** -- Radiant pass over the round's prose (Twilight for a night piece), register only never a claim. Seed section 6.
- **Rota of the canon.** Each lap, deep-read ONE ROW of the 5 x 3 council grid in `recursion-prompts/seed/autonomous-loop.seed.md` section 1 -- element by modality, three documents per lap, **lap N reads row N mod 5** -- so the whole canon returns to awareness roughly once a working day. Seated `20260821.211423`, replacing the 27-doc rota.
- **Living-pin guards healed (`20260817`, REDS %87):** the `work-in-progress -> crux` rename had left the e123 living-pin guard RED four ways (stale paths, em-dash headers the ASCII sweep had moved to `--`, a gone SEAT_MAP header, and REDS grown past its 24576 bound). All repointed to `crux/`, headers matched to the pins as they read now, REDS folded (rows 74-79 to `archive/`) back under bound -- e123 GREEN, its planted-empty-pin RED path still caught. And REDS %83's booked prevention now stands: `tools/living_card_ascii_witness.rish` enforces the operator card and REDS at zero bytes above 0x7F and catches a planted mojibake control on metal. Advisory ratchet: 244 legacy non-ASCII lines across the older pins to sweep on touch; REMEMBER itself is ~200 KB, over its advisory bound -- a condensation debt for a later lap.
- **Season G open-media -- the scene-read family stands whole (condensed `20260819.125806`, cairn `4a50722016`).** The surface's interaction floor is broad and green at **227 image modules**: four selectors (`scene_manifest`, `hit_test`, `region_select`, `lasso_select`), reads that compose over any of them (`selection_summary`, `selection_bounds`, `object_relation`, `scene_graph`), and the whole line family from `line_distance` through `line_pierce`, `line_cross`, and `line_raycast` -- every rung exact integers, compose-not-braid, GREEN on metal. Per-rung detail lives in `session-logs/` and `image/README.md`; `git show 4a50722016:crux/REMEMBER.md` reads the full departing chronicle. Open Season-G doors when the loop returns: a nearest-crossing single-hit fold, a codec read, the color algebra, or the glyph/font collections.
- **Live edge -- the Standfast day: a stopped line, four arcs, and the seed published (`20260821.203103`).** Keaton called a possible Standfast on the season arcs and asked for blind spots. The tree was counted before it was restrategized -- thirteen readings on metal -- and the survey stands at [`external-research/20260821-152409_the-standfast-read-marks-rooms-and-the-vocabulary-pruning.md`](../external-research/20260821-152409_the-standfast-read-marks-rooms-and-the-vocabulary-pruning.md). **Arc one -- the mark and the path:** the [mark law](../.claude/rules/stamp-and-name.md) retires the ascending mark (a mark is a one-clock stamp and a plain name; count, never number; the whole stamp stays in every filename), and `tools/dated_path_resolve.rish` makes a stale reference **resolvable rather than rewritten**. Five rooms folded to `date/YYYYMMDD/` -- session-logs, counsel, active-designing, expanding-prompts, waymarks, **3,482 files moved** -- with living code repointed and dated testimony left whole. **Arc two -- the rooms:** [`active-development/`](../active-development/README.md) opened for round scoping, `counsel/` closed and kept, `journal/` declined. **Arc three -- the vocabulary:** pruned first, then defined; `fold` keeps the filing sense and **`lift`** takes the ladder sense; `Sundial` and `Plumb` split from one word wearing two jobs; ten daily words defined in [`foundations/20260821-175723_the-words-a-round-uses.md`](../foundations/20260821-175723_the-words-a-round-uses.md). **Arc four -- the beginner path:** the fascia meter unbraided and reporting **45/100**, the front door carrying four generated numbers, [the first hour](../docs-geode/tutorials/the-first-hour.md) at six copyable commands, and the ten crush-empty `docs-geode` rooms filled. **The seed is published** at `33ea67d`, IDENT_CLEAN and NO_PERSONAL, verified from a keyless HTTPS clone. Ten reds booked (**%112-%121**), every one closed.

- **Standing equipment -- twenty-two guards, in a file a program reads (`20260823.002511`).** The roster left prose for [`standing-equipment.kyri`](standing-equipment.kyri): twenty-one `guard` / `path` / `seated` records, gated by `tools/standing_equipment_witness.rish` so every path stays real and no recorded run is red. **Run the whole roster with `sh tools/fixtures/standing_equipment_run.sh`** -- it writes `crux/standing-equipment-runs.kyri`, one line per guard saying when it last answered on this pier, untracked on purpose so a fresh clone reads *never run here* rather than another machine's memory. Run them at a cold start rather than trusting them, because **a guard earns confidence by being run, not by being written** -- and the roster's first honest run proved it, returning three reds nobody knew about (%151). Each guard's seating story lives in its own header comment and its dated log; the departing paragraph that held them all waits at cairn `337c466fc7`.

- **A generated page refreshes itself in the commit that moves it (`20260823.000512`).** Two pages here are generated and compared against a fresh render -- README.md's four numbers and the crushed library index `docs-geode/libraries/README.md` -- and both count witnesses and Rye modules, so a lap that adds either leaves a page one behind at the instant it commits, which reds the rostered guard on the NEXT cold start rather than on the lap that earned it (REDS %151, %152, and the library index caught the same way on the lap that wrote the fix). [`../tools/hooks/pre-commit`](../tools/hooks/pre-commit) makes the discipline executable: it rests unless the staged change adds or deletes a `tools/*_witness.rish` or a `*.rye`, then renders each page and stages it alongside, and **refuses** rather than sweep an author's unstaged edits into a commit they never wrote. Arm a fresh clone with `rishi/bin/rishi run tools/install_hooks.rish` -- it arms this beside the commit-msg wall. Proven both ways by `tools/generated_page_freshness_witness.rish` over a throwaway repository, including the tree with no rishi at all, which is the seed's case. **The honest limit:** fascia and the rooms-over count still drift from commits that move neither, and the rostered `readme_metrics` guard is what catches those.

- **`corpus` retires from the pages a newcomer meets (`20260821.213540`).** The seed's Markdown had carried **652** occurrences of it to an audience meeting this tree for the first time. The sweep went **per sense** rather than one abstract word for another, because `corpus` was doing four jobs: the flw draw's 5,526-word source is a **word list**, the hosted `.rye` files are the **hosted sources**, a test set is a **control set**, and Grain's living documents are a **collection**. **56 replacements across 28 files**, and that is the whole audience-facing surface -- against **1,344 code identifiers** and **169 file paths** that keep the word by the Comlink tendency's own ban on churn, and **1,525 occurrences in dated testimony** that keep it by law. **The deep debride was declined and the reason is one line: the published seed is a single Option-B commit, so there is no public history to clean.** A rewrite would have re-signed 2,901 commits and forced every downstream to re-clone, to change private history no front-door reader ever opens. Seated in the Lexicon; guarded so it cannot drift back the way `color` did for five days.

- **The compiler under everything had no guard, and answered a fair question with a segfault (`20260821.232733`).** The `main` lift was measured first and stands **fully surveyed**: 64 rungs at 17 lines, beside a **second family of 15** differing in exactly one qualifier, so **one lifted body serves 79 rungs**, with the widening priced across *whole* cohorts at **zero carry cost** -- `run_self_test` is 97 distinct bodies, `run_dependent` is 68 in three families that move together, `fixed_argv_words` is a const the meter does not read. Then the lift asked four base modules to import the harness that already imports them, and **Rye segfaulted**: `bridge_rye_tree` appended its visited set **after** walking a file's imports, so while a cycle was traversed **nothing was ever marked seen**. Post-order marking orders a graph correctly and terminates on none -- two duties braided into one list, at the very bottom of the stack. It marks on entry now, with a **named bound** beside it (`max_bridge_depth = 1024`, `error.BridgeTooDeep`), and every file bridges under **one name**, root included, since the root once answered to `foo.rye.zig` while its dependencies asked for `foo.zig`. **1,639 witnesses stood in this tree and not one was pointed at the compiler underneath every other** -- every green one is evidence Rye works on the graphs the tree *happens to hold*, and nothing in the tree holds a mutual import, so only a control set written for the question reaches it. Both legs on metal: the previous binary segfaults (code 134) where the fixed one builds, links, and runs. All **107** Caravan witnesses GREEN under it. Two more reds rode along -- the ledger's own monotone guard had been **RED for a day** with `rows=121` written as an *equality* and nothing on any roster calling it (**a count that grows belongs in a floor**, %126), and the two new rows carried the pin past its **24,576-byte** bound, so %122, %123, %124 folded to `archive/` at **22,236**. **Next:** the `main` lift itself, now starting from arithmetic rather than a question -- roughly **1,075 lines** off a carry standing at **87,795** against a ceiling of **87,800**.

- **The ten-fold ladder arc, condensed (`20260822.054053`, cairn `57c365fbc2`).** Between `20260822.012016` and `20260822.050555` the Caravan ladder took ten lifts -- `note_path`, `press_path`, `bearing_published`, `lost_published`, `appraisal_published`, `recant_published`, and the roster round between them -- carrying `caravan/ladder_checks.rye` from a carry of 82,843 down to 75,730 while checks held at 47 and spine at 0 throughout. Each lap's predicted fall met its measured fall to the digit, and each per-lap reasoning lives in its own session log; `git show 57c365fbc2:crux/REMEMBER.md` reads the departing rows whole. **The teaching worth carrying forward is the one this lap corrected:** every one of those rows published a widening cost, and every one of those costs was computed from the family's own visibility rather than from what its body reaches (REDS %130). The falls were right because a meter printed them; the costs were wrong because a hand described them.

- **The address-space debt splits, and the pure half is paid whole (`20260821.222916`).** The prior lap called the debt a design round because *a page table is a machine fact*. That is half true. An address space is a **decision** -- which pages cover a span, at which sizes, over how many levels -- and an **act** -- writing entries, assigning an identifier, invoking a retype. The decision is arithmetic over five numbers seL4 publishes in a header, so `caravan/mapping.rye` lowers a placement to the exact pages and page tables it costs, exactly and minimally, on hosted ground. **Three findings worth carrying.** A declared region has no place -- `regions.rye` says what and how far and deliberately withholds where -- so **placement is its own value**, one concern one list. **The geometry bounds the plan**, so the `too_many_pages` refusal was derived away rather than guarded: at most 511 small pages per end fragment, 511 megabyte-pages per end, 512 gigabyte-pages of middle, **2,556 derived rather than chosen** -- and an unfireable refusal is worse than an absent one, since no witness can prove it. And **the print caught what the asserts could not**: a first cut counted tables from the placement's extent and printed **514** level-two tables for a plan holding one small page; every assert it held was about coverage and refusals, both correct. A module that can print a worked example is a module a reader can catch. Witness `tools/caravan_mapping_witness.rish` GREEN, three RED paths on metal, choir at **107**. **Still owed, named:** page-table entry bytes, an ASID from `seL4_ASIDPoolBits`, and the retype itself -- every one a machine act. Essay: [`active-designing/20260821-222916_the-address-space-debt-and-the-mapping-plan.md`](../active-designing/20260821-222916_the-address-space-debt-and-the-mapping-plan.md).

- **Caravan already stands for five of seL4's nine object types, and the five are exactly the portable tier (`20260821.222459`).** `refusals.rye` read the vocabulary a verified kernel **refuses** with; `caravan/objects.rye` now reads the vocabulary it **offers** -- the nine riscv64 object types in their own numbering, across three enums that compose with no overlap and no gap. `untyped.rye` carves with a real floor, `channels.rye` rosters the protection domains and `concurrent.rye` runs them, `reply.rye` answers an ask through four stages, `notify.rye` rings each declared channel, `capabilities.rye` keeps the table. **Five held, and they are exactly the portable tier**, so the four owed are exactly the mode and architecture tiers -- every one to an **address space** no module has built. `regions.rye` declares which memory a dependent may touch, which is the policy half; the mapping mechanism is the debt. Witness `tools/caravan_objects_witness.rish` GREEN, three RED paths on metal, choir at **106**. **Next constructive door:** the address-space debt, which wants a design round before code -- a page table is a machine fact and a hosted supervisor has no address space to build. **Gate standing:** the libsel4 generator deps (`jinja2`, `ply`) are an environment change and Keaton's word; rungs 3 and 4 of the arc wait behind it.

- **The council rota is a 5 x 3 grid now, and six essays came home (`20260821.211943`).** The d27 rota mixed a 4 KB essay with a 164 KB dictionary and called both one lap's reading, and **five of its twenty-seven entries were dead paths** after the fold. It is fifteen documents as **element by modality** -- Jupiter aether, Saturn air, Mars fire, Venus water, Mercury earth, crossed with cardinal, fixed, dual -- read **three per lap, lap N reads row N mod 5**, so the canon returns once a working day rather than once a fortnight. Six essays **graduated from `active-designing/` to `foundations/`** (three had carried *foundations graduation on Keaton's word* in their own headers since `20260703`), and two were **written** to fill measured blind spots: [Gall's Law](../foundations/20260821-211423_galls-law-and-the-grown-system.md), cited six times and argued never, and [Conway's Law](../foundations/20260821-211423_conways-law-and-the-organization-that-forgets.md), **zero mentions tree-wide** -- which turns out to explain this repository more exactly than any law it already cites. Shape and bench: `recursion-prompts/seed/autonomous-loop.seed.md` section 1.

- **THE FIRST DOOR, chosen rather than left open (`20260821.204005`).** The loop restarts on the **Caravan ladder lift work** -- the door with the most written policy behind it and the least room to improvise, which is what a run wants after a day that seated this much law. The filled prompt is [`recursion-prompts/versions/20260821-211943_autonomous-loop-caravan-and-the-grid.md`](../recursion-prompts/versions/20260821-211943_autonomous-loop-caravan-and-the-grid.md); read it before the first lap (its elder, [`20260821-204005`](../recursion-prompts/versions/20260821-204005_autonomous-loop-caravan-lift.md), stands as the record of the hour before the rota was reshaped). It carries the three teachings the arc already paid for -- **read the harness before the meter**, **a visibility split is sometimes meaning rather than accident**, and **a cohort is measured at the head of the lap, never re-queried against the tree the lap is changing** (REDS %111) -- and the one habit to refuse: **no ascending mark**. Thirty-two laps of muscle memory point at `fold AJ`; the word is **lift**, and the lap is marked by stamp and name.

- **Next doors, after that one (`20260821.203103`).** The Caravan ladder work is real and unfinished; it continues as **`lift`** rather than `fold`, and each lap is marked by **stamp and name** -- never `fold AJ`, and the total is counted with `git log --oneline --grep | wc -l` rather than carried in a name. Beside it: the **Caravan carry meter now reads two numbers** (`carry` and `CARRY_DELEGATES`, 2,365), so a lap that trades lines for delegates shows it. **Season G open doors** stand as the reach round left them -- a root-task skeleton on the vendored `libsel4`, Aurora on RISC-V under QEMU. And two named-and-held: the `manual/` rooms beyond the five guides read this day, and any page whose claim is about **behavior** rather than a name or a path, which only a reader's walk on a colder tree will test.

- **The Caravan ladder arc, condensed (`20260821.203501`, cairn `5f09ec4273`).** Nineteen live-edge rows folded into this one. Between `20260820.220352` and `20260821.142939` the ladder took **thirty-two lifts**, D through AI, carrying `caravan/ladder_checks.rye` from its opening weight down to **89,010 lines across 708 carrying families**, with checks holding at 47 and spine at 0 throughout. The teaching each row carried is the part worth keeping: **read the harness before the meter** (it names a movement where a text-keyed queue sees four separate families), **a split keyed on visibility is sometimes meaning rather than accident** (a rung reached only through the harness must publish; one that calls its own body need not), and **a cohort is a measurement of the tree at the head of the lap, never a query re-run against the tree the lap is changing** (REDS %111). Per-lift reasoning lives in each round's session log; `git show 5f09ec4273:crux/REMEMBER.md` reads the full nineteen as they stood.

- **Standfast closed REDS %85 for real (`20260819.134229`) -- the apostrophe convention is a check now, not a habit.** The `20260817` closure honestly swept all 97 `image_*` witnesses and then read the whole class as guarded. Measurement disagreed: **129 fragile sites stood untouched across 21 HUNK witnesses**, each still checking its GREEN claim by handing captured output to the shell inside a single-quoted word, and no mechanical check existed to catch a 130th. All 129 converted to native `assert out contains "..."`; seven witnesses re-run GREEN on metal rather than merely re-read. The loom stands: `tools/witness_claim_check_witness.rish` over `tools/fixtures/witness_claim_check_scan.sh` enforces **zero captured-output interpolations inside shell quotes across all 1,663 runnable `tools/*.rish`**, reporting two honest ratchets -- the four device-gated host scripts (HAWM, SETU, TUBE; 10 sites) that want a phone or an outer terminal to witness, and the wider printf-into-grep shape tree-wide (8 sites). Both RED paths proven on metal: the tracked fragile control caught by `prove-red`, and a fragile line planted in a real witness making the enforce leg name that file and refuse. REDS folded back under bound (closed notes 73/74/75/78/80 to `crux/archive/`), e123 and ASCII guards GREEN. **A class is guarded on the lap it becomes a check, never on the lap it becomes a rule.**
- **Seated names (`20260816`):** **Scooter** = the CLI chat app (Talk reimplementation on Pond); **Dexter** = the terminal module (our Dill parallel, affirmed); **Lumen** = the inference vane (renames Quin's Q-vane, gathering Lattice, Lantern, Ember, Scribble). Plan: `expanding-prompts/date/20260816/20260816-222322_dexter-terminal-and-scooter-cli-chat.md`.
- **Seated breaches (cairn-first when executed):** **Quin's Q-vane -> Lumen** (rename its references); **Bron -> Kyri** and **Quin voice -> Kyri** (docs-compression season). Each is its own signed round, cairn first; none cut yet.

*One-page carry card for outer terminal - phone - waymarks. Refreshed when Keaton says **remember**. Debrided to the Compass Season `20260809.024320` -- the old season marks are gone; their greens live in the code and in the dated counsel.*

**Git nib:** `ec1b54b3e5` (**One page serves two repositories, and the front door now reads whole in both.** `tools/fixtures/seed_link_scan.sh` reads the seed's allowlist and asks whether a link survives the **projection** -- the question `tracked_link` cannot ask, because it proves a property of the repository it runs in and this tree ships two. **867** dead links measured across living shipped documents, **19 in the front door**; the README rewritten so all **68** of its links land in the seed, a withheld room named in prose rather than linked (%160). Roster audited for blind classes: four healthy, two blind -- the projection, and **empty tracked documents** (%161). **GATE %162:** all three remotes read PUBLIC, so the seed's depersonalisation protects a field that is already public. Twenty-five rostered guards GREEN in one pass.)

*Prior edges:* the generated-page wall that refreshes a front door in the commit that moves its numbers, at `81a9579c34` (%152); the banner-and-path meter, twenty-nine shelf-naming banners compared against their own directories and a bound guard found four laps unrun, at `337c466fc7` (%148, %149); the `reclaim_notes` lift, forty-three rungs folding and the arc's first row free by every column at once, carry 58,838 -> 58,544 at `a96cdb6d73`; the `bound_the_relay` lift, seven rungs folding and the narrowest deepest row the queue held, carry 59,150 -> 58,838 at `96d925c22f`, beside eight standing guards found drifting behind their code and the `|+` rune seated as **barlus** (%147); the `recant_note` lift, thirty-seven rungs folding and the arc's first lift chosen by price rather than by return alone, carry 59,474 -> 59,150 at `11050193c5` (%146); the `hold_the_matter` lift, seven rungs folding and the deepest body of the arc, the sub-record accessor written into the witness as meaning rather than duplication, carry 59,810 -> 59,474 at `98f5746e12`; the `suits_path` lift, fifteen rungs folding and the sixth reader-box path joining its five siblings, carry 60,160 -> 59,810 at `9f157eecf0`; the `mind_the_impasse` lift, eight rungs folding and the deepest body of the arc, the first family a ranked queue found rather than a hand, carry 60,510 -> 60,160 at `99accfdec7`; the `appraisal_note` lift, thirty-eight rungs folding and the lap that found the queue had never been ranked, carry 60,843 -> 60,510 at `eac610ad25` (%145); the `bearing_note` lift at `d4b0e44248`; the `found_path` lift at `ba5295fb99`; the `elder_waits` lift, forty-three of fifty-one rungs folding and the lift widening nothing on either side, carry 63,964 -> 63,445 at `415c017f89`; the meter repair that took the visibility keyword out of both fold scans' body keys, no body lifted and the carry ceiling honestly raised 63,852 -> 63,964 (%144) at `2ab2037965`; the `sibling_waits` lift with its private `waits` helper lifted first, forty-three rungs folding and a caller's priced widening answered from underneath, carry 64,361 -> 63,852 at `ce777eb694`; the `judge_outcome` lift, thirty-seven rungs folding and the first of the arc to pay a widening -- thirty-seven struct openings of a `Finished` its own signature had always named -- carry 64,829 -> 64,361 at `aba50bbec5`; the `lost_note` lift, forty of forty rungs folding and the first family of the arc to owe the harness import list nothing for its freedom, carry 65,180 -> 64,829 at `1f8b227ad7`, beside the guard that had been reading nothing at all (%143); the `word_at` lift, forty-four of forty-five rungs folding with `confer.rye` standing below the fold line, carry 65,579 -> 65,180 at `6a0d0992cb`, beside the fold line's foreclosure correctly re-read from 247 to 235; the `already_holds` lift, forty-three rungs folding with no rung parameter at all and two staying home on a drift, carry 65,999 -> 65,579 at `05b0fe31e8`, beside the line citation that could not survive its own file (%142); the `finding_path` lift, twenty-one rungs folding and nothing published on either side, carry 66,439 -> 65,999 at `32037da087`; the `door_admits` lift, forty-three rungs folding and none staying home, carry 66,901 -> 66,439 at `5a0d17cb6e`, beside the ledger headline left one row behind its own spine (%141); the `case_path` lift, twenty-two rungs of twenty-five lines opening no declaration of any kind, carry 67,363 -> 66,901 at `c25f7a7957`, beside the meter that could not see a capitalized type (%140); the `revoke_one` lift, forty rungs folding and four staying home on their merits, carry 67,831 -> 67,363 at `7969dd3c2f`; the `look_again` lift, twelve rungs folding and the first family of the arc to widen nothing of any kind, carry 68,359 -> 67,831 at `7f8518ddf7`; the `read_own_line` lift, forty-two rungs folding and the rung parameter kept on purpose, carry 68,851 -> 68,359 at `7895393be0`, beside three guards found red in the dark (%137, %138, %139) and the era suite's twelfth rung; the `load_one` lift, sixty rungs folding and the first of the arc to take no rung parameter, carry 69,334 -> 68,851 at `e47d75a28e`; the `read_count` lift, forty-four rungs folding and two regaining an invariant, carry 69,805 -> 69,334 at `f810bbd55d`; the doc-attachment guard at `4958230c60`; the `take_up_again` lift, thirteen rungs folding, carry 70,345 -> 69,805 at `9991482205`; the `read_words` lift at `c917d48169`; the `confirm_pruned` lift at `50b2af3d1c`; the `weigh_the_answer` lift at 624; the `redress_the_meeting` lift at 645; the `apprise_the_raiser` lift at 658; the `ask_path` lift closing the reader-box family at 660; the ten-fold arc from `note_path` through `recant_published`, carry 82,843 -> 75,730, condensed above with cairn `57c365fbc2`.

**Now -- one page serves two repositories, and the front door now reads whole in both.**

`tools/fixtures/seed_link_scan.sh` reads the seed's own **allowlist** (`template-manifest.bron`) and asks the question no rostered guard asked: does a link survive the **projection**. `tracked_link` asks *is this in the repository* -- right for the field, wrong for the seed, which is a different repository with fewer rooms. Measured before the repair: **867** links in living shipped documents pointing at rooms the seed does not carry, **19 in the front door alone** (%160). The root README is rewritten so all **68** of its relative links land on a path the seed ships, and a withheld room is **named in prose rather than linked**. Seated as the roster's **twenty-fifth** guard; GREEN at `front_door_links_outside_seed=0` over 1,888 shipped documents and 6,889 links. **848 stay under a ceiling that only falls** -- `context/LEXICON.md` 131, `bibliography.md` 19, `MAP.md` 16, `docs-geode/` 16, `manual/` 15, `ORGANIZING.md` 6, `SOURCE.md` 4 -- each wanting a rewrite rather than a repoint.

**The roster audit, asked and answered (%159's open question).** Six candidate blind classes tested empirically rather than guessed. **Healthy:** case-insensitive filename collisions (0, so the macOS clone is safe), oversized blobs (largest 2.8 MB against GitHub's 100 MB refusal), editor-rule twin parity (39 Claude / 40 Cursor, none claiming a twin that is absent), submodule declarations. **Blind:** the seed projection (%160), and **empty tracked documents** (%161) -- two at **zero bytes**, one of them a session log *indexed in its own README*, so the index promises a reasoning record that does not exist. **Vacuous truth is the shape of the class:** a check phrased as *nothing here is wrong* is answered perfectly by a file with nothing in it. A rung gating tracked documents at a minimum honest size is the next lap.

**GATE -- %162, and it wants Keaton's word.** The seed withholds the maintainer's GPG signature so the anonymous seed cannot be linked back. Checked directly: **all three remotes read `PUBLIC`** -- `grain-os`, `xykj61`, `autoproject96`. The field the seed is depersonalised *from* is world-readable and tracks `PUBKEYS.md`, a resume draft, a personal ontology, a cover letter, and the `twilight/` poems -- material the manifest carefully excludes from the seed while the field publishes it anyway. Content matching links the two in one search, so the unsigned commit buys nothing. Three resolutions, all Keaton's: make the field private, accept the linkage and restore signing, or debride the personal material. **Two touch published history.**

**The front door and the founding statement, rewritten.** `foundations/20260823-034321_the-return-that-feeds-everyone.md` is the new siloed universal writing -- soil, materials, and the computer as one question, with the closed loop named as the shared discipline (*a system that renews itself from within needs no input it cannot account for*) and a witness named as *a certification for code*. No person, firm, brand, or figure survives the crossing. The README leads with the problem, gives TAME and the witness their first plain-English introduction, names **Caravan** as the crux under way, and explains the rooms -- including the field-only ones -- in prose a stranger can follow.

**Correspondence now has an untracked room.** `letters/` holds sent letters verbatim, withheld by the `.gitignore` allowlist default rather than merely absent from the seed manifest -- because seed-withholding alone publishes, as %162 makes plain.

**Prior edge -- a tool reads a path the repository carries (`20260823.010937`, nib `b0418b8394`).** `phantom_path` seated as the roster's twenty-third guard: `phantom_paths=0` over 2,649 tracked tool sources and 7,604 path literals, after the `work-in-progress -> crux` breach left 116 literals across 58 tracked sources reading the elder room (%155), two guards stood red at HEAD for thirteen days outside the roster (%156), and a planted fixture name counted as field twice (%157, %158). Walk-back: `git show b0418b8394:crux/REMEMBER.md`.

**Prior edge -- Equinox 1's ground reached (`20260821.043831`).** seL4's own libsel4 userlevel API **compiles under our compiler**: the hand-written half builds freestanding riscv64, no CMake, no kernel, no libc, and its vocabulary was **read out of the compiled enums by a program that ran** -- 11 refusal codes, 9 riscv64 object types over a portable 5, a 64-bit word, 256 priorities, 6 of 6 headers BSD-2-Clause. The boundary is the finding: the generated half stops at `sel4/sel4_arch/types_gen.h`, gating five headers together, because seL4's generators want jinja2 and ply. Witness `tools/microkernel_libsel4_reach_witness.rish` GREEN, both RED paths proven.


**Prior living edges -- condensed `20260818.225452` (cairn `d2368cb9db`).** The whole roundness quest's per-nib comment wall once stacked here -- roundness_universal, roundness, hit_test, scene_manifest, euler_scene, euler_number, and every earlier shape descriptor back through regions, some three dozen former live edges as `<!-- prior nib -->` comments. Each is a real commit recoverable by `git log --oneline`, and each rung carries its own dated log in `session-logs/` plus a durable entry in `image/README.md`. Folded to this pointer so the operator card stays the live card of what is next, never a second copy of the log index. Earlier open-media condenses: `20260818.101851` and `20260817.172514` (cairns). Walk-back: `git show d2368cb9db:crux/REMEMBER.md` reads the full verbose stack.

**Host:** Framework - EDT (`America/New_York`) - Vultr SEA VPS (**AMD 4vCPU/8GB shared - 180GB NVMe** - never EWR) - this session in ai-jail. *Corrected `20260821.034037` from a stale `2vCPU/4GB`, measured on metal: 7,937 MB total, `nproc` 4, `vda` 193,273,528,320 bytes. `GLOW_PROFILE.bron` had it right all along; this card had drifted, and a counsel essay reasoned its whole hardware argument from the wrong figure. Gauge protocol and the full measurement: [`../external-research/20260821-034037_the-bench-measured-and-the-standing-gauge-protocol.md`](../external-research/20260821-034037_the-bench-measured-and-the-standing-gauge-protocol.md).*

**Refinement `20260815.191048` -- Kumara crypto re-grounded (not "Lindy"), host updated, Puddle identities cleared, Brix in plan.** Keaton rightly rejected the Lindy metaphor for crypto (past != future when compute/quantum/AI-cryptanalysis all climb). The SLH-DSA choice **stands, re-justified structurally**: hashes have *no algebraic structure* for the rising attacks (Oct 2025 lattice sieving, CRYPTO 2026 sign-based key recovery, AI cryptanalysis) to exploit; quantum is only a Grover quadratic dent (256-bit output covers it); it adds *no new assumption* (Grain already relies on the hash everywhere -- seals, receipts, Merkle catalogs); and no mature *independent* family exists to hybridize with (code/multivariate broken, isogeny young, lattice eroding, symmetric-MPC not independent). **Confident. Parameter set: SLH-DSA-SHAKE-256s** (NIST cat 5). **Puddle cleared:** the agent may key + bootstrap **fake/test Kumara identities** in the constel/Puddle sandbox (consonant-only, non-@p-valid per placeholder-ship-names/FORA) -- the maintainer's *real* Kumara key stays the custody gate. **Brix seated in planning** (Brix config code + Brix docs molt, riding the docs-compression round). **Host updated** (GLOW_PROFILE): editor **Claude Code** (not Zed/Cursor), model claude-opus-4-8, host **Vultr SEA NixOS VPS**, hand **Daylight DC-1** (was Android Termux). No nixos-rebuild needed yet for the crypto rounds; will flag if SLH-DSA parity wants vendored FIPS 205 KAT vectors or a toolchain bump. Brief: [`../active-designing/date/20260815/20260815-184832_sha3-preference-and-post-quantum-kumara.md`](../active-designing/date/20260815/20260815-184832_sha3-preference-and-post-quantum-kumara.md).

**Kumara PQ SEATED `20260815.185922` -- SLH-DSA (SPHINCS+) on SHAKE-256, the most-Lindy path.** After an Aug 2026 news pass (Oct 2025 quantum-sieving cut the lattice-attack exponent ~8%; CRYPTO 2026 carries lattice-signature key-recovery papers), the century-root does not anchor to the assumption under active erosion. Kumara v1 signs **pure hash-based** -- security reduces to the hash alone, the oldest/most-Lindy primitive, industry's 30-year-guarantee tool -- making the SHA-3 preference load-bearing (identity is SHAKE-256). **Algorithm agility** seated: identity format carries a scheme tag + version so a second leg/migration accretes later (accrete-never-break for crypto). **Lattice co-signature deferred, not baked in** (revised my earlier SLH-DSA+ML-DSA hybrid lean on the evidence). Loop crux: SHA3-512/SHAKE-256 in Rye -> SLH-DSA build round, parity vs NIST FIPS 205 KATs; keygen/signing with the identity key stays the custody gate. **RECONCILED `20260821.022912` -- both legs kept, on Keaton's word (*vendor a second SLH-DSA implementation and keep both*).** For five days this line and the tree disagreed: `20260816.161537` pivoted to ML-KEM + ML-DSA because the vendored Zig toolchain ships those two and **no SLH-DSA**, leaving a hash-based rung with a single oracle. That was a real objection to a real discipline, and the answer was to remove the constraint rather than pick a side. **PQClean is vendored** (`vendor/pqclean`, CC0-1.0, gitlink like Monocypher); its `sphincs-shake-256s-simple` is exactly the seated parameter set. `tools/crypto_slhdsa_oracle_witness.rish` is GREEN on metal -- PQClean's own NIST known-answer generator, compiled fresh from that source, reproduces the digest PQClean publishes in its `META.yml`, with that digest and all four lengths **read at run time, never recited**, and the RED path proven by a planted drift. **Nothing already GREEN moves:** the ML-KEM and ML-DSA ladders and both `kumara_pq_*` doors stay as built; the hash-based leg grows beside them. Honest costs named: a 29,792-byte signature, slow `256s` signing, and an explicit `optrand` seam because PQClean signs hedged. Proves the ORACLE only -- authored Rye SLH-DSA is the next arc. Note: [`../active-designing/20260821-022912_slhdsa-vendored-oracle-and-both-legs-kept.md`](../active-designing/20260821-022912_slhdsa-vendored-oracle-and-both-legs-kept.md).

**Crypto policy `20260815.184832` -- SHA-3 preferred; Kumara goes post-quantum.** SEATED: prefer SHA-3/SHAKE over SHA-2 for new designs (structural defense-in-depth -- sponge, no length-extension -- *not* because SHA-2 is broken; SHA-512 stays for Monocypher/Ed25519 parity). Near-term no-gate crux: **SHA3-256/512 + SHAKE-256 in Rye**, parity vs Zig std.crypto + NIST FIPS 202 KATs (Monocypher has no SHA-3). CORRECTION seated: no elliptic curve is quantum-resistant (Shor breaks the curve regardless of hash); 512-bit hashes are already quantum-safe (Grover only halves -> ~256-bit). So Kumara's quantum answer is a **post-quantum signature, not a curve** -- and the conservative ones are hash-based, aligning with SHA-3. PROPOSED (wants Keaton's word): Kumara PQ scheme -- **A** SLH-DSA/SPHINCS+ (SHAKE-256, hash-based, most auditable) - **B** ML-DSA/Dilithium (SHAKE, fast, lattice) - **C** hybrid Ed25519+PQ. Breaking OpenSSH interop accepted. PROPOSED: a checkable security floor (refuse weaker primitives by named error) + root-README post-quantum promise + debride of weaker usage. Signing with the identity key stays the custody gate. Brief: [`../active-designing/date/20260815/20260815-184832_sha3-preference-and-post-quantum-kumara.md`](../active-designing/date/20260815/20260815-184832_sha3-preference-and-post-quantum-kumara.md).

**Decision wave `20260815.175524` -- Rye-first spine + Season G Cryptography double-seat.** Rye is the prioritized language; Glow is implemented on green-witnessed Rye. **Season G -- Cryptography** double-seats: reimplement Monocypher primitives in Rye with byte-for-byte parity witnesses against `vendor/monocypher` on RFC vectors (audit-ready) -- this unblocks the Lotus signed carry (the *library* is agent-doable; signing with the maintainer's identity key stays the custody gate). **Two breaches cairned at `9e3c2dccfa`** (each its own signed round): **Bron->Kyri** (one notation under Kyri -- voice - notation - *compressed receipts* - preferred Grain variant, gratitude to Kyrie Irving) and **work-in-progress->crux** (902 refs repointed; sorts higher). **Standing approval seated:** update the public seed with force pushes as needed -- the *deep* refresh fires once, after the docs-compression molt + README-leaf weave. New surfaces: **deemlow.com** (fair-trade live-shopping, Kumara login, 1Password interim -> Vault-audit gate, Season B) - **anticruel.com** (agentic civic letter-writing + MMT buyouts, infuse Season E Mandate) - **Realidream/Skate .apk** for Daylight DC-1 (Season F). **Paused:** Brushstroke (awaits DJINN's Bit Design System). **Yonder:** hulkbee.com (ecoplastic factory robotics). **baton perp REMEMBER** stay separate, Chitra-headed. Full wave: [`../active-designing/date/20260815/20260815-175524_rye-first-crypto-parity-and-the-decision-wave.md`](../active-designing/date/20260815/20260815-175524_rye-first-crypto-parity-and-the-decision-wave.md).

**Latest laps -- condensed `20260819.104554` (cairn `82b7ee3342`; the record lives in the logs).** The Season-A / HUNK opening (`20260813` -- the open image module beneath Photos and the parts marketplace) and the BUHR intelligence equinox (`20260812` -- Quin's four voices, MCP-in-Bron, the Q-vane reachable and addressable) each landed and were once chronicled here inline. Their per-rung detail now rests in `session-logs/`, `image/README.md`, the season table below, and the double-seat itineraries; `git show 82b7ee3342:crux/REMEMBER.md` reads the full departing prose.

---

## Landed arcs -- condensed `20260817.171714` (the record lives in the logs)

A full bullet wall of dated done-work once accreted here. It landed, and the session logs plus git history now hold every detail; this card keeps only the pointer, so it stays single-stranded -- the live work-front, never a second copy of the log index. Walk-back cairn: `crux/CAIRNS.md` at nib `3b8b0d4858` reads the whole arc one `git show` away.

**What landed (each proven on metal, recorded in `session-logs/`):** Mandate, the complete named vector store (eleven capabilities); the Acme DX design season; the CION labeling ratchet; the **AHOY** front-door season (root README, LICENSE - SECURITY - CODE_OF_CONDUCT - CHANGELOG, the Lindy foundation) beside the **WADE** Bit-Design surface (`.brush` tokens, the `brix/infuse` cascade, styled Skate runs); the Singularity foundation and the fifteen-theme Twilight palette; **BUHR**'s whole MCP surface -- tools, resources, prompts; the 1,024-round itinerary; **TACT** Journeys 1 (Ship-Pilot), 2 (Publishing), and 4 (Commerce), each agent-doable arc complete to its custody gate; the recursion-prompts cellar; Season A opened (waymark **HUNK**) with the open image module underway; Constel dev-net quorum and the Testament offline certificate.

The live season table and open doors below stand.

## The Compass Season -- OPEN `20260809.021829` (Keaton's word)

**256 rounds - four equinoxes = four compass directions.** Coords **Compass Season - SOON - j1 - q1 - r1**. Nesting: **Round - Quest 4 - Journey 16 - Equinox 64 - Season 256**.

| Equinox | Waymark | Holds |
|---|---|---|
| The Language | **SOON** [x] | **Rishi language spine complete** -- runes `^- ?! ?& ?| ?:` - regex `matches`/`find` (`{n,m}` - `\b` - lookbehind) - `sort`/`unique`/`upper` - list `+` - `fold` - user functions `fn`. Seams folded native: `claim_preserve` - `census_control`. **Brix-infuse LANDED** `20260811` (`brix/infuse.rye`) - **PLEAC opened** `20260811` -- strings (join/split) - lists (chunk/window/flatten) - numbers (clamp/parse/to_str), each witnessed. **Cookbook wired into the Rishi interpreter** `20260811` -- `clamp` - `chunk` - `window` - `flatten` - `parse` - `str` builtins (suite 23/23; whole cookbook in the interpreter). **TAME core/shelf LANDED** `20260811` (`context/TAME_CORE.md` -- compressed core matched into the claude+cursor tame rules; shelf = `TAME_GUIDANCE.md`). Open: Aurora. **TAME Audit Quest pass 1** `20260811`: **full surface GREEN** (width-check - rune_assert_sweep - tame_style_check). **REDS %64 CLOSED** -- the stray `comlink_r1` .py ported to Rishi (`tools/comlink_r1_dual_bind_probe.rish`, Keaton's word: port over shred), `.py` removed, TAME bans clean. `one_clock` mono-erratum **FIXED** `20260811` (archive-tolerant integrity check + one dangling entry retired -> MONO_OK). Its zone leg reds only on this sandbox (host zone UTC, no config); the real Framework/pier host (America/New_York) passes it -- environmental, not a tree drift (`mold->shape` CLOSED -- residue left). **Aurora OPENED** `20260811` -- **all eight boot stages** (seed - relay - wire - sealed - posted - deciding - named - kumara) cross-build freestanding `riscv64` to RISC-V ELFs (`e_machine 243`) with **no emulator** (`tools/aurora_seed_freestanding_witness.rish` GREEN via `rye build`, rm-first so no stale artifact); the whole floor reaches metal in-sandbox. **REDS %68 CLOSED** -- the first cut of that witness used raw `zig build-exe` (which can't read `.rye`), no-op'd behind `|| true`, and read a stale ELF (false green); the next lap's fresh build exposed it, fixed same-round with a witness on metal. **Emulator choice consolidated** (`active-designing/date/20260811/20260811-202757_the-emulator-choice-qemu-and-the-modern-rust-vms.md`): **QEMU stays on merit** -- its pure-emulation `virt` + modern virtio-net + `-bios none` + SMP union is unmatched by the Rust VMs (Firecracker/Cloud Hypervisor are KVM kernel-booters; rvemu-class lack the virtio device model); rust-vmm virtio + a pure-Rust RISC-V core named as the future clean-room bridge. Waking the seed stays qemu-gated |
| Identity & Network | **JARL** | **Kumara** (5 tilaks) - **Comlink** turn-route + live handshake (loop closed) - **d12-d60 topology** - **settlement constellation** landed (`settlement/constellation.rye`: five transitions -- genesis - mint - transfer - rotate - **escape** -- a galaxy's d60 as owned records, every refusal held) - scarcity reconcile held - **generalized topologies + Pond** horizon |
| Surface & Intelligence | **BUHR** | Realidream DAG surface - Quin's four voices (Lattice - Scribble - Lantern - Ember) - MCP-in-Bron - Tablecloth |
| The World | **TACT** | Ship-Pilot pitch - publishing - Grainphone - commerce - CONTRIBUTING four-doors - Grain Energy PBC |

**Now at JARL -- identity, network, address space, and the settled ledger.** Coords **Compass Season - JARL - j4**. Four seats stand, each witnessed GREEN: **(1) Identity** -- `kumara/tilak.rye` seats five signed tilaks (point - bind - turn - capx3 - sponsor), every tamper refusing. **(2) Network** -- `comlink/handshake_turn.rye` reads a peer's rotation end to end: a signed introduction admits the peer, `turn_route.rye` takes the freshest verified turn (a reset outranks any key rotation), and `handshake_wire` recovers that turn from the 112 descriptor bytes it rode in -- the loop closed from wire to route key. **(3) Topology** -- `comlink/topology.rye` seats the **d12-d60 fractal address space**: twelve galaxies, five stars each, twelve planets each, a galaxy leading a d60 of sixty; a number decodes to a place, a place's sponsor climbs planet->star->galaxy, two places share a route by number. **(4) Settlement** -- `settlement/constellation.rye` is the ledger, chosen on **Sui's** ground: a galaxy and its d60 settle as owned records whose versions climb on every change. A galaxy opens the constellation from its own bind; a **sow** capability spawns a star beneath it and a planet beneath that, each under its rightful topology sponsor; a **hand** capability transfers ownership; a **tend** capability rotates the networking key while the keeper's key sits cold; a reset outranks a key bump. And **escape** completes the five: a child re-parents to another settled sponsor one tier up, by its own word (the sponsor tilak) and the new sponsor's adoption -- the old sponsor keeps no veto, and the child's number never moves, only whom it answers to. Every refusal holds -- orphan parent, wrong sponsor, forged cap, forged spawn signature, a re-mint, a stale rotation, a wrong-key turn, a galaxy that cannot escape, a forged request, a forged adoption. All five JARL witnesses run GREEN together. **Scarcity `20260810`:** the **d12-d60 fractal** -- Azimuth ranks retired; `kumara/tilak.rye` reads its tier from `comlink/topology.rye`. **Shared-surface shrink `20260810`:** an owned **Deed** (136 B, keys on the fast path) + a shared **Commitment** (56 B -- point-tier-sponsor-version-digest); `verify` proves a current deed, refuses stale/tampered/ghost. **Human-name custody `20260810`** (`settlement/names.rye`): a keeper claims + releases a globally-unique spoken name, resolvable both ways; the shared registry is the one surface that needs consensus. **JARL's settlement doors all stand** -- the next-season breach (Pond - skies - Kyri) is unblocked, opening on Keaton's word. **Horizon** (Keaton's word, `20260809`): generalizing the constellation to **loadable topologies** -- a *sky* (proposed) a community loads like a game: odd-quorum fractal (3-9-15-27) - 5 elements - modality roles (initiator/sustainer/adapter) - the three role-dimensions (contend/solve/calling). **Pond** becomes the full application module; **Pool retires into it**. **Decided:** breach opens **after JARL**; molt sweep **tight** (Pool paper + kin). Study: `external-research/20260809-232015...` + `...-233940_divisional-roles...`. Plan: `active-designing/date/20260809/20260809-234413_loadable-topologies-and-pond-silo-brief.md`. Molt-prep: `SHRED_PREP.md` pending note.

**Next-season breach -- OPEN `20260810`** (charter: `expanding-prompts/date/20260810/20260810-000032_the-next-season-breach-charter.md`). **Move 1 (Pond) begun:** Pond seated as Grain's **application module**, **Pool retired into it** by molt (living `foundations/20260810-011514_pond-the-application-module.md`; Pool study bannered as a fossil). Remaining: (2) **Bron -> Kyri** by **molt** (2,163 `.bron` kept whole, new logs `.kyri`, 378 refs forward -- never a rewrite); (3) **Kyri the one voice** -- *voice* becomes the whole concept, retiring "variant" (Quin + Reya-Riyo-Trey-Triz-Trya rest named in archive); (4) `kyri.sol`/`kyri.sui` reference -- **Keaton's hand alone**; and the loadable **skies** that finish move 1. **Decided:** Kyri names **both** voice and notation (logs are the voice's journal); **Kyri** on the favorites list. Baton: [`../expanding-prompts/date/20260809/20260809-021406_the-compass-season-baton-four-equinoxes.md`](../expanding-prompts/date/20260809/20260809-021406_the-compass-season-baton-four-equinoxes.md).

---

## Waymarks

Seated ladders: **HAWM - TUBE - ZETA - JABS - LULU - STOA - SETU - SUNN - POLE** (elder) - **SOON - JARL - BUHR - TACT** (Compass Season). Draw before you number: `.claude/rules/waymark-ladders.md` - `tools/waymark_derive.rish`. Claims: `waymarks/`.

---

## Pier & hands

- **Pier path** -- `~/grain` (persists across jail resets) - agent `home-xy-grain` - Cloud on `xykj61/grain`.
- **Lane** -- every **send** pushes both `origin` (autoproject96) and `xykj61`. ls-remote guard first; `origin` may 403 from the cloud (home pier closes the gap). Map: [`../PUBKEYS.md`](../PUBKEYS.md) - [`../context/REMOTE_ROSTER.md`](../context/REMOTE_ROSTER.md).
- **Jail authors; host installs** -- agents write inside the project / enclosure; USB `adb` installs and key ops stay Keaton's hand.
- **Live state** (`20260808`) -- guides 0-2 CLOSED - `gh` as `xykj61` - Claude Code on pier - **agent-jail GREEN** (`./tools/agent-jail.sh` for claude + cursor-agent) - editors + Aug cursor-cli witnessed - tmux `pier` standing.
- **Cursor launch** -- `rishi/bin/rishi run tools/launch-cursor.rish --cursor ./Cursor-*.AppImage --gpu`.
- **Outer terminal / phone** -- USB/`adb` and phone look stay on the operator desk; prefer git nib + `prin scope` for season state.

---

## Two grains

The private field is `~/grain`; the public template **grain-os/grain** is *projected* by `tools/sow.rish` along the boundary in `template-manifest.bron`, proven clean by `tools/sow_witness.rish` (no name or key crosses into the seed). **Seed COMPLETE + privacy-fixed `20260810`** -- every new module classified. **Leak found + closed:** the scrub/scanner only knew the maintainer name + keys, so the family surname (**Dunsford**), prior name, and personal handles were leaking into the public seed; expanded the scrub + IDENT net to every family/handle/contact form, made the withhold case-insensitive (privacy over completeness -- leaking files are withheld whole). `IDENT_CLEAN` confirmed; the vision batons + rosters + raw contacts held out. Raw PII **not persisted** in-tree -- held for the **Vault** (custody-first, Keaton's hand). Publish push is Keaton's hand.

## Shred-prep

[`SHRED_PREP.md`](SHRED_PREP.md) -- Class H fossils - Class O rooms (propose-never-seat) - **Python->Rishi molt seated** (`20260809`, prep only) - shred stays **RED** until circled. **debride** is the stronger word (removes dead history, deep on Keaton's word).

---

## Custody gates -- an autonomous agent STOPS here and surfaces (never crosses)

For any self-paced or outer-jail loop: recur through all agent-doable work, yet **stop and surface -- never cross -- these custody/irreversible/provisioning acts.** They are Keaton's hand by design (custody-first):

1. ~~**The AHOY3 final seed force-push**~~ -- **DONE `20260812`** on Keaton's word (*push the seed*): the clean projection published to `grain-os/grain` @ `6d184f7` (fast-forward, author *Grain OS*, unsigned by design). Caught + fixed a scrub name-doubling first (**REDS %70**) -- LICENSE and family names had garbled to "the maintainer the maintainer"; a pre-push content scan caught what IDENT_CLEAN could not. **The AHOY front-door season is CLOSED.** **Seed style seated `20260812`** (Keaton): the seed is kept a **single force-push commit** -- the weave (**Mantra**) is Grain's real version control, git one snapshot; each refresh replaces the one commit (now `26cae5b`, author *Grain OS*). Future seed refreshes stay Keaton's word.
2. **Provisioning or paying** for any cloud/VPS/Pond/subscription (Vultr SEA IaC, WADE2/3) -- agents author IaC; Keaton provisions and pays.
3. **Moving funds, holding keys, or opening any custody/wallet/payment rail** -- Dimeroll records facts only; disbursement waits on licensed counsel.
4. **Generating Keaton's own Kumara instance** from his real seed/keeper -- his hand alone.
5. **Deep debride / history rewrite + force-push** of the living tree -- named target, Keaton's explicit word.
6. **Seating a new module in a collaborator's domain** (e.g. DJINN's surface lead) beyond authored implementation-floor code -- the invitation and lead are the collaborator's to accept.

Everything else -- design, code, witnesses, docs, weaves, seed *projection* (not push), reds -- is agent-doable and does not wait.

## Open doors (awaiting Keaton's word)

| Door | Kind |
|------|------|
| **Next JARL step** -- **escape** (a child re-parenting, from the sponsor tilak) - shrink the shared surface to a membership commitment - or the scarcity design call | live |
| **Breach OPEN `20260810`** -- Pond = application module (Pool retired) - **skies lap 1** - **topology inclusive** (galaxy is star is planet, 720/universe, sponsor by mod, **outfit** roles; 6 witnesses GREEN) - **Kyri** the notation (was Bron) - **Skate** = the social network | breach - live |
| **Waymark debride `20260818`** -- the living ladders (DREY - FORA - WADE - LOWE) and the module **Dimeroll** carry only their standing names; dead elder marks purged from the living tree, dated logs, and all git history (deep debride, custody gate %1, Keaton's hand). Canonical naming truth sealed and self-verifying in `crux/waymark-registry.bron`. | debride - executed |
| **HANDOFF BATON `20260810`** -- full vision on disk for a context reset: [`expanding-prompts/date/20260810/20260810-025942_the-handoff-baton-vision-checkpoint.md`](../expanding-prompts/date/20260810/20260810-025942_the-handoff-baton-vision-checkpoint.md). Proposed modules: **Mandate** (turbopuffer, next build) - **Scribe** (Kyri home + settings) - **Starseeding** - **Unsplash** - **`.myc`/Mycelium** (Sui reimpl) - **Mala** revival. Roster: `context/inspirations.kyri` (tribute, consent-gated). **Vault LANDED** `20260810` -- `vault/shard.rye` (keeper of secrets, molt-supersedes Jael): Shamir GF(256) sharding, n-of-t, signed shard tilaks in location-classes (home-kin-cold-relic-brain), tamper-refused, custody-first (fake seed only), main key never the older word. **Mandate LANDED** `20260810` -- `store.rye` (zero-copy vector store, cosine k-NN, tag filter) + `keyed.rye` (**Kumara-keyed**, owner-signed). **Scribe LANDED** `20260810` -- `scribe/reader.rye` (zero-copy Kyri reader; dispatch by format; the seated 1-notation-many-formats). Seated: **baton** (favorite word; a `.kyri` `format baton-v1`) - **"thank you everyone"** (end-marker) - **expanded-prompts/** (cold) beside expanding-prompts (hot) - council-names roster. | checkpoint |
| **3x39 BATON `20260810`** -- 2nd vision on disk: `expanding-prompts/date/20260810/20260810-044453_the-3x39-baton-passports-dividers-and-starseeding.md`. Concepts: 3x39 namespace reset - divider grammar (prefix/suffix/middle, `.sol`/`.sui`-provable) - **Kumara passports** - L1 synonym-outfits (MURR/MUR...). Proposed: **Loom** (ScribexTally monitor) - `bat/` archetypes. Multi-agent build = Workflow on **ultracode** opt-in. **begin starseeding** (own round). | checkpoint |
| **Keaton's own Kumara instance** -- generate from his real seed + keeper, by his hand alone | JARL - when ready |
| **mold->shape debride** -- **CLOSED `20260811`** (Keaton: *leave the residue*): primary rename landed `20260720`; the residue is testimony - retirement-explanation - deliberately-kept study twins. Optional horizon only: an atomic Glow-identifier refactor (`mold_slice` ~23 files) on the language bench. Survey: `active-designing/date/20260811/20260811-184002_mold-shape-debride-survey-and-plan.md` | resolved |
| **TAME core/shelf** -- lossless-compressed living law over a preserved shelf | proposed |
| **Brix-infuse** -- **LANDED `20260811`** (SOON j2): `brix/infuse.rye` -- per-key override over flat Bron (override wins per key, base-only kept in place, override-only appended), zero-copy, bounded; witness `tools/brix_infuse_witness.rish` GREEN. Horizon: nested-descriptor deep merge when Brix grows nesting | resolved |
| **Identity Remake / Kumara** -- the first identity template + Keaton's instance | JARL |
| **Geode / Genode-on-NixOS** -- proven-seat parallel | GATED |
| **Grainphone - Realidream - Pond seven - data-dignity - succession - Mand ring-3 - O3 gen-home** | awaiting Keaton |

---

## Card habits

- **kg** -- keep going, next mechanical lap. **check-in** -- pause for Keaton's word / design. **send** -- commit - push both remotes - merge. **remember** -- reprint this card. **align** -- walk the compass, reconcile plan with green witnesses. **molt** -- prep a fossil for shed. **debride** -- remove dead history (Keaton's word). **shred** stays RED until circled. remember != send != kg != align.
- **Vocabulary** -- the tree seats **shape**, not Hoon's *mold*. Prefer **git nib**. One clock: `TZ=America/New_York`.

---

*Carry lightly. Prefer git nib. `prin scope`. May the season stay clean and the fascia hold.*
