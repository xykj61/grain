# REMEMBER -- living operator card

**Language:** EN
**Status:** Living pin -- operator carry card
**Voice:** Kyri

## INNER LOOP -- live directives the running loop applies each lap (seated `20260816.214652`)

*The outer shell loop reads this card first every lap, so directives here take effect on the NEXT lap without a restart. The agent MAY edit this block as it goes -- it is the inner loop the outer loop points at. Keep it ASCII, short, and current; move settled items down into the body.*

- **ASCII-first.** Write every new document, comment, and commit message in plain ASCII. No em-dashes, middots, curly quotes, arrows, or non-ASCII math -- use `--`, `-`, `'`, `"`, `->`, `<=`, `gamma_2`. The one exception is an explicitly-named set of work rounds (e.g. a Unicode-handling module's own fixtures). Rule: `.claude/rules/ascii-first.md`. This card was corrupted to mojibake once (REDS #83); ASCII-first is how it never happens again.
- **Spelling: American.** Use `color`, not `colour`; normalize `colour -> color` on touch. This is a USA project.
- **Style sweep before every send** -- Radiant pass over the round's prose (Twilight for a night piece), register only never a claim. Seed section 6.
- **Rota of the canon.** Each lap, read ONE doc from the 27-doc council rota in `recursion-prompts/seed/autonomous-loop.seed.md` section 1, cycling by lap index, so the durable foundations and context stay in awareness across the run.
- **Living-pin guards healed (`20260817`, REDS #87):** the `work-in-progress -> crux` rename had left the e123 living-pin guard RED four ways (stale paths, em-dash headers the ASCII sweep had moved to `--`, a gone SEAT_MAP header, and REDS grown past its 24576 bound). All repointed to `crux/`, headers matched to the pins as they read now, REDS folded (rows 74-79 to `archive/`) back under bound -- e123 GREEN, its planted-empty-pin RED path still caught. And REDS #83's booked prevention now stands: `tools/living_card_ascii_witness.rish` enforces the operator card and REDS at zero bytes above 0x7F and catches a planted mojibake control on metal. Advisory ratchet: 244 legacy non-ASCII lines across the older pins to sweep on touch; REMEMBER itself is ~200 KB, over its advisory bound -- a condensation debt for a later lap.
- **Live edge:** the Season G open-media family stands broad and green. The SCENE-READ family is building a surface's interaction floor: `image/scene_manifest.rye` enumerates every object (area, box, Euler, holes), `image/hit_test.rye` names the ONE object under a POINT, and now `image/region_select.rye` (`20260818.230130`, GREEN) names EVERY object a query RECTANGLE covers -- the marquee / drag-select and crop-region primitive, carrying the hit test's box-is-a-prefilter crux one dimension up (an object's box can overlap the rectangle while the object places no pixel there -- an L's concavity, a ring's hole -- so the box is the prefilter and the label map the arbiter). The shape-descriptor quest CLOSED with the roundness family (inner/outer/universal exact, no float no tolerance). The media codecs are deep and each green-witnessed: color (`image/color.rye` + 18 siblings, Season G's named crux, DONE `20260816`), fonts (`image/font5x7.rye` + ~45 glyph families), and video (`image/frames.rye` + `image/frames_delta.rye`), all composing over the proven QOI codec. 173 image modules stand. NEXT, crux-first and agent-doable: name the next open-media rung that composes over this floor and closes cleanly with a GREEN witness (a further scene read -- sort/filter a selection by joined facts, a lasso or polygon select, an object-count-in-rect summary -- or a codec read), OR advance the Lindy-priority Microkernel Target double-seat's agent-doable STUDY rungs -- yet its seL4 (GPLv2) / Genode (AGPLv3) clean-room study wants a gratitude-fetch plus a per-component license read, a FETCH/LICENSE GATE surfaced here, not crossed. The round top-hat on a real photo also waits on a fetch gate.
- **Seated names (`20260816`):** **Scooter** = the CLI chat app (Talk reimplementation on Pond); **Dexter** = the terminal module (our Dill parallel, affirmed); **Lumen** = the inference vane (renames Quin's Q-vane, gathering Lattice, Lantern, Ember, Scribble). Plan: `expanding-prompts/20260816-222322_dexter-terminal-and-scooter-cli-chat.md`.
- **Seated breaches (cairn-first when executed):** **Quin's Q-vane -> Lumen** (rename its references); **Bron -> Kyri** and **Quin voice -> Kyri** (docs-compression season). Each is its own signed round, cairn first; none cut yet.

*One-page carry card for outer terminal - phone - waymarks. Refreshed when Keaton says **remember**. Debrided to the Compass Season `20260809.024320` -- the old season marks are gone; their greens live in the code and in the dated counsel.*

**Git nib:** `9b365ef260` (last code edge `image/region_select.rye` + `tools/region_select_witness.rish` GREEN, `20260818.230130` -- the RECTANGLE-of-interest select: given a query rectangle, name every object it actually covers, the marquee / drag-select and crop-region primitive one dimension up from the point hit test. The decisive box-is-a-prefilter crux: a rectangle tight on a block in an L's concavity overlaps BOTH boxes yet selects one object; a rectangle in the empty concavity or wholly inside a ring's hole overlaps a box yet selects nothing -- the box claims a rectangle the object does not fill, so the box is the prefilter and the label map the arbiter; an off-map marquee is a valid empty selection, a malformed rectangle refuses BadRect. GREEN on the first full metal run after one brace-escape build fix, no algorithm red. Full account in `session-logs/20260818-230130_region-select-rectangle-of-interest.kyri`. <!-- prior nib: d2368cb9db image/roundness_universal.rye -- CLOSED the universal inner-<=-outer roundness proof by exact inscribed radius; card condensed 20260818.225452, cairn d2368cb9db in crux/CAIRNS.md -->)

**Prior living edges -- condensed `20260818.225452` (cairn `d2368cb9db`).** The whole roundness quest's per-nib comment wall once stacked here -- roundness_universal, roundness, hit_test, scene_manifest, euler_scene, euler_number, and every earlier shape descriptor back through regions, some three dozen former live edges as `<!-- prior nib -->` comments. Each is a real commit recoverable by `git log --oneline`, and each rung carries its own dated log in `session-logs/` plus a durable entry in `image/README.md`. Folded to this pointer so the operator card stays the live card of what is next, never a second copy of the log index. Earlier open-media condenses: `20260818.101851` and `20260817.172514` (cairns). Walk-back: `git show d2368cb9db:crux/REMEMBER.md` reads the full verbose stack.

**Host:** Framework - EDT (`America/New_York`) - Vultr SEA VPS (HP AMD 2vCPU/4GB shared - never EWR) - this session in ai-jail

**Refinement `20260815.191048` -- Kumara crypto re-grounded (not "Lindy"), host updated, Puddle identities cleared, Brix in plan.** Keaton rightly rejected the Lindy metaphor for crypto (past != future when compute/quantum/AI-cryptanalysis all climb). The SLH-DSA choice **stands, re-justified structurally**: hashes have *no algebraic structure* for the rising attacks (Oct 2025 lattice sieving, CRYPTO 2026 sign-based key recovery, AI cryptanalysis) to exploit; quantum is only a Grover quadratic dent (256-bit output covers it); it adds *no new assumption* (Grain already relies on the hash everywhere -- seals, receipts, Merkle catalogs); and no mature *independent* family exists to hybridize with (code/multivariate broken, isogeny young, lattice eroding, symmetric-MPC not independent). **Confident. Parameter set: SLH-DSA-SHAKE-256s** (NIST cat 5). **Puddle cleared:** the agent may key + bootstrap **fake/test Kumara identities** in the constel/Puddle sandbox (consonant-only, non-@p-valid per placeholder-ship-names/FORA) -- the maintainer's *real* Kumara key stays the custody gate. **Brix seated in planning** (Brix config code + Brix docs molt, riding the docs-compression round). **Host updated** (GLOW_PROFILE): editor **Claude Code** (not Zed/Cursor), model claude-opus-4-8, host **Vultr SEA NixOS VPS**, hand **Daylight DC-1** (was Android Termux). No nixos-rebuild needed yet for the crypto rounds; will flag if SLH-DSA parity wants vendored FIPS 205 KAT vectors or a toolchain bump. Brief: [`../active-designing/20260815-184832_sha3-preference-and-post-quantum-kumara.md`](../active-designing/20260815-184832_sha3-preference-and-post-quantum-kumara.md).

**Kumara PQ SEATED `20260815.185922` -- SLH-DSA (SPHINCS+) on SHAKE-256, the most-Lindy path.** After an Aug 2026 news pass (Oct 2025 quantum-sieving cut the lattice-attack exponent ~8%; CRYPTO 2026 carries lattice-signature key-recovery papers), the century-root does not anchor to the assumption under active erosion. Kumara v1 signs **pure hash-based** -- security reduces to the hash alone, the oldest/most-Lindy primitive, industry's 30-year-guarantee tool -- making the SHA-3 preference load-bearing (identity is SHAKE-256). **Algorithm agility** seated: identity format carries a scheme tag + version so a second leg/migration accretes later (accrete-never-break for crypto). **Lattice co-signature deferred, not baked in** (revised my earlier SLH-DSA+ML-DSA hybrid lean on the evidence). Loop crux: SHA3-512/SHAKE-256 in Rye -> SLH-DSA build round, parity vs NIST FIPS 205 KATs; keygen/signing with the identity key stays the custody gate.

**Crypto policy `20260815.184832` -- SHA-3 preferred; Kumara goes post-quantum.** SEATED: prefer SHA-3/SHAKE over SHA-2 for new designs (structural defense-in-depth -- sponge, no length-extension -- *not* because SHA-2 is broken; SHA-512 stays for Monocypher/Ed25519 parity). Near-term no-gate crux: **SHA3-256/512 + SHAKE-256 in Rye**, parity vs Zig std.crypto + NIST FIPS 202 KATs (Monocypher has no SHA-3). CORRECTION seated: no elliptic curve is quantum-resistant (Shor breaks the curve regardless of hash); 512-bit hashes are already quantum-safe (Grover only halves -> ~256-bit). So Kumara's quantum answer is a **post-quantum signature, not a curve** -- and the conservative ones are hash-based, aligning with SHA-3. PROPOSED (wants Keaton's word): Kumara PQ scheme -- **A** SLH-DSA/SPHINCS+ (SHAKE-256, hash-based, most auditable) - **B** ML-DSA/Dilithium (SHAKE, fast, lattice) - **C** hybrid Ed25519+PQ. Breaking OpenSSH interop accepted. PROPOSED: a checkable security floor (refuse weaker primitives by named error) + root-README post-quantum promise + debride of weaker usage. Signing with the identity key stays the custody gate. Brief: [`../active-designing/20260815-184832_sha3-preference-and-post-quantum-kumara.md`](../active-designing/20260815-184832_sha3-preference-and-post-quantum-kumara.md).

**Decision wave `20260815.175524` -- Rye-first spine + Season G Cryptography double-seat.** Rye is the prioritized language; Glow is implemented on green-witnessed Rye. **Season G -- Cryptography** double-seats: reimplement Monocypher primitives in Rye with byte-for-byte parity witnesses against `vendor/monocypher` on RFC vectors (audit-ready) -- this unblocks the Lotus signed carry (the *library* is agent-doable; signing with the maintainer's identity key stays the custody gate). **Two breaches cairned at `9e3c2dccfa`** (each its own signed round): **Bron->Kyri** (one notation under Kyri -- voice - notation - *compressed receipts* - preferred Grain variant, gratitude to Kyrie Irving) and **work-in-progress->crux** (902 refs repointed; sorts higher). **Standing approval seated:** update the public seed with force pushes as needed -- the *deep* refresh fires once, after the docs-compression molt + README-leaf weave. New surfaces: **deemlow.com** (fair-trade live-shopping, Kumara login, 1Password interim -> Vault-audit gate, Season B) - **anticruel.com** (agentic civic letter-writing + MMT buyouts, infuse Season E Mandate) - **Realidream/Skate .apk** for Daylight DC-1 (Season F). **Paused:** Brushstroke (awaits DJINN's Bit Design System). **Yonder:** hulkbee.com (ecoplastic factory robotics). **baton perp REMEMBER** stay separate, Chitra-headed. Full wave: [`../active-designing/20260815-175524_rye-first-crypto-parity-and-the-decision-wave.md`](../active-designing/20260815-175524_rye-first-crypto-parity-and-the-decision-wave.md).

**Latest lap `20260813` -- Season A opens (waymark HUNK).** The Mycelium consensus season (Season D's protocol half) stands whole and witnessed (98 modules - 80 witnesses - foundation + README) -- a clean season close -- so by Lindy-first crux-first the road turned to **Season A (Hardware & Right-to-Repair)** and its opening journey, the **open image module** beneath the parts marketplace and the Photos app. **HUNK0 LANDED:** `image/qoi.rye` -- a whole open **QOI** codec (encode + decode), so `decode(encode(pm))` recovers a pixmap **byte-for-byte** across all six chunk kinds (rgb-rgba-index-diff-luma-run), real compression (37 < 256 raw), five corruptions each refusing by name. Waymark **HUNK** seated (derive script - both rule twins - Lexicon); QOI thanked clean-room (`gratitude/qoi.md`). Witness `tools/hunk_qoi_witness.rish` GREEN. **HUNK1 LANDED `20260813`:** `pond/apps/image_artifact.rye` -- an image is a content-addressed **Tablecloth** artifact. `store_image` encodes a pixmap to QOI and beads it under a name (the name pins a SHA3 content address); `fetch_image` reassembles by name, proving every bead against its digest **first**, so a tampered bead refuses **DigestMismatch** and an unknown name refuses **UnknownArtifact** -- each **before `qoi.decode` reads a single pixel**. Selftest rides a real 8x8 image THROUGH the store byte-for-byte (166 bytes stored), shows the dedup dividend (same image, two names, stored once -- 2/2 beads deduped). Witness `tools/hunk_artifact_witness.rish` GREEN; TAME + width clean; HUNK0 still GREEN (`qoi.rye` symlinked into `pond/apps/`, code untouched). **HUNK2 LANDED `20260813`:** `brushstroke/image_skate.rye` -- the decoded grid meets **Skate**. `down_map` partitions the image into colsxrows blocks, averages each block's color, quantizes to the nearest of a seven-color anchor palette, and wears the matching slot over a full-block glyph (a full-block extended-glyph atlas set on Skate's `extended_glyphs` seam) -- so `rasterize` paints every cell a solid color block. Proven on metal: an 8x8 two-tone image (red left, blue right) down-maps to 2x2 with red reaching slot 3 and blue slot 5 (**spatial**), the map is **deterministic** (same image, same cells), the rasterized 16x32 canvas carries the real down-mapped colors (left cell red, right cell blue, 256 lit pixels = one glyph block per cell), and an empty image / over-ceiling grid each refuse by name. Both grid axes bounded (`max_map_cols - max_map_rows`). Witness `tools/hunk_skate_witness.rish` GREEN; TAME + width clean. **HUNK3 LANDED `20260813` -- the opening HUNK0-HUNK3 quest is COMPLETE:** `image/photos.rye` -- the first Photos verb, a **crop**. `crop` is a pure, bounded function over the decoded RGBA grid: it reads a rectangle and returns a fresh smaller image, the source untouched. Proven on metal: cropping a position-tagged 6x6 image, **every cropped pixel is exactly its source pixel**; the source is unmutated (**pure**); the identity crop recovers the whole byte-for-byte; and a cropped image is still an **open image** -- it re-encodes and round-trips through the codec byte-for-byte. Coordinates checked in u64 so a rectangle can never wrap; empty and both out-of-bounds rectangles refuse by name. Witness `tools/hunk_photos_witness.rish` GREEN; TAME + width clean. **The Open Image journey stands whole: HUNK0 codec - HUNK1 content-addressed - HUNK2 painted - HUNK3 cropped** -- an open image module beneath the parts marketplace and the Photos app, no permission asked to read a pixel. **HUNK4 LANDED `20260813` -- the sprite-marketplace quest opens:** `image/sprite.rye` fixes Season A's McMaster-Carr trick over the open module -- a `SpriteIndex` binds each product name to a bounded rectangle (a **window**) into ONE decoded sheet, and `render` returns that window as a fresh open image by **reusing HUNK3's `photos.crop`**. Every window is bound-checked in u64 at **add time**, so a render can never fail on bounds; unknown name - out-of-bounds - empty - duplicate - overlong each refuse by name. Proven on metal: three products indexed into one 8x8 tagged sheet (256 bytes **decoded once**), each renders exactly its window, disjoint windows carry different pixels (no product leaks a neighbor's bytes), the sheet is shared and **unmutated** across every render, and a rendered product re-encodes and round-trips byte-for-byte. Witness `tools/hunk_sprite_witness.rish` GREEN (HUNK3 crop re-run GREEN beneath); TAME + width clean. Design `active-designing/20260813-201500_hunk-sprite-marketplace-index-exploration.md`. **HUNK5 LANDED `20260813` -- the product page is seen:** `brushstroke/sprite_page.rye` `paint_product` joins HUNK4's window and HUNK2's down-map -- it renders a product's window out of the one sheet (reusing HUNK3's crop) and down-maps it to a Skate grid, so a catalog entry is not only stored and windowed but **seen**. Proven on metal: two products indexed into one two-tone sheet (red left, blue right) paint distinct pages (red -> slot 3, blue -> slot 5, pages differ -- a page is seen as its own product, never a neighbor's), the page **equals a direct down-map of the window cell for cell** (no drift between index and page), lit pixels reach a real canvas (256), and an unknown product refuses `UnknownProduct` through the whole composition before any paint. Pure composition over public APIs (sprite.rye + photos.rye symlinked into `brushstroke/`); no new storage, codec, or palette. Witness `tools/hunk_sprite_page_witness.rish` GREEN (HUNK4 index re-run GREEN beneath); TAME + width clean. **HUNK6 LANDED `20260813` -- the sheet is content-addressed:** `pond/apps/sprite_store.rye` gives the marketplace sheet an identity -- `store_sheet` beads the sprite content-addressed (HUNK1's `image_artifact.store_image`), `open_sheet` fetches it back proving every bead against its digest **before the codec reads a pixel**, so a tampered sheet refuses `DigestMismatch` and an unknown market refuses `UnknownArtifact` **each before a SpriteIndex is built or a product rendered**. Proven on metal: a two-tone sheet stored (54 bytes), a market opened over the **verified** sheet rendering two products from their own windows, then one stored byte flipped and the whole market refusing before any product renders. Pure composition over public APIs (sprite.rye + photos.rye symlinked into `pond/apps/`). Witness `tools/hunk_sprite_store_witness.rish` GREEN (HUNK1 verified fetch re-run GREEN beneath); TAME + width clean. **HUNK7 LANDED `20260813` -- the marketplace catalog travels; the sprite quest CLOSES:** `image/sprite_catalog.rye` lets the index itself travel -- `render_catalog` writes a `SpriteIndex` as a flat-Bron `format sprite-catalog-v1` record (one `product <name> <x> <y> <w> <h>` line per entry), `parse_catalog` reads it back into a fresh index **over a given sheet**, so `render(parse(render(x)))` is a fixed point and the rebuilt index renders every product from the same window byte-for-byte. Parse validates each window against the **real sheet** (through `SpriteIndex.add`), so a catalog can never name bytes the sheet lacks -- proven by parsing over a smaller sheet and refusing `WindowOutOfBounds`; a bad header, a malformed line, and an extra field each refuse `BadCatalog`. Added two additive pub accessors to `sprite.rye` (`len`/`entry`) so a reader walks the catalog without private cell storage; HUNK4 re-runs GREEN. Witness `tools/hunk_sprite_catalog_witness.rish` GREEN; TAME + width clean. **The sprite-marketplace quest stands whole: HUNK4 index - HUNK5 seen page - HUNK6 content-addressed sheet - HUNK7 travels as text** -- McMaster-Carr's one-sheet trick over the tree's own open module, one decode serving a whole catalog. **HUNK8 LANDED `20260813` -- the Photos app turns and mirrors; a fresh quest opens:** `image/photos.rye` gains three pure bounded verbs added **additively** beside `crop` (untouched) -- `flip_h` (mirror left-right), `flip_v` (mirror top-bottom), `rotate_quarter(turns)` (clockwise quarter turns; a 90/270 turn swaps width and height). Each returns a fresh `Pixmap`, leaves the source untouched, and refuses a zero-dimension image `EmptyImage`. The witness proves them by the **algebra** they must obey -- rotate_quarter four times is identity byte-for-byte, `rotate_quarter(2)` equals `flip_h(flip_v)`, each mirror is its own inverse, `rotate_quarter(k)` equals k single turns -- so an index bug is caught regardless of the arithmetic; a non-square 3x5 tagged image makes the width/height swap visible, a concrete corner pins the turn clockwise, a rotated image still re-encodes and round-trips as an open image. Added `/pond/apps/bin/` to `.gitignore` (compiled witness binaries were untracked). Witness `tools/hunk_photos_orient_witness.rish` GREEN; TAME bans + width clean; HUNK3 crop still GREEN beneath. Design `active-designing/20260813-153700_hunk-photos-orient-exploration.md`. **HUNK9 LANDED `20260813` -- the Photos app resizes:** `image/photos.rye` gains `scale(new_w, new_h)` added **additively** beside crop + the orient verbs (all untouched) -- nearest-neighbor, output pixel `(nx,ny)` samples src `(nx-w/new_w, ny-h/new_h)`, sampling in u64 so a product can never wrap; refuses a zero-dimension source/target `EmptyImage` and a target past the `2^20` ceiling `OutOfBounds` before allocating. Witness proves identity resize byte-identical, a 2x nearest upscale sampling every pixel from `src(nx/2,ny/2)`, a downscale to 1x1 picking the top-left pixel, the scaled image still re-encoding + round-tripping as an open image. Nearest-neighbor is the honest first scaler; a bilinear/area-average resampler is a named horizon on the same signature. Witness `tools/hunk_photos_scale_witness.rish` GREEN; TAME + width clean; HUNK3 crop + HUNK8 orient still GREEN beneath. **HUNK10 LANDED `20260813` -- the Photos app tunes light:** `image/photos.rye` gains `adjust(brightness, contrast_num, contrast_den)` added **additively** beside crop + orient + scale (all untouched) -- each color channel takes the affine map `out = (v-128)-num/den + 128 + brightness`, clamped into `0..255`, so contrast pivots around the `128` midpoint and brightness shifts the whole channel; **alpha passes through untouched**. The map runs in `i64` so no product wraps, with contrast terms bounded by a named ceiling (`adjust_contrast_max = 1<<16`) and brightness bounded to a full range (`adjust_brightness_max = 255`) or refuses. Witness proves a neutral adjust (brightness 0, num==den) byte-identical to the source, a `+40`/`-40` brightness shift-and-clamp per channel, a `2/1` contrast pivoting around 128 (0->clamp 0, 100->72, 200->clamp 255), a `0/1` contrast flattening to the midpoint 128, the adjusted image still re-encoding + round-tripping as an open image, and a zero denominator (`BadContrast`), an over-range brightness (`BadBrightness`), and an empty image (`EmptyImage`) each refused by name. Nearest gestures beyond: hue/saturation and a non-destructive edit-list (record verbs as data, replay + content-address) are named horizons. Witness `tools/hunk_photos_adjust_witness.rish` GREEN; TAME + width clean; HUNK3 crop + HUNK8 orient + HUNK9 scale still GREEN beneath. Design `active-designing/20260813-154324_hunk-photos-scale-exploration.md`. **HUNK11 LANDED `20260813` -- the Photos app edits non-destructively:** `image/photo_edits.rye` records the six Photos verbs **as data** -- an `Edit` tagged union (one variant per verb), a bounded `EditList` (`max_edits = 64`), and one `apply` that **replays** the whole list over a source image (folding each verb's fresh result from a clone) to produce a fresh edited image, the source never mutated. Replay is **deterministic** (same list + same source -> same bytes every time, the content-address guarantee an edited image never needs its pixels stored, only the pair source + edit-list), and the list **travels as a flat-Bron `format photo-edits-v1` record**, so `render(parse(render(x)))` is a fixed point like the sprite catalog's. Proven on metal: a 3-verb list replayed over a 6x6 image equals the verbs by hand byte-for-byte, deterministic, **non-destructive** (the source untouched, the empty list recovers it byte-for-byte), the edited image still re-encodes and round-trips as an open image, every verb kind travels to the same bytes, and a bad header - unknown verb - malformed field - extra field each refuse `BadEdits` while an out-of-bounds crop surfaces `OutOfBounds` **through the fold** (no new failure mode). Witness `tools/hunk_photo_edits_witness.rish` GREEN (58-byte fixed-point record); TAME bans clean, width exit 0; the Photos verbs untouched beneath. Design `active-designing/20260813-155719_hunk-photo-edits-non-destructive-exploration.md` names HUNK12-14 horizons (bead the edit-list content-addressed - the Skate view of a pending edit - revert/truncate). Next agent-doable crux: HUNK12 (bead an edit-list into Tablecloth so `(image, edit-list)` reproduces the edited image from cold), another Photos gesture (a smoother resampler, or hue/saturation), or another Season-A / double-seat journey. **HUNK12 LANDED `20260813` -- the edit-list is content-addressed:** `pond/apps/edit_store.rye` banks HUNK11's determinism -- `store_edits` renders an `EditList` to its `format photo-edits-v1` text and **beads it content-addressed** (HUNK1's Tablecloth store, bounded to a resin or refusing `EditsTooLarge`), and `reproduce` fetches it back proving every bead against its digest **before the text is parsed or a verb replayed**, then parses and applies it over a source image. So an edited image **reproduces from cold** out of the pair `(source, edit-list address)`: its pixels never need storing, only the recipe that makes them. Proven on metal: a 3-verb recipe banked (58 bytes), the cold reproduction equals a direct apply byte-for-byte, the same recipe under two names stored once (deduped 1/1 beads), a tampered recipe refusing `DigestMismatch` and an unknown name refusing `UnknownArtifact` each before a verb replays. `photo_edits.rye` symlinked into `pond/apps/`. Witness `tools/hunk_edit_store_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK13 LANDED `20260813` -- a pending edit is seen before it is committed:** `brushstroke/edit_preview.rye` is the eye -- `preview` replays an edit-list over a source image (`photo_edits.apply`, pure -- the source untouched, **nothing stored**) and down-maps the fresh result to a **Skate grid** (`image_skate.down_map`) that lights the canvas, so a keeper **sees** the edit before committing it. Pure composition over two proven seams, adding **no new failure mode**. Proven on metal: a pending `flip_h` over an 8x8 two-tone previews to a 2x2 grid **equal to `down_map(apply(source,list))` cell-for-cell**; the source is untouched and the **empty list previews to the plain source down-map** (non-destructive); the pending edit **changes the view** (left cell red->blue); deterministic; the preview rasterizes to 256 lit pixels with blue in the left cell; an out-of-bounds crop refuses `OutOfBounds` through the fold, an empty grid `EmptyImage`, an over-ceiling grid `GridTooLarge` -- **seen, not committed; no name, no bead, no funds**. `photo_edits.rye` symlinked into `brushstroke/`. Witness `tools/hunk_edit_preview_witness.rish` GREEN; TAME bans clean, width exit 0. Design `active-designing/20260813-160955_hunk-edit-preview-seen-before-committed-exploration.md`. **HUNK14 LANDED `20260813` -- the Photos app reverts:** `image/photo_revert.rye` banks HUNK11's non-destructiveness -- because the source is never mutated, a revert is only dropping edits off the end of the list. `drop_last` forgets the last k edits, `truncate` keeps the first k, `revert_one` drops one -- all **pure over the count**, returning a fresh shorter list and leaving the caller's own list whole, so a keeper reverts without ever losing the edits they might redo (no pixels stored, no history log replayed backward, only the recipe shortened). The earlier image is one `apply` of the shorter list away; the original is one `apply` of the empty list away. Proven on metal: a one-step revert of a 3-verb list equals the first two verbs by hand **byte-for-byte**; `truncate` and `drop_last` are duals; dropping zero is identity; dropping all recovers the **original** one empty-list apply away byte-for-byte; an over-count drop **clamps to empty without underflow** while `truncate` past the length keeps the whole list; the source list is untouched so a revert can be redone; a revert is a **byte-prefix** of the full `format photo-edits-v1` record and still round-trips as an open fixed-point record; the empty list reverts to itself. Witness `tools/hunk_photo_revert_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK11 untouched beneath. **HUNK15 LANDED `20260813` -- the Skate view of a revert, seen before it is committed:** `brushstroke/revert_preview.rye` joins HUNK14's revert to HUNK13's preview -- `preview_revert` drops the last k edits (`photo_revert.drop_last`, pure -- the input list untouched) and hands the shorter list to `edit_preview.preview`, returning the Skate grid of the earlier image, so a keeper **sees exactly what a revert will restore** before committing it. Reverting all the way previews the **original** -- the empty list's own down-map -- a look, not a reconstruction (no pixels stored, no history replayed backward). Pure composition over two proven seams, adding **no new failure mode**. Proven on metal: a one-step revert of a `flip_h` over an 8x8 two-tone previews to a 2x2 grid **equal to the source down-map cell-for-cell** (the original restored, left cell blue->red); a revert of zero is the full view; reverting past the count **clamps to the original**; the input list is untouched; deterministic; reverting away an out-of-bounds crop **rescues** a refusing list to preview cleanly; the restored view rasterizes to 256 lit pixels with red in the left cell; an empty grid refuses `EmptyImage` through the revert. `photo_revert.rye` symlinked into `brushstroke/`. Witness `tools/hunk_revert_preview_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK13/14 untouched beneath. **HUNK16 LANDED `20260813` -- the edit cursor: undo and redo over one history:** `image/edit_cursor.rye` holds the one piece redo actually needs -- an `EditCursor` carries the whole reachable history (a HUNK11 `EditList`) plus a `position` naming how many edits are in effect, with the governing invariant `position <= count`. `undo` steps the position back, `redo` steps it forward, and `view` replays the first `position` edits (HUNK14's `truncate` + HUNK11's `apply`) -- so a walk **destroys nothing** and ten undos roll forward again byte-for-byte. The one new law (the crux): a fresh `push` while rewound **forgets the redo tail** -- the road not taken is discarded the moment a different one is taken. Composes two proven seams, inventing no storage and no new failure mode. Proven on metal: a 3-edit history walks (undo to 2 equals the first two verbs by hand, redo returns to the full image byte-for-byte); rewinding to the original recovers the source and every step rolls forward again unchanged; a fresh edit while rewound forgets the discarded verb (the new road replayed by hand); undo/redo **clamp at both edges** without under/overflow; the applied prefix still round-trips as an open fixed-point record; the 64-edit ceiling refuses `TooManyEdits`. `edit_cursor.rye` reuses `image/` symlinks; a pure look and pure move -- no name, no bead, no key, no funds. Witness `tools/hunk_edit_cursor_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK11/14 GREEN beneath. Design `active-designing/20260813-171500_hunk-edit-cursor-undo-redo-exploration.md`. **HUNK17 LANDED `20260813` -- the Skate view of a cursor's neighbors:** `brushstroke/cursor_preview.rye` paints what an undo or redo would restore **before** a keeper walks -- `preview_at` walks a **value copy** of the cursor to a clamped position and hands its applied prefix to HUNK13's `edit_preview.preview`, so the caller's cursor is **untouched** (a look, not a walk); `preview_undo`/`preview_redo` show the one-step neighbors. The property only a cursor offers (a plain revert cannot, having thrown its tail away): a rewound cursor **retains** its redo tail, so `preview_redo` shows a real image of the road ahead -- and after HUNK16's forget-the-tail push, that same preview honestly **falls back to the current view**. Proven on metal over the 8x8 two-tone: a two-flip history rewound one step previews undo -> the original (left cell red), redo -> the retained tail cell-for-cell, the current view differing (left cell blue); three previews leave the cursor exactly where it stood; a fresh push forgets the tail so `preview_redo` equals the current view **and** a direct apply of the pushed flip_h+crop cell-for-cell; 256 lit pixels. Pure composition over HUNK16+HUNK13, no new failure mode. **Ratchet caught:** `tame_style_check` flagged four compound asserts here and, on inspection, eight in HUNK16's `image/edit_cursor.rye` that had slipped (the tame scan fixture does not cover `image/`) -- split all twelve as an additive tidy this round. `edit_cursor.rye` symlinked into `brushstroke/`. Witness `tools/hunk_cursor_preview_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK16/13 GREEN beneath. **HUNK18 LANDED `20260813` -- the editing session survives a cold restart:** `pond/apps/session_store.rye` beads a whole `EditCursor` content-addressed -- the crux a plain edit-list store (HUNK12) could not reach. `store_session` renders the cursor as a `format photo-session-v1` record (a `position` line wrapping HUNK11's own edit-list body, so the verb grammar is reused, not duplicated) and beads it (HUNK1's Tablecloth store); `open_session` fetches it back -- every bead proven against its digest and the whole against its recorded address **before** parse -- and rebuilds the cursor. Proven on metal over the 6x6 tagged image: a three-edit session **rewound to position 2** (a redo tail waiting) reopens from cold standing exactly where it was left, **undo AND redo both intact** -- the redo tail was stored, not just the applied prefix -- its view matches byte-for-byte, and rolling the tail forward reaches the same image the original would have. Dedup dividend (same session, two names, stored once, 2/2 beads); a tampered session refuses `DigestMismatch`, an unknown name `UnknownArtifact`, a session standing past its own history `BadSession` (the `EditCursor` position bound enforced at the trust boundary), each before a verb is replayed. `edit_cursor.rye`+`photo_revert.rye` symlinked into `pond/apps/`. Witness `tools/hunk_session_store_witness.rish` GREEN; TAME bans clean (one compound assert split), width exit 0; HUNK16/12/1 GREEN beneath. **HUNK19 LANDED `20260813` -- a keeper lists their saved editing sessions:** `pond/apps/session_list.rye` walks the catalog (`tablecloth.artifact_name`), selects every `.session` artifact, and `open_session` each (HUNK18, every bead proven against its digest **before** parse), recording a bounded `SessionSummary` -- name, edit-count, live position (+ a `redo_depth`). The honesty guarantee, borrowed from `mcp_resource_list`: a listing **never names a session it cannot honestly reopen**, so a tampered session refuses `DigestMismatch` through the open and the **whole** listing refuses with it, never advertising a phantom. Proven on metal: a catalog of 3 (two sessions + one `notes.txt`) lists **exactly** the two sessions in held order -- the non-session **skipped, never opened** -- each summary read from a verified reopen (`portrait.session` at 2/3, redo depth 1; `landscape.session` at 2/2); the listing travels as an 82-byte `format session-list-v1` record; a tampered held bead makes the whole listing refuse `DigestMismatch`. Witness `tools/hunk_session_list_witness.rish` GREEN; TAME bans clean (two compound asserts split), width exit 0; HUNK18 GREEN beneath. **HUNK20 LANDED `20260813` -- the session listing travels both ways:** `parse_listing` (additive on HUNK19's own `pond/apps/session_list.rye`) reads a `format session-list-v1` record back into a fresh listing, so `render(parse(render(x)))` is a fixed point exactly as the sprite catalog's is (HUNK7). Each line names a session and exactly its two counts; a bad header, an unknown tag, a malformed field, an extra field, and a **position standing past its edit-count** each refuse `BadListing` -- the `EditCursor` bound enforced again at the text edge. Proven on metal: the 82-byte record round-trips byte-for-byte and five malformed records each refuse by name. Witness `tools/hunk_session_list_witness.rish` extended, GREEN; TAME bans clean, width exit 0; HUNK19 GREEN beneath. **The session persistence story stands whole -- HUNK18 store - reopen - HUNK19 list - HUNK20 travel.** **HUNK21 LANDED `20260813` -- the Photos app tunes saturation:** `image/photos.rye` gains `saturate(sat_num, sat_den)` added **additively** beside crop - orient - scale - adjust (all untouched) -- each color channel takes the luma-preserving map `out = L + (c-L)-num/den`, clamped into `0..255`, where `L` is the pixel's own **Rec.601 luma** (weights `77+150+29` scaled to sum **exactly 256**, so a gray pixel maps to itself with no drift and a right shift by 8 divides exactly). `num==den` recovers the source byte-for-byte, `num==0` flattens every pixel to its own gray (a **true grayscale**), `num>den` pushes colors apart, and a gray pixel (`r==g==b`) is a **fixed point** at every saturation. The map runs in `i64` so no product wraps, the term bounded by `saturate_max = 1<<16`; a zero/over-ceiling term refuses `BadSaturation`, a zero-dimension image `EmptyImage`; alpha passes through untouched. Proven by the algebra it must obey (not one arithmetic): neutral byte-identical, zero-saturation `r==g==b==luma` per pixel, the already-gray pixel a fixed point at every saturation, a boost driving the dominant channel above and the weak channel below its luma, alpha pass-through, purity, the saturated image still re-encoding + round-tripping as an open image, and the three named refusals. Landed a small `luma_of` helper (its own two invariants incl. the weight-sum==256 assert) a future hue rotation will reuse. A true **hue rotation** (modular color-wheel turn, cleanest as a luma-chroma matrix) is a named horizon on the same module, harder to bound cleanly in integer math -- named, not half-built. Witness `tools/hunk_photos_saturate_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop - HUNK8 orient - HUNK9 scale - HUNK10 adjust all GREEN beneath. Design `active-designing/20260813-170307_hunk-photos-saturate-exploration.md`. Next agent-doable crux: the **hue rotation** horizon (reusing `luma_of`), a smoother bilinear/area resampler, or another Season-A / double-seat journey. HUNK's hardware rounds (buying panels, sourcing parts) stay custody gates #2/#3. **HUNK22 LANDED `20260813`:** `image/photos.rye` `scale_area` -- a proper **area-average downscale** beside the nearest-neighbor `scale`. HUNK9's nearest resize is honest for an upscale yet **aliases** when an image shrinks -- a thumbnail or a marketplace catalog cell kept a jittered subset of pixels. `scale_area` gives each output pixel the **exact integer mean, over all four channels including alpha**, of the source pixels its cell covers: the source bands `[ox-w/new_w, (ox+1)-w/new_w)` **tile** the image with no gap or overlap, so every source pixel is read exactly once and every band is non-empty whenever the target is no larger than the source. Each channel sum stays in `u32` (255-2^20 < 2^32) and divides by a non-zero block area -- no float, no wrap. Proven by the algebra it must obey: an identity size byte-identical, a **solid color stays that exact color at any smaller size** (the box-filter no-drift invariant), a 2x2 downscales to 1x1 as the exact average of its four pixels per channel and alpha, source pure, the downscaled image still re-encodes and round-trips as an **open image**, and an upscale (`NotDownscale`) and a zero dimension (`EmptyImage`) each refuse by name. A true **hue rotation** and a fractional-coverage **bilinear** resampler stay named horizons on the same module. Witness `tools/hunk_photos_scale_area_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop - HUNK8 orient - HUNK9 scale - HUNK10 adjust - HUNK21 saturate all GREEN beneath. Design `active-designing/20260813-171223_hunk-photos-scale-area-exploration.md`. **HUNK23 LANDED `20260813`:** `image/photos.rye` `hue_turn` -- the color wheel's three exactly-integer stops. HUNK21 named a hue rotation the next horizon yet flagged it *harder to bound cleanly in integer math*; a general-angle luma-preserving turn wants fixed-point trig. The 120deg thirds need none: a hue turn by a third is a **cyclic permutation of the three color channels** -- `thirds % 3 == 1` maps `(r,g,b) -> (b,r,g)` (+120deg), `== 2` maps `(g,b,r)` (+240deg), a multiple of three is the identity -- no trig, no rounding, no clamp. It stands to a full hue slider exactly as `rotate_quarter` stands to a general affine rotation: the provable, bounded core landed today, the fractional angle an honest horizon. Because the map only *permutes* the color channels, each pixel's **max and min channel are unchanged** -- HSV **value** (`= max`) and **chroma** (`= max - min`) are preserved exactly; only hue turns. Proven by the algebra it must obey: a zero turn byte-identical, **three turns compose to the identity** (the circle closes) and `turn(1)perpturn(2)` too, `turn(2)` equals two single thirds (the arg is honest), a pure red walks **red->green->blue->red** (the +120deg direction, unambiguous), every pixel keeps its max and min channel, an already-gray pixel a fixed point at every turn, alpha pass-through, purity, the turned image still re-encoding + round-tripping as an open image, and a degenerate image refusing `EmptyImage`. The modulo is total, so any `thirds` is valid (no bad-argument refusal, as `rotate_quarter` accepts any `turns`). Witness `tools/hunk_photos_hue_turn_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 crop - HUNK8 orient - HUNK9 scale - HUNK10 adjust - HUNK21 saturate all GREEN beneath. Design `active-designing/20260813-172645_hunk-photos-hue-turn-exploration.md`. Next agent-doable crux: the **general-angle** hue rotation (fixed-point cos/sin about the luma axis, reusing `luma_of`), a bilinear resampler, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK24 LANDED `20260813` -- the Photos app resamples smoothly:** `image/photos.rye` `scale_bilinear` lands the smooth resampler between HUNK9's nearest-neighbor `scale` (blocky up) and HUNK22's area-average `scale_area` (down only), completing the family. Each output pixel is the weighted blend of the four source pixels around the real coordinate it lands on, per channel and alpha; the half-pixel-center map `pos = S-((2n+1)-src-dst)/(2-dst)` runs in fixed point (S=256) so an identity resize lands exactly on the integer pixel -- **byte-identical** -- and the four corner weights sum to exactly S^2 so a **solid color stays that exact color at any size, up or down** (no drift). Each channel sum stays in `u32` (255-256^2 < 2^3^2) and divides with round-to-nearest -- no float, no wrap; edge coordinates clamp to the nearest real pixel. Proven by the algebra it must obey: identity byte-identical, a solid color exact at any size, a 2x1 `[A,B]` upscaled to 3 wide blending to `[A, (A+B)/2, B]` with the **exact average** at the center (red 0,100->50), **alpha blends alongside the colors** (200,40->120), purity, the resized image still re-encoding + round-tripping as an open image, and a zero target (`EmptyImage`) and an over-ceiling target (`OutOfBounds`) each refusing by name (no new error minted). Lanczos/cubic resampling and a general-angle hue rotation stay named horizons on the same module. Witness `tools/hunk_photos_bilinear_witness.rish` GREEN; TAME bans clean (three compound invariant asserts split by hand -- the scan fixture does not cover `image/`), width exit 0; HUNK3 crop - HUNK9 scale - HUNK22 area - HUNK23 hue-turn all GREEN beneath. Design `active-designing/20260813-173146_hunk-photos-bilinear-exploration.md`. Next agent-doable crux: the **general-angle** hue rotation (fixed-point cos/sin about the luma axis, reusing `luma_of`), a Lanczos/cubic resampler, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK25 LANDED `20260813` -- the Photos app filters; the whole filter family opens:** `image/photos.rye` `convolve` is the 3x3 kernel primitive every blur, sharpen, and future edge verb stands on -- each output color channel the kernel-weighted sum of the pixel's 3x3 neighborhood over a divisor plus a bias, clamped into 0..255, edges extended by clamping so no read wraps and no black halo forms; the whole 3x3 accumulation runs in `i64` under `kernel_weight_max = 1<<16` so no term wraps. `blur` (nine ones over nine) and `sharpen` (a five-neighborhood high-pass, weights summing to one) are each **one kernel** over it. Selftest: the identity kernel is the source **byte-identical**; a solid color is a **fixed point** of both filters; a 3x1 red row `[0,90,0]` box-blurs to the exact clamped mean `[30,30,30]`; alpha passes through untouched; source pure; the blurred image re-encodes and round-trips as an open image; an empty image, a zero divisor, and an over-ceiling weight each refuse by name (`EmptyImage` - `BadKernel`). Witness `tools/hunk_photos_convolve_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK3 - HUNK9 - HUNK22 - HUNK24 - HUNK23 all GREEN beneath. **HUNK26 LANDED `20260813` -- the Photos app auto-levels; the analytical filter opens:** `image/photos.rye` `stretch` is a pure two-pass contrast stretch over the decoded grid -- a first scan finds each color channel's darkest and brightest value, then a second pass linearly maps that range onto the full `0..255`, alpha untouched. A flat channel (range zero) passes through rather than dividing by nothing; a full-range channel is a **fixed point**; the map is **monotone** (order preserved). The whole map stays in `u32` (255-255 = 65025). Selftest: a dim red ramp `[40,60,80,100]` stretches to the exact rounded `[0,85,170,255]`; the flat green and blue channels pass through untouched; an already-full-range channel and a solid color are **byte-identical**; alpha rides through; source pure; the stretched image re-encodes and round-trips as an open image; an empty image refuses `EmptyImage`. This is the analytical crux the levels/curves tools stand on. Witness `tools/hunk_photos_stretch_witness.rish` GREEN; TAME bans clean; HUNK25 - HUNK24 - HUNK23 - HUNK3 all GREEN beneath. **HUNK27 LANDED `20260813` -- the Photos app detects edges; the crux `convolve` was named for:** `image/photos.rye` `edges` is a pure **Sobel** gradient magnitude over each pixel's Rec.601 luma (reusing `luma_of`) -- two 3x3 gradient kernels (horizontal `gx`, vertical `gy`) read the luma neighborhood, and the magnitude is the **L1 sum** `|gx| + |gy|` clamped `0..255`, so no square root and no float is needed and the arithmetic stays exact (largest term `4-255 = 1020`, far inside `i32`). Edges clamp so no read wraps; the edge map is a fresh **grayscale** view (`r==g==b`), alpha opaque. Blur and sharpen rode `convolve` directly; a true gradient magnitude combines *two* directional convolutions, so `edges` is its own verb -- the still-tractable move the earlier rung pointed at. Selftest: a sharp luma step `[0,0,255,255]` Sobels to the magnitudes `[0,255,255,0]` (the two pixels straddling the step lighting up, the flat ends black); the map grayscale and opaque even from a translucent source; a solid color a **fixed point at black** (no gradient); source pure; the edge image re-encodes and round-trips as an open image; an empty image refuses `EmptyImage`. Witness `tools/hunk_photos_edges_witness.rish` GREEN; TAME bans clean; HUNK26 - HUNK25 - HUNK3 all GREEN beneath. **HUNK28 LANDED `20260813` -- the filter family joins the edit-list:** `image/photo_edits.rye` grows by one variant so `saturate` (HUNK21) -- the paired tone twin of `adjust` already in the grammar -- now **records, replays, and travels like any verb**, closing the gap between the app's filters (HUNK21-27) and its non-destructive edit-list (HUNK11). `EditKind`/`Edit` gain `saturate: struct { num: u32, den: u32 }`; `apply`, `render_edits`, and `parse_edits` each gain one arm (`"saturate {d} {d}\n"`, 21 bytes, inside the 48-byte line bound). No new failure mode -- a zero-denominator saturate folds its own `BadSaturation` through `apply` by name. The reward is inheritance: a filter recorded as an edit now gets undo/redo (HUNK16), cold-restart persistence (HUNK18), and travel-as-text (HUNK11) for free. Selftest proves composition byte-for-byte, a 58-byte render/parse fixed point, the folded refusal, and the every-verb round-trip carrying saturate. Witness `tools/hunk_photo_edits_witness.rish` GREEN; all five downstream edit-list witnesses (edit_cursor - edit_store - edit_preview - cursor_preview - session_store - session_list) GREEN beneath; `tame_style_check` clean, width exit 0. Self-approved design round `active-designing/20260813-180504_hunk-photos-saturate-edit-exploration.md`. No breach, no cairn. **HUNK29 LANDED `20260813` -- the whole filter family joins the edit-list:** the vocabulary is complete. `hue_turn` (one `u32` `thirds` field, like `rotate`) plus the four nullary filters `blur`, `sharpen`, `stretch`, and `edges` (like `flip_h`) all join `image/photo_edits.rye`'s grammar, so the edit-list is the one home for **every** gesture the Photos app can do -- no edit is a wound. One cohesive keystone (complete the grammar): five variants across `EditKind`, `Edit`, `apply`, `render_edits`, `parse_edits`; two field shapes (a small param struct or `void`) cover the family, no new field kind, no new error. Care caught: the selftest's unknown-verb test moved off `sharpen 3` (now a real verb) to a genuinely-unknown tag `vignette`. Selftest proves `hue_turn -> blur` composes by hand byte-for-byte and a list carrying every verb kind round-trips to the same bytes. All six downstream edit-list witnesses GREEN (edit_cursor - edit_store - edit_preview - cursor_preview - session_store - session_list); `tame_style_check` clean, width exit 0. Self-approved design round `active-designing/20260813-181110_hunk-photos-filter-family-edit-exploration.md`. No breach, no cairn. Next agent-doable crux: a dedicated general filter round-trip witness proving every verb kind travels as a standalone claim, then named filter presets (a saved edit-list applied by name); or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK30 LANDED `20260813` -- the Photos app applies filters by name:** `image/filter_preset.rye` turns a recipe into a name -- a `PresetBook` is a bounded, named library of `photo_edits.EditList` recipes (names copied into its own storage), and `apply_preset` looks a recipe up by name and replays it over **any** image (reusing HUNK11's pure `apply`, the source never touched). `stock_book` ships nine well-known filters, each an honest recipe over proven HUNK21-27 verbs -- `vivid`-`pop` (saturate up), `mono` (saturate 0 -> true grayscale), `noir` (mono + contrast), `bright`, `auto` (auto-levels stretch), `soft` (blur), `sharp` (sharpen), `sketch` (edges). The property HUNK12's content-addressed `edit_store` cannot claim of a single stored recipe: **one preset applies to many different images**, each reproducing that image's own recipe by hand. Proven on metal: `vivid` applied by name equals `photos.saturate(img,3,2)` byte-for-byte, the SAME preset over a DIFFERENT image equals THAT image's own recipe, a two-verb `noir` resolves whole, distinct presets differ, a preset is deterministic and pure and re-encodes as an open image; an unknown name refuses `UnknownPreset` before a verb runs, a duplicate `PresetExists`, a bad name `BadPresetName`, a full book `TooManyPresets`. Witness `tools/hunk_filter_preset_witness.rish` GREEN; TAME bans clean (one compound assert split by hand -- the scan fixture does not cover `image/`), width exit 0; HUNK11 GREEN beneath. Design `active-designing/20260813-181925_hunk-filter-preset-book-exploration.md`. Next agent-doable crux: HUNK31 (the preset book travels as a `format filter-presets-v1` record, a nested grammar over `photo_edits`'s own lines), a preset book banked content-addressed, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK31 LANDED `20260813` -- the preset book travels as text:** `render_book`/`parse_book` (additive on `image/filter_preset.rye`) render a whole `PresetBook` as a `format filter-presets-v1` record and read it back -- each preset a `preset <name>` head, its edit-list body (the `photo_edits` header stripped), and an `end` terminator, reusing `photo_edits.render_edits`/`parse_edits` so the verb grammar is the single source of truth. Proven on metal: `render(parse(render(x)))` is a fixed point over the nine-preset stock book (280 bytes), and a rebuilt `noir` applies to the same bytes as the original recipe -- travel preserves the recipe, not just the names. A bad header, a verb line out of place, an unclosed block, a malformed verb, and a duplicate name each refuse `BadBook`, the `PresetBook` bounds re-enforced through `define` (the `sprite_catalog`/`session_list` convention). Witness `tools/hunk_filter_preset_witness.rish` extended, GREEN; TAME bans clean, width exit 0; HUNK30 GREEN beneath. Next agent-doable crux: a preset book banked content-addressed (bead a curated filter set, reproduce from cold), the Skate preview of a preset (reusing HUNK13), or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK32 LANDED `20260813` -- a curated filter-preset book is content-addressed:** `pond/apps/preset_store.rye` banks a whole `PresetBook` -- `store_book` renders it to its `format filter-presets-v1` record and beads it (HUNK1's Tablecloth store), `open_book` fetches it back proving every bead against its digest and the whole against its recorded address **before** the text is parsed or a preset defined, then `parse_book`s it. So a curated filter set reproduces from cold out of its name alone, recipes never re-authored -- the crux a plain edit-list store cannot claim (a whole named library at once). Mirrors `edit_store` (HUNK12); `image/filter_preset.rye` symlinked into `pond/apps/`. Proven on metal: the nine-preset stock book banked (280 bytes, 4 beads) reopens whole, its `noir` applying byte-for-byte; the same book under two names stored once (deduped 4/4); a tampered book refuses `DigestMismatch` and an unknown name `UnknownArtifact`, each before a preset is defined. Witness `tools/hunk_preset_store_witness.rish` GREEN; TAME bans clean, width exit 0; HUNK30/31 GREEN beneath. **The filter-preset journey stands whole: HUNK30 by-name - HUNK31 travels - HUNK32 content-addressed.** **HUNK33 LANDED `20260813`:** `pond/apps/filter_preset.rye` preview -- a keeper sees a preset's effect on a thumbnail before committing (reusing HUNK13's preview idiom). **HUNK34 LANDED `20260813`:** `pond/apps/preset_list.rye` -- an honest index of installed books (name + preset count), each read from a verified reopen, travelling both ways, a tampered book refusing the whole listing `DigestMismatch`. **HUNK35 LANDED `20260813`:** `pond/apps/preset_detail.rye` -- the index read one step deeper, carrying each book's preset *names* in defined order (stock reads `vivid pop mono noir bright auto soft sharp sketch`), with a new self-consistency invariant that travels: a declared count that does not match its listed names refuses `BadDetail` both directions. Witness `tools/hunk_preset_detail_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK36 LANDED `20260813`:** `pond/apps/preset_shelf.rye` -- the honest detailing painted on glass: `shelf_grid` renders a `DetailListing` onto a Skate grid, one row per book (`<name>  <preset...>`), the book name a color chip, refusing `ShelfTooSmall` rather than silently drop a book (every book or none) -- a pure view over an already-honest detailing, GREEN (72x4 shelf, 1607 lit pixels). Witness `tools/hunk_preset_shelf_witness.rish` GREEN; TAME bans clean, width exit 0. **HUNK37 LANDED `20260813`:** `pond/apps/preset_shelf.rye` pager (additive) -- `page_count` - `window_len` - `shelf_window(offset)` render the books `[offset, offset+rows)` as a page over a shared `draw_row` helper, so a library larger than the screen is fully reachable; **paging loses nothing** (every page walked shows each book once in held order), a window refuses `BadOffset` past the library. GREEN (five-book library, three pages 2-2-1); TAME bans clean, width exit 0. **HUNK38 LANDED `20260813`:** `brushstroke/sprite_shelf.rye` -- the marketplace's own front door: `catalog_shelf` paints a whole `SpriteIndex` onto one Skate grid, **one row per product** reading `<name>` beside a fixed-width color **swatch** wearing the slot its window down-maps to (its dominant anchor, a 1x1 `image_skate.down_map` valid for any window size), so a keeper reads the whole catalog at a glance -- each product's real color drawn from the ONE shared sheet; `catalog_window(offset)` pages a catalog larger than the screen (`page_count`/`window_len`), **paging loses nothing** (walked in order, each product once), refusing `BadOffset`-`ShelfTooSmall`-`EmptyShelf`-`ShelfTooLarge`-`RowTooNarrow` each by name. Two additive seams beneath in `image_skate.rye` (`set_anchor_palette` -- `down_map` refactored to share it, behavior identical; `block_cell` made public). GREEN (four color-band products -> 24x4 shelf, swatch slots 3-4-5-6, 2186 lit pixels, two pages 2-2-0); HUNK5 page + HUNK2 down-map re-run GREEN beneath; TAME bans clean, width exit 0. **HUNK39 LANDED `20260813`:** `brushstroke/sprite_shelf.rye` swatch enriched (additive) -- the swatch is now a real **thumbnail strip**, the product's window down-mapped to a horizontal run so its **left-to-right shape reads**, not only its dominant color: a two-tone window's swatch reads red at the left and green at the right. A window thinner than the swatch stretches to fill it; a one-column window stays a solid swatch (valid for any window). Narrow-window behavior preserved (the four 4x8-band products still wear their solid color slots 3-4-5-6). GREEN; TAME bans clean, width exit 0. **Checkpoint named (module-assembly seam):** the *served* marketplace (verified sheet HUNK6 + travelling manifest HUNK7 -> shelf HUNK38) hits a real Zig type-identity wall -- `catalog_shelf` lives in the `brushstroke/` assembly and `sprite_store`/`sprite_catalog` in `pond/apps/`, and the same `SpriteIndex`/`Pixmap` reached through different symlink paths are *distinct types*. Composing across the two assemblies wants a deliberate assembly decision (where the served-market module lives, which symlinks it roots through), not a quick lap -- held for Keaton / a design round. Next agent-doable crux: that assembly decision, a live scroll offset from a keeper's gesture, or another Season-A / double-seat journey. Hardware buys stay custody gates #2/#3. **HUNK52 LANDED `20260813` -- the strip painted beside the picture:** `brushstroke/edit_strip_view.rye` wraps HUNK51's finger view and adds `paint_strip` -- a chipsx1 Skate row where chip *i* wears the stable color of the verb bound at cell *i* (HUNK2's seven-anchor palette folded from the verb kind), and chip *i* sits on exactly the pixel span `[i-cell_px,(i+1)-cell_px)` that `finger_up` reads as cell *i*. So the palette a keeper SEES equals the palette they TAP, cell-for-cell -- HUNK51's no-drift carried from picture to palette. Added a pure `EditTouch.verb(i)` accessor to HUNK50. Witness `tools/hunk_edit_strip_view_witness.rish` GREEN (a real tap centered on `chip_center_x(i)` commits exactly verb *i*); HUNK50-51-2 GREEN beneath; TAME clean. Next agent-doable: stack chips over the picture into one canvas, or a content-addressed gesture session (HUNK12). **HUNK53 LANDED `20260813` -- the whole Photos screen in one call:** `brushstroke/edit_screen_view.rye` `paint_screen` stacks HUNK52's chip row over HUNK51's picture into one Skate grid (`max(cols,chips)` x `1+rows`), each sub-surface copied cell-for-cell so the composition adds NO drift -- top row equals `paint_strip`, picture region equals `paint`. Refuses `EmptyStrip` (a screen shows a palette). Witness `tools/hunk_edit_screen_view_witness.rish` GREEN (a tap centered on a chip repaints the picture region blue in the composed screen); HUNK52-51-2 GREEN beneath; TAME clean. Next agent-doable: a content-addressed gesture session (HUNK12), or the screen scrolled when the picture outgrows the view (HUNK40-47). **HUNK54 LANDED `20260813` -- the edit history as a filmstrip:** `brushstroke/edit_filmstrip.rye` `filmstrip` replays the cursor for each step k in `0..=history` -- step 0 the source, step k the first k edits -- down-maps each to a thumbnail (HUNK13 preview - HUNK2 down-map) and tiles them into one grid, a white marker block over the current step's band (`cursor.position`). No drift: tile k equals a direct `preview` cell-for-cell. Bounded by `max_film_cols`, refuses `FilmstripTooWide`. Witness `tools/hunk_edit_filmstrip_witness.rish` GREEN; TAME clean. **CHECKPOINT held:** the session-that-travels crux (persist the live editing session content-addressed, composing HUNK18) crosses the pond/apps<->brushstroke **assembly-identity seam** -- both `edit_cursor` symlinks resolve to `image/edit_cursor.rye` yet Zig's per-import-path type identity makes the two graphs' `EditCursor` distinct types. Wants a deliberate assembly decision (where the composed module lives, which symlinks it roots through) -- held for Keaton / a design round, not forced. In-assembly kg still open: a tap-to-jump on the filmstrip (HUNK50 cell index -> cursor position), or the screen scrolled (HUNK40-47). **HUNK55 LANDED `20260813` -- the filmstrip is tapped, the cursor jumps there:** `brushstroke/edit_filmstrip_jump.rye` `FilmstripJump` joins HUNK54's filmstrip geometry, HUNK50's pixel-to-cell idiom, and HUNK16's cursor -- a keeper who could only *read* the history strip now *taps* a thumbnail and lands on that step in one gesture, no longer one swipe per step. `tap_at(x)` classifies a pixel tap into band `x / band_px` (band_px = `tcols-cell_w`, the exact parallel of HUNK50's `x / cell_px`) and seeks the cursor there through a new additive `image/edit_cursor.rye` **`seek(target)`** -- a jump that **forgets nothing** (unlike `push`, which discards the redo tail), clamping to `0..=count`. `paint` is HUNK54's `filmstrip` unchanged, so the marker lands on exactly the sought step: **the band a keeper TAPS is the band the marker SHOWS** (HUNK52's no-drift discipline carried to the strip). Proven on metal over an 8x8 two-tone source, a cursor of three edits: a tap centered on band k seeks the cursor to step k for every step (0..3) and the repainted marker sits on exactly the tapped band cell-for-cell (no drift); a jump forgets nothing (back to step 1 then forward to step 3 recovers the full picture byte-for-byte, steps 2 and 3 still redoable); a tap past the last band and a negative-x tap each fire nothing (the cursor unmoved); the jumped filmstrip rasterizes to a lit canvas; a zero-dimension thumbnail refuses `BadThumb` at open. `seek` also proven in HUNK16's own selftest (jump to 1, back to 3 byte-for-byte, clamp past the count, seek to 0). Pure composition over proven seams -- no new render primitive, no new arithmetic beyond a clamped division, no new failure mode. Witness `tools/hunk_edit_filmstrip_jump_witness.rish` GREEN (HUNK54 filmstrip - HUNK16 cursor+seek GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260813-212112_hunk-filmstrip-jump-exploration.md`. Next agent-doable: a drag to scrub the filmstrip (continuous seek), the screen scrolled (HUNK40-47), or another Season-A / double-seat journey. **HUNK78 LANDED `20260814` -- the parts search is faceted:** `image/part_and.rye` narrows the marketplace on **several facets at once** -- HUNK75-77 built a search box that filters on one facet (`material:stainless-304` **or** `max:100`); a real marketplace is faceted (*the stainless bolts under a dollar*). `parse_and` reads a whitespace-separated line of `key:value` clauses into a bounded `AndQuery` (`max_clauses = 8`, each clause parsed by HUNK76's own `parse_query`), and `filter_and` builds the sub-catalog of parts that satisfy **every** clause over the same shared sheet -- an ordinary `PartCatalog` that paints, pages, and round-trips like any other. The crux: **a line of clauses means the intersection of their matches, and the clauses commute** -- the fast single-pass conjunction (`matches_all`, HUNK75's `matches` asked of every clause) equals the successive `filter_catalog` fold **name-for-name**, so no new matching rule is invented; an empty line is the honest **match-all**, reproducing the catalog byte-for-byte. Proven on metal over five parts: `material:stainless-304 max:60` keeps only hexnut (equal to the fold), price-then-material keeps the same one (commute), three clauses narrow to the bolt, a match-none compound yields a real empty catalog, and a malformed clause (`UnknownKey`, `NoColon`) plus an over-many line (`TooManyClauses`) each refuse by name before any walk. A price clause **compares, never moves** (custody gate #3 untouched). Witness `tools/hunk_part_and_witness.rish` GREEN (HUNK75 filter + HUNK76 parser GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260814-000136_hunk-part-and-compound-query-exploration.md`. Next agent-doable: bring the `AndQuery` to glass (a `search_shelf` over a compound line, mirroring HUNK77), an OR/any-of clause beside the AND, or another Season-A / double-seat journey. No network, no key, no funds. **HUNK79 LANDED `20260814` -- the faceted search box on glass:** `brushstroke/part_and_search.rye` joins HUNK78's conjunction to the shelf -- HUNK78 gave the marketplace a faceted query in the image layer, unseen; HUNK77 drew the single-clause search on glass but reaches only one facet. `and_search_shelf` reads a catalog, a **multi-clause** typed line, and a page offset, and draws exactly the narrowed page -- the faithful composition `parse_and` -> `filter_and` -> `part_window`, plus `and_search_result_count`/`and_search_page_count` so a caller shows *N results* and sizes the paging. The crux is HUNK77's, now over the conjunction: **what a keeper types is what a keeper sees**. Proven on metal over five parts across four bands: `material:stainless-304 max:100` draws the one stainless part under a dollar (hexnut $0.55), the narrowed page **equal to the hand-filtered window drawn directly**; the clauses **commute on glass**; an empty line is a **match-all** (bolt first); a match-none compound draws a **real empty page**; an offset past the result refuses `BadOffset`; a malformed clause (`UnknownKey`, `NoColon`) surfaces the parser's named refusal rather than a wrong shelf. A `brushstroke/part_and.rye` symlink resolves the image-layer conjunction (matching the `part_filter`/`part_query` pattern). A price clause **compares, never moves** (custody gate #3 untouched). Witness `tools/hunk_part_and_search_witness.rish` GREEN (HUNK78 conjunction + HUNK77 single-clause search GREEN beneath); TAME bans clean, width exit 0. Design `active-designing/20260814-000723_hunk-part-and-search-faceted-shelf-on-glass-exploration.md`. Next agent-doable: a query header above the shelf (show the typed clauses and N results), an OR/any-of clause beside the AND, or another Season-A / double-seat journey. No network, no key, no funds.

---

## Landed arcs -- condensed `20260817.171714` (the record lives in the logs)

A full bullet wall of dated done-work once accreted here. It landed, and the session logs plus git history now hold every detail; this card keeps only the pointer, so it stays single-stranded -- the live work-front, never a second copy of the log index. Walk-back cairn: `crux/CAIRNS.md` at nib `3b8b0d4858` reads the whole arc one `git show` away.

**What landed (each proven on metal, recorded in `session-logs/`):** Mandate, the complete named vector store (eleven capabilities); the Acme DX design season; the CION labeling ratchet; the **AHOY** front-door season (root README, LICENSE - SECURITY - CODE_OF_CONDUCT - CHANGELOG, the Lindy foundation) beside the **WADE** Bit-Design surface (`.brush` tokens, the `brix/infuse` cascade, styled Skate runs); the Singularity foundation and the fifteen-theme Twilight palette; **BUHR**'s whole MCP surface -- tools, resources, prompts; the 1,024-round itinerary; **TACT** Journeys 1 (Ship-Pilot), 2 (Publishing), and 4 (Commerce), each agent-doable arc complete to its custody gate; the recursion-prompts cellar; Season A opened (waymark **HUNK**) with the open image module underway; Constel dev-net quorum and the Testament offline certificate.

The live season table and open doors below stand.

## The Compass Season -- OPEN `20260809.021829` (Keaton's word)

**256 rounds - four equinoxes = four compass directions.** Coords **Compass Season - SOON - j1 - q1 - r1**. Nesting: **Round - Quest 4 - Journey 16 - Equinox 64 - Season 256**.

| Equinox | Waymark | Holds |
|---|---|---|
| The Language | **SOON** [x] | **Rishi language spine complete** -- runes `^- ?! ?& ?| ?:` - regex `matches`/`find` (`{n,m}` - `\b` - lookbehind) - `sort`/`unique`/`upper` - list `+` - `fold` - user functions `fn`. Seams folded native: `claim_preserve` - `census_control`. **Brix-infuse LANDED** `20260811` (`brix/infuse.rye`) - **PLEAC opened** `20260811` -- strings (join/split) - lists (chunk/window/flatten) - numbers (clamp/parse/to_str), each witnessed. **Cookbook wired into the Rishi interpreter** `20260811` -- `clamp` - `chunk` - `window` - `flatten` - `parse` - `str` builtins (suite 23/23; whole cookbook in the interpreter). **TAME core/shelf LANDED** `20260811` (`context/TAME_CORE.md` -- compressed core matched into the claude+cursor tame rules; shelf = `TAME_GUIDANCE.md`). Open: Aurora. **TAME Audit Quest pass 1** `20260811`: **full surface GREEN** (width-check - rune_assert_sweep - tame_style_check). **REDS #64 CLOSED** -- the stray `comlink_r1` .py ported to Rishi (`tools/comlink_r1_dual_bind_probe.rish`, Keaton's word: port over shred), `.py` removed, TAME bans clean. `one_clock` mono-erratum **FIXED** `20260811` (archive-tolerant integrity check + one dangling entry retired -> MONO_OK). Its zone leg reds only on this sandbox (host zone UTC, no config); the real Framework/pier host (America/New_York) passes it -- environmental, not a tree drift (`mold->shape` CLOSED -- residue left). **Aurora OPENED** `20260811` -- **all eight boot stages** (seed - relay - wire - sealed - posted - deciding - named - kumara) cross-build freestanding `riscv64` to RISC-V ELFs (`e_machine 243`) with **no emulator** (`tools/aurora_seed_freestanding_witness.rish` GREEN via `rye build`, rm-first so no stale artifact); the whole floor reaches metal in-sandbox. **REDS #68 CLOSED** -- the first cut of that witness used raw `zig build-exe` (which can't read `.rye`), no-op'd behind `|| true`, and read a stale ELF (false green); the next lap's fresh build exposed it, fixed same-round with a witness on metal. **Emulator choice consolidated** (`active-designing/20260811-202757_the-emulator-choice-qemu-and-the-modern-rust-vms.md`): **QEMU stays on merit** -- its pure-emulation `virt` + modern virtio-net + `-bios none` + SMP union is unmatched by the Rust VMs (Firecracker/Cloud Hypervisor are KVM kernel-booters; rvemu-class lack the virtio device model); rust-vmm virtio + a pure-Rust RISC-V core named as the future clean-room bridge. Waking the seed stays qemu-gated |
| Identity & Network | **JARL** | **Kumara** (5 tilaks) - **Comlink** turn-route + live handshake (loop closed) - **d12-d60 topology** - **settlement constellation** landed (`settlement/constellation.rye`: five transitions -- genesis - mint - transfer - rotate - **escape** -- a galaxy's d60 as owned records, every refusal held) - scarcity reconcile held - **generalized topologies + Pond** horizon |
| Surface & Intelligence | **BUHR** | Realidream DAG surface - Quin's four voices (Lattice - Scribble - Lantern - Ember) - MCP-in-Bron - Tablecloth |
| The World | **TACT** | Ship-Pilot pitch - publishing - Grainphone - commerce - CONTRIBUTING four-doors - Grain Energy PBC |

**Now at JARL -- identity, network, address space, and the settled ledger.** Coords **Compass Season - JARL - j4**. Four seats stand, each witnessed GREEN: **(1) Identity** -- `kumara/tilak.rye` seats five signed tilaks (point - bind - turn - capx3 - sponsor), every tamper refusing. **(2) Network** -- `comlink/handshake_turn.rye` reads a peer's rotation end to end: a signed introduction admits the peer, `turn_route.rye` takes the freshest verified turn (a reset outranks any key rotation), and `handshake_wire` recovers that turn from the 112 descriptor bytes it rode in -- the loop closed from wire to route key. **(3) Topology** -- `comlink/topology.rye` seats the **d12-d60 fractal address space**: twelve galaxies, five stars each, twelve planets each, a galaxy leading a d60 of sixty; a number decodes to a place, a place's sponsor climbs planet->star->galaxy, two places share a route by number. **(4) Settlement** -- `settlement/constellation.rye` is the ledger, chosen on **Sui's** ground: a galaxy and its d60 settle as owned records whose versions climb on every change. A galaxy opens the constellation from its own bind; a **sow** capability spawns a star beneath it and a planet beneath that, each under its rightful topology sponsor; a **hand** capability transfers ownership; a **tend** capability rotates the networking key while the keeper's key sits cold; a reset outranks a key bump. And **escape** completes the five: a child re-parents to another settled sponsor one tier up, by its own word (the sponsor tilak) and the new sponsor's adoption -- the old sponsor keeps no veto, and the child's number never moves, only whom it answers to. Every refusal holds -- orphan parent, wrong sponsor, forged cap, forged spawn signature, a re-mint, a stale rotation, a wrong-key turn, a galaxy that cannot escape, a forged request, a forged adoption. All five JARL witnesses run GREEN together. **Scarcity `20260810`:** the **d12-d60 fractal** -- Azimuth ranks retired; `kumara/tilak.rye` reads its tier from `comlink/topology.rye`. **Shared-surface shrink `20260810`:** an owned **Deed** (136 B, keys on the fast path) + a shared **Commitment** (56 B -- point-tier-sponsor-version-digest); `verify` proves a current deed, refuses stale/tampered/ghost. **Human-name custody `20260810`** (`settlement/names.rye`): a keeper claims + releases a globally-unique spoken name, resolvable both ways; the shared registry is the one surface that needs consensus. **JARL's settlement doors all stand** -- the next-season breach (Pond - skies - Kyri) is unblocked, opening on Keaton's word. **Horizon** (Keaton's word, `20260809`): generalizing the constellation to **loadable topologies** -- a *sky* (proposed) a community loads like a game: odd-quorum fractal (3-9-15-27) - 5 elements - modality roles (initiator/sustainer/adapter) - the three role-dimensions (contend/solve/calling). **Pond** becomes the full application module; **Pool retires into it**. **Decided:** breach opens **after JARL**; molt sweep **tight** (Pool paper + kin). Study: `external-research/20260809-232015...` + `...-233940_divisional-roles...`. Plan: `active-designing/20260809-234413_loadable-topologies-and-pond-silo-brief.md`. Molt-prep: `SHRED_PREP.md` pending note.

**Next-season breach -- OPEN `20260810`** (charter: `expanding-prompts/20260810-000032_the-next-season-breach-charter.md`). **Move 1 (Pond) begun:** Pond seated as Grain's **application module**, **Pool retired into it** by molt (living `foundations/20260810-011514_pond-the-application-module.md`; Pool study bannered as a fossil). Remaining: (2) **Bron -> Kyri** by **molt** (2,163 `.bron` kept whole, new logs `.kyri`, 378 refs forward -- never a rewrite); (3) **Kyri the one voice** -- *voice* becomes the whole concept, retiring "variant" (Quin + Reya-Riyo-Trey-Triz-Trya rest named in archive); (4) `kyri.sol`/`kyri.sui` reference -- **Keaton's hand alone**; and the loadable **skies** that finish move 1. **Decided:** Kyri names **both** voice and notation (logs are the voice's journal); **Kyri** on the favorites list. Baton: [`../expanding-prompts/20260809-021406_the-compass-season-baton-four-equinoxes.md`](../expanding-prompts/20260809-021406_the-compass-season-baton-four-equinoxes.md).

---

## Waymarks

Seated ladders: **HAWM - TUBE - ZETA - JABS - LULU - STOA - SETU - SUNN - POLE** (elder) - **SOON - JARL - BUHR - TACT** (Compass Season). Draw before you number: `.claude/rules/waymark-ladders.md` - `tools/waymark_derive.rish`. Claims: `waymarks/`.

---

## Pier & hands

- **Pier path** -- `~/grain` (persists across jail resets) - agent `home-xy-grain` - Cloud on `xykj61/grain`.
- **Lane** -- every **send** pushes both `origin` (autoproject96) and `xykj61`. ls-remote guard first; `origin` may 403 from the cloud (home pier closes the gap). Map: [`../PUBKEYS.md`](../PUBKEYS.md) - [`../context/REMOTE_ROSTER.md`](../context/REMOTE_ROSTER.md).
- **Jail authors; host installs** -- agents write inside the project / enclosure; USB `adb` installs and key ops stay Keaton's hand.
- **Live state** (`20260808`) -- guides 0-2 CLOSED - `gh` as `xykj61` - Claude Code on pier - **agent-jail GREEN** (`./tools/agent-jail.sh` for claude + cursor-agent) - editors + Aug cursor-cli witnessed - tmux `pier` standing.
- **Cursor launch** -- `rishi/bin/rishi run tools/launch-cursor.rish --cursor ./Cursor-*.AppImage --gpu`.
- **Outer terminal / phone** -- USB/`adb` and phone look stay on the operator desk; prefer git nib + `prin scope` for season state.

---

## Two grains

The private field is `~/grain`; the public template **grain-os/grain** is *projected* by `tools/sow.rish` along the boundary in `template-manifest.bron`, proven clean by `tools/sow_witness.rish` (no name or key crosses into the seed). **Seed COMPLETE + privacy-fixed `20260810`** -- every new module classified. **Leak found + closed:** the scrub/scanner only knew the maintainer name + keys, so the family surname (**Dunsford**), prior name, and personal handles were leaking into the public seed; expanded the scrub + IDENT net to every family/handle/contact form, made the withhold case-insensitive (privacy over completeness -- leaking files are withheld whole). `IDENT_CLEAN` confirmed; the vision batons + rosters + raw contacts held out. Raw PII **not persisted** in-tree -- held for the **Vault** (custody-first, Keaton's hand). Publish push is Keaton's hand.

## Shred-prep

[`SHRED_PREP.md`](SHRED_PREP.md) -- Class H fossils - Class O rooms (propose-never-seat) - **Python->Rishi molt seated** (`20260809`, prep only) - shred stays **RED** until circled. **debride** is the stronger word (removes dead history, deep on Keaton's word).

---

## Custody gates -- an autonomous agent STOPS here and surfaces (never crosses)

For any self-paced or outer-jail loop: recur through all agent-doable work, yet **stop and surface -- never cross -- these custody/irreversible/provisioning acts.** They are Keaton's hand by design (custody-first):

1. ~~**The AHOY3 final seed force-push**~~ -- **DONE `20260812`** on Keaton's word (*push the seed*): the clean projection published to `grain-os/grain` @ `6d184f7` (fast-forward, author *Grain OS*, unsigned by design). Caught + fixed a scrub name-doubling first (**REDS #70**) -- LICENSE and family names had garbled to "the maintainer the maintainer"; a pre-push content scan caught what IDENT_CLEAN could not. **The AHOY front-door season is CLOSED.** **Seed style seated `20260812`** (Keaton): the seed is kept a **single force-push commit** -- the weave (**Mantra**) is Grain's real version control, git one snapshot; each refresh replaces the one commit (now `26cae5b`, author *Grain OS*). Future seed refreshes stay Keaton's word.
- **BUHR OPENED (explored) `20260812`** (`active-designing/20260812-053742_buhr-exploration...`): Compass Equinox 3 -- Surface & Intelligence. **Crux = Quin's four voices** (Lattice-Scribble-Lantern-Ember all witnessed GREEN; Quin host named-only, composes through Pond). Realidream = far-horizon composition on real substrate (no module yet, rightly). MCP-in-Bron = greenfield. **First door LANDED `20260812`** -- **BUHR'1r1**: `pond/apps/inference_query.rye` composes Lattice+Lantern as a bounded Q'vane host -- a Lantern request is validated + model'allow'listed, an arithmetic op routes to **real** Lattice math (`[[1,2],[3,4]]x[[5,6],[7,8]]=[[19,22],[43,50]]`), and a Lantern'bounded response returns (token/stop metering); an unknown op is refused (never faked). Both cores byte'identical (composed over public APIs only); witness `tools/buhr_inference_query_witness.rish` GREEN, TAME clean. **BUHR'1r2 LANDED `20260812`** -- `pond/apps/reading_query.rye` carries the same Q'vane host to the **reading voice**: a Lantern request routes through **Scribble** for a real reading of a pinned markdown document (outline -> `Grain Voices Detail` - depth'two fold -> `Voices` - fence kinds -> `rye 1 rish 1 plain 1`), returned bounded by the same allow'list + token/stop metering; unknown op refused, zero budget refused by the Lantern contract, length budget truncates with a length stop. Additive -- rung one stays GREEN, TAME bans clean; witness `tools/buhr_reading_query_witness.rish` GREEN. **BUHR'1r3 LANDED `20260812`** -- `pond/apps/corpus_query.rye` carries the same host to the **craft voice**: a Lantern request routes through **Ember** for a real reading of a pinned corpus catalog (count_rye -> `3` - sum_lines -> `160` - paths_rye -> `rye/forge.rye rye/tally.rye rye/mandi.rye`), returned bounded by the same allow'list + token/stop metering; unknown op refused, zero budget refused, length budget truncates. Additive -- rungs one & two stay GREEN, TAME bans clean; witness `tools/buhr_corpus_query_witness.rish` GREEN. **All three built voices (Lattice - Scribble - Ember) now compose through the Q'vane host, each over public APIs only.** **BUHR'1r4 LANDED `20260812` -- the Q'vane host, realized:** `pond/apps/quin_host.rye` gathers the three voices under one bounded entry (`dispatch`), routing a single Lantern request to whichever voice recognizes the prompt (matmul->Lattice - outline->Scribble - sum_lines->Ember), returning the Lantern Response tagged with the serving `Voice`. Faults surface at once (disallowed model / zero budget -- no fall'through); a prompt no voice knows is refused, not answered; the serving voice's metering (length stop) carries through unchanged. Additive -- imports the three apps, calls only their public `answer`; rungs 1-3 stay their own GREEN binaries. Witness `tools/buhr_quin_host_witness.rish` GREEN, TAME bans clean. **BUHR Journey 1 (Quin's voices) -- the crux -- is COMPLETE.** **BUHR Journey 2, Rung 1 LANDED `20260812` -- the first Realidream app:** `pond/apps/mantra_browser.rye` reads a Mantra `BoltCatalog` and renders its append'only log as a readable revision graph -- under each peer/bolt, each path a lane, each lane's revisions chaining `r1 -> r2 -> r3` with a short content digest per node -- composed over Mantra's public catalog API only (no new abstraction). Deterministic (referential transparency carried into the view), empty log bounded to `(empty log)`, undersized buffer refused with `RenderTooLong`. Witness `tools/buhr_mantra_browser_witness.rish` GREEN, TAME bans clean. **BUHR Journey 2, Rung 2 LANDED `20260812` -- the graph, painted:** `pond/apps/mantra_browser_view.rye` carries rung one's revision graph onto WADE1's styled Skate surface -- `paint_graph` calls `mantra_browser.render` for the exact bytes rung one witnesses and dresses each line onto a Skate grid row (Flax paper - Bark ink), so **editor and view share one graph** (the painted view cannot drift from the text). Glyphs rasterize to lit ink pixels; light and dark token sheets lower to distinct grounds (styling is real); a short viewport bounds the paint with no overflow. Witness `tools/buhr_mantra_browser_view_witness.rish` GREEN (rung one still GREEN -- additive), TAME clean. **BUHR Journey 3 exploration SEATED `20260812`** (`active-designing/20260812-111800_buhr-journey3-mcp-in-bron-exploration.md`) -- MCP'in'Bron: the Model Context Protocol spoken in Bron, not JSON, grounded in real surfaces (Bron notation - `mandate/serve.rye` bounded request/response - `brix/infuse` cascade). Ordered Lindy'first crux'first: the durable artifact is the descriptor **shape** (`format mcp-tool-v1` manifest - `mcp-call-v1`/`mcp-result-v1` envelope), the crux is bounded - value'model'honest - round'trippable with unknown fields **refused**; Bron<->JSON tiles named horizon. **BUHR Journey 3, Rung 1 LANDED `20260812` -- the descriptor shape fixed:** `pond/apps/mcp_bron.rye` -- a `format mcp-call-v1` envelope (id - tool - repeated `arg name value`) parses into a bounded request, routes to a real tool (`add` over integers, `join` over strings), and answers with a paired `format mcp-result-v1` that `parse_result` reads back deterministically (round-trip asserted byte-for-byte); a minimal `format mcp-tool-v1` manifest reader reads tool name + arg count. Refusals real: unknown tool -> `error unknown_tool`, unknown field -> `UnknownField` (never dropped), wrong header -> `BadFormat`, missing tool -> `MissingField`, missing arg -> `error bad_args`. Reuses the flat-Bron idiom (`brix/infuse`) + bounded request/response (`mandate/serve`); no new transport. Witness `tools/buhr_mcp_bron_witness.rish` GREEN (8 cases), TAME clean. **BUHR'J3r2 LANDED `20260812` -- the Bron<->JSON tiles:** `pond/apps/mcp_json.rye` carries the owned shape to a JSON'speaking MCP host and back. A bounded Bron `Call` renders to a real JSON'RPC `tools/call` request (`call_to_json`) and reads back into the same `Call` (`call_from_json`); a Bron `Result` renders to the JSON'RPC response shape (a `content` text item on success, a `code`/`message` object on error) and reads back into the same `Result`. The crux property carries across the tile -- `parse(render(x))` recovers `x` both directions, asserted byte'for'byte, and a quoted+backslashed value survives the crossing. A purpose'built bounded reader (objects - strings - unsigned ids), never a general JSON parser: unknown field refused (not dropped), non'`tools/call` method -> `BadMethod`, truncated envelope -> `Unterminated`. Additive -- imports rung one's shapes, calls only their public API; rung one stays its own GREEN binary. Witness `tools/buhr_mcp_json_witness.rish` GREEN (7 cases + rung'one re'run), TAME clean, width'check clean. **BUHR'J3r3 LANDED `20260812` -- the manifest tile:** `pond/apps/mcp_manifest.rye` lets a JSON host **discover** a Grain tool (the discovery half to r2's call half). A Bron `format mcp-tool-v1` manifest (tool - summary - typed args - result) renders to the JSON tool descriptor a `tools/list` carries (`name` - `description` - `inputSchema` typed properties + `required` - `outputSchema`) and reads back into an equal manifest, asserted field by field. Value model crosses honestly -- `int`/`str`/`bool` <-> `integer`/`string`/`boolean`, an unmappable type refused at the border, unknown descriptor field refused. Additive (exposed r2's JSON helpers `pub`, reuses them; r1/r2 re'run GREEN). Witness `tools/buhr_mcp_manifest_witness.rish` GREEN (4 cases + r1/r2 re'runs), TAME + width clean. **BUHR'J3r4 LANDED `20260812` -- the tools/list envelope:** `pond/apps/mcp_list.rye` carries r3's single'tool discovery to its real shape -- the whole `tools/list` response, a bounded array of descriptors, so a JSON MCP host learns **every** Grain tool a server offers in one exchange. A bounded Bron catalog renders to a JSON'RPC `tools/list` response (`{...,"result":{"tools":[<descriptor>,...]}}`) and reads back into an equal catalog, tool for tool and argument for argument, byte'for'byte. It reuses r3's own writer and reader -- each tool rendered by `manifest_to_json`, each read by a newly'exposed `read_manifest` looped over one shared cursor (r3 refactored additively, re'runs GREEN), so no brace'scanner is duplicated. Refusals real: a list past the catalog bound -> `TooManyTools` (never silently truncated), an unknown field beside the tools array -> `UnknownField`. The list envelope owns its own bounded append (the descriptor tile's 512'byte buffer is too small for the whole catalog); the reader inherits the tile's 512'byte scratch bound. Witness `tools/buhr_mcp_list_witness.rish` GREEN (4 cases + r3 re'run), TAME + width clean. **BUHR'J3r5 LANDED `20260812` -- the JSON host bridge:** `pond/apps/mcp_host.rye` joins r1-r4 into one entry -- a JSON MCP host's whole conversation served over the owned Bron shapes end to end. `serve` reads a JSON'RPC request's method and routes it: `tools/list` -> the server's whole catalog (r4); `tools/call` -> r2 `call_from_json` -> r1 `dispatch` (real `add`/`join`) -> r1 `parse_result` -> r2 `result_to_json`. Pure composition -- no new transport, no new parser but a small bounded `skip_value` so `read_method` passes over any field (including `params`) regardless of order, so a params'first request still routes. Reuses r4's bounded append (made `pub`) to place the call response into the larger buffer (no third append). Refusals real end to end: unknown tool -> JSON'RPC `unknown_tool` (never faked), unrecognized method -> `method_not_found`. Witness `tools/buhr_mcp_host_witness.rish` GREEN (5 cases + r4 re'run), TAME + width clean. **BUHR Journey 3 whole -- shape - tiles - manifest - list - bridge. BUHR'J2r3 LANDED `20260812` -- the living edge, lit:** `pond/apps/mantra_browser_heads.rye` deepens the Realidream surface with **semantic per'cell color** -- each lane's **head** revision (the newest node in a chain) is lit in the sheet's warm `edge` token via WADE1's `dress_run`, while older nodes keep the reading ink, so a reader sees at a glance where each lane's history currently stands. The head is found in the very bytes rung one renders (the rightmost `r<n>[digest]` on a lane line), so the paint can never point at a node the text does not show; the paint is strictly additive (glyphs byte'unchanged, rung two stays its own GREEN binary). Proven on metal -- both token colors reach the canvas at once (1468 ink pixels - 826 head pixels). Added `tokens.token_color` (a public sheet'token->Skate'Color reader, reused by the witness). Witness `tools/buhr_mantra_browser_heads_witness.rish` GREEN (6 welcomes + rung'two re'run), TAME + width clean. **Then refined `20260812`:** the living edge now wears its own **semantic design'system token** -- added `head` (Ochre `0xFFC89B5A`, `head_ochre`, proven warm/low'blue and brighter than both ink and edge) to `base_sheet`, and repointed the app from `edge` to `head`, so a `.brush` theme recolors the head on its own key without touching borders or ink (asserted head != ink != edge). **BUHR'J2r4 LANDED `20260812` -- the living edge, themed:** `pond/apps/mantra_browser_theme.rye` closes the authoring loop the `head` token was named for -- a real `.brush` file (`pond/apps/hot-edge.brush`) whose `style:` block overrides `head` (terracotta `0xFFB4502D`, warm/low'blue) is read through the **actual authoring loop** (`brush_parse.parse_brush` validates - `collect_brush_style` reassembles flat Bron), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by rung three's unchanged `paint_heads`, so **every lane's living edge burns the authored color**. The recolor is proven twice: the themed head resolves to the terracotta, and the default ochre leaves **zero** pixels on the themed canvas (recolor, never addition). Warm/low'blue holds *through* the theme (a theme cannot smuggle a cold head past the palette law); glyphs are byte'unchanged (editor and view stay one graph); a style'less `.brush` refuses with `ThemeMissingHead`. Additive -- rungs 1-3 stay their own GREEN binaries. Witness `tools/buhr_mantra_browser_theme_witness.rish` GREEN (5 welcomes + rung'three re'run), TAME + width clean. **BUHR'J2r5 LANDED `20260812` -- two `.brush` layers compose:** `pond/apps/mantra_browser_theme_stack.rye` proves the deeper design'system property -- an independent **mode** layer (`pond/apps/dark-mode.brush`, ground only) and **accent** layer (`pond/apps/warm-accent.brush`, living edge only) compose through the WADE1 cascade (`tokens.resolve_stack`), the top layer winning per token, the base sheet's untouched tokens inheriting beneath both. Proven by reading back **three sources at once**: the ground (Bark/Flax) from the mode layer, the head (honey `0xFFD9A441`) from the accent layer, the edge rule (`edge_warm`) from the base sheet neither layer named. Each layer read through the real authoring loop; a style'less layer refuses `LayerEmpty`; the composed honey head reaches the canvas. Additive -- rungs 1-4 stay GREEN. Witness `tools/buhr_mantra_browser_theme_stack_witness.rish` GREEN (4 welcomes + rung'four re'run), TAME + width clean. **BUHR'J2r6 LANDED `20260812` -- the reading ground, themed:** `pond/apps/mantra_browser_view_theme.rye` carries the authoring loop to the other side of the sheet -- rung four themed the living edge (the `head` accent); this rung themes the reading **ground** itself. A real `.brush` *mode* file (`pond/apps/dark-mode.brush`) whose `style:` block overrides `paper`+`ink` is read through the actual authoring loop (`parse_brush` - `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by rung two's unchanged `view.paint_graph`, so the whole graph lands on the authored ground -- the reading surface flips light->dark by editing one file, never Skate or Rye. The recolor is proven honestly around **roles**, not a false zero: the dark theme swaps Flax and Bark between the ground and ink roles, so the same graph painted twice shows the ground inverting from the light majority to the dark majority (Bark rises from a minority ink to the majority ground, Flax falls the other way). Warm/low'blue holds through the theme; glyphs byte'unchanged; a style'less `.brush` refuses `ThemeMissingGround`. Additive -- rungs 1-5 stay GREEN. Witness `tools/buhr_mantra_browser_view_theme_witness.rish` GREEN (5 welcomes + rung'two re'run), TAME + width clean. **Now both sides of the sheet are `.brush`'authorable -- the accent (r4) and the ground (r6), each proven end to end. BUHR'J2r7 LANDED `20260812` -- one `.brush` file re'skins a whole component on both axes at once:** `pond/apps/mantra_browser_full_theme.rye` proves the property a design system actually promises -- a single `.brush` file (`pond/apps/hot-edge.brush`, all three tokens) re'skins the heads view on **both** axes together. `resolve_full_theme` requires `paper`+`ink`+`head` all present (else `ThemeIncomplete`), infuses over `base_sheet`, and paints via rung three's unchanged `paint_heads`, so one edited file flips the reading surface dark **and** burns every lane's living edge terracotta in one reading. Each axis by its own honest measure -- the ground by role inversion (Flax rules the base canvas, Bark the themed one, since the theme swaps the two colors between ground and ink roles), the accent by a clean zero (terracotta distinct from both grounds -> zero pixels on the untouched base canvas). Warm/low'blue holds across every axis; glyphs byte'unchanged; a ground'only theme refuses `ThemeIncomplete`. Additive -- rungs 1-6 stay GREEN. Witness `tools/buhr_mantra_browser_full_theme_witness.rish` GREEN (4 welcomes + rung'four & rung'six re'runs), TAME + width clean. **The `.brush` theming arc (BUHR'J2 r1-r7) is complete end to end.** **BUHR Journey 4 OPENED `20260812` -- the Realidream editing surface:** the exploration's three planned journeys (Quin voices - Mantra browser - MCP'in'Bron) all ran green, so the plan'exhausted blind spot opened a new journey -- the **writing** half of Realidream's one'graph promise (J2 built only the reading half). Exploration `active-designing/20260812-123509_buhr-journey4-realidream-editing-surface-exploration.md` names the crux (**an edit is an append the graph already knows how to show**) and one door. **BUHR'J4r1 LANDED `20260812`:** `pond/apps/mantra_editor.rye` `commit` reads a lane's head via a new `pub mantra_browser.head_revision` (added where head'finding already lives -- substrate `recall_lap1.rye` untouched), appends the next revision by Mantra's public `append_leaf` (append'only order - immutability - field bounds enforced), returns it, refuses an unknown lane (`LaneUnknown`). Witness renders before/after: the edited lane grew by exactly one `r<n>` node (chain arrow intact), every other lane byte'unchanged, a second edit chains `r1 -> r2 -> r3` (append'only). An edit is an append -> the editor inherits accrete'never'break as its data model. Additive -- J2 reader re'runs GREEN. Witness `tools/buhr_mantra_editor_witness.rish` GREEN (3 welcomes + reader re'run), TAME + width clean. **BUHR'J4r2 LANDED `20260812` -- the edit moves the living edge forward, visibly:** `pond/apps/mantra_editor_view.rye` composes `editor.commit` with `heads.paint_heads`, proving at the grid'cell level (`heads.head_slot`) that after a commit the edited lane's head'token cells sit further right -- the living edge advanced `r1 -> r2`, the lit head covering the new node's span (`heads.head_span`) -- every other lane byte'unchanged; a second edit advances `r2 -> r3` on a wider grid (the paint's own `ViewTooNarrow` bound named honestly). Grid'cell level -> the edge is a column number, its advance an inequality. Additive -- editor (J4r1) + heads (J2r3) re'run GREEN. Witness `tools/buhr_mantra_editor_view_witness.rish` GREEN (3 welcomes + two re'runs), TAME + width clean. **BUHR'J4r3 LANDED `20260812` -- the editor forks a new lane as well as continuing one:** added `pub open_lane` to `mantra_editor.rye` beside `commit` (the editor's "new file" to commit's "edit file") -- refuses a path that already carries a leaf (`LaneExists`), else appends revision 1. Both are appends to one append'only log, so the whole editor surface stays inside Mantra's order/immutability laws -- no rewrite path to guard. Witnessed in `mantra_editor_fork.rye`: opening `c.txt` grows the graph by one lane line (`r1`, no arrow), lane_count +1, existing lanes byte'unchanged; the forked lane is a lane like any other (commit chains `r1 -> r2`); a duplicate open refuses `LaneExists`. Additive -- editor (J4r1) re'runs GREEN. Witness `tools/buhr_mantra_editor_fork_witness.rish` GREEN (3 welcomes + editor re'run), TAME + width clean. **The editor's core append surface -- open - commit - show'lit -- is complete.** **BUHR'J4r4 LANDED `20260812` -- undo is reading an earlier revision, then appending it:** added `pub revert` to `mantra_editor.rye` beside `commit` -- in an append'only log there is no node to delete, so undo recalls the bytes bound at an earlier revision (`catalog.recall`) and commits them as the lane's next revision. `pond/apps/mantra_editor_undo.rye` proves it by the bytes: on a lane at r2 ("beta'two"), reverting to r1 ("beta") returns the r1 bytes as a new head **r3**, while r2 still recalls "beta'two" untouched (undo added a node, never removed one) and the graph grows `r1 -> r2 -> r3`, every other lane byte'unchanged; a revert past the head refuses `RevisionUnknown`, a revert on a lane with no head refuses `LaneUnknown`. Undo inherits accrete'never'break by construction -- no rewrite path to guard, only a read and an append. Additive -- rung one (editor) re'runs GREEN. Witness `tools/buhr_mantra_editor_undo_witness.rish` GREEN (3 welcomes + editor re'run), TAME + width clean. **The editor's whole core surface -- open - commit - undo - show'lit -- is complete.** **BUHR'J4r5 LANDED `20260812` -- undo, painted through to the styled surface:** `pond/apps/mantra_editor_undo_view.rye` carries r4's undo onto WADE1's Skate surface and shows the visible signature of append'only undo -- after reverting r2 back to r1, the head'token **living edge advances** (to r3, since undo is a commit like any other), **yet the newest node wears the reverted'to node's content fingerprint**: r3's rendered 8'hex digest equals r1's (Mantra derives each digest from content, and undo brought r1's content back), while r2's digest differs. Proven at the cell level: the edge column moved right, the lit head covers the restored r3 node, every other lane paints byte'unchanged. So a reader sees history grow and the content return at once. Additive -- rungs two (view) & four (undo) re'run GREEN. Witness `tools/buhr_mantra_editor_undo_view_witness.rish` GREEN (3 welcomes + two re'runs), TAME + width clean. **BUHR Journey 5 OPENED `20260812` -- the web'read half beside the program'write half:** with J4's editor complete, the plan'exhausted blind spot opened the last half of Realidream's one'graph promise. Exploration `active-designing/20260812-125750_buhr-journey5-web-read-half-exploration.md` (self'approved design round) names the crux -- **a read source and a written program are two lanes on one append'only graph, and one fold shows both** (Realidream's *two sources feed one node*) -- and holds a real network fetch as a **named seam behind a bounded reader** (its own later rung, surfacing for Keaton's word). **BUHR'J5r1 LANDED `20260812`:** `pond/apps/realidream_reader.rye` `pin_read` appends a read source's content under a `read/` path (marking origin honestly; refuses a non'read path `NotAReadPath`, a duplicate `LaneExists`), and the very same `mantra_browser.render` paints the read lane beside the program lanes, grouped by bolt. Proven: one render shows both origins (`local/web` read + `local/notes` program); a commit to a program lane grows it by one node while the read lane stands **byte'for'byte unchanged** -- a write on one origin never disturbs the other. **Reads no network** (content supplied in'process, as the demo catalog does today). Additive -- J4 editor + J2 browser re'run GREEN. Witness `tools/buhr_realidream_reader_witness.rish` GREEN (3 welcomes + editor re'run), TAME + width clean. **BUHR'J5r2 LANDED `20260812` -- a re'read is an append, exactly as an edit is:** `pond/apps/realidream_reread.rye` `refresh_read` continues an existing `read/` lane -- the newer content never overwrites the old, it chains after it (over the editor's own public `commit`), so the read half inherits accrete'never'break by the same construction the editor did. Proven: pin a read lane at r1 and refresh it, and the read lane chains `r1 -> r2` while every program lane stands byte'for'byte unchanged; the earlier read revision still recalls its original bytes after the refresh (r1 -> "first'read" - r2 -> "second'read" -- the old read stands, never rewritten); a non'read path refuses `NotAReadPath`, an unpinned read lane refuses `LaneUnknown` (a re'read continues a chain; `pin_read` opens). Reads no network (refreshed content supplied in'process). Additive -- rung one (J5r1) + J4 editor re'run GREEN. Witness `tools/buhr_realidream_reread_witness.rish` GREEN (3 welcomes + rung'one re'run), TAME + width clean. **BUHR'J5r3 LANDED `20260812` -- a program node depends on a read node (the graph's true fused edge):** `pond/apps/realidream_derive.rye` `derive_from_read` reads a `read/` lane's head content and appends a derived program node whose content is a **pure function** of the read (`read-len=<n>`, standing in for any real consumer), so the read is **upstream** of the program, not beside it. Because the derived node is a pure function of the read it consumed, a re'read flows downstream honestly: refresh the read (rung 2's append), derive again, and the new program node reflects the new read (`read-len=10`) while the earlier program node **still recalls** the value it derived from the earlier read (`read-len=4`) -- referential transparency across the fused edge, nothing mutating behind the reader; the change flows **only** on a fresh derive. First derive opens the program lane, each later one continues it. Refuses a non'read source (`NotAReadPath`), a read destination (`NotAProgramPath`), an unpinned read (`LaneUnknown`). Reads no network. Additive -- rungs one & two re'run GREEN. Witness `tools/buhr_realidream_derive_witness.rish` GREEN (3 welcomes + rung'two re'run), TAME + width clean. **BUHR'J5r4 LANDED `20260812` -- the fusion whole: two read sources feed one program node:** `pond/apps/realidream_fuse.rye` `derive_from_two_reads` reads the head content of **two distinct** `read/` lanes and appends a fused summary (`a=<la> b=<lb>`) whose content is a pure function of **both** current heads -- the foundation's "two sources feed one node," drawn whole. Refreshing **either** source flows downstream independently on the next derive: re'read the first -> `a=8 b=2`, re'read the second -> `a=8 b=3`, while the first fused node still recalls `a=4 b=2` (referential transparency across a two'input edge, nothing mutating downstream). Refuses a non'read source (`NotAReadPath`), a read destination (`NotAProgramPath`), the same lane named twice (`SameSource`), an unpinned read (`LaneUnknown`). Reads no network. Additive -- rung three re'runs GREEN. Witness `tools/buhr_realidream_fuse_witness.rish` GREEN (3 welcomes + rung'three re'run), TAME + width clean. **The Realidream fused'edge arc (J5 r1-r4) stands: two lanes on one graph - a re'read is an append - one read feeds one program node - two reads fuse into one.** **BUHR'J5r5 LANDED `20260812` -- the read, program, and fused lanes painted together, origin made visible:** `pond/apps/realidream_view.rye` `paint_origins` renders the whole fused scene (two `read/` lanes - a `fused` program node - the demo program lanes) onto **one** WADE1 styled Skate surface -- ten styled rows -- and marks each **read origin** in the sheet's own new **`read` (Madder)** token, while every lane's living edge keeps Journey 2's **`head` (Ochre)** token, reused unchanged. So one canvas carries **three** readings at once: the body ink, the read origins, each lane's head -- proven on metal (1758 ink - 391 read'origin - 1604 living'edge pixels share the canvas; read != head asserted). The origin mark reads off the very bytes the browser renders (`read_label_span` finds the `read/` path label -- exactly 2 read lanes tinted, 21 cells; program lanes clean), so the paint can never point at an origin the readable text does not name. Added `tokens.read_madder` + a `read` line in `base_sheet` (warm/low'blue proven, distinct from ink - edge - head), additively -- the tokens selftest, J5r4 fuse, and J2r3 heads all re'run GREEN. Reads no network. Witness `tools/buhr_realidream_view_witness.rish` GREEN (6 welcomes + rung'four & heads re'runs), TAME + width clean. **BUHR'J5r6 LANDED `20260812` -- a `.brush` theme recolors the read origin:** `pond/apps/realidream_view_theme.rye` closes the authoring loop the `read` token was named for -- a real `.brush` accent file (`pond/apps/read-source.brush`) overriding only the `read` key is read through the actual authoring loop (`parse_brush` - `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by J5r5's own `paint_origins` unchanged, so every read origin burns the authored amber while ink - edge - living'edge head stand untouched (proven equal channel by channel). Recolor proven twice: the amber reaches the canvas (391 px), the default Madder leaves **zero** px (recolor, never addition); warm/low'blue holds through the theme; glyphs byte'unchanged; a style'less `.brush` refuses `ThemeMissingRead`. Made `realidream_view.fused_scene` pub (additive); J5r5 re'runs GREEN. Witness `tools/buhr_realidream_view_theme_witness.rish` GREEN (5 welcomes + rung'five re'run), TAME + width clean. **The read origin and the living edge are now both `.brush`'themeable, each proven end to end.** **BUHR'J5r7 LANDED `20260812` -- two `.brush` layers compose the Realidream surface:** `pond/apps/realidream_view_theme_stack.rye` proves the compose property (mirroring J2r5 for the read origin) -- a **mode** layer (`dark-mode.brush`, ground) and a **read'source accent** layer (`read-source.brush`, read token) compose through the WADE1 cascade (`tokens.resolve_stack`), the top layer winning per token, the base's untouched tokens (`head` - edge) inheriting beneath both. Three authors read back from one sheet -- ground from the mode, read origin (amber) from the accent, living'edge head (Ochre) from the base neither layer named; on the canvas the stacked amber read (391 px) and the inherited Ochre head (1604 px) both land through J5r5's `paint_origins`; a style'less `.brush` refuses `LayerEmpty`. Additive -- J5r6 re'runs GREEN. Witness `tools/buhr_realidream_view_theme_stack_witness.rish` GREEN (4 welcomes + rung'six re'run), TAME + width clean. **The read origin's theming story is now symmetric with the living edge's -- single'file theme and multi'layer compose, each proven end to end.** **BUHR'J5r8 LANDED `20260812` -- the fused downstream edge, painted:** `pond/apps/realidream_view_derived.rye` `paint_flow` lights the other end of the edge rung five opened -- a **derived** program node (content a pure function of the reads upstream of it) names itself under a `prog/` path, symmetric to a read origin's `read/`, and wears the sheet's own new **`flow` (Weld)** token, so one canvas carries the whole edge at once: read origin upstream in Madder, derived node downstream in Weld, every lane's living edge in Ochre. The mark reads off the very bytes `mantra_browser.render` writes (`derived_label_span` finds the `prog/` label), so it can never point at a computed node the text does not name; the scene derives `prog/summary` from `read/src` through rung three's own `derive_from_read` -- a real derived node in the log (9 rows - 1 derived lane - 13 flow cells - 162 read'origin + 267 derived'node pixels on one canvas, read != flow asserted). Grew `tokens.base_sheet` with the `flow` (Weld) token -- a plant'leaf'dye gold, warm/low'blue proven, distinct in red from ink - edge - head - read -- additively; `read_token` kept local so no file beneath changed. Reads no network. Witness `tools/buhr_realidream_view_derived_witness.rish` GREEN (tokens - J5r5 - J5r3 re'runs; J5r6/J5r7 themes over base_sheet stay GREEN), TAME + width clean. **Both ends of Realidream's fused edge are now lit on one canvas.** **BUHR'J5r9 LANDED `20260812` -- a `.brush` theme recolors the derived node:** `pond/apps/realidream_view_derived_theme.rye` closes the authoring loop the `flow` token was named for, symmetric to r6's read'origin theme. A real `.brush` accent file (`pond/apps/flow-source.brush`) whose `style:` block overrides only the `flow` key is read through the actual authoring loop (`parse_brush` - `collect_brush_style`), infused over `base_sheet` by the WADE1 cascade (`tokens.resolve`), and painted by r8's own `paint_flow` unchanged -- so every derived node burns the authored honey (`0xFFD2882A`) while ink - read origin - edge - living'edge head stand untouched. Proven twice: the honey reaches the canvas (267 px), the default Weld leaves **zero** px (recolor, never addition); warm/low'blue holds through the theme; glyphs byte'unchanged; a style'less `.brush` refuses `ThemeMissingFlow`. Additive -- J5r8 & J5r6 re'run GREEN. Witness `tools/buhr_realidream_view_derived_theme_witness.rish` GREEN (5 welcomes + two re'runs), TAME + width clean. **Both ends of Realidream's fused edge are now themeable -- upstream read origin and downstream derived node, each on its own key.** **BUHR'J5r10 LANDED `20260812` -- two `.brush` accent layers compose both fused'edge ends at once:** `pond/apps/realidream_view_derived_theme_stack.rye` joins r6's read accent and r9's flow accent -- a **read'source** layer (`read` only) and a **flow'source** layer (`flow` only) compose through the WADE1 cascade (`tokens.resolve_stack`), each winning the one token it names, ground - head - edge inheriting from base -- so a person themes both ends at once by stacking two small `.brush` files (amber read origin over honey derived node). Proven: three authors on one sheet (read amber - flow honey - inherited Ochre head), both accents distinct, both reach the canvas (162 read + 267 flow + head px through r8's `paint_flow`); a style'less layer refuses `LayerEmpty`. A compound'assert red was caught by `tame_style_check` before commit and split. Additive -- J5r9 & J5r7 re'run GREEN. Witness `tools/buhr_realidream_view_derived_theme_stack_witness.rish` GREEN (4 welcomes + two re'runs), TAME + width clean. **The Realidream fused'edge theming arc is symmetric and complete -- each end themeable on its own key (r6 read - r9 flow), both composable together (r7 read+mode - r10 read+flow).** **Held seam (Keaton's word):** the **real network fetch** behind a bounded reader -- the one genuine outward act, its own later rung. **BUHR Journey 6 OPENED `20260812` -- the intelligence reads the surface:** the equinox is named *Surface & Intelligence*, and five journeys built each half alone (J1 the Q-vane voices over pinned corpora; J2-J5 the Realidream graph); no rung had let the intelligence read the living surface -- the itinerary's blind spot. A self-approved design round (`active-designing/20260812-140058_buhr-journey6-...-exploration.md`) double-seated J6 and named its crux: a voice whose corpus is the *living* graph. **BUHR'J6r1 LANDED `20260812`:** `pond/apps/graph_query.rye` -- a Lantern-shaped request routes by prompt to a real reading of the actual `BoltCatalog` the browser renders (`lane_count`->3 - `bolt_count`->2 - `head` of a.txt->r2 - `summary`->`bolts=2 lanes=3`), each bounded/metered by the same Lantern contract every other voice obeys (validate - allow-list - token/stop metering). Not a pinned answer -- the head the voice reports (r2 on a.txt) is exactly the head the browser paints and the editor continues, so intelligence and surface share one graph as editor and view do. A two-token summary proves the length-budget truncation (length stop); a disallowed model, an unknown op (no fake answer), and a zero budget each refuse. Composed over public APIs only (`lantern_core` + `mantra_browser`), no new abstraction. Witness `tools/buhr_graph_query_witness.rish` GREEN (8 cases + browser re-run), TAME + width clean. **BUHR'J6r2 LANDED `20260812` -- the query tracks a live edit (the crux, taken next):** refactored `graph_query` additively (`answer_over` over an *explicit* catalog; `answer` delegates over the demo graph -- rung one re'runs GREEN), then `pond/apps/graph_query_edit.rye` composes the query with J4's editor over one catalog: ask head (r2 on a.txt) -> `mantra_editor.commit` -> ask again -> head advances to r3 (moved by the edit, not guessed); a fork (`open_lane`) grows the reported `lane_count` 3->4; and because an edit is an append the query is **read'only** -- an edited catalog reads r3 while a pristine one reads r2, each its own true head. Additive (imports `graph_query` + `mantra_editor`, public API only); witness `tools/buhr_graph_query_edit_witness.rish` GREEN (3 welcomes + rung'one re'run), TAME + width clean. **BUHR'J6r3 LANDED `20260812` -- the surface voice, gathered under the Q'vane host:** `pond/apps/quin_host_graph.rye` *wraps* J1's `quin_host.dispatch` unchanged (Lattice/Scribble/Ember tried first) and falls through to `graph_query.answer` only on UnknownOp, tagging a fourth **graph** voice -- so arithmetic, reading, corpus, and the living graph all answer under one bounded dispatch (the equinox thesis at the host level). A disallowed model surfaces at once (no fall'through); an unknown prompt refuses. Additive (a `lift` maps the three'voice `Voice` into the wider set; `quin_host` stays its own green binary); witness `tools/buhr_quin_host_graph_witness.rish` GREEN (6 welcomes + 2 unwelcomes + host re'run), TAME + width clean. **BUHR'J6r4 LANDED `20260812` -- the reading voice folds the graph (Journey 6 complete):** added an *additive* markdown door on the browser -- `mantra_browser.render_markdown` emits the same append'only grouping as `render` but as a `# mantra log` title - `## peer/bolt` bolt headings - `- path` lane lines (its own selftest; `render`/`head`/count paths untouched); then `pond/apps/graph_reading_query.rye` folds that markdown with **Scribble** -- `outline`-> title + every bolt (`mantra log local/notes local/site`), `bolts`-> depth'two bolt headings only; a fork under a new bolt grows the fold (`local/wiki` appears), the reading tracking the living surface as the counting voice did. The one source is `render_markdown`, Scribble parses exactly those bytes, so the reading can never drift from the graph the reader sees. Length budget truncates (length stop); disallowed model - unknown op (no fake answer) - zero budget each refuse. Additive (browser + counting voice stay their own green binaries); witness `tools/buhr_graph_reading_query_witness.rish` GREEN (4 welcomes + 3 unwelcomes + browser re'run), TAME + width clean. **BUHR Journey 6 stands complete end to end -- read - track'a'live'edit - gathered'under'host - reading'fold; Surface and Intelligence meet over one graph.** **BUHR Journey 7 OPENED `20260812` -- Tablecloth, the named artifact store:** with Journey 6 complete, the itinerary's blind spot is the equinox's **last unbuilt module** -- **Tablecloth**, the Lexicon's *Brix'facing application database and artifact store* (held apart from the already'witnessed namespace query). A self'approved design round (`active-designing/20260812-142132_buhr-journey7-tablecloth-artifact-store-exploration.md`) opened it Lindy'first crux'first; the crux -- **a human name bound to a content address**, so fetch'by'name returns exactly the bytes whose digest the name pins, a tamper anywhere refusing. **BUHR'J7r1 LANDED `20260812`:** `pond/apps/tablecloth.rye` is the **naming layer over `mantra/beading.rye`'s content'addressed `BeadStore`** -- it invents no storage. `store_artifact` beads content into the shared store (dedup earned: identical content stored once, named twice -- `notes.txt` + `notes-copy.txt` share every bead, 0 new deposits) and binds a name to the whole digest + bead'index recipe; `fetch_artifact` reassembles by name, every bead proving itself by digest and the whole against its recorded address (a tampered bead refuses `DigestMismatch`, an unknown name `UnknownArtifact`, a taken name `NameTaken`). The Brix'facing `format tablecloth-v1` manifest (`artifact <name> <digest> <len>`) round'trips name-address-length field for field; a wrong header refuses `BadManifest`. Migrated the one `parseInt` site to `tally/parse_int.rye` (ratchet 9->8). Additive -- beading stays its own GREEN binary; witness `tools/buhr_tablecloth_witness.rish` GREEN, TAME bans + width clean. **BUHR'J7r2 LANDED `20260812` -- the manifest as the true index:** `pond/apps/tablecloth_index.rye` `verify_against_store` proves a parsed `format tablecloth-v1` manifest is an **honest index of a live store** -- every published name is held, reassembles (each bead by digest, the whole against its recorded address via `fetch_artifact`), and matches its published **length** and **address** (`tablecloth.address_hex`, added additively with `address_hex_width`). A name the store lacks refuses `UnknownArtifact`, a wrong length `LengthMismatch`, a wrong address `AddressMismatch` -- so a person handed a manifest can check the store satisfies it, artifact for artifact. Additive -- rung one (the naming layer) stays its own GREEN binary under the reader. Witness `tools/buhr_tablecloth_index_witness.rish` GREEN, TAME bans + width clean. **BUHR'J7r3 LANDED `20260812` -- a Brix descriptor declares artifacts the store satisfies:** `pond/apps/tablecloth_brix.rye` `evaluate` makes the Brix'facing half literal -- a `format tablecloth-decl-v1` descriptor (one `require <name>` line per needed artifact) is **evaluated against a live store** into a `format tablecloth-v1` manifest of exactly those declared artifacts, in declared order. The join refuses honestly: a required name the store cannot satisfy refuses the whole evaluation (`Unsatisfied`), never emitting a manifest naming bytes the store lacks; a bad header or empty descriptor refuses `BadDescriptor`. The evaluated manifest then **verifies against the store by rung two**, closing the chain -- a system declares what it needs, the store proves it serves it, the manifest is the audited receipt. Additive -- rungs 1-2 stay their own GREEN binaries; `tablecloth.rye` grew only a pub `artifact_len`. Witness `tools/buhr_tablecloth_brix_witness.rish` GREEN, TAME bans + width clean. **BUHR'J7r4 LANDED `20260812` -- the owner'signed catalog:** `pond/apps/tablecloth_keyed.rye` `store_owned` makes a name bind bytes to a settled **Kumara identity**, not merely pin them -- the owner presents the point's Deed (verified against the constellation, exactly as `mandate/keyed.rye` and the name registry gate) and signs the exact publication (point - name - length - a SHA'256 of the content); only then does rung one's `store_artifact` bead + name it, and the owning point is recorded. A publication resolves to *whose* it is -- `owner_of` returns the point, `place_of` resolves it to its galaxy/star/planet (symmetric with `mandate/keyed.rye`'s `place_of`). Every refusal holds: a non'owner (ghost deed, **no byte stored**), a forged signature, a content'mismatched signature (the digest is bound in -- a tampered artifact cannot ride a real signature), and rung one's taken'name refusal. **Demo keeper seeds only -- no real key, no network, no custody** (gate #4, Keaton's real Kumara instance, is untouched). Additive -- `tablecloth.rye` grew only a pub `index_of`; rung one stays its own GREEN binary. Symlinked kumara/tilak/topology/constellation into `pond/apps` (mirrors `mandate/`). Witness `tools/buhr_tablecloth_keyed_witness.rish` GREEN, TAME + width clean. **BUHR Journey 7's agent'doable arc now stands -- store (r1) - honest index (r2) - Brix declaration satisfied (r3) - owner'signed (r4).** The one remaining Tablecloth horizon rung -- **Comlink'served** -- reaches the network/serve gate (Keaton's hand); the held seam stays the real network fetch. **BUHR Journey 8 OPENED `20260812` -- the intelligence reads the artifact store:** BUHR's seven journeys built every named module, yet one pair never met -- the intelligence had read the Realidream graph (J6) and pinned corpora (J1), but no voice had read a **stored artifact** by name from **Tablecloth**. A self'approved design round (`active-designing/20260812-144916_...`) named the crux -- **a voice reads only verified bytes**. **BUHR'J8r1 LANDED `20260812`:** `pond/apps/artifact_query.rye` composes the Lantern contract over Tablecloth's verified fetch -- `bytes`/`words`/`line` over a named artifact, the content ops reading **only through `fetch_artifact`**, so a **tampered bead refuses** (`DigestMismatch`) and the voice answers nothing rather than reading corruption; content'addressed storage becomes the intelligence's integrity guarantee for free. Unknown name - unknown op - disallowed model - zero budget each refuse; a length budget truncates with a length stop. Composed over public APIs only (`lantern_core` + `tablecloth`); J7r1 store stays its own GREEN binary. Witness `tools/buhr_artifact_query_witness.rish` GREEN (9 cases), TAME + width clean. **BUHR'J8r2 LANDED `20260812`** -- `pond/apps/artifact_query_publish.rye` proves the *living* in a living store: it composes J8r1's `artifact_query.answer_over` with J7's `tablecloth.store_artifact` over one catalog+store -- `words brief.txt` refuses (`UnknownArtifact`, no invented answer) before the publish, and reads its true word count after; a publish is an append so held names stand unchanged (`bytes notes.txt` reads the same length across a later publish) and the query itself writes nothing (a published store reads the new name, an independent pristine store still refuses it -- the reading is read'only). Additive -- J8r1 stays its own GREEN binary; witness `tools/buhr_artifact_query_publish_witness.rish` GREEN (3 welcomes + J8r1 re'run), TAME + width clean. **BUHR'J8r3 LANDED `20260812`** -- `pond/apps/quin_host_artifact.rye` gathers the artifact voice under the Q'vane host: it wraps J6r3's `quin_host_graph.dispatch` unchanged (Lattice-Scribble-Ember-graph tried first) and falls through to `artifact_query.answer` only on `UnknownOp`, tagging a fifth **artifact** voice -- so arithmetic, reading, corpus, the living graph, and a stored artifact all answer under one bounded dispatch, the artifact voice's content'address integrity travelling with it. A disallowed model surfaces at once (no fall'through); an unknown artifact name (`UnknownArtifact`) and an unrecognized prompt (`UnknownOp`) each refuse. Additive -- the four'voice host stays its own GREEN binary; witness `tools/buhr_quin_host_artifact_witness.rish` GREEN (5 welcomes + 3 unwelcomes + J6r3 re'run), TAME + width clean. **BUHR Journey 9 OPENED `20260812` -- the intelligence served as MCP tools:** with J8 done, the blind spot was a pair the equinox names that never met -- J3 spoke MCP in Bron, J8 gathered five voices, yet the MCP host still routed `tools/call` only to J3's toy `add`/`join`. A self'approved design round (`active-designing/20260812-150600_...`) opened J9. **BUHR'J9r1 LANDED `20260812`:** `pond/apps/mcp_voice.rye` joins them -- an MCP `ask` call crosses into a Lantern request, dispatches through `quin_host_artifact.dispatch` (routing by prompt to whichever voice knows it), and the response crosses back into a `format mcp-result-v1` envelope read by J3's own `parse_result` (`matmul`->`19 22 43 50` - `words notes.txt`->`16` - `lane_count`->`3`). Every voice refusal crosses back as a **named** MCP error (`unknown_tool`-`model_not_allowed`-`unknown_op`-`unknown_artifact`-`bad_args`); the bridge never invents a result, and the artifact voice's content'address integrity travels through the MCP surface. Reads no network (real fetch + Comlink'served stay the held gates). Additive -- `mcp_bron` (J3r1) + `quin_host_artifact` (J8r3) stay their own GREEN binaries; witness `tools/buhr_mcp_voice_witness.rish` GREEN (8 welcomes + both re'runs), TAME + width clean. **BUHR'J9r2 LANDED `20260812`:** `pond/apps/mcp_voice_tools.rye` gives each voice its own name at the protocol surface -- five typed tools (`lattice`-`scribble`-`ember`-`graph`-`artifact`) discovered through J3r4's `tools/list` envelope (`list_to_json`/`list_from_json` round'trip), and on invoke the tool name is a **typed assertion**: `serve_voice` maps the tool to a required voice, dispatches, and cross'checks `served.voice == want` -- a `words` prompt under the `lattice` tool routes to the artifact voice, so it refuses `wrong_voice` rather than claim Lattice answered; an unlisted tool refuses `unknown_tool`, a disallowed model `model_not_allowed`. Reused J9r1's `render_ok`/`render_error`/`error_name` (made `pub`, additive -- J9r1 witness re'runs GREEN) + J3r4 `mcp_list`. Witness `tools/buhr_mcp_voice_tools_witness.rish` GREEN (6 welcomes + both re'runs), TAME + width clean. **BUHR's whole agent'doable arc stands: the intelligence is reachable, discoverable, and addressable voice by voice over the owned MCP shape.** The remaining BUHR doors -- **owner'signed corpus** + **Comlink'served** -- reach the custody/serve gates that are Keaton's hand.
2. **Provisioning or paying** for any cloud/VPS/Pond/subscription (Vultr SEA IaC, WADE2/3) -- agents author IaC; Keaton provisions and pays.
3. **Moving funds, holding keys, or opening any custody/wallet/payment rail** -- Dimeroll records facts only; disbursement waits on licensed counsel.
4. **Generating Keaton's own Kumara instance** from his real seed/keeper -- his hand alone.
5. **Deep debride / history rewrite + force-push** of the living tree -- named target, Keaton's explicit word.
6. **Seating a new module in a collaborator's domain** (e.g. DJINN's surface lead) beyond authored implementation-floor code -- the invitation and lead are the collaborator's to accept.

Everything else -- design, code, witnesses, docs, weaves, seed *projection* (not push), reds -- is agent-doable and does not wait.

## Open doors (awaiting Keaton's word)

| Door | Kind |
|------|------|
| **Next JARL step** -- **escape** (a child re-parenting, from the sponsor tilak) - shrink the shared surface to a membership commitment - or the scarcity design call | live |
| **Breach OPEN `20260810`** -- Pond = application module (Pool retired) - **skies lap 1** - **topology inclusive** (galaxy is star is planet, 720/universe, sponsor by mod, **outfit** roles; 6 witnesses GREEN) - **Kyri** the notation (was Bron) - **Skate** = the social network | breach - live |
| **Waymark debride `20260818`** -- the living ladders (DREY - FORA - WADE - LOWE) and the module **Dimeroll** carry only their standing names; dead elder marks purged from the living tree, dated logs, and all git history (deep debride, custody gate #1, Keaton's hand). Canonical naming truth sealed and self-verifying in `crux/waymark-registry.bron`. | debride - executed |
| **HANDOFF BATON `20260810`** -- full vision on disk for a context reset: [`expanding-prompts/20260810-025942_the-handoff-baton-vision-checkpoint.md`](../expanding-prompts/20260810-025942_the-handoff-baton-vision-checkpoint.md). Proposed modules: **Mandate** (turbopuffer, next build) - **Scribe** (Kyri home + settings) - **Starseeding** - **Unsplash** - **`.myc`/Mycelium** (Sui reimpl) - **Mala** revival. Roster: `context/inspirations.kyri` (tribute, consent-gated). **Vault LANDED** `20260810` -- `vault/shard.rye` (keeper of secrets, molt-supersedes Jael): Shamir GF(256) sharding, n-of-t, signed shard tilaks in location-classes (home-kin-cold-relic-brain), tamper-refused, custody-first (fake seed only), main key never the older word. **Mandate LANDED** `20260810` -- `store.rye` (zero-copy vector store, cosine k-NN, tag filter) + `keyed.rye` (**Kumara-keyed**, owner-signed). **Scribe LANDED** `20260810` -- `scribe/reader.rye` (zero-copy Kyri reader; dispatch by format; the seated 1-notation-many-formats). Seated: **baton** (favorite word; a `.kyri` `format baton-v1`) - **"thank you everyone"** (end-marker) - **expanded-prompts/** (cold) beside expanding-prompts (hot) - council-names roster. | checkpoint |
| **3x39 BATON `20260810`** -- 2nd vision on disk: `expanding-prompts/20260810-044453_the-3x39-baton-passports-dividers-and-starseeding.md`. Concepts: 3x39 namespace reset - divider grammar (prefix/suffix/middle, `.sol`/`.sui`-provable) - **Kumara passports** - L1 synonym-outfits (MURR/MUR...). Proposed: **Loom** (ScribexTally monitor) - `bat/` archetypes. Multi-agent build = Workflow on **ultracode** opt-in. **begin starseeding** (own round). | checkpoint |
| **Keaton's own Kumara instance** -- generate from his real seed + keeper, by his hand alone | JARL - when ready |
| **mold->shape debride** -- **CLOSED `20260811`** (Keaton: *leave the residue*): primary rename landed `20260720`; the residue is testimony - retirement-explanation - deliberately-kept study twins. Optional horizon only: an atomic Glow-identifier refactor (`mold_slice` ~23 files) on the language bench. Survey: `active-designing/20260811-184002_mold-shape-debride-survey-and-plan.md` | resolved |
| **TAME core/shelf** -- lossless-compressed living law over a preserved shelf | proposed |
| **Brix-infuse** -- **LANDED `20260811`** (SOON j2): `brix/infuse.rye` -- per-key override over flat Bron (override wins per key, base-only kept in place, override-only appended), zero-copy, bounded; witness `tools/brix_infuse_witness.rish` GREEN. Horizon: nested-descriptor deep merge when Brix grows nesting | resolved |
| **Identity Remake / Kumara** -- the first identity template + Keaton's instance | JARL |
| **Geode / Genode-on-NixOS** -- proven-seat parallel | GATED |
| **Grainphone - Realidream - Pond seven - data-dignity - succession - Mand ring-3 - O3 gen-home** | awaiting Keaton |

---

## Card habits

- **kg** -- keep going, next mechanical lap. **check-in** -- pause for Keaton's word / design. **send** -- commit - push both remotes - merge. **remember** -- reprint this card. **align** -- walk the compass, reconcile plan with green witnesses. **molt** -- prep a fossil for shed. **debride** -- remove dead history (Keaton's word). **shred** stays RED until circled. remember != send != kg != align.
- **Vocabulary** -- the tree seats **shape**, not Hoon's *mold*. Prefer **git nib**. One clock: `TZ=America/New_York`.

---

*Carry lightly. Prefer git nib. `prin scope`. May the season stay clean and the fascia hold.*
