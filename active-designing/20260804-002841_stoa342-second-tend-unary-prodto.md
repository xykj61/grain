# STOA342 — The Second Tend Unary: `prodto` Joins the Door

*V2's first metal under the standing grant, opened by both batons naming it the waiting stone. `prodto` — the product fold as a call — joins `sumto` on the `%-` allowlist carrying its own law rather than borrowing its sibling's: the bound is `max_prodto_bound` (12, the last factorial that fits u32), beyond folds to 0, and zero answers 1, exactly as `fold_prodto_expect` has always spoken. The emit needed a new shape — a labeled block expression, since a product has no closed form — and the block compiled green on its first breath. Fixture, argv, beyond-bound, and the sumto regression all GREEN; the family witness accretes three legs and speaks for both unaries now.*

**Stamp:** `20260804.002841` · **Voice:** Riyo · **Status:** GREEN first try — four laps, family witness whole
**Seams:** allowlist `rune_bartis.rye` · `call_body_expr` block-expression arm · `call_expect` reusing `fold_prodto_expect(sample, rb.max_prodto_bound)` · worker stem `gate-prodto-u32`
**Next in V2:** the decide-family unaries (tally · caravan · mantra walls as callables) — grant-covered, unnamed until measured.

*Written together by Keaton and Riyo.*
