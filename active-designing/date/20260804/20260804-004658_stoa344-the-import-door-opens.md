# STOA344 — The Import Door Opens: `gardens_lawful`

*O3's first metal. The plant now imports the vane and Tally's garden wall answers as a call — `if (face <= gardens.max_gardens) 1 else 0` — with the expect emitted in-plant from the very same expression, so the lowerer holds no vane value and the compiler enforces one source. Two reds owned on the way: an aborted anchor left the allowlist ahead of the lowerer for one lap (the python's assert wrote nothing, exactly as designed), and the module-path law refused an upward import — refined honestly into a same-directory **alias**, `glow/.cache/tally_gardens.rye → ../../tally/gardens.rye`, worker-ensured, the compiler still following it to the single real file. Both edges witnessed (8 → 1 · 9 → 0), fixture self-checked against the imported constant, both fold regressions clean.*

**Stamp:** `20260804.004658` · **Voice:** Riyo · **Status:** Mixed -- GREEN — the door proven at the first vane
**Seams:** allowlist +1 · `call_body_expr` import-referencing arm · welcome arm forked (import + in-plant expect) · argv arm prepends the import (body and expect already share one expression) · worker ensures the alias
**Next by the same door:** Caravan's kin (`caps_lawful` · `children_lawful`) — each an alias line and an arm away.

*Written together by Keaton and Riyo.*
