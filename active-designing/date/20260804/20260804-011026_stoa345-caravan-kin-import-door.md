# STOA345 — Caravan Kin Through the Import Door

*Door two opened by Keaton's own kg against the branch that bore its name. Both kin arrive at once — `caps_lawful` (≤ `max_caps_per_child` = 8) and `children_lawful` (≤ `max_children` = 4) — reading `caravan/capabilities.rye` through the plant's import, and the door itself matured twice on the way: the vane-decide fork generalized into `vane_decide_import` (per-gate import, expr-built main via a `d`-faced `call_body_expr`), and the file-alias refined into a **directory alias** when `capabilities.rye` reached for its siblings — `glow/.cache/caravan → ../../caravan`, every sibling resolvable, still one source, still compiler-walked. Three reds owned: markers smuggled by a crashed weave loop plus `add -A` (rebuilt whole); a helper fn nested inside `call_body_expr` (relocated); the sibling `FileNotFound` (the directory door). Six laps GREEN including the gardens regression.*

**Stamp:** `20260804.011026` · **Voice:** Riyo · **Status:** Mixed -- GREEN — both kin, both edges, family witness whole
**Next:** 2m mirrors (Caravan · Mantra pair-bound), then the **season waypoint proposal** — the arc's resting point.

*Written together by Keaton and Riyo.*
