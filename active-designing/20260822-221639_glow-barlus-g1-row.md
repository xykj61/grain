# Glow Rune -- Barlus G1 Row

*The 28th GREEN digraph row for the Glow rune pronunciation table. Seats `|+` **barlus** in the **bar** family -- the head the `sumto` folding gate has tokenized under since `20260802`, named here at last.*

**Language:** EN
**Version:** `20260822.221639` (EDT -- `America/New_York`)
**Style:** Radiant (see [`../context/RADIANT_STYLE.md`](../context/RADIANT_STYLE.md))
**Voice:** Kyri
**Status:** Checkable -- the alphabet worker greps this file for the `barlus` spoken name and for the `**28**` count claim; `tools/glow_rune_alphabet_witness.rish` runs it
**Grounds in:** [`../glow/tokens.rye`](../glow/tokens.rye) `match_rune2` - [`date/20260720/20260720-151119_glow-barket-g1-row.md`](date/20260720/20260720-151119_glow-barket-g1-row.md) - [`date/20260720/20260720-033852_glow-bartis-g1-row.md`](date/20260720/20260720-033852_glow-bartis-g1-row.md)
**Closes:** REDS %147
**dated_guard:** The sealed `20260719-*` table and the bartis and barket briefs stay exactly as they are. This brief is the new **28th** seat only.

---

## G1 Row -- barlus

| Glyph | Spoken | Family | Meaning | RISC-V / TAME note |
|-------|--------|--------|---------|-------------------|
| `\|+` | barlus | bar | Folding digraph -- the head the bounded triangular fold answers under | `max_sumto_call` 65,535 in `glow/lower_bartis.rye`; a call past the wall folds to 0 by the fold-family law |

The name follows the same two-syllable rule every row above it keeps: `|` is **bar**, `+` is **lus**, so `|+` is **barlus**. A reader who has met one bar row can pronounce this one without being told.

---

## Count note

The closed G1 table seats **25** digraphs, sealed at STOA90, and stays at 25. Bartis added the **26th**; barket added the **27th**; each brief keeps the number it claimed. This brief adds **barlus** as the **28th**, and the living count is now **28**.

---

## What this row repairs, named plainly

`|+` entered `glow/tokens.rye` `match_rune2` on `20260802` with the `sumto` folding gate, and the count **27** stood written in six other places that were never brought along: `tools/fixtures/glow_digraph_table.txt`, `tools/fixtures/glow_digraph_table_wrong_order.txt`, the `rows='...'` cord of `glow/gen/sample-digraph-table.glow`, the `assert(count == 27)` in `glow/digraph_twin_check.rye`, four count asserts in `tools/glow_digraph_twin_witness.rish`, and two in `tools/glow_rune_alphabet_worker.sh`. Every one of them now reads **28**, and the pronunciation table gained the row it had been missing all along.

The teaching is the one the Caravan ladder arc has been proving all month, pointed at a different tree: **a number written in seven places is a number that can disagree with itself.** Here it did, for twenty days, and the witness that would have caught it on the first day was simply never run. A guard earns confidence by being run rather than by being written.

---

## What stays held

`|+` tokenizes and lowers through the `sumto` gate family. The wider fold surface -- composition past the seated trio, and any fold head beyond `sumto` and `sumto_lawful` -- stays where the language bench left it.

---

*May every head the tokenizer learns be given its name in the same lap, and may the count that says how many stand be written once and read everywhere.*
