# Glow Tend Compose — the Next Hardest, Measured

*Equality closed 8/8. Nock rests as the second-backend instrument at one crossing. The Pond census stands. The vane roadmap's next Glow Tend rung is the one the other vanes actually lean on: a compose that lets one gate call another as a value. This brief measures that mountain the way the two-doors brief measured equality — choose nothing that is not already known, lean once, open only what Keaton approved.*

**Stamp:** `20260803.161235`
**Voice:** Riyo
**Style:** Radiant (see `../context/RADIANT_STYLE.md`)
**Lens:** TAME — safety first; Gall grows from a running whole
**Status:** 339 GREEN · 339b GREEN `20260803.163623` — Tend unary sumto + compose-after-inc desk; STOA338a seated; 338b next
**Ground:** vane roadmap `20260802-161500` · bartis `%-` closed set · multi-line compose already green in `glow/` · a1×9 · a2 suite 6/6 · eq 8/8

*Written together by Keaton and Riyo.*

---

## Where the value-model ladder stands

| Rung | What it taught | Status |
|------|----------------|--------|
| a1 | Decide within a wall | **GREEN** — nine gates, five families |
| a2 | Fold a bounded value | **GREEN** — suite 6/6 (sumto · prodto · list sum/prod) |
| equality | Speak *exactly* beside `gth` | **CLOSED 8/8** |
| compose | One gate calls another as a value | **OPEN — this brief** |
| Nock seam (codec rows) | String · list · record pins | **Design** until word + witness |
| Nock instrument | Second backend | **RESTS** — one crossing GREEN |

The other vanes — Mantra Weave, Tally gardens, Caravan caps, Aurora stages, Comlink R1 — already name walls and fold small values. What they cannot yet do in Glow Tend's own bartis body is **compose** those answers: feed one gate's product into another's sample without leaving the closed demo set (`double` · `inc` · `dec` · `flip` · `say`).

## What already runs (do not rebuild)

Multi-line compose in `glow/` is Checkable — face then `%-` call, cores, libraries, jam/cue nests. Thin surface desks already call `inc` and `double`. The gap is not "invent compose from zero." The gap is **Tend-facing bartis composition**: a gate body that may invoke another *Tend-named* unary gate from a closed, witnessed allowlist, so vanes compose law rather than re-carve it.

## Three candidate shapes

| Shape | What it is | Reading |
|-------|-----------|---------|
| **O1 — sequential `%-` widen** | Keep the `%-` rune; widen the closed call-body allowlist from demo verbs to a **named Tend unary set** (surface · decide · fold-as-unary where the lowerer already speaks it), one STOA, plant + witness | Smallest Gall step; reuses the rune vanes already write |
| **O2 — pipe rune** | A new body form that threads two unary gates (`|;  a  b  sample` or kin) | Clearer "compose" spelling; new grammar surface; costs a second rune before the first widen proves |
| **O3 — gates as values** | Treat a gate as a first-class Grain value (record/composite), then apply | True value-model depth; pulls string/record rows and the Nock codec early; **too large** for the first metal lap |

**Lean, offered once under Keaton's approval:** **O1** — STOA338 class, first metal lap = one Tend unary added to the bartis call-body allowlist beside the demos, with a desk that composes it after `inc` or `double`, both sides witnessed, unknown Tend names still `BodyGateNotYetLowered`.

## What stays parked beside this open

- **String · record rows** — naming + witness together; still Design on the seam page.
- **`run_gate` graduation** — five sibling copies on this tip; graduates on a second outside consumer or Keaton's word, not as the hardest.
- **Walls / enclosure climb** — charted e271; opens only on an **explicit** word.
- **R2–R4 · geode · shred · Class O · old zips · bar5 · SEA** — hands and words unchanged.

## Prove commands that must stay green while compose grows

```
rishi/bin/rishi run tools/glow_tend_a1_suite.rish
rishi/bin/rishi run tools/glow_tend_a2_suite.rish
rishi/bin/rishi run tools/glow_tend_era_suite.rish
```

---

*May the next hardest be the one the vanes already wait on. May compose grow from the rune that already calls. May every new gate name refuse loudly until its witness lands.*
