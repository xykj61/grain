# Expanding-Prompts Fascia Survey — What to Molt, Debride, or Leave

**Language:** EN
**Stamp:** `20260810.154352` (2026-08-10 EDT)
**Voice:** Kyri · **Style:** Radiant
**Status:** Mixed -- Survey (a looking pass — names sites and gaps, opens no cut) · seated on Keaton's word
**Kin:** `.claude/rules/molt.md` · `.claude/rules/debride.md` · `.claude/rules/cairn.md` · `ORGANIZING.md`

---

## What was counted

`expanding-prompts/` holds **422** top-level `.md` files, **3.9M**:

| Class | Count | Nature |
|---|---:|---|
| equinox-season recursion-prompts | 146 | per-round ephemera |
| voice-season j4 recursion-prompts | 14 | per-round ephemera |
| sunn recursion-prompts | 12 | per-round ephemera |
| other recursion-prompts | 10 | per-round ephemera |
| charters | 136 | per-round intent (substantive) |
| fusion-batons | 54 | handoffs |
| handoffs | 9 | handoffs |

The **182 recursion-prompts** are the clutter candidates — each a spent prompt for one round, superseded by that round's landed work and its session log. `20260801-011942_equinox-season-e138-recursion-prompt.md` is a representative one: a frozen counsel prompt (Riyo voice, Amber, e137/e138 status), correct as testimony, not a living doc.

## The blocker that shapes the verdict

**174 files outside `expanding-prompts/` reference the recursion-prompts** — mostly session logs and counsel. "References are promises" (`collaboration.md`): moving or deleting these files breaks 174 inbound links unless every one is repointed.

And these are **dated artifacts** — the one-clock law and accrete-never-break protect them; they are testimony, not drift.

## The verdict, by option

- **Molt** — does not apply. A molt needs a living successor; a spent recursion-prompt has none. Nothing to molt here.
- **Debride** — **advised against.** It would destroy dated testimony *and* orphan 174 references. If ever done it needs a circled named scope, a cairn first, and honest acceptance of the reference breakage — a heavy, low-return cut.
- **Archive-fold (accrete-safe)** — move spent recursion-prompts to `expanding-prompts/archive/YYYYMMDD/`, **and repoint all 174 inbound links**. This heals fascia (the living directory drops from 422 toward ~240) while keeping every byte. It is a real, bounded **journey** of its own — not a quick cut — because of the reference repointing.
- **Leave (honest count)** — the files are honest history; `SEASONS.md` already rosters closed seasons, and a future fold tool can batch them by day. Doing nothing costs only a larger directory listing.

## The Comlink-tendency favorite-names check

The favorites are **present and current** throughout: `baton` (228 files), `Brix` (201), `Mantra` (84), `Tally` (73), `Caravan` (70), `Aurora` (67), `Comlink` (58), `Amphora` (47), `Pond` (44), `Skate` (39), `Tablecloth` (31), `Brushstroke` (26), `Kyri` (13). The vocabulary is well-seeded and up to date.

The only "stale" names are **retired** ones inside dated files — Kiln (12), Amber (23), Pool (16), Anvil (5), veganreyklah (23), "Rye OS" (16), "voice Riyo" (7). These are **not drift to fix**: each recorded the name that was live at its stamp. Per accrete-never-break and the one-clock law, dated testimony keeps its words and is never rewritten.

## Recommendation

**Do not mass-cut.** The cheap fascia win isn't available cheaply — the recursion-prompts are testimony cited 174 times. Choose between:

1. **Accept the count** — leave the history whole; let `SEASONS.md` + a future fold tool handle it. Lowest risk.
2. **Run an archive-fold journey** — if the directory size genuinely bothers, seat it as a bounded journey in `journey/`: create `expanding-prompts/archive/`, move the 182 spent recursion-prompts, and repoint all 174 inbound links in the same pass, with a cairn first. Real work, fully reversible, nothing lost.

Debride stays reserved for content you explicitly circle as dead — and these dated prompts are history, not dead tissue.

---

*The clutter is real, yet it is honest, cited history — so the fascia heals best by folding and repointing, never by a cut that breaks 174 promises and burns the testimony that earned them.*

---

## Addendum `20260810.1544` — the four named files, the almanac, and the theme

**The four files you named** are low-reference, unlike the class as a whole:

| File | Inbound refs (outside expanding-prompts) | Verdict |
|---|---:|---|
| `20260731-214426_..e120-recursion-prompt.md` | 0 | archivable cheaply; frozen testimony like e138 |
| `20260802-143610_..e209-a1-breach-fusion-baton.md` | 1 | archivable with one repoint |
| `20260802-011821_..e196-fusion-baton.md` | 1 | archivable with one repoint |
| `20260802-142508_..e207-e208-fusion-baton.md` | 0 | archivable cheaply |

So these four *can* be archived (moved to `expanding-prompts/archive/` / `yonder/`) with almost no repointing. A **mitra-shed (deletion)** of them still wants a circled word and a cairn — but archiving is the accrete-safe alternative and needs neither.

**"Our almanac" is ambiguous** — the tree holds many almanac artifacts (dated charters `e11/e12/e16`, and living witnesses `equinox_*_almanac_witness.rish`). Name *which* almanac is "our almanac" and I will molt-update the living one (dated ones stay frozen).

**The theme, seated forward:** the living arc theme moves from **Ashvini → Shatabhishak** (the 24th nakshatra, the maintainer's own outfit-seat). This governs *new* work from here. The dated Ashvini artifacts (`claude-ashvini-cofusion-handoff`, `equinox-ashvini-nest-align-baton`, `ashvini-recursive-prompt-v2`) **keep their names as testimony** — one-clock law, never rewritten. Replacing the theme is a forward choice; it is not a rename of the dated past.

## The one decision to stop circling

We have now surveyed this dated-history question three times, and the answer is consistent: **dated recursion-prompts, batons, and handoffs are cited testimony, protected by accrete-never-break; they are not rewritten or blindly shed.** Rather than keep going file-by-file, one bounded choice settles it:

- **Option A — accept the count.** Leave the history whole; `SEASONS.md` rosters closed seasons; a future fold tool batches by day. Zero risk.
- **Option B — one archive-fold journey.** Seat a single bounded journey in `journey/`: create `expanding-prompts/archive/`, move *all* spent recursion-prompts and closed-arc batons/handoffs, and repoint every inbound link in the same pass, cairn first. Real work, fully reversible, nothing lost — and it ends the file-by-file requests.

Debride/mitra-shed of this dated testimony stays advised-against; it destroys history and orphans references for no gain that archiving does not already give.
