# The Shape-Reading Seam, Measured — Two Doors for One-Source Decides

*The gatekeepers opened the decide family on the two bounds the lowerer already owns; the vane-wall decides waited on this seam. The measurement reframed it before a door was drawn: the pedestals are **shapes** — `src/lib/shape-tally-max-gardens.glow` carries `$: gardens=@u32 ==` and its eight lives only in prose — while the pedestal's own comment points home: *"Names `max_gardens` from `tally/gardens.rye`."* The constants' single source is the vane module itself (`tally/gardens.rye` — `pub const max_gardens: u32 = 8`). So the seam is not shape-parsing at all; it is how a lowered plant reaches a vane constant without a second home. Three doors, one lean, nothing opened.*

**Stamp:** `20260804.004127` · **Voice:** Riyo · **Status:** Vision -- CHARTED — first metal next under the grant

*Written together by Keaton and Riyo.*

## Three doors

| Door | Shape | Reading |
|------|-------|---------|
| **O1 — registry parse** | A decide registry maps gate → file · constant; the lowerer greps the line at lower-time | Works everywhere; yet the lowerer becomes a second parser of Rye text, and a rename breaks silently at run rather than at build |
| **O2 — generated module** | A build step extracts vane constants into `.cache/pedestal_constants.rye` that plants import | One generated home; yet a generator is a second home wearing a build step's clothes |
| **O3 — the plant imports the vane** | The emitted plant carries `const gardens = @import("../../tally/gardens.rye");` and answers `if (face <= gardens.max_gardens) 1 else 0`; the fixture's expect is emitted as the **same expression in-plant**, so the lowerer never holds the value | **The import graph is the registry.** One source, compiler-enforced; a rename fails loudly at build; the lowerer gains zero vane coupling — `expect_for` returns an unused placeholder for this class, commented so |

## The lean, once

**O3.** The compiler already guards one-source truth better than any registry could; the only new machinery is an import line and an in-plant expect, both shapes the pair family already proved. First metal: **`gardens_lawful`** — Tally's garden wall as a callable, reading `max_gardens` from the one place it lives — then Caravan's kin by the same door.

## Held

The lowerer stays vane-free; plants may import vanes read-only; no constant is ever re-typed. Pedestal shapes keep their prose examples as teaching, never as source. Living word is **shape** — study `-mold` allowlist names stay historical in the lowerer; new prose never seats *mold* again.

---

*May the import graph be the only registry. May renames fail loudly and immediately. May every eight in prose bow to the one eight in code.*
